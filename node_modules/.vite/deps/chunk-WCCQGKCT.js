import {
  CloseableScopeTypeId,
  DifferTypeId,
  EffectTypeId as EffectTypeId2,
  GenericTag,
  IllegalArgumentException,
  Interruption,
  NoSuchElementException,
  OpSupervision,
  Reference,
  RevertFlags,
  ScopeTypeId,
  UnknownException,
  WindDown,
  add,
  allLogLevels,
  allocate,
  append,
  append2,
  appendAll,
  as2 as as,
  asVoid,
  async_,
  blocked,
  catchAll,
  catchAllCause,
  catchIf,
  chunk,
  combine as combine2,
  compareAndSet,
  cons,
  context,
  contextWithEffect,
  cooperativeYielding,
  currentConcurrency,
  currentContext,
  currentForkScopeOverride,
  currentInterruptedCause,
  currentLogAnnotations,
  currentLogLevel,
  currentLogSpan,
  currentMaxOpsBeforeYield,
  currentMetricLabels,
  currentRequestBatching,
  currentSchedulingPriority,
  currentTracerEnabled,
  currentTracerSpanAnnotations,
  currentTracerSpanLinks,
  currentTracerTimingEnabled,
  currentUnhandledErrorLogLevel,
  currentVersionMismatchErrorLogLevel,
  dedupe,
  defects,
  deferredAwait,
  deferredMake,
  deferredUnsafeDone,
  deferredUnsafeMake,
  die,
  die2,
  dieMessage,
  dieSync,
  diff,
  difference,
  disable2 as disable,
  either,
  empty10 as empty8,
  empty2 as empty,
  empty3 as empty2,
  empty4 as empty3,
  empty5 as empty4,
  empty6 as empty5,
  empty7 as empty6,
  empty8 as empty7,
  enable2 as enable,
  ensure,
  environment,
  exclude,
  exit,
  exitAsVoid,
  exitCollectAll,
  exitFail,
  exitFailCause,
  exitInterrupt,
  exitIsExit,
  exitIsFailure,
  exitIsSuccess,
  exitMatch,
  exitMatchEffect,
  exitSucceed,
  exitVoid,
  exitZipWith,
  fail2 as fail,
  fail3 as fail2,
  failCause,
  failCause2,
  failCauseSync,
  failSync,
  failureOrCause,
  failures,
  fiberId,
  fiberIdWith,
  fiberRefDelete,
  fiberRefGet,
  fiberRefGetWith,
  fiberRefLocally,
  fiberRefLocallyWith,
  fiberRefSet,
  fiberRefUnsafeMake,
  fiberRefUnsafeMakeContext,
  fiberRefUnsafeMakeHashSet,
  fiberRefUnsafeMakePatch,
  fiberRefUnsafeMakeRuntimeFlags,
  fiberRefUpdate,
  filterMap,
  find,
  flatMap as flatMap2,
  flatMap4 as flatMap3,
  flatten,
  flatten3 as flatten2,
  flatten4 as flatten3,
  flatten5 as flatten4,
  flip,
  fnUntraced,
  forEach2 as forEach,
  forEachEffect,
  forEachSequential,
  forEachSequentialDiscard,
  fromIterable,
  fromIterable2,
  fromIterable3,
  fromIterable4,
  get,
  get2,
  getEquivalence,
  getOption,
  getOrElse as getOrElse2,
  getSomes,
  hashMap,
  hashSet,
  head2 as head,
  headNonEmpty,
  headNonEmpty2,
  ids,
  if_,
  interrupt,
  interrupt2,
  interrupt3,
  interruptAsFiber,
  interruptFiber,
  interruptible,
  interruptible2,
  interruptors,
  intoDeferred,
  isArray,
  isCause,
  isCons,
  isDieType,
  isEffect,
  isEmpty3 as isEmpty,
  isEmptyReadonlyArray,
  isEmptyType,
  isInterrupted,
  isInterruptedException,
  isInterruptedOnly,
  isNoSuchElementException,
  isNonEmpty,
  isNonEmptyArray2 as isNonEmptyArray,
  isNonEmptyReadonlyArray,
  isSuccess,
  join,
  keepDefects,
  last,
  lastNonEmpty,
  logLevelAll,
  logLevelDebug,
  logLevelError,
  logLevelFatal,
  logLevelInfo,
  logLevelNone,
  logLevelTrace,
  logLevelWarning,
  make10 as make6,
  make2 as make,
  make4 as make2,
  make5 as make3,
  make6 as make4,
  make7 as make5,
  makeBy,
  map2,
  map5 as map3,
  mapBoth,
  mapError,
  mapInputContext,
  match as match2,
  match3,
  matchCause,
  matchCauseEffect,
  matchEffect,
  merge as merge2,
  mutate,
  never,
  none2 as none3,
  none3 as none4,
  noopSpan,
  of,
  of2,
  of3,
  onError,
  onExit,
  onInterrupt,
  orElse,
  orElseEither,
  par,
  parallel,
  partitionMap2 as partitionMap,
  patch,
  prepend,
  prepend2,
  prepend3,
  pretty,
  provideContext,
  readonlyArray,
  reduce,
  reduce2,
  reduce4 as reduce3,
  reduceRight,
  runRequestBlock,
  runtimeFlags,
  runtimeMetrics,
  scopeAddFinalizer,
  scopeAddFinalizerExit,
  scopeClose,
  scopeFork,
  sequential,
  sequentialCollectionToChunk,
  set,
  set2,
  size,
  size2,
  sort,
  span,
  spanToTrace,
  step,
  stripFailures,
  succeed,
  succeed2,
  suspend,
  sync,
  tailNonEmpty,
  tailNonEmpty2,
  tap,
  threadName,
  threadName2,
  toReadonlyArray,
  transform,
  transplant,
  unfold,
  uninterruptible,
  uninterruptibleMask,
  union,
  union2,
  unsafeAsync,
  unsafeFromArray,
  unsafeGet,
  unsafeGet3 as unsafeGet2,
  unsafeGetReference,
  unsafeMake2 as unsafeMake,
  update2 as update,
  updateRuntimeFlags,
  updateWith,
  void_,
  whenEffect,
  whileLoop,
  withFiberRuntime,
  withRuntimeFlags,
  yieldNow,
  zip,
  zip2,
  zip3,
  zipLeft,
  zipRight,
  zipWith2
} from "./chunk-ZLEQSUEA.js";
import {
  isLeft,
  isRight,
  left,
  merge,
  right
} from "./chunk-TVBZ6A6Y.js";
import {
  decode,
  greaterThanOrEqualTo as greaterThanOrEqualTo2,
  lessThanOrEqualTo as lessThanOrEqualTo2,
  millis,
  nanos,
  toMillis,
  zero
} from "./chunk-AQW37YSD.js";
import {
  Base,
  ChannelTypeId,
  CommitPrototype,
  EffectPrototype,
  EffectTypeId,
  NodeInspectSymbol,
  OP_ASYNC,
  OP_COMMIT,
  OP_FAILURE,
  OP_ITERATOR,
  OP_ON_FAILURE,
  OP_ON_SUCCESS,
  OP_ON_SUCCESS_AND_FAILURE,
  OP_REVERT_FLAGS,
  OP_SUCCESS,
  OP_SYNC,
  OP_TAG,
  OP_UPDATE_RUNTIME_FLAGS,
  OP_WHILE,
  OP_WITH_RUNTIME,
  OP_YIELD,
  PCGRandom,
  SingleShotGen,
  SinkTypeId,
  StreamTypeId,
  StructuralBase,
  StructuralCommitPrototype,
  StructuralPrototype,
  YieldWrap,
  all,
  array2 as array,
  between,
  bind,
  bindTo,
  boolean,
  boolean2,
  cached,
  clamp,
  combine,
  constFalse,
  constTrue,
  constVoid,
  contains,
  dual,
  equals,
  flatMap,
  format,
  fromNullable,
  getCurrentVersion,
  getOrElse,
  getOrUndefined,
  globalValue,
  greaterThan,
  greaterThanOrEqualTo,
  hasProperty,
  hash,
  identity,
  internalCall,
  isBoolean,
  isEqual,
  isFunction,
  isIterable,
  isNone2 as isNone,
  isNotNullable,
  isNumber,
  isRegExp,
  isSome,
  isString,
  isTagged,
  lessThan,
  lessThanOrEqualTo,
  let_,
  map,
  mapInput2 as mapInput,
  match,
  max,
  min,
  none,
  none2,
  number,
  number3 as number2,
  pipe,
  pipeArguments,
  redact,
  some,
  some2,
  string2 as string,
  stringifyCircular,
  symbol2 as symbol,
  symbol3 as symbol2,
  toJSON,
  toStringUnknown,
  tuple2 as tuple,
  withRedactableContext,
  yieldWrapGet,
  zipWith
} from "./chunk-H62K6C3Q.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Effectable.js
var Effectable_exports = {};
__export(Effectable_exports, {
  ChannelTypeId: () => ChannelTypeId2,
  Class: () => Class,
  CommitPrototype: () => CommitPrototype2,
  EffectPrototype: () => EffectPrototype2,
  EffectTypeId: () => EffectTypeId3,
  SinkTypeId: () => SinkTypeId2,
  StreamTypeId: () => StreamTypeId2,
  StructuralClass: () => StructuralClass,
  StructuralCommitPrototype: () => StructuralCommitPrototype2
});
var EffectTypeId3 = EffectTypeId;
var StreamTypeId2 = StreamTypeId;
var SinkTypeId2 = SinkTypeId;
var ChannelTypeId2 = ChannelTypeId;
var EffectPrototype2 = EffectPrototype;
var CommitPrototype2 = CommitPrototype;
var StructuralCommitPrototype2 = StructuralCommitPrototype;
var Base2 = Base;
var StructuralBase2 = StructuralBase;
var Class = class extends Base2 {
};
var StructuralClass = class extends StructuralBase2 {
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Boolean.js
var Boolean_exports = {};
__export(Boolean_exports, {
  Equivalence: () => Equivalence,
  Order: () => Order,
  and: () => and,
  eqv: () => eqv,
  every: () => every,
  implies: () => implies,
  isBoolean: () => isBoolean2,
  match: () => match4,
  nand: () => nand,
  nor: () => nor,
  not: () => not,
  or: () => or,
  some: () => some3,
  xor: () => xor
});
var isBoolean2 = isBoolean;
var match4 = dual(2, (value2, options) => value2 ? options.onTrue() : options.onFalse());
var Equivalence = boolean;
var Order = boolean2;
var not = (self) => !self;
var and = dual(2, (self, that) => self && that);
var nand = dual(2, (self, that) => !(self && that));
var or = dual(2, (self, that) => self || that);
var nor = dual(2, (self, that) => !(self || that));
var xor = dual(2, (self, that) => !self && that || self && !that);
var eqv = dual(2, (self, that) => !xor(self, that));
var implies = dual(2, (self, that) => self ? that : true);
var every = (collection) => {
  for (const b of collection) {
    if (!b) {
      return false;
    }
  }
  return true;
};
var some3 = (collection) => {
  for (const b of collection) {
    if (b) {
      return true;
    }
  }
  return false;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ExecutionStrategy.js
var ExecutionStrategy_exports = {};
__export(ExecutionStrategy_exports, {
  isParallel: () => isParallel2,
  isParallelN: () => isParallelN2,
  isSequential: () => isSequential2,
  match: () => match6,
  parallel: () => parallel3,
  parallelN: () => parallelN2,
  sequential: () => sequential3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/executionStrategy.js
var OP_SEQUENTIAL = "Sequential";
var OP_PARALLEL = "Parallel";
var OP_PARALLEL_N = "ParallelN";
var sequential2 = {
  _tag: OP_SEQUENTIAL
};
var parallel2 = {
  _tag: OP_PARALLEL
};
var parallelN = (parallelism) => ({
  _tag: OP_PARALLEL_N,
  parallelism
});
var isSequential = (self) => self._tag === OP_SEQUENTIAL;
var isParallel = (self) => self._tag === OP_PARALLEL;
var isParallelN = (self) => self._tag === OP_PARALLEL_N;
var match5 = dual(2, (self, options) => {
  switch (self._tag) {
    case OP_SEQUENTIAL: {
      return options.onSequential();
    }
    case OP_PARALLEL: {
      return options.onParallel();
    }
    case OP_PARALLEL_N: {
      return options.onParallelN(self.parallelism);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ExecutionStrategy.js
var sequential3 = sequential2;
var parallel3 = parallel2;
var parallelN2 = parallelN;
var isSequential2 = isSequential;
var isParallel2 = isParallel;
var isParallelN2 = isParallelN;
var match6 = match5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefs.js
var FiberRefs_exports = {};
__export(FiberRefs_exports, {
  FiberRefsSym: () => FiberRefsSym2,
  delete: () => delete_2,
  empty: () => empty10,
  fiberRefs: () => fiberRefs2,
  forkAs: () => forkAs2,
  get: () => get4,
  getOrDefault: () => getOrDefault2,
  joinAs: () => joinAs2,
  setAll: () => setAll2,
  unsafeMake: () => unsafeMake3,
  updateAs: () => updateAs2,
  updateManyAs: () => updateManyAs2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberRefs.js
function unsafeMake2(fiberRefLocals) {
  return new FiberRefsImpl(fiberRefLocals);
}
function empty9() {
  return unsafeMake2(/* @__PURE__ */ new Map());
}
var FiberRefsSym = /* @__PURE__ */ Symbol.for("effect/FiberRefs");
var FiberRefsImpl = class {
  locals;
  [FiberRefsSym] = FiberRefsSym;
  constructor(locals) {
    this.locals = locals;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var findAncestor = (_ref, _parentStack, _childStack, _childModified = false) => {
  const ref = _ref;
  let parentStack = _parentStack;
  let childStack = _childStack;
  let childModified = _childModified;
  let ret = void 0;
  while (ret === void 0) {
    if (isNonEmptyReadonlyArray(parentStack) && isNonEmptyReadonlyArray(childStack)) {
      const parentFiberId = headNonEmpty(parentStack)[0];
      const parentAncestors = tailNonEmpty(parentStack);
      const childFiberId = headNonEmpty(childStack)[0];
      const childRefValue = headNonEmpty(childStack)[1];
      const childAncestors = tailNonEmpty(childStack);
      if (parentFiberId.startTimeMillis < childFiberId.startTimeMillis) {
        childStack = childAncestors;
        childModified = true;
      } else if (parentFiberId.startTimeMillis > childFiberId.startTimeMillis) {
        parentStack = parentAncestors;
      } else {
        if (parentFiberId.id < childFiberId.id) {
          childStack = childAncestors;
          childModified = true;
        } else if (parentFiberId.id > childFiberId.id) {
          parentStack = parentAncestors;
        } else {
          ret = [childRefValue, childModified];
        }
      }
    } else {
      ret = [ref.initial, true];
    }
  }
  return ret;
};
var joinAs = dual(3, (self, fiberId2, that) => {
  const parentFiberRefs = new Map(self.locals);
  that.locals.forEach((childStack, fiberRef) => {
    const childValue = childStack[0][1];
    if (!childStack[0][0][symbol2](fiberId2)) {
      if (!parentFiberRefs.has(fiberRef)) {
        if (equals(childValue, fiberRef.initial)) {
          return;
        }
        parentFiberRefs.set(fiberRef, [[fiberId2, fiberRef.join(fiberRef.initial, childValue)]]);
        return;
      }
      const parentStack = parentFiberRefs.get(fiberRef);
      const [ancestor, wasModified] = findAncestor(fiberRef, parentStack, childStack);
      if (wasModified) {
        const patch7 = fiberRef.diff(ancestor, childValue);
        const oldValue = parentStack[0][1];
        const newValue = fiberRef.join(oldValue, fiberRef.patch(patch7)(oldValue));
        if (!equals(oldValue, newValue)) {
          let newStack;
          const parentFiberId = parentStack[0][0];
          if (parentFiberId[symbol2](fiberId2)) {
            newStack = [[parentFiberId, newValue], ...parentStack.slice(1)];
          } else {
            newStack = [[fiberId2, newValue], ...parentStack];
          }
          parentFiberRefs.set(fiberRef, newStack);
        }
      }
    }
  });
  return new FiberRefsImpl(parentFiberRefs);
});
var forkAs = dual(2, (self, childId) => {
  const map10 = /* @__PURE__ */ new Map();
  unsafeForkAs(self, map10, childId);
  return new FiberRefsImpl(map10);
});
var unsafeForkAs = (self, map10, fiberId2) => {
  self.locals.forEach((stack, fiberRef) => {
    const oldValue = stack[0][1];
    const newValue = fiberRef.patch(fiberRef.fork)(oldValue);
    if (equals(oldValue, newValue)) {
      map10.set(fiberRef, stack);
    } else {
      map10.set(fiberRef, [[fiberId2, newValue], ...stack]);
    }
  });
};
var fiberRefs = (self) => fromIterable3(self.locals.keys());
var setAll = (self) => forEachSequentialDiscard(fiberRefs(self), (fiberRef) => fiberRefSet(fiberRef, getOrDefault(self, fiberRef)));
var delete_ = dual(2, (self, fiberRef) => {
  const locals = new Map(self.locals);
  locals.delete(fiberRef);
  return new FiberRefsImpl(locals);
});
var get3 = dual(2, (self, fiberRef) => {
  if (!self.locals.has(fiberRef)) {
    return none2();
  }
  return some2(headNonEmpty(self.locals.get(fiberRef))[1]);
});
var getOrDefault = dual(2, (self, fiberRef) => pipe(get3(self, fiberRef), getOrElse(() => fiberRef.initial)));
var updateAs = dual(2, (self, {
  fiberId: fiberId2,
  fiberRef,
  value: value2
}) => {
  if (self.locals.size === 0) {
    return new FiberRefsImpl(/* @__PURE__ */ new Map([[fiberRef, [[fiberId2, value2]]]]));
  }
  const locals = new Map(self.locals);
  unsafeUpdateAs(locals, fiberId2, fiberRef, value2);
  return new FiberRefsImpl(locals);
});
var unsafeUpdateAs = (locals, fiberId2, fiberRef, value2) => {
  const oldStack = locals.get(fiberRef) ?? [];
  let newStack;
  if (isNonEmptyReadonlyArray(oldStack)) {
    const [currentId, currentValue] = headNonEmpty(oldStack);
    if (currentId[symbol2](fiberId2)) {
      if (equals(currentValue, value2)) {
        return;
      } else {
        newStack = [[fiberId2, value2], ...oldStack.slice(1)];
      }
    } else {
      newStack = [[fiberId2, value2], ...oldStack];
    }
  } else {
    newStack = [[fiberId2, value2]];
  }
  locals.set(fiberRef, newStack);
};
var updateManyAs = dual(2, (self, {
  entries,
  forkAs: forkAs3
}) => {
  if (self.locals.size === 0) {
    return new FiberRefsImpl(new Map(entries));
  }
  const locals = new Map(self.locals);
  if (forkAs3 !== void 0) {
    unsafeForkAs(self, locals, forkAs3);
  }
  entries.forEach(([fiberRef, values5]) => {
    if (values5.length === 1) {
      unsafeUpdateAs(locals, values5[0][0], fiberRef, values5[0][1]);
    } else {
      values5.forEach(([fiberId2, value2]) => {
        unsafeUpdateAs(locals, fiberId2, fiberRef, value2);
      });
    }
  });
  return new FiberRefsImpl(locals);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefs.js
var FiberRefsSym2 = FiberRefsSym;
var delete_2 = delete_;
var fiberRefs2 = fiberRefs;
var forkAs2 = forkAs;
var get4 = get3;
var getOrDefault2 = getOrDefault;
var joinAs2 = joinAs;
var setAll2 = setAll;
var updateAs2 = updateAs;
var updateManyAs2 = updateManyAs;
var unsafeMake3 = unsafeMake2;
var empty10 = empty9;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefsPatch.js
var FiberRefsPatch_exports = {};
__export(FiberRefsPatch_exports, {
  combine: () => combine4,
  diff: () => diff3,
  empty: () => empty12,
  patch: () => patch3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberRefs/patch.js
var OP_EMPTY = "Empty";
var OP_ADD = "Add";
var OP_REMOVE = "Remove";
var OP_UPDATE = "Update";
var OP_AND_THEN = "AndThen";
var empty11 = {
  _tag: OP_EMPTY
};
var diff2 = (oldValue, newValue) => {
  const missingLocals = new Map(oldValue.locals);
  let patch7 = empty11;
  for (const [fiberRef, pairs] of newValue.locals.entries()) {
    const newValue2 = headNonEmpty(pairs)[1];
    const old = missingLocals.get(fiberRef);
    if (old !== void 0) {
      const oldValue2 = headNonEmpty(old)[1];
      if (!equals(oldValue2, newValue2)) {
        patch7 = combine3({
          _tag: OP_UPDATE,
          fiberRef,
          patch: fiberRef.diff(oldValue2, newValue2)
        })(patch7);
      }
    } else {
      patch7 = combine3({
        _tag: OP_ADD,
        fiberRef,
        value: newValue2
      })(patch7);
    }
    missingLocals.delete(fiberRef);
  }
  for (const [fiberRef] of missingLocals.entries()) {
    patch7 = combine3({
      _tag: OP_REMOVE,
      fiberRef
    })(patch7);
  }
  return patch7;
};
var combine3 = dual(2, (self, that) => ({
  _tag: OP_AND_THEN,
  first: self,
  second: that
}));
var patch2 = dual(3, (self, fiberId2, oldValue) => {
  let fiberRefs4 = oldValue;
  let patches = of(self);
  while (isNonEmptyReadonlyArray(patches)) {
    const head3 = headNonEmpty(patches);
    const tail = tailNonEmpty(patches);
    switch (head3._tag) {
      case OP_EMPTY: {
        patches = tail;
        break;
      }
      case OP_ADD: {
        fiberRefs4 = updateAs(fiberRefs4, {
          fiberId: fiberId2,
          fiberRef: head3.fiberRef,
          value: head3.value
        });
        patches = tail;
        break;
      }
      case OP_REMOVE: {
        fiberRefs4 = delete_(fiberRefs4, head3.fiberRef);
        patches = tail;
        break;
      }
      case OP_UPDATE: {
        const value2 = getOrDefault(fiberRefs4, head3.fiberRef);
        fiberRefs4 = updateAs(fiberRefs4, {
          fiberId: fiberId2,
          fiberRef: head3.fiberRef,
          value: head3.fiberRef.patch(head3.patch)(value2)
        });
        patches = tail;
        break;
      }
      case OP_AND_THEN: {
        patches = prepend(head3.first)(prepend(head3.second)(tail));
        break;
      }
    }
  }
  return fiberRefs4;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefsPatch.js
var empty12 = empty11;
var diff3 = diff2;
var combine4 = combine3;
var patch3 = patch2;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberStatus.js
var FiberStatus_exports = {};
__export(FiberStatus_exports, {
  FiberStatusTypeId: () => FiberStatusTypeId2,
  done: () => done2,
  isDone: () => isDone2,
  isFiberStatus: () => isFiberStatus2,
  isRunning: () => isRunning2,
  isSuspended: () => isSuspended2,
  running: () => running2,
  suspended: () => suspended2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberStatus.js
var FiberStatusSymbolKey = "effect/FiberStatus";
var FiberStatusTypeId = Symbol.for(FiberStatusSymbolKey);
var OP_DONE = "Done";
var OP_RUNNING = "Running";
var OP_SUSPENDED = "Suspended";
var DoneHash = string(`${FiberStatusSymbolKey}-${OP_DONE}`);
var Done = class {
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_DONE;
  [symbol]() {
    return DoneHash;
  }
  [symbol2](that) {
    return isFiberStatus(that) && that._tag === OP_DONE;
  }
};
var Running = class {
  runtimeFlags;
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_RUNNING;
  constructor(runtimeFlags2) {
    this.runtimeFlags = runtimeFlags2;
  }
  [symbol]() {
    return pipe(hash(FiberStatusSymbolKey), combine(hash(this._tag)), combine(hash(this.runtimeFlags)), cached(this));
  }
  [symbol2](that) {
    return isFiberStatus(that) && that._tag === OP_RUNNING && this.runtimeFlags === that.runtimeFlags;
  }
};
var Suspended = class {
  runtimeFlags;
  blockingOn;
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_SUSPENDED;
  constructor(runtimeFlags2, blockingOn) {
    this.runtimeFlags = runtimeFlags2;
    this.blockingOn = blockingOn;
  }
  [symbol]() {
    return pipe(hash(FiberStatusSymbolKey), combine(hash(this._tag)), combine(hash(this.runtimeFlags)), combine(hash(this.blockingOn)), cached(this));
  }
  [symbol2](that) {
    return isFiberStatus(that) && that._tag === OP_SUSPENDED && this.runtimeFlags === that.runtimeFlags && equals(this.blockingOn, that.blockingOn);
  }
};
var done = new Done();
var running = (runtimeFlags2) => new Running(runtimeFlags2);
var suspended = (runtimeFlags2, blockingOn) => new Suspended(runtimeFlags2, blockingOn);
var isFiberStatus = (u) => hasProperty(u, FiberStatusTypeId);
var isDone = (self) => self._tag === OP_DONE;
var isRunning = (self) => self._tag === OP_RUNNING;
var isSuspended = (self) => self._tag === OP_SUSPENDED;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberStatus.js
var FiberStatusTypeId2 = FiberStatusTypeId;
var done2 = done;
var running2 = running;
var suspended2 = suspended;
var isFiberStatus2 = isFiberStatus;
var isDone2 = isDone;
var isRunning2 = isRunning;
var isSuspended2 = isSuspended;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Number.js
var Number_exports = {};
__export(Number_exports, {
  Equivalence: () => Equivalence2,
  Order: () => Order2,
  between: () => between2,
  clamp: () => clamp2,
  decrement: () => decrement,
  divide: () => divide,
  greaterThan: () => greaterThan2,
  greaterThanOrEqualTo: () => greaterThanOrEqualTo3,
  increment: () => increment,
  isNumber: () => isNumber2,
  lessThan: () => lessThan2,
  lessThanOrEqualTo: () => lessThanOrEqualTo3,
  max: () => max2,
  min: () => min2,
  multiply: () => multiply,
  multiplyAll: () => multiplyAll,
  negate: () => negate,
  nextPow2: () => nextPow2,
  parse: () => parse,
  remainder: () => remainder,
  round: () => round,
  sign: () => sign,
  subtract: () => subtract,
  sum: () => sum,
  sumAll: () => sumAll,
  unsafeDivide: () => unsafeDivide
});
var isNumber2 = isNumber;
var negate = (n) => multiply(n, -1);
var sum = dual(2, (self, that) => self + that);
var sumAll = (collection) => reduce(collection, 0, sum);
var subtract = dual(2, (minuend, subtrahend) => minuend - subtrahend);
var multiply = dual(2, (multiplier, multiplicand) => multiplier * multiplicand);
var multiplyAll = (collection) => {
  let out = 1;
  for (const n of collection) {
    if (n === 0) {
      return 0;
    }
    out *= n;
  }
  return out;
};
var divide = dual(2, (dividend, divisor) => divisor === 0 ? none : some(dividend / divisor));
var unsafeDivide = dual(2, (dividend, divisor) => dividend / divisor);
var increment = (n) => sum(n, 1);
var decrement = (n) => subtract(n, 1);
var Equivalence2 = number;
var Order2 = number2;
var lessThan2 = lessThan(Order2);
var lessThanOrEqualTo3 = lessThanOrEqualTo(Order2);
var greaterThan2 = greaterThan(Order2);
var greaterThanOrEqualTo3 = greaterThanOrEqualTo(Order2);
var between2 = between(Order2);
var clamp2 = clamp(Order2);
var min2 = min(Order2);
var max2 = max(Order2);
var sign = (n) => Order2(n, 0);
var remainder = dual(2, (dividend, divisor) => {
  const selfDecCount = (dividend.toString().split(".")[1] || "").length;
  const divisorDecCount = (divisor.toString().split(".")[1] || "").length;
  const decCount = selfDecCount > divisorDecCount ? selfDecCount : divisorDecCount;
  const selfInt = parseInt(dividend.toFixed(decCount).replace(".", ""));
  const divisorInt = parseInt(divisor.toFixed(decCount).replace(".", ""));
  return selfInt % divisorInt / Math.pow(10, decCount);
});
var nextPow2 = (n) => {
  const nextPow = Math.ceil(Math.log(n) / Math.log(2));
  return Math.max(Math.pow(2, nextPow), 2);
};
var parse = (s) => {
  if (s === "NaN") {
    return some(NaN);
  }
  if (s === "Infinity") {
    return some(Infinity);
  }
  if (s === "-Infinity") {
    return some(-Infinity);
  }
  if (s.trim() === "") {
    return none;
  }
  const n = Number(s);
  return Number.isNaN(n) ? none : some(n);
};
var round = dual(2, (self, precision) => {
  const factor = Math.pow(10, precision);
  return Math.round(self * factor) / factor;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LogLevel.js
var LogLevel_exports = {};
__export(LogLevel_exports, {
  All: () => All,
  Debug: () => Debug,
  Error: () => Error2,
  Fatal: () => Fatal,
  Info: () => Info,
  None: () => None,
  Order: () => Order3,
  Trace: () => Trace,
  Warning: () => Warning,
  allLevels: () => allLevels,
  fromLiteral: () => fromLiteral,
  greaterThan: () => greaterThan3,
  greaterThanEqual: () => greaterThanEqual,
  lessThan: () => lessThan3,
  lessThanEqual: () => lessThanEqual,
  locally: () => locally
});
var All = logLevelAll;
var Fatal = logLevelFatal;
var Error2 = logLevelError;
var Warning = logLevelWarning;
var Info = logLevelInfo;
var Debug = logLevelDebug;
var Trace = logLevelTrace;
var None = logLevelNone;
var allLevels = allLogLevels;
var locally = dual(2, (use, self) => fiberRefLocally(use, currentLogLevel, self));
var Order3 = pipe(Order2, mapInput((level) => level.ordinal));
var lessThan3 = lessThan(Order3);
var lessThanEqual = lessThanOrEqualTo(Order3);
var greaterThan3 = greaterThan(Order3);
var greaterThanEqual = greaterThanOrEqualTo(Order3);
var fromLiteral = (literal) => {
  switch (literal) {
    case "All":
      return All;
    case "Debug":
      return Debug;
    case "Error":
      return Error2;
    case "Fatal":
      return Fatal;
    case "Info":
      return Info;
    case "Trace":
      return Trace;
    case "None":
      return None;
    case "Warning":
      return Warning;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Micro.js
var Micro_exports = {};
__export(Micro_exports, {
  CurrentConcurrency: () => CurrentConcurrency,
  CurrentScheduler: () => CurrentScheduler,
  Do: () => Do,
  Error: () => Error3,
  MaxOpsBeforeYield: () => MaxOpsBeforeYield,
  MicroCauseTypeId: () => MicroCauseTypeId,
  MicroExitTypeId: () => MicroExitTypeId,
  MicroFiberTypeId: () => MicroFiberTypeId,
  MicroSchedulerDefault: () => MicroSchedulerDefault,
  MicroScope: () => MicroScope,
  MicroScopeTypeId: () => MicroScopeTypeId,
  NoSuchElementException: () => NoSuchElementException2,
  TaggedError: () => TaggedError,
  TimeoutException: () => TimeoutException,
  TypeId: () => TypeId,
  acquireRelease: () => acquireRelease,
  acquireUseRelease: () => acquireUseRelease,
  addFinalizer: () => addFinalizer,
  all: () => all2,
  andThen: () => andThen,
  as: () => as2,
  asSome: () => asSome,
  asVoid: () => asVoid2,
  async: () => async,
  bind: () => bind2,
  bindTo: () => bindTo2,
  catchAll: () => catchAll2,
  catchAllCause: () => catchAllCause2,
  catchAllDefect: () => catchAllDefect,
  catchCauseIf: () => catchCauseIf,
  catchIf: () => catchIf2,
  catchTag: () => catchTag,
  causeDie: () => causeDie,
  causeFail: () => causeFail,
  causeInterrupt: () => causeInterrupt,
  causeIsDie: () => causeIsDie,
  causeIsFail: () => causeIsFail,
  causeIsInterrupt: () => causeIsInterrupt,
  causeSquash: () => causeSquash,
  causeWithTrace: () => causeWithTrace,
  context: () => context2,
  delay: () => delay,
  die: () => die3,
  either: () => either2,
  ensuring: () => ensuring,
  exit: () => exit2,
  exitDie: () => exitDie,
  exitFail: () => exitFail2,
  exitFailCause: () => exitFailCause2,
  exitInterrupt: () => exitInterrupt2,
  exitIsDie: () => exitIsDie,
  exitIsFail: () => exitIsFail,
  exitIsFailure: () => exitIsFailure2,
  exitIsInterrupt: () => exitIsInterrupt,
  exitIsSuccess: () => exitIsSuccess2,
  exitSucceed: () => exitSucceed2,
  exitVoid: () => exitVoid2,
  exitVoidAll: () => exitVoidAll,
  fail: () => fail3,
  failCause: () => failCause3,
  failCauseSync: () => failCauseSync2,
  failSync: () => failSync2,
  fiberAwait: () => fiberAwait,
  fiberInterrupt: () => fiberInterrupt,
  fiberInterruptAll: () => fiberInterruptAll,
  fiberJoin: () => fiberJoin,
  filter: () => filter,
  filterMap: () => filterMap2,
  filterOrFail: () => filterOrFail,
  filterOrFailCause: () => filterOrFailCause,
  flatMap: () => flatMap4,
  flatten: () => flatten5,
  flip: () => flip2,
  forEach: () => forEach2,
  forever: () => forever,
  fork: () => fork,
  forkDaemon: () => forkDaemon,
  forkIn: () => forkIn,
  forkScoped: () => forkScoped,
  fromEither: () => fromEither,
  fromOption: () => fromOption,
  gen: () => gen,
  ignore: () => ignore,
  ignoreLogged: () => ignoreLogged,
  interrupt: () => interrupt4,
  interruptible: () => interruptible3,
  isMicro: () => isMicro,
  isMicroCause: () => isMicroCause,
  isMicroExit: () => isMicroExit,
  let: () => let_2,
  map: () => map4,
  mapError: () => mapError2,
  mapErrorCause: () => mapErrorCause,
  match: () => match7,
  matchCause: () => matchCause2,
  matchCauseEffect: () => matchCauseEffect2,
  matchEffect: () => matchEffect2,
  never: () => never2,
  onError: () => onError2,
  onExit: () => onExit2,
  onExitIf: () => onExitIf,
  onInterrupt: () => onInterrupt2,
  option: () => option,
  orDie: () => orDie,
  orElseSucceed: () => orElseSucceed,
  promise: () => promise,
  provideContext: () => provideContext2,
  provideScope: () => provideScope,
  provideService: () => provideService,
  provideServiceEffect: () => provideServiceEffect,
  race: () => race,
  raceAll: () => raceAll,
  raceAllFirst: () => raceAllFirst,
  raceFirst: () => raceFirst,
  repeat: () => repeat,
  repeatExit: () => repeatExit,
  replicate: () => replicate,
  replicateEffect: () => replicateEffect,
  retry: () => retry,
  runFork: () => runFork,
  runPromise: () => runPromise,
  runPromiseExit: () => runPromiseExit,
  runSync: () => runSync,
  runSyncExit: () => runSyncExit,
  sandbox: () => sandbox,
  scheduleAddDelay: () => scheduleAddDelay,
  scheduleExponential: () => scheduleExponential,
  scheduleIntersect: () => scheduleIntersect,
  scheduleRecurs: () => scheduleRecurs,
  scheduleSpaced: () => scheduleSpaced,
  scheduleUnion: () => scheduleUnion,
  scheduleWithMaxDelay: () => scheduleWithMaxDelay,
  scheduleWithMaxElapsed: () => scheduleWithMaxElapsed,
  scope: () => scope,
  scopeMake: () => scopeMake,
  scopeUnsafeMake: () => scopeUnsafeMake,
  scoped: () => scoped,
  service: () => service,
  serviceOption: () => serviceOption,
  sleep: () => sleep,
  succeed: () => succeed3,
  succeedNone: () => succeedNone,
  succeedSome: () => succeedSome,
  suspend: () => suspend2,
  sync: () => sync2,
  tap: () => tap2,
  tapDefect: () => tapDefect,
  tapError: () => tapError,
  tapErrorCause: () => tapErrorCause,
  tapErrorCauseIf: () => tapErrorCauseIf,
  timeout: () => timeout,
  timeoutOption: () => timeoutOption,
  timeoutOrElse: () => timeoutOrElse,
  try: () => try_,
  tryPromise: () => tryPromise,
  uninterruptible: () => uninterruptible2,
  uninterruptibleMask: () => uninterruptibleMask2,
  updateContext: () => updateContext,
  updateService: () => updateService,
  void: () => void_2,
  when: () => when,
  whileLoop: () => whileLoop2,
  withConcurrency: () => withConcurrency,
  withMicroFiber: () => withMicroFiber,
  withTrace: () => withTrace,
  yieldFlush: () => yieldFlush,
  yieldNow: () => yieldNow2,
  yieldNowWith: () => yieldNowWith,
  zip: () => zip4,
  zipWith: () => zipWith3
});
var TypeId = /* @__PURE__ */ Symbol.for("effect/Micro");
var MicroExitTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroExit");
var isMicro = (u) => typeof u === "object" && u !== null && TypeId in u;
var MicroCauseTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroCause");
var isMicroCause = (self) => hasProperty(self, MicroCauseTypeId);
var microCauseVariance = {
  _E: identity
};
var MicroCauseImpl = class extends globalThis.Error {
  _tag;
  traces;
  [MicroCauseTypeId];
  constructor(_tag, originalError, traces) {
    const causeName = `MicroCause.${_tag}`;
    let name;
    let message;
    let stack;
    if (originalError instanceof globalThis.Error) {
      name = `(${causeName}) ${originalError.name}`;
      message = originalError.message;
      const messageLines = message.split("\n").length;
      stack = originalError.stack ? `(${causeName}) ${originalError.stack.split("\n").slice(0, messageLines + 3).join("\n")}` : `${name}: ${message}`;
    } else {
      name = causeName;
      message = toStringUnknown(originalError, 0);
      stack = `${name}: ${message}`;
    }
    if (traces.length > 0) {
      stack += `
    ${traces.join("\n    ")}`;
    }
    super(message);
    this._tag = _tag;
    this.traces = traces;
    this[MicroCauseTypeId] = microCauseVariance;
    this.name = name;
    this.stack = stack;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toString() {
    return this.stack;
  }
  [NodeInspectSymbol]() {
    return this.stack;
  }
};
var Fail = class extends MicroCauseImpl {
  error;
  constructor(error, traces = []) {
    super("Fail", error, traces);
    this.error = error;
  }
};
var causeFail = (error, traces = []) => new Fail(error, traces);
var Die = class extends MicroCauseImpl {
  defect;
  constructor(defect, traces = []) {
    super("Die", defect, traces);
    this.defect = defect;
  }
};
var causeDie = (defect, traces = []) => new Die(defect, traces);
var Interrupt = class extends MicroCauseImpl {
  constructor(traces = []) {
    super("Interrupt", "interrupted", traces);
  }
};
var causeInterrupt = (traces = []) => new Interrupt(traces);
var causeIsFail = (self) => self._tag === "Fail";
var causeIsDie = (self) => self._tag === "Die";
var causeIsInterrupt = (self) => self._tag === "Interrupt";
var causeSquash = (self) => self._tag === "Fail" ? self.error : self._tag === "Die" ? self.defect : self;
var causeWithTrace = dual(2, (self, trace) => {
  const traces = [...self.traces, trace];
  switch (self._tag) {
    case "Die":
      return causeDie(self.defect, traces);
    case "Interrupt":
      return causeInterrupt(traces);
    case "Fail":
      return causeFail(self.error, traces);
  }
});
var MicroFiberTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroFiber");
var fiberVariance = {
  _A: identity,
  _E: identity
};
var MicroFiberImpl = class {
  context;
  interruptible;
  [MicroFiberTypeId];
  _stack = [];
  _observers = [];
  _exit;
  _children;
  currentOpCount = 0;
  constructor(context3, interruptible4 = true) {
    this.context = context3;
    this.interruptible = interruptible4;
    this[MicroFiberTypeId] = fiberVariance;
  }
  getRef(ref) {
    return unsafeGetReference(this.context, ref);
  }
  addObserver(cb) {
    if (this._exit) {
      cb(this._exit);
      return constVoid;
    }
    this._observers.push(cb);
    return () => {
      const index = this._observers.indexOf(cb);
      if (index >= 0) {
        this._observers.splice(index, 1);
      }
    };
  }
  _interrupted = false;
  unsafeInterrupt() {
    if (this._exit) {
      return;
    }
    this._interrupted = true;
    if (this.interruptible) {
      this.evaluate(exitInterrupt2);
    }
  }
  unsafePoll() {
    return this._exit;
  }
  evaluate(effect) {
    if (this._exit) {
      return;
    } else if (this._yielded !== void 0) {
      const yielded = this._yielded;
      this._yielded = void 0;
      yielded();
    }
    const exit3 = this.runLoop(effect);
    if (exit3 === Yield) {
      return;
    }
    const interruptChildren = fiberMiddleware.interruptChildren && fiberMiddleware.interruptChildren(this);
    if (interruptChildren !== void 0) {
      return this.evaluate(flatMap4(interruptChildren, () => exit3));
    }
    this._exit = exit3;
    for (let i = 0; i < this._observers.length; i++) {
      this._observers[i](exit3);
    }
    this._observers.length = 0;
  }
  runLoop(effect) {
    let yielding = false;
    let current = effect;
    this.currentOpCount = 0;
    try {
      while (true) {
        this.currentOpCount++;
        if (!yielding && this.getRef(CurrentScheduler).shouldYield(this)) {
          yielding = true;
          const prev = current;
          current = flatMap4(yieldNow2, () => prev);
        }
        current = current[evaluate](this);
        if (current === Yield) {
          const yielded = this._yielded;
          if (MicroExitTypeId in yielded) {
            this._yielded = void 0;
            return yielded;
          }
          return Yield;
        }
      }
    } catch (error) {
      if (!hasProperty(current, evaluate)) {
        return exitDie(`MicroFiber.runLoop: Not a valid effect: ${String(current)}`);
      }
      return exitDie(error);
    }
  }
  getCont(symbol3) {
    while (true) {
      const op = this._stack.pop();
      if (!op) return void 0;
      const cont = op[ensureCont] && op[ensureCont](this);
      if (cont) return {
        [symbol3]: cont
      };
      if (op[symbol3]) return op;
    }
  }
  // cancel the yielded operation, or for the yielded exit value
  _yielded = void 0;
  yieldWith(value2) {
    this._yielded = value2;
    return Yield;
  }
  children() {
    return this._children ??= /* @__PURE__ */ new Set();
  }
};
var fiberMiddleware = globalValue("effect/Micro/fiberMiddleware", () => ({
  interruptChildren: void 0
}));
var fiberInterruptChildren = (fiber) => {
  if (fiber._children === void 0 || fiber._children.size === 0) {
    return void 0;
  }
  return fiberInterruptAll(fiber._children);
};
var fiberAwait = (self) => async((resume2) => sync2(self.addObserver((exit3) => resume2(succeed3(exit3)))));
var fiberJoin = (self) => flatten5(fiberAwait(self));
var fiberInterrupt = (self) => suspend2(() => {
  self.unsafeInterrupt();
  return asVoid2(fiberAwait(self));
});
var fiberInterruptAll = (fibers) => suspend2(() => {
  for (const fiber of fibers) fiber.unsafeInterrupt();
  const iter = fibers[Symbol.iterator]();
  const wait = suspend2(() => {
    let result = iter.next();
    while (!result.done) {
      if (result.value.unsafePoll()) {
        result = iter.next();
        continue;
      }
      const fiber = result.value;
      return async((resume2) => {
        fiber.addObserver((_) => {
          resume2(wait);
        });
      });
    }
    return exitVoid2;
  });
  return wait;
});
var identifier = /* @__PURE__ */ Symbol.for("effect/Micro/identifier");
var args = /* @__PURE__ */ Symbol.for("effect/Micro/args");
var evaluate = /* @__PURE__ */ Symbol.for("effect/Micro/evaluate");
var successCont = /* @__PURE__ */ Symbol.for("effect/Micro/successCont");
var failureCont = /* @__PURE__ */ Symbol.for("effect/Micro/failureCont");
var ensureCont = /* @__PURE__ */ Symbol.for("effect/Micro/ensureCont");
var Yield = /* @__PURE__ */ Symbol.for("effect/Micro/Yield");
var microVariance = {
  _A: identity,
  _E: identity,
  _R: identity
};
var MicroProto = {
  ...EffectPrototype2,
  _op: "Micro",
  [TypeId]: microVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  },
  toJSON() {
    return {
      _id: "Micro",
      op: this[identifier],
      ...args in this ? {
        args: this[args]
      } : void 0
    };
  },
  toString() {
    return format(this);
  },
  [NodeInspectSymbol]() {
    return format(this);
  }
};
function defaultEvaluate(_fiber) {
  return exitDie(`Micro.evaluate: Not implemented`);
}
var makePrimitiveProto = (options) => ({
  ...MicroProto,
  [identifier]: options.op,
  [evaluate]: options.eval ?? defaultEvaluate,
  [successCont]: options.contA,
  [failureCont]: options.contE,
  [ensureCont]: options.ensure
});
var makePrimitive = (options) => {
  const Proto2 = makePrimitiveProto(options);
  return function() {
    const self = Object.create(Proto2);
    self[args] = options.single === false ? arguments : arguments[0];
    return self;
  };
};
var makeExit = (options) => {
  const Proto2 = {
    ...makePrimitiveProto(options),
    [MicroExitTypeId]: MicroExitTypeId,
    _tag: options.op,
    get [options.prop]() {
      return this[args];
    },
    toJSON() {
      return {
        _id: "MicroExit",
        _tag: options.op,
        [options.prop]: this[args]
      };
    },
    [symbol2](that) {
      return isMicroExit(that) && that._tag === options.op && equals(this[args], that[args]);
    },
    [symbol]() {
      return cached(this, combine(string(options.op))(hash(this[args])));
    }
  };
  return function(value2) {
    const self = Object.create(Proto2);
    self[args] = value2;
    self[successCont] = void 0;
    self[failureCont] = void 0;
    self[ensureCont] = void 0;
    return self;
  };
};
var succeed3 = makeExit({
  op: "Success",
  prop: "value",
  eval(fiber) {
    const cont = fiber.getCont(successCont);
    return cont ? cont[successCont](this[args], fiber) : fiber.yieldWith(this);
  }
});
var failCause3 = makeExit({
  op: "Failure",
  prop: "cause",
  eval(fiber) {
    let cont = fiber.getCont(failureCont);
    while (causeIsInterrupt(this[args]) && cont && fiber.interruptible) {
      cont = fiber.getCont(failureCont);
    }
    return cont ? cont[failureCont](this[args], fiber) : fiber.yieldWith(this);
  }
});
var fail3 = (error) => failCause3(causeFail(error));
var sync2 = makePrimitive({
  op: "Sync",
  eval(fiber) {
    const value2 = this[args]();
    const cont = fiber.getCont(successCont);
    return cont ? cont[successCont](value2, fiber) : fiber.yieldWith(exitSucceed2(value2));
  }
});
var suspend2 = makePrimitive({
  op: "Suspend",
  eval(_fiber) {
    return this[args]();
  }
});
var yieldNowWith = makePrimitive({
  op: "Yield",
  eval(fiber) {
    let resumed = false;
    fiber.getRef(CurrentScheduler).scheduleTask(() => {
      if (resumed) return;
      fiber.evaluate(exitVoid2);
    }, this[args] ?? 0);
    return fiber.yieldWith(() => {
      resumed = true;
    });
  }
});
var yieldNow2 = yieldNowWith(0);
var succeedSome = (a) => succeed3(some2(a));
var succeedNone = succeed3(none2());
var failCauseSync2 = (evaluate2) => suspend2(() => failCause3(evaluate2()));
var die3 = (defect) => exitDie(defect);
var failSync2 = (error) => suspend2(() => fail3(error()));
var fromOption = (option3) => option3._tag === "Some" ? succeed3(option3.value) : fail3(new NoSuchElementException2({}));
var fromEither = (either3) => either3._tag === "Right" ? succeed3(either3.right) : fail3(either3.left);
var void_2 = succeed3(void 0);
var try_ = (options) => suspend2(() => {
  try {
    return succeed3(options.try());
  } catch (err) {
    return fail3(options.catch(err));
  }
});
var promise = (evaluate2) => asyncOptions(function(resume2, signal) {
  evaluate2(signal).then((a) => resume2(succeed3(a)), (e) => resume2(die3(e)));
}, evaluate2.length !== 0);
var tryPromise = (options) => asyncOptions(function(resume2, signal) {
  try {
    options.try(signal).then((a) => resume2(succeed3(a)), (e) => resume2(fail3(options.catch(e))));
  } catch (err) {
    resume2(fail3(options.catch(err)));
  }
}, options.try.length !== 0);
var withMicroFiber = makePrimitive({
  op: "WithMicroFiber",
  eval(fiber) {
    return this[args](fiber);
  }
});
var yieldFlush = withMicroFiber((fiber) => {
  fiber.getRef(CurrentScheduler).flush();
  return exitVoid2;
});
var asyncOptions = makePrimitive({
  op: "Async",
  single: false,
  eval(fiber) {
    const register = this[args][0];
    let resumed = false;
    let yielded = false;
    const controller = this[args][1] ? new AbortController() : void 0;
    const onCancel = register((effect) => {
      if (resumed) return;
      resumed = true;
      if (yielded) {
        fiber.evaluate(effect);
      } else {
        yielded = effect;
      }
    }, controller?.signal);
    if (yielded !== false) return yielded;
    yielded = true;
    fiber._yielded = () => {
      resumed = true;
    };
    if (controller === void 0 && onCancel === void 0) {
      return Yield;
    }
    fiber._stack.push(asyncFinalizer(() => {
      resumed = true;
      controller?.abort();
      return onCancel ?? exitVoid2;
    }));
    return Yield;
  }
});
var asyncFinalizer = makePrimitive({
  op: "AsyncFinalizer",
  ensure(fiber) {
    if (fiber.interruptible) {
      fiber.interruptible = false;
      fiber._stack.push(setInterruptible(true));
    }
  },
  contE(cause2, _fiber) {
    return causeIsInterrupt(cause2) ? flatMap4(this[args](), () => failCause3(cause2)) : failCause3(cause2);
  }
});
var async = (register) => asyncOptions(register, register.length >= 2);
var never2 = async(function() {
  const interval = setInterval(constVoid, 2147483646);
  return sync2(() => clearInterval(interval));
});
var gen = (...args2) => suspend2(() => fromIterator(args2.length === 1 ? args2[0]() : args2[1].call(args2[0])));
var fromIterator = makePrimitive({
  op: "Iterator",
  contA(value2, fiber) {
    const state = this[args].next(value2);
    if (state.done) return succeed3(state.value);
    fiber._stack.push(this);
    return yieldWrapGet(state.value);
  },
  eval(fiber) {
    return this[successCont](void 0, fiber);
  }
});
var as2 = dual(2, (self, value2) => map4(self, (_) => value2));
var asSome = (self) => map4(self, some2);
var flip2 = (self) => matchEffect2(self, {
  onFailure: succeed3,
  onSuccess: fail3
});
var andThen = dual(2, (self, f) => flatMap4(self, (a) => {
  const value2 = isMicro(f) ? f : typeof f === "function" ? f(a) : f;
  return isMicro(value2) ? value2 : succeed3(value2);
}));
var tap2 = dual(2, (self, f) => flatMap4(self, (a) => {
  const value2 = isMicro(f) ? f : typeof f === "function" ? f(a) : f;
  return isMicro(value2) ? as2(value2, a) : succeed3(a);
}));
var asVoid2 = (self) => flatMap4(self, (_) => exitVoid2);
var exit2 = (self) => matchCause2(self, {
  onFailure: exitFailCause2,
  onSuccess: exitSucceed2
});
var sandbox = (self) => catchAllCause2(self, fail3);
var raceAll = (all4) => withMicroFiber((parent) => async((resume2) => {
  const effects = fromIterable(all4);
  const len = effects.length;
  let doneCount = 0;
  let done4 = false;
  const fibers = /* @__PURE__ */ new Set();
  const causes = [];
  const onExit3 = (exit3) => {
    doneCount++;
    if (exit3._tag === "Failure") {
      causes.push(exit3.cause);
      if (doneCount >= len) {
        resume2(failCause3(causes[0]));
      }
      return;
    }
    done4 = true;
    resume2(fibers.size === 0 ? exit3 : flatMap4(uninterruptible2(fiberInterruptAll(fibers)), () => exit3));
  };
  for (let i = 0; i < len; i++) {
    if (done4) break;
    const fiber = unsafeFork(parent, interruptible3(effects[i]), true, true);
    fibers.add(fiber);
    fiber.addObserver((exit3) => {
      fibers.delete(fiber);
      onExit3(exit3);
    });
  }
  return fiberInterruptAll(fibers);
}));
var raceAllFirst = (all4) => withMicroFiber((parent) => async((resume2) => {
  let done4 = false;
  const fibers = /* @__PURE__ */ new Set();
  const onExit3 = (exit3) => {
    done4 = true;
    resume2(fibers.size === 0 ? exit3 : flatMap4(fiberInterruptAll(fibers), () => exit3));
  };
  for (const effect of all4) {
    if (done4) break;
    const fiber = unsafeFork(parent, interruptible3(effect), true, true);
    fibers.add(fiber);
    fiber.addObserver((exit3) => {
      fibers.delete(fiber);
      onExit3(exit3);
    });
  }
  return fiberInterruptAll(fibers);
}));
var race = dual(2, (self, that) => raceAll([self, that]));
var raceFirst = dual(2, (self, that) => raceAllFirst([self, that]));
var flatMap4 = dual(2, (self, f) => {
  const onSuccess = Object.create(OnSuccessProto);
  onSuccess[args] = self;
  onSuccess[successCont] = f;
  return onSuccess;
});
var OnSuccessProto = makePrimitiveProto({
  op: "OnSuccess",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var flatten5 = (self) => flatMap4(self, identity);
var map4 = dual(2, (self, f) => flatMap4(self, (a) => succeed3(f(a))));
var isMicroExit = (u) => hasProperty(u, MicroExitTypeId);
var exitSucceed2 = succeed3;
var exitFailCause2 = failCause3;
var exitInterrupt2 = exitFailCause2(causeInterrupt());
var exitFail2 = (e) => exitFailCause2(causeFail(e));
var exitDie = (defect) => exitFailCause2(causeDie(defect));
var exitIsSuccess2 = (self) => self._tag === "Success";
var exitIsFailure2 = (self) => self._tag === "Failure";
var exitIsInterrupt = (self) => exitIsFailure2(self) && self.cause._tag === "Interrupt";
var exitIsFail = (self) => exitIsFailure2(self) && self.cause._tag === "Fail";
var exitIsDie = (self) => exitIsFailure2(self) && self.cause._tag === "Die";
var exitVoid2 = exitSucceed2(void 0);
var exitVoidAll = (exits) => {
  for (const exit3 of exits) {
    if (exit3._tag === "Failure") {
      return exit3;
    }
  }
  return exitVoid2;
};
var setImmediate = "setImmediate" in globalThis ? globalThis.setImmediate : (f) => setTimeout(f, 0);
var MicroSchedulerDefault = class {
  tasks = [];
  running = false;
  /**
   * @since 3.5.9
   */
  scheduleTask(task, _priority) {
    this.tasks.push(task);
    if (!this.running) {
      this.running = true;
      setImmediate(this.afterScheduled);
    }
  }
  /**
   * @since 3.5.9
   */
  afterScheduled = () => {
    this.running = false;
    this.runTasks();
  };
  /**
   * @since 3.5.9
   */
  runTasks() {
    const tasks = this.tasks;
    this.tasks = [];
    for (let i = 0, len = tasks.length; i < len; i++) {
      tasks[i]();
    }
  }
  /**
   * @since 3.5.9
   */
  shouldYield(fiber) {
    return fiber.currentOpCount >= fiber.getRef(MaxOpsBeforeYield);
  }
  /**
   * @since 3.5.9
   */
  flush() {
    while (this.tasks.length > 0) {
      this.runTasks();
    }
  }
};
var service = (tag) => withMicroFiber((fiber) => succeed3(unsafeGet2(fiber.context, tag)));
var serviceOption = (tag) => withMicroFiber((fiber) => succeed3(getOption(fiber.context, tag)));
var updateContext = dual(2, (self, f) => withMicroFiber((fiber) => {
  const prev = fiber.context;
  fiber.context = f(prev);
  return onExit2(self, () => {
    fiber.context = prev;
    return void_2;
  });
}));
var updateService = dual(3, (self, tag, f) => withMicroFiber((fiber) => {
  const prev = unsafeGet2(fiber.context, tag);
  fiber.context = add(fiber.context, tag, f(prev));
  return onExit2(self, () => {
    fiber.context = add(fiber.context, tag, prev);
    return void_2;
  });
}));
var context2 = () => getContext;
var getContext = withMicroFiber((fiber) => succeed3(fiber.context));
var provideContext2 = dual(2, (self, provided) => updateContext(self, merge2(provided)));
var provideService = dual(3, (self, tag, service2) => updateContext(self, add(tag, service2)));
var provideServiceEffect = dual(3, (self, tag, acquire) => flatMap4(acquire, (service2) => provideService(self, tag, service2)));
var MaxOpsBeforeYield = class extends Reference()("effect/Micro/currentMaxOpsBeforeYield", {
  defaultValue: () => 2048
}) {
};
var CurrentConcurrency = class extends Reference()("effect/Micro/currentConcurrency", {
  defaultValue: () => "unbounded"
}) {
};
var CurrentScheduler = class extends Reference()("effect/Micro/currentScheduler", {
  defaultValue: () => new MicroSchedulerDefault()
}) {
};
var withConcurrency = dual(2, (self, concurrency) => provideService(self, CurrentConcurrency, concurrency));
var zip4 = dual((args2) => isMicro(args2[1]), (self, that, options) => zipWith3(self, that, (a, a2) => [a, a2], options));
var zipWith3 = dual((args2) => isMicro(args2[1]), (self, that, f, options) => options?.concurrent ? map4(all2([self, that], {
  concurrency: 2
}), ([a, a2]) => f(a, a2)) : flatMap4(self, (a) => map4(that, (a2) => f(a, a2))));
var filterOrFailCause = dual((args2) => isMicro(args2[0]), (self, refinement, orFailWith) => flatMap4(self, (a) => refinement(a) ? succeed3(a) : failCause3(orFailWith(a))));
var filterOrFail = dual((args2) => isMicro(args2[0]), (self, refinement, orFailWith) => flatMap4(self, (a) => refinement(a) ? succeed3(a) : fail3(orFailWith(a))));
var when = dual(2, (self, condition) => flatMap4(isMicro(condition) ? condition : sync2(condition), (pass) => pass ? asSome(self) : succeedNone));
var repeatExit = dual(2, (self, options) => suspend2(() => {
  const startedAt = options.schedule ? Date.now() : 0;
  let attempt = 0;
  const loop2 = flatMap4(exit2(self), (exit3) => {
    if (options.while !== void 0 && !options.while(exit3)) {
      return exit3;
    } else if (options.times !== void 0 && attempt >= options.times) {
      return exit3;
    }
    attempt++;
    let delayEffect = yieldNow2;
    if (options.schedule !== void 0) {
      const elapsed = Date.now() - startedAt;
      const duration = options.schedule(attempt, elapsed);
      if (isNone(duration)) {
        return exit3;
      }
      delayEffect = sleep(duration.value);
    }
    return flatMap4(delayEffect, () => loop2);
  });
  return loop2;
}));
var repeat = dual((args2) => isMicro(args2[0]), (self, options) => repeatExit(self, {
  ...options,
  while: (exit3) => exit3._tag === "Success" && (options?.while === void 0 || options.while(exit3.value))
}));
var replicate = dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateEffect = dual((args2) => isMicro(args2[0]), (self, n, options) => all2(replicate(self, n), options));
var forever = (self) => repeat(self);
var scheduleRecurs = (n) => (attempt) => attempt <= n ? some2(0) : none2();
var scheduleSpaced = (millis2) => () => some2(millis2);
var scheduleExponential = (baseMillis, factor = 2) => (attempt) => some2(Math.pow(factor, attempt) * baseMillis);
var scheduleAddDelay = dual(2, (self, f) => (attempt, elapsed) => map(self(attempt, elapsed), (duration) => duration + f()));
var scheduleWithMaxDelay = dual(2, (self, max3) => (attempt, elapsed) => map(self(attempt, elapsed), (duration) => Math.min(duration, max3)));
var scheduleWithMaxElapsed = dual(2, (self, max3) => (attempt, elapsed) => elapsed < max3 ? self(attempt, elapsed) : none2());
var scheduleUnion = dual(2, (self, that) => (attempt, elapsed) => zipWith(self(attempt, elapsed), that(attempt, elapsed), (d1, d2) => Math.min(d1, d2)));
var scheduleIntersect = dual(2, (self, that) => (attempt, elapsed) => zipWith(self(attempt, elapsed), that(attempt, elapsed), (d1, d2) => Math.max(d1, d2)));
var catchAllCause2 = dual(2, (self, f) => {
  const onFailure = Object.create(OnFailureProto);
  onFailure[args] = self;
  onFailure[failureCont] = f;
  return onFailure;
});
var OnFailureProto = makePrimitiveProto({
  op: "OnFailure",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var catchCauseIf = dual(3, (self, predicate, f) => catchAllCause2(self, (cause2) => predicate(cause2) ? f(cause2) : failCause3(cause2)));
var catchAll2 = dual(2, (self, f) => catchCauseIf(self, causeIsFail, (cause2) => f(cause2.error)));
var catchAllDefect = dual(2, (self, f) => catchCauseIf(self, causeIsDie, (die4) => f(die4.defect)));
var tapErrorCause = dual(2, (self, f) => tapErrorCauseIf(self, constTrue, f));
var tapErrorCauseIf = dual(3, (self, refinement, f) => catchCauseIf(self, refinement, (cause2) => andThen(f(cause2), failCause3(cause2))));
var tapError = dual(2, (self, f) => tapErrorCauseIf(self, causeIsFail, (fail6) => f(fail6.error)));
var tapDefect = dual(2, (self, f) => tapErrorCauseIf(self, causeIsDie, (die4) => f(die4.defect)));
var catchIf2 = dual(3, (self, predicate, f) => catchCauseIf(self, (f2) => causeIsFail(f2) && predicate(f2.error), (fail6) => f(fail6.error)));
var catchTag = dual(3, (self, k, f) => catchIf2(self, isTagged(k), f));
var mapErrorCause = dual(2, (self, f) => catchAllCause2(self, (cause2) => failCause3(f(cause2))));
var mapError2 = dual(2, (self, f) => catchAll2(self, (error) => fail3(f(error))));
var orDie = (self) => catchAll2(self, die3);
var orElseSucceed = dual(2, (self, f) => catchAll2(self, (_) => sync2(f)));
var ignore = (self) => matchEffect2(self, {
  onFailure: (_) => void_2,
  onSuccess: (_) => void_2
});
var ignoreLogged = (self) => matchEffect2(self, {
  // eslint-disable-next-line no-console
  onFailure: (error) => sync2(() => console.error(error)),
  onSuccess: (_) => void_2
});
var option = (self) => match7(self, {
  onFailure: none2,
  onSuccess: some2
});
var either2 = (self) => match7(self, {
  onFailure: left,
  onSuccess: right
});
var retry = dual((args2) => isMicro(args2[0]), (self, options) => repeatExit(self, {
  ...options,
  while: (exit3) => exit3._tag === "Failure" && exit3.cause._tag === "Fail" && (options?.while === void 0 || options.while(exit3.cause.error))
}));
var withTrace = function() {
  const prevLimit = globalThis.Error.stackTraceLimit;
  globalThis.Error.stackTraceLimit = 2;
  const error = new globalThis.Error();
  globalThis.Error.stackTraceLimit = prevLimit;
  function generate(name, cause2) {
    const stack = error.stack;
    if (!stack) {
      return cause2;
    }
    const line = stack.split("\n")[2]?.trim().replace(/^at /, "");
    if (!line) {
      return cause2;
    }
    const lineMatch = line.match(/\((.*)\)$/);
    return causeWithTrace(cause2, `at ${name} (${lineMatch ? lineMatch[1] : line})`);
  }
  const f = (name) => (self) => onError2(self, (cause2) => failCause3(generate(name, cause2)));
  if (arguments.length === 2) {
    return f(arguments[1])(arguments[0]);
  }
  return f(arguments[0]);
};
var matchCauseEffect2 = dual(2, (self, options) => {
  const primitive = Object.create(OnSuccessAndFailureProto);
  primitive[args] = self;
  primitive[successCont] = options.onSuccess;
  primitive[failureCont] = options.onFailure;
  return primitive;
});
var OnSuccessAndFailureProto = makePrimitiveProto({
  op: "OnSuccessAndFailure",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var matchCause2 = dual(2, (self, options) => matchCauseEffect2(self, {
  onFailure: (cause2) => sync2(() => options.onFailure(cause2)),
  onSuccess: (value2) => sync2(() => options.onSuccess(value2))
}));
var matchEffect2 = dual(2, (self, options) => matchCauseEffect2(self, {
  onFailure: (cause2) => cause2._tag === "Fail" ? options.onFailure(cause2.error) : failCause3(cause2),
  onSuccess: options.onSuccess
}));
var match7 = dual(2, (self, options) => matchEffect2(self, {
  onFailure: (error) => sync2(() => options.onFailure(error)),
  onSuccess: (value2) => sync2(() => options.onSuccess(value2))
}));
var sleep = (millis2) => async((resume2) => {
  const timeout2 = setTimeout(() => {
    resume2(void_2);
  }, millis2);
  return sync2(() => {
    clearTimeout(timeout2);
  });
});
var delay = dual(2, (self, millis2) => andThen(sleep(millis2), self));
var timeoutOrElse = dual(2, (self, options) => raceFirst(self, andThen(interruptible3(sleep(options.duration)), options.onTimeout)));
var timeout = dual(2, (self, millis2) => timeoutOrElse(self, {
  duration: millis2,
  onTimeout: () => fail3(new TimeoutException())
}));
var timeoutOption = dual(2, (self, millis2) => raceFirst(asSome(self), as2(interruptible3(sleep(millis2)), none2())));
var MicroScopeTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroScope");
var MicroScope = GenericTag("effect/Micro/MicroScope");
var MicroScopeImpl = class _MicroScopeImpl {
  [MicroScopeTypeId];
  state = {
    _tag: "Open",
    finalizers: /* @__PURE__ */ new Set()
  };
  constructor() {
    this[MicroScopeTypeId] = MicroScopeTypeId;
  }
  unsafeAddFinalizer(finalizer) {
    if (this.state._tag === "Open") {
      this.state.finalizers.add(finalizer);
    }
  }
  addFinalizer(finalizer) {
    return suspend2(() => {
      if (this.state._tag === "Open") {
        this.state.finalizers.add(finalizer);
        return void_2;
      }
      return finalizer(this.state.exit);
    });
  }
  unsafeRemoveFinalizer(finalizer) {
    if (this.state._tag === "Open") {
      this.state.finalizers.delete(finalizer);
    }
  }
  close(microExit) {
    return suspend2(() => {
      if (this.state._tag === "Open") {
        const finalizers = Array.from(this.state.finalizers).reverse();
        this.state = {
          _tag: "Closed",
          exit: microExit
        };
        return flatMap4(forEach2(finalizers, (finalizer) => exit2(finalizer(microExit))), exitVoidAll);
      }
      return void_2;
    });
  }
  get fork() {
    return sync2(() => {
      const newScope = new _MicroScopeImpl();
      if (this.state._tag === "Closed") {
        newScope.state = this.state;
        return newScope;
      }
      function fin(exit3) {
        return newScope.close(exit3);
      }
      this.state.finalizers.add(fin);
      newScope.unsafeAddFinalizer((_) => sync2(() => this.unsafeRemoveFinalizer(fin)));
      return newScope;
    });
  }
};
var scopeMake = sync2(() => new MicroScopeImpl());
var scopeUnsafeMake = () => new MicroScopeImpl();
var scope = service(MicroScope);
var provideScope = dual(2, (self, scope3) => provideService(self, MicroScope, scope3));
var scoped = (self) => suspend2(() => {
  const scope3 = new MicroScopeImpl();
  return onExit2(provideService(self, MicroScope, scope3), (exit3) => scope3.close(exit3));
});
var acquireRelease = (acquire, release) => uninterruptible2(flatMap4(scope, (scope3) => tap2(acquire, (a) => scope3.addFinalizer((exit3) => release(a, exit3)))));
var addFinalizer = (finalizer) => flatMap4(scope, (scope3) => scope3.addFinalizer(finalizer));
var onExit2 = dual(2, (self, f) => uninterruptibleMask2((restore) => matchCauseEffect2(restore(self), {
  onFailure: (cause2) => flatMap4(f(exitFailCause2(cause2)), () => failCause3(cause2)),
  onSuccess: (a) => flatMap4(f(exitSucceed2(a)), () => succeed3(a))
})));
var ensuring = dual(2, (self, finalizer) => onExit2(self, (_) => finalizer));
var onExitIf = dual(3, (self, refinement, f) => onExit2(self, (exit3) => refinement(exit3) ? f(exit3) : exitVoid2));
var onError2 = dual(2, (self, f) => onExitIf(self, exitIsFailure2, (exit3) => f(exit3.cause)));
var onInterrupt2 = dual(2, (self, finalizer) => onExitIf(self, exitIsInterrupt, (_) => finalizer));
var acquireUseRelease = (acquire, use, release) => uninterruptibleMask2((restore) => flatMap4(acquire, (a) => flatMap4(exit2(restore(use(a))), (exit3) => andThen(release(a, exit3), exit3))));
var interrupt4 = failCause3(causeInterrupt());
var uninterruptible2 = (self) => withMicroFiber((fiber) => {
  if (!fiber.interruptible) return self;
  fiber.interruptible = false;
  fiber._stack.push(setInterruptible(true));
  return self;
});
var setInterruptible = makePrimitive({
  op: "SetInterruptible",
  ensure(fiber) {
    fiber.interruptible = this[args];
    if (fiber._interrupted && fiber.interruptible) {
      return () => exitInterrupt2;
    }
  }
});
var interruptible3 = (self) => withMicroFiber((fiber) => {
  if (fiber.interruptible) return self;
  fiber.interruptible = true;
  fiber._stack.push(setInterruptible(false));
  if (fiber._interrupted) return exitInterrupt2;
  return self;
});
var uninterruptibleMask2 = (f) => withMicroFiber((fiber) => {
  if (!fiber.interruptible) return f(identity);
  fiber.interruptible = false;
  fiber._stack.push(setInterruptible(true));
  return f(interruptible3);
});
var all2 = (arg, options) => {
  if (Array.isArray(arg) || isIterable(arg)) {
    return forEach2(arg, identity, options);
  } else if (options?.discard) {
    return forEach2(Object.values(arg), identity, options);
  }
  return suspend2(() => {
    const out = {};
    return as2(forEach2(Object.entries(arg), ([key, effect]) => map4(effect, (value2) => {
      out[key] = value2;
    }), {
      discard: true,
      concurrency: options?.concurrency
    }), out);
  });
};
var whileLoop2 = makePrimitive({
  op: "While",
  contA(value2, fiber) {
    this[args].step(value2);
    if (this[args].while()) {
      fiber._stack.push(this);
      return this[args].body();
    }
    return exitVoid2;
  },
  eval(fiber) {
    if (this[args].while()) {
      fiber._stack.push(this);
      return this[args].body();
    }
    return exitVoid2;
  }
});
var forEach2 = (iterable, f, options) => withMicroFiber((parent) => {
  const concurrencyOption = options?.concurrency === "inherit" ? parent.getRef(CurrentConcurrency) : options?.concurrency ?? 1;
  const concurrency = concurrencyOption === "unbounded" ? Number.POSITIVE_INFINITY : Math.max(1, concurrencyOption);
  const items = fromIterable(iterable);
  let length = items.length;
  if (length === 0) {
    return options?.discard ? void_2 : succeed3([]);
  }
  const out = options?.discard ? void 0 : new Array(length);
  let index = 0;
  if (concurrency === 1) {
    return as2(whileLoop2({
      while: () => index < items.length,
      body: () => f(items[index], index),
      step: out ? (b) => out[index++] = b : (_) => index++
    }), out);
  }
  return async((resume2) => {
    const fibers = /* @__PURE__ */ new Set();
    let result = void 0;
    let inProgress = 0;
    let doneCount = 0;
    let pumping = false;
    let interrupted2 = false;
    function pump() {
      pumping = true;
      while (inProgress < concurrency && index < length) {
        const currentIndex = index;
        const item = items[currentIndex];
        index++;
        inProgress++;
        try {
          const child = unsafeFork(parent, f(item, currentIndex), true, true);
          fibers.add(child);
          child.addObserver((exit3) => {
            fibers.delete(child);
            if (interrupted2) {
              return;
            } else if (exit3._tag === "Failure") {
              if (result === void 0) {
                result = exit3;
                length = index;
                fibers.forEach((fiber) => fiber.unsafeInterrupt());
              }
            } else if (out !== void 0) {
              out[currentIndex] = exit3.value;
            }
            doneCount++;
            inProgress--;
            if (doneCount === length) {
              resume2(result ?? succeed3(out));
            } else if (!pumping && inProgress < concurrency) {
              pump();
            }
          });
        } catch (err) {
          result = exitDie(err);
          length = index;
          fibers.forEach((fiber) => fiber.unsafeInterrupt());
        }
      }
      pumping = false;
    }
    pump();
    return suspend2(() => {
      interrupted2 = true;
      index = length;
      return fiberInterruptAll(fibers);
    });
  });
});
var filter = (iterable, f, options) => filterMap2(iterable, (a) => map4(f(a), (pass) => {
  pass = options?.negate ? !pass : pass;
  return pass ? some2(a) : none2();
}), options);
var filterMap2 = (iterable, f, options) => suspend2(() => {
  const out = [];
  return as2(forEach2(iterable, (a) => map4(f(a), (o) => {
    if (o._tag === "Some") {
      out.push(o.value);
    }
  }), {
    discard: true,
    concurrency: options?.concurrency
  }), out);
});
var Do = succeed3({});
var bindTo2 = bindTo(map4);
var bind2 = bind(map4, flatMap4);
var let_2 = let_(map4);
var fork = (self) => withMicroFiber((fiber) => {
  fiberMiddleware.interruptChildren ??= fiberInterruptChildren;
  return succeed3(unsafeFork(fiber, self));
});
var unsafeFork = (parent, effect, immediate = false, daemon = false) => {
  const child = new MicroFiberImpl(parent.context, parent.interruptible);
  if (!daemon) {
    parent.children().add(child);
    child.addObserver(() => parent.children().delete(child));
  }
  if (immediate) {
    child.evaluate(effect);
  } else {
    parent.getRef(CurrentScheduler).scheduleTask(() => child.evaluate(effect), 0);
  }
  return child;
};
var forkDaemon = (self) => withMicroFiber((fiber) => succeed3(unsafeFork(fiber, self, false, true)));
var forkIn = dual(2, (self, scope3) => uninterruptibleMask2((restore) => flatMap4(scope3.fork, (scope4) => tap2(restore(forkDaemon(onExit2(self, (exit3) => scope4.close(exit3)))), (fiber) => scope4.addFinalizer((_) => fiberInterrupt(fiber))))));
var forkScoped = (self) => flatMap4(scope, (scope3) => forkIn(self, scope3));
var runFork = (effect, options) => {
  const fiber = new MicroFiberImpl(CurrentScheduler.context(options?.scheduler ?? new MicroSchedulerDefault()));
  fiber.evaluate(effect);
  if (options?.signal) {
    if (options.signal.aborted) {
      fiber.unsafeInterrupt();
    } else {
      const abort = () => fiber.unsafeInterrupt();
      options.signal.addEventListener("abort", abort, {
        once: true
      });
      fiber.addObserver(() => options.signal.removeEventListener("abort", abort));
    }
  }
  return fiber;
};
var runPromiseExit = (effect, options) => new Promise((resolve, _reject) => {
  const handle = runFork(effect, options);
  handle.addObserver(resolve);
});
var runPromise = (effect, options) => runPromiseExit(effect, options).then((exit3) => {
  if (exit3._tag === "Failure") {
    throw exit3.cause;
  }
  return exit3.value;
});
var runSyncExit = (effect) => {
  const scheduler = new MicroSchedulerDefault();
  const fiber = runFork(effect, {
    scheduler
  });
  scheduler.flush();
  return fiber._exit ?? exitDie(fiber);
};
var runSync = (effect) => {
  const exit3 = runSyncExit(effect);
  if (exit3._tag === "Failure") throw exit3.cause;
  return exit3.value;
};
var YieldableError = (function() {
  class YieldableError2 extends globalThis.Error {
  }
  Object.assign(YieldableError2.prototype, MicroProto, StructuralPrototype, {
    [identifier]: "Failure",
    [evaluate]() {
      return fail3(this);
    },
    toString() {
      return this.message ? `${this.name}: ${this.message}` : this.name;
    },
    toJSON() {
      return {
        ...this
      };
    },
    [NodeInspectSymbol]() {
      const stack = this.stack;
      if (stack) {
        return `${this.toString()}
${stack.split("\n").slice(1).join("\n")}`;
      }
      return this.toString();
    }
  });
  return YieldableError2;
})();
var Error3 = /* @__PURE__ */ (function() {
  return class extends YieldableError {
    constructor(args2) {
      super();
      if (args2) {
        Object.assign(this, args2);
      }
    }
  };
})();
var TaggedError = (tag) => {
  class Base3 extends Error3 {
    _tag = tag;
  }
  ;
  Base3.prototype.name = tag;
  return Base3;
};
var NoSuchElementException2 = class extends TaggedError("NoSuchElementException") {
};
var TimeoutException = class extends TaggedError("TimeoutException") {
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Readable.js
var Readable_exports = {};
__export(Readable_exports, {
  TypeId: () => TypeId2,
  isReadable: () => isReadable,
  make: () => make7,
  map: () => map5,
  mapEffect: () => mapEffect,
  unwrap: () => unwrap
});
var TypeId2 = /* @__PURE__ */ Symbol.for("effect/Readable");
var isReadable = (u) => hasProperty(u, TypeId2);
var Proto = {
  [TypeId2]: TypeId2,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make7 = (get8) => {
  const self = Object.create(Proto);
  self.get = get8;
  return self;
};
var map5 = dual(2, (self, f) => make7(map3(self.get, f)));
var mapEffect = dual(2, (self, f) => make7(flatMap3(self.get, f)));
var unwrap = (effect) => make7(flatMap3(effect, (s) => s.get));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Ref.js
var Ref_exports = {};
__export(Ref_exports, {
  RefTypeId: () => RefTypeId2,
  get: () => get6,
  getAndSet: () => getAndSet2,
  getAndUpdate: () => getAndUpdate2,
  getAndUpdateSome: () => getAndUpdateSome2,
  make: () => make9,
  modify: () => modify2,
  modifySome: () => modifySome2,
  set: () => set4,
  setAndGet: () => setAndGet2,
  unsafeMake: () => unsafeMake5,
  update: () => update3,
  updateAndGet: () => updateAndGet2,
  updateSome: () => updateSome2,
  updateSomeAndGet: () => updateSomeAndGet2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/ref.js
var RefTypeId = /* @__PURE__ */ Symbol.for("effect/Ref");
var refVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var RefImpl = class extends Class {
  ref;
  commit() {
    return this.get;
  }
  [RefTypeId] = refVariance;
  [TypeId2] = TypeId2;
  constructor(ref) {
    super();
    this.ref = ref;
    this.get = sync(() => get(this.ref));
  }
  get;
  modify(f) {
    return sync(() => {
      const current = get(this.ref);
      const [b, a] = f(current);
      if (current !== a) {
        set(a)(this.ref);
      }
      return b;
    });
  }
};
var unsafeMake4 = (value2) => new RefImpl(make2(value2));
var make8 = (value2) => sync(() => unsafeMake4(value2));
var get5 = (self) => self.get;
var set3 = dual(2, (self, value2) => self.modify(() => [void 0, value2]));
var getAndSet = dual(2, (self, value2) => self.modify((a) => [a, value2]));
var getAndUpdate = dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome = dual(2, (self, pf) => self.modify((value2) => {
  const option3 = pf(value2);
  switch (option3._tag) {
    case "None": {
      return [value2, value2];
    }
    case "Some": {
      return [value2, option3.value];
    }
  }
}));
var setAndGet = dual(2, (self, value2) => self.modify(() => [value2, value2]));
var modify = dual(2, (self, f) => self.modify(f));
var modifySome = dual(3, (self, fallback, pf) => self.modify((value2) => {
  const option3 = pf(value2);
  switch (option3._tag) {
    case "None": {
      return [fallback, value2];
    }
    case "Some": {
      return option3.value;
    }
  }
}));
var update2 = dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet = dual(2, (self, f) => self.modify((a) => {
  const result = f(a);
  return [result, result];
}));
var updateSome = dual(2, (self, f) => self.modify((a) => [void 0, match(f(a), {
  onNone: () => a,
  onSome: (b) => b
})]));
var updateSomeAndGet = dual(2, (self, pf) => self.modify((value2) => {
  const option3 = pf(value2);
  switch (option3._tag) {
    case "None": {
      return [value2, value2];
    }
    case "Some": {
      return [option3.value, option3.value];
    }
  }
}));
var unsafeGet3 = (self) => get(self.ref);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Ref.js
var RefTypeId2 = RefTypeId;
var make9 = make8;
var get6 = get5;
var getAndSet2 = getAndSet;
var getAndUpdate2 = getAndUpdate;
var getAndUpdateSome2 = getAndUpdateSome;
var modify2 = modify;
var modifySome2 = modifySome;
var set4 = set3;
var setAndGet2 = setAndGet;
var update3 = update2;
var updateAndGet2 = updateAndGet;
var updateSome2 = updateSome;
var updateSomeAndGet2 = updateSomeAndGet;
var unsafeMake5 = unsafeMake4;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Scheduler.js
var Scheduler_exports = {};
__export(Scheduler_exports, {
  ControlledScheduler: () => ControlledScheduler,
  MixedScheduler: () => MixedScheduler,
  PriorityBuckets: () => PriorityBuckets,
  SyncScheduler: () => SyncScheduler,
  currentScheduler: () => currentScheduler,
  defaultScheduler: () => defaultScheduler,
  defaultShouldYield: () => defaultShouldYield,
  make: () => make10,
  makeBatched: () => makeBatched,
  makeMatrix: () => makeMatrix,
  timer: () => timer,
  timerBatched: () => timerBatched,
  withScheduler: () => withScheduler
});
var PriorityBuckets = class {
  /**
   * @since 2.0.0
   */
  buckets = [];
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    const length = this.buckets.length;
    let bucket = void 0;
    let index = 0;
    for (; index < length; index++) {
      if (this.buckets[index][0] <= priority) {
        bucket = this.buckets[index];
      } else {
        break;
      }
    }
    if (bucket && bucket[0] === priority) {
      bucket[1].push(task);
    } else if (index === length) {
      this.buckets.push([priority, [task]]);
    } else {
      this.buckets.splice(index, 0, [priority, [task]]);
    }
  }
};
var MixedScheduler = class {
  maxNextTickBeforeTimer;
  /**
   * @since 2.0.0
   */
  running = false;
  /**
   * @since 2.0.0
   */
  tasks = new PriorityBuckets();
  constructor(maxNextTickBeforeTimer) {
    this.maxNextTickBeforeTimer = maxNextTickBeforeTimer;
  }
  /**
   * @since 2.0.0
   */
  starveInternal(depth) {
    const tasks = this.tasks.buckets;
    this.tasks.buckets = [];
    for (const [_, toRun] of tasks) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
    if (this.tasks.buckets.length === 0) {
      this.running = false;
    } else {
      this.starve(depth);
    }
  }
  /**
   * @since 2.0.0
   */
  starve(depth = 0) {
    if (depth >= this.maxNextTickBeforeTimer) {
      setTimeout(() => this.starveInternal(0), 0);
    } else {
      Promise.resolve(void 0).then(() => this.starveInternal(depth + 1));
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    this.tasks.scheduleTask(task, priority);
    if (!this.running) {
      this.running = true;
      this.starve();
    }
  }
};
var defaultScheduler = globalValue(/* @__PURE__ */ Symbol.for("effect/Scheduler/defaultScheduler"), () => new MixedScheduler(2048));
var SyncScheduler = class {
  /**
   * @since 2.0.0
   */
  tasks = new PriorityBuckets();
  /**
   * @since 2.0.0
   */
  deferred = false;
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    if (this.deferred) {
      defaultScheduler.scheduleTask(task, priority);
    } else {
      this.tasks.scheduleTask(task, priority);
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  flush() {
    while (this.tasks.buckets.length > 0) {
      const tasks = this.tasks.buckets;
      this.tasks.buckets = [];
      for (const [_, toRun] of tasks) {
        for (let i = 0; i < toRun.length; i++) {
          toRun[i]();
        }
      }
    }
    this.deferred = true;
  }
};
var ControlledScheduler = class {
  /**
   * @since 2.0.0
   */
  tasks = new PriorityBuckets();
  /**
   * @since 2.0.0
   */
  deferred = false;
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    if (this.deferred) {
      defaultScheduler.scheduleTask(task, priority);
    } else {
      this.tasks.scheduleTask(task, priority);
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  step() {
    const tasks = this.tasks.buckets;
    this.tasks.buckets = [];
    for (const [_, toRun] of tasks) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
  }
};
var makeMatrix = (...record) => {
  const index = record.sort(([p0], [p1]) => p0 < p1 ? -1 : p0 > p1 ? 1 : 0);
  return {
    shouldYield(fiber) {
      for (const scheduler of record) {
        const priority = scheduler[1].shouldYield(fiber);
        if (priority !== false) {
          return priority;
        }
      }
      return false;
    },
    scheduleTask(task, priority) {
      let scheduler = void 0;
      for (const i of index) {
        if (priority >= i[0]) {
          scheduler = i[1];
        } else {
          return (scheduler ?? defaultScheduler).scheduleTask(task, priority);
        }
      }
      return (scheduler ?? defaultScheduler).scheduleTask(task, priority);
    }
  };
};
var defaultShouldYield = (fiber) => {
  return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
};
var make10 = (scheduleTask, shouldYield = defaultShouldYield) => ({
  scheduleTask,
  shouldYield
});
var makeBatched = (callback, shouldYield = defaultShouldYield) => {
  let running3 = false;
  const tasks = new PriorityBuckets();
  const starveInternal = () => {
    const tasksToRun = tasks.buckets;
    tasks.buckets = [];
    for (const [_, toRun] of tasksToRun) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
    if (tasks.buckets.length === 0) {
      running3 = false;
    } else {
      starve();
    }
  };
  const starve = () => callback(starveInternal);
  return make10((task, priority) => {
    tasks.scheduleTask(task, priority);
    if (!running3) {
      running3 = true;
      starve();
    }
  }, shouldYield);
};
var timer = (ms, shouldYield = defaultShouldYield) => make10((task) => setTimeout(task, ms), shouldYield);
var timerBatched = (ms, shouldYield = defaultShouldYield) => makeBatched((task) => setTimeout(task, ms), shouldYield);
var currentScheduler = globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentScheduler"), () => fiberRefUnsafeMake(defaultScheduler));
var withScheduler = dual(2, (self, scheduler) => fiberRefLocally(self, currentScheduler, scheduler));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RegExp.js
var RegExp_exports = {};
__export(RegExp_exports, {
  escape: () => escape,
  isRegExp: () => isRegExp2
});
var isRegExp2 = isRegExp;
var escape = (string2) => string2.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&");

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/tracer.js
var TracerTypeId = /* @__PURE__ */ Symbol.for("effect/Tracer");
var make11 = (options) => ({
  [TracerTypeId]: TracerTypeId,
  ...options
});
var tracerTag = GenericTag("effect/Tracer");
var spanTag = GenericTag("effect/ParentSpan");
var randomHexString = (function() {
  const characters = "abcdef0123456789";
  const charactersLength = characters.length;
  return function(length) {
    let result = "";
    for (let i = 0; i < length; i++) {
      result += characters.charAt(Math.floor(Math.random() * charactersLength));
    }
    return result;
  };
})();
var NativeSpan = class {
  name;
  parent;
  context;
  startTime;
  kind;
  _tag = "Span";
  spanId;
  traceId = "native";
  sampled = true;
  status;
  attributes;
  events = [];
  links;
  constructor(name, parent, context3, links, startTime, kind) {
    this.name = name;
    this.parent = parent;
    this.context = context3;
    this.startTime = startTime;
    this.kind = kind;
    this.status = {
      _tag: "Started",
      startTime
    };
    this.attributes = /* @__PURE__ */ new Map();
    this.traceId = parent._tag === "Some" ? parent.value.traceId : randomHexString(32);
    this.spanId = randomHexString(16);
    this.links = Array.from(links);
  }
  end(endTime, exit3) {
    this.status = {
      _tag: "Ended",
      endTime,
      exit: exit3,
      startTime: this.status.startTime
    };
  }
  attribute(key, value2) {
    this.attributes.set(key, value2);
  }
  event(name, startTime, attributes) {
    this.events.push([name, startTime, attributes ?? {}]);
  }
  addLinks(links) {
    this.links.push(...links);
  }
};
var nativeTracer = make11({
  span: (name, parent, context3, links, startTime, kind) => new NativeSpan(name, parent, context3, links, startTime, kind),
  context: (f) => f()
});
var externalSpan = (options) => ({
  _tag: "ExternalSpan",
  spanId: options.spanId,
  traceId: options.traceId,
  sampled: options.sampled ?? true,
  context: options.context ?? empty3()
});
var addSpanStackTrace = (options) => {
  if (options?.captureStackTrace === false) {
    return options;
  } else if (options?.captureStackTrace !== void 0 && typeof options.captureStackTrace !== "boolean") {
    return options;
  }
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 3;
  const traceError = new Error();
  Error.stackTraceLimit = limit;
  let cache = false;
  return {
    ...options,
    captureStackTrace: () => {
      if (cache !== false) {
        return cache;
      }
      if (traceError.stack !== void 0) {
        const stack = traceError.stack.split("\n");
        if (stack[3] !== void 0) {
          cache = stack[3].trim();
          return cache;
        }
      }
    }
  };
};
var DisablePropagation = Reference()("effect/Tracer/DisablePropagation", {
  defaultValue: constFalse
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/clock.js
var ClockSymbolKey = "effect/Clock";
var ClockTypeId = Symbol.for(ClockSymbolKey);
var clockTag = GenericTag("effect/Clock");
var MAX_TIMER_MILLIS = 2 ** 31 - 1;
var globalClockScheduler = {
  unsafeSchedule(task, duration) {
    const millis2 = toMillis(duration);
    if (millis2 > MAX_TIMER_MILLIS) {
      return constFalse;
    }
    let completed = false;
    const handle = setTimeout(() => {
      completed = true;
      task();
    }, millis2);
    return () => {
      clearTimeout(handle);
      return !completed;
    };
  }
};
var performanceNowNanos = (function() {
  const bigint1e6 = BigInt(1e6);
  if (typeof performance === "undefined" || typeof performance.now !== "function") {
    return () => BigInt(Date.now()) * bigint1e6;
  }
  let origin;
  return () => {
    if (origin === void 0) {
      origin = BigInt(Date.now()) * bigint1e6 - BigInt(Math.round(performance.now() * 1e6));
    }
    return origin + BigInt(Math.round(performance.now() * 1e6));
  };
})();
var processOrPerformanceNow = (function() {
  const processHrtime = typeof process === "object" && "hrtime" in process && typeof process.hrtime.bigint === "function" ? process.hrtime : void 0;
  if (!processHrtime) {
    return performanceNowNanos;
  }
  const origin = performanceNowNanos() - processHrtime.bigint();
  return () => origin + processHrtime.bigint();
})();
var ClockImpl = class {
  [ClockTypeId] = ClockTypeId;
  unsafeCurrentTimeMillis() {
    return Date.now();
  }
  unsafeCurrentTimeNanos() {
    return processOrPerformanceNow();
  }
  currentTimeMillis = sync(() => this.unsafeCurrentTimeMillis());
  currentTimeNanos = sync(() => this.unsafeCurrentTimeNanos());
  scheduler() {
    return succeed(globalClockScheduler);
  }
  sleep(duration) {
    return async_((resume2) => {
      const canceler = globalClockScheduler.unsafeSchedule(() => resume2(void_), duration);
      return asVoid(sync(canceler));
    });
  }
};
var make12 = () => new ClockImpl();

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/configError.js
var OP_AND = "And";
var OP_OR = "Or";
var OP_INVALID_DATA = "InvalidData";
var OP_MISSING_DATA = "MissingData";
var OP_SOURCE_UNAVAILABLE = "SourceUnavailable";
var OP_UNSUPPORTED = "Unsupported";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/configError.js
var ConfigErrorSymbolKey = "effect/ConfigError";
var ConfigErrorTypeId = Symbol.for(ConfigErrorSymbolKey);
var proto = {
  _tag: "ConfigError",
  [ConfigErrorTypeId]: ConfigErrorTypeId
};
var And = (self, that) => {
  const error = Object.create(proto);
  error._op = OP_AND;
  error.left = self;
  error.right = that;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      return `${this.left} and ${this.right}`;
    }
  });
  Object.defineProperty(error, "message", {
    enumerable: false,
    get() {
      return this.toString();
    }
  });
  return error;
};
var Or = (self, that) => {
  const error = Object.create(proto);
  error._op = OP_OR;
  error.left = self;
  error.right = that;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      return `${this.left} or ${this.right}`;
    }
  });
  Object.defineProperty(error, "message", {
    enumerable: false,
    get() {
      return this.toString();
    }
  });
  return error;
};
var InvalidData = (path, message, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_INVALID_DATA;
  error.path = path;
  error.message = message;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Invalid data at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var MissingData = (path, message, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_MISSING_DATA;
  error.path = path;
  error.message = message;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Missing data at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var SourceUnavailable = (path, message, cause2, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_SOURCE_UNAVAILABLE;
  error.path = path;
  error.message = message;
  error.cause = cause2;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Source unavailable at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var Unsupported = (path, message, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_UNSUPPORTED;
  error.path = path;
  error.message = message;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Unsupported operation at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var isConfigError = (u) => hasProperty(u, ConfigErrorTypeId);
var isAnd = (self) => self._op === OP_AND;
var isOr = (self) => self._op === OP_OR;
var isInvalidData = (self) => self._op === OP_INVALID_DATA;
var isMissingData = (self) => self._op === OP_MISSING_DATA;
var isSourceUnavailable = (self) => self._op === OP_SOURCE_UNAVAILABLE;
var isUnsupported = (self) => self._op === OP_UNSUPPORTED;
var prefixed = dual(2, (self, prefix) => {
  switch (self._op) {
    case OP_AND: {
      return And(prefixed(self.left, prefix), prefixed(self.right, prefix));
    }
    case OP_OR: {
      return Or(prefixed(self.left, prefix), prefixed(self.right, prefix));
    }
    case OP_INVALID_DATA: {
      return InvalidData([...prefix, ...self.path], self.message);
    }
    case OP_MISSING_DATA: {
      return MissingData([...prefix, ...self.path], self.message);
    }
    case OP_SOURCE_UNAVAILABLE: {
      return SourceUnavailable([...prefix, ...self.path], self.message, self.cause);
    }
    case OP_UNSUPPORTED: {
      return Unsupported([...prefix, ...self.path], self.message);
    }
  }
});
var IsMissingDataOnlyReducer = {
  andCase: (_, left2, right2) => left2 && right2,
  orCase: (_, left2, right2) => left2 && right2,
  invalidDataCase: constFalse,
  missingDataCase: constTrue,
  sourceUnavailableCase: constFalse,
  unsupportedCase: constFalse
};
var reduceWithContext = dual(3, (self, context3, reducer) => {
  const input = [self];
  const output = [];
  while (input.length > 0) {
    const error = input.pop();
    switch (error._op) {
      case OP_AND: {
        input.push(error.right);
        input.push(error.left);
        output.push(left({
          _op: "AndCase"
        }));
        break;
      }
      case OP_OR: {
        input.push(error.right);
        input.push(error.left);
        output.push(left({
          _op: "OrCase"
        }));
        break;
      }
      case OP_INVALID_DATA: {
        output.push(right(reducer.invalidDataCase(context3, error.path, error.message)));
        break;
      }
      case OP_MISSING_DATA: {
        output.push(right(reducer.missingDataCase(context3, error.path, error.message)));
        break;
      }
      case OP_SOURCE_UNAVAILABLE: {
        output.push(right(reducer.sourceUnavailableCase(context3, error.path, error.message, error.cause)));
        break;
      }
      case OP_UNSUPPORTED: {
        output.push(right(reducer.unsupportedCase(context3, error.path, error.message)));
        break;
      }
    }
  }
  const accumulator = [];
  while (output.length > 0) {
    const either3 = output.pop();
    switch (either3._op) {
      case "Left": {
        switch (either3.left._op) {
          case "AndCase": {
            const left2 = accumulator.pop();
            const right2 = accumulator.pop();
            const value2 = reducer.andCase(context3, left2, right2);
            accumulator.push(value2);
            break;
          }
          case "OrCase": {
            const left2 = accumulator.pop();
            const right2 = accumulator.pop();
            const value2 = reducer.orCase(context3, left2, right2);
            accumulator.push(value2);
            break;
          }
        }
        break;
      }
      case "Right": {
        accumulator.push(either3.right);
        break;
      }
    }
  }
  if (accumulator.length === 0) {
    throw new Error("BUG: ConfigError.reduceWithContext - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return accumulator.pop();
});
var isMissingDataOnly = (self) => reduceWithContext(self, void 0, IsMissingDataOnlyReducer);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/configProvider/pathPatch.js
var empty13 = {
  _tag: "Empty"
};
var andThen2 = dual(2, (self, that) => ({
  _tag: "AndThen",
  first: self,
  second: that
}));
var mapName = dual(2, (self, f) => andThen2(self, {
  _tag: "MapName",
  f
}));
var nested = dual(2, (self, name) => andThen2(self, {
  _tag: "Nested",
  name
}));
var unnested = dual(2, (self, name) => andThen2(self, {
  _tag: "Unnested",
  name
}));
var patch4 = dual(2, (path, patch7) => {
  let input = of3(patch7);
  let output = path;
  while (isCons(input)) {
    const patch8 = input.head;
    switch (patch8._tag) {
      case "Empty": {
        input = input.tail;
        break;
      }
      case "AndThen": {
        input = cons(patch8.first, cons(patch8.second, input.tail));
        break;
      }
      case "MapName": {
        output = map2(output, patch8.f);
        input = input.tail;
        break;
      }
      case "Nested": {
        output = prepend(output, patch8.name);
        input = input.tail;
        break;
      }
      case "Unnested": {
        const containsName = pipe(head(output), contains(patch8.name));
        if (containsName) {
          output = tailNonEmpty(output);
          input = input.tail;
        } else {
          return left(MissingData(output, `Expected ${patch8.name} to be in path in ConfigProvider#unnested`));
        }
        break;
      }
    }
  }
  return right(output);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/config.js
var OP_CONSTANT = "Constant";
var OP_FAIL = "Fail";
var OP_FALLBACK = "Fallback";
var OP_DESCRIBED = "Described";
var OP_LAZY = "Lazy";
var OP_MAP_OR_FAIL = "MapOrFail";
var OP_NESTED = "Nested";
var OP_PRIMITIVE = "Primitive";
var OP_SEQUENCE = "Sequence";
var OP_HASHMAP = "HashMap";
var OP_ZIP_WITH = "ZipWith";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/string-utils.js
var lowerCase = (str) => str.toLowerCase();
var upperCase = (str) => str.toUpperCase();
var replace = (input, re, value2) => re instanceof RegExp ? input.replace(re, value2) : re.reduce((input2, re2) => input2.replace(re2, value2), input);
var DEFAULT_SPLIT_REGEXP = [/([a-z0-9])([A-Z])/g, /([A-Z])([A-Z][a-z])/g];
var DEFAULT_STRIP_REGEXP = /[^A-Z0-9]+/gi;
var noCase = (input, options = {}) => {
  const {
    delimiter = " ",
    splitRegexp = DEFAULT_SPLIT_REGEXP,
    stripRegexp = DEFAULT_STRIP_REGEXP,
    transform: transform3 = lowerCase
  } = options;
  const result = replace(replace(input, splitRegexp, "$1\0$2"), stripRegexp, "\0");
  let start = 0;
  let end = result.length;
  while (result.charAt(start) === "\0") {
    start++;
  }
  while (result.charAt(end - 1) === "\0") {
    end--;
  }
  return result.slice(start, end).split("\0").map(transform3).join(delimiter);
};
var constantCase = (input, options) => noCase(input, {
  delimiter: "_",
  transform: upperCase,
  ...options
});
var kebabCase = (input, options) => noCase(input, {
  delimiter: "-",
  ...options
});
var snakeCase = (input, options) => noCase(input, {
  delimiter: "_",
  ...options
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/configProvider.js
var concat = (l, r) => [...l, ...r];
var ConfigProviderSymbolKey = "effect/ConfigProvider";
var ConfigProviderTypeId = Symbol.for(ConfigProviderSymbolKey);
var configProviderTag = GenericTag("effect/ConfigProvider");
var FlatConfigProviderSymbolKey = "effect/ConfigProviderFlat";
var FlatConfigProviderTypeId = Symbol.for(FlatConfigProviderSymbolKey);
var make13 = (options) => ({
  [ConfigProviderTypeId]: ConfigProviderTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...options
});
var makeFlat = (options) => ({
  [FlatConfigProviderTypeId]: FlatConfigProviderTypeId,
  patch: options.patch,
  load: (path, config2, split = true) => options.load(path, config2, split),
  enumerateChildren: options.enumerateChildren
});
var fromFlat = (flat) => make13({
  load: (config2) => flatMap3(fromFlatLoop(flat, empty(), config2, false), (chunk3) => match(head(chunk3), {
    onNone: () => fail(MissingData(empty(), `Expected a single value having structure: ${config2}`)),
    onSome: succeed
  })),
  flattened: flat
});
var fromEnv = (options) => {
  const {
    pathDelim,
    seqDelim
  } = Object.assign({}, {
    pathDelim: "_",
    seqDelim: ","
  }, options);
  const makePathString = (path) => pipe(path, join(pathDelim));
  const unmakePathString = (pathString) => pathString.split(pathDelim);
  const getEnv = () => typeof process !== "undefined" && "env" in process && typeof process.env === "object" ? process.env : {};
  const load = (path, primitive, split = true) => {
    const pathString = makePathString(path);
    const current = getEnv();
    const valueOpt = pathString in current ? some2(current[pathString]) : none2();
    return pipe(valueOpt, mapError(() => MissingData(path, `Expected ${pathString} to exist in the process context`)), flatMap3((value2) => parsePrimitive(value2, path, primitive, seqDelim, split)));
  };
  const enumerateChildren = (path) => sync(() => {
    const current = getEnv();
    const keys4 = Object.keys(current);
    const keyPaths = keys4.map((value2) => unmakePathString(value2.toUpperCase()));
    const filteredKeyPaths = keyPaths.filter((keyPath) => {
      for (let i = 0; i < path.length; i++) {
        const pathComponent = pipe(path, unsafeGet(i));
        const currentElement = keyPath[i];
        if (currentElement === void 0 || pathComponent !== currentElement) {
          return false;
        }
      }
      return true;
    }).flatMap((keyPath) => keyPath.slice(path.length, path.length + 1));
    return fromIterable3(filteredKeyPaths);
  });
  return fromFlat(makeFlat({
    load,
    enumerateChildren,
    patch: empty13
  }));
};
var fromMap = (map10, config2) => {
  const {
    pathDelim,
    seqDelim
  } = Object.assign({
    seqDelim: ",",
    pathDelim: "."
  }, config2);
  const makePathString = (path) => pipe(path, join(pathDelim));
  const unmakePathString = (pathString) => pathString.split(pathDelim);
  const mapWithIndexSplit = splitIndexInKeys(map10, (str) => unmakePathString(str), makePathString);
  const load = (path, primitive, split = true) => {
    const pathString = makePathString(path);
    const valueOpt = mapWithIndexSplit.has(pathString) ? some2(mapWithIndexSplit.get(pathString)) : none2();
    return pipe(valueOpt, mapError(() => MissingData(path, `Expected ${pathString} to exist in the provided map`)), flatMap3((value2) => parsePrimitive(value2, path, primitive, seqDelim, split)));
  };
  const enumerateChildren = (path) => sync(() => {
    const keyPaths = fromIterable(mapWithIndexSplit.keys()).map(unmakePathString);
    const filteredKeyPaths = keyPaths.filter((keyPath) => {
      for (let i = 0; i < path.length; i++) {
        const pathComponent = pipe(path, unsafeGet(i));
        const currentElement = keyPath[i];
        if (currentElement === void 0 || pathComponent !== currentElement) {
          return false;
        }
      }
      return true;
    }).flatMap((keyPath) => keyPath.slice(path.length, path.length + 1));
    return fromIterable3(filteredKeyPaths);
  });
  return fromFlat(makeFlat({
    load,
    enumerateChildren,
    patch: empty13
  }));
};
var extend = (leftDef, rightDef, left2, right2) => {
  const leftPad = unfold(left2.length, (index) => index >= right2.length ? none2() : some2([leftDef(index), index + 1]));
  const rightPad = unfold(right2.length, (index) => index >= left2.length ? none2() : some2([rightDef(index), index + 1]));
  const leftExtension = concat(left2, leftPad);
  const rightExtension = concat(right2, rightPad);
  return [leftExtension, rightExtension];
};
var appendConfigPath = (path, config2) => {
  let op = config2;
  if (op._tag === "Nested") {
    const out = path.slice();
    while (op._tag === "Nested") {
      out.push(op.name);
      op = op.config;
    }
    return out;
  }
  return path;
};
var fromFlatLoop = (flat, prefix, config2, split) => {
  const op = config2;
  switch (op._tag) {
    case OP_CONSTANT: {
      return succeed(of(op.value));
    }
    case OP_DESCRIBED: {
      return suspend(() => fromFlatLoop(flat, prefix, op.config, split));
    }
    case OP_FAIL: {
      return fail(MissingData(prefix, op.message));
    }
    case OP_FALLBACK: {
      return pipe(suspend(() => fromFlatLoop(flat, prefix, op.first, split)), catchAll((error1) => {
        if (op.condition(error1)) {
          return pipe(fromFlatLoop(flat, prefix, op.second, split), catchAll((error2) => fail(Or(error1, error2))));
        }
        return fail(error1);
      }));
    }
    case OP_LAZY: {
      return suspend(() => fromFlatLoop(flat, prefix, op.config(), split));
    }
    case OP_MAP_OR_FAIL: {
      return suspend(() => pipe(fromFlatLoop(flat, prefix, op.original, split), flatMap3(forEachSequential((a) => pipe(op.mapOrFail(a), mapError(prefixed(appendConfigPath(prefix, op.original))))))));
    }
    case OP_NESTED: {
      return suspend(() => fromFlatLoop(flat, concat(prefix, of(op.name)), op.config, split));
    }
    case OP_PRIMITIVE: {
      return pipe(patch4(prefix, flat.patch), flatMap3((prefix2) => pipe(flat.load(prefix2, op, split), flatMap3((values5) => {
        if (values5.length === 0) {
          const name = pipe(last(prefix2), getOrElse(() => "<n/a>"));
          return fail(MissingData([], `Expected ${op.description} with name ${name}`));
        }
        return succeed(values5);
      }))));
    }
    case OP_SEQUENCE: {
      return pipe(patch4(prefix, flat.patch), flatMap3((patchedPrefix) => pipe(flat.enumerateChildren(patchedPrefix), flatMap3(indicesFrom), flatMap3((indices) => {
        if (indices.length === 0) {
          return suspend(() => map3(fromFlatLoop(flat, prefix, op.config, true), of));
        }
        return pipe(forEachSequential(indices, (index) => fromFlatLoop(flat, append(prefix, `[${index}]`), op.config, true)), map3((chunkChunk) => {
          const flattened = flatten(chunkChunk);
          if (flattened.length === 0) {
            return of(empty());
          }
          return of(flattened);
        }));
      }))));
    }
    case OP_HASHMAP: {
      return suspend(() => pipe(patch4(prefix, flat.patch), flatMap3((prefix2) => pipe(flat.enumerateChildren(prefix2), flatMap3((keys4) => {
        return pipe(keys4, forEachSequential((key) => fromFlatLoop(flat, concat(prefix2, of(key)), op.valueConfig, split)), map3((matrix) => {
          if (matrix.length === 0) {
            return of(empty5());
          }
          return pipe(transpose(matrix), map2((values5) => fromIterable4(zip(fromIterable(keys4), values5))));
        }));
      })))));
    }
    case OP_ZIP_WITH: {
      return suspend(() => pipe(fromFlatLoop(flat, prefix, op.left, split), either, flatMap3((left2) => pipe(fromFlatLoop(flat, prefix, op.right, split), either, flatMap3((right2) => {
        if (isLeft(left2) && isLeft(right2)) {
          return fail(And(left2.left, right2.left));
        }
        if (isLeft(left2) && isRight(right2)) {
          return fail(left2.left);
        }
        if (isRight(left2) && isLeft(right2)) {
          return fail(right2.left);
        }
        if (isRight(left2) && isRight(right2)) {
          const path = pipe(prefix, join("."));
          const fail6 = fromFlatLoopFail(prefix, path);
          const [lefts, rights] = extend(fail6, fail6, pipe(left2.right, map2(right)), pipe(right2.right, map2(right)));
          return pipe(lefts, zip(rights), forEachSequential(([left3, right3]) => pipe(zip3(left3, right3), map3(([left4, right4]) => op.zip(left4, right4)))));
        }
        throw new Error("BUG: ConfigProvider.fromFlatLoop - please report an issue at https://github.com/Effect-TS/effect/issues");
      })))));
    }
  }
};
var fromFlatLoopFail = (prefix, path) => (index) => left(MissingData(prefix, `The element at index ${index} in a sequence at path "${path}" was missing`));
var mapInputPath = dual(2, (self, f) => fromFlat(mapInputPathFlat(self.flattened, f)));
var mapInputPathFlat = (self, f) => makeFlat({
  load: (path, config2, split = true) => self.load(path, config2, split),
  enumerateChildren: (path) => self.enumerateChildren(path),
  patch: mapName(self.patch, f)
});
var nested2 = dual(2, (self, name) => fromFlat(makeFlat({
  load: (path, config2) => self.flattened.load(path, config2, true),
  enumerateChildren: (path) => self.flattened.enumerateChildren(path),
  patch: nested(self.flattened.patch, name)
})));
var unnested2 = dual(2, (self, name) => fromFlat(makeFlat({
  load: (path, config2) => self.flattened.load(path, config2, true),
  enumerateChildren: (path) => self.flattened.enumerateChildren(path),
  patch: unnested(self.flattened.patch, name)
})));
var orElse2 = dual(2, (self, that) => fromFlat(orElseFlat(self.flattened, () => that().flattened)));
var orElseFlat = (self, that) => makeFlat({
  load: (path, config2, split) => pipe(patch4(path, self.patch), flatMap3((patch7) => self.load(patch7, config2, split)), catchAll((error1) => pipe(sync(that), flatMap3((that2) => pipe(patch4(path, that2.patch), flatMap3((patch7) => that2.load(patch7, config2, split)), catchAll((error2) => fail(Or(error1, error2)))))))),
  enumerateChildren: (path) => pipe(patch4(path, self.patch), flatMap3((patch7) => self.enumerateChildren(patch7)), either, flatMap3((left2) => pipe(sync(that), flatMap3((that2) => pipe(patch4(path, that2.patch), flatMap3((patch7) => that2.enumerateChildren(patch7)), either, flatMap3((right2) => {
    if (isLeft(left2) && isLeft(right2)) {
      return fail(And(left2.left, right2.left));
    }
    if (isLeft(left2) && isRight(right2)) {
      return succeed(right2.right);
    }
    if (isRight(left2) && isLeft(right2)) {
      return succeed(left2.right);
    }
    if (isRight(left2) && isRight(right2)) {
      return succeed(pipe(left2.right, union2(right2.right)));
    }
    throw new Error("BUG: ConfigProvider.orElseFlat - please report an issue at https://github.com/Effect-TS/effect/issues");
  })))))),
  patch: empty13
});
var constantCase2 = (self) => mapInputPath(self, constantCase);
var kebabCase2 = (self) => mapInputPath(self, kebabCase);
var lowerCase2 = (self) => mapInputPath(self, lowerCase);
var snakeCase2 = (self) => mapInputPath(self, snakeCase);
var upperCase2 = (self) => mapInputPath(self, upperCase);
var within = dual(3, (self, path, f) => {
  const unnest = reduce2(path, self, (provider, name) => unnested2(provider, name));
  const nest = reduceRight(path, f(unnest), (provider, name) => nested2(provider, name));
  return orElse2(nest, () => self);
});
var splitPathString = (text, delim) => {
  const split = text.split(new RegExp(`\\s*${escape(delim)}\\s*`));
  return split;
};
var parsePrimitive = (text, path, primitive, delimiter, split) => {
  if (!split) {
    return pipe(primitive.parse(text), mapBoth({
      onFailure: prefixed(path),
      onSuccess: of
    }));
  }
  return pipe(splitPathString(text, delimiter), forEachSequential((char) => primitive.parse(char.trim())), mapError(prefixed(path)));
};
var transpose = (array2) => {
  return Object.keys(array2[0]).map((column) => array2.map((row) => row[column]));
};
var indicesFrom = (quotedIndices) => pipe(forEachSequential(quotedIndices, parseQuotedIndex), mapBoth({
  onFailure: () => empty(),
  onSuccess: sort(Order2)
}), either, map3(merge));
var STR_INDEX_REGEX = /(^.+)(\[(\d+)\])$/;
var QUOTED_INDEX_REGEX = /^(\[(\d+)\])$/;
var parseQuotedIndex = (str) => {
  const match11 = str.match(QUOTED_INDEX_REGEX);
  if (match11 !== null) {
    const matchedIndex = match11[2];
    return pipe(matchedIndex !== void 0 && matchedIndex.length > 0 ? some2(matchedIndex) : none2(), flatMap(parseInteger));
  }
  return none2();
};
var splitIndexInKeys = (map10, unmakePathString, makePathString) => {
  const newMap = /* @__PURE__ */ new Map();
  for (const [pathString, value2] of map10) {
    const keyWithIndex = pipe(unmakePathString(pathString), flatMap2((key) => match(splitIndexFrom(key), {
      onNone: () => of(key),
      onSome: ([key2, index]) => make(key2, `[${index}]`)
    })));
    newMap.set(makePathString(keyWithIndex), value2);
  }
  return newMap;
};
var splitIndexFrom = (key) => {
  const match11 = key.match(STR_INDEX_REGEX);
  if (match11 !== null) {
    const matchedString = match11[1];
    const matchedIndex = match11[3];
    const optionalString = matchedString !== void 0 && matchedString.length > 0 ? some2(matchedString) : none2();
    const optionalIndex = pipe(matchedIndex !== void 0 && matchedIndex.length > 0 ? some2(matchedIndex) : none2(), flatMap(parseInteger));
    return all([optionalString, optionalIndex]);
  }
  return none2();
};
var parseInteger = (str) => {
  const parsedIndex = Number.parseInt(str);
  return Number.isNaN(parsedIndex) ? none2() : some2(parsedIndex);
};
var keyName = (name) => ({
  _tag: "KeyName",
  name
});
var keyIndex = (index) => ({
  _tag: "KeyIndex",
  index
});
var fromJson = (json) => {
  const hiddenDelimiter = "\uFEFF";
  const indexedEntries = map2(getIndexedEntries(json), ([key, value2]) => [configPathToString(key).join(hiddenDelimiter), value2]);
  return fromMap(new Map(indexedEntries), {
    pathDelim: hiddenDelimiter,
    seqDelim: hiddenDelimiter
  });
};
var configPathToString = (path) => {
  const output = [];
  let i = 0;
  while (i < path.length) {
    const component = path[i];
    if (component._tag === "KeyName") {
      if (i + 1 < path.length) {
        const nextComponent = path[i + 1];
        if (nextComponent._tag === "KeyIndex") {
          output.push(`${component.name}[${nextComponent.index}]`);
          i += 2;
        } else {
          output.push(component.name);
          i += 1;
        }
      } else {
        output.push(component.name);
        i += 1;
      }
    }
  }
  return output;
};
var getIndexedEntries = (config2) => {
  const loopAny = (path, value2) => {
    if (typeof value2 === "string") {
      return make([path, value2]);
    }
    if (typeof value2 === "number" || typeof value2 === "boolean") {
      return make([path, String(value2)]);
    }
    if (isArray(value2)) {
      return loopArray(path, value2);
    }
    if (typeof value2 === "object" && value2 !== null) {
      return loopObject(path, value2);
    }
    return empty();
  };
  const loopArray = (path, values5) => match2(values5, {
    onEmpty: () => make([path, "<nil>"]),
    onNonEmpty: flatMap2((value2, index) => loopAny(append(path, keyIndex(index)), value2))
  });
  const loopObject = (path, value2) => Object.entries(value2).filter(([, value3]) => isNotNullable(value3)).flatMap(([key, value3]) => {
    const newPath = append(path, keyName(key));
    const result = loopAny(newPath, value3);
    if (isEmptyReadonlyArray(result)) {
      return make([newPath, ""]);
    }
    return result;
  });
  return loopObject(empty(), config2);
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/defaultServices/console.js
var TypeId3 = /* @__PURE__ */ Symbol.for("effect/Console");
var consoleTag = GenericTag("effect/Console");
var defaultConsole = {
  [TypeId3]: TypeId3,
  assert(condition, ...args2) {
    return sync(() => {
      console.assert(condition, ...args2);
    });
  },
  clear: sync(() => {
    console.clear();
  }),
  count(label) {
    return sync(() => {
      console.count(label);
    });
  },
  countReset(label) {
    return sync(() => {
      console.countReset(label);
    });
  },
  debug(...args2) {
    return sync(() => {
      console.debug(...args2);
    });
  },
  dir(item, options) {
    return sync(() => {
      console.dir(item, options);
    });
  },
  dirxml(...args2) {
    return sync(() => {
      console.dirxml(...args2);
    });
  },
  error(...args2) {
    return sync(() => {
      console.error(...args2);
    });
  },
  group(options) {
    return options?.collapsed ? sync(() => console.groupCollapsed(options?.label)) : sync(() => console.group(options?.label));
  },
  groupEnd: sync(() => {
    console.groupEnd();
  }),
  info(...args2) {
    return sync(() => {
      console.info(...args2);
    });
  },
  log(...args2) {
    return sync(() => {
      console.log(...args2);
    });
  },
  table(tabularData, properties) {
    return sync(() => {
      console.table(tabularData, properties);
    });
  },
  time(label) {
    return sync(() => console.time(label));
  },
  timeEnd(label) {
    return sync(() => console.timeEnd(label));
  },
  timeLog(label, ...args2) {
    return sync(() => {
      console.timeLog(label, ...args2);
    });
  },
  trace(...args2) {
    return sync(() => {
      console.trace(...args2);
    });
  },
  warn(...args2) {
    return sync(() => {
      console.warn(...args2);
    });
  },
  unsafe: console
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/random.js
var RandomSymbolKey = "effect/Random";
var RandomTypeId = Symbol.for(RandomSymbolKey);
var randomTag = GenericTag("effect/Random");
var RandomImpl = class {
  seed;
  [RandomTypeId] = RandomTypeId;
  PRNG;
  constructor(seed) {
    this.seed = seed;
    this.PRNG = new PCGRandom(seed);
  }
  get next() {
    return sync(() => this.PRNG.number());
  }
  get nextBoolean() {
    return map3(this.next, (n) => n > 0.5);
  }
  get nextInt() {
    return sync(() => this.PRNG.integer(Number.MAX_SAFE_INTEGER));
  }
  nextRange(min3, max3) {
    return map3(this.next, (n) => (max3 - min3) * n + min3);
  }
  nextIntBetween(min3, max3) {
    return sync(() => this.PRNG.integer(max3 - min3) + min3);
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var shuffleWith = (elements, nextIntBounded) => {
  return suspend(() => pipe(sync(() => Array.from(elements)), flatMap3((buffer) => {
    const numbers = [];
    for (let i = buffer.length; i >= 2; i = i - 1) {
      numbers.push(i);
    }
    return pipe(numbers, forEachSequentialDiscard((n) => pipe(nextIntBounded(n), map3((k) => swap(buffer, n - 1, k)))), as(fromIterable2(buffer)));
  })));
};
var swap = (buffer, index1, index2) => {
  const tmp = buffer[index1];
  buffer[index1] = buffer[index2];
  buffer[index2] = tmp;
  return buffer;
};
var make14 = (seed) => new RandomImpl(hash(seed));
var FixedRandomImpl = class {
  values;
  [RandomTypeId] = RandomTypeId;
  index = 0;
  constructor(values5) {
    this.values = values5;
    if (values5.length === 0) {
      throw new Error("Requires at least one value");
    }
  }
  getNextValue() {
    const value2 = this.values[this.index];
    this.index = (this.index + 1) % this.values.length;
    return value2;
  }
  get next() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number") {
        return Math.max(0, Math.min(1, value2));
      }
      return hash(value2) / 2147483647;
    });
  }
  get nextBoolean() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "boolean") {
        return value2;
      }
      return hash(value2) % 2 === 0;
    });
  }
  get nextInt() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number" && Number.isFinite(value2)) {
        return Math.round(value2);
      }
      return Math.abs(hash(value2));
    });
  }
  nextRange(min3, max3) {
    return map3(this.next, (n) => (max3 - min3) * n + min3);
  }
  nextIntBetween(min3, max3) {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number" && Number.isFinite(value2)) {
        return Math.max(min3, Math.min(max3 - 1, Math.round(value2)));
      }
      const hash2 = Math.abs(hash(value2));
      return min3 + hash2 % (max3 - min3);
    });
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var fixed = (values5) => new FixedRandomImpl(values5);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/defaultServices.js
var liveServices = pipe(empty3(), add(clockTag, make12()), add(consoleTag, defaultConsole), add(randomTag, make14(Math.random())), add(configProviderTag, fromEnv()), add(tracerTag, nativeTracer));
var currentServices = globalValue(/* @__PURE__ */ Symbol.for("effect/DefaultServices/currentServices"), () => fiberRefUnsafeMakeContext(liveServices));
var sleep2 = (duration) => {
  const decodedDuration = decode(duration);
  return clockWith((clock2) => clock2.sleep(decodedDuration));
};
var defaultServicesWith = (f) => withFiberRuntime((fiber) => f(fiber.currentDefaultServices));
var clockWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(clockTag.key)));
var currentTimeMillis = clockWith((clock2) => clock2.currentTimeMillis);
var currentTimeNanos = clockWith((clock2) => clock2.currentTimeNanos);
var withClock = dual(2, (effect, c) => fiberRefLocallyWith(currentServices, add(clockTag, c))(effect));
var withConfigProvider = dual(2, (self, provider) => fiberRefLocallyWith(currentServices, add(configProviderTag, provider))(self));
var configProviderWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(configProviderTag.key)));
var config = (config2) => configProviderWith((_) => _.load(config2));
var randomWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(randomTag.key)));
var withRandom = dual(2, (effect, value2) => fiberRefLocallyWith(currentServices, add(randomTag, value2))(effect));
var next = randomWith((random2) => random2.next);
var nextInt = randomWith((random2) => random2.nextInt);
var nextBoolean = randomWith((random2) => random2.nextBoolean);
var nextRange = (min3, max3) => randomWith((random2) => random2.nextRange(min3, max3));
var nextIntBetween = (min3, max3) => randomWith((random2) => random2.nextIntBetween(min3, max3));
var shuffle = (elements) => randomWith((random2) => random2.shuffle(elements));
var choice = (elements) => {
  const array2 = fromIterable(elements);
  return map3(array2.length === 0 ? fail(new NoSuchElementException("Cannot select a random element from an empty array")) : randomWith((random2) => random2.nextIntBetween(0, array2.length)), (i) => array2[i]);
};
var tracerWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(tracerTag.key)));
var withTracer = dual(2, (effect, value2) => fiberRefLocallyWith(currentServices, add(tracerTag, value2))(effect));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Clock.js
var Clock_exports = {};
__export(Clock_exports, {
  Clock: () => Clock,
  ClockTypeId: () => ClockTypeId2,
  clockWith: () => clockWith2,
  currentTimeMillis: () => currentTimeMillis2,
  currentTimeNanos: () => currentTimeNanos2,
  make: () => make15,
  sleep: () => sleep3
});
var ClockTypeId2 = ClockTypeId;
var make15 = make12;
var sleep3 = sleep2;
var currentTimeMillis2 = currentTimeMillis;
var currentTimeNanos2 = currentTimeNanos;
var clockWith2 = clockWith;
var Clock = clockTag;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LogSpan.js
var LogSpan_exports = {};
__export(LogSpan_exports, {
  make: () => make17,
  render: () => render2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/logSpan.js
var make16 = (label, startTime) => ({
  label,
  startTime
});
var formatLabel = (key) => key.replace(/[\s="]/g, "_");
var render = (now) => (self) => {
  const label = formatLabel(self.label);
  return `${label}=${now - self.startTime}ms`;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LogSpan.js
var make17 = make16;
var render2 = render;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Tracer.js
var Tracer_exports = {};
__export(Tracer_exports, {
  DisablePropagation: () => DisablePropagation2,
  ParentSpan: () => ParentSpan,
  Tracer: () => Tracer,
  TracerTypeId: () => TracerTypeId2,
  externalSpan: () => externalSpan2,
  make: () => make18,
  tracerWith: () => tracerWith2
});
var TracerTypeId2 = TracerTypeId;
var ParentSpan = spanTag;
var Tracer = tracerTag;
var make18 = make11;
var externalSpan2 = externalSpan;
var tracerWith2 = tracerWith;
var DisablePropagation2 = DisablePropagation;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/label.js
var MetricLabelSymbolKey = "effect/MetricLabel";
var MetricLabelTypeId = Symbol.for(MetricLabelSymbolKey);
var MetricLabelImpl = class {
  key;
  value;
  [MetricLabelTypeId] = MetricLabelTypeId;
  _hash;
  constructor(key, value2) {
    this.key = key;
    this.value = value2;
    this._hash = string(MetricLabelSymbolKey + this.key + this.value);
  }
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isMetricLabel(that) && this.key === that.key && this.value === that.value;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make19 = (key, value2) => {
  return new MetricLabelImpl(key, value2);
};
var isMetricLabel = (u) => hasProperty(u, MetricLabelTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/core-effect.js
var annotateLogs = dual((args2) => isEffect(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith(args2[0], currentLogAnnotations, typeof args2[1] === "string" ? set2(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set2(acc, key, value2), annotations));
});
var asSome2 = (self) => map3(self, some2);
var asSomeError = (self) => mapError(self, some2);
var try_2 = (arg) => {
  let evaluate2;
  let onFailure = void 0;
  if (typeof arg === "function") {
    evaluate2 = arg;
  } else {
    evaluate2 = arg.try;
    onFailure = arg.catch;
  }
  return suspend(() => {
    try {
      return succeed(internalCall(evaluate2));
    } catch (error) {
      return fail(onFailure ? internalCall(() => onFailure(error)) : new UnknownException(error, "An unknown error occurred in Effect.try"));
    }
  });
};
var _catch = dual(3, (self, tag, options) => catchAll(self, (e) => {
  if (hasProperty(e, tag) && e[tag] === options.failure) {
    return options.onFailure(e);
  }
  return fail(e);
}));
var catchAllDefect2 = dual(2, (self, f) => catchAllCause(self, (cause2) => {
  const option3 = find(cause2, (_) => isDieType(_) ? some2(_) : none2());
  switch (option3._tag) {
    case "None": {
      return failCause(cause2);
    }
    case "Some": {
      return f(option3.value.defect);
    }
  }
}));
var catchSomeCause = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const option3 = f(cause2);
    switch (option3._tag) {
      case "None": {
        return failCause(cause2);
      }
      case "Some": {
        return option3.value;
      }
    }
  },
  onSuccess: succeed
}));
var catchSomeDefect = dual(2, (self, pf) => catchAllCause(self, (cause2) => {
  const option3 = find(cause2, (_) => isDieType(_) ? some2(_) : none2());
  switch (option3._tag) {
    case "None": {
      return failCause(cause2);
    }
    case "Some": {
      const optionEffect = pf(option3.value.defect);
      return optionEffect._tag === "Some" ? optionEffect.value : failCause(cause2);
    }
  }
}));
var catchTag2 = dual((args2) => isEffect(args2[0]), (self, ...args2) => {
  const f = args2[args2.length - 1];
  let predicate;
  if (args2.length === 2) {
    predicate = isTagged(args2[0]);
  } else {
    predicate = (e) => {
      const tag = hasProperty(e, "_tag") ? e["_tag"] : void 0;
      if (!tag) return false;
      for (let i = 0; i < args2.length - 1; i++) {
        if (args2[i] === tag) return true;
      }
      return false;
    };
  }
  return catchIf(self, predicate, f);
});
var catchTags = dual(2, (self, cases) => {
  let keys4;
  return catchIf(self, (e) => {
    keys4 ??= Object.keys(cases);
    return hasProperty(e, "_tag") && isString(e["_tag"]) && keys4.includes(e["_tag"]);
  }, (e) => cases[e["_tag"]](e));
});
var cause = (self) => matchCause(self, {
  onFailure: identity,
  onSuccess: () => empty8
});
var clockWith3 = clockWith2;
var clock = clockWith3(succeed);
var delay2 = dual(2, (self, duration) => zipRight(sleep3(duration), self));
var descriptorWith = (f) => withFiberRuntime((state, status2) => f({
  id: state.id(),
  status: status2,
  interruptors: interruptors(state.getFiberRef(currentInterruptedCause))
}));
var allowInterrupt = descriptorWith((descriptor2) => size(descriptor2.interruptors) > 0 ? interrupt2 : void_);
var descriptor = descriptorWith(succeed);
var diffFiberRefs = (self) => summarized(self, fiberRefs3, diff2);
var diffFiberRefsAndRuntimeFlags = (self) => summarized(self, zip3(fiberRefs3, runtimeFlags), ([refs, flags], [refsNew, flagsNew]) => [diff2(refs, refsNew), diff(flags, flagsNew)]);
var Do2 = succeed({});
var bind3 = bind(map3, flatMap3);
var bindTo3 = bindTo(map3);
var let_3 = let_(map3);
var dropUntil = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let dropping = succeed(false);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    dropping = flatMap3(dropping, (bool) => {
      if (bool) {
        builder.push(a);
        return succeed(true);
      }
      return predicate(a, index);
    });
  }
  return map3(dropping, () => builder);
}));
var dropWhile = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let dropping = succeed(true);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    dropping = flatMap3(dropping, (d) => map3(d ? predicate(a, index) : succeed(false), (b) => {
      if (!b) {
        builder.push(a);
      }
      return b;
    }));
  }
  return map3(dropping, () => builder);
}));
var contextWith = (f) => map3(context(), f);
var eventually = (self) => orElse(self, () => flatMap3(yieldNow(), () => eventually(self)));
var filterMap3 = dual(2, (elements, pf) => map3(forEachSequential(elements, identity), filterMap(pf)));
var filterOrDie = dual(3, (self, predicate, orDieWith) => filterOrElse(self, predicate, (a) => dieSync(() => orDieWith(a))));
var filterOrDieMessage = dual(3, (self, predicate, message) => filterOrElse(self, predicate, () => dieMessage(message)));
var filterOrElse = dual(3, (self, predicate, orElse4) => flatMap3(self, (a) => predicate(a) ? succeed(a) : orElse4(a)));
var liftPredicate = dual(3, (self, predicate, orFailWith) => suspend(() => predicate(self) ? succeed(self) : fail(orFailWith(self))));
var filterOrFail2 = dual((args2) => isEffect(args2[0]), (self, predicate, orFailWith) => filterOrElse(self, predicate, (a) => orFailWith === void 0 ? fail(new NoSuchElementException()) : failSync(() => orFailWith(a))));
var findFirst = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const next2 = iterator.next();
  if (!next2.done) {
    return findLoop(iterator, 0, predicate, next2.value);
  }
  return succeed(none2());
}));
var findLoop = (iterator, index, f, value2) => flatMap3(f(value2, index), (result) => {
  if (result) {
    return succeed(some2(value2));
  }
  const next2 = iterator.next();
  if (!next2.done) {
    return findLoop(iterator, index + 1, f, next2.value);
  }
  return succeed(none2());
});
var firstSuccessOf = (effects) => suspend(() => {
  const list = fromIterable2(effects);
  if (!isNonEmpty(list)) {
    return dieSync(() => new IllegalArgumentException(`Received an empty collection of effects`));
  }
  return pipe(tailNonEmpty2(list), reduce2(headNonEmpty2(list), (left2, right2) => orElse(left2, () => right2)));
});
var flipWith = dual(2, (self, f) => flip(f(flip(self))));
var match8 = dual(2, (self, options) => matchEffect(self, {
  onFailure: (e) => succeed(options.onFailure(e)),
  onSuccess: (a) => succeed(options.onSuccess(a))
}));
var every2 = dual(2, (elements, predicate) => suspend(() => forAllLoop(elements[Symbol.iterator](), 0, predicate)));
var forAllLoop = (iterator, index, f) => {
  const next2 = iterator.next();
  return next2.done ? succeed(true) : flatMap3(f(next2.value, index), (b) => b ? forAllLoop(iterator, index + 1, f) : succeed(b));
};
var forever2 = (self) => {
  const loop2 = flatMap3(flatMap3(self, () => yieldNow()), () => loop2);
  return loop2;
};
var fiberRefs3 = withFiberRuntime((state) => succeed(state.getFiberRefs()));
var head2 = (self) => flatMap3(self, (as3) => {
  const iterator = as3[Symbol.iterator]();
  const next2 = iterator.next();
  if (next2.done) {
    return fail(new NoSuchElementException());
  }
  return succeed(next2.value);
});
var ignore2 = (self) => match8(self, {
  onFailure: constVoid,
  onSuccess: constVoid
});
var ignoreLogged2 = (self) => matchCauseEffect(self, {
  onFailure: (cause2) => logDebug(cause2, "An error was silently ignored because it is not anticipated to be useful"),
  onSuccess: () => void_
});
var inheritFiberRefs = (childFiberRefs) => updateFiberRefs((parentFiberId, parentFiberRefs) => joinAs2(parentFiberRefs, parentFiberId, childFiberRefs));
var isFailure = (self) => match8(self, {
  onFailure: constTrue,
  onSuccess: constFalse
});
var isSuccess2 = (self) => match8(self, {
  onFailure: constFalse,
  onSuccess: constTrue
});
var iterate = (initial, options) => suspend(() => {
  if (options.while(initial)) {
    return flatMap3(options.body(initial), (z2) => iterate(z2, options));
  }
  return succeed(initial);
});
var logWithLevel = (level) => (...message) => {
  const levelOption = fromNullable(level);
  let cause2 = void 0;
  for (let i = 0, len = message.length; i < len; i++) {
    const msg = message[i];
    if (isCause(msg)) {
      if (cause2 !== void 0) {
        cause2 = sequential(cause2, msg);
      } else {
        cause2 = msg;
      }
      message = [...message.slice(0, i), ...message.slice(i + 1)];
      i--;
    }
  }
  if (cause2 === void 0) {
    cause2 = empty8;
  }
  return withFiberRuntime((fiberState) => {
    fiberState.log(message, cause2, levelOption);
    return void_;
  });
};
var log = logWithLevel();
var logTrace = logWithLevel(Trace);
var logDebug = logWithLevel(Debug);
var logInfo = logWithLevel(Info);
var logWarning = logWithLevel(Warning);
var logError = logWithLevel(Error2);
var logFatal = logWithLevel(Fatal);
var withLogSpan = dual(2, (effect, label) => flatMap3(currentTimeMillis2, (now) => fiberRefLocallyWith(effect, currentLogSpan, prepend3(make17(label, now)))));
var logAnnotations = fiberRefGet(currentLogAnnotations);
var loop = (initial, options) => options.discard ? loopDiscard(initial, options.while, options.step, options.body) : map3(loopInternal(initial, options.while, options.step, options.body), fromIterable);
var loopInternal = (initial, cont, inc, body) => suspend(() => cont(initial) ? flatMap3(body(initial), (a) => map3(loopInternal(inc(initial), cont, inc, body), prepend3(a))) : sync(() => empty6()));
var loopDiscard = (initial, cont, inc, body) => suspend(() => cont(initial) ? flatMap3(body(initial), () => loopDiscard(inc(initial), cont, inc, body)) : void_);
var mapAccum = dual(3, (elements, initial, f) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let result = succeed(initial);
  let next2;
  let i = 0;
  while (!(next2 = iterator.next()).done) {
    const index = i++;
    const value2 = next2.value;
    result = flatMap3(result, (state) => map3(f(state, value2, index), ([z, b]) => {
      builder.push(b);
      return z;
    }));
  }
  return map3(result, (z) => [z, builder]);
}));
var mapErrorCause2 = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (c) => failCauseSync(() => f(c)),
  onSuccess: succeed
}));
var memoize = (self) => pipe(deferredMake(), flatMap3((deferred) => pipe(diffFiberRefsAndRuntimeFlags(self), intoDeferred(deferred), once, map3((complete2) => zipRight(complete2, pipe(deferredAwait(deferred), flatMap3(([patch7, a]) => as(zip3(patchFiberRefs(patch7[0]), updateRuntimeFlags(patch7[1])), a))))))));
var merge3 = (self) => matchEffect(self, {
  onFailure: (e) => succeed(e),
  onSuccess: succeed
});
var negate2 = (self) => map3(self, (b) => !b);
var none5 = (self) => flatMap3(self, (option3) => {
  switch (option3._tag) {
    case "None":
      return void_;
    case "Some":
      return fail(new NoSuchElementException());
  }
});
var once = (self) => map3(make9(true), (ref) => asVoid(whenEffect(self, getAndSet2(ref, false))));
var option2 = (self) => matchEffect(self, {
  onFailure: () => succeed(none2()),
  onSuccess: (a) => succeed(some2(a))
});
var orElseFail = dual(2, (self, evaluate2) => orElse(self, () => failSync(evaluate2)));
var orElseSucceed2 = dual(2, (self, evaluate2) => orElse(self, () => sync(evaluate2)));
var parallelErrors = (self) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const errors = fromIterable(failures(cause2));
    return errors.length === 0 ? failCause(cause2) : fail(errors);
  },
  onSuccess: succeed
});
var patchFiberRefs = (patch7) => updateFiberRefs((fiberId2, fiberRefs4) => pipe(patch7, patch2(fiberId2, fiberRefs4)));
var promise2 = (evaluate2) => evaluate2.length >= 1 ? async_((resolve, signal) => {
  try {
    evaluate2(signal).then((a) => resolve(succeed(a)), (e) => resolve(die2(e)));
  } catch (e) {
    resolve(die2(e));
  }
}) : async_((resolve) => {
  try {
    ;
    evaluate2().then((a) => resolve(succeed(a)), (e) => resolve(die2(e)));
  } catch (e) {
    resolve(die2(e));
  }
});
var provideService2 = dual(3, (self, tag, service2) => contextWithEffect((env) => provideContext(self, add(env, tag, service2))));
var provideServiceEffect2 = dual(3, (self, tag, effect) => contextWithEffect((env) => flatMap3(effect, (service2) => provideContext(self, pipe(env, add(tag, service2))))));
var random = randomWith(succeed);
var reduce4 = dual(3, (elements, zero2, f) => fromIterable(elements).reduce((acc, el, i) => flatMap3(acc, (a) => f(a, el, i)), succeed(zero2)));
var reduceRight2 = dual(3, (elements, zero2, f) => fromIterable(elements).reduceRight((acc, el, i) => flatMap3(acc, (a) => f(el, a, i)), succeed(zero2)));
var reduceWhile = dual(3, (elements, zero2, options) => flatMap3(sync(() => elements[Symbol.iterator]()), (iterator) => reduceWhileLoop(iterator, 0, zero2, options.while, options.body)));
var reduceWhileLoop = (iterator, index, state, predicate, f) => {
  const next2 = iterator.next();
  if (!next2.done && predicate(state)) {
    return flatMap3(f(state, next2.value, index), (nextState) => reduceWhileLoop(iterator, index + 1, nextState, predicate, f));
  }
  return succeed(state);
};
var repeatN = dual(2, (self, n) => suspend(() => repeatNLoop(self, n)));
var repeatNLoop = (self, n) => flatMap3(self, (a) => n <= 0 ? succeed(a) : zipRight(yieldNow(), repeatNLoop(self, n - 1)));
var sandbox2 = (self) => matchCauseEffect(self, {
  onFailure: fail,
  onSuccess: succeed
});
var setFiberRefs = (fiberRefs4) => suspend(() => setAll2(fiberRefs4));
var sleep4 = sleep3;
var succeedNone2 = succeed(none2());
var succeedSome2 = (value2) => succeed(some2(value2));
var summarized = dual(3, (self, summary6, f) => flatMap3(summary6, (start) => flatMap3(self, (value2) => map3(summary6, (end) => [f(start, end), value2]))));
var tagMetrics = dual((args2) => isEffect(args2[0]), function() {
  return labelMetrics(arguments[0], typeof arguments[1] === "string" ? [make19(arguments[1], arguments[2])] : Object.entries(arguments[1]).map(([k, v]) => make19(k, v)));
});
var labelMetrics = dual(2, (self, labels) => fiberRefLocallyWith(self, currentMetricLabels, (old) => union(old, labels)));
var takeUntil = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let effect = succeed(false);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    effect = flatMap3(effect, (bool) => {
      if (bool) {
        return succeed(true);
      }
      builder.push(a);
      return predicate(a, index);
    });
  }
  return map3(effect, () => builder);
}));
var takeWhile = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let taking = succeed(true);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    taking = flatMap3(taking, (taking2) => pipe(taking2 ? predicate(a, index) : succeed(false), map3((bool) => {
      if (bool) {
        builder.push(a);
      }
      return bool;
    })));
  }
  return map3(taking, () => builder);
}));
var tapBoth = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const either3 = failureOrCause(cause2);
    switch (either3._tag) {
      case "Left": {
        return zipRight(onFailure(either3.left), failCause(cause2));
      }
      case "Right": {
        return failCause(cause2);
      }
    }
  },
  onSuccess: (a) => as(onSuccess(a), a)
}));
var tapDefect2 = dual(2, (self, f) => catchAllCause(self, (cause2) => match(keepDefects(cause2), {
  onNone: () => failCause(cause2),
  onSome: (a) => zipRight(f(a), failCause(cause2))
})));
var tapError2 = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const either3 = failureOrCause(cause2);
    switch (either3._tag) {
      case "Left":
        return zipRight(f(either3.left), failCause(cause2));
      case "Right":
        return failCause(cause2);
    }
  },
  onSuccess: succeed
}));
var tapErrorTag = dual(3, (self, k, f) => tapError2(self, (e) => {
  if (isTagged(e, k)) {
    return f(e);
  }
  return void_;
}));
var tapErrorCause2 = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause2) => zipRight(f(cause2), failCause(cause2)),
  onSuccess: succeed
}));
var timed = (self) => timedWith(self, currentTimeNanos2);
var timedWith = dual(2, (self, nanos2) => summarized(self, nanos2, (start, end) => nanos(end - start)));
var tracerWith3 = tracerWith2;
var tracer = tracerWith3(succeed);
var tryPromise2 = (arg) => {
  let evaluate2;
  let catcher = void 0;
  if (typeof arg === "function") {
    evaluate2 = arg;
  } else {
    evaluate2 = arg.try;
    catcher = arg.catch;
  }
  const fail6 = (e) => catcher ? failSync(() => catcher(e)) : fail(new UnknownException(e, "An unknown error occurred in Effect.tryPromise"));
  if (evaluate2.length >= 1) {
    return async_((resolve, signal) => {
      try {
        evaluate2(signal).then((a) => resolve(succeed(a)), (e) => resolve(fail6(e)));
      } catch (e) {
        resolve(fail6(e));
      }
    });
  }
  return async_((resolve) => {
    try {
      evaluate2().then((a) => resolve(succeed(a)), (e) => resolve(fail6(e)));
    } catch (e) {
      resolve(fail6(e));
    }
  });
};
var tryMap = dual(2, (self, options) => flatMap3(self, (a) => try_2({
  try: () => options.try(a),
  catch: options.catch
})));
var tryMapPromise = dual(2, (self, options) => flatMap3(self, (a) => tryPromise2({
  try: options.try.length >= 1 ? (signal) => options.try(a, signal) : () => options.try(a),
  catch: options.catch
})));
var unless = dual(2, (self, condition) => suspend(() => condition() ? succeedNone2 : asSome2(self)));
var unlessEffect = dual(2, (self, condition) => flatMap3(condition, (b) => b ? succeedNone2 : asSome2(self)));
var unsandbox = (self) => mapErrorCause2(self, flatten3);
var updateFiberRefs = (f) => withFiberRuntime((state) => {
  state.setFiberRefs(f(state.id(), state.getFiberRefs()));
  return void_;
});
var updateService2 = dual(3, (self, tag, f) => mapInputContext(self, (context3) => add(context3, tag, f(unsafeGet2(context3, tag)))));
var when2 = dual(2, (self, condition) => suspend(() => condition() ? map3(self, some2) : succeed(none2())));
var whenFiberRef = dual(3, (self, fiberRef, predicate) => flatMap3(fiberRefGet(fiberRef), (s) => predicate(s) ? map3(self, (a) => [s, some2(a)]) : succeed([s, none2()])));
var whenRef = dual(3, (self, ref, predicate) => flatMap3(get6(ref), (s) => predicate(s) ? map3(self, (a) => [s, some2(a)]) : succeed([s, none2()])));
var withMetric = dual(2, (self, metric) => metric(self));
var serviceFunctionEffect = (getService, f) => (...args2) => flatMap3(getService, (a) => f(a)(...args2));
var serviceFunction = (getService, f) => (...args2) => map3(getService, (a) => f(a)(...args2));
var serviceFunctions = (getService) => new Proxy({}, {
  get(_target, prop, _receiver) {
    return (...args2) => flatMap3(getService, (s) => s[prop](...args2));
  }
});
var serviceConstants = (getService) => new Proxy({}, {
  get(_target, prop, _receiver) {
    return flatMap3(getService, (s) => isEffect(s[prop]) ? s[prop] : succeed(s[prop]));
  }
});
var serviceMembers = (getService) => ({
  functions: serviceFunctions(getService),
  constants: serviceConstants(getService)
});
var serviceOption2 = (tag) => map3(context(), getOption(tag));
var serviceOptional = (tag) => flatMap3(context(), getOption(tag));
var annotateCurrentSpan = function() {
  const args2 = arguments;
  return ignore2(flatMap3(currentSpan, (span2) => sync(() => {
    if (typeof args2[0] === "string") {
      span2.attribute(args2[0], args2[1]);
    } else {
      for (const key in args2[0]) {
        span2.attribute(key, args2[0][key]);
      }
    }
  })));
};
var linkSpanCurrent = function() {
  const args2 = arguments;
  const links = Array.isArray(args2[0]) ? args2[0] : [{
    _tag: "SpanLink",
    span: args2[0],
    attributes: args2[1] ?? {}
  }];
  return ignore2(flatMap3(currentSpan, (span2) => sync(() => span2.addLinks(links))));
};
var annotateSpans = dual((args2) => isEffect(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith(args2[0], currentTracerSpanAnnotations, typeof args2[1] === "string" ? set2(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set2(acc, key, value2), annotations));
});
var currentParentSpan = serviceOptional(spanTag);
var currentSpan = flatMap3(context(), (context3) => {
  const span2 = context3.unsafeMap.get(spanTag.key);
  return span2 !== void 0 && span2._tag === "Span" ? succeed(span2) : fail(new NoSuchElementException());
});
var linkSpans = dual((args2) => isEffect(args2[0]), (self, span2, attributes) => fiberRefLocallyWith(self, currentTracerSpanLinks, append2({
  _tag: "SpanLink",
  span: span2,
  attributes: attributes ?? {}
})));
var bigint0 = BigInt(0);
var filterDisablePropagation = flatMap((span2) => get2(span2.context, DisablePropagation) ? span2._tag === "Span" ? filterDisablePropagation(span2.parent) : none2() : some2(span2));
var unsafeMakeSpan = (fiber, name, options) => {
  const disablePropagation = !fiber.getFiberRef(currentTracerEnabled) || options.context && get2(options.context, DisablePropagation);
  const context3 = fiber.getFiberRef(currentContext);
  const parent = options.parent ? some2(options.parent) : options.root ? none2() : filterDisablePropagation(getOption(context3, spanTag));
  let span2;
  if (disablePropagation) {
    span2 = noopSpan({
      name,
      parent,
      context: add(options.context ?? empty3(), DisablePropagation, true)
    });
  } else {
    const services = fiber.getFiberRef(currentServices);
    const tracer2 = get2(services, tracerTag);
    const clock2 = get2(services, Clock);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const fiberRefs4 = fiber.getFiberRefs();
    const annotationsFromEnv = get4(fiberRefs4, currentTracerSpanAnnotations);
    const linksFromEnv = get4(fiberRefs4, currentTracerSpanLinks);
    const links = linksFromEnv._tag === "Some" ? options.links !== void 0 ? [...toReadonlyArray(linksFromEnv.value), ...options.links ?? []] : toReadonlyArray(linksFromEnv.value) : options.links ?? empty();
    span2 = tracer2.span(name, parent, options.context ?? empty3(), links, timingEnabled ? clock2.unsafeCurrentTimeNanos() : bigint0, options.kind ?? "internal", options);
    if (annotationsFromEnv._tag === "Some") {
      forEach(annotationsFromEnv.value, (value2, key) => span2.attribute(key, value2));
    }
    if (options.attributes !== void 0) {
      Object.entries(options.attributes).forEach(([k, v]) => span2.attribute(k, v));
    }
  }
  if (typeof options.captureStackTrace === "function") {
    spanToTrace.set(span2, options.captureStackTrace);
  }
  return span2;
};
var makeSpan = (name, options) => {
  options = addSpanStackTrace(options);
  return withFiberRuntime((fiber) => succeed(unsafeMakeSpan(fiber, name, options)));
};
var spanAnnotations = fiberRefGet(currentTracerSpanAnnotations);
var spanLinks = fiberRefGet(currentTracerSpanLinks);
var endSpan = (span2, exit3, clock2, timingEnabled) => sync(() => {
  if (span2.status._tag === "Ended") {
    return;
  }
  if (exitIsFailure(exit3) && spanToTrace.has(span2)) {
    span2.attribute("code.stacktrace", spanToTrace.get(span2)());
  }
  span2.end(timingEnabled ? clock2.unsafeCurrentTimeNanos() : bigint0, exit3);
});
var useSpan = (name, ...args2) => {
  const options = addSpanStackTrace(args2.length === 1 ? void 0 : args2[0]);
  const evaluate2 = args2[args2.length - 1];
  return withFiberRuntime((fiber) => {
    const span2 = unsafeMakeSpan(fiber, name, options);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const clock2 = get2(fiber.getFiberRef(currentServices), clockTag);
    return onExit(evaluate2(span2), (exit3) => endSpan(span2, exit3, clock2, timingEnabled));
  });
};
var withParentSpan = dual(2, (self, span2) => provideService2(self, spanTag, span2));
var withSpan = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return useSpan(name, options, (span2) => withParentSpan(self, span2));
  }
  return (self) => useSpan(name, options, (span2) => withParentSpan(self, span2));
};
var functionWithSpan = (options) => function() {
  let captureStackTrace = options.captureStackTrace ?? false;
  if (options.captureStackTrace !== false) {
    const limit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const error = new Error();
    Error.stackTraceLimit = limit;
    let cache = false;
    captureStackTrace = () => {
      if (cache !== false) {
        return cache;
      }
      if (error.stack) {
        const stack = error.stack.trim().split("\n");
        cache = stack.slice(2).join("\n").trim();
        return cache;
      }
    };
  }
  return suspend(() => {
    const opts = typeof options.options === "function" ? options.options.apply(null, arguments) : options.options;
    return withSpan(suspend(() => internalCall(() => options.body.apply(this, arguments))), opts.name, {
      ...opts,
      captureStackTrace
    });
  });
};
var fromNullable2 = (value2) => value2 == null ? fail(new NoSuchElementException()) : succeed(value2);
var optionFromOptional = (self) => catchAll(map3(self, some2), (error) => isNoSuchElementException(error) ? succeedNone2 : fail(error));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberMessage.js
var OP_INTERRUPT_SIGNAL = "InterruptSignal";
var OP_STATEFUL = "Stateful";
var OP_RESUME = "Resume";
var OP_YIELD_NOW = "YieldNow";
var interruptSignal = (cause2) => ({
  _tag: OP_INTERRUPT_SIGNAL,
  cause: cause2
});
var stateful = (onFiber) => ({
  _tag: OP_STATEFUL,
  onFiber
});
var resume = (effect) => ({
  _tag: OP_RESUME,
  effect
});
var yieldNow3 = () => ({
  _tag: OP_YIELD_NOW
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberScope.js
var FiberScopeSymbolKey = "effect/FiberScope";
var FiberScopeTypeId = Symbol.for(FiberScopeSymbolKey);
var Global = class {
  [FiberScopeTypeId] = FiberScopeTypeId;
  fiberId = none3;
  roots = /* @__PURE__ */ new Set();
  add(_runtimeFlags, child) {
    this.roots.add(child);
    child.addObserver(() => {
      this.roots.delete(child);
    });
  }
};
var Local = class {
  fiberId;
  parent;
  [FiberScopeTypeId] = FiberScopeTypeId;
  constructor(fiberId2, parent) {
    this.fiberId = fiberId2;
    this.parent = parent;
  }
  add(_runtimeFlags, child) {
    this.parent.tell(stateful((parentFiber) => {
      parentFiber.addChild(child);
      child.addObserver(() => {
        parentFiber.removeChild(child);
      });
    }));
  }
};
var unsafeMake6 = (fiber) => {
  return new Local(fiber.id(), fiber);
};
var globalScope = globalValue(/* @__PURE__ */ Symbol.for("effect/FiberScope/Global"), () => new Global());

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiber.js
var FiberSymbolKey = "effect/Fiber";
var FiberTypeId = Symbol.for(FiberSymbolKey);
var fiberVariance2 = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var fiberProto = {
  [FiberTypeId]: fiberVariance2,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var RuntimeFiberSymbolKey = "effect/Fiber";
var RuntimeFiberTypeId = Symbol.for(RuntimeFiberSymbolKey);
var Order4 = pipe(tuple(Order2, Order2), mapInput((fiber) => [fiber.id().startTimeMillis, fiber.id().id]));
var isFiber = (u) => hasProperty(u, FiberTypeId);
var isRuntimeFiber = (self) => RuntimeFiberTypeId in self;
var _await = (self) => self.await;
var children = (self) => self.children;
var done3 = (exit3) => {
  const _fiber = {
    ...CommitPrototype,
    commit() {
      return join2(this);
    },
    ...fiberProto,
    id: () => none3,
    await: succeed(exit3),
    children: succeed([]),
    inheritAll: void_,
    poll: succeed(some2(exit3)),
    interruptAsFork: () => void_
  };
  return _fiber;
};
var dump = (self) => map3(self.status, (status2) => ({
  id: self.id(),
  status: status2
}));
var dumpAll = (fibers) => forEachSequential(fibers, dump);
var fail4 = (error) => done3(fail2(error));
var failCause4 = (cause2) => done3(failCause2(cause2));
var fromEffect = (effect) => map3(exit(effect), done3);
var id = (self) => self.id();
var inheritAll = (self) => self.inheritAll;
var interrupted = (fiberId2) => done3(interrupt3(fiberId2));
var interruptAll = (fibers) => flatMap3(fiberId, (fiberId2) => pipe(fibers, interruptAllAs(fiberId2)));
var interruptAllAs = dual(2, fnUntraced(function* (fibers, fiberId2) {
  for (const fiber of fibers) {
    if (isRuntimeFiber(fiber)) {
      fiber.unsafeInterruptAsFork(fiberId2);
      continue;
    }
    yield* fiber.interruptAsFork(fiberId2);
  }
  for (const fiber of fibers) {
    if (isRuntimeFiber(fiber) && fiber.unsafePoll()) {
      continue;
    }
    yield* fiber.await;
  }
}));
var interruptAsFork = dual(2, (self, fiberId2) => self.interruptAsFork(fiberId2));
var join2 = (self) => zipLeft(flatten4(self.await), self.inheritAll);
var map6 = dual(2, (self, f) => mapEffect2(self, (a) => sync(() => f(a))));
var mapEffect2 = dual(2, (self, f) => {
  const _fiber = {
    ...CommitPrototype,
    commit() {
      return join2(this);
    },
    ...fiberProto,
    id: () => self.id(),
    await: flatMap3(self.await, forEachEffect(f)),
    children: self.children,
    inheritAll: self.inheritAll,
    poll: flatMap3(self.poll, (result) => {
      switch (result._tag) {
        case "None":
          return succeed(none2());
        case "Some":
          return pipe(forEachEffect(result.value, f), map3(some2));
      }
    }),
    interruptAsFork: (id2) => self.interruptAsFork(id2)
  };
  return _fiber;
});
var mapFiber = dual(2, (self, f) => map3(self.await, match3({
  onFailure: (cause2) => failCause4(cause2),
  onSuccess: (a) => f(a)
})));
var match9 = dual(2, (self, {
  onFiber,
  onRuntimeFiber
}) => {
  if (isRuntimeFiber(self)) {
    return onRuntimeFiber(self);
  }
  return onFiber(self);
});
var _never = {
  ...CommitPrototype,
  commit() {
    return join2(this);
  },
  ...fiberProto,
  id: () => none3,
  await: never,
  children: succeed([]),
  inheritAll: never,
  poll: succeed(none2()),
  interruptAsFork: () => never
};
var never3 = _never;
var orElse3 = dual(2, (self, that) => ({
  ...CommitPrototype,
  commit() {
    return join2(this);
  },
  ...fiberProto,
  id: () => getOrElse2(self.id(), that.id()),
  await: zipWith2(self.await, that.await, (exit1, exit22) => isSuccess(exit1) ? exit1 : exit22),
  children: self.children,
  inheritAll: zipRight(that.inheritAll, self.inheritAll),
  poll: zipWith2(self.poll, that.poll, (option1, option22) => {
    switch (option1._tag) {
      case "None": {
        return none2();
      }
      case "Some": {
        return isSuccess(option1.value) ? option1 : option22;
      }
    }
  }),
  interruptAsFork: (id2) => pipe(interruptAsFiber(self, id2), zipRight(pipe(that, interruptAsFiber(id2))), asVoid)
}));
var orElseEither2 = dual(2, (self, that) => orElse3(map6(self, left), map6(that, right)));
var poll = (self) => self.poll;
var parseMs = (milliseconds) => {
  const roundTowardsZero = milliseconds > 0 ? Math.floor : Math.ceil;
  return {
    days: roundTowardsZero(milliseconds / 864e5),
    hours: roundTowardsZero(milliseconds / 36e5) % 24,
    minutes: roundTowardsZero(milliseconds / 6e4) % 60,
    seconds: roundTowardsZero(milliseconds / 1e3) % 60,
    milliseconds: roundTowardsZero(milliseconds) % 1e3,
    microseconds: roundTowardsZero(milliseconds * 1e3) % 1e3,
    nanoseconds: roundTowardsZero(milliseconds * 1e6) % 1e3
  };
};
var renderStatus = (status2) => {
  if (isDone2(status2)) {
    return "Done";
  }
  if (isRunning2(status2)) {
    return "Running";
  }
  const isInterruptible = interruptible(status2.runtimeFlags) ? "interruptible" : "uninterruptible";
  return `Suspended(${isInterruptible})`;
};
var pretty2 = (self) => flatMap3(currentTimeMillis2, (now) => map3(dump(self), (dump2) => {
  const time = now - dump2.id.startTimeMillis;
  const {
    days,
    hours,
    milliseconds,
    minutes,
    seconds
  } = parseMs(time);
  const lifeMsg = (days === 0 ? "" : `${days}d`) + (days === 0 && hours === 0 ? "" : `${hours}h`) + (days === 0 && hours === 0 && minutes === 0 ? "" : `${minutes}m`) + (days === 0 && hours === 0 && minutes === 0 && seconds === 0 ? "" : `${seconds}s`) + `${milliseconds}ms`;
  const waitMsg = isSuspended2(dump2.status) ? (() => {
    const ids2 = ids(dump2.status.blockingOn);
    return size(ids2) > 0 ? `waiting on ` + Array.from(ids2).map((id2) => `${id2}`).join(", ") : "";
  })() : "";
  const statusMsg = renderStatus(dump2.status);
  return `[Fiber](#${dump2.id.id}) (${lifeMsg}) ${waitMsg}
   Status: ${statusMsg}`;
}));
var unsafeRoots = () => Array.from(globalScope.roots);
var roots = sync(unsafeRoots);
var status = (self) => self.status;
var succeed4 = (value2) => done3(succeed2(value2));
var void_3 = succeed4(void 0);
var currentFiberURI = "effect/FiberCurrent";
var getCurrentFiber = () => fromNullable(globalThis[currentFiberURI]);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MutableHashMap.js
var MutableHashMap_exports = {};
__export(MutableHashMap_exports, {
  clear: () => clear,
  empty: () => empty14,
  forEach: () => forEach3,
  fromIterable: () => fromIterable5,
  get: () => get7,
  has: () => has,
  isEmpty: () => isEmpty2,
  keys: () => keys,
  make: () => make20,
  modify: () => modify3,
  modifyAt: () => modifyAt,
  remove: () => remove,
  set: () => set5,
  size: () => size3,
  values: () => values
});
var TypeId4 = /* @__PURE__ */ Symbol.for("effect/MutableHashMap");
var MutableHashMapProto = {
  [TypeId4]: TypeId4,
  [Symbol.iterator]() {
    return new MutableHashMapIterator(this);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableHashMap",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var MutableHashMapIterator = class _MutableHashMapIterator {
  self;
  referentialIterator;
  bucketIterator;
  constructor(self) {
    this.self = self;
    this.referentialIterator = self.referential[Symbol.iterator]();
  }
  next() {
    if (this.bucketIterator !== void 0) {
      return this.bucketIterator.next();
    }
    const result = this.referentialIterator.next();
    if (result.done) {
      this.bucketIterator = new BucketIterator(this.self.buckets.values());
      return this.next();
    }
    return result;
  }
  [Symbol.iterator]() {
    return new _MutableHashMapIterator(this.self);
  }
};
var BucketIterator = class {
  backing;
  constructor(backing) {
    this.backing = backing;
  }
  currentBucket;
  next() {
    if (this.currentBucket === void 0) {
      const result2 = this.backing.next();
      if (result2.done) {
        return result2;
      }
      this.currentBucket = result2.value[Symbol.iterator]();
    }
    const result = this.currentBucket.next();
    if (result.done) {
      this.currentBucket = void 0;
      return this.next();
    }
    return result;
  }
};
var empty14 = () => {
  const self = Object.create(MutableHashMapProto);
  self.referential = /* @__PURE__ */ new Map();
  self.buckets = /* @__PURE__ */ new Map();
  self.bucketsSize = 0;
  return self;
};
var make20 = (...entries) => fromIterable5(entries);
var fromIterable5 = (entries) => {
  const self = empty14();
  for (const [key, value2] of entries) {
    set5(self, key, value2);
  }
  return self;
};
var get7 = dual(2, (self, key) => {
  if (isEqual(key) === false) {
    return self.referential.has(key) ? some2(self.referential.get(key)) : none2();
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return none2();
  }
  return getFromBucket(self, bucket, key);
});
var keys = (self) => {
  const keys4 = Array.from(self.referential.keys());
  for (const bucket of self.buckets.values()) {
    for (let i = 0, len = bucket.length; i < len; i++) {
      keys4.push(bucket[i][0]);
    }
  }
  return keys4;
};
var values = (self) => {
  const values5 = Array.from(self.referential.values());
  for (const bucket of self.buckets.values()) {
    for (let i = 0, len = bucket.length; i < len; i++) {
      values5.push(bucket[i][1]);
    }
  }
  return values5;
};
var getFromBucket = (self, bucket, key, remove3 = false) => {
  for (let i = 0, len = bucket.length; i < len; i++) {
    if (key[symbol2](bucket[i][0])) {
      const value2 = bucket[i][1];
      if (remove3) {
        bucket.splice(i, 1);
        self.bucketsSize--;
      }
      return some2(value2);
    }
  }
  return none2();
};
var has = dual(2, (self, key) => isSome(get7(self, key)));
var set5 = dual(3, (self, key, value2) => {
  if (isEqual(key) === false) {
    self.referential.set(key, value2);
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    self.buckets.set(hash2, [[key, value2]]);
    self.bucketsSize++;
    return self;
  }
  removeFromBucket(self, bucket, key);
  bucket.push([key, value2]);
  self.bucketsSize++;
  return self;
});
var removeFromBucket = (self, bucket, key) => {
  for (let i = 0, len = bucket.length; i < len; i++) {
    if (key[symbol2](bucket[i][0])) {
      bucket.splice(i, 1);
      self.bucketsSize--;
      return;
    }
  }
};
var modify3 = dual(3, (self, key, f) => {
  if (isEqual(key) === false) {
    if (self.referential.has(key)) {
      self.referential.set(key, f(self.referential.get(key)));
    }
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return self;
  }
  const value2 = getFromBucket(self, bucket, key, true);
  if (isNone(value2)) {
    return self;
  }
  bucket.push([key, f(value2.value)]);
  self.bucketsSize++;
  return self;
});
var modifyAt = dual(3, (self, key, f) => {
  if (isEqual(key) === false) {
    const result2 = f(get7(self, key));
    if (isSome(result2)) {
      set5(self, key, result2.value);
    } else {
      remove(self, key);
    }
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    const result2 = f(none2());
    return isSome(result2) ? set5(self, key, result2.value) : self;
  }
  const result = f(getFromBucket(self, bucket, key, true));
  if (isNone(result)) {
    if (bucket.length === 0) {
      self.buckets.delete(hash2);
    }
    return self;
  }
  bucket.push([key, result.value]);
  self.bucketsSize++;
  return self;
});
var remove = dual(2, (self, key) => {
  if (isEqual(key) === false) {
    self.referential.delete(key);
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return self;
  }
  removeFromBucket(self, bucket, key);
  if (bucket.length === 0) {
    self.buckets.delete(hash2);
  }
  return self;
});
var clear = (self) => {
  self.referential.clear();
  self.buckets.clear();
  self.bucketsSize = 0;
  return self;
};
var size3 = (self) => {
  return self.referential.size + self.bucketsSize;
};
var isEmpty2 = (self) => size3(self) === 0;
var forEach3 = dual(2, (self, f) => {
  self.referential.forEach(f);
  for (const bucket of self.buckets.values()) {
    for (const [key, value2] of bucket) {
      f(value2, key);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RedBlackTree.js
var RedBlackTree_exports = {};
__export(RedBlackTree_exports, {
  Direction: () => Direction2,
  at: () => at2,
  atReversed: () => atReversed,
  empty: () => empty16,
  findAll: () => findAll2,
  findFirst: () => findFirst3,
  first: () => first2,
  forEach: () => forEach5,
  forEachBetween: () => forEachBetween2,
  forEachGreaterThanEqual: () => forEachGreaterThanEqual2,
  forEachLessThan: () => forEachLessThan2,
  fromIterable: () => fromIterable7,
  getAt: () => getAt2,
  getOrder: () => getOrder2,
  greaterThan: () => greaterThan5,
  greaterThanEqual: () => greaterThanEqual3,
  greaterThanEqualReversed: () => greaterThanEqualReversed,
  greaterThanReversed: () => greaterThanReversed,
  has: () => has3,
  insert: () => insert2,
  isRedBlackTree: () => isRedBlackTree2,
  keys: () => keys3,
  keysReversed: () => keysReversed,
  last: () => last3,
  lessThan: () => lessThan5,
  lessThanEqual: () => lessThanEqual3,
  lessThanEqualReversed: () => lessThanEqualReversed,
  lessThanReversed: () => lessThanReversed,
  make: () => make22,
  reduce: () => reduce6,
  removeFirst: () => removeFirst2,
  reversed: () => reversed2,
  size: () => size5,
  values: () => values3,
  valuesReversed: () => valuesReversed
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/redBlackTree/iterator.js
var Direction = {
  Forward: 0,
  Backward: 1 << 0
};
var RedBlackTreeIterator = class _RedBlackTreeIterator {
  self;
  stack;
  direction;
  count = 0;
  constructor(self, stack, direction) {
    this.self = self;
    this.stack = stack;
    this.direction = direction;
  }
  /**
   * Clones the iterator
   */
  clone() {
    return new _RedBlackTreeIterator(this.self, this.stack.slice(), this.direction);
  }
  /**
   * Reverse the traversal direction
   */
  reversed() {
    return new _RedBlackTreeIterator(this.self, this.stack.slice(), this.direction === Direction.Forward ? Direction.Backward : Direction.Forward);
  }
  /**
   * Iterator next
   */
  next() {
    const entry = this.entry;
    this.count++;
    if (this.direction === Direction.Forward) {
      this.moveNext();
    } else {
      this.movePrev();
    }
    switch (entry._tag) {
      case "None": {
        return {
          done: true,
          value: this.count
        };
      }
      case "Some": {
        return {
          done: false,
          value: entry.value
        };
      }
    }
  }
  /**
   * Returns the key
   */
  get key() {
    if (this.stack.length > 0) {
      return some2(this.stack[this.stack.length - 1].key);
    }
    return none2();
  }
  /**
   * Returns the value
   */
  get value() {
    if (this.stack.length > 0) {
      return some2(this.stack[this.stack.length - 1].value);
    }
    return none2();
  }
  /**
   * Returns the key
   */
  get entry() {
    return map(last(this.stack), (node) => [node.key, node.value]);
  }
  /**
   * Returns the position of this iterator in the sorted list
   */
  get index() {
    let idx = 0;
    const stack = this.stack;
    if (stack.length === 0) {
      const r = this.self._root;
      if (r != null) {
        return r.count;
      }
      return 0;
    } else if (stack[stack.length - 1].left != null) {
      idx = stack[stack.length - 1].left.count;
    }
    for (let s = stack.length - 2; s >= 0; --s) {
      if (stack[s + 1] === stack[s].right) {
        ++idx;
        if (stack[s].left != null) {
          idx += stack[s].left.count;
        }
      }
    }
    return idx;
  }
  /**
   * Advances iterator to next element in list
   */
  moveNext() {
    const stack = this.stack;
    if (stack.length === 0) {
      return;
    }
    let n = stack[stack.length - 1];
    if (n.right != null) {
      n = n.right;
      while (n != null) {
        stack.push(n);
        n = n.left;
      }
    } else {
      stack.pop();
      while (stack.length > 0 && stack[stack.length - 1].right === n) {
        n = stack[stack.length - 1];
        stack.pop();
      }
    }
  }
  /**
   * Checks if there is a next element
   */
  get hasNext() {
    const stack = this.stack;
    if (stack.length === 0) {
      return false;
    }
    if (stack[stack.length - 1].right != null) {
      return true;
    }
    for (let s = stack.length - 1; s > 0; --s) {
      if (stack[s - 1].left === stack[s]) {
        return true;
      }
    }
    return false;
  }
  /**
   * Advances iterator to previous element in list
   */
  movePrev() {
    const stack = this.stack;
    if (stack.length === 0) {
      return;
    }
    let n = stack[stack.length - 1];
    if (n != null && n.left != null) {
      n = n.left;
      while (n != null) {
        stack.push(n);
        n = n.right;
      }
    } else {
      stack.pop();
      while (stack.length > 0 && stack[stack.length - 1].left === n) {
        n = stack[stack.length - 1];
        stack.pop();
      }
    }
  }
  /**
   * Checks if there is a previous element
   */
  get hasPrev() {
    const stack = this.stack;
    if (stack.length === 0) {
      return false;
    }
    if (stack[stack.length - 1].left != null) {
      return true;
    }
    for (let s = stack.length - 1; s > 0; --s) {
      if (stack[s - 1].right === stack[s]) {
        return true;
      }
    }
    return false;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/redBlackTree/node.js
var Color = {
  Red: 0,
  Black: 1 << 0
};
var clone = ({
  color,
  count,
  key,
  left: left2,
  right: right2,
  value: value2
}) => ({
  color,
  key,
  value: value2,
  left: left2,
  right: right2,
  count
});
function swap2(n, v) {
  n.key = v.key;
  n.value = v.value;
  n.left = v.left;
  n.right = v.right;
  n.color = v.color;
  n.count = v.count;
}
var repaint = ({
  count,
  key,
  left: left2,
  right: right2,
  value: value2
}, color) => ({
  color,
  key,
  value: value2,
  left: left2,
  right: right2,
  count
});
var recount = (node) => {
  node.count = 1 + (node.left?.count ?? 0) + (node.right?.count ?? 0);
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/redBlackTree.js
var RedBlackTreeSymbolKey = "effect/RedBlackTree";
var RedBlackTreeTypeId = Symbol.for(RedBlackTreeSymbolKey);
var redBlackTreeVariance = {
  /* c8 ignore next */
  _Key: (_) => _,
  /* c8 ignore next */
  _Value: (_) => _
};
var RedBlackTreeProto = {
  [RedBlackTreeTypeId]: redBlackTreeVariance,
  [symbol]() {
    let hash2 = hash(RedBlackTreeSymbolKey);
    for (const item of this) {
      hash2 ^= pipe(hash(item[0]), combine(hash(item[1])));
    }
    return cached(this, hash2);
  },
  [symbol2](that) {
    if (isRedBlackTree(that)) {
      if ((this._root?.count ?? 0) !== (that._root?.count ?? 0)) {
        return false;
      }
      const entries = Array.from(that);
      return Array.from(this).every((itemSelf, i) => {
        const itemThat = entries[i];
        return equals(itemSelf[0], itemThat[0]) && equals(itemSelf[1], itemThat[1]);
      });
    }
    return false;
  },
  [Symbol.iterator]() {
    const stack = [];
    let n = this._root;
    while (n != null) {
      stack.push(n);
      n = n.left;
    }
    return new RedBlackTreeIterator(this, stack, Direction.Forward);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "RedBlackTree",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl = (ord, root) => {
  const tree = Object.create(RedBlackTreeProto);
  tree._ord = ord;
  tree._root = root;
  return tree;
};
var isRedBlackTree = (u) => hasProperty(u, RedBlackTreeTypeId);
var empty15 = (ord) => makeImpl(ord, void 0);
var fromIterable6 = dual(2, (entries, ord) => {
  let tree = empty15(ord);
  for (const [key, value2] of entries) {
    tree = insert(tree, key, value2);
  }
  return tree;
});
var make21 = (ord) => (...entries) => {
  return fromIterable6(entries, ord);
};
var atBackwards = dual(2, (self, index) => at(self, index, Direction.Backward));
var atForwards = dual(2, (self, index) => at(self, index, Direction.Forward));
var at = (self, index, direction) => {
  return {
    [Symbol.iterator]: () => {
      if (index < 0) {
        return new RedBlackTreeIterator(self, [], direction);
      }
      let node = self._root;
      const stack = [];
      while (node !== void 0) {
        stack.push(node);
        if (node.left !== void 0) {
          if (index < node.left.count) {
            node = node.left;
            continue;
          }
          index -= node.left.count;
        }
        if (!index) {
          return new RedBlackTreeIterator(self, stack, direction);
        }
        index -= 1;
        if (node.right !== void 0) {
          if (index >= node.right.count) {
            break;
          }
          node = node.right;
        } else {
          break;
        }
      }
      return new RedBlackTreeIterator(self, [], direction);
    }
  };
};
var findAll = dual(2, (self, key) => {
  const stack = [];
  let node = self._root;
  let result = empty2();
  while (node !== void 0 || stack.length > 0) {
    if (node) {
      stack.push(node);
      node = node.left;
    } else {
      const current = stack.pop();
      if (equals(key, current.key)) {
        result = prepend2(current.value)(result);
      }
      node = current.right;
    }
  }
  return result;
});
var findFirst2 = dual(2, (self, key) => {
  const cmp = self._ord;
  let node = self._root;
  while (node !== void 0) {
    const d = cmp(key, node.key);
    if (equals(key, node.key)) {
      return some2(node.value);
    }
    if (d <= 0) {
      node = node.left;
    } else {
      node = node.right;
    }
  }
  return none2();
});
var first = (self) => {
  let node = self._root;
  let current = self._root;
  while (node !== void 0) {
    current = node;
    node = node.left;
  }
  return current ? some2([current.key, current.value]) : none2();
};
var getAt = dual(2, (self, index) => {
  if (index < 0) {
    return none2();
  }
  let root = self._root;
  let node = void 0;
  while (root !== void 0) {
    node = root;
    if (root.left) {
      if (index < root.left.count) {
        root = root.left;
        continue;
      }
      index -= root.left.count;
    }
    if (!index) {
      return some2([node.key, node.value]);
    }
    index -= 1;
    if (root.right) {
      if (index >= root.right.count) {
        break;
      }
      root = root.right;
    } else {
      break;
    }
  }
  return none2();
});
var getOrder = (tree) => tree._ord;
var has2 = dual(2, (self, key) => isSome(findFirst2(self, key)));
var insert = dual(3, (self, key, value2) => {
  const cmp = self._ord;
  let n = self._root;
  const n_stack = [];
  const d_stack = [];
  while (n != null) {
    const d = cmp(key, n.key);
    n_stack.push(n);
    d_stack.push(d);
    if (d <= 0) {
      n = n.left;
    } else {
      n = n.right;
    }
  }
  n_stack.push({
    color: Color.Red,
    key,
    value: value2,
    left: void 0,
    right: void 0,
    count: 1
  });
  for (let s = n_stack.length - 2; s >= 0; --s) {
    const n2 = n_stack[s];
    if (d_stack[s] <= 0) {
      n_stack[s] = {
        color: n2.color,
        key: n2.key,
        value: n2.value,
        left: n_stack[s + 1],
        right: n2.right,
        count: n2.count + 1
      };
    } else {
      n_stack[s] = {
        color: n2.color,
        key: n2.key,
        value: n2.value,
        left: n2.left,
        right: n_stack[s + 1],
        count: n2.count + 1
      };
    }
  }
  for (let s = n_stack.length - 1; s > 1; --s) {
    const p = n_stack[s - 1];
    const n3 = n_stack[s];
    if (p.color === Color.Black || n3.color === Color.Black) {
      break;
    }
    const pp = n_stack[s - 2];
    if (pp.left === p) {
      if (p.left === n3) {
        const y = pp.right;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.right = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          pp.color = Color.Red;
          pp.left = p.right;
          p.color = Color.Black;
          p.right = pp;
          n_stack[s - 2] = p;
          n_stack[s - 1] = n3;
          recount(pp);
          recount(p);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.left === pp) {
              ppp.left = p;
            } else {
              ppp.right = p;
            }
          }
          break;
        }
      } else {
        const y = pp.right;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.right = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          p.right = n3.left;
          pp.color = Color.Red;
          pp.left = n3.right;
          n3.color = Color.Black;
          n3.left = p;
          n3.right = pp;
          n_stack[s - 2] = n3;
          n_stack[s - 1] = p;
          recount(pp);
          recount(p);
          recount(n3);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.left === pp) {
              ppp.left = n3;
            } else {
              ppp.right = n3;
            }
          }
          break;
        }
      }
    } else {
      if (p.right === n3) {
        const y = pp.left;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.left = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          pp.color = Color.Red;
          pp.right = p.left;
          p.color = Color.Black;
          p.left = pp;
          n_stack[s - 2] = p;
          n_stack[s - 1] = n3;
          recount(pp);
          recount(p);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.right === pp) {
              ppp.right = p;
            } else {
              ppp.left = p;
            }
          }
          break;
        }
      } else {
        const y = pp.left;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.left = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          p.left = n3.right;
          pp.color = Color.Red;
          pp.right = n3.left;
          n3.color = Color.Black;
          n3.right = p;
          n3.left = pp;
          n_stack[s - 2] = n3;
          n_stack[s - 1] = p;
          recount(pp);
          recount(p);
          recount(n3);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.right === pp) {
              ppp.right = n3;
            } else {
              ppp.left = n3;
            }
          }
          break;
        }
      }
    }
  }
  n_stack[0].color = Color.Black;
  return makeImpl(self._ord, n_stack[0]);
});
var keysForward = (self) => keys2(self, Direction.Forward);
var keysBackward = (self) => keys2(self, Direction.Backward);
var keys2 = (self, direction) => {
  const begin = self[Symbol.iterator]();
  let count = 0;
  return {
    [Symbol.iterator]: () => keys2(self, direction),
    next: () => {
      count++;
      const entry = begin.key;
      if (direction === Direction.Forward) {
        begin.moveNext();
      } else {
        begin.movePrev();
      }
      switch (entry._tag) {
        case "None": {
          return {
            done: true,
            value: count
          };
        }
        case "Some": {
          return {
            done: false,
            value: entry.value
          };
        }
      }
    }
  };
};
var last2 = (self) => {
  let node = self._root;
  let current = self._root;
  while (node !== void 0) {
    current = node;
    node = node.right;
  }
  return current ? some2([current.key, current.value]) : none2();
};
var reversed = (self) => {
  return {
    [Symbol.iterator]: () => {
      const stack = [];
      let node = self._root;
      while (node !== void 0) {
        stack.push(node);
        node = node.right;
      }
      return new RedBlackTreeIterator(self, stack, Direction.Backward);
    }
  };
};
var greaterThanBackwards = dual(2, (self, key) => greaterThan4(self, key, Direction.Backward));
var greaterThanForwards = dual(2, (self, key) => greaterThan4(self, key, Direction.Forward));
var greaterThan4 = (self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d < 0) {
          last_ptr = stack.length;
        }
        if (d < 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var greaterThanEqualBackwards = dual(2, (self, key) => greaterThanEqual2(self, key, Direction.Backward));
var greaterThanEqualForwards = dual(2, (self, key) => greaterThanEqual2(self, key, Direction.Forward));
var greaterThanEqual2 = (self, key, direction = Direction.Forward) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d <= 0) {
          last_ptr = stack.length;
        }
        if (d <= 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var lessThanBackwards = dual(2, (self, key) => lessThan4(self, key, Direction.Backward));
var lessThanForwards = dual(2, (self, key) => lessThan4(self, key, Direction.Forward));
var lessThan4 = (self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d > 0) {
          last_ptr = stack.length;
        }
        if (d <= 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var lessThanEqualBackwards = dual(2, (self, key) => lessThanEqual2(self, key, Direction.Backward));
var lessThanEqualForwards = dual(2, (self, key) => lessThanEqual2(self, key, Direction.Forward));
var lessThanEqual2 = (self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d >= 0) {
          last_ptr = stack.length;
        }
        if (d < 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var forEach4 = dual(2, (self, f) => {
  const root = self._root;
  if (root !== void 0) {
    visitFull(root, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachGreaterThanEqual = dual(3, (self, min3, f) => {
  const root = self._root;
  const ord = self._ord;
  if (root !== void 0) {
    visitGreaterThanEqual(root, min3, ord, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachLessThan = dual(3, (self, max3, f) => {
  const root = self._root;
  const ord = self._ord;
  if (root !== void 0) {
    visitLessThan(root, max3, ord, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachBetween = dual(2, (self, {
  body,
  max: max3,
  min: min3
}) => {
  const root = self._root;
  const ord = self._ord;
  if (root) {
    visitBetween(root, min3, max3, ord, (key, value2) => {
      body(key, value2);
      return none2();
    });
  }
});
var reduce5 = dual(3, (self, zero2, f) => {
  let accumulator = zero2;
  for (const entry of self) {
    accumulator = f(accumulator, entry[1], entry[0]);
  }
  return accumulator;
});
var removeFirst = dual(2, (self, key) => {
  if (!has2(self, key)) {
    return self;
  }
  const ord = self._ord;
  const cmp = ord;
  let node = self._root;
  const stack = [];
  while (node !== void 0) {
    const d = cmp(key, node.key);
    stack.push(node);
    if (equals(key, node.key)) {
      node = void 0;
    } else if (d <= 0) {
      node = node.left;
    } else {
      node = node.right;
    }
  }
  if (stack.length === 0) {
    return self;
  }
  const cstack = new Array(stack.length);
  let n = stack[stack.length - 1];
  cstack[cstack.length - 1] = {
    color: n.color,
    key: n.key,
    value: n.value,
    left: n.left,
    right: n.right,
    count: n.count
  };
  for (let i = stack.length - 2; i >= 0; --i) {
    n = stack[i];
    if (n.left === stack[i + 1]) {
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: cstack[i + 1],
        right: n.right,
        count: n.count
      };
    } else {
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: n.left,
        right: cstack[i + 1],
        count: n.count
      };
    }
  }
  n = cstack[cstack.length - 1];
  if (n.left !== void 0 && n.right !== void 0) {
    const split = cstack.length;
    n = n.left;
    while (n.right != null) {
      cstack.push(n);
      n = n.right;
    }
    const v = cstack[split - 1];
    cstack.push({
      color: n.color,
      key: v.key,
      value: v.value,
      left: n.left,
      right: n.right,
      count: n.count
    });
    cstack[split - 1].key = n.key;
    cstack[split - 1].value = n.value;
    for (let i = cstack.length - 2; i >= split; --i) {
      n = cstack[i];
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: n.left,
        right: cstack[i + 1],
        count: n.count
      };
    }
    cstack[split - 1].left = cstack[split];
  }
  n = cstack[cstack.length - 1];
  if (n.color === Color.Red) {
    const p = cstack[cstack.length - 2];
    if (p.left === n) {
      p.left = void 0;
    } else if (p.right === n) {
      p.right = void 0;
    }
    cstack.pop();
    for (let i = 0; i < cstack.length; ++i) {
      cstack[i].count--;
    }
    return makeImpl(ord, cstack[0]);
  } else {
    if (n.left !== void 0 || n.right !== void 0) {
      if (n.left !== void 0) {
        swap2(n, n.left);
      } else if (n.right !== void 0) {
        swap2(n, n.right);
      }
      n.color = Color.Black;
      for (let i = 0; i < cstack.length - 1; ++i) {
        cstack[i].count--;
      }
      return makeImpl(ord, cstack[0]);
    } else if (cstack.length === 1) {
      return makeImpl(ord, void 0);
    } else {
      for (let i = 0; i < cstack.length; ++i) {
        cstack[i].count--;
      }
      const parent = cstack[cstack.length - 2];
      fixDoubleBlack(cstack);
      if (parent.left === n) {
        parent.left = void 0;
      } else {
        parent.right = void 0;
      }
    }
  }
  return makeImpl(ord, cstack[0]);
});
var size4 = (self) => self._root?.count ?? 0;
var valuesForward = (self) => values2(self, Direction.Forward);
var valuesBackward = (self) => values2(self, Direction.Backward);
var values2 = (self, direction) => {
  const begin = self[Symbol.iterator]();
  let count = 0;
  return {
    [Symbol.iterator]: () => values2(self, direction),
    next: () => {
      count++;
      const entry = begin.value;
      if (direction === Direction.Forward) {
        begin.moveNext();
      } else {
        begin.movePrev();
      }
      switch (entry._tag) {
        case "None": {
          return {
            done: true,
            value: count
          };
        }
        case "Some": {
          return {
            done: false,
            value: entry.value
          };
        }
      }
    }
  };
};
var visitFull = (node, visit) => {
  let current = node;
  let stack = void 0;
  let done4 = false;
  while (!done4) {
    if (current != null) {
      stack = make4(current, stack);
      current = current.left;
    } else if (stack != null) {
      const value2 = visit(stack.value.key, stack.value.value);
      if (isSome(value2)) {
        return value2;
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done4 = true;
    }
  }
  return none2();
};
var visitGreaterThanEqual = (node, min3, ord, visit) => {
  let current = node;
  let stack = void 0;
  let done4 = false;
  while (!done4) {
    if (current !== void 0) {
      stack = make4(current, stack);
      if (ord(min3, current.key) <= 0) {
        current = current.left;
      } else {
        current = void 0;
      }
    } else if (stack !== void 0) {
      if (ord(min3, stack.value.key) <= 0) {
        const value2 = visit(stack.value.key, stack.value.value);
        if (isSome(value2)) {
          return value2;
        }
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done4 = true;
    }
  }
  return none2();
};
var visitLessThan = (node, max3, ord, visit) => {
  let current = node;
  let stack = void 0;
  let done4 = false;
  while (!done4) {
    if (current !== void 0) {
      stack = make4(current, stack);
      current = current.left;
    } else if (stack !== void 0 && ord(max3, stack.value.key) > 0) {
      const value2 = visit(stack.value.key, stack.value.value);
      if (isSome(value2)) {
        return value2;
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done4 = true;
    }
  }
  return none2();
};
var visitBetween = (node, min3, max3, ord, visit) => {
  let current = node;
  let stack = void 0;
  let done4 = false;
  while (!done4) {
    if (current !== void 0) {
      stack = make4(current, stack);
      if (ord(min3, current.key) <= 0) {
        current = current.left;
      } else {
        current = void 0;
      }
    } else if (stack !== void 0 && ord(max3, stack.value.key) > 0) {
      if (ord(min3, stack.value.key) <= 0) {
        const value2 = visit(stack.value.key, stack.value.value);
        if (isSome(value2)) {
          return value2;
        }
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done4 = true;
    }
  }
  return none2();
};
var fixDoubleBlack = (stack) => {
  let n, p, s, z;
  for (let i = stack.length - 1; i >= 0; --i) {
    n = stack[i];
    if (i === 0) {
      n.color = Color.Black;
      return;
    }
    p = stack[i - 1];
    if (p.left === n) {
      s = p.right;
      if (s !== void 0 && s.right !== void 0 && s.right.color === Color.Red) {
        s = p.right = clone(s);
        z = s.right = clone(s.right);
        p.right = s.left;
        s.left = p;
        s.right = z;
        s.color = p.color;
        n.color = Color.Black;
        p.color = Color.Black;
        z.color = Color.Black;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = s;
          } else {
            pp.right = s;
          }
        }
        stack[i - 1] = s;
        return;
      } else if (s !== void 0 && s.left !== void 0 && s.left.color === Color.Red) {
        s = p.right = clone(s);
        z = s.left = clone(s.left);
        p.right = z.left;
        s.left = z.right;
        z.left = p;
        z.right = s;
        z.color = p.color;
        p.color = Color.Black;
        s.color = Color.Black;
        n.color = Color.Black;
        recount(p);
        recount(s);
        recount(z);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = z;
          } else {
            pp.right = z;
          }
        }
        stack[i - 1] = z;
        return;
      }
      if (s !== void 0 && s.color === Color.Black) {
        if (p.color === Color.Red) {
          p.color = Color.Black;
          p.right = repaint(s, Color.Red);
          return;
        } else {
          p.right = repaint(s, Color.Red);
          continue;
        }
      } else if (s !== void 0) {
        s = clone(s);
        p.right = s.left;
        s.left = p;
        s.color = p.color;
        p.color = Color.Red;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = s;
          } else {
            pp.right = s;
          }
        }
        stack[i - 1] = s;
        stack[i] = p;
        if (i + 1 < stack.length) {
          stack[i + 1] = n;
        } else {
          stack.push(n);
        }
        i = i + 2;
      }
    } else {
      s = p.left;
      if (s !== void 0 && s.left !== void 0 && s.left.color === Color.Red) {
        s = p.left = clone(s);
        z = s.left = clone(s.left);
        p.left = s.right;
        s.right = p;
        s.left = z;
        s.color = p.color;
        n.color = Color.Black;
        p.color = Color.Black;
        z.color = Color.Black;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = s;
          } else {
            pp.left = s;
          }
        }
        stack[i - 1] = s;
        return;
      } else if (s !== void 0 && s.right !== void 0 && s.right.color === Color.Red) {
        s = p.left = clone(s);
        z = s.right = clone(s.right);
        p.left = z.right;
        s.right = z.left;
        z.right = p;
        z.left = s;
        z.color = p.color;
        p.color = Color.Black;
        s.color = Color.Black;
        n.color = Color.Black;
        recount(p);
        recount(s);
        recount(z);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = z;
          } else {
            pp.left = z;
          }
        }
        stack[i - 1] = z;
        return;
      }
      if (s !== void 0 && s.color === Color.Black) {
        if (p.color === Color.Red) {
          p.color = Color.Black;
          p.left = repaint(s, Color.Red);
          return;
        } else {
          p.left = repaint(s, Color.Red);
          continue;
        }
      } else if (s !== void 0) {
        s = clone(s);
        p.left = s.right;
        s.right = p;
        s.color = p.color;
        p.color = Color.Red;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = s;
          } else {
            pp.left = s;
          }
        }
        stack[i - 1] = s;
        stack[i] = p;
        if (i + 1 < stack.length) {
          stack[i + 1] = n;
        } else {
          stack.push(n);
        }
        i = i + 2;
      }
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RedBlackTree.js
var Direction2 = Direction;
var isRedBlackTree2 = isRedBlackTree;
var empty16 = empty15;
var fromIterable7 = fromIterable6;
var make22 = make21;
var at2 = atForwards;
var atReversed = atBackwards;
var findAll2 = findAll;
var findFirst3 = findFirst2;
var first2 = first;
var getAt2 = getAt;
var getOrder2 = getOrder;
var greaterThan5 = greaterThanForwards;
var greaterThanReversed = greaterThanBackwards;
var greaterThanEqual3 = greaterThanEqualForwards;
var greaterThanEqualReversed = greaterThanEqualBackwards;
var has3 = has2;
var insert2 = insert;
var keys3 = keysForward;
var keysReversed = keysBackward;
var last3 = last2;
var lessThan5 = lessThanForwards;
var lessThanReversed = lessThanBackwards;
var lessThanEqual3 = lessThanEqualForwards;
var lessThanEqualReversed = lessThanEqualBackwards;
var forEach5 = forEach4;
var forEachGreaterThanEqual2 = forEachGreaterThanEqual;
var forEachLessThan2 = forEachLessThan;
var forEachBetween2 = forEachBetween;
var reduce6 = reduce5;
var removeFirst2 = removeFirst;
var reversed2 = reversed;
var size5 = size4;
var values3 = valuesForward;
var valuesReversed = valuesBackward;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SortedSet.js
var SortedSet_exports = {};
__export(SortedSet_exports, {
  add: () => add2,
  difference: () => difference2,
  empty: () => empty17,
  every: () => every3,
  filter: () => filter2,
  flatMap: () => flatMap5,
  forEach: () => forEach6,
  fromIterable: () => fromIterable8,
  getEquivalence: () => getEquivalence2,
  has: () => has4,
  intersection: () => intersection,
  isSortedSet: () => isSortedSet,
  isSubset: () => isSubset,
  make: () => make23,
  map: () => map7,
  partition: () => partition,
  remove: () => remove2,
  size: () => size6,
  some: () => some4,
  toggle: () => toggle,
  union: () => union3,
  values: () => values4
});
var TypeId5 = /* @__PURE__ */ Symbol.for("effect/SortedSet");
var SortedSetProto = {
  [TypeId5]: {
    _A: (_) => _
  },
  [symbol]() {
    return pipe(hash(this.keyTree), combine(hash(TypeId5)), cached(this));
  },
  [symbol2](that) {
    return isSortedSet(that) && equals(this.keyTree, that.keyTree);
  },
  [Symbol.iterator]() {
    return keys3(this.keyTree);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "SortedSet",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var fromTree = (keyTree) => {
  const a = Object.create(SortedSetProto);
  a.keyTree = keyTree;
  return a;
};
var isSortedSet = (u) => hasProperty(u, TypeId5);
var empty17 = (O) => fromTree(empty16(O));
var fromIterable8 = dual(2, (iterable, ord) => fromTree(fromIterable7(Array.from(iterable).map((k) => [k, true]), ord)));
var make23 = (ord) => (...entries) => fromIterable8(entries, ord);
var add2 = dual(2, (self, value2) => has3(self.keyTree, value2) ? self : fromTree(insert2(self.keyTree, value2, true)));
var difference2 = dual(2, (self, that) => {
  let out = self;
  for (const value2 of that) {
    out = remove2(out, value2);
  }
  return out;
});
var every3 = dual(2, (self, predicate) => {
  for (const value2 of self) {
    if (!predicate(value2)) {
      return false;
    }
  }
  return true;
});
var filter2 = dual(2, (self, predicate) => {
  const ord = getOrder2(self.keyTree);
  let out = empty17(ord);
  for (const value2 of self) {
    if (predicate(value2)) {
      out = add2(out, value2);
    }
  }
  return out;
});
var flatMap5 = dual(3, (self, O, f) => {
  let out = empty17(O);
  forEach6(self, (a) => {
    for (const b of f(a)) {
      out = add2(out, b);
    }
  });
  return out;
});
var forEach6 = dual(2, (self, f) => forEach5(self.keyTree, f));
var has4 = dual(2, (self, value2) => has3(self.keyTree, value2));
var intersection = dual(2, (self, that) => {
  const ord = getOrder2(self.keyTree);
  let out = empty17(ord);
  for (const value2 of that) {
    if (has4(self, value2)) {
      out = add2(out, value2);
    }
  }
  return out;
});
var isSubset = dual(2, (self, that) => every3(self, (a) => has4(that, a)));
var map7 = dual(3, (self, O, f) => {
  let out = empty17(O);
  forEach6(self, (a) => {
    const b = f(a);
    if (!has4(out, b)) {
      out = add2(out, b);
    }
  });
  return out;
});
var partition = dual(2, (self, predicate) => {
  const ord = getOrder2(self.keyTree);
  let right2 = empty17(ord);
  let left2 = empty17(ord);
  for (const value2 of self) {
    if (predicate(value2)) {
      right2 = add2(right2, value2);
    } else {
      left2 = add2(left2, value2);
    }
  }
  return [left2, right2];
});
var remove2 = dual(2, (self, value2) => fromTree(removeFirst2(self.keyTree, value2)));
var size6 = (self) => size5(self.keyTree);
var some4 = dual(2, (self, predicate) => {
  for (const value2 of self) {
    if (predicate(value2)) {
      return true;
    }
  }
  return false;
});
var toggle = dual(2, (self, value2) => has4(self, value2) ? remove2(self, value2) : add2(self, value2));
var union3 = dual(2, (self, that) => {
  const ord = getOrder2(self.keyTree);
  let out = empty17(ord);
  for (const value2 of self) {
    out = add2(value2)(out);
  }
  for (const value2 of that) {
    out = add2(value2)(out);
  }
  return out;
});
var values4 = (self) => keys3(self.keyTree);
var getEquivalence2 = () => (a, b) => isSubset(a, b) && isSubset(b, a);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Differ.js
var Differ_exports = {};
__export(Differ_exports, {
  TypeId: () => TypeId6,
  chunk: () => chunk2,
  combine: () => combine5,
  diff: () => diff4,
  empty: () => empty18,
  environment: () => environment2,
  hashMap: () => hashMap2,
  hashSet: () => hashSet2,
  make: () => make24,
  orElseEither: () => orElseEither3,
  patch: () => patch5,
  readonlyArray: () => readonlyArray2,
  transform: () => transform2,
  update: () => update4,
  updateWith: () => updateWith2,
  zip: () => zip5
});
var TypeId6 = DifferTypeId;
var empty18 = (self) => self.empty;
var diff4 = dual(3, (self, oldValue, newValue) => self.diff(oldValue, newValue));
var combine5 = dual(3, (self, first3, second) => self.combine(first3, second));
var patch5 = dual(3, (self, patch7, oldValue) => self.patch(patch7, oldValue));
var make24 = make6;
var environment2 = environment;
var chunk2 = chunk;
var hashMap2 = hashMap;
var hashSet2 = hashSet;
var orElseEither3 = orElseEither;
var readonlyArray2 = readonlyArray;
var transform2 = transform;
var update4 = update;
var updateWith2 = updateWith;
var zip5 = zip2;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/completedRequestMap.js
var currentRequestMap = globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentRequestMap"), () => fiberRefUnsafeMake(/* @__PURE__ */ new Map()));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/concurrency.js
var match10 = (concurrency, sequential4, unbounded, bounded) => {
  switch (concurrency) {
    case void 0:
      return sequential4();
    case "unbounded":
      return unbounded();
    case "inherit":
      return fiberRefGetWith(currentConcurrency, (concurrency2) => concurrency2 === "unbounded" ? unbounded() : concurrency2 > 1 ? bounded(concurrency2) : sequential4());
    default:
      return concurrency > 1 ? bounded(concurrency) : sequential4();
  }
};
var matchSimple = (concurrency, sequential4, concurrent) => {
  switch (concurrency) {
    case void 0:
      return sequential4();
    case "unbounded":
      return concurrent();
    case "inherit":
      return fiberRefGetWith(currentConcurrency, (concurrency2) => concurrency2 === "unbounded" || concurrency2 > 1 ? concurrent() : sequential4());
    default:
      return concurrency > 1 ? concurrent() : sequential4();
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/logger.js
var LoggerSymbolKey = "effect/Logger";
var LoggerTypeId = Symbol.for(LoggerSymbolKey);
var loggerVariance = {
  /* c8 ignore next */
  _Message: (_) => _,
  /* c8 ignore next */
  _Output: (_) => _
};
var makeLogger = (log2) => ({
  [LoggerTypeId]: loggerVariance,
  log: log2,
  pipe() {
    return pipeArguments(this, arguments);
  }
});
var mapInput2 = dual(2, (self, f) => makeLogger((options) => self.log({
  ...options,
  message: f(options.message)
})));
var mapInputOptions = dual(2, (self, f) => makeLogger((options) => self.log(f(options))));
var filterLogLevel = dual(2, (self, f) => makeLogger((options) => f(options.logLevel) ? some2(self.log(options)) : none2()));
var map8 = dual(2, (self, f) => makeLogger((options) => f(self.log(options))));
var none6 = {
  [LoggerTypeId]: loggerVariance,
  log: constVoid,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var simple = (log2) => ({
  [LoggerTypeId]: loggerVariance,
  log: ({
    message
  }) => log2(message),
  pipe() {
    return pipeArguments(this, arguments);
  }
});
var succeed5 = (value2) => {
  return simple(() => value2);
};
var sync3 = (evaluate2) => {
  return simple(evaluate2);
};
var zip6 = dual(2, (self, that) => makeLogger((options) => [self.log(options), that.log(options)]));
var zipLeft2 = dual(2, (self, that) => map8(zip6(self, that), (tuple2) => tuple2[0]));
var zipRight2 = dual(2, (self, that) => map8(zip6(self, that), (tuple2) => tuple2[1]));
var textOnly = /^[^\s"=]*$/;
var format2 = (quoteValue, whitespace) => ({
  annotations,
  cause: cause2,
  date,
  fiberId: fiberId2,
  logLevel,
  message,
  spans
}) => {
  const formatValue = (value2) => value2.match(textOnly) ? value2 : quoteValue(value2);
  const format3 = (label, value2) => `${formatLabel(label)}=${formatValue(value2)}`;
  const append3 = (label, value2) => " " + format3(label, value2);
  let out = format3("timestamp", date.toISOString());
  out += append3("level", logLevel.label);
  out += append3("fiber", threadName(fiberId2));
  const messages = ensure(message);
  for (let i = 0; i < messages.length; i++) {
    out += append3("message", toStringUnknown(messages[i], whitespace));
  }
  if (!isEmptyType(cause2)) {
    out += append3("cause", pretty(cause2, {
      renderErrorCause: true
    }));
  }
  for (const span2 of spans) {
    out += " " + render(date.getTime())(span2);
  }
  for (const [label, value2] of annotations) {
    out += append3(label, toStringUnknown(value2, whitespace));
  }
  return out;
};
var escapeDoubleQuotes = (s) => `"${s.replace(/\\([\s\S])|(")/g, "\\$1$2")}"`;
var stringLogger = makeLogger(format2(escapeDoubleQuotes));
var logfmtLogger = makeLogger(format2(JSON.stringify, 0));
var structuredLogger = makeLogger(({
  annotations,
  cause: cause2,
  date,
  fiberId: fiberId2,
  logLevel,
  message,
  spans
}) => {
  const now = date.getTime();
  const annotationsObj = {};
  const spansObj = {};
  if (size2(annotations) > 0) {
    for (const [k, v] of annotations) {
      annotationsObj[k] = structuredMessage(v);
    }
  }
  if (isCons(spans)) {
    for (const span2 of spans) {
      spansObj[span2.label] = now - span2.startTime;
    }
  }
  const messageArr = ensure(message);
  return {
    message: messageArr.length === 1 ? structuredMessage(messageArr[0]) : messageArr.map(structuredMessage),
    logLevel: logLevel.label,
    timestamp: date.toISOString(),
    cause: isEmpty(cause2) ? void 0 : pretty(cause2, {
      renderErrorCause: true
    }),
    annotations: annotationsObj,
    spans: spansObj,
    fiberId: threadName(fiberId2)
  };
});
var structuredMessage = (u) => {
  switch (typeof u) {
    case "bigint":
    case "function":
    case "symbol": {
      return String(u);
    }
    default: {
      return toJSON(u);
    }
  }
};
var jsonLogger = map8(structuredLogger, stringifyCircular);
var isLogger = (u) => {
  return typeof u === "object" && u != null && LoggerTypeId in u;
};
var withColor = (text, ...colors2) => {
  let out = "";
  for (let i = 0; i < colors2.length; i++) {
    out += `\x1B[${colors2[i]}m`;
  }
  return out + text + "\x1B[0m";
};
var withColorNoop = (text, ..._colors) => text;
var colors = {
  bold: "1",
  red: "31",
  green: "32",
  yellow: "33",
  blue: "34",
  cyan: "36",
  white: "37",
  gray: "90",
  black: "30",
  bgBrightRed: "101"
};
var logLevelColors = {
  None: [],
  All: [],
  Trace: [colors.gray],
  Debug: [colors.blue],
  Info: [colors.green],
  Warning: [colors.yellow],
  Error: [colors.red],
  Fatal: [colors.bgBrightRed, colors.black]
};
var logLevelStyle = {
  None: "",
  All: "",
  Trace: "color:gray",
  Debug: "color:blue",
  Info: "color:green",
  Warning: "color:orange",
  Error: "color:red",
  Fatal: "background-color:red;color:white"
};
var defaultDateFormat = (date) => `${date.getHours().toString().padStart(2, "0")}:${date.getMinutes().toString().padStart(2, "0")}:${date.getSeconds().toString().padStart(2, "0")}.${date.getMilliseconds().toString().padStart(3, "0")}`;
var hasProcessStdout = typeof process === "object" && process !== null && typeof process.stdout === "object" && process.stdout !== null;
var processStdoutIsTTY = hasProcessStdout && process.stdout.isTTY === true;
var hasProcessStdoutOrDeno = hasProcessStdout || "Deno" in globalThis;
var prettyLogger = (options) => {
  const mode_ = options?.mode ?? "auto";
  const mode = mode_ === "auto" ? hasProcessStdoutOrDeno ? "tty" : "browser" : mode_;
  const isBrowser = mode === "browser";
  const showColors = typeof options?.colors === "boolean" ? options.colors : processStdoutIsTTY || isBrowser;
  const formatDate = options?.formatDate ?? defaultDateFormat;
  return isBrowser ? prettyLoggerBrowser({
    colors: showColors,
    formatDate
  }) : prettyLoggerTty({
    colors: showColors,
    formatDate,
    stderr: options?.stderr === true
  });
};
var prettyLoggerTty = (options) => {
  const color = options.colors ? withColor : withColorNoop;
  return makeLogger(({
    annotations,
    cause: cause2,
    context: context3,
    date,
    fiberId: fiberId2,
    logLevel,
    message: message_,
    spans
  }) => {
    const services = getOrDefault2(context3, currentServices);
    const console2 = get2(services, consoleTag).unsafe;
    const log2 = options.stderr === true ? console2.error : console2.log;
    const message = ensure(message_);
    let firstLine = color(`[${options.formatDate(date)}]`, colors.white) + ` ${color(logLevel.label, ...logLevelColors[logLevel._tag])} (${threadName(fiberId2)})`;
    if (isCons(spans)) {
      const now = date.getTime();
      const render3 = render(now);
      for (const span2 of spans) {
        firstLine += " " + render3(span2);
      }
    }
    firstLine += ":";
    let messageIndex = 0;
    if (message.length > 0) {
      const firstMaybeString = structuredMessage(message[0]);
      if (typeof firstMaybeString === "string") {
        firstLine += " " + color(firstMaybeString, colors.bold, colors.cyan);
        messageIndex++;
      }
    }
    log2(firstLine);
    console2.group();
    if (!isEmpty(cause2)) {
      log2(pretty(cause2, {
        renderErrorCause: true
      }));
    }
    if (messageIndex < message.length) {
      for (; messageIndex < message.length; messageIndex++) {
        log2(redact(message[messageIndex]));
      }
    }
    if (size2(annotations) > 0) {
      for (const [key, value2] of annotations) {
        log2(color(`${key}:`, colors.bold, colors.white), redact(value2));
      }
    }
    console2.groupEnd();
  });
};
var prettyLoggerBrowser = (options) => {
  const color = options.colors ? "%c" : "";
  return makeLogger(({
    annotations,
    cause: cause2,
    context: context3,
    date,
    fiberId: fiberId2,
    logLevel,
    message: message_,
    spans
  }) => {
    const services = getOrDefault2(context3, currentServices);
    const console2 = get2(services, consoleTag).unsafe;
    const message = ensure(message_);
    let firstLine = `${color}[${options.formatDate(date)}]`;
    const firstParams = [];
    if (options.colors) {
      firstParams.push("color:gray");
    }
    firstLine += ` ${color}${logLevel.label}${color} (${threadName(fiberId2)})`;
    if (options.colors) {
      firstParams.push(logLevelStyle[logLevel._tag], "");
    }
    if (isCons(spans)) {
      const now = date.getTime();
      const render3 = render(now);
      for (const span2 of spans) {
        firstLine += " " + render3(span2);
      }
    }
    firstLine += ":";
    let messageIndex = 0;
    if (message.length > 0) {
      const firstMaybeString = structuredMessage(message[0]);
      if (typeof firstMaybeString === "string") {
        firstLine += ` ${color}${firstMaybeString}`;
        if (options.colors) {
          firstParams.push("color:deepskyblue");
        }
        messageIndex++;
      }
    }
    console2.groupCollapsed(firstLine, ...firstParams);
    if (!isEmpty(cause2)) {
      console2.error(pretty(cause2, {
        renderErrorCause: true
      }));
    }
    if (messageIndex < message.length) {
      for (; messageIndex < message.length; messageIndex++) {
        console2.log(redact(message[messageIndex]));
      }
    }
    if (size2(annotations) > 0) {
      for (const [key, value2] of annotations) {
        const redacted = redact(value2);
        if (options.colors) {
          console2.log(`%c${key}:`, "color:gray", redacted);
        } else {
          console2.log(`${key}:`, redacted);
        }
      }
    }
    console2.groupEnd();
  });
};
var prettyLoggerDefault = globalValue("effect/Logger/prettyLoggerDefault", () => prettyLogger());

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/boundaries.js
var MetricBoundariesSymbolKey = "effect/MetricBoundaries";
var MetricBoundariesTypeId = Symbol.for(MetricBoundariesSymbolKey);
var MetricBoundariesImpl = class {
  values;
  [MetricBoundariesTypeId] = MetricBoundariesTypeId;
  constructor(values5) {
    this.values = values5;
    this._hash = pipe(string(MetricBoundariesSymbolKey), combine(array(this.values)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](u) {
    return isMetricBoundaries(u) && equals(this.values, u.values);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isMetricBoundaries = (u) => hasProperty(u, MetricBoundariesTypeId);
var fromIterable9 = (iterable) => {
  const values5 = pipe(iterable, appendAll(of2(Number.POSITIVE_INFINITY)), dedupe);
  return new MetricBoundariesImpl(values5);
};
var linear = (options) => pipe(makeBy(options.count - 1, (i) => options.start + i * options.width), unsafeFromArray, fromIterable9);
var exponential = (options) => pipe(makeBy(options.count - 1, (i) => options.start * Math.pow(options.factor, i)), unsafeFromArray, fromIterable9);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/keyType.js
var MetricKeyTypeSymbolKey = "effect/MetricKeyType";
var MetricKeyTypeTypeId = Symbol.for(MetricKeyTypeSymbolKey);
var CounterKeyTypeSymbolKey = "effect/MetricKeyType/Counter";
var CounterKeyTypeTypeId = Symbol.for(CounterKeyTypeSymbolKey);
var FrequencyKeyTypeSymbolKey = "effect/MetricKeyType/Frequency";
var FrequencyKeyTypeTypeId = Symbol.for(FrequencyKeyTypeSymbolKey);
var GaugeKeyTypeSymbolKey = "effect/MetricKeyType/Gauge";
var GaugeKeyTypeTypeId = Symbol.for(GaugeKeyTypeSymbolKey);
var HistogramKeyTypeSymbolKey = "effect/MetricKeyType/Histogram";
var HistogramKeyTypeTypeId = Symbol.for(HistogramKeyTypeSymbolKey);
var SummaryKeyTypeSymbolKey = "effect/MetricKeyType/Summary";
var SummaryKeyTypeTypeId = Symbol.for(SummaryKeyTypeSymbolKey);
var metricKeyTypeVariance = {
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _Out: (_) => _
};
var CounterKeyType = class {
  incremental;
  bigint;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [CounterKeyTypeTypeId] = CounterKeyTypeTypeId;
  constructor(incremental, bigint) {
    this.incremental = incremental;
    this.bigint = bigint;
    this._hash = string(CounterKeyTypeSymbolKey);
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isCounterKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var FrequencyKeyTypeHash = string(FrequencyKeyTypeSymbolKey);
var FrequencyKeyType = class {
  preregisteredWords;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [FrequencyKeyTypeTypeId] = FrequencyKeyTypeTypeId;
  constructor(preregisteredWords) {
    this.preregisteredWords = preregisteredWords;
  }
  [symbol]() {
    return FrequencyKeyTypeHash;
  }
  [symbol2](that) {
    return isFrequencyKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GaugeKeyTypeHash = string(GaugeKeyTypeSymbolKey);
var GaugeKeyType = class {
  bigint;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [GaugeKeyTypeTypeId] = GaugeKeyTypeTypeId;
  constructor(bigint) {
    this.bigint = bigint;
  }
  [symbol]() {
    return GaugeKeyTypeHash;
  }
  [symbol2](that) {
    return isGaugeKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var HistogramKeyType = class {
  boundaries;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [HistogramKeyTypeTypeId] = HistogramKeyTypeTypeId;
  constructor(boundaries) {
    this.boundaries = boundaries;
    this._hash = pipe(string(HistogramKeyTypeSymbolKey), combine(hash(this.boundaries)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isHistogramKey(that) && equals(this.boundaries, that.boundaries);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var SummaryKeyType = class {
  maxAge;
  maxSize;
  error;
  quantiles;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [SummaryKeyTypeTypeId] = SummaryKeyTypeTypeId;
  constructor(maxAge, maxSize, error, quantiles) {
    this.maxAge = maxAge;
    this.maxSize = maxSize;
    this.error = error;
    this.quantiles = quantiles;
    this._hash = pipe(string(SummaryKeyTypeSymbolKey), combine(hash(this.maxAge)), combine(hash(this.maxSize)), combine(hash(this.error)), combine(array(this.quantiles)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isSummaryKey(that) && equals(this.maxAge, that.maxAge) && this.maxSize === that.maxSize && this.error === that.error && equals(this.quantiles, that.quantiles);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var counter = (options) => new CounterKeyType(options?.incremental ?? false, options?.bigint ?? false);
var frequency = (options) => new FrequencyKeyType(options?.preregisteredWords ?? []);
var gauge = (options) => new GaugeKeyType(options?.bigint ?? false);
var histogram = (boundaries) => {
  return new HistogramKeyType(boundaries);
};
var summary = (options) => {
  return new SummaryKeyType(decode(options.maxAge), options.maxSize, options.error, options.quantiles);
};
var isMetricKeyType = (u) => hasProperty(u, MetricKeyTypeTypeId);
var isCounterKey = (u) => hasProperty(u, CounterKeyTypeTypeId);
var isFrequencyKey = (u) => hasProperty(u, FrequencyKeyTypeTypeId);
var isGaugeKey = (u) => hasProperty(u, GaugeKeyTypeTypeId);
var isHistogramKey = (u) => hasProperty(u, HistogramKeyTypeTypeId);
var isSummaryKey = (u) => hasProperty(u, SummaryKeyTypeTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/key.js
var MetricKeySymbolKey = "effect/MetricKey";
var MetricKeyTypeId = Symbol.for(MetricKeySymbolKey);
var metricKeyVariance = {
  /* c8 ignore next */
  _Type: (_) => _
};
var arrayEquivilence = getEquivalence(equals);
var MetricKeyImpl = class {
  name;
  keyType;
  description;
  tags;
  [MetricKeyTypeId] = metricKeyVariance;
  constructor(name, keyType, description, tags = []) {
    this.name = name;
    this.keyType = keyType;
    this.description = description;
    this.tags = tags;
    this._hash = pipe(string(this.name + this.description), combine(hash(this.keyType)), combine(array(this.tags)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](u) {
    return isMetricKey(u) && this.name === u.name && equals(this.keyType, u.keyType) && equals(this.description, u.description) && arrayEquivilence(this.tags, u.tags);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isMetricKey = (u) => hasProperty(u, MetricKeyTypeId);
var counter2 = (name, options) => new MetricKeyImpl(name, counter(options), fromNullable(options?.description));
var frequency2 = (name, options) => new MetricKeyImpl(name, frequency(options), fromNullable(options?.description));
var gauge2 = (name, options) => new MetricKeyImpl(name, gauge(options), fromNullable(options?.description));
var histogram2 = (name, boundaries, description) => new MetricKeyImpl(name, histogram(boundaries), fromNullable(description));
var summary2 = (options) => new MetricKeyImpl(options.name, summary(options), fromNullable(options.description));
var tagged = dual(3, (self, key, value2) => taggedWithLabels(self, [make19(key, value2)]));
var taggedWithLabels = dual(2, (self, extraTags) => extraTags.length === 0 ? self : new MetricKeyImpl(self.name, self.keyType, self.description, union(self.tags, extraTags)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/state.js
var MetricStateSymbolKey = "effect/MetricState";
var MetricStateTypeId = Symbol.for(MetricStateSymbolKey);
var CounterStateSymbolKey = "effect/MetricState/Counter";
var CounterStateTypeId = Symbol.for(CounterStateSymbolKey);
var FrequencyStateSymbolKey = "effect/MetricState/Frequency";
var FrequencyStateTypeId = Symbol.for(FrequencyStateSymbolKey);
var GaugeStateSymbolKey = "effect/MetricState/Gauge";
var GaugeStateTypeId = Symbol.for(GaugeStateSymbolKey);
var HistogramStateSymbolKey = "effect/MetricState/Histogram";
var HistogramStateTypeId = Symbol.for(HistogramStateSymbolKey);
var SummaryStateSymbolKey = "effect/MetricState/Summary";
var SummaryStateTypeId = Symbol.for(SummaryStateSymbolKey);
var metricStateVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var CounterState = class {
  count;
  [MetricStateTypeId] = metricStateVariance;
  [CounterStateTypeId] = CounterStateTypeId;
  constructor(count) {
    this.count = count;
  }
  [symbol]() {
    return pipe(hash(CounterStateSymbolKey), combine(hash(this.count)), cached(this));
  }
  [symbol2](that) {
    return isCounterState(that) && this.count === that.count;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var arrayEquals = getEquivalence(equals);
var FrequencyState = class {
  occurrences;
  [MetricStateTypeId] = metricStateVariance;
  [FrequencyStateTypeId] = FrequencyStateTypeId;
  constructor(occurrences) {
    this.occurrences = occurrences;
  }
  _hash;
  [symbol]() {
    return pipe(string(FrequencyStateSymbolKey), combine(array(fromIterable(this.occurrences.entries()))), cached(this));
  }
  [symbol2](that) {
    return isFrequencyState(that) && arrayEquals(fromIterable(this.occurrences.entries()), fromIterable(that.occurrences.entries()));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GaugeState = class {
  value;
  [MetricStateTypeId] = metricStateVariance;
  [GaugeStateTypeId] = GaugeStateTypeId;
  constructor(value2) {
    this.value = value2;
  }
  [symbol]() {
    return pipe(hash(GaugeStateSymbolKey), combine(hash(this.value)), cached(this));
  }
  [symbol2](u) {
    return isGaugeState(u) && this.value === u.value;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var HistogramState = class {
  buckets;
  count;
  min;
  max;
  sum;
  [MetricStateTypeId] = metricStateVariance;
  [HistogramStateTypeId] = HistogramStateTypeId;
  constructor(buckets, count, min3, max3, sum2) {
    this.buckets = buckets;
    this.count = count;
    this.min = min3;
    this.max = max3;
    this.sum = sum2;
  }
  [symbol]() {
    return pipe(hash(HistogramStateSymbolKey), combine(hash(this.buckets)), combine(hash(this.count)), combine(hash(this.min)), combine(hash(this.max)), combine(hash(this.sum)), cached(this));
  }
  [symbol2](that) {
    return isHistogramState(that) && equals(this.buckets, that.buckets) && this.count === that.count && this.min === that.min && this.max === that.max && this.sum === that.sum;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var SummaryState = class {
  error;
  quantiles;
  count;
  min;
  max;
  sum;
  [MetricStateTypeId] = metricStateVariance;
  [SummaryStateTypeId] = SummaryStateTypeId;
  constructor(error, quantiles, count, min3, max3, sum2) {
    this.error = error;
    this.quantiles = quantiles;
    this.count = count;
    this.min = min3;
    this.max = max3;
    this.sum = sum2;
  }
  [symbol]() {
    return pipe(hash(SummaryStateSymbolKey), combine(hash(this.error)), combine(hash(this.quantiles)), combine(hash(this.count)), combine(hash(this.min)), combine(hash(this.max)), combine(hash(this.sum)), cached(this));
  }
  [symbol2](that) {
    return isSummaryState(that) && this.error === that.error && equals(this.quantiles, that.quantiles) && this.count === that.count && this.min === that.min && this.max === that.max && this.sum === that.sum;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var counter3 = (count) => new CounterState(count);
var frequency3 = (occurrences) => {
  return new FrequencyState(occurrences);
};
var gauge3 = (count) => new GaugeState(count);
var histogram3 = (options) => new HistogramState(options.buckets, options.count, options.min, options.max, options.sum);
var summary3 = (options) => new SummaryState(options.error, options.quantiles, options.count, options.min, options.max, options.sum);
var isMetricState = (u) => hasProperty(u, MetricStateTypeId);
var isCounterState = (u) => hasProperty(u, CounterStateTypeId);
var isFrequencyState = (u) => hasProperty(u, FrequencyStateTypeId);
var isGaugeState = (u) => hasProperty(u, GaugeStateTypeId);
var isHistogramState = (u) => hasProperty(u, HistogramStateTypeId);
var isSummaryState = (u) => hasProperty(u, SummaryStateTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/hook.js
var MetricHookSymbolKey = "effect/MetricHook";
var MetricHookTypeId = Symbol.for(MetricHookSymbolKey);
var metricHookVariance = {
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _Out: (_) => _
};
var make25 = (options) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...options
});
var onModify = dual(2, (self, f) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  get: self.get,
  update: self.update,
  modify: (input) => {
    self.modify(input);
    return f(input);
  }
}));
var onUpdate = dual(2, (self, f) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  get: self.get,
  update: (input) => {
    self.update(input);
    return f(input);
  },
  modify: self.modify
}));
var bigint02 = BigInt(0);
var counter4 = (key) => {
  let sum2 = key.keyType.bigint ? bigint02 : 0;
  const canUpdate = key.keyType.incremental ? key.keyType.bigint ? (value2) => value2 >= bigint02 : (value2) => value2 >= 0 : (_value) => true;
  const update6 = (value2) => {
    if (canUpdate(value2)) {
      sum2 = sum2 + value2;
    }
  };
  return make25({
    get: () => counter3(sum2),
    update: update6,
    modify: update6
  });
};
var frequency4 = (key) => {
  const values5 = /* @__PURE__ */ new Map();
  for (const word of key.keyType.preregisteredWords) {
    values5.set(word, 0);
  }
  const update6 = (word) => {
    const slotCount = values5.get(word) ?? 0;
    values5.set(word, slotCount + 1);
  };
  return make25({
    get: () => frequency3(values5),
    update: update6,
    modify: update6
  });
};
var gauge4 = (_key, startAt) => {
  let value2 = startAt;
  return make25({
    get: () => gauge3(value2),
    update: (v) => {
      value2 = v;
    },
    modify: (v) => {
      value2 = value2 + v;
    }
  });
};
var histogram4 = (key) => {
  const bounds = key.keyType.boundaries.values;
  const size7 = bounds.length;
  const values5 = new Uint32Array(size7 + 1);
  const boundaries = new Float64Array(size7);
  let count = 0;
  let sum2 = 0;
  let min3 = Number.MAX_VALUE;
  let max3 = Number.MIN_VALUE;
  pipe(bounds, sort(Order2), map2((n, i) => {
    boundaries[i] = n;
  }));
  const update6 = (value2) => {
    let from = 0;
    let to = size7;
    while (from !== to) {
      const mid = Math.floor(from + (to - from) / 2);
      const boundary = boundaries[mid];
      if (value2 <= boundary) {
        to = mid;
      } else {
        from = mid;
      }
      if (to === from + 1) {
        if (value2 <= boundaries[from]) {
          to = from;
        } else {
          from = to;
        }
      }
    }
    values5[from] = values5[from] + 1;
    count = count + 1;
    sum2 = sum2 + value2;
    if (value2 < min3) {
      min3 = value2;
    }
    if (value2 > max3) {
      max3 = value2;
    }
  };
  const getBuckets = () => {
    const builder = allocate(size7);
    let cumulated = 0;
    for (let i = 0; i < size7; i++) {
      const boundary = boundaries[i];
      const value2 = values5[i];
      cumulated = cumulated + value2;
      builder[i] = [boundary, cumulated];
    }
    return builder;
  };
  return make25({
    get: () => histogram3({
      buckets: getBuckets(),
      count,
      min: min3,
      max: max3,
      sum: sum2
    }),
    update: update6,
    modify: update6
  });
};
var summary4 = (key) => {
  const {
    error,
    maxAge,
    maxSize,
    quantiles
  } = key.keyType;
  const sortedQuantiles = pipe(quantiles, sort(Order2));
  const values5 = allocate(maxSize);
  let head3 = 0;
  let count = 0;
  let sum2 = 0;
  let min3 = 0;
  let max3 = 0;
  const snapshot2 = (now) => {
    const builder = [];
    let i = 0;
    while (i !== maxSize - 1) {
      const item = values5[i];
      if (item != null) {
        const [t, v] = item;
        const age = millis(now - t);
        if (greaterThanOrEqualTo2(age, zero) && lessThanOrEqualTo2(age, maxAge)) {
          builder.push(v);
        }
      }
      i = i + 1;
    }
    return calculateQuantiles(error, sortedQuantiles, sort(builder, Order2));
  };
  const observe = (value2, timestamp) => {
    if (maxSize > 0) {
      head3 = head3 + 1;
      const target = head3 % maxSize;
      values5[target] = [timestamp, value2];
    }
    min3 = count === 0 ? value2 : Math.min(min3, value2);
    max3 = count === 0 ? value2 : Math.max(max3, value2);
    count = count + 1;
    sum2 = sum2 + value2;
  };
  return make25({
    get: () => summary3({
      error,
      quantiles: snapshot2(Date.now()),
      count,
      min: min3,
      max: max3,
      sum: sum2
    }),
    update: ([value2, timestamp]) => observe(value2, timestamp),
    modify: ([value2, timestamp]) => observe(value2, timestamp)
  });
};
var calculateQuantiles = (error, sortedQuantiles, sortedSamples) => {
  const sampleCount = sortedSamples.length;
  if (!isNonEmptyReadonlyArray(sortedQuantiles)) {
    return empty();
  }
  const head3 = sortedQuantiles[0];
  const tail = sortedQuantiles.slice(1);
  const resolvedHead = resolveQuantile(error, sampleCount, none2(), 0, head3, sortedSamples);
  const resolved = of(resolvedHead);
  tail.forEach((quantile) => {
    resolved.push(resolveQuantile(error, sampleCount, resolvedHead.value, resolvedHead.consumed, quantile, resolvedHead.rest));
  });
  return map2(resolved, (rq) => [rq.quantile, rq.value]);
};
var resolveQuantile = (error, sampleCount, current, consumed, quantile, rest) => {
  let error_1 = error;
  let sampleCount_1 = sampleCount;
  let current_1 = current;
  let consumed_1 = consumed;
  let quantile_1 = quantile;
  let rest_1 = rest;
  let error_2 = error;
  let sampleCount_2 = sampleCount;
  let current_2 = current;
  let consumed_2 = consumed;
  let quantile_2 = quantile;
  let rest_2 = rest;
  while (1) {
    if (!isNonEmptyReadonlyArray(rest_1)) {
      return {
        quantile: quantile_1,
        value: none2(),
        consumed: consumed_1,
        rest: []
      };
    }
    if (quantile_1 === 1) {
      return {
        quantile: quantile_1,
        value: some2(lastNonEmpty(rest_1)),
        consumed: consumed_1 + rest_1.length,
        rest: []
      };
    }
    const headValue = headNonEmpty(rest_1);
    const sameHead = span(rest_1, (n) => n === headValue);
    const desired = quantile_1 * sampleCount_1;
    const allowedError = error_1 / 2 * desired;
    const candConsumed = consumed_1 + sameHead[0].length;
    const candError = Math.abs(candConsumed - desired);
    if (candConsumed < desired - allowedError) {
      error_2 = error_1;
      sampleCount_2 = sampleCount_1;
      current_2 = head(rest_1);
      consumed_2 = candConsumed;
      quantile_2 = quantile_1;
      rest_2 = sameHead[1];
      error_1 = error_2;
      sampleCount_1 = sampleCount_2;
      current_1 = current_2;
      consumed_1 = consumed_2;
      quantile_1 = quantile_2;
      rest_1 = rest_2;
      continue;
    }
    if (candConsumed > desired + allowedError) {
      const valueToReturn = isNone(current_1) ? some2(headValue) : current_1;
      return {
        quantile: quantile_1,
        value: valueToReturn,
        consumed: consumed_1,
        rest: rest_1
      };
    }
    switch (current_1._tag) {
      case "None": {
        error_2 = error_1;
        sampleCount_2 = sampleCount_1;
        current_2 = head(rest_1);
        consumed_2 = candConsumed;
        quantile_2 = quantile_1;
        rest_2 = sameHead[1];
        error_1 = error_2;
        sampleCount_1 = sampleCount_2;
        current_1 = current_2;
        consumed_1 = consumed_2;
        quantile_1 = quantile_2;
        rest_1 = rest_2;
        continue;
      }
      case "Some": {
        const prevError = Math.abs(desired - current_1.value);
        if (candError < prevError) {
          error_2 = error_1;
          sampleCount_2 = sampleCount_1;
          current_2 = head(rest_1);
          consumed_2 = candConsumed;
          quantile_2 = quantile_1;
          rest_2 = sameHead[1];
          error_1 = error_2;
          sampleCount_1 = sampleCount_2;
          current_1 = current_2;
          consumed_1 = consumed_2;
          quantile_1 = quantile_2;
          rest_1 = rest_2;
          continue;
        }
        return {
          quantile: quantile_1,
          value: some2(current_1.value),
          consumed: consumed_1,
          rest: rest_1
        };
      }
    }
  }
  throw new Error("BUG: MetricHook.resolveQuantiles - please report an issue at https://github.com/Effect-TS/effect/issues");
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/pair.js
var MetricPairSymbolKey = "effect/MetricPair";
var MetricPairTypeId = Symbol.for(MetricPairSymbolKey);
var metricPairVariance = {
  /* c8 ignore next */
  _Type: (_) => _
};
var make26 = (metricKey, metricState) => {
  return {
    [MetricPairTypeId]: metricPairVariance,
    metricKey,
    metricState,
    pipe() {
      return pipeArguments(this, arguments);
    }
  };
};
var unsafeMake7 = (metricKey, metricState) => {
  return {
    [MetricPairTypeId]: metricPairVariance,
    metricKey,
    metricState,
    pipe() {
      return pipeArguments(this, arguments);
    }
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/registry.js
var MetricRegistrySymbolKey = "effect/MetricRegistry";
var MetricRegistryTypeId = Symbol.for(MetricRegistrySymbolKey);
var MetricRegistryImpl = class {
  [MetricRegistryTypeId] = MetricRegistryTypeId;
  map = empty14();
  snapshot() {
    const result = [];
    for (const [key, hook] of this.map) {
      result.push(unsafeMake7(key, hook.get()));
    }
    return result;
  }
  get(key) {
    const hook = pipe(this.map, get7(key), getOrUndefined);
    if (hook == null) {
      if (isCounterKey(key.keyType)) {
        return this.getCounter(key);
      }
      if (isGaugeKey(key.keyType)) {
        return this.getGauge(key);
      }
      if (isFrequencyKey(key.keyType)) {
        return this.getFrequency(key);
      }
      if (isHistogramKey(key.keyType)) {
        return this.getHistogram(key);
      }
      if (isSummaryKey(key.keyType)) {
        return this.getSummary(key);
      }
      throw new Error("BUG: MetricRegistry.get - unknown MetricKeyType - please report an issue at https://github.com/Effect-TS/effect/issues");
    } else {
      return hook;
    }
  }
  getCounter(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const counter6 = counter4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, counter6));
      }
      value2 = counter6;
    }
    return value2;
  }
  getFrequency(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const frequency6 = frequency4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, frequency6));
      }
      value2 = frequency6;
    }
    return value2;
  }
  getGauge(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const gauge6 = gauge4(key, key.keyType.bigint ? BigInt(0) : 0);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, gauge6));
      }
      value2 = gauge6;
    }
    return value2;
  }
  getHistogram(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const histogram6 = histogram4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, histogram6));
      }
      value2 = histogram6;
    }
    return value2;
  }
  getSummary(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const summary6 = summary4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, summary6));
      }
      value2 = summary6;
    }
    return value2;
  }
};
var make27 = () => {
  return new MetricRegistryImpl();
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric.js
var MetricSymbolKey = "effect/Metric";
var MetricTypeId = Symbol.for(MetricSymbolKey);
var metricVariance = {
  /* c8 ignore next */
  _Type: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _Out: (_) => _
};
var globalMetricRegistry = globalValue(/* @__PURE__ */ Symbol.for("effect/Metric/globalMetricRegistry"), () => make27());
var make28 = function(keyType, unsafeUpdate, unsafeValue, unsafeModify) {
  const metric = Object.assign((effect) => tap(effect, (a) => update5(metric, a)), {
    [MetricTypeId]: metricVariance,
    keyType,
    unsafeUpdate,
    unsafeValue,
    unsafeModify,
    register() {
      this.unsafeValue([]);
      return this;
    },
    pipe() {
      return pipeArguments(this, arguments);
    }
  });
  return metric;
};
var mapInput3 = dual(2, (self, f) => make28(self.keyType, (input, extraTags) => self.unsafeUpdate(f(input), extraTags), self.unsafeValue, (input, extraTags) => self.unsafeModify(f(input), extraTags)));
var counter5 = (name, options) => fromMetricKey(counter2(name, options));
var frequency5 = (name, options) => fromMetricKey(frequency2(name, options));
var withConstantInput = dual(2, (self, input) => mapInput3(self, () => input));
var fromMetricKey = (key) => {
  let untaggedHook;
  const hookCache = /* @__PURE__ */ new WeakMap();
  const hook = (extraTags) => {
    if (extraTags.length === 0) {
      if (untaggedHook !== void 0) {
        return untaggedHook;
      }
      untaggedHook = globalMetricRegistry.get(key);
      return untaggedHook;
    }
    let hook2 = hookCache.get(extraTags);
    if (hook2 !== void 0) {
      return hook2;
    }
    hook2 = globalMetricRegistry.get(taggedWithLabels(key, extraTags));
    hookCache.set(extraTags, hook2);
    return hook2;
  };
  return make28(key.keyType, (input, extraTags) => hook(extraTags).update(input), (extraTags) => hook(extraTags).get(), (input, extraTags) => hook(extraTags).modify(input));
};
var gauge5 = (name, options) => fromMetricKey(gauge2(name, options));
var histogram5 = (name, boundaries, description) => fromMetricKey(histogram2(name, boundaries, description));
var increment2 = (self) => isCounterKey(self.keyType) ? update5(self, self.keyType.bigint ? BigInt(1) : 1) : modify4(self, self.keyType.bigint ? BigInt(1) : 1);
var incrementBy = dual(2, (self, amount) => isCounterKey(self.keyType) ? update5(self, amount) : modify4(self, amount));
var map9 = dual(2, (self, f) => make28(self.keyType, self.unsafeUpdate, (extraTags) => f(self.unsafeValue(extraTags)), self.unsafeModify));
var mapType = dual(2, (self, f) => make28(f(self.keyType), self.unsafeUpdate, self.unsafeValue, self.unsafeModify));
var modify4 = dual(2, (self, input) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeModify(input, tags))));
var set6 = dual(2, (self, value2) => update5(self, value2));
var succeed6 = (out) => make28(void 0, constVoid, () => out, constVoid);
var sync4 = (evaluate2) => make28(void 0, constVoid, evaluate2, constVoid);
var summary5 = (options) => withNow(summaryTimestamp(options));
var summaryTimestamp = (options) => fromMetricKey(summary2(options));
var tagged2 = dual(3, (self, key, value2) => taggedWithLabels2(self, [make19(key, value2)]));
var taggedWithLabelsInput = dual(2, (self, f) => map9(make28(self.keyType, (input, extraTags) => self.unsafeUpdate(input, union(f(input), extraTags)), self.unsafeValue, (input, extraTags) => self.unsafeModify(input, union(f(input), extraTags))), constVoid));
var taggedWithLabels2 = dual(2, (self, extraTags) => {
  return make28(self.keyType, (input, extraTags1) => self.unsafeUpdate(input, union(extraTags, extraTags1)), (extraTags1) => self.unsafeValue(union(extraTags, extraTags1)), (input, extraTags1) => self.unsafeModify(input, union(extraTags, extraTags1)));
});
var timer2 = (name, description) => {
  const boundaries = exponential({
    start: 0.5,
    factor: 2,
    count: 35
  });
  const base = pipe(histogram5(name, boundaries, description), tagged2("time_unit", "milliseconds"));
  return mapInput3(base, toMillis);
};
var timerWithBoundaries = (name, boundaries, description) => {
  const base = pipe(histogram5(name, fromIterable9(boundaries), description), tagged2("time_unit", "milliseconds"));
  return mapInput3(base, toMillis);
};
var trackAll = dual(2, (self, input) => (effect) => matchCauseEffect(effect, {
  onFailure: (cause2) => zipRight(update5(self, input), failCause(cause2)),
  onSuccess: (value2) => zipRight(update5(self, input), succeed(value2))
}));
var trackDefect = dual(2, (self, metric) => trackDefectWith(self, metric, identity));
var trackDefectWith = dual(3, (self, metric, f) => {
  const updater = (defect) => update5(metric, f(defect));
  return tapDefect2(self, (cause2) => forEachSequentialDiscard(defects(cause2), updater));
});
var trackDuration = dual(2, (self, metric) => trackDurationWith(self, metric, identity));
var trackDurationWith = dual(3, (self, metric, f) => clockWith2((clock2) => {
  const startTime = clock2.unsafeCurrentTimeNanos();
  return tap(self, (_) => {
    const endTime = clock2.unsafeCurrentTimeNanos();
    const duration = nanos(endTime - startTime);
    return update5(metric, f(duration));
  });
}));
var trackError = dual(2, (self, metric) => trackErrorWith(self, metric, (a) => a));
var trackErrorWith = dual(3, (self, metric, f) => {
  const updater = (error) => update5(metric, f(error));
  return tapError2(self, updater);
});
var trackSuccess = dual(2, (self, metric) => trackSuccessWith(self, metric, (a) => a));
var trackSuccessWith = dual(3, (self, metric, f) => {
  const updater = (value2) => update5(metric, f(value2));
  return tap(self, updater);
});
var update5 = dual(2, (self, input) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeUpdate(input, tags))));
var value = (self) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeValue(tags)));
var withNow = (self) => mapInput3(self, (input) => [input, Date.now()]);
var zip7 = dual(2, (self, that) => make28([self.keyType, that.keyType], (input, extraTags) => {
  const [l, r] = input;
  self.unsafeUpdate(l, extraTags);
  that.unsafeUpdate(r, extraTags);
}, (extraTags) => [self.unsafeValue(extraTags), that.unsafeValue(extraTags)], (input, extraTags) => {
  const [l, r] = input;
  self.unsafeModify(l, extraTags);
  that.unsafeModify(r, extraTags);
}));
var unsafeSnapshot = () => globalMetricRegistry.snapshot();
var snapshot = sync(unsafeSnapshot);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/request.js
var RequestSymbolKey = "effect/Request";
var RequestTypeId = Symbol.for(RequestSymbolKey);
var requestVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var RequestPrototype = {
  ...StructuralPrototype,
  [RequestTypeId]: requestVariance
};
var isRequest = (u) => hasProperty(u, RequestTypeId);
var of4 = () => (args2) => Object.assign(Object.create(RequestPrototype), args2);
var tagged3 = (tag) => (args2) => {
  const request = Object.assign(Object.create(RequestPrototype), args2);
  request._tag = tag;
  return request;
};
var Class2 = (function() {
  function Class3(args2) {
    if (args2) {
      Object.assign(this, args2);
    }
  }
  Class3.prototype = RequestPrototype;
  return Class3;
})();
var TaggedClass = (tag) => {
  return class TaggedClass extends Class2 {
    _tag = tag;
  };
};
var complete = dual(2, (self, result) => fiberRefGetWith(currentRequestMap, (map10) => sync(() => {
  if (map10.has(self)) {
    const entry = map10.get(self);
    if (!entry.state.completed) {
      entry.state.completed = true;
      deferredUnsafeDone(entry.result, result);
    }
  }
})));
var completeEffect = dual(2, (self, effect) => matchEffect(effect, {
  onFailure: (error) => complete(self, exitFail(error)),
  onSuccess: (value2) => complete(self, exitSucceed(value2))
}));
var fail5 = dual(2, (self, error) => complete(self, exitFail(error)));
var failCause5 = dual(2, (self, cause2) => complete(self, exitFailCause(cause2)));
var succeed7 = dual(2, (self, value2) => complete(self, exitSucceed(value2)));
var Listeners = class {
  count = 0;
  observers = /* @__PURE__ */ new Set();
  interrupted = false;
  addObserver(f) {
    this.observers.add(f);
  }
  removeObserver(f) {
    this.observers.delete(f);
  }
  increment() {
    this.count++;
    this.observers.forEach((f) => f(this.count));
  }
  decrement() {
    this.count--;
    this.observers.forEach((f) => f(this.count));
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/supervisor.js
var SupervisorSymbolKey = "effect/Supervisor";
var SupervisorTypeId = Symbol.for(SupervisorSymbolKey);
var supervisorVariance = {
  /* c8 ignore next */
  _T: (_) => _
};
var ProxySupervisor = class _ProxySupervisor {
  underlying;
  value0;
  [SupervisorTypeId] = supervisorVariance;
  constructor(underlying, value0) {
    this.underlying = underlying;
    this.value0 = value0;
  }
  get value() {
    return this.value0;
  }
  onStart(context3, effect, parent, fiber) {
    this.underlying.onStart(context3, effect, parent, fiber);
  }
  onEnd(value2, fiber) {
    this.underlying.onEnd(value2, fiber);
  }
  onEffect(fiber, effect) {
    this.underlying.onEffect(fiber, effect);
  }
  onSuspend(fiber) {
    this.underlying.onSuspend(fiber);
  }
  onResume(fiber) {
    this.underlying.onResume(fiber);
  }
  map(f) {
    return new _ProxySupervisor(this, pipe(this.value, map3(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
};
var Zip = class _Zip {
  left;
  right;
  _tag = "Zip";
  [SupervisorTypeId] = supervisorVariance;
  constructor(left2, right2) {
    this.left = left2;
    this.right = right2;
  }
  get value() {
    return zip3(this.left.value, this.right.value);
  }
  onStart(context3, effect, parent, fiber) {
    this.left.onStart(context3, effect, parent, fiber);
    this.right.onStart(context3, effect, parent, fiber);
  }
  onEnd(value2, fiber) {
    this.left.onEnd(value2, fiber);
    this.right.onEnd(value2, fiber);
  }
  onEffect(fiber, effect) {
    this.left.onEffect(fiber, effect);
    this.right.onEffect(fiber, effect);
  }
  onSuspend(fiber) {
    this.left.onSuspend(fiber);
    this.right.onSuspend(fiber);
  }
  onResume(fiber) {
    this.left.onResume(fiber);
    this.right.onResume(fiber);
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map3(f)));
  }
  zip(right2) {
    return new _Zip(this, right2);
  }
};
var isZip = (self) => hasProperty(self, SupervisorTypeId) && isTagged(self, "Zip");
var Track = class {
  [SupervisorTypeId] = supervisorVariance;
  fibers = /* @__PURE__ */ new Set();
  get value() {
    return sync(() => Array.from(this.fibers));
  }
  onStart(_context, _effect, _parent, fiber) {
    this.fibers.add(fiber);
  }
  onEnd(_value, fiber) {
    this.fibers.delete(fiber);
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map3(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var Const = class {
  effect;
  [SupervisorTypeId] = supervisorVariance;
  constructor(effect) {
    this.effect = effect;
  }
  get value() {
    return this.effect;
  }
  onStart(_context, _effect, _parent, _fiber) {
  }
  onEnd(_value, _fiber) {
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map3(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var FibersIn = class {
  ref;
  [SupervisorTypeId] = supervisorVariance;
  constructor(ref) {
    this.ref = ref;
  }
  get value() {
    return sync(() => get(this.ref));
  }
  onStart(_context, _effect, _parent, fiber) {
    pipe(this.ref, set(pipe(get(this.ref), add2(fiber))));
  }
  onEnd(_value, fiber) {
    pipe(this.ref, set(pipe(get(this.ref), remove2(fiber))));
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map3(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var unsafeTrack = () => {
  return new Track();
};
var track = sync(unsafeTrack);
var fromEffect2 = (effect) => {
  return new Const(effect);
};
var none7 = globalValue("effect/Supervisor/none", () => fromEffect2(void_));
var fibersIn = (ref) => sync(() => new FibersIn(ref));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/supervisor/patch.js
var OP_EMPTY2 = "Empty";
var OP_ADD_SUPERVISOR = "AddSupervisor";
var OP_REMOVE_SUPERVISOR = "RemoveSupervisor";
var OP_AND_THEN2 = "AndThen";
var empty19 = {
  _tag: OP_EMPTY2
};
var combine6 = (self, that) => {
  return {
    _tag: OP_AND_THEN2,
    first: self,
    second: that
  };
};
var patch6 = (self, supervisor) => {
  return patchLoop(supervisor, of2(self));
};
var patchLoop = (_supervisor, _patches) => {
  let supervisor = _supervisor;
  let patches = _patches;
  while (isNonEmpty(patches)) {
    const head3 = headNonEmpty2(patches);
    switch (head3._tag) {
      case OP_EMPTY2: {
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_ADD_SUPERVISOR: {
        supervisor = supervisor.zip(head3.supervisor);
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_REMOVE_SUPERVISOR: {
        supervisor = removeSupervisor(supervisor, head3.supervisor);
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_AND_THEN2: {
        patches = prepend2(head3.first)(prepend2(head3.second)(tailNonEmpty2(patches)));
        break;
      }
    }
  }
  return supervisor;
};
var removeSupervisor = (self, that) => {
  if (equals(self, that)) {
    return none7;
  } else {
    if (isZip(self)) {
      return removeSupervisor(self.left, that).zip(removeSupervisor(self.right, that));
    } else {
      return self;
    }
  }
};
var toSet = (self) => {
  if (equals(self, none7)) {
    return empty4();
  } else {
    if (isZip(self)) {
      return pipe(toSet(self.left), union2(toSet(self.right)));
    } else {
      return make5(self);
    }
  }
};
var diff5 = (oldValue, newValue) => {
  if (equals(oldValue, newValue)) {
    return empty19;
  }
  const oldSupervisors = toSet(oldValue);
  const newSupervisors = toSet(newValue);
  const added = pipe(newSupervisors, difference(oldSupervisors), reduce3(empty19, (patch7, supervisor) => combine6(patch7, {
    _tag: OP_ADD_SUPERVISOR,
    supervisor
  })));
  const removed = pipe(oldSupervisors, difference(newSupervisors), reduce3(empty19, (patch7, supervisor) => combine6(patch7, {
    _tag: OP_REMOVE_SUPERVISOR,
    supervisor
  })));
  return combine6(added, removed);
};
var differ = make24({
  empty: empty19,
  patch: patch6,
  combine: combine6,
  diff: diff5
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberRuntime.js
var fiberStarted = counter5("effect_fiber_started", {
  incremental: true
});
var fiberActive = counter5("effect_fiber_active");
var fiberSuccesses = counter5("effect_fiber_successes", {
  incremental: true
});
var fiberFailures = counter5("effect_fiber_failures", {
  incremental: true
});
var fiberLifetimes = tagged2(histogram5("effect_fiber_lifetimes", exponential({
  start: 0.5,
  factor: 2,
  count: 35
})), "time_unit", "milliseconds");
var EvaluationSignalContinue = "Continue";
var EvaluationSignalDone = "Done";
var EvaluationSignalYieldNow = "Yield";
var runtimeFiberVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var absurd = (_) => {
  throw new Error(`BUG: FiberRuntime - ${toStringUnknown(_)} - please report an issue at https://github.com/Effect-TS/effect/issues`);
};
var YieldedOp = /* @__PURE__ */ Symbol.for("effect/internal/fiberRuntime/YieldedOp");
var yieldedOpChannel = globalValue("effect/internal/fiberRuntime/yieldedOpChannel", () => ({
  currentOp: null
}));
var contOpSuccess = {
  [OP_ON_SUCCESS]: (_, cont, value2) => {
    return internalCall(() => cont.effect_instruction_i1(value2));
  },
  ["OnStep"]: (_, _cont, value2) => {
    return exitSucceed(exitSucceed(value2));
  },
  [OP_ON_SUCCESS_AND_FAILURE]: (_, cont, value2) => {
    return internalCall(() => cont.effect_instruction_i2(value2));
  },
  [OP_REVERT_FLAGS]: (self, cont, value2) => {
    self.patchRuntimeFlags(self.currentRuntimeFlags, cont.patch);
    if (interruptible(self.currentRuntimeFlags) && self.isInterrupted()) {
      return exitFailCause(self.getInterruptedCause());
    } else {
      return exitSucceed(value2);
    }
  },
  [OP_WHILE]: (self, cont, value2) => {
    internalCall(() => cont.effect_instruction_i2(value2));
    if (internalCall(() => cont.effect_instruction_i0())) {
      self.pushStack(cont);
      return internalCall(() => cont.effect_instruction_i1());
    } else {
      return void_;
    }
  },
  [OP_ITERATOR]: (self, cont, value2) => {
    while (true) {
      const state = internalCall(() => cont.effect_instruction_i0.next(value2));
      if (state.done) {
        return exitSucceed(state.value);
      }
      const primitive = yieldWrapGet(state.value);
      if (!exitIsExit(primitive)) {
        self.pushStack(cont);
        return primitive;
      } else if (primitive._tag === "Failure") {
        return primitive;
      }
      value2 = primitive.value;
    }
  }
};
var drainQueueWhileRunningTable = {
  [OP_INTERRUPT_SIGNAL]: (self, runtimeFlags2, cur, message) => {
    self.processNewInterruptSignal(message.cause);
    return interruptible(runtimeFlags2) ? exitFailCause(message.cause) : cur;
  },
  [OP_RESUME]: (_self, _runtimeFlags, _cur, _message) => {
    throw new Error("It is illegal to have multiple concurrent run loops in a single fiber");
  },
  [OP_STATEFUL]: (self, runtimeFlags2, cur, message) => {
    message.onFiber(self, running2(runtimeFlags2));
    return cur;
  },
  [OP_YIELD_NOW]: (_self, _runtimeFlags, cur, _message) => {
    return flatMap3(yieldNow(), () => cur);
  }
};
var runBlockedRequests = (self) => forEachSequentialDiscard(flatten2(self), (requestsByRequestResolver) => forEachConcurrentDiscard(sequentialCollectionToChunk(requestsByRequestResolver), ([dataSource, sequential4]) => {
  const map10 = /* @__PURE__ */ new Map();
  const arr = [];
  for (const block of sequential4) {
    arr.push(toReadonlyArray(block));
    for (const entry of block) {
      map10.set(entry.request, entry);
    }
  }
  const flat = arr.flat();
  return fiberRefLocally(invokeWithInterrupt(dataSource.runAll(arr), flat, () => flat.forEach((entry) => {
    entry.listeners.interrupted = true;
  })), currentRequestMap, map10);
}, false, false));
var _version = getCurrentVersion();
var FiberRuntime = class extends Class {
  [FiberTypeId] = fiberVariance2;
  [RuntimeFiberTypeId] = runtimeFiberVariance;
  _fiberRefs;
  _fiberId;
  _queue = new Array();
  _children = null;
  _observers = new Array();
  _running = false;
  _stack = [];
  _asyncInterruptor = null;
  _asyncBlockingOn = null;
  _exitValue = null;
  _steps = [];
  _isYielding = false;
  currentRuntimeFlags;
  currentOpCount = 0;
  currentSupervisor;
  currentScheduler;
  currentTracer;
  currentSpan;
  currentContext;
  currentDefaultServices;
  constructor(fiberId2, fiberRefs0, runtimeFlags0) {
    super();
    this.currentRuntimeFlags = runtimeFlags0;
    this._fiberId = fiberId2;
    this._fiberRefs = fiberRefs0;
    if (runtimeMetrics(runtimeFlags0)) {
      const tags = this.getFiberRef(currentMetricLabels);
      fiberStarted.unsafeUpdate(1, tags);
      fiberActive.unsafeUpdate(1, tags);
    }
    this.refreshRefCache();
  }
  commit() {
    return join2(this);
  }
  /**
   * The identity of the fiber.
   */
  id() {
    return this._fiberId;
  }
  /**
   * Begins execution of the effect associated with this fiber on in the
   * background. This can be called to "kick off" execution of a fiber after
   * it has been created.
   */
  resume(effect) {
    this.tell(resume(effect));
  }
  /**
   * The status of the fiber.
   */
  get status() {
    return this.ask((_, status2) => status2);
  }
  /**
   * Gets the fiber runtime flags.
   */
  get runtimeFlags() {
    return this.ask((state, status2) => {
      if (isDone2(status2)) {
        return state.currentRuntimeFlags;
      }
      return status2.runtimeFlags;
    });
  }
  /**
   * Returns the current `FiberScope` for the fiber.
   */
  scope() {
    return unsafeMake6(this);
  }
  /**
   * Retrieves the immediate children of the fiber.
   */
  get children() {
    return this.ask((fiber) => Array.from(fiber.getChildren()));
  }
  /**
   * Gets the fiber's set of children.
   */
  getChildren() {
    if (this._children === null) {
      this._children = /* @__PURE__ */ new Set();
    }
    return this._children;
  }
  /**
   * Retrieves the interrupted cause of the fiber, which will be `Cause.empty`
   * if the fiber has not been interrupted.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getInterruptedCause() {
    return this.getFiberRef(currentInterruptedCause);
  }
  /**
   * Retrieves the whole set of fiber refs.
   */
  fiberRefs() {
    return this.ask((fiber) => fiber.getFiberRefs());
  }
  /**
   * Returns an effect that will contain information computed from the fiber
   * state and status while running on the fiber.
   *
   * This allows the outside world to interact safely with mutable fiber state
   * without locks or immutable data.
   */
  ask(f) {
    return suspend(() => {
      const deferred = deferredUnsafeMake(this._fiberId);
      this.tell(stateful((fiber, status2) => {
        deferredUnsafeDone(deferred, sync(() => f(fiber, status2)));
      }));
      return deferredAwait(deferred);
    });
  }
  /**
   * Adds a message to be processed by the fiber on the fiber.
   */
  tell(message) {
    this._queue.push(message);
    if (!this._running) {
      this._running = true;
      this.drainQueueLaterOnExecutor();
    }
  }
  get await() {
    return async_((resume2) => {
      const cb = (exit3) => resume2(succeed(exit3));
      if (this._exitValue !== null) {
        cb(this._exitValue);
        return;
      }
      this.tell(stateful((fiber, _) => {
        if (fiber._exitValue !== null) {
          cb(this._exitValue);
        } else {
          fiber.addObserver(cb);
        }
      }));
      return sync(() => this.tell(stateful((fiber, _) => {
        fiber.removeObserver(cb);
      })));
    }, this.id());
  }
  get inheritAll() {
    return withFiberRuntime((parentFiber, parentStatus) => {
      const parentFiberId = parentFiber.id();
      const parentFiberRefs = parentFiber.getFiberRefs();
      const parentRuntimeFlags = parentStatus.runtimeFlags;
      const childFiberRefs = this.getFiberRefs();
      const updatedFiberRefs = joinAs(parentFiberRefs, parentFiberId, childFiberRefs);
      parentFiber.setFiberRefs(updatedFiberRefs);
      const updatedRuntimeFlags = parentFiber.getFiberRef(currentRuntimeFlags);
      const patch7 = pipe(
        diff(parentRuntimeFlags, updatedRuntimeFlags),
        // Do not inherit WindDown or Interruption!
        exclude(Interruption),
        exclude(WindDown)
      );
      return updateRuntimeFlags(patch7);
    });
  }
  /**
   * Tentatively observes the fiber, but returns immediately if it is not
   * already done.
   */
  get poll() {
    return sync(() => fromNullable(this._exitValue));
  }
  /**
   * Unsafely observes the fiber, but returns immediately if it is not
   * already done.
   */
  unsafePoll() {
    return this._exitValue;
  }
  /**
   * In the background, interrupts the fiber as if interrupted from the specified fiber.
   */
  interruptAsFork(fiberId2) {
    return sync(() => this.tell(interruptSignal(interrupt(fiberId2))));
  }
  /**
   * In the background, interrupts the fiber as if interrupted from the specified fiber.
   */
  unsafeInterruptAsFork(fiberId2) {
    this.tell(interruptSignal(interrupt(fiberId2)));
  }
  /**
   * Adds an observer to the list of observers.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addObserver(observer) {
    if (this._exitValue !== null) {
      observer(this._exitValue);
    } else {
      this._observers.push(observer);
    }
  }
  /**
   * Removes the specified observer from the list of observers that will be
   * notified when the fiber exits.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  removeObserver(observer) {
    this._observers = this._observers.filter((o) => o !== observer);
  }
  /**
   * Retrieves all fiber refs of the fiber.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getFiberRefs() {
    this.setFiberRef(currentRuntimeFlags, this.currentRuntimeFlags);
    return this._fiberRefs;
  }
  /**
   * Deletes the specified fiber ref.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  unsafeDeleteFiberRef(fiberRef) {
    this._fiberRefs = delete_(this._fiberRefs, fiberRef);
  }
  /**
   * Retrieves the state of the fiber ref, or else its initial value.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getFiberRef(fiberRef) {
    if (this._fiberRefs.locals.has(fiberRef)) {
      return this._fiberRefs.locals.get(fiberRef)[0][1];
    }
    return fiberRef.initial;
  }
  /**
   * Sets the fiber ref to the specified value.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  setFiberRef(fiberRef, value2) {
    this._fiberRefs = updateAs(this._fiberRefs, {
      fiberId: this._fiberId,
      fiberRef,
      value: value2
    });
    this.refreshRefCache();
  }
  refreshRefCache() {
    this.currentDefaultServices = this.getFiberRef(currentServices);
    this.currentTracer = this.currentDefaultServices.unsafeMap.get(tracerTag.key);
    this.currentSupervisor = this.getFiberRef(currentSupervisor);
    this.currentScheduler = this.getFiberRef(currentScheduler);
    this.currentContext = this.getFiberRef(currentContext);
    this.currentSpan = this.currentContext.unsafeMap.get(spanTag.key);
  }
  /**
   * Wholesale replaces all fiber refs of this fiber.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  setFiberRefs(fiberRefs4) {
    this._fiberRefs = fiberRefs4;
    this.refreshRefCache();
  }
  /**
   * Adds a reference to the specified fiber inside the children set.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addChild(child) {
    this.getChildren().add(child);
  }
  /**
   * Removes a reference to the specified fiber inside the children set.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  removeChild(child) {
    this.getChildren().delete(child);
  }
  /**
   * Transfers all children of this fiber that are currently running to the
   * specified fiber scope.
   *
   * **NOTE**: This method must be invoked by the fiber itself after it has
   * evaluated the effects but prior to exiting.
   */
  transferChildren(scope3) {
    const children2 = this._children;
    this._children = null;
    if (children2 !== null && children2.size > 0) {
      for (const child of children2) {
        if (child._exitValue === null) {
          scope3.add(this.currentRuntimeFlags, child);
        }
      }
    }
  }
  /**
   * On the current thread, executes all messages in the fiber's inbox. This
   * method may return before all work is done, in the event the fiber executes
   * an asynchronous operation.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueOnCurrentThread() {
    let recurse = true;
    while (recurse) {
      let evaluationSignal = EvaluationSignalContinue;
      const prev = globalThis[currentFiberURI];
      globalThis[currentFiberURI] = this;
      try {
        while (evaluationSignal === EvaluationSignalContinue) {
          evaluationSignal = this._queue.length === 0 ? EvaluationSignalDone : this.evaluateMessageWhileSuspended(this._queue.splice(0, 1)[0]);
        }
      } finally {
        this._running = false;
        globalThis[currentFiberURI] = prev;
      }
      if (this._queue.length > 0 && !this._running) {
        this._running = true;
        if (evaluationSignal === EvaluationSignalYieldNow) {
          this.drainQueueLaterOnExecutor();
          recurse = false;
        } else {
          recurse = true;
        }
      } else {
        recurse = false;
      }
    }
  }
  /**
   * Schedules the execution of all messages in the fiber's inbox.
   *
   * This method will return immediately after the scheduling
   * operation is completed, but potentially before such messages have been
   * executed.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueLaterOnExecutor() {
    this.currentScheduler.scheduleTask(this.run, this.getFiberRef(currentSchedulingPriority));
  }
  /**
   * Drains the fiber's message queue while the fiber is actively running,
   * returning the next effect to execute, which may be the input effect if no
   * additional effect needs to be executed.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueWhileRunning(runtimeFlags2, cur0) {
    let cur = cur0;
    while (this._queue.length > 0) {
      const message = this._queue.splice(0, 1)[0];
      cur = drainQueueWhileRunningTable[message._tag](this, runtimeFlags2, cur, message);
    }
    return cur;
  }
  /**
   * Determines if the fiber is interrupted.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  isInterrupted() {
    return !isEmpty(this.getFiberRef(currentInterruptedCause));
  }
  /**
   * Adds an interruptor to the set of interruptors that are interrupting this
   * fiber.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addInterruptedCause(cause2) {
    const oldSC = this.getFiberRef(currentInterruptedCause);
    this.setFiberRef(currentInterruptedCause, sequential(oldSC, cause2));
  }
  /**
   * Processes a new incoming interrupt signal.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  processNewInterruptSignal(cause2) {
    this.addInterruptedCause(cause2);
    this.sendInterruptSignalToAllChildren();
  }
  /**
   * Interrupts all children of the current fiber, returning an effect that will
   * await the exit of the children. This method will return null if the fiber
   * has no children.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  sendInterruptSignalToAllChildren() {
    if (this._children === null || this._children.size === 0) {
      return false;
    }
    let told = false;
    for (const child of this._children) {
      child.tell(interruptSignal(interrupt(this.id())));
      told = true;
    }
    return told;
  }
  /**
   * Interrupts all children of the current fiber, returning an effect that will
   * await the exit of the children. This method will return null if the fiber
   * has no children.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  interruptAllChildren() {
    if (this.sendInterruptSignalToAllChildren()) {
      const it = this._children.values();
      this._children = null;
      let isDone3 = false;
      const body = () => {
        const next2 = it.next();
        if (!next2.done) {
          return asVoid(next2.value.await);
        } else {
          return sync(() => {
            isDone3 = true;
          });
        }
      };
      return whileLoop({
        while: () => !isDone3,
        body,
        step: () => {
        }
      });
    }
    return null;
  }
  reportExitValue(exit3) {
    if (runtimeMetrics(this.currentRuntimeFlags)) {
      const tags = this.getFiberRef(currentMetricLabels);
      const startTimeMillis = this.id().startTimeMillis;
      const endTimeMillis = Date.now();
      fiberLifetimes.unsafeUpdate(endTimeMillis - startTimeMillis, tags);
      fiberActive.unsafeUpdate(-1, tags);
      switch (exit3._tag) {
        case OP_SUCCESS: {
          fiberSuccesses.unsafeUpdate(1, tags);
          break;
        }
        case OP_FAILURE: {
          fiberFailures.unsafeUpdate(1, tags);
          break;
        }
      }
    }
    if (exit3._tag === "Failure") {
      const level = this.getFiberRef(currentUnhandledErrorLogLevel);
      if (!isInterruptedOnly(exit3.cause) && level._tag === "Some") {
        this.log("Fiber terminated with an unhandled error", exit3.cause, level);
      }
    }
  }
  setExitValue(exit3) {
    this._exitValue = exit3;
    this.reportExitValue(exit3);
    for (let i = this._observers.length - 1; i >= 0; i--) {
      this._observers[i](exit3);
    }
    this._observers = [];
  }
  getLoggers() {
    return this.getFiberRef(currentLoggers);
  }
  log(message, cause2, overrideLogLevel) {
    const logLevel = isSome(overrideLogLevel) ? overrideLogLevel.value : this.getFiberRef(currentLogLevel);
    const minimumLogLevel = this.getFiberRef(currentMinimumLogLevel);
    if (greaterThan3(minimumLogLevel, logLevel)) {
      return;
    }
    const spans = this.getFiberRef(currentLogSpan);
    const annotations = this.getFiberRef(currentLogAnnotations);
    const loggers = this.getLoggers();
    const contextMap = this.getFiberRefs();
    if (size(loggers) > 0) {
      const clockService = get2(this.getFiberRef(currentServices), clockTag);
      const date = new Date(clockService.unsafeCurrentTimeMillis());
      withRedactableContext(contextMap, () => {
        for (const logger of loggers) {
          logger.log({
            fiberId: this.id(),
            logLevel,
            message,
            cause: cause2,
            context: contextMap,
            spans,
            annotations,
            date
          });
        }
      });
    }
  }
  /**
   * Evaluates a single message on the current thread, while the fiber is
   * suspended. This method should only be called while evaluation of the
   * fiber's effect is suspended due to an asynchronous operation.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  evaluateMessageWhileSuspended(message) {
    switch (message._tag) {
      case OP_YIELD_NOW: {
        return EvaluationSignalYieldNow;
      }
      case OP_INTERRUPT_SIGNAL: {
        this.processNewInterruptSignal(message.cause);
        if (this._asyncInterruptor !== null) {
          this._asyncInterruptor(exitFailCause(message.cause));
          this._asyncInterruptor = null;
        }
        return EvaluationSignalContinue;
      }
      case OP_RESUME: {
        this._asyncInterruptor = null;
        this._asyncBlockingOn = null;
        this.evaluateEffect(message.effect);
        return EvaluationSignalContinue;
      }
      case OP_STATEFUL: {
        message.onFiber(this, this._exitValue !== null ? done2 : suspended2(this.currentRuntimeFlags, this._asyncBlockingOn));
        return EvaluationSignalContinue;
      }
      default: {
        return absurd(message);
      }
    }
  }
  /**
   * Evaluates an effect until completion, potentially asynchronously.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  evaluateEffect(effect0) {
    this.currentSupervisor.onResume(this);
    try {
      let effect = interruptible(this.currentRuntimeFlags) && this.isInterrupted() ? exitFailCause(this.getInterruptedCause()) : effect0;
      while (effect !== null) {
        const eff = effect;
        const exit3 = this.runLoop(eff);
        if (exit3 === YieldedOp) {
          const op = yieldedOpChannel.currentOp;
          yieldedOpChannel.currentOp = null;
          if (op._op === OP_YIELD) {
            if (cooperativeYielding(this.currentRuntimeFlags)) {
              this.tell(yieldNow3());
              this.tell(resume(exitVoid));
              effect = null;
            } else {
              effect = exitVoid;
            }
          } else if (op._op === OP_ASYNC) {
            effect = null;
          }
        } else {
          this.currentRuntimeFlags = pipe(this.currentRuntimeFlags, enable(WindDown));
          const interruption = this.interruptAllChildren();
          if (interruption !== null) {
            effect = flatMap3(interruption, () => exit3);
          } else {
            if (this._queue.length === 0) {
              this.setExitValue(exit3);
            } else {
              this.tell(resume(exit3));
            }
            effect = null;
          }
        }
      }
    } finally {
      this.currentSupervisor.onSuspend(this);
    }
  }
  /**
   * Begins execution of the effect associated with this fiber on the current
   * thread. This can be called to "kick off" execution of a fiber after it has
   * been created, in hopes that the effect can be executed synchronously.
   *
   * This is not the normal way of starting a fiber, but it is useful when the
   * express goal of executing the fiber is to synchronously produce its exit.
   */
  start(effect) {
    if (!this._running) {
      this._running = true;
      const prev = globalThis[currentFiberURI];
      globalThis[currentFiberURI] = this;
      try {
        this.evaluateEffect(effect);
      } finally {
        this._running = false;
        globalThis[currentFiberURI] = prev;
        if (this._queue.length > 0) {
          this.drainQueueLaterOnExecutor();
        }
      }
    } else {
      this.tell(resume(effect));
    }
  }
  /**
   * Begins execution of the effect associated with this fiber on in the
   * background, and on the correct thread pool. This can be called to "kick
   * off" execution of a fiber after it has been created, in hopes that the
   * effect can be executed synchronously.
   */
  startFork(effect) {
    this.tell(resume(effect));
  }
  /**
   * Takes the current runtime flags, patches them to return the new runtime
   * flags, and then makes any changes necessary to fiber state based on the
   * specified patch.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  patchRuntimeFlags(oldRuntimeFlags, patch7) {
    const newRuntimeFlags = patch(oldRuntimeFlags, patch7);
    globalThis[currentFiberURI] = this;
    this.currentRuntimeFlags = newRuntimeFlags;
    return newRuntimeFlags;
  }
  /**
   * Initiates an asynchronous operation, by building a callback that will
   * resume execution, and then feeding that callback to the registration
   * function, handling error cases and repeated resumptions appropriately.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  initiateAsync(runtimeFlags2, asyncRegister) {
    let alreadyCalled = false;
    const callback = (effect) => {
      if (!alreadyCalled) {
        alreadyCalled = true;
        this.tell(resume(effect));
      }
    };
    if (interruptible(runtimeFlags2)) {
      this._asyncInterruptor = callback;
    }
    try {
      asyncRegister(callback);
    } catch (e) {
      callback(failCause(die(e)));
    }
  }
  pushStack(cont) {
    this._stack.push(cont);
    if (cont._op === "OnStep") {
      this._steps.push({
        refs: this.getFiberRefs(),
        flags: this.currentRuntimeFlags
      });
    }
  }
  popStack() {
    const item = this._stack.pop();
    if (item) {
      if (item._op === "OnStep") {
        this._steps.pop();
      }
      return item;
    }
    return;
  }
  getNextSuccessCont() {
    let frame = this.popStack();
    while (frame) {
      if (frame._op !== OP_ON_FAILURE) {
        return frame;
      }
      frame = this.popStack();
    }
  }
  getNextFailCont() {
    let frame = this.popStack();
    while (frame) {
      if (frame._op !== OP_ON_SUCCESS && frame._op !== OP_WHILE && frame._op !== OP_ITERATOR) {
        return frame;
      }
      frame = this.popStack();
    }
  }
  [OP_TAG](op) {
    return sync(() => unsafeGet2(this.currentContext, op));
  }
  ["Left"](op) {
    return fail(op.left);
  }
  ["None"](_) {
    return fail(new NoSuchElementException());
  }
  ["Right"](op) {
    return exitSucceed(op.right);
  }
  ["Some"](op) {
    return exitSucceed(op.value);
  }
  ["Micro"](op) {
    return unsafeAsync((microResume) => {
      let resume2 = microResume;
      const fiber = runFork(provideContext2(op, this.currentContext));
      fiber.addObserver((exit3) => {
        if (exit3._tag === "Success") {
          return resume2(exitSucceed(exit3.value));
        }
        switch (exit3.cause._tag) {
          case "Interrupt": {
            return resume2(exitFailCause(interrupt(none3)));
          }
          case "Fail": {
            return resume2(fail(exit3.cause.error));
          }
          case "Die": {
            return resume2(die2(exit3.cause.defect));
          }
        }
      });
      return unsafeAsync((abortResume) => {
        resume2 = (_) => {
          abortResume(void_);
        };
        fiber.unsafeInterrupt();
      });
    });
  }
  [OP_SYNC](op) {
    const value2 = internalCall(() => op.effect_instruction_i0());
    const cont = this.getNextSuccessCont();
    if (cont !== void 0) {
      if (!(cont._op in contOpSuccess)) {
        absurd(cont);
      }
      return contOpSuccess[cont._op](this, cont, value2);
    } else {
      yieldedOpChannel.currentOp = exitSucceed(value2);
      return YieldedOp;
    }
  }
  [OP_SUCCESS](op) {
    const oldCur = op;
    const cont = this.getNextSuccessCont();
    if (cont !== void 0) {
      if (!(cont._op in contOpSuccess)) {
        absurd(cont);
      }
      return contOpSuccess[cont._op](this, cont, oldCur.effect_instruction_i0);
    } else {
      yieldedOpChannel.currentOp = oldCur;
      return YieldedOp;
    }
  }
  [OP_FAILURE](op) {
    const cause2 = op.effect_instruction_i0;
    const cont = this.getNextFailCont();
    if (cont !== void 0) {
      switch (cont._op) {
        case OP_ON_FAILURE:
        case OP_ON_SUCCESS_AND_FAILURE: {
          if (!(interruptible(this.currentRuntimeFlags) && this.isInterrupted())) {
            return internalCall(() => cont.effect_instruction_i1(cause2));
          } else {
            return exitFailCause(stripFailures(cause2));
          }
        }
        case "OnStep": {
          if (!(interruptible(this.currentRuntimeFlags) && this.isInterrupted())) {
            return exitSucceed(exitFailCause(cause2));
          } else {
            return exitFailCause(stripFailures(cause2));
          }
        }
        case OP_REVERT_FLAGS: {
          this.patchRuntimeFlags(this.currentRuntimeFlags, cont.patch);
          if (interruptible(this.currentRuntimeFlags) && this.isInterrupted()) {
            return exitFailCause(sequential(cause2, this.getInterruptedCause()));
          } else {
            return exitFailCause(cause2);
          }
        }
        default: {
          absurd(cont);
        }
      }
    } else {
      yieldedOpChannel.currentOp = exitFailCause(cause2);
      return YieldedOp;
    }
  }
  [OP_WITH_RUNTIME](op) {
    return internalCall(() => op.effect_instruction_i0(this, running2(this.currentRuntimeFlags)));
  }
  ["Blocked"](op) {
    const refs = this.getFiberRefs();
    const flags = this.currentRuntimeFlags;
    if (this._steps.length > 0) {
      const frames = [];
      const snap = this._steps[this._steps.length - 1];
      let frame = this.popStack();
      while (frame && frame._op !== "OnStep") {
        frames.push(frame);
        frame = this.popStack();
      }
      this.setFiberRefs(snap.refs);
      this.currentRuntimeFlags = snap.flags;
      const patchRefs = diff3(snap.refs, refs);
      const patchFlags = diff(snap.flags, flags);
      return exitSucceed(blocked(op.effect_instruction_i0, withFiberRuntime((newFiber) => {
        while (frames.length > 0) {
          newFiber.pushStack(frames.pop());
        }
        newFiber.setFiberRefs(patch3(newFiber.id(), newFiber.getFiberRefs())(patchRefs));
        newFiber.currentRuntimeFlags = patch(patchFlags)(newFiber.currentRuntimeFlags);
        return op.effect_instruction_i1;
      })));
    }
    return uninterruptibleMask((restore) => flatMap3(forkDaemon2(runRequestBlock(op.effect_instruction_i0)), () => restore(op.effect_instruction_i1)));
  }
  ["RunBlocked"](op) {
    return runBlockedRequests(op.effect_instruction_i0);
  }
  [OP_UPDATE_RUNTIME_FLAGS](op) {
    const updateFlags = op.effect_instruction_i0;
    const oldRuntimeFlags = this.currentRuntimeFlags;
    const newRuntimeFlags = patch(oldRuntimeFlags, updateFlags);
    if (interruptible(newRuntimeFlags) && this.isInterrupted()) {
      return exitFailCause(this.getInterruptedCause());
    } else {
      this.patchRuntimeFlags(this.currentRuntimeFlags, updateFlags);
      if (op.effect_instruction_i1) {
        const revertFlags = diff(newRuntimeFlags, oldRuntimeFlags);
        this.pushStack(new RevertFlags(revertFlags, op));
        return internalCall(() => op.effect_instruction_i1(oldRuntimeFlags));
      } else {
        return exitVoid;
      }
    }
  }
  [OP_ON_SUCCESS](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  ["OnStep"](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ON_FAILURE](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ON_SUCCESS_AND_FAILURE](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ASYNC](op) {
    this._asyncBlockingOn = op.effect_instruction_i1;
    this.initiateAsync(this.currentRuntimeFlags, op.effect_instruction_i0);
    yieldedOpChannel.currentOp = op;
    return YieldedOp;
  }
  [OP_YIELD](op) {
    this._isYielding = false;
    yieldedOpChannel.currentOp = op;
    return YieldedOp;
  }
  [OP_WHILE](op) {
    const check = op.effect_instruction_i0;
    const body = op.effect_instruction_i1;
    if (check()) {
      this.pushStack(op);
      return body();
    } else {
      return exitVoid;
    }
  }
  [OP_ITERATOR](op) {
    return contOpSuccess[OP_ITERATOR](this, op, void 0);
  }
  [OP_COMMIT](op) {
    return internalCall(() => op.commit());
  }
  /**
   * The main run-loop for evaluating effects.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  runLoop(effect0) {
    let cur = effect0;
    this.currentOpCount = 0;
    while (true) {
      if ((this.currentRuntimeFlags & OpSupervision) !== 0) {
        this.currentSupervisor.onEffect(this, cur);
      }
      if (this._queue.length > 0) {
        cur = this.drainQueueWhileRunning(this.currentRuntimeFlags, cur);
      }
      if (!this._isYielding) {
        this.currentOpCount += 1;
        const shouldYield = this.currentScheduler.shouldYield(this);
        if (shouldYield !== false) {
          this._isYielding = true;
          this.currentOpCount = 0;
          const oldCur = cur;
          cur = flatMap3(yieldNow({
            priority: shouldYield
          }), () => oldCur);
        }
      }
      try {
        cur = this.currentTracer.context(() => {
          if (_version !== cur[EffectTypeId2]._V) {
            const level = this.getFiberRef(currentVersionMismatchErrorLogLevel);
            if (level._tag === "Some") {
              const effectVersion = cur[EffectTypeId2]._V;
              this.log(`Executing an Effect versioned ${effectVersion} with a Runtime of version ${getCurrentVersion()}, you may want to dedupe the effect dependencies, you can use the language service plugin to detect this at compile time: https://github.com/Effect-TS/language-service`, empty8, level);
            }
          }
          return this[cur._op](cur);
        }, this);
        if (cur === YieldedOp) {
          const op = yieldedOpChannel.currentOp;
          if (op._op === OP_YIELD || op._op === OP_ASYNC) {
            return YieldedOp;
          }
          yieldedOpChannel.currentOp = null;
          return op._op === OP_SUCCESS || op._op === OP_FAILURE ? op : exitFailCause(die(op));
        }
      } catch (e) {
        if (cur !== YieldedOp && !hasProperty(cur, "_op") || !(cur._op in this)) {
          cur = dieMessage(`Not a valid effect: ${toStringUnknown(cur)}`);
        } else if (isInterruptedException(e)) {
          cur = exitFailCause(sequential(die(e), interrupt(none3)));
        } else {
          cur = die2(e);
        }
      }
    }
  }
  run = () => {
    this.drainQueueOnCurrentThread();
  };
};
var currentMinimumLogLevel = globalValue("effect/FiberRef/currentMinimumLogLevel", () => fiberRefUnsafeMake(fromLiteral("Info")));
var loggerWithConsoleLog = (self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  get2(services, consoleTag).unsafe.log(self.log(opts));
});
var loggerWithLeveledLog = (self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  const unsafeLogger = get2(services, consoleTag).unsafe;
  switch (opts.logLevel._tag) {
    case "Debug":
      return unsafeLogger.debug(self.log(opts));
    case "Info":
      return unsafeLogger.info(self.log(opts));
    case "Trace":
      return unsafeLogger.trace(self.log(opts));
    case "Warning":
      return unsafeLogger.warn(self.log(opts));
    case "Error":
    case "Fatal":
      return unsafeLogger.error(self.log(opts));
    default:
      return unsafeLogger.log(self.log(opts));
  }
});
var loggerWithConsoleError = (self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  get2(services, consoleTag).unsafe.error(self.log(opts));
});
var defaultLogger = globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/defaultLogger"), () => loggerWithConsoleLog(stringLogger));
var jsonLogger2 = globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/jsonLogger"), () => loggerWithConsoleLog(jsonLogger));
var logFmtLogger = globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/logFmtLogger"), () => loggerWithConsoleLog(logfmtLogger));
var prettyLogger2 = globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/prettyLogger"), () => prettyLoggerDefault);
var structuredLogger2 = globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/structuredLogger"), () => loggerWithConsoleLog(structuredLogger));
var tracerLogger = globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/tracerLogger"), () => makeLogger(({
  annotations,
  cause: cause2,
  context: context3,
  fiberId: fiberId2,
  logLevel,
  message
}) => {
  const span2 = getOption(getOrDefault(context3, currentContext), spanTag);
  if (span2._tag === "None" || span2.value._tag === "ExternalSpan") {
    return;
  }
  const clockService = unsafeGet2(getOrDefault(context3, currentServices), clockTag);
  const attributes = {};
  for (const [key, value2] of annotations) {
    attributes[key] = value2;
  }
  attributes["effect.fiberId"] = threadName2(fiberId2);
  attributes["effect.logLevel"] = logLevel.label;
  if (cause2 !== null && cause2._tag !== "Empty") {
    attributes["effect.cause"] = pretty(cause2, {
      renderErrorCause: true
    });
  }
  span2.value.event(toStringUnknown(Array.isArray(message) && message.length === 1 ? message[0] : message), clockService.unsafeCurrentTimeNanos(), attributes);
}));
var loggerWithSpanAnnotations = (self) => mapInputOptions(self, (options) => {
  const span2 = flatMap(get3(options.context, currentContext), getOption(spanTag));
  if (span2._tag === "None") {
    return options;
  }
  return {
    ...options,
    annotations: pipe(options.annotations, set2("effect.traceId", span2.value.traceId), set2("effect.spanId", span2.value.spanId), span2.value._tag === "Span" ? set2("effect.spanName", span2.value.name) : identity)
  };
});
var currentLoggers = globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentLoggers"), () => fiberRefUnsafeMakeHashSet(make5(defaultLogger, tracerLogger)));
var batchedLogger = dual(3, (self, window, f) => flatMap3(scope2, (scope3) => {
  let buffer = [];
  const flush = suspend(() => {
    if (buffer.length === 0) {
      return void_;
    }
    const arr = buffer;
    buffer = [];
    return f(arr);
  });
  return uninterruptibleMask((restore) => pipe(sleep4(window), zipRight(flush), forever2, restore, forkDaemon2, flatMap3((fiber) => scopeAddFinalizer(scope3, interruptFiber(fiber))), zipRight(addFinalizer2(() => flush)), as(makeLogger((options) => {
    buffer.push(self.log(options));
  }))));
}));
var annotateLogsScoped = function() {
  if (typeof arguments[0] === "string") {
    return fiberRefLocallyScopedWith(currentLogAnnotations, set2(arguments[0], arguments[1]));
  }
  const entries = Object.entries(arguments[0]);
  return fiberRefLocallyScopedWith(currentLogAnnotations, mutate((annotations) => {
    for (let i = 0; i < entries.length; i++) {
      const [key, value2] = entries[i];
      set2(annotations, key, value2);
    }
    return annotations;
  }));
};
var whenLogLevel = dual(2, (effect, level) => {
  const requiredLogLevel = typeof level === "string" ? fromLiteral(level) : level;
  return withFiberRuntime((fiberState) => {
    const minimumLogLevel = fiberState.getFiberRef(currentMinimumLogLevel);
    if (greaterThan3(minimumLogLevel, requiredLogLevel)) {
      return succeed(none2());
    }
    return map3(effect, some2);
  });
});
var acquireRelease2 = dual((args2) => isEffect(args2[0]), (acquire, release) => uninterruptible(tap(acquire, (a) => addFinalizer2((exit3) => release(a, exit3)))));
var acquireReleaseInterruptible = dual((args2) => isEffect(args2[0]), (acquire, release) => ensuring2(acquire, addFinalizer2((exit3) => release(exit3))));
var addFinalizer2 = (finalizer) => withFiberRuntime((runtime) => {
  const acquireRefs = runtime.getFiberRefs();
  const acquireFlags = disable(runtime.currentRuntimeFlags, Interruption);
  return flatMap3(scope2, (scope3) => scopeAddFinalizerExit(scope3, (exit3) => withFiberRuntime((runtimeFinalizer) => {
    const preRefs = runtimeFinalizer.getFiberRefs();
    const preFlags = runtimeFinalizer.currentRuntimeFlags;
    const patchRefs = diff3(preRefs, acquireRefs);
    const patchFlags = diff(preFlags, acquireFlags);
    const inverseRefs = diff3(acquireRefs, preRefs);
    runtimeFinalizer.setFiberRefs(patch3(patchRefs, runtimeFinalizer.id(), acquireRefs));
    return ensuring2(withRuntimeFlags(finalizer(exit3), patchFlags), sync(() => {
      runtimeFinalizer.setFiberRefs(patch3(inverseRefs, runtimeFinalizer.id(), runtimeFinalizer.getFiberRefs()));
    }));
  })));
});
var daemonChildren = (self) => {
  const forkScope = fiberRefLocally(currentForkScopeOverride, some2(globalScope));
  return forkScope(self);
};
var _existsParFound = /* @__PURE__ */ Symbol.for("effect/Effect/existsPar/found");
var exists = dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, predicate, options) => matchSimple(options?.concurrency, () => suspend(() => existsLoop(elements[Symbol.iterator](), 0, predicate)), () => matchEffect(forEach7(elements, (a, i) => if_(predicate(a, i), {
  onTrue: () => fail(_existsParFound),
  onFalse: () => void_
}), options), {
  onFailure: (e) => e === _existsParFound ? succeed(true) : fail(e),
  onSuccess: () => succeed(false)
})));
var existsLoop = (iterator, index, f) => {
  const next2 = iterator.next();
  if (next2.done) {
    return succeed(false);
  }
  return flatMap3(f(next2.value, index), (b) => b ? succeed(b) : existsLoop(iterator, index + 1, f));
};
var filter3 = dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, predicate, options) => {
  const predicate_ = options?.negate ? (a, i) => map3(predicate(a, i), not) : predicate;
  return matchSimple(options?.concurrency, () => suspend(() => fromIterable(elements).reduceRight((effect, a, i) => zipWith2(effect, suspend(() => predicate_(a, i)), (list, b) => b ? [a, ...list] : list), sync(() => new Array()))), () => map3(forEach7(elements, (a, i) => map3(predicate_(a, i), (b) => b ? some2(a) : none2()), options), getSomes));
});
var allResolveInput = (input) => {
  if (Array.isArray(input) || isIterable(input)) {
    return [input, none2()];
  }
  const keys4 = Object.keys(input);
  const size7 = keys4.length;
  return [keys4.map((k) => input[k]), some2((values5) => {
    const res = {};
    for (let i = 0; i < size7; i++) {
      ;
      res[keys4[i]] = values5[i];
    }
    return res;
  })];
};
var allValidate = (effects, reconcile, options) => {
  const eitherEffects = [];
  for (const effect of effects) {
    eitherEffects.push(either(effect));
  }
  return flatMap3(forEach7(eitherEffects, identity, {
    concurrency: options?.concurrency,
    batching: options?.batching,
    concurrentFinalizers: options?.concurrentFinalizers
  }), (eithers) => {
    const none8 = none2();
    const size7 = eithers.length;
    const errors = new Array(size7);
    const successes = new Array(size7);
    let errored = false;
    for (let i = 0; i < size7; i++) {
      const either3 = eithers[i];
      if (either3._tag === "Left") {
        errors[i] = some2(either3.left);
        errored = true;
      } else {
        successes[i] = either3.right;
        errors[i] = none8;
      }
    }
    if (errored) {
      return reconcile._tag === "Some" ? fail(reconcile.value(errors)) : fail(errors);
    } else if (options?.discard) {
      return void_;
    }
    return reconcile._tag === "Some" ? succeed(reconcile.value(successes)) : succeed(successes);
  });
};
var allEither = (effects, reconcile, options) => {
  const eitherEffects = [];
  for (const effect of effects) {
    eitherEffects.push(either(effect));
  }
  if (options?.discard) {
    return forEach7(eitherEffects, identity, {
      concurrency: options?.concurrency,
      batching: options?.batching,
      discard: true,
      concurrentFinalizers: options?.concurrentFinalizers
    });
  }
  return map3(forEach7(eitherEffects, identity, {
    concurrency: options?.concurrency,
    batching: options?.batching,
    concurrentFinalizers: options?.concurrentFinalizers
  }), (eithers) => reconcile._tag === "Some" ? reconcile.value(eithers) : eithers);
};
var all3 = (arg, options) => {
  const [effects, reconcile] = allResolveInput(arg);
  if (options?.mode === "validate") {
    return allValidate(effects, reconcile, options);
  } else if (options?.mode === "either") {
    return allEither(effects, reconcile, options);
  }
  return options?.discard !== true && reconcile._tag === "Some" ? map3(forEach7(effects, identity, options), reconcile.value) : forEach7(effects, identity, options);
};
var allWith = (options) => (arg) => all3(arg, options);
var allSuccesses = (elements, options) => map3(all3(fromIterable(elements).map(exit), options), filterMap((exit3) => exitIsSuccess(exit3) ? some2(exit3.effect_instruction_i0) : none2()));
var replicate2 = dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateEffect2 = dual((args2) => isEffect(args2[0]), (self, n, options) => all3(replicate2(self, n), options));
var forEach7 = dual((args2) => isIterable(args2[0]), (self, f, options) => withFiberRuntime((r) => {
  const isRequestBatchingEnabled = options?.batching === true || options?.batching === "inherit" && r.getFiberRef(currentRequestBatching);
  if (options?.discard) {
    return match10(options.concurrency, () => finalizersMaskInternal(sequential3, options?.concurrentFinalizers)((restore) => isRequestBatchingEnabled ? forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), true, false, 1) : forEachSequentialDiscard(self, (a, i) => restore(f(a, i)))), () => finalizersMaskInternal(parallel3, options?.concurrentFinalizers)((restore) => forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled, false)), (n) => finalizersMaskInternal(parallelN2(n), options?.concurrentFinalizers)((restore) => forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled, false, n)));
  }
  return match10(options?.concurrency, () => finalizersMaskInternal(sequential3, options?.concurrentFinalizers)((restore) => isRequestBatchingEnabled ? forEachParN(self, 1, (a, i) => restore(f(a, i)), true) : forEachSequential(self, (a, i) => restore(f(a, i)))), () => finalizersMaskInternal(parallel3, options?.concurrentFinalizers)((restore) => forEachParUnbounded(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled)), (n) => finalizersMaskInternal(parallelN2(n), options?.concurrentFinalizers)((restore) => forEachParN(self, n, (a, i) => restore(f(a, i)), isRequestBatchingEnabled)));
}));
var forEachParUnbounded = (self, f, batching) => suspend(() => {
  const as3 = fromIterable(self);
  const array2 = new Array(as3.length);
  const fn = (a, i) => flatMap3(f(a, i), (b) => sync(() => array2[i] = b));
  return zipRight(forEachConcurrentDiscard(as3, fn, batching, false), succeed(array2));
});
var forEachConcurrentDiscard = (self, f, batching, processAll, n) => uninterruptibleMask((restore) => transplant((graft) => withFiberRuntime((parent) => {
  let todos = Array.from(self).reverse();
  let target = todos.length;
  if (target === 0) {
    return void_;
  }
  let counter6 = 0;
  let interrupted2 = false;
  const fibersCount = n ? Math.min(todos.length, n) : todos.length;
  const fibers = /* @__PURE__ */ new Set();
  const results = new Array();
  const interruptAll2 = () => fibers.forEach((fiber) => {
    fiber.currentScheduler.scheduleTask(() => {
      fiber.unsafeInterruptAsFork(parent.id());
    }, 0);
  });
  const startOrder = new Array();
  const joinOrder = new Array();
  const residual = new Array();
  const collectExits = () => {
    const exits = results.filter(({
      exit: exit3
    }) => exit3._tag === "Failure").sort((a, b) => a.index < b.index ? -1 : a.index === b.index ? 0 : 1).map(({
      exit: exit3
    }) => exit3);
    if (exits.length === 0) {
      exits.push(exitVoid);
    }
    return exits;
  };
  const runFiber = (eff, interruptImmediately = false) => {
    const runnable = uninterruptible(graft(eff));
    const fiber = unsafeForkUnstarted(runnable, parent, parent.currentRuntimeFlags, globalScope);
    parent.currentScheduler.scheduleTask(() => {
      if (interruptImmediately) {
        fiber.unsafeInterruptAsFork(parent.id());
      }
      fiber.resume(runnable);
    }, 0);
    return fiber;
  };
  const onInterruptSignal = () => {
    if (!processAll) {
      target -= todos.length;
      todos = [];
    }
    interrupted2 = true;
    interruptAll2();
  };
  const stepOrExit = batching ? step : exit;
  const processingFiber = runFiber(async_((resume2) => {
    const pushResult = (res, index) => {
      if (res._op === "Blocked") {
        residual.push(res);
      } else {
        results.push({
          index,
          exit: res
        });
        if (res._op === "Failure" && !interrupted2) {
          onInterruptSignal();
        }
      }
    };
    const next2 = () => {
      if (todos.length > 0) {
        const a = todos.pop();
        let index = counter6++;
        const returnNextElement = () => {
          const a2 = todos.pop();
          index = counter6++;
          return flatMap3(yieldNow(), () => flatMap3(stepOrExit(restore(f(a2, index))), onRes));
        };
        const onRes = (res) => {
          if (todos.length > 0) {
            pushResult(res, index);
            if (todos.length > 0) {
              return returnNextElement();
            }
          }
          return succeed(res);
        };
        const todo = flatMap3(stepOrExit(restore(f(a, index))), onRes);
        const fiber = runFiber(todo);
        startOrder.push(fiber);
        fibers.add(fiber);
        if (interrupted2) {
          fiber.currentScheduler.scheduleTask(() => {
            fiber.unsafeInterruptAsFork(parent.id());
          }, 0);
        }
        fiber.addObserver((wrapped) => {
          let exit3;
          if (wrapped._op === "Failure") {
            exit3 = wrapped;
          } else {
            exit3 = wrapped.effect_instruction_i0;
          }
          joinOrder.push(fiber);
          fibers.delete(fiber);
          pushResult(exit3, index);
          if (results.length === target) {
            resume2(succeed(getOrElse(exitCollectAll(collectExits(), {
              parallel: true
            }), () => exitVoid)));
          } else if (residual.length + results.length === target) {
            const exits = collectExits();
            const requests = residual.map((blocked2) => blocked2.effect_instruction_i0).reduce(par);
            resume2(succeed(blocked(requests, forEachConcurrentDiscard([getOrElse(exitCollectAll(exits, {
              parallel: true
            }), () => exitVoid), ...residual.map((blocked2) => blocked2.effect_instruction_i1)], (i) => i, batching, true, n))));
          } else {
            next2();
          }
        });
      }
    };
    for (let i = 0; i < fibersCount; i++) {
      next2();
    }
  }));
  return asVoid(onExit(flatten4(restore(join2(processingFiber))), exitMatch({
    onFailure: (cause2) => {
      onInterruptSignal();
      const target2 = residual.length + 1;
      const concurrency = Math.min(typeof n === "number" ? n : residual.length, residual.length);
      const toPop = Array.from(residual);
      return async_((cb) => {
        const exits = [];
        let count = 0;
        let index = 0;
        const check = (index2, hitNext) => (exit3) => {
          exits[index2] = exit3;
          count++;
          if (count === target2) {
            cb(exitSucceed(exitFailCause(cause2)));
          }
          if (toPop.length > 0 && hitNext) {
            next2();
          }
        };
        const next2 = () => {
          runFiber(toPop.pop(), true).addObserver(check(index, true));
          index++;
        };
        processingFiber.addObserver(check(index, false));
        index++;
        for (let i = 0; i < concurrency; i++) {
          next2();
        }
      });
    },
    onSuccess: () => forEachSequential(joinOrder, (f2) => f2.inheritAll)
  })));
})));
var forEachParN = (self, n, f, batching) => suspend(() => {
  const as3 = fromIterable(self);
  const array2 = new Array(as3.length);
  const fn = (a, i) => map3(f(a, i), (b) => array2[i] = b);
  return zipRight(forEachConcurrentDiscard(as3, fn, batching, false, n), succeed(array2));
});
var fork2 = (self) => withFiberRuntime((state, status2) => succeed(unsafeFork2(self, state, status2.runtimeFlags)));
var forkDaemon2 = (self) => forkWithScopeOverride(self, globalScope);
var forkWithErrorHandler = dual(2, (self, handler) => fork2(onError(self, (cause2) => {
  const either3 = failureOrCause(cause2);
  switch (either3._tag) {
    case "Left":
      return handler(either3.left);
    case "Right":
      return failCause(either3.right);
  }
})));
var unsafeFork2 = (effect, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childFiber = unsafeMakeChildFiber(effect, parentFiber, parentRuntimeFlags, overrideScope);
  childFiber.resume(effect);
  return childFiber;
};
var unsafeForkUnstarted = (effect, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childFiber = unsafeMakeChildFiber(effect, parentFiber, parentRuntimeFlags, overrideScope);
  return childFiber;
};
var unsafeMakeChildFiber = (effect, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childId = unsafeMake();
  const parentFiberRefs = parentFiber.getFiberRefs();
  const childFiberRefs = forkAs(parentFiberRefs, childId);
  const childFiber = new FiberRuntime(childId, childFiberRefs, parentRuntimeFlags);
  const childContext = getOrDefault(childFiberRefs, currentContext);
  const supervisor = childFiber.currentSupervisor;
  supervisor.onStart(childContext, effect, some2(parentFiber), childFiber);
  childFiber.addObserver((exit3) => supervisor.onEnd(exit3, childFiber));
  const parentScope = overrideScope !== null ? overrideScope : pipe(parentFiber.getFiberRef(currentForkScopeOverride), getOrElse(() => parentFiber.scope()));
  parentScope.add(parentRuntimeFlags, childFiber);
  return childFiber;
};
var forkWithScopeOverride = (self, scopeOverride) => withFiberRuntime((parentFiber, parentStatus) => succeed(unsafeFork2(self, parentFiber, parentStatus.runtimeFlags, scopeOverride)));
var mergeAll = dual((args2) => isFunction(args2[2]), (elements, zero2, f, options) => matchSimple(options?.concurrency, () => fromIterable(elements).reduce((acc, a, i) => zipWith2(acc, a, (acc2, a2) => f(acc2, a2, i)), succeed(zero2)), () => flatMap3(make9(zero2), (acc) => flatMap3(forEach7(elements, (effect, i) => flatMap3(effect, (a) => update3(acc, (b) => f(b, a, i))), options), () => get6(acc)))));
var partition2 = dual((args2) => isIterable(args2[0]), (elements, f, options) => pipe(forEach7(elements, (a, i) => either(f(a, i)), options), map3((chunk3) => partitionMap(chunk3, identity))));
var validateAll = dual((args2) => isIterable(args2[0]), (elements, f, options) => flatMap3(partition2(elements, f, {
  concurrency: options?.concurrency,
  batching: options?.batching,
  concurrentFinalizers: options?.concurrentFinalizers
}), ([es, bs]) => isNonEmptyArray(es) ? fail(es) : options?.discard ? void_ : succeed(bs)));
var raceAll2 = (all4) => withFiberRuntime((state, status2) => async_((resume2) => {
  const fibers = /* @__PURE__ */ new Set();
  let winner;
  let failures2 = empty8;
  const interruptAll2 = () => {
    for (const fiber of fibers) {
      fiber.unsafeInterruptAsFork(state.id());
    }
  };
  let latch = false;
  let empty20 = true;
  for (const self of all4) {
    empty20 = false;
    const fiber = unsafeFork2(interruptible2(self), state, status2.runtimeFlags);
    fibers.add(fiber);
    fiber.addObserver((exit3) => {
      fibers.delete(fiber);
      if (!winner) {
        if (exit3._tag === "Success") {
          latch = true;
          winner = fiber;
          failures2 = empty8;
          interruptAll2();
        } else {
          failures2 = parallel(exit3.cause, failures2);
        }
      }
      if (latch && fibers.size === 0) {
        resume2(winner ? zipRight(inheritAll(winner), winner.unsafePoll()) : failCause(failures2));
      }
    });
    if (winner) break;
  }
  if (empty20) {
    return resume2(dieSync(() => new IllegalArgumentException(`Received an empty collection of effects`)));
  }
  latch = true;
  return interruptAllAs(fibers, state.id());
}));
var reduceEffect = dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, zero2, f, options) => matchSimple(options?.concurrency, () => fromIterable(elements).reduce((acc, a, i) => zipWith2(acc, a, (acc2, a2) => f(acc2, a2, i)), zero2), () => suspend(() => pipe(mergeAll([zero2, ...elements], none2(), (acc, elem, i) => {
  switch (acc._tag) {
    case "None": {
      return some2(elem);
    }
    case "Some": {
      return some2(f(acc.value, elem, i));
    }
  }
}, options), map3((option3) => {
  switch (option3._tag) {
    case "None": {
      throw new Error("BUG: Effect.reduceEffect - please report an issue at https://github.com/Effect-TS/effect/issues");
    }
    case "Some": {
      return option3.value;
    }
  }
})))));
var parallelFinalizers = (self) => contextWithEffect((context3) => match(getOption(context3, scopeTag), {
  onNone: () => self,
  onSome: (scope3) => {
    switch (scope3.strategy._tag) {
      case "Parallel":
        return self;
      case "Sequential":
      case "ParallelN":
        return flatMap3(scopeFork(scope3, parallel3), (inner) => scopeExtend(self, inner));
    }
  }
}));
var parallelNFinalizers = (parallelism) => (self) => contextWithEffect((context3) => match(getOption(context3, scopeTag), {
  onNone: () => self,
  onSome: (scope3) => {
    if (scope3.strategy._tag === "ParallelN" && scope3.strategy.parallelism === parallelism) {
      return self;
    }
    return flatMap3(scopeFork(scope3, parallelN2(parallelism)), (inner) => scopeExtend(self, inner));
  }
}));
var finalizersMask = (strategy) => (self) => finalizersMaskInternal(strategy, true)(self);
var finalizersMaskInternal = (strategy, concurrentFinalizers) => (self) => contextWithEffect((context3) => match(getOption(context3, scopeTag), {
  onNone: () => self(identity),
  onSome: (scope3) => {
    if (concurrentFinalizers === true) {
      const patch7 = strategy._tag === "Parallel" ? parallelFinalizers : strategy._tag === "Sequential" ? sequentialFinalizers : parallelNFinalizers(strategy.parallelism);
      switch (scope3.strategy._tag) {
        case "Parallel":
          return patch7(self(parallelFinalizers));
        case "Sequential":
          return patch7(self(sequentialFinalizers));
        case "ParallelN":
          return patch7(self(parallelNFinalizers(scope3.strategy.parallelism)));
      }
    } else {
      return self(identity);
    }
  }
}));
var scopeWith = (f) => flatMap3(scopeTag, f);
var scopedWith = (f) => flatMap3(scopeMake2(), (scope3) => onExit(f(scope3), (exit3) => scope3.close(exit3)));
var scopedEffect = (effect) => flatMap3(scopeMake2(), (scope3) => scopeUse(effect, scope3));
var sequentialFinalizers = (self) => contextWithEffect((context3) => match(getOption(context3, scopeTag), {
  onNone: () => self,
  onSome: (scope3) => {
    switch (scope3.strategy._tag) {
      case "Sequential":
        return self;
      case "Parallel":
      case "ParallelN":
        return flatMap3(scopeFork(scope3, sequential3), (inner) => scopeExtend(self, inner));
    }
  }
}));
var tagMetricsScoped = (key, value2) => labelMetricsScoped([make19(key, value2)]);
var labelMetricsScoped = (labels) => fiberRefLocallyScopedWith(currentMetricLabels, (old) => union(old, labels));
var using = dual(2, (self, use) => scopedWith((scope3) => flatMap3(scopeExtend(self, scope3), use)));
var validate = dual((args2) => isEffect(args2[1]), (self, that, options) => validateWith(self, that, (a, b) => [a, b], options));
var validateWith = dual((args2) => isEffect(args2[1]), (self, that, f, options) => flatten4(zipWithOptions(exit(self), exit(that), (ea, eb) => exitZipWith(ea, eb, {
  onSuccess: f,
  onFailure: (ca, cb) => options?.concurrent ? parallel(ca, cb) : sequential(ca, cb)
}), options)));
var validateAllPar = dual(2, (elements, f) => flatMap3(partition2(elements, f), ([es, bs]) => es.length === 0 ? succeed(bs) : fail(es)));
var validateAllParDiscard = dual(2, (elements, f) => flatMap3(partition2(elements, f), ([es, _]) => es.length === 0 ? void_ : fail(es)));
var validateFirst = dual((args2) => isIterable(args2[0]), (elements, f, options) => flip(forEach7(elements, (a, i) => flip(f(a, i)), options)));
var withClockScoped = (c) => fiberRefLocallyScopedWith(currentServices, add(clockTag, c));
var withRandomScoped = (value2) => fiberRefLocallyScopedWith(currentServices, add(randomTag, value2));
var withConfigProviderScoped = (provider) => fiberRefLocallyScopedWith(currentServices, add(configProviderTag, provider));
var withEarlyRelease = (self) => scopeWith((parent) => flatMap3(scopeFork(parent, sequential2), (child) => pipe(self, scopeExtend(child), map3((value2) => [fiberIdWith((fiberId2) => scopeClose(child, exitInterrupt(fiberId2))), value2]))));
var zipOptions = dual((args2) => isEffect(args2[1]), (self, that, options) => zipWithOptions(self, that, (a, b) => [a, b], options));
var zipLeftOptions = dual((args2) => isEffect(args2[1]), (self, that, options) => {
  if (options?.concurrent !== true && (options?.batching === void 0 || options.batching === false)) {
    return zipLeft(self, that);
  }
  return zipWithOptions(self, that, (a, _) => a, options);
});
var zipRightOptions = dual((args2) => isEffect(args2[1]), (self, that, options) => {
  if (options?.concurrent !== true && (options?.batching === void 0 || options.batching === false)) {
    return zipRight(self, that);
  }
  return zipWithOptions(self, that, (_, b) => b, options);
});
var zipWithOptions = dual((args2) => isEffect(args2[1]), (self, that, f, options) => map3(all3([self, that], {
  concurrency: options?.concurrent ? 2 : 1,
  batching: options?.batching,
  concurrentFinalizers: options?.concurrentFinalizers
}), ([a, a2]) => f(a, a2)));
var withRuntimeFlagsScoped = (update6) => {
  if (update6 === empty7) {
    return void_;
  }
  return pipe(runtimeFlags, flatMap3((runtimeFlags2) => {
    const updatedRuntimeFlags = patch(runtimeFlags2, update6);
    const revertRuntimeFlags = diff(updatedRuntimeFlags, runtimeFlags2);
    return pipe(updateRuntimeFlags(update6), zipRight(addFinalizer2(() => updateRuntimeFlags(revertRuntimeFlags))), asVoid);
  }), uninterruptible);
};
var scopeTag = GenericTag("effect/Scope");
var scope2 = scopeTag;
var scopeUnsafeAddFinalizer = (scope3, fin) => {
  if (scope3.state._tag === "Open") {
    scope3.state.finalizers.set({}, fin);
  }
};
var ScopeImplProto = {
  [ScopeTypeId]: ScopeTypeId,
  [CloseableScopeTypeId]: CloseableScopeTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  fork(strategy) {
    return sync(() => {
      const newScope = scopeUnsafeMake2(strategy);
      if (this.state._tag === "Closed") {
        newScope.state = this.state;
        return newScope;
      }
      const key = {};
      const fin = (exit3) => newScope.close(exit3);
      this.state.finalizers.set(key, fin);
      scopeUnsafeAddFinalizer(newScope, (_) => sync(() => {
        if (this.state._tag === "Open") {
          this.state.finalizers.delete(key);
        }
      }));
      return newScope;
    });
  },
  close(exit3) {
    return suspend(() => {
      if (this.state._tag === "Closed") {
        return void_;
      }
      const finalizers = Array.from(this.state.finalizers.values()).reverse();
      this.state = {
        _tag: "Closed",
        exit: exit3
      };
      if (finalizers.length === 0) {
        return void_;
      }
      return isSequential(this.strategy) ? pipe(forEachSequential(finalizers, (fin) => exit(fin(exit3))), flatMap3((results) => pipe(exitCollectAll(results), map(exitAsVoid), getOrElse(() => exitVoid)))) : isParallel(this.strategy) ? pipe(forEachParUnbounded(finalizers, (fin) => exit(fin(exit3)), false), flatMap3((results) => pipe(exitCollectAll(results, {
        parallel: true
      }), map(exitAsVoid), getOrElse(() => exitVoid)))) : pipe(forEachParN(finalizers, this.strategy.parallelism, (fin) => exit(fin(exit3)), false), flatMap3((results) => pipe(exitCollectAll(results, {
        parallel: true
      }), map(exitAsVoid), getOrElse(() => exitVoid))));
    });
  },
  addFinalizer(fin) {
    return suspend(() => {
      if (this.state._tag === "Closed") {
        return fin(this.state.exit);
      }
      this.state.finalizers.set({}, fin);
      return void_;
    });
  }
};
var scopeUnsafeMake2 = (strategy = sequential2) => {
  const scope3 = Object.create(ScopeImplProto);
  scope3.strategy = strategy;
  scope3.state = {
    _tag: "Open",
    finalizers: /* @__PURE__ */ new Map()
  };
  return scope3;
};
var scopeMake2 = (strategy = sequential2) => sync(() => scopeUnsafeMake2(strategy));
var scopeExtend = dual(2, (effect, scope3) => mapInputContext(
  effect,
  // @ts-expect-error
  merge2(make3(scopeTag, scope3))
));
var scopeUse = dual(2, (effect, scope3) => pipe(effect, scopeExtend(scope3), onExit((exit3) => scope3.close(exit3))));
var fiberRefUnsafeMakeSupervisor = (initial) => fiberRefUnsafeMakePatch(initial, {
  differ,
  fork: empty19
});
var fiberRefLocallyScoped = dual(2, (self, value2) => asVoid(acquireRelease2(flatMap3(fiberRefGet(self), (oldValue) => as(fiberRefSet(self, value2), oldValue)), (oldValue) => fiberRefSet(self, oldValue))));
var fiberRefLocallyScopedWith = dual(2, (self, f) => fiberRefGetWith(self, (a) => fiberRefLocallyScoped(self, f(a))));
var fiberRefMake = (initial, options) => fiberRefMakeWith(() => fiberRefUnsafeMake(initial, options));
var fiberRefMakeWith = (ref) => acquireRelease2(tap(sync(ref), (ref2) => fiberRefUpdate(ref2, identity)), (fiberRef) => fiberRefDelete(fiberRef));
var fiberRefMakeContext = (initial) => fiberRefMakeWith(() => fiberRefUnsafeMakeContext(initial));
var fiberRefMakeRuntimeFlags = (initial) => fiberRefMakeWith(() => fiberRefUnsafeMakeRuntimeFlags(initial));
var currentRuntimeFlags = fiberRefUnsafeMakeRuntimeFlags(none4);
var currentSupervisor = fiberRefUnsafeMakeSupervisor(none7);
var fiberAwaitAll = (fibers) => forEach7(fibers, _await);
var fiberAll = (fibers) => {
  const _fiberAll = {
    ...CommitPrototype2,
    commit() {
      return join2(this);
    },
    [FiberTypeId]: fiberVariance2,
    id: () => fromIterable(fibers).reduce((id2, fiber) => combine2(id2, fiber.id()), none3),
    await: exit(forEachParUnbounded(fibers, (fiber) => flatten4(fiber.await), false)),
    children: map3(forEachParUnbounded(fibers, (fiber) => fiber.children, false), flatten),
    inheritAll: forEachSequentialDiscard(fibers, (fiber) => fiber.inheritAll),
    poll: map3(forEachSequential(fibers, (fiber) => fiber.poll), reduceRight(some2(exitSucceed(new Array())), (optionB, optionA) => {
      switch (optionA._tag) {
        case "None": {
          return none2();
        }
        case "Some": {
          switch (optionB._tag) {
            case "None": {
              return none2();
            }
            case "Some": {
              return some2(exitZipWith(optionA.value, optionB.value, {
                onSuccess: (a, chunk3) => [a, ...chunk3],
                onFailure: parallel
              }));
            }
          }
        }
      }
    })),
    interruptAsFork: (fiberId2) => forEachSequentialDiscard(fibers, (fiber) => fiber.interruptAsFork(fiberId2))
  };
  return _fiberAll;
};
var fiberInterruptFork = (self) => asVoid(forkDaemon2(interruptFiber(self)));
var fiberJoinAll = (fibers) => join2(fiberAll(fibers));
var fiberScoped = (self) => acquireRelease2(succeed(self), interruptFiber);
var raceWith = dual(3, (self, other, options) => raceFibersWith(self, other, {
  onSelfWin: (winner, loser) => flatMap3(winner.await, (exit3) => {
    switch (exit3._tag) {
      case OP_SUCCESS: {
        return flatMap3(winner.inheritAll, () => options.onSelfDone(exit3, loser));
      }
      case OP_FAILURE: {
        return options.onSelfDone(exit3, loser);
      }
    }
  }),
  onOtherWin: (winner, loser) => flatMap3(winner.await, (exit3) => {
    switch (exit3._tag) {
      case OP_SUCCESS: {
        return flatMap3(winner.inheritAll, () => options.onOtherDone(exit3, loser));
      }
      case OP_FAILURE: {
        return options.onOtherDone(exit3, loser);
      }
    }
  })
}));
var disconnect = (self) => uninterruptibleMask((restore) => fiberIdWith((fiberId2) => flatMap3(forkDaemon2(restore(self)), (fiber) => pipe(restore(join2(fiber)), onInterrupt(() => pipe(fiber, interruptAsFork(fiberId2)))))));
var race2 = dual(2, (self, that) => fiberIdWith((parentFiberId) => raceWith(self, that, {
  onSelfDone: (exit3, right2) => exitMatchEffect(exit3, {
    onFailure: (cause2) => pipe(join2(right2), mapErrorCause2((cause22) => parallel(cause2, cause22))),
    onSuccess: (value2) => pipe(right2, interruptAsFiber(parentFiberId), as(value2))
  }),
  onOtherDone: (exit3, left2) => exitMatchEffect(exit3, {
    onFailure: (cause2) => pipe(join2(left2), mapErrorCause2((cause22) => parallel(cause22, cause2))),
    onSuccess: (value2) => pipe(left2, interruptAsFiber(parentFiberId), as(value2))
  })
})));
var raceFibersWith = dual(3, (self, other, options) => withFiberRuntime((parentFiber, parentStatus) => {
  const parentRuntimeFlags = parentStatus.runtimeFlags;
  const raceIndicator = make2(true);
  const leftFiber = unsafeMakeChildFiber(self, parentFiber, parentRuntimeFlags, options.selfScope);
  const rightFiber = unsafeMakeChildFiber(other, parentFiber, parentRuntimeFlags, options.otherScope);
  return async_((cb) => {
    leftFiber.addObserver(() => completeRace(leftFiber, rightFiber, options.onSelfWin, raceIndicator, cb));
    rightFiber.addObserver(() => completeRace(rightFiber, leftFiber, options.onOtherWin, raceIndicator, cb));
    leftFiber.startFork(self);
    rightFiber.startFork(other);
  }, combine2(leftFiber.id(), rightFiber.id()));
}));
var completeRace = (winner, loser, cont, ab, cb) => {
  if (compareAndSet(true, false)(ab)) {
    cb(cont(winner, loser));
  }
};
var ensuring2 = dual(2, (self, finalizer) => uninterruptibleMask((restore) => matchCauseEffect(restore(self), {
  onFailure: (cause1) => matchCauseEffect(finalizer, {
    onFailure: (cause2) => failCause(sequential(cause1, cause2)),
    onSuccess: () => failCause(cause1)
  }),
  onSuccess: (a) => as(finalizer, a)
})));
var invokeWithInterrupt = (self, entries, onInterrupt3) => fiberIdWith((id2) => flatMap3(flatMap3(forkDaemon2(interruptible2(self)), (processing) => async_((cb) => {
  const counts = entries.map((_) => _.listeners.count);
  const checkDone = () => {
    if (counts.every((count) => count === 0)) {
      if (entries.every((_) => {
        if (_.result.state.current._tag === "Pending") {
          return true;
        } else if (_.result.state.current._tag === "Done" && exitIsExit(_.result.state.current.effect) && _.result.state.current.effect._tag === "Failure" && isInterrupted(_.result.state.current.effect.cause)) {
          return true;
        } else {
          return false;
        }
      })) {
        cleanup.forEach((f) => f());
        onInterrupt3?.();
        cb(interruptFiber(processing));
      }
    }
  };
  processing.addObserver((exit3) => {
    cleanup.forEach((f) => f());
    cb(exit3);
  });
  const cleanup = entries.map((r, i) => {
    const observer = (count) => {
      counts[i] = count;
      checkDone();
    };
    r.listeners.addObserver(observer);
    return () => r.listeners.removeObserver(observer);
  });
  checkDone();
  return sync(() => {
    cleanup.forEach((f) => f());
  });
})), () => suspend(() => {
  const residual = entries.flatMap((entry) => {
    if (!entry.state.completed) {
      return [entry];
    }
    return [];
  });
  return forEachSequentialDiscard(residual, (entry) => complete(entry.request, exitInterrupt(id2)));
})));
var interruptWhenPossible = dual(2, (self, all4) => fiberRefGetWith(currentRequestMap, (map10) => suspend(() => {
  const entries = fromIterable(all4).flatMap((_) => map10.has(_) ? [map10.get(_)] : []);
  return invokeWithInterrupt(self, entries);
})));
var makeSpanScoped = (name, options) => {
  options = addSpanStackTrace(options);
  return uninterruptible(withFiberRuntime((fiber) => {
    const scope3 = unsafeGet2(fiber.getFiberRef(currentContext), scopeTag);
    const span2 = unsafeMakeSpan(fiber, name, options);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const clock_ = get2(fiber.getFiberRef(currentServices), clockTag);
    return as(scopeAddFinalizerExit(scope3, (exit3) => endSpan(span2, exit3, clock_, timingEnabled)), span2);
  }));
};
var withTracerScoped = (value2) => fiberRefLocallyScopedWith(currentServices, add(tracerTag, value2));
var withSpanScoped = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return flatMap3(makeSpanScoped(name, addSpanStackTrace(options)), (span2) => provideService2(self, spanTag, span2));
  }
  return (self) => flatMap3(makeSpanScoped(name, addSpanStackTrace(options)), (span2) => provideService2(self, spanTag, span2));
};

export {
  CommitPrototype2 as CommitPrototype,
  Class,
  Effectable_exports,
  not,
  Boolean_exports,
  sequential2 as sequential,
  parallel2 as parallel,
  sequential3 as sequential2,
  ExecutionStrategy_exports,
  empty9 as empty,
  delete_2 as delete_,
  updateAs2 as updateAs,
  updateManyAs2 as updateManyAs,
  empty10 as empty2,
  FiberRefs_exports,
  diff3 as diff,
  patch3 as patch,
  FiberRefsPatch_exports,
  isDone2 as isDone,
  isSuspended2 as isSuspended,
  FiberStatus_exports,
  Order2 as Order,
  greaterThanOrEqualTo3 as greaterThanOrEqualTo,
  clamp2 as clamp,
  sign,
  remainder,
  nextPow2,
  parse,
  Number_exports,
  LogLevel_exports,
  Micro_exports,
  TypeId2 as TypeId,
  Readable_exports,
  RefTypeId,
  refVariance,
  unsafeMake4 as unsafeMake,
  make8 as make,
  get5 as get,
  set3 as set,
  getAndSet,
  getAndUpdate,
  getAndUpdateSome,
  setAndGet,
  modify,
  modifySome,
  update2 as update,
  updateAndGet,
  updateSome,
  updateSomeAndGet,
  unsafeGet3 as unsafeGet,
  RefTypeId2,
  make9 as make2,
  get6 as get2,
  getAndSet2,
  getAndUpdate2,
  getAndUpdateSome2,
  modify2,
  modifySome2,
  set4 as set2,
  setAndGet2,
  update3 as update2,
  updateAndGet2,
  updateSome2,
  updateSomeAndGet2,
  Ref_exports,
  SyncScheduler,
  currentScheduler,
  withScheduler,
  Scheduler_exports,
  ClockTypeId,
  clockTag,
  escape,
  RegExp_exports,
  ConfigErrorTypeId,
  And,
  Or,
  InvalidData,
  MissingData,
  SourceUnavailable,
  Unsupported,
  isConfigError,
  isAnd,
  isOr,
  isInvalidData,
  isMissingData,
  isSourceUnavailable,
  isUnsupported,
  prefixed,
  reduceWithContext,
  isMissingDataOnly,
  empty13 as empty3,
  andThen2 as andThen,
  mapName,
  nested,
  unnested,
  OP_CONSTANT,
  OP_FAIL,
  OP_FALLBACK,
  OP_DESCRIBED,
  OP_LAZY,
  OP_MAP_OR_FAIL,
  OP_NESTED,
  OP_PRIMITIVE,
  OP_SEQUENCE,
  OP_HASHMAP,
  OP_ZIP_WITH,
  ConfigProviderTypeId,
  configProviderTag,
  FlatConfigProviderTypeId,
  make13 as make3,
  makeFlat,
  fromFlat,
  fromEnv,
  fromMap,
  mapInputPath,
  nested2,
  unnested2,
  orElse2 as orElse,
  constantCase2 as constantCase,
  kebabCase2 as kebabCase,
  lowerCase2 as lowerCase,
  snakeCase2 as snakeCase,
  upperCase2 as upperCase,
  within,
  fromJson,
  TypeId3 as TypeId2,
  consoleTag,
  RandomTypeId,
  randomTag,
  make14 as make4,
  fixed,
  spanTag,
  addSpanStackTrace,
  DisablePropagation,
  liveServices,
  currentServices,
  clockWith,
  withClock,
  withConfigProvider,
  configProviderWith,
  config,
  randomWith,
  withRandom,
  next,
  nextInt,
  nextBoolean,
  nextRange,
  nextIntBetween,
  shuffle,
  choice,
  tracerWith,
  withTracer,
  sleep3 as sleep,
  currentTimeMillis2 as currentTimeMillis,
  Clock_exports,
  LogSpan_exports,
  ParentSpan,
  Tracer_exports,
  MetricLabelTypeId,
  make19 as make5,
  isMetricLabel,
  annotateLogs,
  asSome2 as asSome,
  asSomeError,
  try_2 as try_,
  _catch,
  catchAllDefect2 as catchAllDefect,
  catchSomeCause,
  catchSomeDefect,
  catchTag2 as catchTag,
  catchTags,
  cause,
  clockWith3 as clockWith2,
  clock,
  delay2 as delay,
  descriptorWith,
  allowInterrupt,
  descriptor,
  diffFiberRefs,
  Do2 as Do,
  bind3 as bind,
  bindTo3 as bindTo,
  let_3 as let_,
  dropUntil,
  dropWhile,
  contextWith,
  eventually,
  filterMap3 as filterMap,
  filterOrDie,
  filterOrDieMessage,
  filterOrElse,
  liftPredicate,
  filterOrFail2 as filterOrFail,
  findFirst,
  firstSuccessOf,
  flipWith,
  match8 as match,
  every2 as every,
  forever2 as forever,
  fiberRefs3 as fiberRefs,
  head2 as head,
  ignore2 as ignore,
  ignoreLogged2 as ignoreLogged,
  inheritFiberRefs,
  isFailure,
  isSuccess2 as isSuccess,
  iterate,
  logWithLevel,
  log,
  logTrace,
  logDebug,
  logInfo,
  logWarning,
  logError,
  logFatal,
  withLogSpan,
  logAnnotations,
  loop,
  mapAccum,
  mapErrorCause2 as mapErrorCause,
  memoize,
  merge3 as merge,
  negate2 as negate,
  none5 as none,
  once,
  option2 as option,
  orElseFail,
  orElseSucceed2 as orElseSucceed,
  parallelErrors,
  patchFiberRefs,
  promise2 as promise,
  provideService2 as provideService,
  provideServiceEffect2 as provideServiceEffect,
  random,
  reduce4 as reduce,
  reduceRight2 as reduceRight,
  reduceWhile,
  repeatN,
  sandbox2 as sandbox,
  setFiberRefs,
  sleep4 as sleep2,
  succeedNone2 as succeedNone,
  succeedSome2 as succeedSome,
  summarized,
  tagMetrics,
  labelMetrics,
  takeUntil,
  takeWhile,
  tapBoth,
  tapDefect2 as tapDefect,
  tapError2 as tapError,
  tapErrorTag,
  tapErrorCause2 as tapErrorCause,
  timed,
  timedWith,
  tracer,
  tryPromise2 as tryPromise,
  tryMap,
  tryMapPromise,
  unless,
  unlessEffect,
  unsandbox,
  updateFiberRefs,
  updateService2 as updateService,
  when2 as when,
  whenFiberRef,
  whenRef,
  withMetric,
  serviceFunctionEffect,
  serviceFunction,
  serviceFunctions,
  serviceConstants,
  serviceMembers,
  serviceOption2 as serviceOption,
  serviceOptional,
  annotateCurrentSpan,
  linkSpanCurrent,
  annotateSpans,
  currentParentSpan,
  currentSpan,
  linkSpans,
  makeSpan,
  spanAnnotations,
  spanLinks,
  endSpan,
  useSpan,
  withParentSpan,
  withSpan,
  functionWithSpan,
  fromNullable2 as fromNullable,
  optionFromOptional,
  globalScope,
  FiberTypeId,
  fiberVariance2 as fiberVariance,
  RuntimeFiberTypeId,
  Order4 as Order2,
  isFiber,
  isRuntimeFiber,
  _await,
  children,
  done3 as done,
  dump,
  dumpAll,
  fail4 as fail,
  failCause4 as failCause,
  fromEffect,
  id,
  inheritAll,
  interrupted,
  interruptAll,
  interruptAllAs,
  interruptAsFork,
  join2 as join,
  map6 as map,
  mapEffect2 as mapEffect,
  mapFiber,
  match9 as match2,
  never3 as never,
  orElse3 as orElse2,
  orElseEither2 as orElseEither,
  poll,
  pretty2 as pretty,
  unsafeRoots,
  roots,
  status,
  succeed4 as succeed,
  void_3 as void_,
  getCurrentFiber,
  LoggerTypeId,
  makeLogger,
  mapInput2 as mapInput,
  mapInputOptions,
  filterLogLevel,
  map8 as map2,
  none6 as none2,
  simple,
  succeed5 as succeed2,
  sync3 as sync,
  zip6 as zip,
  zipLeft2 as zipLeft,
  zipRight2 as zipRight,
  stringLogger,
  logfmtLogger,
  structuredLogger,
  jsonLogger,
  isLogger,
  prettyLogger,
  prettyLoggerDefault,
  MetricBoundariesTypeId,
  isMetricBoundaries,
  fromIterable9 as fromIterable,
  linear,
  exponential,
  MetricKeyTypeTypeId,
  CounterKeyTypeTypeId,
  FrequencyKeyTypeTypeId,
  GaugeKeyTypeTypeId,
  HistogramKeyTypeTypeId,
  SummaryKeyTypeTypeId,
  counter,
  frequency,
  gauge,
  histogram,
  summary,
  isMetricKeyType,
  isCounterKey,
  isFrequencyKey,
  isGaugeKey,
  isHistogramKey,
  isSummaryKey,
  MetricKeyTypeId,
  isMetricKey,
  counter2,
  frequency2,
  gauge2,
  histogram2,
  summary2,
  tagged,
  taggedWithLabels,
  empty14 as empty4,
  fromIterable5 as fromIterable2,
  get7 as get3,
  keys,
  has,
  set5 as set3,
  remove,
  clear,
  size3 as size,
  MutableHashMap_exports,
  MetricStateTypeId,
  CounterStateTypeId,
  FrequencyStateTypeId,
  GaugeStateTypeId,
  HistogramStateTypeId,
  SummaryStateTypeId,
  counter3,
  frequency3,
  gauge3,
  histogram3,
  summary3,
  isMetricState,
  isCounterState,
  isFrequencyState,
  isGaugeState,
  isHistogramState,
  isSummaryState,
  MetricHookTypeId,
  make25 as make6,
  onModify,
  onUpdate,
  counter4,
  frequency4,
  gauge4,
  histogram4,
  summary4,
  MetricPairTypeId,
  make26 as make7,
  unsafeMake7 as unsafeMake2,
  MetricRegistryTypeId,
  make27 as make8,
  MetricTypeId,
  globalMetricRegistry,
  make28 as make9,
  mapInput3 as mapInput2,
  counter5,
  frequency5,
  withConstantInput,
  fromMetricKey,
  gauge5,
  histogram5,
  increment2 as increment,
  incrementBy,
  map9 as map3,
  mapType,
  modify4 as modify3,
  set6 as set4,
  succeed6 as succeed3,
  sync4 as sync2,
  summary5,
  summaryTimestamp,
  tagged2,
  taggedWithLabelsInput,
  taggedWithLabels2,
  timer2 as timer,
  timerWithBoundaries,
  trackAll,
  trackDefect,
  trackDefectWith,
  trackDuration,
  trackDurationWith,
  trackError,
  trackErrorWith,
  trackSuccess,
  trackSuccessWith,
  update5 as update3,
  value,
  withNow,
  zip7 as zip2,
  unsafeSnapshot,
  snapshot,
  RequestTypeId,
  isRequest,
  of4 as of,
  tagged3,
  Class2,
  TaggedClass,
  complete,
  completeEffect,
  fail5 as fail2,
  failCause5 as failCause2,
  succeed7 as succeed4,
  Listeners,
  empty16 as empty5,
  fromIterable7 as fromIterable3,
  findFirst3 as findFirst2,
  first2 as first,
  getOrder2 as getOrder,
  has3 as has2,
  insert2 as insert,
  keys3 as keys2,
  last3 as last,
  reduce6 as reduce2,
  removeFirst2 as removeFirst,
  size5 as size2,
  values3 as values,
  RedBlackTree_exports,
  isSortedSet,
  empty17 as empty6,
  fromIterable8 as fromIterable4,
  filter2 as filter,
  union3 as union,
  values4 as values2,
  getEquivalence2 as getEquivalence,
  SortedSet_exports,
  SupervisorTypeId,
  supervisorVariance,
  ProxySupervisor,
  Zip,
  unsafeTrack,
  track,
  fromEffect2,
  none7 as none3,
  fibersIn,
  Differ_exports,
  fiberStarted,
  fiberActive,
  fiberSuccesses,
  fiberFailures,
  fiberLifetimes,
  FiberRuntime,
  currentMinimumLogLevel,
  loggerWithConsoleLog,
  loggerWithLeveledLog,
  loggerWithConsoleError,
  defaultLogger,
  jsonLogger2,
  logFmtLogger,
  prettyLogger2,
  structuredLogger2,
  tracerLogger,
  loggerWithSpanAnnotations,
  currentLoggers,
  batchedLogger,
  annotateLogsScoped,
  whenLogLevel,
  acquireRelease2 as acquireRelease,
  acquireReleaseInterruptible,
  addFinalizer2 as addFinalizer,
  daemonChildren,
  exists,
  filter3 as filter2,
  all3 as all,
  allWith,
  allSuccesses,
  replicate2 as replicate,
  replicateEffect2 as replicateEffect,
  forEach7 as forEach,
  forEachParUnbounded,
  forEachConcurrentDiscard,
  fork2 as fork,
  forkDaemon2 as forkDaemon,
  forkWithErrorHandler,
  unsafeFork2 as unsafeFork,
  mergeAll,
  partition2 as partition,
  validateAll,
  raceAll2 as raceAll,
  reduceEffect,
  parallelFinalizers,
  finalizersMask,
  scopeWith,
  scopedWith,
  scopedEffect,
  sequentialFinalizers,
  tagMetricsScoped,
  labelMetricsScoped,
  using,
  validate,
  validateWith,
  validateFirst,
  withClockScoped,
  withRandomScoped,
  withConfigProviderScoped,
  withEarlyRelease,
  zipOptions,
  zipLeftOptions,
  zipRightOptions,
  zipWithOptions,
  withRuntimeFlagsScoped,
  scopeTag,
  scope2 as scope,
  scopeMake2 as scopeMake,
  scopeExtend,
  scopeUse,
  fiberRefUnsafeMakeSupervisor,
  fiberRefLocallyScoped,
  fiberRefLocallyScopedWith,
  fiberRefMake,
  fiberRefMakeWith,
  fiberRefMakeContext,
  fiberRefMakeRuntimeFlags,
  currentRuntimeFlags,
  currentSupervisor,
  fiberAwaitAll,
  fiberAll,
  fiberInterruptFork,
  fiberJoinAll,
  fiberScoped,
  raceWith,
  disconnect,
  race2 as race,
  raceFibersWith,
  ensuring2 as ensuring,
  invokeWithInterrupt,
  interruptWhenPossible,
  makeSpanScoped,
  withTracerScoped,
  withSpanScoped
};
//# sourceMappingURL=chunk-WCCQGKCT.js.map
