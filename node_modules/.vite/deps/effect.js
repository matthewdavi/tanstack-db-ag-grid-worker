import {
  Match_exports
} from "./chunk-MJXWV3L7.js";
import {
  ScopedRef_exports,
  fromAcquire,
  get as get7,
  set as set7
} from "./chunk-XVUTMT2N.js";
import {
  Channel_exports,
  Pool_exports,
  get as get10,
  makeWithTTL
} from "./chunk-X2W4I7LK.js";
import {
  FiberSet_exports,
  Mailbox_exports
} from "./chunk-AE34XXM4.js";
import {
  ArbitraryAnnotationId,
  BigDecimal_exports,
  BigInt_exports,
  ConfigError_exports,
  Config_exports,
  DateFromSelfSchemaId,
  DateTime_exports,
  DescriptionAnnotationId,
  Encoding_exports,
  FastCheck_exports,
  FiniteSchemaId,
  IntSchemaId,
  JsonNumberSchemaId,
  NonNaNSchemaId,
  ParseJsonSchemaId,
  ParseResult_exports,
  PrettyAnnotationId,
  Redacted_exports,
  SchemaAST_exports,
  SchemaIdAnnotationId,
  Schema_exports,
  SecretTypeId,
  Struct_exports,
  TitleAnnotationId,
  booleanKeyword,
  defaultParseOption,
  fromIterable as fromIterable7,
  fromString,
  getAnnotation,
  getArbitraryEmptyEnumErrorMessage,
  getArbitraryMissingAnnotationErrorMessage,
  getCompiler,
  getDefaultAnnotation,
  getDescriptionAnnotation,
  getExamplesAnnotation,
  getJSONIdentifier,
  getJSONSchemaAnnotation,
  getJSONSchemaMissingAnnotationErrorMessage,
  getJSONSchemaMissingIdentifierAnnotationErrorMessage,
  getJSONSchemaUnsupportedKeyErrorMessage,
  getJSONSchemaUnsupportedPostRestElementsErrorMessage,
  getKeysForIndexSignature,
  getOption,
  getPrettyMissingAnnotationErrorMessage,
  getPrettyNeverErrorMessage,
  getPrettyNoMatchingSchemaErrorMessage,
  getSchemaIdAnnotation,
  getSurrogateAnnotation,
  getTemplateLiteralRegExp,
  getTitleAnnotation,
  getTransformationFrom,
  is,
  isDeclaration,
  isRefinement,
  isSecret,
  isSuspend,
  isTransformation,
  isTypeLiteral,
  make as make22,
  memoizeThunk,
  neverKeyword,
  numberKeyword,
  pruneUndefined,
  stringKeyword,
  unsafeMake as unsafeMake5,
  unsafeWipe,
  value as value2
} from "./chunk-JEDKWGIH.js";
import {
  BackPressure,
  BothRunning,
  BufferSliding,
  ChildExecutorDecisionTypeId,
  Close,
  Continue,
  CurrentMemoMap,
  FiberRef_exports,
  GroupByTypeId,
  Layer_exports,
  LeftDone,
  MergeDecision_exports,
  MergeStateTypeId,
  MergeStrategyTypeId,
  NoUpstream,
  PubSub_exports,
  PullAfterAllEnqueued,
  PullAfterNext,
  Pulled,
  RcRef_exports,
  RightDone,
  Runtime_exports,
  STMTypeId,
  SinkTypeId,
  StreamHaltStrategy_exports,
  StreamTypeId,
  Stream_exports,
  TDequeueTypeId,
  TPubSub_exports,
  TQueue_exports,
  TRefImpl,
  TRefTypeId,
  TakeTypeId,
  UpstreamPullRequestTypeId,
  UpstreamPullStrategyTypeId,
  Yield,
  acquireUseRelease2,
  addLogger,
  addLoggerEffect,
  addLoggerScoped,
  addSupervisor,
  all as all3,
  as2 as as3,
  as3 as as4,
  asSome as asSome2,
  asSomeError,
  asVoid2 as asVoid3,
  attempt,
  bind,
  bindTo,
  buildWithMemoMap,
  catchAll2 as catchAll,
  catchSome,
  catchTag,
  catchTags,
  check,
  chunk,
  collect2 as collect,
  collectAll,
  collectAllFrom,
  collectAllN,
  collectAllToMap,
  collectAllToMapN,
  collectAllToSet,
  collectAllToSetN,
  collectAllUntil,
  collectAllUntilEffect,
  collectAllWhile,
  collectAllWhileEffect,
  collectAllWhileWith,
  collectLeftover,
  collectSTM,
  commit,
  commitEither,
  concat,
  cond,
  context2 as context3,
  context3 as context4,
  contextWith2,
  contextWith3,
  contextWithEffect2 as contextWithEffect,
  contextWithSTM,
  contextWithSink,
  count as count2,
  defaultRuntime as defaultRuntime2,
  die as die2,
  die2 as die3,
  die3 as die4,
  dieMessage,
  dieMessage2,
  dieMessage3,
  dieSync,
  dieSync2,
  dimap,
  dimapChunks,
  dimapChunksEffect,
  dimapEffect,
  disableCooperativeYielding,
  disableInterruption,
  disableOpSupervision,
  disableRuntimeMetrics,
  disableWindDown,
  done as done2,
  drain2 as drain,
  drop,
  dropUntil,
  dropUntilEffect,
  dropWhile,
  dropWhileEffect,
  effect,
  effect2,
  either,
  enableCooperativeYielding,
  enableInterruption,
  enableOpSupervision,
  enableRuntimeMetrics,
  enableWindDown,
  end,
  ensuring2 as ensuring3,
  ensuring3 as ensuring4,
  ensuringWith2 as ensuringWith,
  evaluate,
  eventually as eventually2,
  every,
  every2,
  exists,
  fail2,
  fail3,
  fail4,
  failCause2 as failCause3,
  failCause3 as failCause4,
  failCauseSync2 as failCauseSync,
  failSync2 as failSync,
  failSync3 as failSync2,
  fiberId as fiberId2,
  filter as filter4,
  filter2 as filter5,
  filterInput,
  filterInputEffect,
  filterNot,
  filterOrDie,
  filterOrDieMessage,
  filterOrElse,
  filterOrFail as filterOrFail2,
  findEffect,
  first as first2,
  flatMap2 as flatMap3,
  flatMap3 as flatMap4,
  flatten2 as flatten3,
  flip,
  flipWith,
  fold,
  foldChunks,
  foldChunksEffect,
  foldEffect,
  foldLeft,
  foldLeftChunks,
  foldLeftChunksEffect,
  foldLeftEffect,
  foldSink,
  foldUntil,
  foldUntilEffect,
  foldWeighted,
  foldWeightedDecompose,
  foldWeightedDecomposeEffect,
  foldWeightedEffect,
  forEach as forEach2,
  forEach2 as forEach3,
  forEachChunk,
  forEachChunkWhile,
  forEachWhile,
  fromChannel,
  fromEffect2,
  fromEffect3,
  fromEither2 as fromEither,
  fromExit,
  fromOption2 as fromOption,
  fromPubSub2 as fromPubSub,
  fromPubSub3 as fromPubSub2,
  fromPull,
  fromPush,
  fromQueue2 as fromQueue,
  fromTQueue,
  gen as gen3,
  get as get8,
  get2 as get9,
  getAndSet as getAndSet3,
  getAndUpdate as getAndUpdate3,
  getAndUpdateSome as getAndUpdateSome3,
  head,
  head2,
  if_,
  ignore,
  ignoreLeftover,
  interrupt as interrupt3,
  interruptAs as interruptAs2,
  isBackPressure,
  isBothRunning,
  isBufferSliding,
  isChildExecutorDecision,
  isClose,
  isContinue,
  isDone as isDone2,
  isFailure as isFailure2,
  isFailure2 as isFailure3,
  isLayer,
  isLeftDone,
  isMergeState,
  isMergeStrategy,
  isNoUpstream,
  isPullAfterAllEnqueued,
  isPullAfterNext,
  isPulled,
  isRightDone,
  isSTM,
  isSuccess as isSuccess2,
  isSuccess2 as isSuccess3,
  isUpstreamPullRequest,
  isUpstreamPullStrategy,
  isYield,
  iterate,
  last as last2,
  leftover,
  let_,
  loop,
  make as make17,
  make2 as make18,
  make3 as make19,
  make4 as make20,
  make5 as make21,
  make6 as make23,
  makeMemoMap,
  map2 as map9,
  map3 as map10,
  map4 as map11,
  map5 as map12,
  mapAttempt,
  mapBoth,
  mapEffect2 as mapEffect,
  mapEffect3 as mapEffect2,
  mapError2 as mapError,
  mapError3 as mapError2,
  mapInput2 as mapInput4,
  mapInputChunks,
  mapInputChunksEffect,
  mapInputContext2,
  mapInputEffect2 as mapInputEffect,
  mapLeftover,
  match as match3,
  match2 as match4,
  match3 as match5,
  match4 as match6,
  match5 as match7,
  match6 as match8,
  match7 as match9,
  matchEffect as matchEffect2,
  matchSTM,
  merge2 as merge3,
  mergeAll2 as mergeAll,
  minimumLogLevel,
  mkString,
  modify as modify4,
  modifySome as modifySome3,
  negate,
  never2,
  none as none7,
  of as of2,
  offer as offer2,
  option,
  orDie2 as orDie,
  orDieWith2 as orDieWith,
  orElse2,
  orElse3,
  orElseEither,
  orElseFail,
  orElseOptional,
  orElseSucceed,
  orTry,
  partition as partition2,
  provide,
  provideContext2,
  provideContext3,
  provideService2 as provideService,
  provideServiceSTM,
  provideSomeContext,
  publish,
  publish2,
  race as race2,
  raceBoth,
  raceWith,
  reduce as reduce6,
  reduceAll,
  reduceRight,
  refineOrDie,
  refineOrDie2,
  refineOrDieWith,
  refineOrDieWith2,
  reject,
  rejectSTM,
  removeLogger,
  repeatUntil,
  repeatWhile,
  replaceLogger,
  replaceLoggerEffect,
  replaceLoggerScoped,
  replicate,
  replicateSTM,
  replicateSTMDiscard,
  retry,
  retryUntil,
  retryWhile,
  runFork as runFork2,
  scoped as scoped2,
  scopedContext,
  set as set8,
  setAndGet as setAndGet3,
  shutdown,
  some as some3,
  some2 as some4,
  splitWhere,
  subscribe,
  succeed3 as succeed6,
  succeed4 as succeed7,
  succeedContext,
  succeedNone as succeedNone2,
  succeedSome,
  sum,
  summarized,
  summarized2,
  suspend2 as suspend3,
  suspend3 as suspend4,
  sync3 as sync5,
  sync4 as sync6,
  tDequeueVariance,
  tRefVariance,
  take,
  tap as tap2,
  tap2 as tap3,
  tapBoth,
  tapError,
  timed,
  toChannel,
  toChannel3 as toChannel2,
  try_,
  unbounded as unbounded2,
  unbounded2 as unbounded3,
  unbounded3 as unbounded4,
  unless,
  unlessSTM,
  unsafeGet as unsafeGet4,
  unsafeMake as unsafeMake4,
  unsafeSet,
  unsome,
  unwrap2 as unwrap,
  unwrap3 as unwrap2,
  unwrap4 as unwrap3,
  unwrapScoped,
  unwrapScoped3 as unwrapScoped2,
  unwrapScoped4 as unwrapScoped3,
  unwrapScopedWith2 as unwrapScopedWith,
  update as update5,
  updateAndGet as updateAndGet3,
  updateSome as updateSome3,
  updateSomeAndGet as updateSomeAndGet3,
  validateAll,
  validateFirst,
  void_2 as void_3,
  when as when2,
  whenSTM,
  withDuration,
  withMinimumLogLevel,
  withSTMRuntime,
  zip2 as zip4,
  zip3 as zip5,
  zipLeft2 as zipLeft3,
  zipLeft3 as zipLeft4,
  zipRight2 as zipRight4,
  zipRight3 as zipRight5,
  zipWith as zipWith2,
  zipWith2 as zipWith3
} from "./chunk-XIVDWX6L.js";
import {
  Schedule_exports
} from "./chunk-YQ6ETMON.js";
import {
  CacheTypeId,
  ConsumerCacheTypeId,
  Deferred_exports,
  Effect_exports,
  Request_exports,
  TypeId as TypeId3,
  TypeId2 as TypeId4,
  _await,
  _void,
  acquireRelease as acquireRelease2,
  acquireReleaseInterruptible,
  all as all2,
  as as as2,
  asVoid as asVoid2,
  assert,
  async,
  build,
  clear as clear2,
  consoleWith,
  context2,
  contextWith,
  count,
  countReset,
  debug,
  defaultRuntime,
  diffFiberRefs,
  dir,
  dirxml,
  done,
  ensuring as ensuring2,
  error,
  exit as exit2,
  failCause3 as failCause2,
  fiberIdWith,
  flatMap2,
  fnUntraced as fnUntraced2,
  forEach,
  forever,
  forkScoped,
  gen as gen2,
  group,
  info,
  interrupt2,
  interruptible as interruptible3,
  intoDeferred,
  isEffect,
  isExecutionPlan,
  locally,
  log,
  make as make14,
  make3 as make16,
  makeCacheStats,
  makeEntryStats,
  makeKeySet,
  makeMapKey,
  makeSemaphore,
  makeWith,
  map2 as map8,
  matchCauseEffect as matchCauseEffect2,
  merge as merge2,
  never,
  provideMerge,
  race,
  runFork,
  runtime,
  scopeWith,
  scoped,
  setConsole,
  sleep,
  succeed3 as succeed5,
  suspend2,
  sync2 as sync4,
  syncContext,
  table,
  time,
  timeLog,
  toRuntimeWithMemoMap,
  trace,
  uninterruptibleMask as uninterruptibleMask2,
  unsafeDone,
  unsafeFork,
  unsafeForkEffect,
  unsafeMakeMemoMap,
  unsafeRunCallback,
  unsafeRunPromise,
  unsafeRunPromiseEffect,
  unsafeRunPromiseExit,
  unsafeRunPromiseExitEffect,
  unsafeRunSync,
  unsafeRunSyncEffect,
  unsafeRunSyncExit,
  unsafeRunSyncExitEffect,
  warn,
  whileLoop,
  withConsole,
  withFiberRuntime as withFiberRuntime2,
  withGroup,
  withTime,
  zipLeft as zipLeft2,
  zipRight as zipRight3
} from "./chunk-PUCBIOTD.js";
import {
  Fiber_exports,
  _await as _await2,
  interruptAllAs,
  interruptAs
} from "./chunk-UDX4HWWS.js";
import {
  Cause_exports,
  Class as Class2,
  Cron_exports,
  Data_exports,
  IllegalArgumentException,
  Random_exports,
  ScheduleDecision_exports,
  ScheduleInterval_exports,
  ScheduleIntervals_exports,
  String_exports,
  TaggedError,
  empty as empty12,
  isInterruptedOnly,
  reduceWithContext,
  retry_Effect,
  scheduleForked,
  schedule_Effect,
  squash,
  struct
} from "./chunk-7AVSUGOT.js";
import {
  Scope,
  Scope_exports,
  addFinalizerExit,
  close,
  extend,
  make as make15
} from "./chunk-N4COQNTR.js";
import {
  Queue_exports
} from "./chunk-7QORDSG4.js";
import {
  EmptyMutableQueue,
  MutableList_exports,
  MutableQueue_exports,
  offer,
  poll,
  unbounded
} from "./chunk-NKVUKIFR.js";
import {
  getAndUpdateEffect,
  getAndUpdateSomeEffect,
  modify as modify3,
  modifyEffect,
  modifySomeEffect,
  updateAndGetEffect,
  updateEffect,
  updateSomeEffect
} from "./chunk-TTQAUG2R.js";
import {
  SynchronizedTypeId,
  forkIn,
  makeSynchronized,
  synchronizedVariance,
  unsafeMakeSemaphore,
  unsafeMakeSynchronized,
  updateSomeAndGetEffectSynchronized
} from "./chunk-CBQWHB33.js";
import {
  Boolean_exports,
  Class,
  ClockTypeId,
  Clock_exports,
  CommitPrototype as CommitPrototype2,
  ConfigProviderTypeId,
  CounterKeyTypeTypeId,
  CounterStateTypeId,
  Differ_exports,
  Effectable_exports,
  ExecutionStrategy_exports,
  FiberRefsPatch_exports,
  FiberRefs_exports,
  FiberStatus_exports,
  FlatConfigProviderTypeId,
  FrequencyKeyTypeTypeId,
  FrequencyStateTypeId,
  GaugeKeyTypeTypeId,
  GaugeStateTypeId,
  HistogramKeyTypeTypeId,
  HistogramStateTypeId,
  LogLevel_exports,
  LogSpan_exports,
  LoggerTypeId,
  MetricBoundariesTypeId,
  MetricHookTypeId,
  MetricKeyTypeId,
  MetricKeyTypeTypeId,
  MetricLabelTypeId,
  MetricPairTypeId,
  MetricRegistryTypeId,
  MetricStateTypeId,
  MetricTypeId,
  Micro_exports,
  MutableHashMap_exports,
  Number_exports,
  Order,
  Order2,
  ProxySupervisor,
  Readable_exports,
  RedBlackTree_exports,
  RefTypeId2 as RefTypeId,
  Ref_exports,
  RegExp_exports,
  Scheduler_exports,
  SortedSet_exports,
  SummaryKeyTypeTypeId,
  SummaryStateTypeId,
  SupervisorTypeId,
  SyncScheduler,
  Tracer_exports,
  TypeId,
  TypeId2,
  Zip,
  acquireRelease,
  addFinalizer,
  all,
  andThen,
  asSome,
  batchedLogger,
  clear,
  clock,
  clockTag,
  clockWith2 as clockWith,
  complete,
  configProviderTag,
  consoleTag,
  constantCase,
  counter,
  counter2,
  counter3,
  counter4,
  counter5,
  currentServices,
  defaultLogger,
  delay,
  descriptorWith,
  diff as diff2,
  empty as empty7,
  empty3 as empty8,
  empty4 as empty9,
  empty5 as empty10,
  empty6 as empty11,
  ensuring,
  eventually,
  exponential,
  fiberActive,
  fiberFailures,
  fiberLifetimes,
  fiberRefLocallyScopedWith,
  fiberRefMake,
  fiberStarted,
  fiberSuccesses,
  fibersIn,
  filter as filter3,
  filterLogLevel,
  filterOrFail,
  findFirst2,
  first,
  forEachConcurrentDiscard,
  fork,
  forkDaemon,
  frequency,
  frequency2,
  frequency3,
  frequency4,
  frequency5,
  fromEffect2 as fromEffect,
  fromEnv,
  fromFlat,
  fromIterable as fromIterable4,
  fromIterable2 as fromIterable5,
  fromIterable3 as fromIterable6,
  fromJson,
  fromMap,
  fromMetricKey,
  gauge,
  gauge2,
  gauge3,
  gauge4,
  gauge5,
  get as get4,
  get2 as get5,
  get3 as get6,
  getAndSet,
  getAndSet2,
  getAndUpdate,
  getAndUpdate2,
  getAndUpdateSome,
  getAndUpdateSome2,
  getOrder,
  globalMetricRegistry,
  has as has4,
  has2 as has5,
  histogram,
  histogram2,
  histogram3,
  histogram4,
  histogram5,
  ignoreLogged,
  increment,
  incrementBy,
  insert,
  invokeWithInterrupt,
  isCounterKey,
  isCounterState,
  isDone,
  isFrequencyKey,
  isFrequencyState,
  isGaugeKey,
  isGaugeState,
  isHistogramKey,
  isHistogramState,
  isLogger,
  isMetricBoundaries,
  isMetricKey,
  isMetricKeyType,
  isMetricLabel,
  isMetricState,
  isSummaryKey,
  isSummaryState,
  isSuspended,
  jsonLogger,
  jsonLogger2,
  kebabCase,
  keys,
  keys2,
  last,
  linear,
  liveServices,
  logFmtLogger,
  logWarning,
  logfmtLogger,
  loggerWithConsoleError,
  loggerWithConsoleLog,
  loggerWithLeveledLog,
  loggerWithSpanAnnotations,
  lowerCase,
  make2 as make7,
  make3 as make8,
  make5 as make9,
  make6 as make10,
  make7 as make11,
  make8 as make12,
  make9 as make13,
  makeFlat,
  makeLogger,
  map2 as map6,
  map3 as map7,
  mapInput as mapInput2,
  mapInput2 as mapInput3,
  mapInputOptions,
  mapInputPath,
  mapName,
  mapType,
  memoize,
  modify,
  modify3 as modify2,
  modifySome,
  modifySome2,
  nested,
  nested2,
  none2 as none5,
  none3 as none6,
  onModify,
  onUpdate,
  orElse,
  patch as patch2,
  prettyLogger,
  prettyLogger2,
  prettyLoggerDefault,
  reduce as reduce4,
  reduce2 as reduce5,
  refVariance,
  remove as remove2,
  removeFirst,
  scopeExtend,
  scopeMake,
  scopeTag,
  scopedEffect,
  set as set3,
  set2 as set4,
  set3 as set5,
  set4 as set6,
  setAndGet,
  setAndGet2,
  simple,
  size as size2,
  size2 as size3,
  snakeCase,
  snapshot,
  stringLogger,
  structuredLogger,
  structuredLogger2,
  succeed2 as succeed3,
  succeed3 as succeed4,
  succeedNone,
  summary,
  summary2,
  summary3,
  summary4,
  summary5,
  summaryTimestamp,
  supervisorVariance,
  sync as sync2,
  sync2 as sync3,
  tagged,
  tagged2,
  taggedWithLabels,
  taggedWithLabels2,
  taggedWithLabelsInput,
  timer,
  timerWithBoundaries,
  tracerLogger,
  track,
  trackAll,
  trackDefect,
  trackDefectWith,
  trackDuration,
  trackDurationWith,
  trackError,
  trackErrorWith,
  trackSuccess,
  trackSuccessWith,
  union as union2,
  unnested,
  unnested2,
  unsafeGet as unsafeGet3,
  unsafeMake as unsafeMake2,
  unsafeMake2 as unsafeMake3,
  unsafeSnapshot,
  unsafeTrack,
  update as update2,
  update2 as update3,
  update3 as update4,
  updateAndGet,
  updateAndGet2,
  updateSome,
  updateSome2,
  updateSomeAndGet,
  updateSomeAndGet2,
  upperCase,
  value,
  values,
  when,
  withClockScoped,
  withConstantInput,
  withNow,
  within,
  zip as zip2,
  zip2 as zip3,
  zipLeft,
  zipLeftOptions,
  zipRight as zipRight2,
  zipWithOptions
} from "./chunk-WCCQGKCT.js";
import {
  Array_exports,
  Chunk_exports,
  Context_exports,
  CooperativeYielding,
  ExceededCapacityException,
  Exit_exports,
  FiberId_exports,
  GenericTag,
  HashMap_exports,
  HashSet_exports,
  Interruption,
  Iterable_exports,
  List_exports,
  MutableRef_exports,
  None,
  OpSupervision,
  Record_exports,
  RequestResolverImpl,
  RequestResolverTypeId,
  RuntimeFlagsPatch_exports,
  RuntimeMetrics,
  Tuple_exports,
  WindDown,
  acquireUseRelease,
  add,
  add2,
  append2 as append,
  appendAll2 as appendAll,
  as2 as as,
  asVoid,
  async_,
  chunksOf,
  combine as combine2,
  compareAndSet,
  context,
  cooperativeYielding,
  currentContext,
  decrementAndGet,
  deferredAwait,
  deferredDone,
  deferredFailCause,
  deferredMake,
  deferredSucceed,
  deferredUnsafeMake,
  die2 as die,
  diff,
  differ,
  disable2 as disable,
  disableAll,
  empty,
  empty3 as empty2,
  empty5 as empty3,
  empty6 as empty4,
  empty7 as empty5,
  empty9 as empty6,
  enable2 as enable,
  enableAll,
  exit,
  exitFail,
  exitSucceed,
  exitVoid,
  fail2 as fail,
  failCause,
  fiberId,
  fiberRefGet,
  fiberRefGetWith,
  fiberRefLocally,
  fiberRefLocallyWith,
  fiberRefUnsafeMake,
  fiberRefUnsafeMakeContext,
  filter2,
  filterMap,
  findFirst2 as findFirst,
  flatMap4 as flatMap,
  flatten,
  flatten5 as flatten2,
  fnUntraced,
  forEachSequential,
  forEachSequentialDiscard,
  fromIterable,
  fromIterable2,
  fromIterable3,
  gen,
  get,
  get2,
  get3,
  has,
  has2,
  has3,
  headNonEmpty2 as headNonEmpty,
  ids,
  incrementAndGet,
  interrupt2 as interrupt,
  interruptFiber,
  interruptible,
  interruptible2,
  interruptibleMask,
  interruption,
  isDisabled,
  isEmpty2 as isEmpty,
  isEmptyRecord,
  isEnabled,
  isFailure2 as isFailure,
  isNonEmpty,
  isNonEmptyArray2 as isNonEmptyArray,
  isNonEmptyReadonlyArray,
  isRequestResolver,
  isSuccess,
  logLevelInfo,
  make11 as make6,
  make3 as make,
  make4 as make2,
  make5 as make3,
  make8 as make4,
  make9 as make5,
  map as map2,
  map2 as map3,
  map3 as map4,
  map5,
  mapInputContext,
  mapRequestResolvers,
  matchCauseEffect,
  matchEffect2 as matchEffect,
  merge,
  none as none2,
  none2 as none3,
  none3 as none4,
  of,
  onInterrupt,
  opSupervision,
  par,
  partition,
  partitionMap,
  patch,
  pick,
  prepend,
  prepend2,
  provideContext,
  reduce2 as reduce,
  reduce3 as reduce2,
  reduce5 as reduce3,
  remove2 as remove,
  render,
  resolverLocally,
  runtimeMetrics,
  scopeClose,
  seq,
  set,
  set2,
  single,
  size2 as size,
  some as some2,
  sort2 as sort,
  splitAt,
  succeed,
  succeed2,
  suspend,
  sync,
  tailNonEmpty2 as tailNonEmpty,
  tap,
  threadName2 as threadName,
  toSet,
  toValues,
  uninterruptibleMask,
  union2 as union,
  unsafeFromArray,
  unsafeGet3 as unsafeGet,
  unsafeGet4 as unsafeGet2,
  unsafeHead,
  unsafeMake,
  update,
  void_,
  void_2,
  windDown,
  withFiberRuntime,
  yieldNow,
  zip3 as zip,
  zipRight,
  zipWith2 as zipWith
} from "./chunk-ZLEQSUEA.js";
import {
  Either_exports,
  getOrThrowWith,
  isLeft,
  isRight,
  left,
  match,
  right
} from "./chunk-TVBZ6A6Y.js";
import {
  Duration_exports,
  decode,
  infinity,
  isFinite,
  millis,
  seconds,
  toMillis
} from "./chunk-AQW37YSD.js";
import {
  BaseProto,
  CommitPrototype,
  Equal_exports,
  Equivalence_exports,
  Function_exports,
  GlobalValue_exports,
  Hash_exports,
  Inspectable_exports,
  NodeInspectSymbol,
  Option_exports,
  Order_exports,
  PCGRandom,
  Pipeable_exports,
  Predicate_exports,
  Utils_exports,
  absurd,
  cached,
  combine,
  constFalse,
  constVoid,
  dual,
  equals,
  filter,
  flow,
  format,
  formatPropertyKey,
  formatUnknown,
  fromNullable,
  getBugErrorMessage,
  getCurrentVersion,
  getOrElse,
  getOrUndefined,
  getRight2 as getRight,
  globalValue,
  greaterThan,
  hasProperty,
  hash,
  hole,
  identity,
  isBigInt,
  isBoolean,
  isDate,
  isNone2 as isNone,
  isNumber,
  isPropertyKey,
  isReadonlyRecord,
  isRecord,
  isSome,
  isString,
  isSymbol,
  isTagged,
  lessThan,
  map,
  mapInput2 as mapInput,
  match as match2,
  none2 as none,
  number2 as number,
  pipe,
  pipeArguments,
  setCurrentVersion,
  some2 as some,
  string2 as string,
  symbol,
  symbol2,
  symbol3,
  toJSON,
  unsafeCoerce
} from "./chunk-H62K6C3Q.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Arbitrary.js
var Arbitrary_exports = {};
__export(Arbitrary_exports, {
  getDescription: () => getDescription,
  make: () => make24,
  makeArrayConstraints: () => makeArrayConstraints,
  makeBigIntConstraints: () => makeBigIntConstraints,
  makeDateConstraints: () => makeDateConstraints,
  makeLazy: () => makeLazy,
  makeNumberConstraints: () => makeNumberConstraints,
  makeStringConstraints: () => makeStringConstraints
});
var makeLazy = (schema) => {
  const description = getDescription(schema.ast, []);
  return go(description, {
    maxDepth: 2
  });
};
var make24 = (schema) => makeLazy(schema)(FastCheck_exports);
var makeStringConstraints = (options) => {
  const out = {
    _tag: "StringConstraints",
    constraints: {}
  };
  if (isNumber(options.minLength)) {
    out.constraints.minLength = options.minLength;
  }
  if (isNumber(options.maxLength)) {
    out.constraints.maxLength = options.maxLength;
  }
  if (isString(options.pattern)) {
    out.pattern = options.pattern;
  }
  return out;
};
var makeNumberConstraints = (options) => {
  const out = {
    _tag: "NumberConstraints",
    constraints: {},
    isInteger: options.isInteger ?? false
  };
  if (isNumber(options.min)) {
    out.constraints.min = Math.fround(options.min);
  }
  if (isBoolean(options.minExcluded)) {
    out.constraints.minExcluded = options.minExcluded;
  }
  if (isNumber(options.max)) {
    out.constraints.max = Math.fround(options.max);
  }
  if (isBoolean(options.maxExcluded)) {
    out.constraints.maxExcluded = options.maxExcluded;
  }
  if (isBoolean(options.noNaN)) {
    out.constraints.noNaN = options.noNaN;
  }
  if (isBoolean(options.noDefaultInfinity)) {
    out.constraints.noDefaultInfinity = options.noDefaultInfinity;
  }
  return out;
};
var makeBigIntConstraints = (options) => {
  const out = {
    _tag: "BigIntConstraints",
    constraints: {}
  };
  if (isBigInt(options.min)) {
    out.constraints.min = options.min;
  }
  if (isBigInt(options.max)) {
    out.constraints.max = options.max;
  }
  return out;
};
var makeArrayConstraints = (options) => {
  const out = {
    _tag: "ArrayConstraints",
    constraints: {}
  };
  if (isNumber(options.minLength)) {
    out.constraints.minLength = options.minLength;
  }
  if (isNumber(options.maxLength)) {
    out.constraints.maxLength = options.maxLength;
  }
  return out;
};
var makeDateConstraints = (options) => {
  const out = {
    _tag: "DateConstraints",
    constraints: {}
  };
  if (isDate(options.min)) {
    out.constraints.min = options.min;
  }
  if (isDate(options.max)) {
    out.constraints.max = options.max;
  }
  if (isBoolean(options.noInvalidDate)) {
    out.constraints.noInvalidDate = options.noInvalidDate;
  }
  return out;
};
var getArbitraryAnnotation = getAnnotation(ArbitraryAnnotationId);
var getASTConstraints = (ast) => {
  const TypeAnnotationId = ast.annotations[SchemaIdAnnotationId];
  if (isPropertyKey(TypeAnnotationId)) {
    const out = ast.annotations[TypeAnnotationId];
    if (isReadonlyRecord(out)) {
      return out;
    }
  }
};
var idMemoMap = globalValue(/* @__PURE__ */ Symbol.for("effect/Arbitrary/IdMemoMap"), () => /* @__PURE__ */ new Map());
var counter6 = 0;
function wrapGetDescription(f, g) {
  return (ast, path) => f(ast, g(ast, path));
}
function parseMeta(ast) {
  const jsonSchema = getJSONSchemaAnnotation(ast).pipe(filter(isReadonlyRecord), getOrUndefined);
  const schemaId = getOrElse(getSchemaIdAnnotation(ast), () => void 0);
  const schemaParams = fromNullable(schemaId).pipe(map((id) => ast.annotations[id]), filter(isReadonlyRecord), getOrUndefined);
  return [schemaId, {
    ...schemaParams,
    ...jsonSchema
  }];
}
var getDescription = wrapGetDescription((ast, description) => {
  const annotation = getArbitraryAnnotation(ast);
  if (isSome(annotation)) {
    return {
      ...description,
      annotations: [...description.annotations, annotation.value]
    };
  }
  return description;
}, (ast, path) => {
  const [schemaId, meta] = parseMeta(ast);
  switch (ast._tag) {
    case "Refinement": {
      const from = getDescription(ast.from, path);
      switch (from._tag) {
        case "StringKeyword":
          return {
            ...from,
            constraints: [...from.constraints, makeStringConstraints(meta)],
            refinements: [...from.refinements, ast]
          };
        case "NumberKeyword": {
          const c = schemaId === NonNaNSchemaId ? makeNumberConstraints({
            noNaN: true
          }) : schemaId === FiniteSchemaId || schemaId === JsonNumberSchemaId ? makeNumberConstraints({
            noDefaultInfinity: true,
            noNaN: true
          }) : makeNumberConstraints({
            isInteger: "type" in meta && meta.type === "integer",
            noNaN: void 0,
            noDefaultInfinity: void 0,
            min: meta.exclusiveMinimum ?? meta.minimum,
            minExcluded: "exclusiveMinimum" in meta ? true : void 0,
            max: meta.exclusiveMaximum ?? meta.maximum,
            maxExcluded: "exclusiveMaximum" in meta ? true : void 0
          });
          return {
            ...from,
            constraints: [...from.constraints, c],
            refinements: [...from.refinements, ast]
          };
        }
        case "BigIntKeyword": {
          const c = getASTConstraints(ast);
          return {
            ...from,
            constraints: c !== void 0 ? [...from.constraints, makeBigIntConstraints(c)] : from.constraints,
            refinements: [...from.refinements, ast]
          };
        }
        case "TupleType":
          return {
            ...from,
            constraints: [...from.constraints, makeArrayConstraints({
              minLength: meta.minItems,
              maxLength: meta.maxItems
            })],
            refinements: [...from.refinements, ast]
          };
        case "DateFromSelf":
          return {
            ...from,
            constraints: [...from.constraints, makeDateConstraints(meta)],
            refinements: [...from.refinements, ast]
          };
        default:
          return {
            ...from,
            refinements: [...from.refinements, ast]
          };
      }
    }
    case "Declaration": {
      if (schemaId === DateFromSelfSchemaId) {
        return {
          _tag: "DateFromSelf",
          constraints: [makeDateConstraints(meta)],
          path,
          refinements: [],
          annotations: []
        };
      }
      return {
        _tag: "Declaration",
        typeParameters: ast.typeParameters.map((ast2) => getDescription(ast2, path)),
        path,
        refinements: [],
        annotations: [],
        ast
      };
    }
    case "Literal": {
      return {
        _tag: "Literal",
        literal: ast.literal,
        path,
        refinements: [],
        annotations: []
      };
    }
    case "UniqueSymbol": {
      return {
        _tag: "UniqueSymbol",
        symbol: ast.symbol,
        path,
        refinements: [],
        annotations: []
      };
    }
    case "Enums": {
      return {
        _tag: "Enums",
        enums: ast.enums,
        path,
        refinements: [],
        annotations: [],
        ast
      };
    }
    case "TemplateLiteral": {
      return {
        _tag: "TemplateLiteral",
        head: ast.head,
        spans: ast.spans.map((span) => ({
          description: getDescription(span.type, path),
          literal: span.literal
        })),
        path,
        refinements: [],
        annotations: []
      };
    }
    case "StringKeyword":
      return {
        _tag: "StringKeyword",
        constraints: [],
        path,
        refinements: [],
        annotations: []
      };
    case "NumberKeyword":
      return {
        _tag: "NumberKeyword",
        constraints: [],
        path,
        refinements: [],
        annotations: []
      };
    case "BigIntKeyword":
      return {
        _tag: "BigIntKeyword",
        constraints: [],
        path,
        refinements: [],
        annotations: []
      };
    case "TupleType":
      return {
        _tag: "TupleType",
        constraints: [],
        elements: ast.elements.map((element, i) => ({
          isOptional: element.isOptional,
          description: getDescription(element.type, [...path, i])
        })),
        rest: ast.rest.map((element, i) => getDescription(element.type, [...path, i])),
        path,
        refinements: [],
        annotations: []
      };
    case "TypeLiteral":
      return {
        _tag: "TypeLiteral",
        propertySignatures: ast.propertySignatures.map((ps) => ({
          isOptional: ps.isOptional,
          name: ps.name,
          value: getDescription(ps.type, [...path, ps.name])
        })),
        indexSignatures: ast.indexSignatures.map((is2) => ({
          parameter: getDescription(is2.parameter, path),
          value: getDescription(is2.type, path)
        })),
        path,
        refinements: [],
        annotations: []
      };
    case "Union":
      return {
        _tag: "Union",
        members: ast.types.map((member, i) => getDescription(member, [...path, i])),
        path,
        refinements: [],
        annotations: []
      };
    case "Suspend": {
      const memoId = idMemoMap.get(ast);
      if (memoId !== void 0) {
        return {
          _tag: "Ref",
          id: memoId,
          ast,
          path,
          refinements: [],
          annotations: []
        };
      }
      counter6++;
      const id = `__id-${counter6}__`;
      idMemoMap.set(ast, id);
      return {
        _tag: "Suspend",
        id,
        ast,
        description: () => getDescription(ast.f(), path),
        path,
        refinements: [],
        annotations: []
      };
    }
    case "Transformation":
      return getDescription(ast.to, path);
    case "NeverKeyword":
      return {
        _tag: "NeverKeyword",
        path,
        refinements: [],
        annotations: [],
        ast
      };
    default: {
      return {
        _tag: "Keyword",
        value: ast._tag,
        path,
        refinements: [],
        annotations: []
      };
    }
  }
});
function getMax(n1, n2) {
  return n1 === void 0 ? n2 : n2 === void 0 ? n1 : n1 <= n2 ? n2 : n1;
}
function getMin(n1, n2) {
  return n1 === void 0 ? n2 : n2 === void 0 ? n1 : n1 <= n2 ? n1 : n2;
}
var getOr = (a, b) => {
  return a === void 0 ? b : b === void 0 ? a : a || b;
};
function mergePattern(pattern1, pattern2) {
  if (pattern1 === void 0) {
    return pattern2;
  }
  if (pattern2 === void 0) {
    return pattern1;
  }
  return `(?:${pattern1})|(?:${pattern2})`;
}
function mergeStringConstraints(c1, c2) {
  return makeStringConstraints({
    minLength: getMax(c1.constraints.minLength, c2.constraints.minLength),
    maxLength: getMin(c1.constraints.maxLength, c2.constraints.maxLength),
    pattern: mergePattern(c1.pattern, c2.pattern)
  });
}
function buildStringConstraints(description) {
  return description.constraints.length === 0 ? void 0 : description.constraints.reduce(mergeStringConstraints);
}
function mergeNumberConstraints(c1, c2) {
  return makeNumberConstraints({
    isInteger: c1.isInteger || c2.isInteger,
    min: getMax(c1.constraints.min, c2.constraints.min),
    minExcluded: getOr(c1.constraints.minExcluded, c2.constraints.minExcluded),
    max: getMin(c1.constraints.max, c2.constraints.max),
    maxExcluded: getOr(c1.constraints.maxExcluded, c2.constraints.maxExcluded),
    noNaN: getOr(c1.constraints.noNaN, c2.constraints.noNaN),
    noDefaultInfinity: getOr(c1.constraints.noDefaultInfinity, c2.constraints.noDefaultInfinity)
  });
}
function buildNumberConstraints(description) {
  return description.constraints.length === 0 ? void 0 : description.constraints.reduce(mergeNumberConstraints);
}
function mergeBigIntConstraints(c1, c2) {
  return makeBigIntConstraints({
    min: getMax(c1.constraints.min, c2.constraints.min),
    max: getMin(c1.constraints.max, c2.constraints.max)
  });
}
function buildBigIntConstraints(description) {
  return description.constraints.length === 0 ? void 0 : description.constraints.reduce(mergeBigIntConstraints);
}
function mergeDateConstraints(c1, c2) {
  return makeDateConstraints({
    min: getMax(c1.constraints.min, c2.constraints.min),
    max: getMin(c1.constraints.max, c2.constraints.max),
    noInvalidDate: getOr(c1.constraints.noInvalidDate, c2.constraints.noInvalidDate)
  });
}
function buildDateConstraints(description) {
  return description.constraints.length === 0 ? void 0 : description.constraints.reduce(mergeDateConstraints);
}
var constArrayConstraints = makeArrayConstraints({});
function mergeArrayConstraints(c1, c2) {
  return makeArrayConstraints({
    minLength: getMax(c1.constraints.minLength, c2.constraints.minLength),
    maxLength: getMin(c1.constraints.maxLength, c2.constraints.maxLength)
  });
}
function buildArrayConstraints(description) {
  return description.constraints.length === 0 ? void 0 : description.constraints.reduce(mergeArrayConstraints);
}
var arbitraryMemoMap = globalValue(/* @__PURE__ */ Symbol.for("effect/Arbitrary/arbitraryMemoMap"), () => /* @__PURE__ */ new WeakMap());
function applyFilters(filters, arb) {
  return (fc) => filters.reduce((arb2, filter10) => arb2.filter(filter10), arb(fc));
}
function absurd2(message) {
  return () => {
    throw new Error(message);
  };
}
function getContextConstraints(description) {
  switch (description._tag) {
    case "StringKeyword":
      return buildStringConstraints(description);
    case "NumberKeyword":
      return buildNumberConstraints(description);
    case "BigIntKeyword":
      return buildBigIntConstraints(description);
    case "DateFromSelf":
      return buildDateConstraints(description);
    case "TupleType":
      return buildArrayConstraints(description);
  }
}
function wrapGo(f, g) {
  return (description, ctx) => f(description, ctx, g(description, ctx));
}
var go = wrapGo((description, ctx, lazyArb) => {
  const annotation = description.annotations[description.annotations.length - 1];
  if (annotation === void 0) {
    switch (description._tag) {
      case "Declaration":
      case "NeverKeyword":
        throw new Error(getArbitraryMissingAnnotationErrorMessage(description.path, description.ast));
      case "Enums":
        if (description.enums.length === 0) {
          throw new Error(getArbitraryEmptyEnumErrorMessage(description.path));
        }
    }
  }
  const filters = description.refinements.map((ast) => (a) => isNone(ast.filter(a, defaultParseOption, ast)));
  if (annotation === void 0) {
    return applyFilters(filters, lazyArb);
  }
  const constraints = getContextConstraints(description);
  if (constraints !== void 0) {
    ctx = {
      ...ctx,
      constraints
    };
  }
  if (description._tag === "Declaration") {
    return applyFilters(filters, annotation(...description.typeParameters.map((p) => go(p, ctx)), ctx));
  }
  if (description.refinements.length > 0) {
    return applyFilters(filters, annotation(lazyArb, ctx));
  }
  return annotation(ctx);
}, (description, ctx) => {
  switch (description._tag) {
    case "DateFromSelf": {
      const constraints = buildDateConstraints(description);
      return (fc) => fc.date(constraints?.constraints);
    }
    case "Declaration":
    case "NeverKeyword":
      return absurd2(`BUG: cannot generate an arbitrary for ${description._tag}`);
    case "Literal":
      return (fc) => fc.constant(description.literal);
    case "UniqueSymbol":
      return (fc) => fc.constant(description.symbol);
    case "Keyword": {
      switch (description.value) {
        case "UndefinedKeyword":
          return (fc) => fc.constant(void 0);
        case "VoidKeyword":
        case "UnknownKeyword":
        case "AnyKeyword":
          return (fc) => fc.anything();
        case "BooleanKeyword":
          return (fc) => fc.boolean();
        case "SymbolKeyword":
          return (fc) => fc.string().map((s) => Symbol.for(s));
        case "ObjectKeyword":
          return (fc) => fc.oneof(fc.object(), fc.array(fc.anything()));
      }
    }
    case "Enums":
      return (fc) => fc.oneof(...description.enums.map(([_, value6]) => fc.constant(value6)));
    case "TemplateLiteral": {
      return (fc) => {
        const string2 = fc.string({
          maxLength: 5
        });
        const number2 = fc.float({
          noDefaultInfinity: true,
          noNaN: true
        });
        const getTemplateLiteralArb = (description2) => {
          const components = description2.head !== "" ? [fc.constant(description2.head)] : [];
          const getTemplateLiteralSpanTypeArb = (description3) => {
            switch (description3._tag) {
              case "StringKeyword":
                return string2;
              case "NumberKeyword":
                return number2;
              case "Literal":
                return fc.constant(String(description3.literal));
              case "Union":
                return fc.oneof(...description3.members.map(getTemplateLiteralSpanTypeArb));
              case "TemplateLiteral":
                return getTemplateLiteralArb(description3);
              default:
                return fc.constant("");
            }
          };
          description2.spans.forEach((span) => {
            components.push(getTemplateLiteralSpanTypeArb(span.description));
            if (span.literal !== "") {
              components.push(fc.constant(span.literal));
            }
          });
          return fc.tuple(...components).map((spans) => spans.join(""));
        };
        return getTemplateLiteralArb(description);
      };
    }
    case "StringKeyword": {
      const constraints = buildStringConstraints(description);
      const pattern = constraints?.pattern;
      return pattern !== void 0 ? (fc) => fc.stringMatching(new RegExp(pattern)) : (fc) => fc.string(constraints?.constraints);
    }
    case "NumberKeyword": {
      const constraints = buildNumberConstraints(description);
      return constraints?.isInteger ? (fc) => fc.integer(constraints.constraints) : (fc) => fc.float(constraints?.constraints);
    }
    case "BigIntKeyword": {
      const constraints = buildBigIntConstraints(description);
      return (fc) => fc.bigInt(constraints?.constraints ?? {});
    }
    case "TupleType": {
      const elements = [];
      let hasOptionals = false;
      for (const element of description.elements) {
        elements.push(go(element.description, ctx));
        if (element.isOptional) {
          hasOptionals = true;
        }
      }
      const rest = description.rest.map((d) => go(d, ctx));
      return (fc) => {
        let output = fc.tuple(...elements.map((arb) => arb(fc)));
        if (hasOptionals) {
          const indexes = fc.tuple(...description.elements.map((element) => element.isOptional ? fc.boolean() : fc.constant(true)));
          output = output.chain((tuple) => indexes.map((booleans) => {
            for (const [i, b] of booleans.reverse().entries()) {
              if (!b) {
                tuple.splice(booleans.length - i, 1);
              }
            }
            return tuple;
          }));
        }
        if (isNonEmptyReadonlyArray(rest)) {
          const constraints = buildArrayConstraints(description) ?? constArrayConstraints;
          const [head5, ...tail] = rest;
          const item = head5(fc);
          output = output.chain((as7) => {
            const len = as7.length;
            const restArrayConstraints = subtractElementsLength(constraints.constraints, len);
            if (restArrayConstraints.maxLength === 0) {
              return fc.constant(as7);
            }
            const arr = ctx.depthIdentifier !== void 0 ? getSuspendedArray(fc, ctx.depthIdentifier, ctx.maxDepth, item, restArrayConstraints) : fc.array(item, restArrayConstraints);
            if (len === 0) {
              return arr;
            }
            return arr.map((rest2) => [...as7, ...rest2]);
          });
          for (let j = 0; j < tail.length; j++) {
            output = output.chain((as7) => tail[j](fc).map((a) => [...as7, a]));
          }
        }
        return output;
      };
    }
    case "TypeLiteral": {
      const propertySignatures = [];
      const requiredKeys = [];
      for (const ps of description.propertySignatures) {
        if (!ps.isOptional) {
          requiredKeys.push(ps.name);
        }
        propertySignatures.push(go(ps.value, ctx));
      }
      const indexSignatures = description.indexSignatures.map((is2) => [go(is2.parameter, ctx), go(is2.value, ctx)]);
      return (fc) => {
        const pps = {};
        for (let i = 0; i < propertySignatures.length; i++) {
          const ps = description.propertySignatures[i];
          pps[ps.name] = propertySignatures[i](fc);
        }
        let output = fc.record(pps, {
          requiredKeys
        });
        for (let i = 0; i < indexSignatures.length; i++) {
          const key = indexSignatures[i][0](fc);
          const value6 = indexSignatures[i][1](fc);
          output = output.chain((o) => {
            const item = fc.tuple(key, value6);
            const arr = ctx.depthIdentifier !== void 0 ? getSuspendedArray(fc, ctx.depthIdentifier, ctx.maxDepth, item, {
              maxLength: 2
            }) : fc.array(item);
            return arr.map((tuples) => ({
              ...Object.fromEntries(tuples),
              ...o
            }));
          });
        }
        return output;
      };
    }
    case "Union": {
      const members = description.members.map((member) => go(member, ctx));
      return (fc) => fc.oneof(...members.map((arb) => arb(fc)));
    }
    case "Suspend": {
      const memo = arbitraryMemoMap.get(description.ast);
      if (memo) {
        return memo;
      }
      if (ctx.depthIdentifier === void 0) {
        ctx = {
          ...ctx,
          depthIdentifier: description.id
        };
      }
      const get37 = memoizeThunk(() => {
        return go(description.description(), ctx);
      });
      const out = (fc) => fc.constant(null).chain(() => get37()(fc));
      arbitraryMemoMap.set(description.ast, out);
      return out;
    }
    case "Ref": {
      const memo = arbitraryMemoMap.get(description.ast);
      if (memo) {
        return memo;
      }
      throw new Error(`BUG: Ref ${JSON.stringify(description.id)} not found`);
    }
  }
});
function subtractElementsLength(constraints, len) {
  if (len === 0 || constraints.minLength === void 0 && constraints.maxLength === void 0) {
    return constraints;
  }
  const out = {
    ...constraints
  };
  if (out.minLength !== void 0) {
    out.minLength = Math.max(out.minLength - len, 0);
  }
  if (out.maxLength !== void 0) {
    out.maxLength = Math.max(out.maxLength - len, 0);
  }
  return out;
}
var getSuspendedArray = (fc, depthIdentifier, maxDepth, item, constraints) => {
  const maxLengthLimit = Math.max(2, constraints.minLength ?? 0);
  if (constraints.maxLength !== void 0 && constraints.maxLength > maxLengthLimit) {
    constraints = {
      ...constraints,
      maxLength: maxLengthLimit
    };
  }
  return fc.oneof({
    maxDepth,
    depthIdentifier
  }, fc.constant([]), fc.array(item, constraints));
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Brand.js
var Brand_exports = {};
__export(Brand_exports, {
  BrandTypeId: () => BrandTypeId,
  RefinedConstructorsTypeId: () => RefinedConstructorsTypeId,
  all: () => all4,
  error: () => error2,
  errors: () => errors,
  nominal: () => nominal,
  refined: () => refined,
  unbranded: () => unbranded
});
var BrandTypeId = /* @__PURE__ */ Symbol.for("effect/Brand");
var RefinedConstructorsTypeId = /* @__PURE__ */ Symbol.for("effect/Brand/Refined");
var error2 = (message, meta) => [{
  message,
  meta
}];
var errors = (...errors2) => flatten(errors2);
function refined(...args) {
  const either3 = args.length === 2 ? (unbranded2) => args[0](unbranded2) ? right(unbranded2) : left(args[1](unbranded2)) : (unbranded2) => {
    return match2(args[0](unbranded2), {
      onNone: () => right(unbranded2),
      onSome: left
    });
  };
  return Object.assign((unbranded2) => getOrThrowWith(either3(unbranded2), identity), {
    [RefinedConstructorsTypeId]: RefinedConstructorsTypeId,
    option: (args2) => getRight(either3(args2)),
    either: either3,
    is: (args2) => isRight(either3(args2))
  });
}
var nominal = () => {
  return Object.assign((args) => args, {
    [RefinedConstructorsTypeId]: RefinedConstructorsTypeId,
    option: (args) => some(args),
    either: (args) => right(args),
    is: (_args) => true
  });
};
var all4 = (...brands) => {
  const either3 = (args) => {
    let result = right(args);
    for (const brand of brands) {
      const nextResult = brand.either(args);
      if (isLeft(result) && isLeft(nextResult)) {
        result = left([...result.left, ...nextResult.left]);
      } else {
        result = isLeft(result) ? result : nextResult;
      }
    }
    return result;
  };
  return Object.assign((args) => match(either3(args), {
    onLeft: (e) => {
      throw e;
    },
    onRight: identity
  }), {
    [RefinedConstructorsTypeId]: RefinedConstructorsTypeId,
    option: (args) => getRight(either3(args)),
    either: either3,
    is: (args) => isRight(either3(args))
  });
};
var unbranded = unsafeCoerce;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cache.js
var Cache_exports = {};
__export(Cache_exports, {
  CacheTypeId: () => CacheTypeId2,
  ConsumerCacheTypeId: () => ConsumerCacheTypeId2,
  make: () => make25,
  makeCacheStats: () => makeCacheStats2,
  makeEntryStats: () => makeEntryStats2,
  makeWith: () => makeWith2
});
var CacheTypeId2 = CacheTypeId;
var ConsumerCacheTypeId2 = ConsumerCacheTypeId;
var make25 = make16;
var makeWith2 = makeWith;
var makeCacheStats2 = makeCacheStats;
var makeEntryStats2 = makeEntryStats;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ChildExecutorDecision.js
var ChildExecutorDecision_exports = {};
__export(ChildExecutorDecision_exports, {
  ChildExecutorDecisionTypeId: () => ChildExecutorDecisionTypeId2,
  Close: () => Close2,
  Continue: () => Continue2,
  Yield: () => Yield2,
  isChildExecutorDecision: () => isChildExecutorDecision2,
  isClose: () => isClose2,
  isContinue: () => isContinue2,
  isYield: () => isYield2,
  match: () => match10
});
var ChildExecutorDecisionTypeId2 = ChildExecutorDecisionTypeId;
var Continue2 = Continue;
var Close2 = Close;
var Yield2 = Yield;
var isChildExecutorDecision2 = isChildExecutorDecision;
var isContinue2 = isContinue;
var isClose2 = isClose;
var isYield2 = isYield;
var match10 = match3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ConfigProvider.js
var ConfigProvider_exports = {};
__export(ConfigProvider_exports, {
  ConfigProvider: () => ConfigProvider,
  ConfigProviderTypeId: () => ConfigProviderTypeId2,
  FlatConfigProviderTypeId: () => FlatConfigProviderTypeId2,
  constantCase: () => constantCase2,
  fromEnv: () => fromEnv2,
  fromFlat: () => fromFlat2,
  fromJson: () => fromJson2,
  fromMap: () => fromMap2,
  kebabCase: () => kebabCase2,
  lowerCase: () => lowerCase2,
  make: () => make26,
  makeFlat: () => makeFlat2,
  mapInputPath: () => mapInputPath2,
  nested: () => nested3,
  orElse: () => orElse4,
  snakeCase: () => snakeCase2,
  unnested: () => unnested3,
  upperCase: () => upperCase2,
  within: () => within2
});
var ConfigProviderTypeId2 = ConfigProviderTypeId;
var FlatConfigProviderTypeId2 = FlatConfigProviderTypeId;
var ConfigProvider = configProviderTag;
var make26 = make8;
var makeFlat2 = makeFlat;
var fromEnv2 = fromEnv;
var fromFlat2 = fromFlat;
var fromJson2 = fromJson;
var fromMap2 = fromMap;
var constantCase2 = constantCase;
var mapInputPath2 = mapInputPath;
var kebabCase2 = kebabCase;
var lowerCase2 = lowerCase;
var nested3 = nested2;
var orElse4 = orElse;
var unnested3 = unnested2;
var snakeCase2 = snakeCase;
var upperCase2 = upperCase;
var within2 = within;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ConfigProviderPathPatch.js
var ConfigProviderPathPatch_exports = {};
__export(ConfigProviderPathPatch_exports, {
  andThen: () => andThen2,
  empty: () => empty13,
  mapName: () => mapName2,
  nested: () => nested4,
  unnested: () => unnested4
});
var empty13 = empty8;
var andThen2 = andThen;
var mapName2 = mapName;
var nested4 = nested;
var unnested4 = unnested;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Console.js
var Console_exports = {};
__export(Console_exports, {
  Console: () => Console,
  TypeId: () => TypeId5,
  assert: () => assert2,
  clear: () => clear3,
  consoleWith: () => consoleWith2,
  count: () => count3,
  countReset: () => countReset2,
  debug: () => debug2,
  dir: () => dir2,
  dirxml: () => dirxml2,
  error: () => error3,
  group: () => group2,
  info: () => info2,
  log: () => log2,
  setConsole: () => setConsole2,
  table: () => table2,
  time: () => time2,
  timeLog: () => timeLog2,
  trace: () => trace2,
  warn: () => warn2,
  withConsole: () => withConsole2,
  withGroup: () => withGroup2,
  withTime: () => withTime2
});
var TypeId5 = TypeId2;
var Console = consoleTag;
var withConsole2 = withConsole;
var setConsole2 = setConsole;
var consoleWith2 = consoleWith;
var assert2 = assert;
var clear3 = clear2;
var count3 = count;
var countReset2 = countReset;
var debug2 = debug;
var dir2 = dir;
var dirxml2 = dirxml;
var error3 = error;
var group2 = group;
var info2 = info;
var log2 = log;
var table2 = table;
var time2 = time;
var timeLog2 = timeLog;
var trace2 = trace;
var warn2 = warn;
var withGroup2 = withGroup;
var withTime2 = withTime;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/DefaultServices.js
var DefaultServices_exports = {};
__export(DefaultServices_exports, {
  currentServices: () => currentServices2,
  liveServices: () => liveServices2
});
var liveServices2 = liveServices;
var currentServices2 = currentServices;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ExecutionPlan.js
var ExecutionPlan_exports = {};
__export(ExecutionPlan_exports, {
  TypeId: () => TypeId6,
  isExecutionPlan: () => isExecutionPlan2,
  make: () => make27,
  merge: () => merge4
});
var TypeId6 = TypeId4;
var isExecutionPlan2 = isExecutionPlan;
var make27 = (...steps) => makeProto(steps.map((options, i) => {
  if (options.attempts && options.attempts < 1) {
    throw new Error(`ExecutionPlan.make: step[${i}].attempts must be greater than 0`);
  }
  return {
    schedule: options.schedule,
    attempts: options.attempts,
    while: options.while ? (input) => suspend2(() => {
      const result = options.while(input);
      return typeof result === "boolean" ? succeed5(result) : result;
    }) : void 0,
    provide: options.provide
  };
}));
var Proto = {
  [TypeId6]: TypeId6,
  get withRequirements() {
    const self = this;
    return contextWith((context7) => makeProto(self.steps.map((step) => ({
      ...step,
      provide: isLayer(step.provide) ? provide(step.provide, succeedContext(context7)) : step.provide
    }))));
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeProto = (steps) => {
  const self = Object.create(Proto);
  self.steps = steps;
  return self;
};
var merge4 = (...plans) => makeProto(plans.flatMap((plan) => plan.steps));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberHandle.js
var FiberHandle_exports = {};
__export(FiberHandle_exports, {
  TypeId: () => TypeId7,
  awaitEmpty: () => awaitEmpty,
  clear: () => clear4,
  get: () => get11,
  isFiberHandle: () => isFiberHandle,
  join: () => join,
  make: () => make28,
  makeRuntime: () => makeRuntime,
  makeRuntimePromise: () => makeRuntimePromise,
  run: () => run,
  runtime: () => runtime2,
  runtimePromise: () => runtimePromise,
  set: () => set9,
  unsafeGet: () => unsafeGet5,
  unsafeSet: () => unsafeSet2
});
var TypeId7 = /* @__PURE__ */ Symbol.for("effect/FiberHandle");
var isFiberHandle = (u) => hasProperty(u, TypeId7);
var Proto2 = {
  [TypeId7]: TypeId7,
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "FiberHandle",
      state: this.state
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var unsafeMake6 = (deferred) => {
  const self = Object.create(Proto2);
  self.state = {
    _tag: "Open",
    fiber: void 0
  };
  self.deferred = deferred;
  return self;
};
var make28 = () => acquireRelease2(map8(make14(), (deferred) => unsafeMake6(deferred)), (handle) => withFiberRuntime2((parent) => {
  const state = handle.state;
  if (state._tag === "Closed") return _void;
  handle.state = {
    _tag: "Closed"
  };
  return state.fiber ? intoDeferred(asVoid2(interruptAs(state.fiber, combine2(parent.id(), internalFiberId))), handle.deferred) : done(handle.deferred, void_2);
}));
var makeRuntime = () => flatMap2(make28(), (self) => runtime2(self)());
var makeRuntimePromise = () => flatMap2(make28(), (self) => runtimePromise(self)());
var internalFiberIdId = -1;
var internalFiberId = make4(internalFiberIdId, 0);
var isInternalInterruption = reduceWithContext(void 0, {
  emptyCase: constFalse,
  failCase: constFalse,
  dieCase: constFalse,
  interruptCase: (_, fiberId4) => has2(ids(fiberId4), internalFiberIdId),
  sequentialCase: (_, left2, right2) => left2 || right2,
  parallelCase: (_, left2, right2) => left2 || right2
});
var unsafeSet2 = dual((args) => isFiberHandle(args[0]), (self, fiber, options) => {
  if (self.state._tag === "Closed") {
    fiber.unsafeInterruptAsFork(combine2(options?.interruptAs ?? none3, internalFiberId));
    return;
  } else if (self.state.fiber !== void 0) {
    if (options?.onlyIfMissing === true) {
      fiber.unsafeInterruptAsFork(combine2(options?.interruptAs ?? none3, internalFiberId));
      return;
    } else if (self.state.fiber === fiber) {
      return;
    }
    self.state.fiber.unsafeInterruptAsFork(combine2(options?.interruptAs ?? none3, internalFiberId));
    self.state.fiber = void 0;
  }
  self.state.fiber = fiber;
  fiber.addObserver((exit3) => {
    if (self.state._tag === "Open" && fiber === self.state.fiber) {
      self.state.fiber = void 0;
    }
    if (isFailure(exit3) && (options?.propagateInterruption === true ? !isInternalInterruption(exit3.cause) : !isInterruptedOnly(exit3.cause))) {
      unsafeDone(self.deferred, exit3);
    }
  });
});
var set9 = dual((args) => isFiberHandle(args[0]), (self, fiber, options) => fiberIdWith((fiberId4) => sync4(() => unsafeSet2(self, fiber, {
  interruptAs: fiberId4,
  onlyIfMissing: options?.onlyIfMissing,
  propagateInterruption: options?.propagateInterruption
}))));
var unsafeGet5 = (self) => self.state._tag === "Closed" ? none() : fromNullable(self.state.fiber);
var get11 = (self) => suspend2(() => unsafeGet5(self));
var clear4 = (self) => uninterruptibleMask2((restore) => withFiberRuntime2((fiber) => {
  if (self.state._tag === "Closed" || self.state.fiber === void 0) {
    return _void;
  }
  return zipRight3(restore(interruptAs(self.state.fiber, combine2(fiber.id(), internalFiberId))), sync4(() => {
    if (self.state._tag === "Open") {
      self.state.fiber = void 0;
    }
  }));
}));
var constInterruptedFiber = /* @__PURE__ */ (function() {
  let fiber = void 0;
  return () => {
    if (fiber === void 0) {
      fiber = runFork(interrupt2);
    }
    return fiber;
  };
})();
var run = function() {
  const self = arguments[0];
  if (isEffect(arguments[1])) {
    return runImpl(self, arguments[1], arguments[2]);
  }
  const options = arguments[1];
  return (effect3) => runImpl(self, effect3, options);
};
var runImpl = (self, effect3, options) => withFiberRuntime2((parent) => {
  if (self.state._tag === "Closed") {
    return interrupt2;
  } else if (self.state.fiber !== void 0 && options?.onlyIfMissing === true) {
    return sync4(constInterruptedFiber);
  }
  const runtime4 = make18({
    context: parent.currentContext,
    fiberRefs: parent.getFiberRefs(),
    runtimeFlags: defaultRuntime2.runtimeFlags
  });
  const fiber = runFork2(runtime4)(effect3);
  unsafeSet2(self, fiber, {
    ...options,
    interruptAs: parent.id()
  });
  return succeed5(fiber);
});
var runtime2 = (self) => () => map8(runtime(), (runtime4) => {
  const runFork3 = runFork2(runtime4);
  return (effect3, options) => {
    if (self.state._tag === "Closed") {
      return constInterruptedFiber();
    } else if (self.state.fiber !== void 0 && options?.onlyIfMissing === true) {
      return constInterruptedFiber();
    }
    const fiber = runFork3(effect3, options);
    unsafeSet2(self, fiber, options);
    return fiber;
  };
});
var runtimePromise = (self) => () => map8(runtime2(self)(), (runFork3) => (effect3, options) => new Promise((resolve, reject3) => runFork3(effect3, options).addObserver((exit3) => {
  if (isSuccess(exit3)) {
    resolve(exit3.value);
  } else {
    reject3(squash(exit3.cause));
  }
})));
var join = (self) => _await(self.deferred);
var awaitEmpty = (self) => suspend2(() => {
  if (self.state._tag === "Closed" || self.state.fiber === void 0) {
    return _void;
  }
  return _await2(self.state.fiber);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberMap.js
var FiberMap_exports = {};
__export(FiberMap_exports, {
  TypeId: () => TypeId8,
  awaitEmpty: () => awaitEmpty2,
  clear: () => clear5,
  get: () => get12,
  has: () => has6,
  isFiberMap: () => isFiberMap,
  join: () => join2,
  make: () => make29,
  makeRuntime: () => makeRuntime2,
  makeRuntimePromise: () => makeRuntimePromise2,
  remove: () => remove3,
  run: () => run2,
  runtime: () => runtime3,
  runtimePromise: () => runtimePromise2,
  set: () => set10,
  size: () => size4,
  unsafeGet: () => unsafeGet6,
  unsafeHas: () => unsafeHas,
  unsafeSet: () => unsafeSet3
});
var TypeId8 = /* @__PURE__ */ Symbol.for("effect/FiberMap");
var isFiberMap = (u) => hasProperty(u, TypeId8);
var Proto3 = {
  [TypeId8]: TypeId8,
  [Symbol.iterator]() {
    if (this.state._tag === "Closed") {
      return empty();
    }
    return this.state.backing[Symbol.iterator]();
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "FiberMap",
      state: this.state
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var unsafeMake7 = (backing, deferred) => {
  const self = Object.create(Proto3);
  self.state = {
    _tag: "Open",
    backing
  };
  self.deferred = deferred;
  return self;
};
var make29 = () => acquireRelease2(map8(make14(), (deferred) => unsafeMake7(empty9(), deferred)), (map22) => withFiberRuntime2((parent) => {
  const state = map22.state;
  if (state._tag === "Closed") return _void;
  map22.state = {
    _tag: "Closed"
  };
  return interruptAllAs(map2(state.backing, ([, fiber]) => fiber), combine2(parent.id(), internalFiberId2)).pipe(intoDeferred(map22.deferred));
}));
var makeRuntime2 = () => flatMap2(make29(), (self) => runtime3(self)());
var makeRuntimePromise2 = () => flatMap2(make29(), (self) => runtimePromise2(self)());
var internalFiberIdId2 = -1;
var internalFiberId2 = make4(internalFiberIdId2, 0);
var isInternalInterruption2 = reduceWithContext(void 0, {
  emptyCase: constFalse,
  failCase: constFalse,
  dieCase: constFalse,
  interruptCase: (_, fiberId4) => has2(ids(fiberId4), internalFiberIdId2),
  sequentialCase: (_, left2, right2) => left2 || right2,
  parallelCase: (_, left2, right2) => left2 || right2
});
var unsafeSet3 = dual((args) => isFiberMap(args[0]), (self, key, fiber, options) => {
  if (self.state._tag === "Closed") {
    fiber.unsafeInterruptAsFork(combine2(options?.interruptAs ?? none3, internalFiberId2));
    return;
  }
  const previous = get6(self.state.backing, key);
  if (previous._tag === "Some") {
    if (options?.onlyIfMissing === true) {
      fiber.unsafeInterruptAsFork(combine2(options?.interruptAs ?? none3, internalFiberId2));
      return;
    } else if (previous.value === fiber) {
      return;
    }
    previous.value.unsafeInterruptAsFork(combine2(options?.interruptAs ?? none3, internalFiberId2));
  }
  set5(self.state.backing, key, fiber);
  fiber.addObserver((exit3) => {
    if (self.state._tag === "Closed") {
      return;
    }
    const current = get6(self.state.backing, key);
    if (isSome(current) && fiber === current.value) {
      remove2(self.state.backing, key);
    }
    if (isFailure(exit3) && (options?.propagateInterruption === true ? !isInternalInterruption2(exit3.cause) : !isInterruptedOnly(exit3.cause))) {
      unsafeDone(self.deferred, exit3);
    }
  });
});
var set10 = dual((args) => isFiberMap(args[0]), (self, key, fiber, options) => fiberIdWith((fiberId4) => sync4(() => unsafeSet3(self, key, fiber, {
  ...options,
  interruptAs: fiberId4
}))));
var unsafeGet6 = dual(2, (self, key) => self.state._tag === "Closed" ? none() : get6(self.state.backing, key));
var get12 = dual(2, (self, key) => suspend2(() => unsafeGet6(self, key)));
var unsafeHas = dual(2, (self, key) => self.state._tag === "Closed" ? false : has4(self.state.backing, key));
var has6 = dual(2, (self, key) => sync4(() => unsafeHas(self, key)));
var remove3 = dual(2, (self, key) => withFiberRuntime2((removeFiber) => {
  if (self.state._tag === "Closed") {
    return _void;
  }
  const fiber = get6(self.state.backing, key);
  if (fiber._tag === "None") {
    return _void;
  }
  return interruptAs(fiber.value, combine2(removeFiber.id(), internalFiberId2));
}));
var clear5 = (self) => withFiberRuntime2((clearFiber) => {
  if (self.state._tag === "Closed") {
    return _void;
  }
  return forEach(self.state.backing, ([, fiber]) => (
    // will be removed by the observer
    interruptAs(fiber, combine2(clearFiber.id(), internalFiberId2))
  ));
});
var constInterruptedFiber2 = /* @__PURE__ */ (function() {
  let fiber = void 0;
  return () => {
    if (fiber === void 0) {
      fiber = runFork(interrupt2);
    }
    return fiber;
  };
})();
var run2 = function() {
  const self = arguments[0];
  if (isEffect(arguments[2])) {
    return runImpl2(self, arguments[1], arguments[2], arguments[3]);
  }
  const key = arguments[1];
  const options = arguments[2];
  return (effect3) => runImpl2(self, key, effect3, options);
};
var runImpl2 = (self, key, effect3, options) => withFiberRuntime2((parent) => {
  if (self.state._tag === "Closed") {
    return interrupt2;
  } else if (options?.onlyIfMissing === true && unsafeHas(self, key)) {
    return sync4(constInterruptedFiber2);
  }
  const runtime4 = make18({
    context: parent.currentContext,
    fiberRefs: parent.getFiberRefs(),
    runtimeFlags: defaultRuntime2.runtimeFlags
  });
  const fiber = runFork2(runtime4)(effect3);
  unsafeSet3(self, key, fiber, {
    ...options,
    interruptAs: parent.id()
  });
  return succeed5(fiber);
});
var runtime3 = (self) => () => map8(runtime(), (runtime4) => {
  const runFork3 = runFork2(runtime4);
  return (key, effect3, options) => {
    if (self.state._tag === "Closed") {
      return constInterruptedFiber2();
    } else if (options?.onlyIfMissing === true && unsafeHas(self, key)) {
      return constInterruptedFiber2();
    }
    const fiber = runFork3(effect3, options);
    unsafeSet3(self, key, fiber, options);
    return fiber;
  };
});
var runtimePromise2 = (self) => () => map8(runtime3(self)(), (runFork3) => (key, effect3, options) => new Promise((resolve, reject3) => runFork3(key, effect3, options).addObserver((exit3) => {
  if (isSuccess(exit3)) {
    resolve(exit3.value);
  } else {
    reject3(squash(exit3.cause));
  }
})));
var size4 = (self) => sync4(() => self.state._tag === "Closed" ? 0 : size2(self.state.backing));
var join2 = (self) => _await(self.deferred);
var awaitEmpty2 = (self) => whileLoop({
  while: () => self.state._tag === "Open" && size2(self.state.backing) > 0,
  body: () => _await2(unsafeHead(self)[1]),
  step: constVoid
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Graph.js
var Graph_exports = {};
__export(Graph_exports, {
  Edge: () => Edge,
  GraphError: () => GraphError,
  TypeId: () => TypeId9,
  Walker: () => Walker,
  addEdge: () => addEdge,
  addNode: () => addNode,
  astar: () => astar,
  beginMutation: () => beginMutation,
  bellmanFord: () => bellmanFord,
  bfs: () => bfs,
  connectedComponents: () => connectedComponents,
  dfs: () => dfs,
  dfsPostOrder: () => dfsPostOrder,
  dijkstra: () => dijkstra,
  directed: () => directed,
  edgeCount: () => edgeCount,
  edges: () => edges,
  endMutation: () => endMutation,
  entries: () => entries,
  externals: () => externals,
  filterEdges: () => filterEdges,
  filterMapEdges: () => filterMapEdges,
  filterMapNodes: () => filterMapNodes,
  filterNodes: () => filterNodes,
  findEdge: () => findEdge,
  findEdges: () => findEdges,
  findNode: () => findNode,
  findNodes: () => findNodes,
  floydWarshall: () => floydWarshall,
  getEdge: () => getEdge,
  getNode: () => getNode,
  hasEdge: () => hasEdge,
  hasNode: () => hasNode,
  indices: () => indices,
  isAcyclic: () => isAcyclic,
  isBipartite: () => isBipartite,
  isGraph: () => isGraph,
  mapEdges: () => mapEdges,
  mapNodes: () => mapNodes,
  mutate: () => mutate,
  neighbors: () => neighbors,
  neighborsDirected: () => neighborsDirected,
  nodeCount: () => nodeCount,
  nodes: () => nodes,
  removeEdge: () => removeEdge,
  removeNode: () => removeNode,
  reverse: () => reverse,
  stronglyConnectedComponents: () => stronglyConnectedComponents,
  toGraphViz: () => toGraphViz,
  toMermaid: () => toMermaid,
  topo: () => topo,
  undirected: () => undirected,
  updateEdge: () => updateEdge,
  updateNode: () => updateNode,
  values: () => values2
});
var TypeId9 = "~effect/Graph";
var Edge = class extends Class2 {
};
var ProtoGraph = {
  [TypeId9]: TypeId9,
  [Symbol.iterator]() {
    return this.nodes[Symbol.iterator]();
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol3](that) {
    if (isGraph(that)) {
      if (this.nodes.size !== that.nodes.size || this.edges.size !== that.edges.size || this.type !== that.type) {
        return false;
      }
      for (const [nodeIndex, nodeData] of this.nodes) {
        if (!that.nodes.has(nodeIndex)) {
          return false;
        }
        const otherNodeData = that.nodes.get(nodeIndex);
        if (!equals(nodeData, otherNodeData)) {
          return false;
        }
      }
      for (const [edgeIndex, edgeData] of this.edges) {
        if (!that.edges.has(edgeIndex)) {
          return false;
        }
        const otherEdge = that.edges.get(edgeIndex);
        if (!equals(edgeData, otherEdge)) {
          return false;
        }
      }
      return true;
    }
    return false;
  },
  [symbol2]() {
    let hash3 = string("Graph");
    hash3 = hash3 ^ string(this.type);
    hash3 = hash3 ^ number(this.nodes.size);
    hash3 = hash3 ^ number(this.edges.size);
    for (const [nodeIndex, nodeData] of this.nodes) {
      hash3 = hash3 ^ hash(nodeIndex) + hash(nodeData);
    }
    for (const [edgeIndex, edgeData] of this.edges) {
      hash3 = hash3 ^ hash(edgeIndex) + hash(edgeData);
    }
    return hash3;
  },
  toJSON() {
    return {
      _id: "Graph",
      nodeCount: this.nodes.size,
      edgeCount: this.edges.size,
      type: this.type
    };
  },
  toString() {
    return format(this);
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GraphError = class extends TaggedError("GraphError") {
};
var missingNode = (node) => new GraphError({
  message: `Node ${node} does not exist`
});
var isGraph = (u) => typeof u === "object" && u !== null && TypeId9 in u;
var directed = (mutate2) => {
  const graph = Object.create(ProtoGraph);
  graph.type = "directed";
  graph.nodes = /* @__PURE__ */ new Map();
  graph.edges = /* @__PURE__ */ new Map();
  graph.adjacency = /* @__PURE__ */ new Map();
  graph.reverseAdjacency = /* @__PURE__ */ new Map();
  graph.nextNodeIndex = 0;
  graph.nextEdgeIndex = 0;
  graph.isAcyclic = some(true);
  graph.mutable = false;
  if (mutate2) {
    const mutable = beginMutation(graph);
    mutate2(mutable);
    return endMutation(mutable);
  }
  return graph;
};
var undirected = (mutate2) => {
  const graph = Object.create(ProtoGraph);
  graph.type = "undirected";
  graph.nodes = /* @__PURE__ */ new Map();
  graph.edges = /* @__PURE__ */ new Map();
  graph.adjacency = /* @__PURE__ */ new Map();
  graph.reverseAdjacency = /* @__PURE__ */ new Map();
  graph.nextNodeIndex = 0;
  graph.nextEdgeIndex = 0;
  graph.isAcyclic = some(true);
  graph.mutable = false;
  if (mutate2) {
    const mutable = beginMutation(graph);
    mutate2(mutable);
    return endMutation(mutable);
  }
  return graph;
};
var beginMutation = (graph) => {
  const adjacency = /* @__PURE__ */ new Map();
  const reverseAdjacency = /* @__PURE__ */ new Map();
  for (const [nodeIndex, edges2] of graph.adjacency) {
    adjacency.set(nodeIndex, [...edges2]);
  }
  for (const [nodeIndex, edges2] of graph.reverseAdjacency) {
    reverseAdjacency.set(nodeIndex, [...edges2]);
  }
  const mutable = Object.create(ProtoGraph);
  mutable.type = graph.type;
  mutable.nodes = new Map(graph.nodes);
  mutable.edges = new Map(graph.edges);
  mutable.adjacency = adjacency;
  mutable.reverseAdjacency = reverseAdjacency;
  mutable.nextNodeIndex = graph.nextNodeIndex;
  mutable.nextEdgeIndex = graph.nextEdgeIndex;
  mutable.isAcyclic = graph.isAcyclic;
  mutable.mutable = true;
  return mutable;
};
var endMutation = (mutable) => {
  const graph = Object.create(ProtoGraph);
  graph.type = mutable.type;
  graph.nodes = new Map(mutable.nodes);
  graph.edges = new Map(mutable.edges);
  graph.adjacency = mutable.adjacency;
  graph.reverseAdjacency = mutable.reverseAdjacency;
  graph.nextNodeIndex = mutable.nextNodeIndex;
  graph.nextEdgeIndex = mutable.nextEdgeIndex;
  graph.isAcyclic = mutable.isAcyclic;
  graph.mutable = false;
  return graph;
};
var mutate = dual(2, (graph, f) => {
  const mutable = beginMutation(graph);
  f(mutable);
  return endMutation(mutable);
});
var addNode = (mutable, data) => {
  const nodeIndex = mutable.nextNodeIndex;
  mutable.nodes.set(nodeIndex, data);
  mutable.adjacency.set(nodeIndex, []);
  mutable.reverseAdjacency.set(nodeIndex, []);
  mutable.nextNodeIndex = mutable.nextNodeIndex + 1;
  return nodeIndex;
};
var getNode = (graph, nodeIndex) => graph.nodes.has(nodeIndex) ? some(graph.nodes.get(nodeIndex)) : none();
var hasNode = (graph, nodeIndex) => graph.nodes.has(nodeIndex);
var nodeCount = (graph) => graph.nodes.size;
var findNode = (graph, predicate) => {
  for (const [index, data] of graph.nodes) {
    if (predicate(data)) {
      return some(index);
    }
  }
  return none();
};
var findNodes = (graph, predicate) => {
  const results = [];
  for (const [index, data] of graph.nodes) {
    if (predicate(data)) {
      results.push(index);
    }
  }
  return results;
};
var findEdge = (graph, predicate) => {
  for (const [edgeIndex, edgeData] of graph.edges) {
    if (predicate(edgeData.data, edgeData.source, edgeData.target)) {
      return some(edgeIndex);
    }
  }
  return none();
};
var findEdges = (graph, predicate) => {
  const results = [];
  for (const [edgeIndex, edgeData] of graph.edges) {
    if (predicate(edgeData.data, edgeData.source, edgeData.target)) {
      results.push(edgeIndex);
    }
  }
  return results;
};
var updateNode = (mutable, index, f) => {
  if (!mutable.nodes.has(index)) {
    return;
  }
  const currentData = mutable.nodes.get(index);
  const newData = f(currentData);
  mutable.nodes.set(index, newData);
};
var updateEdge = (mutable, edgeIndex, f) => {
  if (!mutable.edges.has(edgeIndex)) {
    return;
  }
  const currentEdge = mutable.edges.get(edgeIndex);
  const newData = f(currentEdge.data);
  mutable.edges.set(edgeIndex, {
    ...currentEdge,
    data: newData
  });
};
var mapNodes = (mutable, f) => {
  for (const [index, data] of mutable.nodes) {
    const newData = f(data);
    mutable.nodes.set(index, newData);
  }
};
var mapEdges = (mutable, f) => {
  for (const [index, edgeData] of mutable.edges) {
    const newData = f(edgeData.data);
    mutable.edges.set(index, {
      ...edgeData,
      data: newData
    });
  }
};
var reverse = (mutable) => {
  for (const [index, edgeData] of mutable.edges) {
    mutable.edges.set(index, {
      source: edgeData.target,
      target: edgeData.source,
      data: edgeData.data
    });
  }
  mutable.adjacency.clear();
  mutable.reverseAdjacency.clear();
  for (const [edgeIndex, edgeData] of mutable.edges) {
    const sourceEdges = mutable.adjacency.get(edgeData.source) || [];
    sourceEdges.push(edgeIndex);
    mutable.adjacency.set(edgeData.source, sourceEdges);
    const targetEdges = mutable.reverseAdjacency.get(edgeData.target) || [];
    targetEdges.push(edgeIndex);
    mutable.reverseAdjacency.set(edgeData.target, targetEdges);
  }
  mutable.isAcyclic = none();
};
var filterMapNodes = (mutable, f) => {
  const nodesToRemove = [];
  for (const [index, data] of mutable.nodes) {
    const result = f(data);
    if (isSome(result)) {
      mutable.nodes.set(index, result.value);
    } else {
      nodesToRemove.push(index);
    }
  }
  for (const nodeIndex of nodesToRemove) {
    removeNode(mutable, nodeIndex);
  }
};
var filterMapEdges = (mutable, f) => {
  const edgesToRemove = [];
  for (const [index, edgeData] of mutable.edges) {
    const result = f(edgeData.data);
    if (isSome(result)) {
      mutable.edges.set(index, {
        ...edgeData,
        data: result.value
      });
    } else {
      edgesToRemove.push(index);
    }
  }
  for (const edgeIndex of edgesToRemove) {
    removeEdge(mutable, edgeIndex);
  }
};
var filterNodes = (mutable, predicate) => {
  const nodesToRemove = [];
  for (const [index, data] of mutable.nodes) {
    if (!predicate(data)) {
      nodesToRemove.push(index);
    }
  }
  for (const nodeIndex of nodesToRemove) {
    removeNode(mutable, nodeIndex);
  }
};
var filterEdges = (mutable, predicate) => {
  const edgesToRemove = [];
  for (const [index, edgeData] of mutable.edges) {
    if (!predicate(edgeData.data)) {
      edgesToRemove.push(index);
    }
  }
  for (const edgeIndex of edgesToRemove) {
    removeEdge(mutable, edgeIndex);
  }
};
var invalidateCycleFlagOnRemoval = (mutable) => {
  if (isSome(mutable.isAcyclic) && mutable.isAcyclic.value === false) {
    mutable.isAcyclic = none();
  }
};
var invalidateCycleFlagOnAddition = (mutable) => {
  if (isSome(mutable.isAcyclic) && mutable.isAcyclic.value === true) {
    mutable.isAcyclic = none();
  }
};
var addEdge = (mutable, source, target, data) => {
  if (!mutable.nodes.has(source)) {
    throw missingNode(source);
  }
  if (!mutable.nodes.has(target)) {
    throw missingNode(target);
  }
  const edgeIndex = mutable.nextEdgeIndex;
  const edgeData = new Edge({
    source,
    target,
    data
  });
  mutable.edges.set(edgeIndex, edgeData);
  const sourceAdjacency = mutable.adjacency.get(source);
  if (sourceAdjacency !== void 0) {
    sourceAdjacency.push(edgeIndex);
  }
  const targetReverseAdjacency = mutable.reverseAdjacency.get(target);
  if (targetReverseAdjacency !== void 0) {
    targetReverseAdjacency.push(edgeIndex);
  }
  if (mutable.type === "undirected") {
    const targetAdjacency = mutable.adjacency.get(target);
    if (targetAdjacency !== void 0) {
      targetAdjacency.push(edgeIndex);
    }
    const sourceReverseAdjacency = mutable.reverseAdjacency.get(source);
    if (sourceReverseAdjacency !== void 0) {
      sourceReverseAdjacency.push(edgeIndex);
    }
  }
  mutable.nextEdgeIndex = mutable.nextEdgeIndex + 1;
  invalidateCycleFlagOnAddition(mutable);
  return edgeIndex;
};
var removeNode = (mutable, nodeIndex) => {
  if (!mutable.nodes.has(nodeIndex)) {
    return;
  }
  const edgesToRemove = [];
  const outgoingEdges = mutable.adjacency.get(nodeIndex);
  if (outgoingEdges !== void 0) {
    for (const edge of outgoingEdges) {
      edgesToRemove.push(edge);
    }
  }
  const incomingEdges = mutable.reverseAdjacency.get(nodeIndex);
  if (incomingEdges !== void 0) {
    for (const edge of incomingEdges) {
      edgesToRemove.push(edge);
    }
  }
  for (const edgeIndex of edgesToRemove) {
    removeEdgeInternal(mutable, edgeIndex);
  }
  mutable.nodes.delete(nodeIndex);
  mutable.adjacency.delete(nodeIndex);
  mutable.reverseAdjacency.delete(nodeIndex);
  invalidateCycleFlagOnRemoval(mutable);
};
var removeEdge = (mutable, edgeIndex) => {
  const wasRemoved = removeEdgeInternal(mutable, edgeIndex);
  if (wasRemoved) {
    invalidateCycleFlagOnRemoval(mutable);
  }
};
var removeEdgeInternal = (mutable, edgeIndex) => {
  const edge = mutable.edges.get(edgeIndex);
  if (edge === void 0) {
    return false;
  }
  const {
    source,
    target
  } = edge;
  const sourceAdjacency = mutable.adjacency.get(source);
  if (sourceAdjacency !== void 0) {
    const index = sourceAdjacency.indexOf(edgeIndex);
    if (index !== -1) {
      sourceAdjacency.splice(index, 1);
    }
  }
  const targetReverseAdjacency = mutable.reverseAdjacency.get(target);
  if (targetReverseAdjacency !== void 0) {
    const index = targetReverseAdjacency.indexOf(edgeIndex);
    if (index !== -1) {
      targetReverseAdjacency.splice(index, 1);
    }
  }
  if (mutable.type === "undirected") {
    const targetAdjacency = mutable.adjacency.get(target);
    if (targetAdjacency !== void 0) {
      const index = targetAdjacency.indexOf(edgeIndex);
      if (index !== -1) {
        targetAdjacency.splice(index, 1);
      }
    }
    const sourceReverseAdjacency = mutable.reverseAdjacency.get(source);
    if (sourceReverseAdjacency !== void 0) {
      const index = sourceReverseAdjacency.indexOf(edgeIndex);
      if (index !== -1) {
        sourceReverseAdjacency.splice(index, 1);
      }
    }
  }
  mutable.edges.delete(edgeIndex);
  return true;
};
var getEdge = (graph, edgeIndex) => graph.edges.has(edgeIndex) ? some(graph.edges.get(edgeIndex)) : none();
var hasEdge = (graph, source, target) => {
  const adjacencyList = graph.adjacency.get(source);
  if (adjacencyList === void 0) {
    return false;
  }
  for (const edgeIndex of adjacencyList) {
    const edge = graph.edges.get(edgeIndex);
    if (edge !== void 0 && edge.target === target) {
      return true;
    }
  }
  return false;
};
var edgeCount = (graph) => graph.edges.size;
var neighbors = (graph, nodeIndex) => {
  if (graph.type === "undirected") {
    return getUndirectedNeighbors(graph, nodeIndex);
  }
  const adjacencyList = graph.adjacency.get(nodeIndex);
  if (adjacencyList === void 0) {
    return [];
  }
  const result = [];
  for (const edgeIndex of adjacencyList) {
    const edge = graph.edges.get(edgeIndex);
    if (edge !== void 0) {
      result.push(edge.target);
    }
  }
  return result;
};
var neighborsDirected = (graph, nodeIndex, direction) => {
  const adjacencyMap = direction === "incoming" ? graph.reverseAdjacency : graph.adjacency;
  const adjacencyList = adjacencyMap.get(nodeIndex);
  if (adjacencyList === void 0) {
    return [];
  }
  const result = [];
  for (const edgeIndex of adjacencyList) {
    const edge = graph.edges.get(edgeIndex);
    if (edge !== void 0) {
      const neighborNode = direction === "incoming" ? edge.source : edge.target;
      result.push(neighborNode);
    }
  }
  return result;
};
var toGraphViz = (graph, options) => {
  const {
    edgeLabel = (data) => String(data),
    graphName = "G",
    nodeLabel = (data) => String(data)
  } = options ?? {};
  const isDirected = graph.type === "directed";
  const graphType = isDirected ? "digraph" : "graph";
  const edgeOperator = isDirected ? "->" : "--";
  const lines = [];
  lines.push(`${graphType} ${graphName} {`);
  for (const [nodeIndex, nodeData] of graph.nodes) {
    const label = nodeLabel(nodeData).replace(/"/g, '\\"');
    lines.push(`  "${nodeIndex}" [label="${label}"];`);
  }
  for (const [, edgeData] of graph.edges) {
    const label = edgeLabel(edgeData.data).replace(/"/g, '\\"');
    lines.push(`  "${edgeData.source}" ${edgeOperator} "${edgeData.target}" [label="${label}"];`);
  }
  lines.push("}");
  return lines.join("\n");
};
var escapeMermaidLabel = (label) => {
  return label.replace(/#/g, "#35;").replace(/"/g, "#quot;").replace(/</g, "#lt;").replace(/>/g, "#gt;").replace(/&/g, "#amp;").replace(/\[/g, "#91;").replace(/\]/g, "#93;").replace(/\{/g, "#123;").replace(/\}/g, "#125;").replace(/\(/g, "#40;").replace(/\)/g, "#41;").replace(/\|/g, "#124;").replace(/\\/g, "#92;").replace(/\n/g, "<br/>");
};
var formatMermaidNode = (nodeId, label, shape) => {
  switch (shape) {
    case "rectangle":
      return `${nodeId}["${label}"]`;
    case "rounded":
      return `${nodeId}("${label}")`;
    case "circle":
      return `${nodeId}(("${label}"))`;
    case "diamond":
      return `${nodeId}{"${label}"}`;
    case "hexagon":
      return `${nodeId}{{"${label}"}}`;
    case "stadium":
      return `${nodeId}(["${label}"])`;
    case "subroutine":
      return `${nodeId}[["${label}"]]`;
    case "cylindrical":
      return `${nodeId}[("${label}")]`;
  }
};
var toMermaid = (graph, options) => {
  const {
    diagramType,
    direction = "TD",
    edgeLabel = (data) => String(data),
    nodeLabel = (data) => String(data),
    nodeShape = () => "rectangle"
  } = options ?? {};
  const finalDiagramType = diagramType ?? (graph.type === "directed" ? "flowchart" : "graph");
  const lines = [];
  lines.push(`${finalDiagramType} ${direction}`);
  for (const [nodeIndex, nodeData] of graph.nodes) {
    const nodeId = String(nodeIndex);
    const label = escapeMermaidLabel(nodeLabel(nodeData));
    const shape = nodeShape(nodeData);
    const formattedNode = formatMermaidNode(nodeId, label, shape);
    lines.push(`  ${formattedNode}`);
  }
  const edgeOperator = finalDiagramType === "flowchart" ? "-->" : "---";
  for (const [, edgeData] of graph.edges) {
    const sourceId = String(edgeData.source);
    const targetId = String(edgeData.target);
    const label = escapeMermaidLabel(edgeLabel(edgeData.data));
    if (label) {
      lines.push(`  ${sourceId} ${edgeOperator}|"${label}"| ${targetId}`);
    } else {
      lines.push(`  ${sourceId} ${edgeOperator} ${targetId}`);
    }
  }
  return lines.join("\n");
};
var isAcyclic = (graph) => {
  if (isSome(graph.isAcyclic)) {
    return graph.isAcyclic.value;
  }
  const visited = /* @__PURE__ */ new Set();
  const recursionStack = /* @__PURE__ */ new Set();
  for (const startNode of graph.nodes.keys()) {
    if (visited.has(startNode)) {
      continue;
    }
    const stack = [[startNode, [], 0, true]];
    while (stack.length > 0) {
      const [node, neighbors2, neighborIndex, isFirstVisit] = stack[stack.length - 1];
      if (isFirstVisit) {
        if (recursionStack.has(node)) {
          graph.isAcyclic = some(false);
          return false;
        }
        if (visited.has(node)) {
          stack.pop();
          continue;
        }
        visited.add(node);
        recursionStack.add(node);
        const nodeNeighbors = Array.from(neighborsDirected(graph, node, "outgoing"));
        stack[stack.length - 1] = [node, nodeNeighbors, 0, false];
        continue;
      }
      if (neighborIndex < neighbors2.length) {
        const neighbor = neighbors2[neighborIndex];
        stack[stack.length - 1] = [node, neighbors2, neighborIndex + 1, false];
        if (recursionStack.has(neighbor)) {
          graph.isAcyclic = some(false);
          return false;
        }
        if (!visited.has(neighbor)) {
          stack.push([neighbor, [], 0, true]);
        }
      } else {
        recursionStack.delete(node);
        stack.pop();
      }
    }
  }
  graph.isAcyclic = some(true);
  return true;
};
var isBipartite = (graph) => {
  const coloring = /* @__PURE__ */ new Map();
  const discovered = /* @__PURE__ */ new Set();
  let isBipartiteGraph = true;
  for (const startNode of graph.nodes.keys()) {
    if (!discovered.has(startNode)) {
      const queue = [startNode];
      coloring.set(startNode, 0);
      discovered.add(startNode);
      while (queue.length > 0 && isBipartiteGraph) {
        const current = queue.shift();
        const currentColor = coloring.get(current);
        const neighborColor = currentColor === 0 ? 1 : 0;
        const nodeNeighbors = getUndirectedNeighbors(graph, current);
        for (const neighbor of nodeNeighbors) {
          if (!discovered.has(neighbor)) {
            coloring.set(neighbor, neighborColor);
            discovered.add(neighbor);
            queue.push(neighbor);
          } else {
            if (coloring.get(neighbor) === currentColor) {
              isBipartiteGraph = false;
              break;
            }
          }
        }
      }
      if (!isBipartiteGraph) {
        break;
      }
    }
  }
  return isBipartiteGraph;
};
var getUndirectedNeighbors = (graph, nodeIndex) => {
  const neighbors2 = /* @__PURE__ */ new Set();
  const adjacencyList = graph.adjacency.get(nodeIndex);
  if (adjacencyList !== void 0) {
    for (const edgeIndex of adjacencyList) {
      const edge = graph.edges.get(edgeIndex);
      if (edge !== void 0) {
        const otherNode = edge.source === nodeIndex ? edge.target : edge.source;
        neighbors2.add(otherNode);
      }
    }
  }
  return Array.from(neighbors2);
};
var connectedComponents = (graph) => {
  const visited = /* @__PURE__ */ new Set();
  const components = [];
  for (const startNode of graph.nodes.keys()) {
    if (!visited.has(startNode)) {
      const component = [];
      const stack = [startNode];
      while (stack.length > 0) {
        const current = stack.pop();
        if (!visited.has(current)) {
          visited.add(current);
          component.push(current);
          const nodeNeighbors = getUndirectedNeighbors(graph, current);
          for (const neighbor of nodeNeighbors) {
            if (!visited.has(neighbor)) {
              stack.push(neighbor);
            }
          }
        }
      }
      components.push(component);
    }
  }
  return components;
};
var stronglyConnectedComponents = (graph) => {
  const visited = /* @__PURE__ */ new Set();
  const finishOrder = [];
  for (const startNode of graph.nodes.keys()) {
    if (visited.has(startNode)) {
      continue;
    }
    const stack = [[startNode, [], 0, true]];
    while (stack.length > 0) {
      const [node, nodeNeighbors, neighborIndex, isFirstVisit] = stack[stack.length - 1];
      if (isFirstVisit) {
        if (visited.has(node)) {
          stack.pop();
          continue;
        }
        visited.add(node);
        const nodeNeighborsList = neighbors(graph, node);
        stack[stack.length - 1] = [node, nodeNeighborsList, 0, false];
        continue;
      }
      if (neighborIndex < nodeNeighbors.length) {
        const neighbor = nodeNeighbors[neighborIndex];
        stack[stack.length - 1] = [node, nodeNeighbors, neighborIndex + 1, false];
        if (!visited.has(neighbor)) {
          stack.push([neighbor, [], 0, true]);
        }
      } else {
        finishOrder.push(node);
        stack.pop();
      }
    }
  }
  visited.clear();
  const sccs = [];
  for (let i = finishOrder.length - 1; i >= 0; i--) {
    const startNode = finishOrder[i];
    if (visited.has(startNode)) {
      continue;
    }
    const scc = [];
    const stack = [startNode];
    while (stack.length > 0) {
      const node = stack.pop();
      if (visited.has(node)) {
        continue;
      }
      visited.add(node);
      scc.push(node);
      const reverseAdjacency = graph.reverseAdjacency.get(node);
      if (reverseAdjacency !== void 0) {
        for (const edgeIndex of reverseAdjacency) {
          const edge = graph.edges.get(edgeIndex);
          if (edge !== void 0) {
            const predecessor = edge.source;
            if (!visited.has(predecessor)) {
              stack.push(predecessor);
            }
          }
        }
      }
    }
    sccs.push(scc);
  }
  return sccs;
};
var dijkstra = (graph, config) => {
  const {
    cost,
    source,
    target
  } = config;
  if (!graph.nodes.has(source)) {
    throw missingNode(source);
  }
  if (!graph.nodes.has(target)) {
    throw missingNode(target);
  }
  if (source === target) {
    return some({
      path: [source],
      distance: 0,
      costs: []
    });
  }
  const distances = /* @__PURE__ */ new Map();
  const previous = /* @__PURE__ */ new Map();
  const visited = /* @__PURE__ */ new Set();
  for (const node of graph.nodes.keys()) {
    distances.set(node, node === source ? 0 : Infinity);
    previous.set(node, null);
  }
  const priorityQueue = [{
    node: source,
    distance: 0
  }];
  while (priorityQueue.length > 0) {
    let minIndex = 0;
    for (let i = 1; i < priorityQueue.length; i++) {
      if (priorityQueue[i].distance < priorityQueue[minIndex].distance) {
        minIndex = i;
      }
    }
    const current = priorityQueue.splice(minIndex, 1)[0];
    const currentNode2 = current.node;
    if (visited.has(currentNode2)) {
      continue;
    }
    visited.add(currentNode2);
    if (currentNode2 === target) {
      break;
    }
    const currentDistance = distances.get(currentNode2);
    const adjacencyList = graph.adjacency.get(currentNode2);
    if (adjacencyList !== void 0) {
      for (const edgeIndex of adjacencyList) {
        const edge = graph.edges.get(edgeIndex);
        if (edge !== void 0) {
          const neighbor = edge.target;
          const weight = cost(edge.data);
          if (weight < 0) {
            throw new Error(`Dijkstra's algorithm requires non-negative edge weights, found ${weight}`);
          }
          const newDistance = currentDistance + weight;
          const neighborDistance = distances.get(neighbor);
          if (newDistance < neighborDistance) {
            distances.set(neighbor, newDistance);
            previous.set(neighbor, {
              node: currentNode2,
              edgeData: edge.data
            });
            if (!visited.has(neighbor)) {
              priorityQueue.push({
                node: neighbor,
                distance: newDistance
              });
            }
          }
        }
      }
    }
  }
  const targetDistance = distances.get(target);
  if (targetDistance === Infinity) {
    return none();
  }
  const path = [];
  const costs = [];
  let currentNode = target;
  while (currentNode !== null) {
    path.unshift(currentNode);
    const prev = previous.get(currentNode);
    if (prev !== null) {
      costs.unshift(prev.edgeData);
      currentNode = prev.node;
    } else {
      currentNode = null;
    }
  }
  return some({
    path,
    distance: targetDistance,
    costs
  });
};
var floydWarshall = (graph, cost) => {
  const allNodes = Array.from(graph.nodes.keys());
  const dist = /* @__PURE__ */ new Map();
  const next3 = /* @__PURE__ */ new Map();
  const edgeMatrix = /* @__PURE__ */ new Map();
  for (const i of allNodes) {
    dist.set(i, /* @__PURE__ */ new Map());
    next3.set(i, /* @__PURE__ */ new Map());
    edgeMatrix.set(i, /* @__PURE__ */ new Map());
    for (const j of allNodes) {
      dist.get(i).set(j, i === j ? 0 : Infinity);
      next3.get(i).set(j, null);
      edgeMatrix.get(i).set(j, null);
    }
  }
  for (const [, edgeData] of graph.edges) {
    const weight = cost(edgeData.data);
    const i = edgeData.source;
    const j = edgeData.target;
    const currentWeight = dist.get(i).get(j);
    if (weight < currentWeight) {
      dist.get(i).set(j, weight);
      next3.get(i).set(j, j);
      edgeMatrix.get(i).set(j, edgeData.data);
    }
  }
  for (const k of allNodes) {
    for (const i of allNodes) {
      for (const j of allNodes) {
        const distIK = dist.get(i).get(k);
        const distKJ = dist.get(k).get(j);
        const distIJ = dist.get(i).get(j);
        if (distIK !== Infinity && distKJ !== Infinity && distIK + distKJ < distIJ) {
          dist.get(i).set(j, distIK + distKJ);
          next3.get(i).set(j, next3.get(i).get(k));
        }
      }
    }
  }
  for (const i of allNodes) {
    if (dist.get(i).get(i) < 0) {
      throw new Error(`Negative cycle detected involving node ${i}`);
    }
  }
  const paths = /* @__PURE__ */ new Map();
  const resultCosts = /* @__PURE__ */ new Map();
  for (const i of allNodes) {
    paths.set(i, /* @__PURE__ */ new Map());
    resultCosts.set(i, /* @__PURE__ */ new Map());
    for (const j of allNodes) {
      if (i === j) {
        paths.get(i).set(j, [i]);
        resultCosts.get(i).set(j, []);
      } else if (dist.get(i).get(j) === Infinity) {
        paths.get(i).set(j, null);
        resultCosts.get(i).set(j, []);
      } else {
        const path = [];
        const weights = [];
        let current = i;
        path.push(current);
        while (current !== j) {
          const nextNode = next3.get(current).get(j);
          if (nextNode === null) break;
          const edgeData = edgeMatrix.get(current).get(nextNode);
          if (edgeData !== null) {
            weights.push(edgeData);
          }
          current = nextNode;
          path.push(current);
        }
        paths.get(i).set(j, path);
        resultCosts.get(i).set(j, weights);
      }
    }
  }
  return {
    distances: dist,
    paths,
    costs: resultCosts
  };
};
var astar = (graph, config) => {
  const {
    cost,
    heuristic,
    source,
    target
  } = config;
  if (!graph.nodes.has(source)) {
    throw missingNode(source);
  }
  if (!graph.nodes.has(target)) {
    throw missingNode(target);
  }
  if (source === target) {
    return some({
      path: [source],
      distance: 0,
      costs: []
    });
  }
  const targetNodeData = graph.nodes.get(target);
  if (targetNodeData === void 0) {
    throw new Error(`Target node ${target} data not found`);
  }
  const gScore = /* @__PURE__ */ new Map();
  const fScore = /* @__PURE__ */ new Map();
  const previous = /* @__PURE__ */ new Map();
  const visited = /* @__PURE__ */ new Set();
  for (const node of graph.nodes.keys()) {
    gScore.set(node, node === source ? 0 : Infinity);
    fScore.set(node, Infinity);
    previous.set(node, null);
  }
  const sourceNodeData = graph.nodes.get(source);
  if (sourceNodeData !== void 0) {
    const h = heuristic(sourceNodeData, targetNodeData);
    fScore.set(source, h);
  }
  const openSet = [{
    node: source,
    fScore: fScore.get(source)
  }];
  while (openSet.length > 0) {
    let minIndex = 0;
    for (let i = 1; i < openSet.length; i++) {
      if (openSet[i].fScore < openSet[minIndex].fScore) {
        minIndex = i;
      }
    }
    const current = openSet.splice(minIndex, 1)[0];
    const currentNode2 = current.node;
    if (visited.has(currentNode2)) {
      continue;
    }
    visited.add(currentNode2);
    if (currentNode2 === target) {
      break;
    }
    const currentGScore = gScore.get(currentNode2);
    const adjacencyList = graph.adjacency.get(currentNode2);
    if (adjacencyList !== void 0) {
      for (const edgeIndex of adjacencyList) {
        const edge = graph.edges.get(edgeIndex);
        if (edge !== void 0) {
          const neighbor = edge.target;
          const weight = cost(edge.data);
          if (weight < 0) {
            throw new Error(`A* algorithm requires non-negative edge weights, found ${weight}`);
          }
          const tentativeGScore = currentGScore + weight;
          const neighborGScore = gScore.get(neighbor);
          if (tentativeGScore < neighborGScore) {
            gScore.set(neighbor, tentativeGScore);
            previous.set(neighbor, {
              node: currentNode2,
              edgeData: edge.data
            });
            const neighborNodeData = graph.nodes.get(neighbor);
            if (neighborNodeData !== void 0) {
              const h = heuristic(neighborNodeData, targetNodeData);
              const f = tentativeGScore + h;
              fScore.set(neighbor, f);
              if (!visited.has(neighbor)) {
                openSet.push({
                  node: neighbor,
                  fScore: f
                });
              }
            }
          }
        }
      }
    }
  }
  const targetGScore = gScore.get(target);
  if (targetGScore === Infinity) {
    return none();
  }
  const path = [];
  const costs = [];
  let currentNode = target;
  while (currentNode !== null) {
    path.unshift(currentNode);
    const prev = previous.get(currentNode);
    if (prev !== null) {
      costs.unshift(prev.edgeData);
      currentNode = prev.node;
    } else {
      currentNode = null;
    }
  }
  return some({
    path,
    distance: targetGScore,
    costs
  });
};
var bellmanFord = (graph, config) => {
  const {
    cost,
    source,
    target
  } = config;
  if (!graph.nodes.has(source)) {
    throw missingNode(source);
  }
  if (!graph.nodes.has(target)) {
    throw missingNode(target);
  }
  if (source === target) {
    return some({
      path: [source],
      distance: 0,
      costs: []
    });
  }
  const distances = /* @__PURE__ */ new Map();
  const previous = /* @__PURE__ */ new Map();
  for (const node of graph.nodes.keys()) {
    distances.set(node, node === source ? 0 : Infinity);
    previous.set(node, null);
  }
  const edges2 = [];
  for (const [, edgeData] of graph.edges) {
    const weight = cost(edgeData.data);
    edges2.push({
      source: edgeData.source,
      target: edgeData.target,
      weight,
      edgeData: edgeData.data
    });
  }
  const nodeCount2 = graph.nodes.size;
  for (let i = 0; i < nodeCount2 - 1; i++) {
    let hasUpdate = false;
    for (const edge of edges2) {
      const sourceDistance = distances.get(edge.source);
      const targetDistance2 = distances.get(edge.target);
      if (sourceDistance !== Infinity && sourceDistance + edge.weight < targetDistance2) {
        distances.set(edge.target, sourceDistance + edge.weight);
        previous.set(edge.target, {
          node: edge.source,
          edgeData: edge.edgeData
        });
        hasUpdate = true;
      }
    }
    if (!hasUpdate) {
      break;
    }
  }
  for (const edge of edges2) {
    const sourceDistance = distances.get(edge.source);
    const targetDistance2 = distances.get(edge.target);
    if (sourceDistance !== Infinity && sourceDistance + edge.weight < targetDistance2) {
      const affectedNodes = /* @__PURE__ */ new Set();
      const queue = [edge.target];
      while (queue.length > 0) {
        const node = queue.shift();
        if (affectedNodes.has(node)) continue;
        affectedNodes.add(node);
        const adjacencyList = graph.adjacency.get(node);
        if (adjacencyList !== void 0) {
          for (const edgeIndex of adjacencyList) {
            const edge2 = graph.edges.get(edgeIndex);
            if (edge2 !== void 0) {
              queue.push(edge2.target);
            }
          }
        }
      }
      if (affectedNodes.has(target)) {
        return none();
      }
    }
  }
  const targetDistance = distances.get(target);
  if (targetDistance === Infinity) {
    return none();
  }
  const path = [];
  const costs = [];
  let currentNode = target;
  while (currentNode !== null) {
    path.unshift(currentNode);
    const prev = previous.get(currentNode);
    if (prev !== null) {
      costs.unshift(prev.edgeData);
      currentNode = prev.node;
    } else {
      currentNode = null;
    }
  }
  return some({
    path,
    distance: targetDistance,
    costs
  });
};
var Walker = class {
  /**
   * @since 3.18.0
   */
  // @ts-ignore
  [Symbol.iterator];
  /**
   * Visits each element and maps it to a value using the provided function.
   *
   * Takes a function that receives the index and data,
   * and returns an iterable of the mapped values. Skips elements that
   * no longer exist in the graph.
   *
   * @example
   * ```ts
   * import { Graph } from "effect"
   *
   * const graph = Graph.directed<string, number>((mutable) => {
   *   const a = Graph.addNode(mutable, "A")
   *   const b = Graph.addNode(mutable, "B")
   *   Graph.addEdge(mutable, a, b, 1)
   * })
   *
   * const dfs = Graph.dfs(graph, { start: [0] })
   *
   * // Map to just the node data
   * const values = Array.from(dfs.visit((index, data) => data))
   * console.log(values) // ["A", "B"]
   *
   * // Map to custom objects
   * const custom = Array.from(dfs.visit((index, data) => ({ id: index, name: data })))
   * console.log(custom) // [{ id: 0, name: "A" }, { id: 1, name: "B" }]
   * ```
   *
   * @since 3.18.0
   * @category iterators
   */
  visit;
  constructor(visit) {
    this.visit = visit;
    this[Symbol.iterator] = visit((index, data) => [index, data])[Symbol.iterator];
  }
};
var indices = (walker) => walker.visit((index, _) => index);
var values2 = (walker) => walker.visit((_, data) => data);
var entries = (walker) => walker.visit((index, data) => [index, data]);
var dfs = (graph, config = {}) => {
  const start3 = config.start ?? [];
  const direction = config.direction ?? "outgoing";
  for (const nodeIndex of start3) {
    if (!hasNode(graph, nodeIndex)) {
      throw missingNode(nodeIndex);
    }
  }
  return new Walker((f) => ({
    [Symbol.iterator]: () => {
      const stack = [...start3];
      const discovered = /* @__PURE__ */ new Set();
      const nextMapped = () => {
        while (stack.length > 0) {
          const current = stack.pop();
          if (discovered.has(current)) {
            continue;
          }
          discovered.add(current);
          const nodeDataOption = graph.nodes.get(current);
          if (nodeDataOption === void 0) {
            continue;
          }
          const neighbors2 = neighborsDirected(graph, current, direction);
          for (let i = neighbors2.length - 1; i >= 0; i--) {
            const neighbor = neighbors2[i];
            if (!discovered.has(neighbor)) {
              stack.push(neighbor);
            }
          }
          return {
            done: false,
            value: f(current, nodeDataOption)
          };
        }
        return {
          done: true,
          value: void 0
        };
      };
      return {
        next: nextMapped
      };
    }
  }));
};
var bfs = (graph, config = {}) => {
  const start3 = config.start ?? [];
  const direction = config.direction ?? "outgoing";
  for (const nodeIndex of start3) {
    if (!hasNode(graph, nodeIndex)) {
      throw missingNode(nodeIndex);
    }
  }
  return new Walker((f) => ({
    [Symbol.iterator]: () => {
      const queue = [...start3];
      const discovered = /* @__PURE__ */ new Set();
      const nextMapped = () => {
        while (queue.length > 0) {
          const current = queue.shift();
          if (!discovered.has(current)) {
            discovered.add(current);
            const neighbors2 = neighborsDirected(graph, current, direction);
            for (const neighbor of neighbors2) {
              if (!discovered.has(neighbor)) {
                queue.push(neighbor);
              }
            }
            const nodeData = getNode(graph, current);
            if (isSome(nodeData)) {
              return {
                done: false,
                value: f(current, nodeData.value)
              };
            }
            return nextMapped();
          }
        }
        return {
          done: true,
          value: void 0
        };
      };
      return {
        next: nextMapped
      };
    }
  }));
};
var topo = (graph, config = {}) => {
  if (!isAcyclic(graph)) {
    throw new Error("Cannot perform topological sort on cyclic graph");
  }
  const initials = config.initials ?? [];
  for (const nodeIndex of initials) {
    if (!hasNode(graph, nodeIndex)) {
      throw missingNode(nodeIndex);
    }
  }
  return new Walker((f) => ({
    [Symbol.iterator]: () => {
      const inDegree = /* @__PURE__ */ new Map();
      const remaining = /* @__PURE__ */ new Set();
      const queue = [...initials];
      for (const [nodeIndex] of graph.nodes) {
        inDegree.set(nodeIndex, 0);
        remaining.add(nodeIndex);
      }
      for (const [, edgeData] of graph.edges) {
        const currentInDegree = inDegree.get(edgeData.target) || 0;
        inDegree.set(edgeData.target, currentInDegree + 1);
      }
      if (initials.length === 0) {
        for (const [nodeIndex, degree] of inDegree) {
          if (degree === 0) {
            queue.push(nodeIndex);
          }
        }
      }
      const nextMapped = () => {
        while (queue.length > 0) {
          const current = queue.shift();
          if (remaining.has(current)) {
            remaining.delete(current);
            const neighbors2 = neighborsDirected(graph, current, "outgoing");
            for (const neighbor of neighbors2) {
              if (remaining.has(neighbor)) {
                const currentInDegree = inDegree.get(neighbor) || 0;
                const newInDegree = currentInDegree - 1;
                inDegree.set(neighbor, newInDegree);
                if (newInDegree === 0) {
                  queue.push(neighbor);
                }
              }
            }
            const nodeData = getNode(graph, current);
            if (isSome(nodeData)) {
              return {
                done: false,
                value: f(current, nodeData.value)
              };
            }
            return nextMapped();
          }
        }
        return {
          done: true,
          value: void 0
        };
      };
      return {
        next: nextMapped
      };
    }
  }));
};
var dfsPostOrder = (graph, config = {}) => {
  const start3 = config.start ?? [];
  const direction = config.direction ?? "outgoing";
  for (const nodeIndex of start3) {
    if (!hasNode(graph, nodeIndex)) {
      throw missingNode(nodeIndex);
    }
  }
  return new Walker((f) => ({
    [Symbol.iterator]: () => {
      const stack = [];
      const discovered = /* @__PURE__ */ new Set();
      const finished = /* @__PURE__ */ new Set();
      for (let i = start3.length - 1; i >= 0; i--) {
        stack.push({
          node: start3[i],
          visitedChildren: false
        });
      }
      const nextMapped = () => {
        while (stack.length > 0) {
          const current = stack[stack.length - 1];
          if (!discovered.has(current.node)) {
            discovered.add(current.node);
            current.visitedChildren = false;
          }
          if (!current.visitedChildren) {
            current.visitedChildren = true;
            const neighbors2 = neighborsDirected(graph, current.node, direction);
            for (let i = neighbors2.length - 1; i >= 0; i--) {
              const neighbor = neighbors2[i];
              if (!discovered.has(neighbor) && !finished.has(neighbor)) {
                stack.push({
                  node: neighbor,
                  visitedChildren: false
                });
              }
            }
          } else {
            const nodeToEmit = stack.pop().node;
            if (!finished.has(nodeToEmit)) {
              finished.add(nodeToEmit);
              const nodeData = getNode(graph, nodeToEmit);
              if (isSome(nodeData)) {
                return {
                  done: false,
                  value: f(nodeToEmit, nodeData.value)
                };
              }
              return nextMapped();
            }
          }
        }
        return {
          done: true,
          value: void 0
        };
      };
      return {
        next: nextMapped
      };
    }
  }));
};
var nodes = (graph) => new Walker((f) => ({
  [Symbol.iterator]() {
    const nodeMap = graph.nodes;
    const iterator = nodeMap.entries();
    return {
      next() {
        const result = iterator.next();
        if (result.done) {
          return {
            done: true,
            value: void 0
          };
        }
        const [nodeIndex, nodeData] = result.value;
        return {
          done: false,
          value: f(nodeIndex, nodeData)
        };
      }
    };
  }
}));
var edges = (graph) => new Walker((f) => ({
  [Symbol.iterator]() {
    const edgeMap = graph.edges;
    const iterator = edgeMap.entries();
    return {
      next() {
        const result = iterator.next();
        if (result.done) {
          return {
            done: true,
            value: void 0
          };
        }
        const [edgeIndex, edgeData] = result.value;
        return {
          done: false,
          value: f(edgeIndex, edgeData)
        };
      }
    };
  }
}));
var externals = (graph, config = {}) => {
  const direction = config.direction ?? "outgoing";
  return new Walker((f) => ({
    [Symbol.iterator]: () => {
      const nodeMap = graph.nodes;
      const adjacencyMap = direction === "incoming" ? graph.reverseAdjacency : graph.adjacency;
      const nodeIterator = nodeMap.entries();
      const nextMapped = () => {
        let current = nodeIterator.next();
        while (!current.done) {
          const [nodeIndex, nodeData] = current.value;
          const adjacencyList = adjacencyMap.get(nodeIndex);
          if (adjacencyList === void 0 || adjacencyList.length === 0) {
            return {
              done: false,
              value: f(nodeIndex, nodeData)
            };
          }
          current = nodeIterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      };
      return {
        next: nextMapped
      };
    }
  }));
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/GroupBy.js
var GroupBy_exports = {};
__export(GroupBy_exports, {
  GroupByTypeId: () => GroupByTypeId2,
  evaluate: () => evaluate2,
  filter: () => filter6,
  first: () => first3,
  make: () => make30
});
var GroupByTypeId2 = GroupByTypeId;
var evaluate2 = evaluate;
var filter6 = filter5;
var first3 = first2;
var make30 = make23;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HKT.js
var HKT_exports = {};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HashRing.js
var HashRing_exports = {};
__export(HashRing_exports, {
  add: () => add3,
  addMany: () => addMany,
  get: () => get13,
  getShards: () => getShards,
  has: () => has7,
  isHashRing: () => isHashRing,
  make: () => make31,
  remove: () => remove4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/PrimaryKey.js
var PrimaryKey_exports = {};
__export(PrimaryKey_exports, {
  symbol: () => symbol4,
  value: () => value3
});
var symbol4 = /* @__PURE__ */ Symbol.for("effect/PrimaryKey");
var value3 = (self) => self[symbol4]();

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HashRing.js
var TypeId10 = "~effect/cluster/HashRing";
var isHashRing = (u) => hasProperty(u, TypeId10);
var make31 = (options) => {
  const self = Object.create(Proto4);
  self.baseWeight = Math.max(options?.baseWeight ?? 128, 1);
  self.totalWeightCache = 0;
  self.nodes = /* @__PURE__ */ new Map();
  self.ring = [];
  return self;
};
var Proto4 = {
  [TypeId10]: TypeId10,
  [Symbol.iterator]() {
    return map2(this.nodes.values(), ([n]) => n)[Symbol.iterator]();
  },
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...BaseProto,
  toJSON() {
    return {
      _id: "HashRing",
      baseWeight: this.baseWeight,
      nodes: this.ring.map(([, n]) => this.nodes.get(n)[0])
    };
  }
};
var addMany = dual((args) => isHashRing(args[0]), (self, nodes2, options) => {
  const weight = Math.max(options?.weight ?? 1, 0.1);
  const keys10 = [];
  let toRemove;
  for (const node of nodes2) {
    const key = value3(node);
    const entry = self.nodes.get(key);
    if (entry) {
      if (entry[1] === weight) continue;
      toRemove ??= /* @__PURE__ */ new Set();
      toRemove.add(key);
      self.totalWeightCache -= entry[1];
      self.totalWeightCache += weight;
      entry[1] = weight;
    } else {
      self.nodes.set(key, [node, weight]);
      self.totalWeightCache += weight;
    }
    keys10.push(key);
  }
  if (toRemove) {
    self.ring = self.ring.filter(([, n]) => !toRemove.has(n));
  }
  addNodesToRing(self, keys10, Math.round(weight * self.baseWeight));
  return self;
});
function addNodesToRing(self, keys10, weight) {
  for (let i = weight; i > 0; i--) {
    for (let j = 0; j < keys10.length; j++) {
      const key = keys10[j];
      self.ring.push([string(`${key}:${i}`), key]);
    }
  }
  self.ring.sort((a, b) => a[0] - b[0]);
}
var add3 = dual((args) => isHashRing(args[0]), (self, node, options) => addMany(self, [node], options));
var remove4 = dual(2, (self, node) => {
  const key = value3(node);
  const entry = self.nodes.get(key);
  if (entry) {
    self.nodes.delete(key);
    self.ring = self.ring.filter(([, n]) => n !== key);
    self.totalWeightCache -= entry[1];
  }
  return self;
});
var has7 = dual(2, (self, node) => self.nodes.has(value3(node)));
var get13 = (self, input) => {
  if (self.ring.length === 0) {
    return void 0;
  }
  const index = getIndexForInput(self, string(input))[0];
  const node = self.ring[index][1];
  return self.nodes.get(node)[0];
};
var getShards = (self, count7) => {
  if (self.ring.length === 0) {
    return void 0;
  }
  const shards = new Array(count7);
  const allocations = /* @__PURE__ */ new Map();
  const remaining = /* @__PURE__ */ new Set();
  const exclude = /* @__PURE__ */ new Set();
  const distances = new Array(count7);
  for (let shard = 0; shard < count7; shard++) {
    const hash3 = shardHashes[shard] ??= string(`shard-${shard}`);
    const [index, distance] = getIndexForInput(self, hash3);
    const node = self.ring[index][1];
    distances[shard] = [shard, node, distance];
    remaining.add(shard);
  }
  distances.sort((a, b) => a[2] - b[2]);
  for (let i = 0; i < count7; i++) {
    const [shard, node] = distances[i];
    if (exclude.has(node)) continue;
    const [value6, weight] = self.nodes.get(node);
    shards[shard] = value6;
    remaining.delete(shard);
    const nodeCount2 = (allocations.get(node) ?? 0) + 1;
    allocations.set(node, nodeCount2);
    const maxPerNode = Math.max(1, Math.floor(count7 * (weight / self.totalWeightCache)));
    if (nodeCount2 >= maxPerNode) {
      exclude.add(node);
    }
  }
  let allAtMax = exclude.size === self.nodes.size;
  remaining.forEach((shard) => {
    const index = getIndexForInput(self, shardHashes[shard], allAtMax ? void 0 : exclude)[0];
    const node = self.ring[index][1];
    const [value6, weight] = self.nodes.get(node);
    shards[shard] = value6;
    if (allAtMax) return;
    const nodeCount2 = (allocations.get(node) ?? 0) + 1;
    allocations.set(node, nodeCount2);
    const maxPerNode = Math.max(1, Math.floor(count7 * (weight / self.totalWeightCache)));
    if (nodeCount2 >= maxPerNode) {
      exclude.add(node);
      if (exclude.size === self.nodes.size) {
        allAtMax = true;
      }
    }
  });
  return shards;
};
var shardHashes = [];
function getIndexForInput(self, hash3, exclude) {
  const ring = self.ring;
  const len = ring.length;
  let mid;
  let lo = 0;
  let hi = len - 1;
  while (lo <= hi) {
    mid = (lo + hi) / 2 >>> 0;
    if (ring[mid][0] >= hash3) {
      hi = mid - 1;
    } else {
      lo = mid + 1;
    }
  }
  const a = lo === len ? lo - 1 : lo;
  const distA = Math.abs(ring[a][0] - hash3);
  if (exclude === void 0) {
    const b = lo - 1;
    if (b < 0) {
      return [a, distA];
    }
    const distB = Math.abs(ring[b][0] - hash3);
    return distA <= distB ? [a, distA] : [b, distB];
  } else if (!exclude.has(ring[a][1])) {
    return [a, distA];
  }
  const range = Math.max(lo, len - lo);
  for (let i = 1; i < range; i++) {
    let index = lo - i;
    if (index >= 0 && index < len && !exclude.has(ring[index][1])) {
      return [index, Math.abs(ring[index][0] - hash3)];
    }
    index = lo + i;
    if (index >= 0 && index < len && !exclude.has(ring[index][1])) {
      return [index, Math.abs(ring[index][0] - hash3)];
    }
  }
  return [a, distA];
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/JSONSchema.js
var JSONSchema_exports = {};
__export(JSONSchema_exports, {
  fromAST: () => fromAST,
  getMetaSchemaUri: () => getMetaSchemaUri,
  make: () => make32
});
var make32 = (schema, options) => {
  const definitions = {};
  const target = options?.target ?? "jsonSchema7";
  const ast = isTransformation(schema.ast) && isParseJsonTransformation(schema.ast.from) ? schema.ast.to : schema.ast;
  const jsonSchema = fromAST(ast, {
    definitions,
    target
  });
  const out = {
    $schema: getMetaSchemaUri(target),
    $defs: {},
    ...jsonSchema
  };
  if (isEmptyRecord(definitions)) {
    delete out.$defs;
  } else {
    out.$defs = definitions;
  }
  return out;
};
function getMetaSchemaUri(target) {
  switch (target) {
    case "jsonSchema7":
      return "http://json-schema.org/draft-07/schema#";
    case "jsonSchema2019-09":
      return "https://json-schema.org/draft/2019-09/schema";
    case "jsonSchema2020-12":
    case "openApi3.1":
      return "https://json-schema.org/draft/2020-12/schema";
  }
}
var fromAST = (ast, options) => {
  const definitionPath = options.definitionPath ?? "#/$defs/";
  const getRef = (id) => definitionPath + id;
  const target = options.target ?? "jsonSchema7";
  const topLevelReferenceStrategy = options.topLevelReferenceStrategy ?? "keep";
  const additionalPropertiesStrategy = options.additionalPropertiesStrategy ?? "strict";
  return go2(ast, options.definitions, "handle-identifier", [], {
    getRef,
    target,
    topLevelReferenceStrategy,
    additionalPropertiesStrategy
  }, "handle-annotation", "handle-errors");
};
var constNever = {
  $id: "/schemas/never",
  not: {}
};
var constAny = {
  $id: "/schemas/any"
};
var constUnknown = {
  $id: "/schemas/unknown"
};
var constVoid2 = {
  $id: "/schemas/void"
};
var constObject = {
  $id: "/schemas/object",
  "anyOf": [{
    "type": "object"
  }, {
    "type": "array"
  }]
};
var constEmptyStruct = {
  $id: "/schemas/%7B%7D",
  "anyOf": [{
    "type": "object"
  }, {
    "type": "array"
  }]
};
function getRawDescription(annotated) {
  if (annotated !== void 0) return getOrUndefined(getDescriptionAnnotation(annotated));
}
function getRawTitle(annotated) {
  if (annotated !== void 0) return getOrUndefined(getTitleAnnotation(annotated));
}
function getRawDefault(annotated) {
  if (annotated !== void 0) return getDefaultAnnotation(annotated);
  return none();
}
function encodeDefault(ast, def) {
  const getOption2 = getOption(ast, false);
  return getOption2(def);
}
function getRawExamples(annotated) {
  if (annotated !== void 0) return getOrUndefined(getExamplesAnnotation(annotated));
}
function encodeExamples(ast, examples) {
  const getOption2 = getOption(ast, false);
  const out = filterMap(examples, (e) => getOption2(e).pipe(filter(isJsonValue)));
  return out.length > 0 ? out : void 0;
}
function filterBuiltIn(ast, annotation, key) {
  if (annotation !== void 0) {
    switch (ast._tag) {
      case "StringKeyword":
        return annotation !== stringKeyword.annotations[key] ? annotation : void 0;
      case "NumberKeyword":
        return annotation !== numberKeyword.annotations[key] ? annotation : void 0;
      case "BooleanKeyword":
        return annotation !== booleanKeyword.annotations[key] ? annotation : void 0;
    }
  }
  return annotation;
}
function isJsonValue(value6, visited = /* @__PURE__ */ new Set()) {
  if (value6 === null || typeof value6 === "string" || typeof value6 === "number" || typeof value6 === "boolean") {
    return true;
  }
  if (Array.isArray(value6) || typeof value6 === "object") {
    if (visited.has(value6)) {
      return false;
    }
    visited.add(value6);
    try {
      if (Array.isArray(value6)) {
        return value6.every((item) => isJsonValue(item, visited));
      }
      const proto2 = Object.getPrototypeOf(value6);
      if (proto2 !== null && proto2 !== Object.prototype) {
        return false;
      }
      if (Object.getOwnPropertySymbols(value6).length > 0) {
        return false;
      }
      return Object.values(value6).every((v) => isJsonValue(v, visited));
    } finally {
      visited.delete(value6);
    }
  }
  return false;
}
function pruneJsonSchemaAnnotations(ast, description, title, def, examples) {
  const out = {};
  if (description !== void 0) out.description = description;
  if (title !== void 0) out.title = title;
  if (isSome(def)) {
    const o = encodeDefault(ast, def.value);
    if (isSome(o) && isJsonValue(o.value)) {
      out.default = o.value;
    }
  }
  if (examples !== void 0) {
    const encodedExamples = encodeExamples(ast, examples);
    if (encodedExamples !== void 0) {
      out.examples = encodedExamples;
    }
  }
  if (Object.keys(out).length === 0) {
    return void 0;
  }
  return out;
}
function getContextJsonSchemaAnnotations(ast, annotated) {
  return pruneJsonSchemaAnnotations(ast, getRawDescription(annotated), getRawTitle(annotated), getRawDefault(annotated), getRawExamples(annotated));
}
function getJsonSchemaAnnotations(ast) {
  return pruneJsonSchemaAnnotations(ast, filterBuiltIn(ast, getRawDescription(ast), DescriptionAnnotationId), filterBuiltIn(ast, getRawTitle(ast), TitleAnnotationId), getRawDefault(ast), getRawExamples(ast));
}
function mergeJsonSchemaAnnotations(jsonSchema, jsonSchemaAnnotations) {
  if (jsonSchemaAnnotations) {
    if ("$ref" in jsonSchema) {
      return {
        allOf: [jsonSchema],
        ...jsonSchemaAnnotations
      };
    }
    return {
      ...jsonSchema,
      ...jsonSchemaAnnotations
    };
  }
  return jsonSchema;
}
var pruneUndefined2 = (ast) => {
  if (isNone(getJSONSchemaAnnotation(ast))) {
    return pruneUndefined(ast, pruneUndefined2, (ast2) => pruneUndefined2(ast2.from));
  }
};
var isParseJsonTransformation = (ast) => ast.annotations[SchemaIdAnnotationId] === ParseJsonSchemaId;
var isOverrideAnnotation = (ast, jsonSchema) => {
  if (isRefinement(ast)) {
    const schemaId = ast.annotations[SchemaIdAnnotationId];
    if (schemaId === IntSchemaId) {
      return "type" in jsonSchema && jsonSchema.type !== "integer";
    }
  }
  return "type" in jsonSchema || "oneOf" in jsonSchema || "anyOf" in jsonSchema || "$ref" in jsonSchema;
};
var mergeRefinements = (from, jsonSchema, ast) => {
  const out = {
    ...from,
    ...getJsonSchemaAnnotations(ast),
    ...jsonSchema
  };
  out.allOf ??= [];
  const handle = (name, filter10) => {
    if (name in jsonSchema && name in from) {
      out.allOf.unshift({
        [name]: from[name]
      });
      out.allOf = out.allOf.filter(filter10);
    }
  };
  handle("minLength", (i) => i.minLength > jsonSchema.minLength);
  handle("maxLength", (i) => i.maxLength < jsonSchema.maxLength);
  handle("pattern", (i) => i.pattern !== jsonSchema.pattern);
  handle("minItems", (i) => i.minItems > jsonSchema.minItems);
  handle("maxItems", (i) => i.maxItems < jsonSchema.maxItems);
  handle("minimum", (i) => i.minimum > jsonSchema.minimum);
  handle("maximum", (i) => i.maximum < jsonSchema.maximum);
  handle("exclusiveMinimum", (i) => i.exclusiveMinimum > jsonSchema.exclusiveMinimum);
  handle("exclusiveMaximum", (i) => i.exclusiveMaximum < jsonSchema.exclusiveMaximum);
  handle("multipleOf", (i) => i.multipleOf !== jsonSchema.multipleOf);
  if (out.allOf.length === 0) {
    delete out.allOf;
  }
  return out;
};
function isContentSchemaSupported(options) {
  switch (options.target) {
    case "jsonSchema7":
      return false;
    case "jsonSchema2019-09":
    case "jsonSchema2020-12":
    case "openApi3.1":
      return true;
  }
}
function getAdditionalProperties(options) {
  switch (options.additionalPropertiesStrategy) {
    case "allow":
      return true;
    case "strict":
      return false;
  }
}
function addASTAnnotations(jsonSchema, ast) {
  return addAnnotations(jsonSchema, getJsonSchemaAnnotations(ast));
}
function addAnnotations(jsonSchema, annotations2) {
  if (annotations2 === void 0 || Object.keys(annotations2).length === 0) {
    return jsonSchema;
  }
  if ("$ref" in jsonSchema) {
    return {
      allOf: [jsonSchema],
      ...annotations2
    };
  }
  return {
    ...jsonSchema,
    ...annotations2
  };
}
function getIdentifierAnnotation(ast) {
  const identifier = getOrUndefined(getJSONIdentifier(ast));
  if (identifier === void 0) {
    if (isSuspend(ast)) {
      return getIdentifierAnnotation(ast.f());
    }
    if (isTransformation(ast) && isTypeLiteral(ast.from) && isDeclaration(ast.to)) {
      const to = ast.to;
      const surrogate = getSurrogateAnnotation(to);
      if (isSome(surrogate)) {
        return getIdentifierAnnotation(to);
      }
    }
  }
  return identifier;
}
function go2(ast, $defs, identifier, path, options, annotation, errors2) {
  if (identifier === "handle-identifier" && (options.topLevelReferenceStrategy !== "skip" || isSuspend(ast))) {
    const id = getIdentifierAnnotation(ast);
    if (id !== void 0) {
      const escapedId = id.replace(/~/ig, "~0").replace(/\//ig, "~1");
      const out = {
        $ref: options.getRef(escapedId)
      };
      if (!has($defs, id)) {
        $defs[id] = out;
        $defs[id] = go2(ast, $defs, "ignore-identifier", path, options, "handle-annotation", errors2);
      }
      return out;
    }
  }
  if (annotation === "handle-annotation") {
    const hook = getJSONSchemaAnnotation(ast);
    if (isSome(hook)) {
      const handler = hook.value;
      if (isOverrideAnnotation(ast, handler)) {
        switch (ast._tag) {
          case "Declaration":
            return addASTAnnotations(handler, ast);
          default:
            return handler;
        }
      } else {
        switch (ast._tag) {
          case "Refinement": {
            const t = getTransformationFrom(ast);
            if (t === void 0) {
              return mergeRefinements(go2(ast.from, $defs, identifier, path, options, "handle-annotation", errors2), handler, ast);
            } else {
              return go2(t, $defs, identifier, path, options, "handle-annotation", errors2);
            }
          }
          default:
            return {
              ...go2(ast, $defs, identifier, path, options, "ignore-annotation", errors2),
              ...handler
            };
        }
      }
    }
  }
  const surrogate = getSurrogateAnnotation(ast);
  if (isSome(surrogate)) {
    return go2(surrogate.value, $defs, identifier, path, options, "handle-annotation", errors2);
  }
  switch (ast._tag) {
    // Unsupported
    case "Declaration":
    case "UndefinedKeyword":
    case "BigIntKeyword":
    case "UniqueSymbol":
    case "SymbolKeyword": {
      if (errors2 === "ignore-errors") return addASTAnnotations(constAny, ast);
      throw new Error(getJSONSchemaMissingAnnotationErrorMessage(path, ast));
    }
    case "Suspend": {
      if (identifier === "handle-identifier") {
        if (errors2 === "ignore-errors") return addASTAnnotations(constAny, ast);
        throw new Error(getJSONSchemaMissingIdentifierAnnotationErrorMessage(path, ast));
      }
      return go2(ast.f(), $defs, "ignore-identifier", path, options, "handle-annotation", errors2);
    }
    // Primitives
    case "NeverKeyword":
      return addASTAnnotations(constNever, ast);
    case "VoidKeyword":
      return addASTAnnotations(constVoid2, ast);
    case "UnknownKeyword":
      return addASTAnnotations(constUnknown, ast);
    case "AnyKeyword":
      return addASTAnnotations(constAny, ast);
    case "ObjectKeyword":
      return addASTAnnotations(constObject, ast);
    case "StringKeyword":
      return addASTAnnotations({
        type: "string"
      }, ast);
    case "NumberKeyword":
      return addASTAnnotations({
        type: "number"
      }, ast);
    case "BooleanKeyword":
      return addASTAnnotations({
        type: "boolean"
      }, ast);
    case "Literal": {
      const literal = ast.literal;
      if (literal === null) {
        return addASTAnnotations({
          type: "null"
        }, ast);
      } else if (isString(literal)) {
        return addASTAnnotations({
          type: "string",
          enum: [literal]
        }, ast);
      } else if (isNumber(literal)) {
        return addASTAnnotations({
          type: "number",
          enum: [literal]
        }, ast);
      } else if (isBoolean(literal)) {
        return addASTAnnotations({
          type: "boolean",
          enum: [literal]
        }, ast);
      }
      if (errors2 === "ignore-errors") return addASTAnnotations(constAny, ast);
      throw new Error(getJSONSchemaMissingAnnotationErrorMessage(path, ast));
    }
    case "Enums": {
      const anyOf = ast.enums.map((e) => {
        const type = isNumber(e[1]) ? "number" : "string";
        return {
          type,
          title: e[0],
          enum: [e[1]]
        };
      });
      return anyOf.length >= 1 ? addASTAnnotations({
        $comment: "/schemas/enums",
        anyOf
      }, ast) : addASTAnnotations(constNever, ast);
    }
    case "TupleType": {
      const elements = ast.elements.map((e, i) => mergeJsonSchemaAnnotations(go2(e.type, $defs, "handle-identifier", path.concat(i), options, "handle-annotation", errors2), getContextJsonSchemaAnnotations(e.type, e)));
      const rest = ast.rest.map((type) => mergeJsonSchemaAnnotations(go2(type.type, $defs, "handle-identifier", path, options, "handle-annotation", errors2), getContextJsonSchemaAnnotations(type.type, type)));
      const output = {
        type: "array"
      };
      const len = ast.elements.length;
      if (len > 0) {
        output.minItems = len - ast.elements.filter((element) => element.isOptional).length;
        if (options.target === "jsonSchema7") {
          output.items = elements;
        } else {
          output.prefixItems = elements;
        }
      }
      const restLength = rest.length;
      if (restLength > 0) {
        const head5 = rest[0];
        const isHomogeneous = restLength === 1 && ast.elements.every((e) => e.type === ast.rest[0].type);
        if (isHomogeneous) {
          if (options.target === "jsonSchema7") {
            output.items = head5;
          } else {
            output.items = head5;
            delete output.prefixItems;
          }
        } else {
          if (options.target === "jsonSchema7") {
            output.additionalItems = head5;
          } else {
            output.items = head5;
          }
        }
        if (restLength > 1) {
          if (errors2 === "ignore-errors") return addASTAnnotations(constAny, ast);
          throw new Error(getJSONSchemaUnsupportedPostRestElementsErrorMessage(path));
        }
      } else {
        if (len > 0) {
          if (options.target === "jsonSchema7") {
            output.additionalItems = false;
          } else {
            output.items = false;
          }
        } else {
          output.maxItems = 0;
        }
      }
      return addASTAnnotations(output, ast);
    }
    case "TypeLiteral": {
      if (ast.propertySignatures.length === 0 && ast.indexSignatures.length === 0) {
        return addASTAnnotations(constEmptyStruct, ast);
      }
      const output = {
        type: "object",
        required: [],
        properties: {},
        additionalProperties: getAdditionalProperties(options)
      };
      let patternProperties = void 0;
      let propertyNames = void 0;
      for (const is2 of ast.indexSignatures) {
        const pruned = pruneUndefined2(is2.type) ?? is2.type;
        const parameter = is2.parameter;
        switch (parameter._tag) {
          case "StringKeyword": {
            output.additionalProperties = go2(pruned, $defs, "handle-identifier", path, options, "handle-annotation", errors2);
            break;
          }
          case "TemplateLiteral": {
            patternProperties = go2(pruned, $defs, "handle-identifier", path, options, "handle-annotation", errors2);
            propertyNames = {
              type: "string",
              pattern: getTemplateLiteralRegExp(parameter).source
            };
            break;
          }
          case "Refinement": {
            patternProperties = go2(pruned, $defs, "handle-identifier", path, options, "handle-annotation", errors2);
            propertyNames = go2(parameter, $defs, "handle-identifier", path, options, "handle-annotation", errors2);
            break;
          }
          case "SymbolKeyword": {
            const indexSignaturePath = path.concat("[symbol]");
            output.additionalProperties = go2(pruned, $defs, "handle-identifier", indexSignaturePath, options, "handle-annotation", errors2);
            propertyNames = go2(parameter, $defs, "handle-identifier", indexSignaturePath, options, "handle-annotation", errors2);
            break;
          }
        }
      }
      for (let i = 0; i < ast.propertySignatures.length; i++) {
        const ps = ast.propertySignatures[i];
        const name = ps.name;
        if (isString(name)) {
          const pruned = pruneUndefined2(ps.type);
          const type = pruned ?? ps.type;
          output.properties[name] = mergeJsonSchemaAnnotations(go2(type, $defs, "handle-identifier", path.concat(ps.name), options, "handle-annotation", errors2), getContextJsonSchemaAnnotations(type, ps));
          if (!ps.isOptional && pruned === void 0) {
            output.required.push(name);
          }
        } else {
          if (errors2 === "ignore-errors") return addASTAnnotations(constAny, ast);
          throw new Error(getJSONSchemaUnsupportedKeyErrorMessage(name, path));
        }
      }
      if (patternProperties !== void 0) {
        delete output.additionalProperties;
        output.patternProperties = {
          "": patternProperties
        };
      }
      if (propertyNames !== void 0) {
        output.propertyNames = propertyNames;
      }
      return addASTAnnotations(output, ast);
    }
    case "Union": {
      const members = ast.types.map((t) => go2(t, $defs, "handle-identifier", path, options, "handle-annotation", errors2));
      const anyOf = compactUnion(members);
      switch (anyOf.length) {
        case 0:
          return constNever;
        case 1:
          return addASTAnnotations(anyOf[0], ast);
        default:
          return addASTAnnotations({
            anyOf
          }, ast);
      }
    }
    case "Refinement":
      return go2(ast.from, $defs, identifier, path, options, "handle-annotation", errors2);
    case "TemplateLiteral": {
      const regex = getTemplateLiteralRegExp(ast);
      return addASTAnnotations({
        type: "string",
        title: String(ast),
        description: "a template literal",
        pattern: regex.source
      }, ast);
    }
    case "Transformation": {
      if (isParseJsonTransformation(ast.from)) {
        const out = {
          "type": "string",
          "contentMediaType": "application/json"
        };
        if (isContentSchemaSupported(options)) {
          out["contentSchema"] = go2(ast.to, $defs, identifier, path, options, "handle-annotation", errors2);
        }
        return out;
      }
      const from = go2(ast.from, $defs, identifier, path, options, "handle-annotation", errors2);
      if (ast.transformation._tag === "TypeLiteralTransformation" && isJsonSchema7Object(from)) {
        const to = go2(ast.to, {}, "ignore-identifier", path, options, "handle-annotation", "ignore-errors");
        if (isJsonSchema7Object(to)) {
          for (const t of ast.transformation.propertySignatureTransformations) {
            const toKey = t.to;
            const fromKey = t.from;
            if (isString(toKey) && isString(fromKey)) {
              const toProperty = to.properties[toKey];
              if (isRecord(toProperty)) {
                const fromProperty = from.properties[fromKey];
                if (isRecord(fromProperty)) {
                  const annotations2 = {};
                  if (isString(toProperty.title)) annotations2.title = toProperty.title;
                  if (isString(toProperty.description)) annotations2.description = toProperty.description;
                  if (Array.isArray(toProperty.examples)) annotations2.examples = toProperty.examples;
                  if (Object.hasOwn(toProperty, "default") && toProperty.default !== void 0) {
                    annotations2.default = toProperty.default;
                  }
                  from.properties[fromKey] = addAnnotations(fromProperty, annotations2);
                }
              }
            }
          }
        }
      }
      return addASTAnnotations(from, ast);
    }
  }
}
function isJsonSchema7Object(jsonSchema) {
  return isRecord(jsonSchema) && jsonSchema.type === "object" && isRecord(jsonSchema.properties);
}
function isNeverWithoutCustomAnnotations(jsonSchema) {
  return jsonSchema === constNever || hasProperty(jsonSchema, "$id") && jsonSchema.$id === constNever.$id && Object.keys(jsonSchema).length === 3 && jsonSchema.title === neverKeyword.annotations[TitleAnnotationId];
}
function isAny(jsonSchema) {
  return "$id" in jsonSchema && jsonSchema.$id === constAny.$id;
}
function isUnknown(jsonSchema) {
  return "$id" in jsonSchema && jsonSchema.$id === constUnknown.$id;
}
function isVoid(jsonSchema) {
  return "$id" in jsonSchema && jsonSchema.$id === constVoid2.$id;
}
function isCompactableLiteral(jsonSchema) {
  return hasProperty(jsonSchema, "enum") && "type" in jsonSchema && Object.keys(jsonSchema).length === 2;
}
function compactUnion(members) {
  const out = [];
  for (const m of members) {
    if (isNeverWithoutCustomAnnotations(m)) continue;
    if (isAny(m) || isUnknown(m) || isVoid(m)) return [m];
    if (isCompactableLiteral(m) && out.length > 0) {
      const last4 = out[out.length - 1];
      if (isCompactableLiteral(last4) && last4.type === m.type) {
        out[out.length - 1] = {
          type: last4.type,
          enum: [...last4.enum, ...m.enum]
        };
        continue;
      }
    }
    out.push(m);
  }
  return out;
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/KeyedPool.js
var KeyedPool_exports = {};
__export(KeyedPool_exports, {
  KeyedPoolTypeId: () => KeyedPoolTypeId2,
  get: () => get15,
  invalidate: () => invalidate2,
  make: () => make34,
  makeWith: () => makeWith4,
  makeWithTTL: () => makeWithTTL3,
  makeWithTTLBy: () => makeWithTTLBy2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/keyedPool.js
var KeyedPoolSymbolKey = "effect/KeyedPool";
var KeyedPoolTypeId = Symbol.for(KeyedPoolSymbolKey);
var KeyedPoolMapValueSymbol = /* @__PURE__ */ Symbol.for("effect/KeyedPool/MapValue");
var keyedPoolVariance = {
  /* c8 ignore next */
  _K: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var KeyedPoolImpl = class {
  getOrCreatePool;
  activePools;
  [KeyedPoolTypeId] = keyedPoolVariance;
  constructor(getOrCreatePool, activePools) {
    this.getOrCreatePool = getOrCreatePool;
    this.activePools = activePools;
  }
  get(key) {
    return flatMap(this.getOrCreatePool(key), get10);
  }
  invalidate(item) {
    return flatMap(this.activePools, forEachSequentialDiscard((pool) => pool.invalidate(item)));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var Complete = class {
  pool;
  _tag = "Complete";
  [KeyedPoolMapValueSymbol] = KeyedPoolMapValueSymbol;
  constructor(pool) {
    this.pool = pool;
  }
  [symbol2]() {
    return pipe(string("effect/KeyedPool/Complete"), combine(hash(this.pool)), cached(this));
  }
  [symbol3](u) {
    return isComplete(u) && equals(this.pool, u.pool);
  }
};
var isComplete = (u) => isTagged(u, "Complete") && KeyedPoolMapValueSymbol in u;
var Pending = class {
  deferred;
  _tag = "Pending";
  [KeyedPoolMapValueSymbol] = KeyedPoolMapValueSymbol;
  constructor(deferred) {
    this.deferred = deferred;
  }
  [symbol2]() {
    return pipe(string("effect/KeyedPool/Pending"), combine(hash(this.deferred)), cached(this));
  }
  [symbol3](u) {
    return isPending(u) && equals(this.deferred, u.deferred);
  }
};
var isPending = (u) => isTagged(u, "Pending") && KeyedPoolMapValueSymbol in u;
var makeImpl = (get37, min, max, timeToLive) => pipe(all([context(), fiberId, sync(() => make2(empty4())), scopeMake()]), map5(([context7, fiberId4, map22, scope]) => {
  const getOrCreatePool = (key) => suspend(() => {
    let value6 = getOrUndefined(get3(get(map22), key));
    if (value6 === void 0) {
      return uninterruptibleMask((restore) => {
        const deferred = deferredUnsafeMake(fiberId4);
        value6 = new Pending(deferred);
        let previous = void 0;
        if (has3(get(map22), key)) {
          previous = getOrUndefined(get3(get(map22), key));
        } else {
          update(map22, set2(key, value6));
        }
        if (previous === void 0) {
          return pipe(restore(scopeExtend(makeWithTTL({
            acquire: provideContext(get37(key), context7),
            min: min(key),
            max: max(key),
            timeToLive: getOrElse(timeToLive(key), () => infinity)
          }), scope)), matchCauseEffect({
            onFailure: (cause) => {
              const current = getOrUndefined(get3(get(map22), key));
              if (equals(current, value6)) {
                update(map22, remove(key));
              }
              return zipRight(deferredFailCause(deferred, cause), failCause(cause));
            },
            onSuccess: (pool) => {
              update(map22, set2(key, new Complete(pool)));
              return as(deferredSucceed(deferred, pool), pool);
            }
          }));
        }
        switch (previous._tag) {
          case "Complete": {
            return succeed(previous.pool);
          }
          case "Pending": {
            return restore(deferredAwait(previous.deferred));
          }
        }
      });
    }
    switch (value6._tag) {
      case "Complete": {
        return succeed(value6.pool);
      }
      case "Pending": {
        return deferredAwait(value6.deferred);
      }
    }
  });
  const activePools = suspend(() => forEachSequential(toValues(get(map22)), (value6) => {
    switch (value6._tag) {
      case "Complete": {
        return succeed(value6.pool);
      }
      case "Pending": {
        return deferredAwait(value6.deferred);
      }
    }
  }));
  return new KeyedPoolImpl(getOrCreatePool, activePools);
}));
var make33 = (options) => makeImpl(options.acquire, () => options.size, () => options.size, () => none());
var makeWith3 = (options) => makeImpl(options.acquire, options.size, options.size, () => none());
var makeWithTTL2 = (options) => {
  const timeToLive = decode(options.timeToLive);
  return makeImpl(options.acquire, options.min, options.max, () => some(timeToLive));
};
var makeWithTTLBy = (options) => makeImpl(options.acquire, options.min, options.max, (key) => some(decode(options.timeToLive(key))));
var get14 = dual(2, (self, key) => self.get(key));
var invalidate = dual(2, (self, item) => self.invalidate(item));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/KeyedPool.js
var KeyedPoolTypeId2 = KeyedPoolTypeId;
var make34 = make33;
var makeWith4 = makeWith3;
var makeWithTTL3 = makeWithTTL2;
var makeWithTTLBy2 = makeWithTTLBy;
var get15 = get14;
var invalidate2 = invalidate;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LayerMap.js
var LayerMap_exports = {};
__export(LayerMap_exports, {
  Service: () => Service,
  TypeId: () => TypeId13,
  fromRecord: () => fromRecord,
  make: () => make37
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RcMap.js
var RcMap_exports = {};
__export(RcMap_exports, {
  TypeId: () => TypeId12,
  get: () => get17,
  has: () => has9,
  invalidate: () => invalidate4,
  keys: () => keys4,
  make: () => make36,
  touch: () => touch2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/rcMap.js
var TypeId11 = /* @__PURE__ */ Symbol.for("effect/RcMap");
var variance = {
  _K: identity,
  _A: identity,
  _E: identity
};
var RcMapImpl = class {
  lookup;
  context;
  scope;
  idleTimeToLive;
  capacity;
  [TypeId11];
  state = {
    _tag: "Open",
    map: empty9()
  };
  semaphore = unsafeMakeSemaphore(1);
  constructor(lookup, context7, scope, idleTimeToLive, capacity) {
    this.lookup = lookup;
    this.context = context7;
    this.scope = scope;
    this.idleTimeToLive = idleTimeToLive;
    this.capacity = capacity;
    this[TypeId11] = variance;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make35 = (options) => withFiberRuntime((fiber) => {
  const context7 = fiber.getFiberRef(currentContext);
  const scope = get2(context7, scopeTag);
  const self = new RcMapImpl(options.lookup, context7, scope, options.idleTimeToLive ? decode(options.idleTimeToLive) : void 0, Math.max(options.capacity ?? Number.POSITIVE_INFINITY, 0));
  return as(scope.addFinalizer(() => suspend(() => {
    if (self.state._tag === "Closed") {
      return void_;
    }
    const map22 = self.state.map;
    self.state = {
      _tag: "Closed"
    };
    return forEachSequentialDiscard(map22, ([, entry]) => scopeClose(entry.scope, exitVoid)).pipe(tap(() => {
      clear(map22);
    }), self.semaphore.withPermits(1));
  })), self);
});
var get16 = dual(2, (self_, key) => {
  const self = self_;
  return uninterruptibleMask((restore) => getImpl(self, key, restore));
});
var getImpl = fnUntraced(function* (self, key, restore) {
  if (self.state._tag === "Closed") {
    return yield* interrupt;
  }
  const state = self.state;
  const o = get6(state.map, key);
  let entry;
  if (o._tag === "Some") {
    entry = o.value;
    entry.refCount++;
  } else if (Number.isFinite(self.capacity) && size2(self.state.map) >= self.capacity) {
    return yield* fail(new ExceededCapacityException(`RcMap attempted to exceed capacity of ${self.capacity}`));
  } else {
    entry = yield* self.semaphore.withPermits(1)(acquire(self, key, restore));
  }
  const scope = yield* scopeTag;
  yield* scope.addFinalizer(() => entry.finalizer);
  return yield* restore(deferredAwait(entry.deferred));
});
var acquire = fnUntraced(function* (self, key, restore) {
  const scope = yield* scopeMake();
  const deferred = yield* deferredMake();
  const acquire4 = self.lookup(key);
  const contextMap = new Map(self.context.unsafeMap);
  yield* restore(mapInputContext(acquire4, (inputContext) => {
    inputContext.unsafeMap.forEach((value6, key2) => {
      contextMap.set(key2, value6);
    });
    contextMap.set(scopeTag.key, scope);
    return unsafeMake(contextMap);
  })).pipe(exit, flatMap((exit3) => deferredDone(deferred, exit3)), forkIn(scope));
  const entry = {
    deferred,
    scope,
    finalizer: void 0,
    fiber: void 0,
    expiresAt: 0,
    refCount: 1
  };
  entry.finalizer = release(self, key, entry);
  if (self.state._tag === "Open") {
    set5(self.state.map, key, entry);
  }
  return entry;
});
var release = (self, key, entry) => clockWith((clock2) => {
  entry.refCount--;
  if (entry.refCount > 0) {
    return void_;
  } else if (self.state._tag === "Closed" || !has4(self.state.map, key) || self.idleTimeToLive === void 0) {
    if (self.state._tag === "Open") {
      remove2(self.state.map, key);
    }
    return scopeClose(entry.scope, exitVoid);
  }
  if (!isFinite(self.idleTimeToLive)) {
    return void_;
  }
  entry.expiresAt = clock2.unsafeCurrentTimeMillis() + toMillis(self.idleTimeToLive);
  if (entry.fiber) return void_;
  return interruptibleMask(function loop3(restore) {
    const now = clock2.unsafeCurrentTimeMillis();
    const remaining = entry.expiresAt - now;
    if (remaining <= 0) {
      if (self.state._tag === "Closed" || entry.refCount > 0) return void_;
      remove2(self.state.map, key);
      return restore(scopeClose(entry.scope, exitVoid));
    }
    return flatMap(clock2.sleep(millis(remaining)), () => loop3(restore));
  }).pipe(ensuring(sync(() => {
    entry.fiber = void 0;
  })), forkIn(self.scope), tap((fiber) => {
    entry.fiber = fiber;
  }), self.semaphore.withPermits(1));
});
var keys3 = (self) => {
  const impl = self;
  return suspend(() => impl.state._tag === "Closed" ? interrupt : succeed(keys(impl.state.map)));
};
var invalidate3 = dual(2, fnUntraced(function* (self_, key) {
  const self = self_;
  if (self.state._tag === "Closed") return;
  const o = get6(self.state.map, key);
  if (o._tag === "None") return;
  const entry = o.value;
  remove2(self.state.map, key);
  if (entry.refCount > 0) return;
  yield* scopeClose(entry.scope, exitVoid);
  if (entry.fiber) yield* interruptFiber(entry.fiber);
}));
var has8 = dual(2, (self_, key) => {
  const self = self_;
  return sync(() => {
    if (self.state._tag === "Closed") return false;
    return has4(self.state.map, key);
  });
});
var touch = dual(2, (self_, key) => clockWith((clock2) => {
  const self = self_;
  if (!self.idleTimeToLive || self.state._tag === "Closed") return void_;
  const o = get6(self.state.map, key);
  if (o._tag === "None") return void_;
  o.value.expiresAt = clock2.unsafeCurrentTimeMillis() + toMillis(self.idleTimeToLive);
  return void_;
}));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RcMap.js
var TypeId12 = TypeId11;
var make36 = make35;
var get17 = get16;
var has9 = has8;
var keys4 = keys3;
var invalidate4 = invalidate3;
var touch2 = touch;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LayerMap.js
var TypeId13 = /* @__PURE__ */ Symbol.for("effect/LayerMap");
var make37 = fnUntraced2(function* (lookup, options) {
  const context7 = yield* context2();
  const memoMap = context7.unsafeMap.has(CurrentMemoMap.key) ? get2(context7, CurrentMemoMap) : yield* makeMemoMap;
  const rcMap = yield* make36({
    lookup: (key) => scopeWith((scope) => diffFiberRefs(buildWithMemoMap(lookup(key), memoMap, scope))).pipe(map8(([patch4, context8]) => ({
      layer: scopedContext(withFiberRuntime((fiber) => {
        const scope = unsafeGet(fiber.currentContext, Scope);
        const oldRefs = fiber.getFiberRefs();
        const newRefs = patch2(patch4, fiber.id(), oldRefs);
        const revert = diff2(newRefs, oldRefs);
        fiber.setFiberRefs(newRefs);
        return as2(addFinalizerExit(scope, () => {
          fiber.setFiberRefs(patch2(revert, fiber.id(), fiber.getFiberRefs()));
          return _void;
        }), context8);
      })),
      runtimeEffect: withFiberRuntime2((fiber) => {
        const fiberRefs = patch2(patch4, fiber.id(), fiber.getFiberRefs());
        return succeed5(make18({
          context: context8,
          fiberRefs,
          runtimeFlags: defaultRuntime2.runtimeFlags
        }));
      })
    }))),
    idleTimeToLive: options?.idleTimeToLive
  });
  if (options?.preloadKeys) {
    for (const key of options.preloadKeys) {
      yield* get17(rcMap, key);
    }
  }
  return identity({
    [TypeId13]: TypeId13,
    rcMap,
    get: (key) => unwrapScoped(map8(get17(rcMap, key), ({
      layer
    }) => layer)),
    runtime: (key) => flatMap2(get17(rcMap, key), ({
      runtimeEffect
    }) => runtimeEffect),
    invalidate: (key) => invalidate4(rcMap, key)
  });
});
var fromRecord = (layers, options) => make37((key) => layers[key], {
  ...options,
  preloadKeys: options?.preload ? Object.keys(layers) : void 0
});
var Service = () => (id, options) => {
  const Err = globalThis.Error;
  const limit = Err.stackTraceLimit;
  Err.stackTraceLimit = 2;
  const creationError = new Err();
  Err.stackTraceLimit = limit;
  function TagClass() {
  }
  const TagClass_ = TagClass;
  Object.setPrototypeOf(TagClass, Object.getPrototypeOf(GenericTag(id)));
  TagClass.key = id;
  Object.defineProperty(TagClass, "stack", {
    get() {
      return creationError.stack;
    }
  });
  TagClass_.DefaultWithoutDependencies = scoped2(TagClass_, "lookup" in options ? make37(options.lookup, options) : fromRecord(options.layers, options));
  TagClass_.Default = options.dependencies && options.dependencies.length > 0 ? provide(TagClass_.DefaultWithoutDependencies, options.dependencies) : TagClass_.DefaultWithoutDependencies;
  TagClass_.get = (key) => unwrapScoped(map8(TagClass_, (layerMap) => layerMap.get(key)));
  TagClass_.runtime = (key) => flatMap2(TagClass_, (layerMap) => layerMap.runtime(key));
  TagClass_.invalidate = (key) => flatMap2(TagClass_, (layerMap) => layerMap.invalidate(key));
  return TagClass;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Logger.js
var Logger_exports = {};
__export(Logger_exports, {
  LoggerTypeId: () => LoggerTypeId2,
  add: () => add4,
  addEffect: () => addEffect,
  addScoped: () => addScoped,
  batched: () => batched,
  defaultLogger: () => defaultLogger2,
  filterLogLevel: () => filterLogLevel2,
  isLogger: () => isLogger2,
  json: () => json,
  jsonLogger: () => jsonLogger3,
  logFmt: () => logFmt,
  logfmtLogger: () => logfmtLogger2,
  make: () => make38,
  map: () => map13,
  mapInput: () => mapInput5,
  mapInputOptions: () => mapInputOptions2,
  minimumLogLevel: () => minimumLogLevel2,
  none: () => none8,
  pretty: () => pretty,
  prettyLogger: () => prettyLogger3,
  prettyLoggerDefault: () => prettyLoggerDefault2,
  remove: () => remove5,
  replace: () => replace,
  replaceEffect: () => replaceEffect,
  replaceScoped: () => replaceScoped,
  simple: () => simple2,
  stringLogger: () => stringLogger2,
  structured: () => structured,
  structuredLogger: () => structuredLogger3,
  succeed: () => succeed8,
  sync: () => sync7,
  test: () => test2,
  tracerLogger: () => tracerLogger2,
  withConsoleError: () => withConsoleError,
  withConsoleLog: () => withConsoleLog,
  withLeveledConsole: () => withLeveledConsole,
  withMinimumLogLevel: () => withMinimumLogLevel2,
  withSpanAnnotations: () => withSpanAnnotations,
  zip: () => zip6,
  zipLeft: () => zipLeft5,
  zipRight: () => zipRight6
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/logger-circular.js
var test = dual(2, (self, input) => self.log({
  fiberId: none2,
  logLevel: logLevelInfo,
  message: input,
  cause: empty12,
  context: empty7(),
  spans: empty5(),
  annotations: empty4(),
  date: /* @__PURE__ */ new Date()
}));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Logger.js
var LoggerTypeId2 = LoggerTypeId;
var make38 = makeLogger;
var add4 = addLogger;
var addEffect = addLoggerEffect;
var addScoped = addLoggerScoped;
var mapInput5 = mapInput2;
var mapInputOptions2 = mapInputOptions;
var filterLogLevel2 = filterLogLevel;
var map13 = map6;
var batched = batchedLogger;
var withConsoleLog = loggerWithConsoleLog;
var withLeveledConsole = loggerWithLeveledLog;
var withConsoleError = loggerWithConsoleError;
var none8 = none5;
var remove5 = removeLogger;
var replace = replaceLogger;
var replaceEffect = replaceLoggerEffect;
var replaceScoped = replaceLoggerScoped;
var simple2 = simple;
var succeed8 = succeed3;
var sync7 = sync2;
var test2 = test;
var withMinimumLogLevel2 = withMinimumLogLevel;
var withSpanAnnotations = loggerWithSpanAnnotations;
var zip6 = zip2;
var zipLeft5 = zipLeft;
var zipRight6 = zipRight2;
var defaultLogger2 = defaultLogger;
var jsonLogger3 = jsonLogger;
var logfmtLogger2 = logfmtLogger;
var stringLogger2 = stringLogger;
var prettyLogger3 = prettyLogger;
var prettyLoggerDefault2 = prettyLoggerDefault;
var structuredLogger3 = structuredLogger;
var tracerLogger2 = tracerLogger;
var json = replace(defaultLogger, jsonLogger2);
var logFmt = replace(defaultLogger, logFmtLogger);
var pretty = replace(defaultLogger, prettyLogger2);
var structured = replace(defaultLogger, structuredLogger2);
var minimumLogLevel2 = minimumLogLevel;
var isLogger2 = isLogger;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ManagedRuntime.js
var ManagedRuntime_exports = {};
__export(ManagedRuntime_exports, {
  TypeId: () => TypeId14,
  isManagedRuntime: () => isManagedRuntime2,
  make: () => make40
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/managedRuntime.js
var isManagedRuntime = (u) => hasProperty(u, TypeId3);
function provide2(managed, effect3) {
  return flatMap(managed.runtimeEffect, (rt) => withFiberRuntime((fiber) => {
    fiber.setFiberRefs(rt.fiberRefs);
    fiber.currentRuntimeFlags = rt.runtimeFlags;
    return provideContext(effect3, rt.context);
  }));
}
var ManagedRuntimeProto = {
  ...CommitPrototype2,
  [TypeId3]: TypeId3,
  pipe() {
    return pipeArguments(this, arguments);
  },
  commit() {
    return this.runtimeEffect;
  }
};
var make39 = (layer, memoMap) => {
  memoMap = memoMap ?? unsafeMakeMemoMap();
  const scope = unsafeRunSyncEffect(scopeMake());
  let buildFiber;
  const runtimeEffect = suspend(() => {
    if (!buildFiber) {
      const scheduler = new SyncScheduler();
      buildFiber = unsafeForkEffect(tap(extend(toRuntimeWithMemoMap(layer, memoMap), scope), (rt) => {
        self.cachedRuntime = rt;
      }), {
        scope,
        scheduler
      });
      scheduler.flush();
    }
    return flatten2(buildFiber.await);
  });
  const self = Object.assign(Object.create(ManagedRuntimeProto), {
    memoMap,
    scope,
    runtimeEffect,
    cachedRuntime: void 0,
    runtime() {
      return self.cachedRuntime === void 0 ? unsafeRunPromiseEffect(self.runtimeEffect) : Promise.resolve(self.cachedRuntime);
    },
    dispose() {
      return unsafeRunPromiseEffect(self.disposeEffect);
    },
    disposeEffect: suspend(() => {
      ;
      self.runtimeEffect = die("ManagedRuntime disposed");
      self.cachedRuntime = void 0;
      return close(self.scope, exitVoid);
    }),
    runFork(effect3, options) {
      return self.cachedRuntime === void 0 ? unsafeForkEffect(provide2(self, effect3), options) : unsafeFork(self.cachedRuntime)(effect3, options);
    },
    runSyncExit(effect3) {
      return self.cachedRuntime === void 0 ? unsafeRunSyncExitEffect(provide2(self, effect3)) : unsafeRunSyncExit(self.cachedRuntime)(effect3);
    },
    runSync(effect3) {
      return self.cachedRuntime === void 0 ? unsafeRunSyncEffect(provide2(self, effect3)) : unsafeRunSync(self.cachedRuntime)(effect3);
    },
    runPromiseExit(effect3, options) {
      return self.cachedRuntime === void 0 ? unsafeRunPromiseExitEffect(provide2(self, effect3), options) : unsafeRunPromiseExit(self.cachedRuntime)(effect3, options);
    },
    runCallback(effect3, options) {
      return self.cachedRuntime === void 0 ? unsafeRunCallback(defaultRuntime)(provide2(self, effect3), options) : unsafeRunCallback(self.cachedRuntime)(effect3, options);
    },
    runPromise(effect3, options) {
      return self.cachedRuntime === void 0 ? unsafeRunPromiseEffect(provide2(self, effect3), options) : unsafeRunPromise(self.cachedRuntime)(effect3, options);
    }
  });
  return self;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ManagedRuntime.js
var TypeId14 = TypeId3;
var isManagedRuntime2 = isManagedRuntime;
var make40 = make39;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MergeState.js
var MergeState_exports = {};
__export(MergeState_exports, {
  BothRunning: () => BothRunning2,
  LeftDone: () => LeftDone2,
  MergeStateTypeId: () => MergeStateTypeId2,
  RightDone: () => RightDone2,
  isBothRunning: () => isBothRunning2,
  isLeftDone: () => isLeftDone2,
  isMergeState: () => isMergeState2,
  isRightDone: () => isRightDone2,
  match: () => match11
});
var MergeStateTypeId2 = MergeStateTypeId;
var BothRunning2 = BothRunning;
var LeftDone2 = LeftDone;
var RightDone2 = RightDone;
var isMergeState2 = isMergeState;
var isBothRunning2 = isBothRunning;
var isLeftDone2 = isLeftDone;
var isRightDone2 = isRightDone;
var match11 = match6;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MergeStrategy.js
var MergeStrategy_exports = {};
__export(MergeStrategy_exports, {
  BackPressure: () => BackPressure2,
  BufferSliding: () => BufferSliding2,
  MergeStrategyTypeId: () => MergeStrategyTypeId2,
  isBackPressure: () => isBackPressure2,
  isBufferSliding: () => isBufferSliding2,
  isMergeStrategy: () => isMergeStrategy2,
  match: () => match12
});
var MergeStrategyTypeId2 = MergeStrategyTypeId;
var BackPressure2 = BackPressure;
var BufferSliding2 = BufferSliding;
var isMergeStrategy2 = isMergeStrategy;
var isBackPressure2 = isBackPressure;
var isBufferSliding2 = isBufferSliding;
var match12 = match7;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Metric.js
var Metric_exports = {};
__export(Metric_exports, {
  MetricTypeId: () => MetricTypeId2,
  counter: () => counter7,
  fiberActive: () => fiberActive2,
  fiberFailures: () => fiberFailures2,
  fiberLifetimes: () => fiberLifetimes2,
  fiberStarted: () => fiberStarted2,
  fiberSuccesses: () => fiberSuccesses2,
  frequency: () => frequency6,
  fromMetricKey: () => fromMetricKey2,
  gauge: () => gauge6,
  globalMetricRegistry: () => globalMetricRegistry2,
  histogram: () => histogram6,
  increment: () => increment2,
  incrementBy: () => incrementBy2,
  make: () => make41,
  map: () => map14,
  mapInput: () => mapInput6,
  mapType: () => mapType2,
  modify: () => modify5,
  set: () => set11,
  snapshot: () => snapshot2,
  succeed: () => succeed9,
  summary: () => summary6,
  summaryTimestamp: () => summaryTimestamp2,
  sync: () => sync8,
  tagged: () => tagged3,
  taggedWithLabels: () => taggedWithLabels3,
  taggedWithLabelsInput: () => taggedWithLabelsInput2,
  timer: () => timer2,
  timerWithBoundaries: () => timerWithBoundaries2,
  trackAll: () => trackAll2,
  trackDefect: () => trackDefect2,
  trackDefectWith: () => trackDefectWith2,
  trackDuration: () => trackDuration2,
  trackDurationWith: () => trackDurationWith2,
  trackError: () => trackError2,
  trackErrorWith: () => trackErrorWith2,
  trackSuccess: () => trackSuccess2,
  trackSuccessWith: () => trackSuccessWith2,
  unsafeSnapshot: () => unsafeSnapshot2,
  update: () => update6,
  value: () => value4,
  withConstantInput: () => withConstantInput2,
  withNow: () => withNow2,
  zip: () => zip7
});
var MetricTypeId2 = MetricTypeId;
var globalMetricRegistry2 = globalMetricRegistry;
var make41 = make13;
var mapInput6 = mapInput3;
var counter7 = counter5;
var frequency6 = frequency5;
var withConstantInput2 = withConstantInput;
var fromMetricKey2 = fromMetricKey;
var gauge6 = gauge5;
var histogram6 = histogram5;
var increment2 = increment;
var incrementBy2 = incrementBy;
var map14 = map7;
var mapType2 = mapType;
var modify5 = modify2;
var set11 = set6;
var snapshot2 = snapshot;
var succeed9 = succeed4;
var sync8 = sync3;
var summary6 = summary5;
var summaryTimestamp2 = summaryTimestamp;
var tagged3 = tagged2;
var taggedWithLabelsInput2 = taggedWithLabelsInput;
var taggedWithLabels3 = taggedWithLabels2;
var timer2 = timer;
var timerWithBoundaries2 = timerWithBoundaries;
var trackAll2 = trackAll;
var trackDefect2 = trackDefect;
var trackDefectWith2 = trackDefectWith;
var trackDuration2 = trackDuration;
var trackDurationWith2 = trackDurationWith;
var trackError2 = trackError;
var trackErrorWith2 = trackErrorWith;
var trackSuccess2 = trackSuccess;
var trackSuccessWith2 = trackSuccessWith;
var update6 = update4;
var value4 = value;
var withNow2 = withNow;
var zip7 = zip3;
var unsafeSnapshot2 = unsafeSnapshot;
var fiberStarted2 = fiberStarted;
var fiberSuccesses2 = fiberSuccesses;
var fiberFailures2 = fiberFailures;
var fiberLifetimes2 = fiberLifetimes;
var fiberActive2 = fiberActive;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricBoundaries.js
var MetricBoundaries_exports = {};
__export(MetricBoundaries_exports, {
  MetricBoundariesTypeId: () => MetricBoundariesTypeId2,
  exponential: () => exponential2,
  fromIterable: () => fromIterable8,
  isMetricBoundaries: () => isMetricBoundaries2,
  linear: () => linear2
});
var MetricBoundariesTypeId2 = MetricBoundariesTypeId;
var isMetricBoundaries2 = isMetricBoundaries;
var fromIterable8 = fromIterable4;
var linear2 = linear;
var exponential2 = exponential;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricHook.js
var MetricHook_exports = {};
__export(MetricHook_exports, {
  MetricHookTypeId: () => MetricHookTypeId2,
  counter: () => counter8,
  frequency: () => frequency7,
  gauge: () => gauge7,
  histogram: () => histogram7,
  make: () => make42,
  onModify: () => onModify2,
  onUpdate: () => onUpdate2,
  summary: () => summary7
});
var MetricHookTypeId2 = MetricHookTypeId;
var make42 = make10;
var counter8 = counter4;
var frequency7 = frequency4;
var gauge7 = gauge4;
var histogram7 = histogram4;
var summary7 = summary4;
var onUpdate2 = onUpdate;
var onModify2 = onModify;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricKey.js
var MetricKey_exports = {};
__export(MetricKey_exports, {
  MetricKeyTypeId: () => MetricKeyTypeId2,
  counter: () => counter9,
  frequency: () => frequency8,
  gauge: () => gauge8,
  histogram: () => histogram8,
  isMetricKey: () => isMetricKey2,
  summary: () => summary8,
  tagged: () => tagged4,
  taggedWithLabels: () => taggedWithLabels4
});
var MetricKeyTypeId2 = MetricKeyTypeId;
var isMetricKey2 = isMetricKey;
var counter9 = counter2;
var frequency8 = frequency2;
var gauge8 = gauge2;
var histogram8 = histogram2;
var summary8 = summary2;
var tagged4 = tagged;
var taggedWithLabels4 = taggedWithLabels;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricKeyType.js
var MetricKeyType_exports = {};
__export(MetricKeyType_exports, {
  CounterKeyTypeTypeId: () => CounterKeyTypeTypeId2,
  FrequencyKeyTypeTypeId: () => FrequencyKeyTypeTypeId2,
  GaugeKeyTypeTypeId: () => GaugeKeyTypeTypeId2,
  HistogramKeyTypeTypeId: () => HistogramKeyTypeTypeId2,
  MetricKeyTypeTypeId: () => MetricKeyTypeTypeId2,
  SummaryKeyTypeTypeId: () => SummaryKeyTypeTypeId2,
  counter: () => counter10,
  frequency: () => frequency9,
  gauge: () => gauge9,
  histogram: () => histogram9,
  isCounterKey: () => isCounterKey2,
  isFrequencyKey: () => isFrequencyKey2,
  isGaugeKey: () => isGaugeKey2,
  isHistogramKey: () => isHistogramKey2,
  isMetricKeyType: () => isMetricKeyType2,
  isSummaryKey: () => isSummaryKey2,
  summary: () => summary9
});
var MetricKeyTypeTypeId2 = MetricKeyTypeTypeId;
var CounterKeyTypeTypeId2 = CounterKeyTypeTypeId;
var FrequencyKeyTypeTypeId2 = FrequencyKeyTypeTypeId;
var GaugeKeyTypeTypeId2 = GaugeKeyTypeTypeId;
var HistogramKeyTypeTypeId2 = HistogramKeyTypeTypeId;
var SummaryKeyTypeTypeId2 = SummaryKeyTypeTypeId;
var counter10 = counter;
var frequency9 = frequency;
var gauge9 = gauge;
var histogram9 = histogram;
var summary9 = summary;
var isMetricKeyType2 = isMetricKeyType;
var isCounterKey2 = isCounterKey;
var isFrequencyKey2 = isFrequencyKey;
var isGaugeKey2 = isGaugeKey;
var isHistogramKey2 = isHistogramKey;
var isSummaryKey2 = isSummaryKey;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricLabel.js
var MetricLabel_exports = {};
__export(MetricLabel_exports, {
  MetricLabelTypeId: () => MetricLabelTypeId2,
  isMetricLabel: () => isMetricLabel2,
  make: () => make43
});
var MetricLabelTypeId2 = MetricLabelTypeId;
var make43 = make9;
var isMetricLabel2 = isMetricLabel;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricPair.js
var MetricPair_exports = {};
__export(MetricPair_exports, {
  MetricPairTypeId: () => MetricPairTypeId2,
  make: () => make44,
  unsafeMake: () => unsafeMake8
});
var MetricPairTypeId2 = MetricPairTypeId;
var make44 = make11;
var unsafeMake8 = unsafeMake3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricPolling.js
var MetricPolling_exports = {};
__export(MetricPolling_exports, {
  MetricPollingTypeId: () => MetricPollingTypeId2,
  collectAll: () => collectAll3,
  launch: () => launch2,
  make: () => make46,
  poll: () => poll3,
  pollAndUpdate: () => pollAndUpdate2,
  retry: () => retry3,
  zip: () => zip9
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/polling.js
var MetricPollingSymbolKey = "effect/MetricPolling";
var MetricPollingTypeId = Symbol.for(MetricPollingSymbolKey);
var make45 = (metric, poll6) => {
  return {
    [MetricPollingTypeId]: MetricPollingTypeId,
    pipe() {
      return pipeArguments(this, arguments);
    },
    metric,
    poll: poll6
  };
};
var collectAll2 = (iterable) => {
  const metrics = Array.from(iterable);
  return {
    [MetricPollingTypeId]: MetricPollingTypeId,
    pipe() {
      return pipeArguments(this, arguments);
    },
    metric: make13(Array.of(void 0), (inputs, extraTags) => {
      for (let i = 0; i < inputs.length; i++) {
        const pollingMetric = metrics[i];
        const input = pipe(inputs, (x) => x[i]);
        pollingMetric.metric.unsafeUpdate(input, extraTags);
      }
    }, (extraTags) => Array.from(metrics.map((pollingMetric) => pollingMetric.metric.unsafeValue(extraTags))), (inputs, extraTags) => {
      for (let i = 0; i < inputs.length; i++) {
        const pollingMetric = metrics[i];
        const input = pipe(inputs, (x) => x[i]);
        pollingMetric.metric.unsafeModify(input, extraTags);
      }
    }),
    poll: forEachSequential(metrics, (metric) => metric.poll)
  };
};
var launch = dual(2, (self, schedule) => pipe(pollAndUpdate(self), zipRight(value(self.metric)), scheduleForked(schedule)));
var poll2 = (self) => self.poll;
var pollAndUpdate = (self) => flatMap(self.poll, (value6) => update4(self.metric, value6));
var retry2 = dual(2, (self, policy) => ({
  [MetricPollingTypeId]: MetricPollingTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  metric: self.metric,
  poll: retry_Effect(self.poll, policy)
}));
var zip8 = dual(2, (self, that) => ({
  [MetricPollingTypeId]: MetricPollingTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  metric: pipe(self.metric, zip3(that.metric)),
  poll: zip(self.poll, that.poll)
}));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricPolling.js
var MetricPollingTypeId2 = MetricPollingTypeId;
var make46 = make45;
var collectAll3 = collectAll2;
var launch2 = launch;
var poll3 = poll2;
var pollAndUpdate2 = pollAndUpdate;
var retry3 = retry2;
var zip9 = zip8;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricRegistry.js
var MetricRegistry_exports = {};
__export(MetricRegistry_exports, {
  MetricRegistryTypeId: () => MetricRegistryTypeId2,
  make: () => make47
});
var MetricRegistryTypeId2 = MetricRegistryTypeId;
var make47 = make12;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MetricState.js
var MetricState_exports = {};
__export(MetricState_exports, {
  CounterStateTypeId: () => CounterStateTypeId2,
  FrequencyStateTypeId: () => FrequencyStateTypeId2,
  GaugeStateTypeId: () => GaugeStateTypeId2,
  HistogramStateTypeId: () => HistogramStateTypeId2,
  MetricStateTypeId: () => MetricStateTypeId2,
  SummaryStateTypeId: () => SummaryStateTypeId2,
  counter: () => counter11,
  frequency: () => frequency10,
  gauge: () => gauge10,
  histogram: () => histogram10,
  isCounterState: () => isCounterState2,
  isFrequencyState: () => isFrequencyState2,
  isGaugeState: () => isGaugeState2,
  isHistogramState: () => isHistogramState2,
  isMetricState: () => isMetricState2,
  isSummaryState: () => isSummaryState2,
  summary: () => summary10
});
var MetricStateTypeId2 = MetricStateTypeId;
var CounterStateTypeId2 = CounterStateTypeId;
var FrequencyStateTypeId2 = FrequencyStateTypeId;
var GaugeStateTypeId2 = GaugeStateTypeId;
var HistogramStateTypeId2 = HistogramStateTypeId;
var SummaryStateTypeId2 = SummaryStateTypeId;
var counter11 = counter3;
var frequency10 = frequency3;
var gauge10 = gauge3;
var histogram10 = histogram3;
var summary10 = summary3;
var isMetricState2 = isMetricState;
var isCounterState2 = isCounterState;
var isFrequencyState2 = isFrequencyState;
var isGaugeState2 = isGaugeState;
var isHistogramState2 = isHistogramState;
var isSummaryState2 = isSummaryState;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ModuleVersion.js
var ModuleVersion_exports = {};
__export(ModuleVersion_exports, {
  getCurrentVersion: () => getCurrentVersion2,
  setCurrentVersion: () => setCurrentVersion2
});
var getCurrentVersion2 = getCurrentVersion;
var setCurrentVersion2 = setCurrentVersion;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MutableHashSet.js
var MutableHashSet_exports = {};
__export(MutableHashSet_exports, {
  add: () => add5,
  clear: () => clear6,
  empty: () => empty14,
  fromIterable: () => fromIterable9,
  has: () => has10,
  make: () => make48,
  remove: () => remove6,
  size: () => size5
});
var TypeId15 = /* @__PURE__ */ Symbol.for("effect/MutableHashSet");
var MutableHashSetProto = {
  [TypeId15]: TypeId15,
  [Symbol.iterator]() {
    return Array.from(this.keyMap).map(([_]) => _)[Symbol.iterator]();
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableHashSet",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var fromHashMap = (keyMap) => {
  const set21 = Object.create(MutableHashSetProto);
  set21.keyMap = keyMap;
  return set21;
};
var empty14 = () => fromHashMap(empty9());
var fromIterable9 = (keys10) => fromHashMap(fromIterable5(Array.from(keys10).map((k) => [k, true])));
var make48 = (...keys10) => fromIterable9(keys10);
var add5 = dual(2, (self, key) => (set5(self.keyMap, key, true), self));
var has10 = dual(2, (self, key) => has4(self.keyMap, key));
var remove6 = dual(2, (self, key) => (remove2(self.keyMap, key), self));
var size5 = (self) => size2(self.keyMap);
var clear6 = (self) => (clear(self.keyMap), self);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/NonEmptyIterable.js
var NonEmptyIterable_exports = {};
__export(NonEmptyIterable_exports, {
  unprepend: () => unprepend
});
var unprepend = (self) => {
  const iterator = self[Symbol.iterator]();
  const next3 = iterator.next();
  if (next3.done) {
    throw new Error("BUG: NonEmptyIterator should not be empty - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return [next3.value, iterator];
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Ordering.js
var Ordering_exports = {};
__export(Ordering_exports, {
  combine: () => combine3,
  combineAll: () => combineAll,
  combineMany: () => combineMany,
  match: () => match13,
  reverse: () => reverse2
});
var reverse2 = (o) => o === -1 ? 1 : o === 1 ? -1 : 0;
var match13 = dual(2, (self, {
  onEqual,
  onGreaterThan,
  onLessThan
}) => self === -1 ? onLessThan() : self === 0 ? onEqual() : onGreaterThan());
var combine3 = dual(2, (self, that) => self !== 0 ? self : that);
var combineMany = dual(2, (self, collection) => {
  let ordering = self;
  if (ordering !== 0) {
    return ordering;
  }
  for (ordering of collection) {
    if (ordering !== 0) {
      return ordering;
    }
  }
  return ordering;
});
var combineAll = (collection) => combineMany(0, collection);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/PartitionedSemaphore.js
var PartitionedSemaphore_exports = {};
__export(PartitionedSemaphore_exports, {
  TypeId: () => TypeId16,
  make: () => make49,
  makeUnsafe: () => makeUnsafe
});
var TypeId16 = "~effect/PartitionedSemaphore";
var makeUnsafe = (options) => {
  const maxPermits = Math.max(0, options.permits);
  if (!Number.isFinite(maxPermits)) {
    return {
      [TypeId16]: TypeId16,
      withPermits: () => (effect3) => effect3
    };
  }
  let totalPermits = maxPermits;
  let waitingPermits = 0;
  const partitions = empty9();
  const take5 = (key, permits) => async((resume) => {
    if (maxPermits < permits) {
      return resume(never);
    } else if (totalPermits >= permits) {
      totalPermits -= permits;
      return resume(_void);
    }
    const needed = permits - totalPermits;
    const taken = permits - needed;
    if (totalPermits > 0) {
      totalPermits = 0;
    }
    waitingPermits += needed;
    const waiters = getOrElse(get6(partitions, key), () => {
      const set21 = /* @__PURE__ */ new Set();
      set5(partitions, key, set21);
      return set21;
    });
    const entry = {
      permits: needed,
      resume() {
        cleanup();
        resume(_void);
      }
    };
    function cleanup() {
      waiters.delete(entry);
      if (waiters.size === 0) {
        remove2(partitions, key);
      }
    }
    waiters.add(entry);
    return sync4(() => {
      cleanup();
      waitingPermits -= entry.permits;
      if (taken > 0) {
        releaseUnsafe(taken);
      }
    });
  });
  let iterator = partitions[Symbol.iterator]();
  const releaseUnsafe = (permits) => {
    while (permits > 0) {
      if (waitingPermits === 0) {
        totalPermits += permits;
        return;
      }
      let state = iterator.next();
      if (state.done) {
        iterator = partitions[Symbol.iterator]();
        state = iterator.next();
        if (state.done) return;
      }
      const entry = unsafeHead(state.value[1]);
      entry.permits--;
      waitingPermits--;
      if (entry.permits === 0) entry.resume();
      permits--;
    }
  };
  return {
    [TypeId16]: TypeId16,
    withPermits: (key, permits) => {
      const takePermits = take5(key, permits);
      const release4 = matchCauseEffect2({
        onFailure(cause) {
          releaseUnsafe(permits);
          return failCause2(cause);
        },
        onSuccess(value6) {
          releaseUnsafe(permits);
          return succeed5(value6);
        }
      });
      return (effect3) => uninterruptibleMask2((restore) => flatMap2(restore(takePermits), () => release4(restore(effect3))));
    }
  };
};
var make49 = (options) => sync4(() => makeUnsafe(options));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Pretty.js
var Pretty_exports = {};
__export(Pretty_exports, {
  make: () => make50,
  match: () => match14
});
var make50 = (schema) => compile(schema.ast, []);
var getPrettyAnnotation = getAnnotation(PrettyAnnotationId);
var getMatcher = (defaultPretty) => (ast) => match2(getPrettyAnnotation(ast), {
  onNone: () => defaultPretty,
  onSome: (handler) => handler()
});
var toString = getMatcher((a) => String(a));
var stringify = getMatcher((a) => JSON.stringify(a));
var formatUnknown2 = getMatcher(formatUnknown);
var match14 = {
  "Declaration": (ast, go3, path) => {
    const annotation = getPrettyAnnotation(ast);
    if (isSome(annotation)) {
      return annotation.value(...ast.typeParameters.map((tp) => go3(tp, path)));
    }
    throw new Error(getPrettyMissingAnnotationErrorMessage(path, ast));
  },
  "VoidKeyword": getMatcher(() => "void(0)"),
  "NeverKeyword": getMatcher(() => {
    throw new Error(getPrettyNeverErrorMessage);
  }),
  "Literal": getMatcher((literal) => typeof literal === "bigint" ? `${String(literal)}n` : JSON.stringify(literal)),
  "SymbolKeyword": toString,
  "UniqueSymbol": toString,
  "TemplateLiteral": stringify,
  "UndefinedKeyword": toString,
  "UnknownKeyword": formatUnknown2,
  "AnyKeyword": formatUnknown2,
  "ObjectKeyword": formatUnknown2,
  "StringKeyword": stringify,
  "NumberKeyword": toString,
  "BooleanKeyword": toString,
  "BigIntKeyword": getMatcher((a) => `${String(a)}n`),
  "Enums": stringify,
  "TupleType": (ast, go3, path) => {
    const hook = getPrettyAnnotation(ast);
    if (isSome(hook)) {
      return hook.value();
    }
    const elements = ast.elements.map((e, i) => go3(e.type, path.concat(i)));
    const rest = ast.rest.map((annotatedAST) => go3(annotatedAST.type, path));
    return (input) => {
      const output = [];
      let i = 0;
      for (; i < elements.length; i++) {
        if (input.length < i + 1) {
          if (ast.elements[i].isOptional) {
            continue;
          }
        } else {
          output.push(elements[i](input[i]));
        }
      }
      if (isNonEmptyReadonlyArray(rest)) {
        const [head5, ...tail] = rest;
        for (; i < input.length - tail.length; i++) {
          output.push(head5(input[i]));
        }
        for (let j = 0; j < tail.length; j++) {
          i += j;
          output.push(tail[j](input[i]));
        }
      }
      return "[" + output.join(", ") + "]";
    };
  },
  "TypeLiteral": (ast, go3, path) => {
    const hook = getPrettyAnnotation(ast);
    if (isSome(hook)) {
      return hook.value();
    }
    const propertySignaturesTypes = ast.propertySignatures.map((ps) => go3(ps.type, path.concat(ps.name)));
    const indexSignatureTypes = ast.indexSignatures.map((is2) => go3(is2.type, path));
    const expectedKeys = {};
    for (let i = 0; i < propertySignaturesTypes.length; i++) {
      expectedKeys[ast.propertySignatures[i].name] = null;
    }
    return (input) => {
      const output = [];
      for (let i = 0; i < propertySignaturesTypes.length; i++) {
        const ps = ast.propertySignatures[i];
        const name = ps.name;
        if (ps.isOptional && !Object.prototype.hasOwnProperty.call(input, name)) {
          continue;
        }
        output.push(`${formatPropertyKey(name)}: ${propertySignaturesTypes[i](input[name])}`);
      }
      if (indexSignatureTypes.length > 0) {
        for (let i = 0; i < indexSignatureTypes.length; i++) {
          const type = indexSignatureTypes[i];
          const keys10 = getKeysForIndexSignature(input, ast.indexSignatures[i].parameter);
          for (const key of keys10) {
            if (Object.prototype.hasOwnProperty.call(expectedKeys, key)) {
              continue;
            }
            output.push(`${formatPropertyKey(key)}: ${type(input[key])}`);
          }
        }
      }
      return isNonEmptyReadonlyArray(output) ? "{ " + output.join(", ") + " }" : "{}";
    };
  },
  "Union": (ast, go3, path) => {
    const hook = getPrettyAnnotation(ast);
    if (isSome(hook)) {
      return hook.value();
    }
    const types = ast.types.map((ast2) => [is({
      ast: ast2
    }), go3(ast2, path)]);
    return (a) => {
      const index = types.findIndex(([is2]) => is2(a));
      if (index === -1) {
        throw new Error(getPrettyNoMatchingSchemaErrorMessage(a, path, ast));
      }
      return types[index][1](a);
    };
  },
  "Suspend": (ast, go3, path) => {
    return match2(getPrettyAnnotation(ast), {
      onNone: () => {
        const get37 = memoizeThunk(() => go3(ast.f(), path));
        return (a) => get37()(a);
      },
      onSome: (handler) => handler()
    });
  },
  "Refinement": (ast, go3, path) => {
    return match2(getPrettyAnnotation(ast), {
      onNone: () => go3(ast.from, path),
      onSome: (handler) => handler()
    });
  },
  "Transformation": (ast, go3, path) => {
    return match2(getPrettyAnnotation(ast), {
      onNone: () => go3(ast.to, path),
      onSome: (handler) => handler()
    });
  }
};
var compile = getCompiler(match14);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RateLimiter.js
var RateLimiter_exports = {};
__export(RateLimiter_exports, {
  make: () => make52,
  withCost: () => withCost2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/rateLimiter.js
var make51 = ({
  algorithm = "token-bucket",
  interval,
  limit
}) => {
  switch (algorithm) {
    case "fixed-window": {
      return fixedWindow(limit, interval);
    }
    case "token-bucket": {
      return tokenBucket(limit, interval);
    }
  }
};
var tokenBucket = (limit, window) => gen2(function* () {
  const millisPerToken = Math.ceil(toMillis(window) / limit);
  const semaphore = yield* makeSemaphore(limit);
  const latch = yield* makeSemaphore(0);
  const refill = sleep(millisPerToken).pipe(zipRight3(latch.releaseAll), zipRight3(semaphore.release(1)), flatMap2((free) => free === limit ? _void : refill));
  yield* pipe(latch.take(1), zipRight3(refill), forever, forkScoped, interruptible3);
  const take5 = uninterruptibleMask2((restore) => flatMap2(get8(currentCost), (cost) => zipRight3(restore(semaphore.take(cost)), latch.release(1))));
  return (effect3) => zipRight3(take5, effect3);
});
var fixedWindow = (limit, window) => gen2(function* () {
  const semaphore = yield* makeSemaphore(limit);
  const latch = yield* makeSemaphore(0);
  yield* pipe(latch.take(1), zipRight3(sleep(window)), zipRight3(latch.releaseAll), zipRight3(semaphore.releaseAll), forever, forkScoped, interruptible3);
  const take5 = uninterruptibleMask2((restore) => flatMap2(get8(currentCost), (cost) => zipRight3(restore(semaphore.take(cost)), latch.release(1))));
  return (effect3) => zipRight3(take5, effect3);
});
var currentCost = globalValue(/* @__PURE__ */ Symbol.for("effect/RateLimiter/currentCost"), () => unsafeMake4(1));
var withCost = (cost) => locally(currentCost, cost);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RateLimiter.js
var make52 = make51;
var withCost2 = withCost;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Reloadable.js
var Reloadable_exports = {};
__export(Reloadable_exports, {
  ReloadableTypeId: () => ReloadableTypeId2,
  auto: () => auto2,
  autoFromConfig: () => autoFromConfig2,
  get: () => get19,
  manual: () => manual2,
  reload: () => reload2,
  reloadFork: () => reloadFork2,
  tag: () => tag
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/reloadable.js
var ReloadableSymbolKey = "effect/Reloadable";
var ReloadableTypeId = Symbol.for(ReloadableSymbolKey);
var reloadableVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var auto = (tag2, options) => scoped(reloadableTag(tag2), pipe(build(manual(tag2, {
  layer: options.layer
})), map5(unsafeGet(reloadableTag(tag2))), tap((reloadable) => acquireRelease(pipe(reloadable.reload, ignoreLogged, schedule_Effect(options.schedule), forkDaemon), interruptFiber))));
var autoFromConfig = (tag2, options) => scoped(reloadableTag(tag2), pipe(context(), flatMap((env) => pipe(build(auto(tag2, {
  layer: options.layer,
  schedule: options.scheduleFromConfig(env)
})), map5(unsafeGet(reloadableTag(tag2)))))));
var get18 = (tag2) => flatMap(reloadableTag(tag2), (reloadable) => get7(reloadable.scopedRef));
var manual = (tag2, options) => scoped(reloadableTag(tag2), pipe(context(), flatMap((env) => pipe(fromAcquire(pipe(build(options.layer), map5(unsafeGet(tag2)))), map5((ref) => ({
  [ReloadableTypeId]: reloadableVariance,
  scopedRef: ref,
  reload: pipe(set7(ref, pipe(build(options.layer), map5(unsafeGet(tag2)))), provideContext(env))
}))))));
var reloadableTag = (tag2) => {
  return GenericTag(`effect/Reloadable<${tag2.key}>`);
};
var reload = (tag2) => flatMap(reloadableTag(tag2), (reloadable) => reloadable.reload);
var reloadFork = (tag2) => flatMap(reloadableTag(tag2), (reloadable) => pipe(reloadable.reload, ignoreLogged, forkDaemon, asVoid));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Reloadable.js
var ReloadableTypeId2 = ReloadableTypeId;
var auto2 = auto;
var autoFromConfig2 = autoFromConfig;
var get19 = get18;
var manual2 = manual;
var reload2 = reload;
var tag = reloadableTag;
var reloadFork2 = reloadFork;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RequestBlock.js
var RequestBlock_exports = {};
__export(RequestBlock_exports, {
  empty: () => empty15,
  mapRequestResolvers: () => mapRequestResolvers2,
  parallel: () => parallel,
  reduce: () => reduce7,
  sequential: () => sequential,
  single: () => single2
});
var single2 = single;
var empty15 = empty6;
var mapRequestResolvers2 = mapRequestResolvers;
var parallel = par;
var reduce7 = reduce3;
var sequential = seq;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RequestResolver.js
var RequestResolver_exports = {};
__export(RequestResolver_exports, {
  RequestResolverTypeId: () => RequestResolverTypeId2,
  around: () => around2,
  aroundRequests: () => aroundRequests2,
  batchN: () => batchN2,
  contextFromEffect: () => contextFromEffect,
  contextFromServices: () => contextFromServices,
  eitherWith: () => eitherWith2,
  fromEffect: () => fromEffect5,
  fromEffectTagged: () => fromEffectTagged2,
  fromFunction: () => fromFunction2,
  fromFunctionBatched: () => fromFunctionBatched2,
  isRequestResolver: () => isRequestResolver2,
  locally: () => locally2,
  make: () => make54,
  makeBatched: () => makeBatched2,
  makeWithEntry: () => makeWithEntry2,
  mapInputContext: () => mapInputContext4,
  never: () => never4,
  provideContext: () => provideContext5,
  race: () => race4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/dataSource.js
var make53 = (runAll) => new RequestResolverImpl((requests) => runAll(requests.map((_) => _.map((_2) => _2.request))));
var makeWithEntry = (runAll) => new RequestResolverImpl((requests) => runAll(requests));
var makeBatched = (run3) => new RequestResolverImpl((requests) => {
  if (requests.length > 1) {
    return forEachSequentialDiscard(requests, (block) => {
      const filtered = block.filter((_) => !_.state.completed).map((_) => _.request);
      if (!isNonEmptyArray(filtered)) {
        return void_;
      }
      return invokeWithInterrupt(run3(filtered), block);
    });
  } else if (requests.length === 1) {
    const filtered = requests[0].filter((_) => !_.state.completed).map((_) => _.request);
    if (!isNonEmptyArray(filtered)) {
      return void_;
    }
    return run3(filtered);
  }
  return void_;
});
var around = dual(3, (self, before, after) => new RequestResolverImpl((requests) => acquireUseRelease(before, () => self.runAll(requests), after), make("Around", self, before, after)));
var aroundRequests = dual(3, (self, before, after) => new RequestResolverImpl((requests) => {
  const flatRequests = requests.flatMap((chunk3) => chunk3.map((entry) => entry.request));
  return acquireUseRelease(before(flatRequests), () => self.runAll(requests), (a2) => after(flatRequests, a2));
}, make("AroundRequests", self, before, after)));
var batchN = dual(2, (self, n) => new RequestResolverImpl((requests) => {
  return n < 1 ? die(new IllegalArgumentException("RequestResolver.batchN: n must be at least 1")) : self.runAll(Array.from(map4(reduce(requests, empty2(), (acc, chunk3) => appendAll(acc, chunksOf(unsafeFromArray(chunk3), n))), (chunk3) => Array.from(chunk3))));
}, make("BatchN", self, n)));
var mapInputContext3 = dual(2, (self, f) => new RequestResolverImpl((requests) => mapInputContext(self.runAll(requests), (context7) => f(context7)), make("MapInputContext", self, f)));
var eitherWith = dual(3, (self, that, f) => new RequestResolverImpl((batch) => forEachSequential(batch, (requests) => {
  const [as7, bs] = pipe(requests, partitionMap(f));
  return zipWithOptions(self.runAll(Array.of(as7)), that.runAll(Array.of(bs)), () => void 0, {
    concurrent: true
  });
}), make("EitherWith", self, that, f)));
var fromFunction = (f) => makeBatched((requests) => forEachSequentialDiscard(requests, (request) => complete(request, exitSucceed(f(request))))).identified("FromFunction", f);
var fromFunctionBatched = (f) => makeBatched((as7) => forEach(f(as7), (res, i) => complete(as7[i], exitSucceed(res)), {
  discard: true
})).identified("FromFunctionBatched", f);
var fromEffect4 = (f) => makeBatched((requests) => forEach(requests, (a) => flatMap2(exit2(f(a)), (e) => complete(a, e)), {
  concurrency: "unbounded",
  discard: true
})).identified("FromEffect", f);
var fromEffectTagged = () => (fns) => makeBatched((requests) => {
  const grouped = {};
  const tags = [];
  for (let i = 0, len = requests.length; i < len; i++) {
    if (tags.includes(requests[i]._tag)) {
      grouped[requests[i]._tag].push(requests[i]);
    } else {
      grouped[requests[i]._tag] = [requests[i]];
      tags.push(requests[i]._tag);
    }
  }
  return forEach(tags, (tag2) => matchCauseEffect2(fns[tag2](grouped[tag2]), {
    onFailure: (cause) => forEach(grouped[tag2], (req) => complete(req, exitFail(cause)), {
      discard: true
    }),
    onSuccess: (res) => forEach(grouped[tag2], (req, i) => complete(req, exitSucceed(res[i])), {
      discard: true
    })
  }), {
    concurrency: "unbounded",
    discard: true
  });
}).identified("FromEffectTagged", fns);
var never3 = make53(() => never).identified("Never");
var provideContext4 = dual(2, (self, context7) => mapInputContext3(self, (_) => context7).identified("ProvideContext", self, context7));
var race3 = dual(2, (self, that) => new RequestResolverImpl((requests) => race(self.runAll(requests), that.runAll(requests))).identified("Race", self, that));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RequestResolver.js
var RequestResolverTypeId2 = RequestResolverTypeId;
var contextFromEffect = (self) => contextWith((_) => provideContext5(self, _));
var contextFromServices = (...services) => (self) => contextWith((_) => provideContext5(self, pick(...services)(_)));
var isRequestResolver2 = isRequestResolver;
var make54 = make53;
var makeWithEntry2 = makeWithEntry;
var makeBatched2 = makeBatched;
var around2 = around;
var aroundRequests2 = aroundRequests;
var batchN2 = batchN;
var mapInputContext4 = mapInputContext3;
var eitherWith2 = eitherWith;
var fromFunction2 = fromFunction;
var fromFunctionBatched2 = fromFunctionBatched;
var fromEffect5 = fromEffect4;
var fromEffectTagged2 = fromEffectTagged;
var never4 = never3;
var provideContext5 = provideContext4;
var race4 = race3;
var locally2 = resolverLocally;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Resource.js
var Resource_exports = {};
__export(Resource_exports, {
  ResourceTypeId: () => ResourceTypeId2,
  auto: () => auto4,
  get: () => get21,
  manual: () => manual4,
  refresh: () => refresh2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/resource.js
var ResourceSymbolKey = "effect/Resource";
var ResourceTypeId = Symbol.for(ResourceSymbolKey);
var resourceVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var proto = {
  ...CommitPrototype,
  commit() {
    return get20(this);
  },
  [ResourceTypeId]: resourceVariance
};
var auto3 = (acquire4, policy) => tap(manual3(acquire4), (manual5) => acquireRelease(pipe(refresh(manual5), schedule_Effect(policy), interruptible2, forkDaemon), interruptFiber));
var manual3 = (acquire4) => flatMap(context(), (env) => pipe(fromAcquire(exit(acquire4)), map5((ref) => {
  const resource = Object.create(proto);
  resource.scopedRef = ref;
  resource.acquire = provideContext(acquire4, env);
  return resource;
})));
var get20 = (self) => flatMap(get7(self.scopedRef), identity);
var refresh = (self) => set7(self.scopedRef, map5(self.acquire, exitSucceed));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Resource.js
var ResourceTypeId2 = ResourceTypeId;
var auto4 = auto3;
var get21 = get20;
var manual4 = manual3;
var refresh2 = refresh;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RuntimeFlags.js
var RuntimeFlags_exports = {};
__export(RuntimeFlags_exports, {
  CooperativeYielding: () => CooperativeYielding2,
  Interruption: () => Interruption2,
  None: () => None2,
  OpSupervision: () => OpSupervision2,
  RuntimeMetrics: () => RuntimeMetrics2,
  WindDown: () => WindDown2,
  cooperativeYielding: () => cooperativeYielding2,
  diff: () => diff3,
  differ: () => differ2,
  disable: () => disable2,
  disableAll: () => disableAll2,
  disableCooperativeYielding: () => disableCooperativeYielding2,
  disableInterruption: () => disableInterruption2,
  disableOpSupervision: () => disableOpSupervision2,
  disableRuntimeMetrics: () => disableRuntimeMetrics2,
  disableWindDown: () => disableWindDown2,
  enable: () => enable2,
  enableAll: () => enableAll2,
  enableCooperativeYielding: () => enableCooperativeYielding2,
  enableInterruption: () => enableInterruption2,
  enableOpSupervision: () => enableOpSupervision2,
  enableRuntimeMetrics: () => enableRuntimeMetrics2,
  enableWindDown: () => enableWindDown2,
  interruptible: () => interruptible4,
  interruption: () => interruption2,
  isDisabled: () => isDisabled2,
  isEnabled: () => isEnabled2,
  make: () => make55,
  none: () => none9,
  opSupervision: () => opSupervision2,
  patch: () => patch3,
  render: () => render2,
  runtimeMetrics: () => runtimeMetrics2,
  toSet: () => toSet2,
  windDown: () => windDown2
});
var None2 = None;
var Interruption2 = Interruption;
var OpSupervision2 = OpSupervision;
var RuntimeMetrics2 = RuntimeMetrics;
var WindDown2 = WindDown;
var CooperativeYielding2 = CooperativeYielding;
var cooperativeYielding2 = cooperativeYielding;
var diff3 = diff;
var differ2 = differ;
var disable2 = disable;
var disableAll2 = disableAll;
var disableCooperativeYielding2 = disableCooperativeYielding;
var disableInterruption2 = disableInterruption;
var disableOpSupervision2 = disableOpSupervision;
var disableRuntimeMetrics2 = disableRuntimeMetrics;
var disableWindDown2 = disableWindDown;
var enable2 = enable;
var enableAll2 = enableAll;
var enableCooperativeYielding2 = enableCooperativeYielding;
var enableInterruption2 = enableInterruption;
var enableOpSupervision2 = enableOpSupervision;
var enableRuntimeMetrics2 = enableRuntimeMetrics;
var enableWindDown2 = enableWindDown;
var interruptible4 = interruptible;
var interruption2 = interruption;
var isEnabled2 = isEnabled;
var isDisabled2 = isDisabled;
var make55 = make6;
var none9 = none4;
var opSupervision2 = opSupervision;
var patch3 = patch;
var render2 = render;
var runtimeMetrics2 = runtimeMetrics;
var toSet2 = toSet;
var windDown2 = windDown;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/STM.js
var STM_exports = {};
__export(STM_exports, {
  Do: () => Do,
  STMTypeId: () => STMTypeId2,
  acquireUseRelease: () => acquireUseRelease3,
  all: () => all5,
  as: () => as5,
  asSome: () => asSome3,
  asSomeError: () => asSomeError2,
  asVoid: () => asVoid4,
  attempt: () => attempt2,
  bind: () => bind2,
  bindTo: () => bindTo2,
  catchAll: () => catchAll2,
  catchSome: () => catchSome2,
  catchTag: () => catchTag2,
  catchTags: () => catchTags2,
  check: () => check2,
  collect: () => collect2,
  collectSTM: () => collectSTM2,
  commit: () => commit2,
  commitEither: () => commitEither2,
  cond: () => cond2,
  context: () => context5,
  contextWith: () => contextWith4,
  contextWithSTM: () => contextWithSTM2,
  die: () => die5,
  dieMessage: () => dieMessage4,
  dieSync: () => dieSync3,
  either: () => either2,
  ensuring: () => ensuring5,
  eventually: () => eventually3,
  every: () => every3,
  exists: () => exists2,
  fail: () => fail5,
  failSync: () => failSync3,
  fiberId: () => fiberId3,
  filter: () => filter7,
  filterNot: () => filterNot2,
  filterOrDie: () => filterOrDie2,
  filterOrDieMessage: () => filterOrDieMessage2,
  filterOrElse: () => filterOrElse2,
  filterOrFail: () => filterOrFail3,
  firstSuccessOf: () => firstSuccessOf,
  flatMap: () => flatMap5,
  flatten: () => flatten4,
  flip: () => flip2,
  flipWith: () => flipWith2,
  forEach: () => forEach4,
  fromEither: () => fromEither2,
  fromOption: () => fromOption2,
  gen: () => gen4,
  head: () => head3,
  if: () => if_2,
  ignore: () => ignore2,
  interrupt: () => interrupt4,
  interruptAs: () => interruptAs3,
  isFailure: () => isFailure4,
  isSTM: () => isSTM2,
  isSuccess: () => isSuccess4,
  iterate: () => iterate2,
  let: () => let_2,
  loop: () => loop2,
  map: () => map15,
  mapAttempt: () => mapAttempt2,
  mapBoth: () => mapBoth2,
  mapError: () => mapError3,
  mapInputContext: () => mapInputContext5,
  match: () => match15,
  matchSTM: () => matchSTM2,
  merge: () => merge5,
  mergeAll: () => mergeAll2,
  negate: () => negate2,
  none: () => none10,
  option: () => option2,
  orDie: () => orDie2,
  orDieWith: () => orDieWith2,
  orElse: () => orElse5,
  orElseEither: () => orElseEither2,
  orElseFail: () => orElseFail2,
  orElseOptional: () => orElseOptional2,
  orElseSucceed: () => orElseSucceed2,
  orTry: () => orTry2,
  partition: () => partition3,
  provideContext: () => provideContext6,
  provideService: () => provideService2,
  provideServiceSTM: () => provideServiceSTM2,
  provideSomeContext: () => provideSomeContext2,
  reduce: () => reduce8,
  reduceAll: () => reduceAll2,
  reduceRight: () => reduceRight2,
  refineOrDie: () => refineOrDie3,
  refineOrDieWith: () => refineOrDieWith3,
  reject: () => reject2,
  rejectSTM: () => rejectSTM2,
  repeatUntil: () => repeatUntil2,
  repeatWhile: () => repeatWhile2,
  replicate: () => replicate2,
  replicateSTM: () => replicateSTM2,
  replicateSTMDiscard: () => replicateSTMDiscard2,
  retry: () => retry4,
  retryUntil: () => retryUntil2,
  retryWhile: () => retryWhile2,
  some: () => some5,
  succeed: () => succeed10,
  succeedNone: () => succeedNone3,
  succeedSome: () => succeedSome2,
  summarized: () => summarized3,
  suspend: () => suspend5,
  sync: () => sync9,
  tap: () => tap4,
  tapBoth: () => tapBoth2,
  tapError: () => tapError2,
  try: () => try_2,
  unless: () => unless2,
  unlessSTM: () => unlessSTM2,
  unsome: () => unsome2,
  validateAll: () => validateAll2,
  validateFirst: () => validateFirst2,
  void: () => void_4,
  when: () => when3,
  whenSTM: () => whenSTM2,
  zip: () => zip10,
  zipLeft: () => zipLeft6,
  zipRight: () => zipRight7,
  zipWith: () => zipWith4
});
var STMTypeId2 = STMTypeId;
var isSTM2 = isSTM;
var acquireUseRelease3 = acquireUseRelease2;
var all5 = all3;
var as5 = as4;
var asSome3 = asSome2;
var asSomeError2 = asSomeError;
var asVoid4 = asVoid3;
var attempt2 = attempt;
var catchAll2 = catchAll;
var catchSome2 = catchSome;
var catchTag2 = catchTag;
var catchTags2 = catchTags;
var check2 = check;
var collect2 = collect;
var collectSTM2 = collectSTM;
var commit2 = commit;
var commitEither2 = commitEither;
var cond2 = cond;
var context5 = context4;
var contextWith4 = contextWith3;
var contextWithSTM2 = contextWithSTM;
var mapInputContext5 = mapInputContext2;
var die5 = die3;
var dieMessage4 = dieMessage2;
var dieSync3 = dieSync2;
var either2 = either;
var ensuring5 = ensuring4;
var eventually3 = eventually2;
var every3 = every2;
var exists2 = exists;
var fail5 = fail3;
var failSync3 = failSync2;
var fiberId3 = fiberId2;
var filter7 = filter4;
var filterNot2 = filterNot;
var filterOrDie2 = filterOrDie;
var filterOrDieMessage2 = filterOrDieMessage;
var filterOrElse2 = filterOrElse;
var filterOrFail3 = filterOrFail2;
var flatMap5 = flatMap4;
var flatten4 = flatten3;
var flip2 = flip;
var flipWith2 = flipWith;
var match15 = match8;
var matchSTM2 = matchSTM;
var forEach4 = forEach3;
var fromEither2 = fromEither;
var fromOption2 = fromOption;
var gen4 = gen3;
var head3 = head2;
var if_2 = if_;
var ignore2 = ignore;
var interrupt4 = interrupt3;
var interruptAs3 = interruptAs2;
var isFailure4 = isFailure2;
var isSuccess4 = isSuccess2;
var iterate2 = iterate;
var loop2 = loop;
var map15 = map10;
var mapAttempt2 = mapAttempt;
var mapBoth2 = mapBoth;
var mapError3 = mapError2;
var merge5 = merge3;
var mergeAll2 = mergeAll;
var negate2 = negate;
var none10 = none7;
var option2 = option;
var orDie2 = orDie;
var orDieWith2 = orDieWith;
var orElse5 = orElse3;
var orElseEither2 = orElseEither;
var orElseFail2 = orElseFail;
var orElseOptional2 = orElseOptional;
var orElseSucceed2 = orElseSucceed;
var orTry2 = orTry;
var partition3 = partition2;
var provideContext6 = provideContext3;
var provideSomeContext2 = provideSomeContext;
var provideService2 = provideService;
var provideServiceSTM2 = provideServiceSTM;
var reduce8 = reduce6;
var reduceAll2 = reduceAll;
var reduceRight2 = reduceRight;
var refineOrDie3 = refineOrDie2;
var refineOrDieWith3 = refineOrDieWith2;
var reject2 = reject;
var rejectSTM2 = rejectSTM;
var repeatUntil2 = repeatUntil;
var repeatWhile2 = repeatWhile;
var replicate2 = replicate;
var replicateSTM2 = replicateSTM;
var replicateSTMDiscard2 = replicateSTMDiscard;
var retry4 = retry;
var retryUntil2 = retryUntil;
var retryWhile2 = retryWhile;
var some5 = some4;
var succeed10 = succeed7;
var succeedNone3 = succeedNone2;
var succeedSome2 = succeedSome;
var summarized3 = summarized2;
var suspend5 = suspend4;
var sync9 = sync6;
var tap4 = tap2;
var tapBoth2 = tapBoth;
var tapError2 = tapError;
var try_2 = try_;
var unless2 = unless;
var unlessSTM2 = unlessSTM;
var unsome2 = unsome;
var void_4 = void_3;
var validateAll2 = validateAll;
var validateFirst2 = validateFirst;
var when3 = when2;
var whenSTM2 = whenSTM;
var zip10 = zip5;
var zipLeft6 = zipLeft4;
var zipRight7 = zipRight5;
var zipWith4 = zipWith3;
var firstSuccessOf = (effects) => suspend5(() => {
  const list = fromIterable2(effects);
  if (!isNonEmpty(list)) {
    return dieSync3(() => new IllegalArgumentException(`Received an empty collection of effects`));
  }
  return reduce2(tailNonEmpty(list), headNonEmpty(list), (left2, right2) => orElse5(left2, () => right2));
});
var Do = succeed10({});
var bind2 = bind;
var let_2 = let_;
var bindTo2 = bindTo;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScopedCache.js
var ScopedCache_exports = {};
__export(ScopedCache_exports, {
  ScopedCacheTypeId: () => ScopedCacheTypeId2,
  make: () => make57,
  makeWith: () => makeWith6
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/scopedCache.js
var makeCacheState = (map22, keys10, accesses, updating, hits, misses) => ({
  map: map22,
  keys: keys10,
  accesses,
  updating,
  hits,
  misses
});
var initialCacheState = () => makeCacheState(empty9(), makeKeySet(), unbounded(), make2(false), 0, 0);
var complete2 = (key, exit3, ownerCount, entryStats, timeToLive) => struct({
  _tag: "Complete",
  key,
  exit: exit3,
  ownerCount,
  entryStats,
  timeToLive
});
var pending = (key, scoped3) => struct({
  _tag: "Pending",
  key,
  scoped: scoped3
});
var refreshing = (scoped3, complete3) => struct({
  _tag: "Refreshing",
  scoped: scoped3,
  complete: complete3
});
var toScoped = (self) => matchEffect(self.exit, {
  onFailure: (cause) => failCause(cause),
  onSuccess: ([value6]) => acquireRelease(as(sync(() => incrementAndGet(self.ownerCount)), value6), () => releaseOwner(self))
});
var releaseOwner = (self) => matchEffect(self.exit, {
  onFailure: () => void_,
  onSuccess: ([, finalizer]) => flatMap(sync(() => decrementAndGet(self.ownerCount)), (numOwner) => when(finalizer(void_2), () => numOwner === 0))
});
var ScopedCacheSymbolKey = "effect/ScopedCache";
var ScopedCacheTypeId = Symbol.for(ScopedCacheSymbolKey);
var scopedCacheVariance = {
  /* c8 ignore next */
  _Key: (_) => _,
  /* c8 ignore next */
  _Error: (_) => _,
  /* c8 ignore next */
  _Value: (_) => _
};
var ScopedCacheImpl = class {
  capacity;
  scopedLookup;
  clock;
  timeToLive;
  context;
  [ScopedCacheTypeId] = scopedCacheVariance;
  cacheState;
  constructor(capacity, scopedLookup, clock2, timeToLive, context7) {
    this.capacity = capacity;
    this.scopedLookup = scopedLookup;
    this.clock = clock2;
    this.timeToLive = timeToLive;
    this.context = context7;
    this.cacheState = initialCacheState();
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  get cacheStats() {
    return sync(() => makeCacheStats({
      hits: this.cacheState.hits,
      misses: this.cacheState.misses,
      size: size2(this.cacheState.map)
    }));
  }
  getOption(key) {
    return suspend(() => match2(get6(this.cacheState.map, key), {
      onNone: () => succeedNone,
      onSome: (value6) => flatten2(this.resolveMapValue(value6))
    }));
  }
  getOptionComplete(key) {
    return suspend(() => match2(get6(this.cacheState.map, key), {
      onNone: () => succeedNone,
      onSome: (value6) => flatten2(this.resolveMapValue(value6, true))
    }));
  }
  contains(key) {
    return sync(() => has4(this.cacheState.map, key));
  }
  entryStats(key) {
    return sync(() => {
      const value6 = getOrUndefined(get6(this.cacheState.map, key));
      if (value6 === void 0) {
        return none();
      }
      switch (value6._tag) {
        case "Complete": {
          return some(makeEntryStats(value6.entryStats.loadedMillis));
        }
        case "Pending": {
          return none();
        }
        case "Refreshing": {
          return some(makeEntryStats(value6.complete.entryStats.loadedMillis));
        }
      }
    });
  }
  get(key) {
    return pipe(this.lookupValueOf(key), memoize, flatMap((lookupValue) => suspend(() => {
      let k = void 0;
      let value6 = getOrUndefined(get6(this.cacheState.map, key));
      if (value6 === void 0) {
        k = makeMapKey(key);
        if (has4(this.cacheState.map, key)) {
          value6 = getOrUndefined(get6(this.cacheState.map, key));
        } else {
          set5(this.cacheState.map, key, pending(k, lookupValue));
        }
      }
      if (value6 === void 0) {
        this.trackMiss();
        return zipRight(this.ensureMapSizeNotExceeded(k), lookupValue);
      }
      return map5(this.resolveMapValue(value6), flatMap(match2({
        onNone: () => {
          const val = value6;
          const current = getOrUndefined(get6(this.cacheState.map, key));
          if (equals(current, value6)) {
            remove2(this.cacheState.map, key);
          }
          return pipe(this.ensureMapSizeNotExceeded(val.key), zipRight(releaseOwner(val)), zipRight(this.get(key)));
        },
        onSome: succeed
      })));
    })), flatten2);
  }
  invalidate(key) {
    return suspend(() => {
      if (has4(this.cacheState.map, key)) {
        const mapValue = getOrUndefined(get6(this.cacheState.map, key));
        remove2(this.cacheState.map, key);
        switch (mapValue._tag) {
          case "Complete": {
            return releaseOwner(mapValue);
          }
          case "Pending": {
            return void_;
          }
          case "Refreshing": {
            return releaseOwner(mapValue.complete);
          }
        }
      }
      return void_;
    });
  }
  get invalidateAll() {
    return forEachConcurrentDiscard(fromIterable3(Array.from(this.cacheState.map).map(([key]) => key)), (key) => this.invalidate(key), false, false);
  }
  refresh(key) {
    return pipe(this.lookupValueOf(key), memoize, flatMap((scoped3) => {
      let value6 = getOrUndefined(get6(this.cacheState.map, key));
      let newKey = void 0;
      if (value6 === void 0) {
        newKey = makeMapKey(key);
        if (has4(this.cacheState.map, key)) {
          value6 = getOrUndefined(get6(this.cacheState.map, key));
        } else {
          set5(this.cacheState.map, key, pending(newKey, scoped3));
        }
      }
      let finalScoped;
      if (value6 === void 0) {
        finalScoped = zipRight(this.ensureMapSizeNotExceeded(newKey), scoped3);
      } else {
        switch (value6._tag) {
          case "Complete": {
            if (this.hasExpired(value6.timeToLive)) {
              finalScoped = succeed(this.get(key));
            } else {
              const current = getOrUndefined(get6(this.cacheState.map, key));
              if (equals(current, value6)) {
                const mapValue = refreshing(scoped3, value6);
                set5(this.cacheState.map, key, mapValue);
                finalScoped = scoped3;
              } else {
                finalScoped = succeed(this.get(key));
              }
            }
            break;
          }
          case "Pending": {
            finalScoped = value6.scoped;
            break;
          }
          case "Refreshing": {
            finalScoped = value6.scoped;
            break;
          }
        }
      }
      return flatMap(finalScoped, (s) => scopedEffect(asVoid(s)));
    }));
  }
  get size() {
    return sync(() => size2(this.cacheState.map));
  }
  resolveMapValue(value6, ignorePending = false) {
    switch (value6._tag) {
      case "Complete": {
        this.trackHit();
        if (this.hasExpired(value6.timeToLive)) {
          return succeed(succeedNone);
        }
        return as(this.ensureMapSizeNotExceeded(value6.key), asSome(toScoped(value6)));
      }
      case "Pending": {
        this.trackHit();
        if (ignorePending) {
          return succeed(succeedNone);
        }
        return zipRight(this.ensureMapSizeNotExceeded(value6.key), map5(value6.scoped, asSome));
      }
      case "Refreshing": {
        this.trackHit();
        if (this.hasExpired(value6.complete.timeToLive)) {
          if (ignorePending) {
            return succeed(succeedNone);
          }
          return zipRight(this.ensureMapSizeNotExceeded(value6.complete.key), map5(value6.scoped, asSome));
        }
        return as(this.ensureMapSizeNotExceeded(value6.complete.key), asSome(toScoped(value6.complete)));
      }
    }
  }
  lookupValueOf(key) {
    return pipe(onInterrupt(flatMap(make15(), (scope) => pipe(this.scopedLookup(key), provideContext(pipe(this.context, add(Scope, scope))), exit, map5((exit3) => [exit3, (exit4) => close(scope, exit4)]))), () => sync(() => remove2(this.cacheState.map, key))), flatMap(([exit3, release4]) => {
      const now = this.clock.unsafeCurrentTimeMillis();
      const expiredAt = now + toMillis(this.timeToLive(exit3));
      switch (exit3._tag) {
        case "Success": {
          const exitWithFinalizer = succeed2([exit3.value, release4]);
          const completedResult = complete2(makeMapKey(key), exitWithFinalizer, make2(1), makeEntryStats(now), expiredAt);
          let previousValue = void 0;
          if (has4(this.cacheState.map, key)) {
            previousValue = getOrUndefined(get6(this.cacheState.map, key));
          }
          set5(this.cacheState.map, key, completedResult);
          return sync(() => flatten2(as(this.cleanMapValue(previousValue), toScoped(completedResult))));
        }
        case "Failure": {
          const completedResult = complete2(makeMapKey(key), exit3, make2(0), makeEntryStats(now), expiredAt);
          let previousValue = void 0;
          if (has4(this.cacheState.map, key)) {
            previousValue = getOrUndefined(get6(this.cacheState.map, key));
          }
          set5(this.cacheState.map, key, completedResult);
          return zipRight(release4(exit3), sync(() => flatten2(as(this.cleanMapValue(previousValue), toScoped(completedResult)))));
        }
      }
    }), memoize, flatten2);
  }
  hasExpired(timeToLive) {
    return this.clock.unsafeCurrentTimeMillis() > timeToLive;
  }
  trackHit() {
    this.cacheState.hits = this.cacheState.hits + 1;
  }
  trackMiss() {
    this.cacheState.misses = this.cacheState.misses + 1;
  }
  trackAccess(key) {
    const cleanedKeys = [];
    offer(this.cacheState.accesses, key);
    if (compareAndSet(this.cacheState.updating, false, true)) {
      let loop3 = true;
      while (loop3) {
        const key2 = poll(this.cacheState.accesses, EmptyMutableQueue);
        if (key2 === EmptyMutableQueue) {
          loop3 = false;
        } else {
          this.cacheState.keys.add(key2);
        }
      }
      let size18 = size2(this.cacheState.map);
      loop3 = size18 > this.capacity;
      while (loop3) {
        const key2 = this.cacheState.keys.remove();
        if (key2 === void 0) {
          loop3 = false;
        } else {
          if (has4(this.cacheState.map, key2.current)) {
            const removed = getOrUndefined(get6(this.cacheState.map, key2.current));
            remove2(this.cacheState.map, key2.current);
            size18 = size18 - 1;
            cleanedKeys.push(removed);
            loop3 = size18 > this.capacity;
          }
        }
      }
      set(this.cacheState.updating, false);
    }
    return cleanedKeys;
  }
  cleanMapValue(mapValue) {
    if (mapValue === void 0) {
      return void_;
    }
    switch (mapValue._tag) {
      case "Complete": {
        return releaseOwner(mapValue);
      }
      case "Pending": {
        return void_;
      }
      case "Refreshing": {
        return releaseOwner(mapValue.complete);
      }
    }
  }
  ensureMapSizeNotExceeded(key) {
    return forEachConcurrentDiscard(this.trackAccess(key), (cleanedMapValue) => this.cleanMapValue(cleanedMapValue), false, false);
  }
};
var make56 = (options) => {
  const timeToLive = decode(options.timeToLive);
  return makeWith5({
    capacity: options.capacity,
    lookup: options.lookup,
    timeToLive: () => timeToLive
  });
};
var makeWith5 = (options) => flatMap(clock, (clock2) => buildWith(options.capacity, options.lookup, clock2, (exit3) => decode(options.timeToLive(exit3))));
var buildWith = (capacity, scopedLookup, clock2, timeToLive) => acquireRelease(flatMap(context(), (context7) => sync(() => new ScopedCacheImpl(capacity, scopedLookup, clock2, timeToLive, context7))), (cache) => cache.invalidateAll);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScopedCache.js
var ScopedCacheTypeId2 = ScopedCacheTypeId;
var make57 = make56;
var makeWith6 = makeWith5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Secret.js
var Secret_exports = {};
__export(Secret_exports, {
  SecretTypeId: () => SecretTypeId2,
  fromIterable: () => fromIterable10,
  fromString: () => fromString2,
  isSecret: () => isSecret2,
  make: () => make58,
  unsafeWipe: () => unsafeWipe2,
  value: () => value5
});
var SecretTypeId2 = SecretTypeId;
var isSecret2 = isSecret;
var make58 = make22;
var fromIterable10 = fromIterable7;
var fromString2 = fromString;
var value5 = value2;
var unsafeWipe2 = unsafeWipe;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SingleProducerAsyncInput.js
var SingleProducerAsyncInput_exports = {};
__export(SingleProducerAsyncInput_exports, {
  make: () => make59
});
var make59 = make17;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Sink.js
var Sink_exports = {};
__export(Sink_exports, {
  SinkTypeId: () => SinkTypeId2,
  as: () => as6,
  collectAll: () => collectAll4,
  collectAllFrom: () => collectAllFrom2,
  collectAllN: () => collectAllN2,
  collectAllToMap: () => collectAllToMap2,
  collectAllToMapN: () => collectAllToMapN2,
  collectAllToSet: () => collectAllToSet2,
  collectAllToSetN: () => collectAllToSetN2,
  collectAllUntil: () => collectAllUntil2,
  collectAllUntilEffect: () => collectAllUntilEffect2,
  collectAllWhile: () => collectAllWhile2,
  collectAllWhileEffect: () => collectAllWhileEffect2,
  collectAllWhileWith: () => collectAllWhileWith2,
  collectLeftover: () => collectLeftover2,
  context: () => context6,
  contextWith: () => contextWith5,
  contextWithEffect: () => contextWithEffect2,
  contextWithSink: () => contextWithSink2,
  count: () => count4,
  die: () => die6,
  dieMessage: () => dieMessage5,
  dieSync: () => dieSync4,
  dimap: () => dimap2,
  dimapChunks: () => dimapChunks2,
  dimapChunksEffect: () => dimapChunksEffect2,
  dimapEffect: () => dimapEffect2,
  drain: () => drain2,
  drop: () => drop2,
  dropUntil: () => dropUntil2,
  dropUntilEffect: () => dropUntilEffect2,
  dropWhile: () => dropWhile2,
  dropWhileEffect: () => dropWhileEffect2,
  ensuring: () => ensuring6,
  ensuringWith: () => ensuringWith2,
  every: () => every4,
  fail: () => fail6,
  failCause: () => failCause5,
  failCauseSync: () => failCauseSync2,
  failSync: () => failSync4,
  filterInput: () => filterInput2,
  filterInputEffect: () => filterInputEffect2,
  findEffect: () => findEffect2,
  flatMap: () => flatMap6,
  fold: () => fold2,
  foldChunks: () => foldChunks2,
  foldChunksEffect: () => foldChunksEffect2,
  foldEffect: () => foldEffect2,
  foldLeft: () => foldLeft2,
  foldLeftChunks: () => foldLeftChunks2,
  foldLeftChunksEffect: () => foldLeftChunksEffect2,
  foldLeftEffect: () => foldLeftEffect2,
  foldSink: () => foldSink2,
  foldUntil: () => foldUntil2,
  foldUntilEffect: () => foldUntilEffect2,
  foldWeighted: () => foldWeighted2,
  foldWeightedDecompose: () => foldWeightedDecompose2,
  foldWeightedDecomposeEffect: () => foldWeightedDecomposeEffect2,
  foldWeightedEffect: () => foldWeightedEffect2,
  forEach: () => forEach5,
  forEachChunk: () => forEachChunk2,
  forEachChunkWhile: () => forEachChunkWhile2,
  forEachWhile: () => forEachWhile2,
  fromChannel: () => fromChannel2,
  fromEffect: () => fromEffect6,
  fromPubSub: () => fromPubSub3,
  fromPush: () => fromPush2,
  fromQueue: () => fromQueue2,
  head: () => head4,
  ignoreLeftover: () => ignoreLeftover2,
  last: () => last3,
  leftover: () => leftover2,
  map: () => map16,
  mapEffect: () => mapEffect3,
  mapError: () => mapError4,
  mapInput: () => mapInput7,
  mapInputChunks: () => mapInputChunks2,
  mapInputChunksEffect: () => mapInputChunksEffect2,
  mapInputEffect: () => mapInputEffect2,
  mapLeftover: () => mapLeftover2,
  mkString: () => mkString2,
  never: () => never5,
  orElse: () => orElse6,
  provideContext: () => provideContext7,
  race: () => race5,
  raceBoth: () => raceBoth2,
  raceWith: () => raceWith2,
  refineOrDie: () => refineOrDie4,
  refineOrDieWith: () => refineOrDieWith4,
  some: () => some6,
  splitWhere: () => splitWhere2,
  succeed: () => succeed11,
  sum: () => sum2,
  summarized: () => summarized4,
  suspend: () => suspend6,
  sync: () => sync10,
  take: () => take2,
  timed: () => timed2,
  toChannel: () => toChannel3,
  unwrap: () => unwrap4,
  unwrapScoped: () => unwrapScoped4,
  unwrapScopedWith: () => unwrapScopedWith2,
  withDuration: () => withDuration2,
  zip: () => zip11,
  zipLeft: () => zipLeft7,
  zipRight: () => zipRight8,
  zipWith: () => zipWith5
});
var SinkTypeId2 = SinkTypeId;
var as6 = as3;
var collectAll4 = collectAll;
var collectAllN2 = collectAllN;
var collectAllFrom2 = collectAllFrom;
var collectAllToMap2 = collectAllToMap;
var collectAllToMapN2 = collectAllToMapN;
var collectAllToSet2 = collectAllToSet;
var collectAllToSetN2 = collectAllToSetN;
var collectAllUntil2 = collectAllUntil;
var collectAllUntilEffect2 = collectAllUntilEffect;
var collectAllWhile2 = collectAllWhile;
var collectAllWhileEffect2 = collectAllWhileEffect;
var collectAllWhileWith2 = collectAllWhileWith;
var collectLeftover2 = collectLeftover;
var mapInput7 = mapInput4;
var mapInputEffect2 = mapInputEffect;
var mapInputChunks2 = mapInputChunks;
var mapInputChunksEffect2 = mapInputChunksEffect;
var count4 = count2;
var die6 = die2;
var dieMessage5 = dieMessage;
var dieSync4 = dieSync;
var dimap2 = dimap;
var dimapEffect2 = dimapEffect;
var dimapChunks2 = dimapChunks;
var dimapChunksEffect2 = dimapChunksEffect;
var drain2 = drain;
var drop2 = drop;
var dropUntil2 = dropUntil;
var dropUntilEffect2 = dropUntilEffect;
var dropWhile2 = dropWhile;
var dropWhileEffect2 = dropWhileEffect;
var ensuring6 = ensuring3;
var ensuringWith2 = ensuringWith;
var context6 = context3;
var contextWith5 = contextWith2;
var contextWithEffect2 = contextWithEffect;
var contextWithSink2 = contextWithSink;
var every4 = every;
var fail6 = fail2;
var failSync4 = failSync;
var failCause5 = failCause3;
var failCauseSync2 = failCauseSync;
var filterInput2 = filterInput;
var filterInputEffect2 = filterInputEffect;
var findEffect2 = findEffect;
var fold2 = fold;
var foldSink2 = foldSink;
var foldChunks2 = foldChunks;
var foldChunksEffect2 = foldChunksEffect;
var foldEffect2 = foldEffect;
var foldLeft2 = foldLeft;
var foldLeftChunks2 = foldLeftChunks;
var foldLeftChunksEffect2 = foldLeftChunksEffect;
var foldLeftEffect2 = foldLeftEffect;
var foldUntil2 = foldUntil;
var foldUntilEffect2 = foldUntilEffect;
var foldWeighted2 = foldWeighted;
var foldWeightedDecompose2 = foldWeightedDecompose;
var foldWeightedDecomposeEffect2 = foldWeightedDecomposeEffect;
var foldWeightedEffect2 = foldWeightedEffect;
var forEach5 = forEach2;
var forEachChunk2 = forEachChunk;
var forEachChunkWhile2 = forEachChunkWhile;
var forEachWhile2 = forEachWhile;
var flatMap6 = flatMap3;
var fromChannel2 = fromChannel;
var toChannel3 = toChannel;
var fromEffect6 = fromEffect2;
var fromPubSub3 = fromPubSub;
var fromPush2 = fromPush;
var fromQueue2 = fromQueue;
var head4 = head;
var ignoreLeftover2 = ignoreLeftover;
var last3 = last2;
var leftover2 = leftover;
var map16 = map9;
var mapEffect3 = mapEffect;
var mapError4 = mapError;
var mapLeftover2 = mapLeftover;
var mkString2 = mkString;
var never5 = never2;
var orElse6 = orElse2;
var provideContext7 = provideContext2;
var race5 = race2;
var raceBoth2 = raceBoth;
var raceWith2 = raceWith;
var refineOrDie4 = refineOrDie;
var refineOrDieWith4 = refineOrDieWith;
var some6 = some3;
var splitWhere2 = splitWhere;
var succeed11 = succeed6;
var sum2 = sum;
var summarized4 = summarized;
var suspend6 = suspend3;
var sync10 = sync5;
var take2 = take;
var timed2 = timed;
var unwrap4 = unwrap;
var unwrapScoped4 = unwrapScoped2;
var unwrapScopedWith2 = unwrapScopedWith;
var withDuration2 = withDuration;
var zip11 = zip4;
var zipLeft7 = zipLeft3;
var zipRight8 = zipRight4;
var zipWith5 = zipWith2;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SortedMap.js
var SortedMap_exports = {};
__export(SortedMap_exports, {
  empty: () => empty16,
  entries: () => entries2,
  fromIterable: () => fromIterable11,
  get: () => get22,
  getOrder: () => getOrder2,
  has: () => has11,
  headOption: () => headOption,
  isEmpty: () => isEmpty2,
  isNonEmpty: () => isNonEmpty2,
  isSortedMap: () => isSortedMap,
  keys: () => keys5,
  lastOption: () => lastOption,
  make: () => make60,
  map: () => map17,
  partition: () => partition4,
  reduce: () => reduce9,
  remove: () => remove7,
  set: () => set12,
  size: () => size6,
  values: () => values3
});
var TypeId17 = /* @__PURE__ */ Symbol.for("effect/SortedMap");
var SortedMapProto = {
  [TypeId17]: {
    _K: (_) => _,
    _V: (_) => _
  },
  [symbol2]() {
    return pipe(hash(this.tree), combine(hash("effect/SortedMap")), cached(this));
  },
  [symbol3](that) {
    return isSortedMap(that) && equals(this.tree, that.tree);
  },
  [Symbol.iterator]() {
    return this.tree[Symbol.iterator]();
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "SortedMap",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl2 = (tree) => {
  const self = Object.create(SortedMapProto);
  self.tree = tree;
  return self;
};
var isSortedMap = (u) => hasProperty(u, TypeId17);
var empty16 = (ord) => makeImpl2(empty10(ord));
var fromIterable11 = dual(2, (iterable, ord) => makeImpl2(fromIterable6(iterable, ord)));
var make60 = (ord) => (...entries5) => fromIterable11(ord)(entries5);
var isEmpty2 = (self) => size6(self) === 0;
var isNonEmpty2 = (self) => size6(self) > 0;
var get22 = dual(2, (self, key) => findFirst2(self.tree, key));
var getOrder2 = (self) => getOrder(self.tree);
var has11 = dual(2, (self, key) => isSome(get22(self, key)));
var headOption = (self) => first(self.tree);
var map17 = dual(2, (self, f) => reduce9(self, empty16(getOrder(self.tree)), (acc, v, k) => set12(acc, k, f(v, k))));
var reduce9 = dual(3, (self, zero, f) => reduce5(self.tree, zero, f));
var remove7 = dual(2, (self, key) => makeImpl2(removeFirst(self.tree, key)));
var set12 = dual(3, (self, key, value6) => has5(self.tree, key) ? makeImpl2(insert(removeFirst(self.tree, key), key, value6)) : makeImpl2(insert(self.tree, key, value6)));
var size6 = (self) => size3(self.tree);
var keys5 = (self) => keys2(self.tree);
var values3 = (self) => values(self.tree);
var entries2 = (self) => {
  const iterator = self.tree[Symbol.iterator]();
  iterator[Symbol.iterator] = () => entries2(self);
  return iterator;
};
var lastOption = (self) => last(self.tree);
var partition4 = dual(2, (self, predicate) => {
  const ord = getOrder(self.tree);
  let right2 = empty16(ord);
  let left2 = empty16(ord);
  for (const value6 of self) {
    if (predicate(value6[0])) {
      right2 = set12(right2, value6[0], value6[1]);
    } else {
      left2 = set12(left2, value6[0], value6[1]);
    }
  }
  return [left2, right2];
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/StreamEmit.js
var StreamEmit_exports = {};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Streamable.js
var Streamable_exports = {};
__export(Streamable_exports, {
  Class: () => Class3
});
var streamVariance = {
  /* c8 ignore next */
  _R: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var Class3 = class {
  /**
   * @since 2.0.0
   */
  [StreamTypeId] = streamVariance;
  /**
   * @since 2.0.0
   */
  pipe() {
    return pipeArguments(this, arguments);
  }
  /**
   * @internal
   */
  get channel() {
    return toChannel2(this.toStream());
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Subscribable.js
var Subscribable_exports = {};
__export(Subscribable_exports, {
  TypeId: () => TypeId18,
  isSubscribable: () => isSubscribable,
  make: () => make61,
  map: () => map18,
  mapEffect: () => mapEffect4,
  unwrap: () => unwrap5
});
var TypeId18 = /* @__PURE__ */ Symbol.for("effect/Subscribable");
var isSubscribable = (u) => hasProperty(u, TypeId18);
var Proto5 = {
  [TypeId]: TypeId,
  [TypeId18]: TypeId18,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make61 = (options) => Object.assign(Object.create(Proto5), options);
var map18 = dual(2, (self, f) => make61({
  get: map8(self.get, f),
  changes: map12(self.changes, f)
}));
var mapEffect4 = dual(2, (self, f) => make61({
  get: flatMap2(self.get, f),
  changes: mapEffect2(self.changes, f)
}));
var unwrap5 = (effect3) => make61({
  get: flatMap2(effect3, (s) => s.get),
  changes: unwrap3(map8(effect3, (s) => s.changes))
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SubscriptionRef.js
var SubscriptionRef_exports = {};
__export(SubscriptionRef_exports, {
  SubscriptionRefTypeId: () => SubscriptionRefTypeId2,
  get: () => get25,
  getAndSet: () => getAndSet5,
  getAndUpdate: () => getAndUpdate5,
  getAndUpdateEffect: () => getAndUpdateEffect3,
  getAndUpdateSome: () => getAndUpdateSome5,
  getAndUpdateSomeEffect: () => getAndUpdateSomeEffect3,
  make: () => make64,
  modify: () => modify8,
  modifyEffect: () => modifyEffect4,
  modifySome: () => modifySome5,
  modifySomeEffect: () => modifySomeEffect3,
  set: () => set15,
  setAndGet: () => setAndGet5,
  update: () => update8,
  updateAndGet: () => updateAndGet5,
  updateAndGetEffect: () => updateAndGetEffect3,
  updateEffect: () => updateEffect3,
  updateSome: () => updateSome5,
  updateSomeAndGet: () => updateSomeAndGet5,
  updateSomeAndGetEffect: () => updateSomeAndGetEffect2,
  updateSomeEffect: () => updateSomeEffect3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SynchronizedRef.js
var SynchronizedRef_exports = {};
__export(SynchronizedRef_exports, {
  SynchronizedRefTypeId: () => SynchronizedRefTypeId,
  get: () => get23,
  getAndSet: () => getAndSet4,
  getAndUpdate: () => getAndUpdate4,
  getAndUpdateEffect: () => getAndUpdateEffect2,
  getAndUpdateSome: () => getAndUpdateSome4,
  getAndUpdateSomeEffect: () => getAndUpdateSomeEffect2,
  make: () => make62,
  modify: () => modify6,
  modifyEffect: () => modifyEffect2,
  modifySome: () => modifySome4,
  modifySomeEffect: () => modifySomeEffect2,
  set: () => set13,
  setAndGet: () => setAndGet4,
  unsafeMake: () => unsafeMake9,
  update: () => update7,
  updateAndGet: () => updateAndGet4,
  updateAndGetEffect: () => updateAndGetEffect2,
  updateEffect: () => updateEffect2,
  updateSome: () => updateSome4,
  updateSomeAndGet: () => updateSomeAndGet4,
  updateSomeAndGetEffect: () => updateSomeAndGetEffect,
  updateSomeEffect: () => updateSomeEffect2
});
var SynchronizedRefTypeId = SynchronizedTypeId;
var make62 = makeSynchronized;
var get23 = get4;
var getAndSet4 = getAndSet;
var getAndUpdate4 = getAndUpdate;
var getAndUpdateEffect2 = getAndUpdateEffect;
var getAndUpdateSome4 = getAndUpdateSome;
var getAndUpdateSomeEffect2 = getAndUpdateSomeEffect;
var modify6 = modify3;
var modifyEffect2 = modifyEffect;
var modifySome4 = modifySome;
var modifySomeEffect2 = modifySomeEffect;
var set13 = set3;
var setAndGet4 = setAndGet;
var update7 = update2;
var updateEffect2 = updateEffect;
var updateAndGet4 = updateAndGet;
var updateAndGetEffect2 = updateAndGetEffect;
var updateSome4 = updateSome;
var updateSomeEffect2 = updateSomeEffect;
var updateSomeAndGet4 = updateSomeAndGet;
var updateSomeAndGetEffect = updateSomeAndGetEffectSynchronized;
var unsafeMake9 = unsafeMakeSynchronized;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/subscriptionRef.js
var SubscriptionRefSymbolKey = "effect/SubscriptionRef";
var SubscriptionRefTypeId = Symbol.for(SubscriptionRefSymbolKey);
var subscriptionRefVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var SubscriptionRefImpl = class extends Class {
  ref;
  pubsub;
  semaphore;
  [TypeId] = TypeId;
  [TypeId18] = TypeId18;
  [RefTypeId] = refVariance;
  [SynchronizedRefTypeId] = synchronizedVariance;
  [SubscriptionRefTypeId] = subscriptionRefVariance;
  constructor(ref, pubsub, semaphore) {
    super();
    this.ref = ref;
    this.pubsub = pubsub;
    this.semaphore = semaphore;
    this.get = get5(this.ref);
  }
  commit() {
    return this.get;
  }
  get;
  get changes() {
    return pipe(get5(this.ref), flatMap2((a) => map8(fromPubSub2(this.pubsub, {
      scoped: true
    }), (s) => concat(make21(a), s))), this.semaphore.withPermits(1), unwrapScoped3);
  }
  modify(f) {
    return this.modifyEffect((a) => succeed5(f(a)));
  }
  modifyEffect(f) {
    return pipe(get5(this.ref), flatMap2(f), flatMap2(([b, a]) => pipe(set4(this.ref, a), as2(b), zipLeft2(publish(this.pubsub, a)))), this.semaphore.withPermits(1));
  }
};
var get24 = (self) => get5(self.ref);
var make63 = (value6) => pipe(all2([unbounded2(), make7(value6), makeSemaphore(1)]), map8(([pubsub, ref, semaphore]) => new SubscriptionRefImpl(ref, pubsub, semaphore)));
var modify7 = dual(2, (self, f) => self.modify(f));
var modifyEffect3 = dual(2, (self, f) => self.modifyEffect(f));
var set14 = dual(2, (self, value6) => pipe(set4(self.ref, value6), zipLeft2(publish(self.pubsub, value6)), self.semaphore.withPermits(1)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SubscriptionRef.js
var SubscriptionRefTypeId2 = SubscriptionRefTypeId;
var get25 = get24;
var getAndSet5 = getAndSet2;
var getAndUpdate5 = getAndUpdate2;
var getAndUpdateEffect3 = getAndUpdateEffect2;
var getAndUpdateSome5 = getAndUpdateSome2;
var getAndUpdateSomeEffect3 = getAndUpdateSomeEffect2;
var make64 = make63;
var modify8 = modify7;
var modifyEffect4 = modifyEffect3;
var modifySome5 = modifySome2;
var modifySomeEffect3 = modifySomeEffect2;
var set15 = set14;
var setAndGet5 = setAndGet2;
var update8 = update3;
var updateEffect3 = updateEffect2;
var updateAndGet5 = updateAndGet2;
var updateAndGetEffect3 = updateAndGetEffect2;
var updateSome5 = updateSome2;
var updateSomeEffect3 = updateSomeEffect2;
var updateSomeAndGet5 = updateSomeAndGet2;
var updateSomeAndGetEffect2 = updateSomeAndGetEffect;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Supervisor.js
var Supervisor_exports = {};
__export(Supervisor_exports, {
  AbstractSupervisor: () => AbstractSupervisor,
  SupervisorTypeId: () => SupervisorTypeId2,
  addSupervisor: () => addSupervisor2,
  fibersIn: () => fibersIn2,
  fromEffect: () => fromEffect7,
  none: () => none11,
  track: () => track2,
  unsafeTrack: () => unsafeTrack2
});
var SupervisorTypeId2 = SupervisorTypeId;
var addSupervisor2 = addSupervisor;
var fibersIn2 = fibersIn;
var fromEffect7 = fromEffect;
var none11 = none6;
var track2 = track;
var unsafeTrack2 = unsafeTrack;
var AbstractSupervisor = class {
  /**
   * @since 2.0.0
   */
  onStart(_context, _effect, _parent, _fiber) {
  }
  /**
   * @since 2.0.0
   */
  onEnd(_value, _fiber) {
  }
  /**
   * @since 2.0.0
   */
  onEffect(_fiber, _effect) {
  }
  /**
   * @since 2.0.0
   */
  onSuspend(_fiber) {
  }
  /**
   * @since 2.0.0
   */
  onResume(_fiber) {
  }
  /**
   * @since 2.0.0
   */
  map(f) {
    return new ProxySupervisor(this, map5(this.value, f));
  }
  /**
   * @since 2.0.0
   */
  zip(right2) {
    return new Zip(this, right2);
  }
  /**
   * @since 2.0.0
   */
  onRun(execution, _fiber) {
    return execution();
  }
  /**
   * @since 2.0.0
   */
  [SupervisorTypeId2] = supervisorVariance;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Symbol.js
var Symbol_exports = {};
__export(Symbol_exports, {
  Equivalence: () => Equivalence,
  isSymbol: () => isSymbol2
});
var isSymbol2 = isSymbol;
var Equivalence = symbol;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TArray.js
var TArray_exports = {};
__export(TArray_exports, {
  TArrayTypeId: () => TArrayTypeId2,
  collectFirst: () => collectFirst2,
  collectFirstSTM: () => collectFirstSTM2,
  contains: () => contains2,
  count: () => count6,
  countSTM: () => countSTM2,
  empty: () => empty18,
  every: () => every6,
  everySTM: () => everySTM2,
  findFirst: () => findFirst4,
  findFirstIndex: () => findFirstIndex2,
  findFirstIndexFrom: () => findFirstIndexFrom2,
  findFirstIndexWhere: () => findFirstIndexWhere2,
  findFirstIndexWhereFrom: () => findFirstIndexWhereFrom2,
  findFirstIndexWhereFromSTM: () => findFirstIndexWhereFromSTM2,
  findFirstIndexWhereSTM: () => findFirstIndexWhereSTM2,
  findFirstSTM: () => findFirstSTM2,
  findLast: () => findLast2,
  findLastIndex: () => findLastIndex2,
  findLastIndexFrom: () => findLastIndexFrom2,
  findLastSTM: () => findLastSTM2,
  forEach: () => forEach7,
  fromIterable: () => fromIterable13,
  get: () => get27,
  headOption: () => headOption3,
  lastOption: () => lastOption3,
  make: () => make66,
  maxOption: () => maxOption2,
  minOption: () => minOption2,
  reduce: () => reduce11,
  reduceOption: () => reduceOption2,
  reduceOptionSTM: () => reduceOptionSTM2,
  reduceSTM: () => reduceSTM2,
  size: () => size8,
  some: () => some8,
  someSTM: () => someSTM2,
  toArray: () => toArray2,
  transform: () => transform2,
  transformSTM: () => transformSTM2,
  update: () => update10,
  updateSTM: () => updateSTM2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tArray.js
var TArraySymbolKey = "effect/TArray";
var TArrayTypeId = Symbol.for(TArraySymbolKey);
var tArrayVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var TArrayImpl = class {
  chunk;
  [TArrayTypeId] = tArrayVariance;
  constructor(chunk3) {
    this.chunk = chunk3;
  }
};
var collectFirst = dual(2, (self, pf) => collectFirstSTM(self, (a) => pipe(pf(a), map(succeed7))));
var collectFirstSTM = dual(2, (self, pf) => withSTMRuntime((runtime4) => {
  let index = 0;
  let result = none();
  while (isNone(result) && index < self.chunk.length) {
    const element = pipe(self.chunk[index], unsafeGet4(runtime4.journal));
    const option3 = pf(element);
    if (isSome(option3)) {
      result = option3;
    }
    index = index + 1;
  }
  return pipe(result, match2({
    onNone: () => succeedNone2,
    onSome: map10(some)
  }));
}));
var contains = dual(2, (self, value6) => some7(self, (a) => equals(a)(value6)));
var count5 = dual(2, (self, predicate) => reduce10(self, 0, (n, a) => predicate(a) ? n + 1 : n));
var countSTM = dual(2, (self, predicate) => reduceSTM(self, 0, (n, a) => map10(predicate(a), (bool) => bool ? n + 1 : n)));
var empty17 = () => fromIterable12([]);
var every5 = dual(2, (self, predicate) => negate(some7(self, (a) => !predicate(a))));
var everySTM = dual(2, (self, predicate) => map10(countSTM(self, predicate), (count7) => count7 === self.chunk.length));
var findFirst3 = dual(2, (self, predicate) => collectFirst(self, (a) => predicate(a) ? some(a) : none()));
var findFirstIndex = dual(2, (self, value6) => findFirstIndexFrom(self, value6, 0));
var findFirstIndexFrom = dual(3, (self, value6, from) => findFirstIndexWhereFrom(self, (a) => equals(a)(value6), from));
var findFirstIndexWhere = dual(2, (self, predicate) => findFirstIndexWhereFrom(self, predicate, 0));
var findFirstIndexWhereFrom = dual(3, (self, predicate, from) => {
  if (from < 0) {
    return succeedNone2;
  }
  return effect2((journal) => {
    let index = from;
    let found = false;
    while (!found && index < self.chunk.length) {
      const element = unsafeGet4(self.chunk[index], journal);
      found = predicate(element);
      index = index + 1;
    }
    if (found) {
      return some(index - 1);
    }
    return none();
  });
});
var findFirstIndexWhereSTM = dual(2, (self, predicate) => findFirstIndexWhereFromSTM(self, predicate, 0));
var findFirstIndexWhereFromSTM = dual(3, (self, predicate, from) => {
  const forIndex = (index) => index < self.chunk.length ? pipe(get9(self.chunk[index]), flatMap4(predicate), flatMap4((bool) => bool ? succeed7(some(index)) : forIndex(index + 1))) : succeedNone2;
  return from < 0 ? succeedNone2 : forIndex(from);
});
var findFirstSTM = dual(2, (self, predicate) => {
  const init = [none(), 0];
  const cont = (state) => isNone(state[0]) && state[1] < self.chunk.length - 1;
  return map10(iterate(init, {
    while: cont,
    body: (state) => {
      const index = state[1];
      return pipe(get9(self.chunk[index]), flatMap4((value6) => map10(predicate(value6), (bool) => [bool ? some(value6) : none(), index + 1])));
    }
  }), (state) => state[0]);
});
var findLast = dual(2, (self, predicate) => effect2((journal) => {
  let index = self.chunk.length - 1;
  let result = none();
  while (isNone(result) && index >= 0) {
    const element = unsafeGet4(self.chunk[index], journal);
    if (predicate(element)) {
      result = some(element);
    }
    index = index - 1;
  }
  return result;
}));
var findLastIndex = dual(2, (self, value6) => findLastIndexFrom(self, value6, self.chunk.length - 1));
var findLastIndexFrom = dual(3, (self, value6, end3) => {
  if (end3 >= self.chunk.length) {
    return succeedNone2;
  }
  return effect2((journal) => {
    let index = end3;
    let found = false;
    while (!found && index >= 0) {
      const element = unsafeGet4(self.chunk[index], journal);
      found = equals(element)(value6);
      index = index - 1;
    }
    if (found) {
      return some(index + 1);
    }
    return none();
  });
});
var findLastSTM = dual(2, (self, predicate) => {
  const init = [none(), self.chunk.length - 1];
  const cont = (state) => isNone(state[0]) && state[1] >= 0;
  return map10(iterate(init, {
    while: cont,
    body: (state) => {
      const index = state[1];
      return pipe(get9(self.chunk[index]), flatMap4((value6) => map10(predicate(value6), (bool) => [bool ? some(value6) : none(), index - 1])));
    }
  }), (state) => state[0]);
});
var forEach6 = dual(2, (self, f) => reduceSTM(self, void 0, (_, a) => f(a)));
var fromIterable12 = (iterable) => map10(forEach3(iterable, make19), (chunk3) => new TArrayImpl(chunk3));
var get26 = dual(2, (self, index) => {
  if (index < 0 || index >= self.chunk.length) {
    return dieMessage2("Index out of bounds");
  }
  return get9(self.chunk[index]);
});
var headOption2 = (self) => self.chunk.length === 0 ? succeed7(none()) : map10(get9(self.chunk[0]), some);
var lastOption2 = (self) => self.chunk.length === 0 ? succeedNone2 : map10(get9(self.chunk[self.chunk.length - 1]), some);
var make65 = (...elements) => fromIterable12(elements);
var maxOption = dual(2, (self, order) => {
  const greaterThan2 = greaterThan(order);
  return reduceOption(self, (acc, curr) => greaterThan2(acc)(curr) ? curr : acc);
});
var minOption = dual(2, (self, order) => {
  const lessThan2 = lessThan(order);
  return reduceOption(self, (acc, curr) => lessThan2(acc)(curr) ? curr : acc);
});
var reduce10 = dual(3, (self, zero, f) => effect2((journal) => {
  let index = 0;
  let result = zero;
  while (index < self.chunk.length) {
    const element = unsafeGet4(self.chunk[index], journal);
    result = f(result, element);
    index = index + 1;
  }
  return result;
}));
var reduceOption = dual(2, (self, f) => effect2((journal) => {
  let index = 0;
  let result = void 0;
  while (index < self.chunk.length) {
    const element = unsafeGet4(self.chunk[index], journal);
    result = result === void 0 ? element : f(result, element);
    index = index + 1;
  }
  return fromNullable(result);
}));
var reduceOptionSTM = dual(2, (self, f) => reduceSTM(self, none(), (acc, curr) => isSome(acc) ? map10(f(acc.value, curr), some) : succeedSome(curr)));
var reduceSTM = dual(3, (self, zero, f) => flatMap4(toArray(self), reduce6(zero, f)));
var size7 = (self) => self.chunk.length;
var some7 = dual(2, (self, predicate) => map10(findFirst3(self, predicate), isSome));
var someSTM = dual(2, (self, predicate) => map10(countSTM(self, predicate), (n) => n > 0));
var toArray = (self) => forEach3(self.chunk, get9);
var transform = dual(2, (self, f) => effect2((journal) => {
  let index = 0;
  while (index < self.chunk.length) {
    const ref = self.chunk[index];
    unsafeSet(ref, f(unsafeGet4(ref, journal)), journal);
    index = index + 1;
  }
  return void 0;
}));
var transformSTM = dual(2, (self, f) => flatMap4(forEach3(self.chunk, (ref) => flatMap4(get9(ref), f)), (chunk3) => effect2((journal) => {
  const iterator = chunk3[Symbol.iterator]();
  let index = 0;
  let next3;
  while ((next3 = iterator.next()) && !next3.done) {
    unsafeSet(self.chunk[index], next3.value, journal);
    index = index + 1;
  }
  return void 0;
})));
var update9 = dual(3, (self, index, f) => {
  if (index < 0 || index >= self.chunk.length) {
    return dieMessage2("Index out of bounds");
  }
  return update5(self.chunk[index], f);
});
var updateSTM = dual(3, (self, index, f) => {
  if (index < 0 || index >= self.chunk.length) {
    return dieMessage2("Index out of bounds");
  }
  return pipe(get9(self.chunk[index]), flatMap4(f), flatMap4((updated) => set8(self.chunk[index], updated)));
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TArray.js
var TArrayTypeId2 = TArrayTypeId;
var collectFirst2 = collectFirst;
var collectFirstSTM2 = collectFirstSTM;
var contains2 = contains;
var count6 = count5;
var countSTM2 = countSTM;
var empty18 = empty17;
var every6 = every5;
var everySTM2 = everySTM;
var findFirst4 = findFirst3;
var findFirstIndex2 = findFirstIndex;
var findFirstIndexFrom2 = findFirstIndexFrom;
var findFirstIndexWhere2 = findFirstIndexWhere;
var findFirstIndexWhereFrom2 = findFirstIndexWhereFrom;
var findFirstIndexWhereSTM2 = findFirstIndexWhereSTM;
var findFirstIndexWhereFromSTM2 = findFirstIndexWhereFromSTM;
var findFirstSTM2 = findFirstSTM;
var findLast2 = findLast;
var findLastIndex2 = findLastIndex;
var findLastIndexFrom2 = findLastIndexFrom;
var findLastSTM2 = findLastSTM;
var forEach7 = forEach6;
var fromIterable13 = fromIterable12;
var get27 = get26;
var headOption3 = headOption2;
var lastOption3 = lastOption2;
var make66 = make65;
var maxOption2 = maxOption;
var minOption2 = minOption;
var reduce11 = reduce10;
var reduceOption2 = reduceOption;
var reduceOptionSTM2 = reduceOptionSTM;
var reduceSTM2 = reduceSTM;
var size8 = size7;
var some8 = some7;
var someSTM2 = someSTM;
var toArray2 = toArray;
var transform2 = transform;
var transformSTM2 = transformSTM;
var update10 = update9;
var updateSTM2 = updateSTM;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TDeferred.js
var TDeferred_exports = {};
__export(TDeferred_exports, {
  TDeferredTypeId: () => TDeferredTypeId2,
  await: () => _await4,
  done: () => done4,
  fail: () => fail8,
  make: () => make68,
  poll: () => poll5,
  succeed: () => succeed13
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tDeferred.js
var TDeferredSymbolKey = "effect/TDeferred";
var TDeferredTypeId = Symbol.for(TDeferredSymbolKey);
var tDeferredVariance = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _E: (_) => _
};
var TDeferredImpl = class {
  ref;
  [TDeferredTypeId] = tDeferredVariance;
  constructor(ref) {
    this.ref = ref;
  }
};
var _await3 = (self) => flatten3(collect(get9(self.ref), (option3) => isSome(option3) ? some(fromEither(option3.value)) : none()));
var done3 = dual(2, (self, either3) => flatMap4(get9(self.ref), match2({
  onNone: () => zipRight5(set8(self.ref, some(either3)), succeed7(true)),
  onSome: () => succeed7(false)
})));
var fail7 = dual(2, (self, error4) => done3(self, left(error4)));
var make67 = () => map10(make19(none()), (ref) => new TDeferredImpl(ref));
var poll4 = (self) => get9(self.ref);
var succeed12 = dual(2, (self, value6) => done3(self, right(value6)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TDeferred.js
var TDeferredTypeId2 = TDeferredTypeId;
var _await4 = _await3;
var done4 = done3;
var fail8 = fail7;
var make68 = make67;
var poll5 = poll4;
var succeed13 = succeed12;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TMap.js
var TMap_exports = {};
__export(TMap_exports, {
  TMapTypeId: () => TMapTypeId2,
  empty: () => empty20,
  find: () => find2,
  findAll: () => findAll2,
  findAllSTM: () => findAllSTM2,
  findSTM: () => findSTM2,
  forEach: () => forEach9,
  fromIterable: () => fromIterable15,
  get: () => get29,
  getOrElse: () => getOrElse3,
  has: () => has13,
  isEmpty: () => isEmpty4,
  keys: () => keys7,
  make: () => make70,
  merge: () => merge7,
  reduce: () => reduce13,
  reduceSTM: () => reduceSTM4,
  remove: () => remove9,
  removeAll: () => removeAll2,
  removeIf: () => removeIf2,
  retainIf: () => retainIf2,
  set: () => set17,
  setIfAbsent: () => setIfAbsent2,
  size: () => size10,
  takeFirst: () => takeFirst2,
  takeFirstSTM: () => takeFirstSTM2,
  takeSome: () => takeSome2,
  takeSomeSTM: () => takeSomeSTM2,
  toArray: () => toArray4,
  toChunk: () => toChunk2,
  toHashMap: () => toHashMap2,
  toMap: () => toMap2,
  transform: () => transform4,
  transformSTM: () => transformSTM4,
  transformValues: () => transformValues2,
  transformValuesSTM: () => transformValuesSTM2,
  updateWith: () => updateWith2,
  values: () => values5
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tMap.js
var TMapSymbolKey = "effect/TMap";
var TMapTypeId = Symbol.for(TMapSymbolKey);
var tMapVariance = {
  /* c8 ignore next */
  _K: (_) => _,
  /* c8 ignore next */
  _V: (_) => _
};
var TMapImpl = class {
  tBuckets;
  tSize;
  [TMapTypeId] = tMapVariance;
  constructor(tBuckets, tSize) {
    this.tBuckets = tBuckets;
    this.tSize = tSize;
  }
};
var isTMap = (u) => hasProperty(u, TMapTypeId);
var InitialCapacity = 16;
var LoadFactor = 0.75;
var nextPowerOfTwo = (size18) => {
  const n = -1 >>> Math.clz32(size18 - 1);
  return n < 0 ? 1 : n + 1;
};
var hash2 = (key) => {
  const h = hash(key);
  return h ^ h >>> 16;
};
var indexOf = (k, capacity) => hash2(k) & capacity - 1;
var allocate = (capacity, data) => {
  const buckets = Array.from({
    length: capacity
  }, () => empty2());
  const distinct = new Map(data);
  let size18 = 0;
  for (const entry of distinct) {
    const index = indexOf(entry[0], capacity);
    buckets[index] = pipe(buckets[index], prepend2(entry));
    size18 = size18 + 1;
  }
  return pipe(fromIterable12(buckets), flatMap4((buckets2) => pipe(make19(buckets2), flatMap4((tBuckets) => pipe(make19(size18), map10((tSize) => new TMapImpl(tBuckets, tSize)))))));
};
var empty19 = () => fromIterable14([]);
var find = dual(2, (self, pf) => findSTM(self, (key, value6) => {
  const option3 = pf(key, value6);
  if (isSome(option3)) {
    return succeed7(option3.value);
  }
  return fail3(none());
}));
var findSTM = dual(2, (self, f) => reduceSTM3(self, none(), (acc, value6, key) => isNone(acc) ? matchSTM(f(key, value6), {
  onFailure: match2({
    onNone: () => succeedNone2,
    onSome: fail3
  }),
  onSuccess: succeedSome
}) : succeed10(acc)));
var findAll = dual(2, (self, pf) => findAllSTM(self, (key, value6) => {
  const option3 = pf(key, value6);
  if (isSome(option3)) {
    return succeed7(option3.value);
  }
  return fail3(none());
}));
var findAllSTM = dual(2, (self, pf) => map10(reduceSTM3(self, empty2(), (acc, value6, key) => matchSTM(pf(key, value6), {
  onFailure: match2({
    onNone: () => succeed7(acc),
    onSome: fail3
  }),
  onSuccess: (a) => succeed7(append(acc, a))
})), (a) => Array.from(a)));
var forEach8 = dual(2, (self, f) => reduceSTM3(self, void 0, (_, value6, key) => asVoid3(f(key, value6))));
var fromIterable14 = (iterable) => suspend4(() => {
  const data = fromIterable2(iterable);
  const capacity = data.length < InitialCapacity ? InitialCapacity : nextPowerOfTwo(data.length);
  return allocate(capacity, data);
});
var get28 = dual(2, (self, key) => effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  const index = indexOf(key, buckets.chunk.length);
  const bucket = unsafeGet4(buckets.chunk[index], journal);
  return pipe(findFirst(bucket, (entry) => equals(entry[0])(key)), map((entry) => entry[1]));
}));
var getOrElse2 = dual(3, (self, key, fallback) => map10(get28(self, key), getOrElse(fallback)));
var has12 = dual(2, (self, key) => map10(get28(self, key), isSome));
var isEmpty3 = (self) => map10(get9(self.tSize), (size18) => size18 === 0);
var keys6 = (self) => map10(toReadonlyArray(self), map3((entry) => entry[0]));
var make69 = (...entries5) => fromIterable14(entries5);
var merge6 = dual(4, (self, key, value6, f) => flatMap4(get28(self, key), match2({
  onNone: () => as4(set16(self, key, value6), value6),
  onSome: (v0) => {
    const v1 = f(v0, value6);
    return as4(set16(self, key, v1), v1);
  }
})));
var reduce12 = dual(3, (self, zero, f) => effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  let result = zero;
  let index = 0;
  while (index < buckets.chunk.length) {
    const bucket = buckets.chunk[index];
    const items = unsafeGet4(bucket, journal);
    result = reduce2(items, result, (acc, entry) => f(acc, entry[1], entry[0]));
    index = index + 1;
  }
  return result;
}));
var reduceSTM3 = dual(3, (self, zero, f) => flatMap4(toReadonlyArray(self), reduce6(zero, (acc, entry) => f(acc, entry[1], entry[0]))));
var remove8 = dual(2, (self, key) => effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  const index = indexOf(key, buckets.chunk.length);
  const bucket = unsafeGet4(buckets.chunk[index], journal);
  const [toRemove, toRetain] = partition(bucket, (entry) => equals(entry[1], key));
  if (isNonEmpty(toRemove)) {
    const currentSize = unsafeGet4(self.tSize, journal);
    unsafeSet(buckets.chunk[index], toRetain, journal);
    unsafeSet(self.tSize, currentSize - 1, journal);
  }
}));
var removeAll = dual(2, (self, keys10) => effect2((journal) => {
  const iterator = keys10[Symbol.iterator]();
  let next3;
  while ((next3 = iterator.next()) && !next3.done) {
    const buckets = unsafeGet4(self.tBuckets, journal);
    const index = indexOf(next3.value, buckets.chunk.length);
    const bucket = unsafeGet4(buckets.chunk[index], journal);
    const [toRemove, toRetain] = partition(bucket, (entry) => equals(next3.value)(entry[0]));
    if (isNonEmpty(toRemove)) {
      const currentSize = unsafeGet4(self.tSize, journal);
      unsafeSet(buckets.chunk[index], toRetain, journal);
      unsafeSet(self.tSize, currentSize - 1, journal);
    }
  }
}));
var removeIf = dual((args) => isTMap(args[0]), (self, predicate, options) => effect2((journal) => {
  const discard = options?.discard === true;
  const buckets = unsafeGet4(self.tBuckets, journal);
  const capacity = buckets.chunk.length;
  const removed = [];
  let index = 0;
  let newSize = 0;
  while (index < capacity) {
    const bucket = unsafeGet4(buckets.chunk[index], journal);
    const iterator = bucket[Symbol.iterator]();
    let next3;
    let newBucket = empty2();
    while ((next3 = iterator.next()) && !next3.done) {
      const [k, v] = next3.value;
      if (!predicate(k, v)) {
        newBucket = prepend2(newBucket, next3.value);
        newSize = newSize + 1;
      } else {
        if (!discard) {
          removed.push([k, v]);
        }
      }
    }
    unsafeSet(buckets.chunk[index], newBucket, journal);
    index = index + 1;
  }
  unsafeSet(self.tSize, newSize, journal);
  if (!discard) {
    return removed;
  }
}));
var retainIf = dual((args) => isTMap(args[0]), (self, predicate, options) => removeIf(self, (key, value6) => !predicate(key, value6), options));
var set16 = dual(3, (self, key, value6) => {
  const resize = (journal, buckets) => {
    const capacity = buckets.chunk.length;
    const newCapacity = capacity << 1;
    const newBuckets = Array.from({
      length: newCapacity
    }, () => empty2());
    let index = 0;
    while (index < capacity) {
      const pairs = unsafeGet4(buckets.chunk[index], journal);
      const iterator = pairs[Symbol.iterator]();
      let next3;
      while ((next3 = iterator.next()) && !next3.done) {
        const newIndex2 = indexOf(next3.value[0], newCapacity);
        newBuckets[newIndex2] = prepend2(newBuckets[newIndex2], next3.value);
      }
      index = index + 1;
    }
    const newIndex = indexOf(key, newCapacity);
    newBuckets[newIndex] = prepend2(newBuckets[newIndex], [key, value6]);
    const newArray = [];
    index = 0;
    while (index < newCapacity) {
      newArray[index] = new TRefImpl(newBuckets[index]);
      index = index + 1;
    }
    const newTArray = new TArrayImpl(newArray);
    unsafeSet(self.tBuckets, newTArray, journal);
  };
  return effect2((journal) => {
    const buckets = unsafeGet4(self.tBuckets, journal);
    const capacity = buckets.chunk.length;
    const index = indexOf(key, capacity);
    const bucket = unsafeGet4(buckets.chunk[index], journal);
    const shouldUpdate = some2(bucket, (entry) => equals(key)(entry[0]));
    if (shouldUpdate) {
      const newBucket = map4(bucket, (entry) => equals(key)(entry[0]) ? [key, value6] : entry);
      unsafeSet(buckets.chunk[index], newBucket, journal);
    } else {
      const newSize = unsafeGet4(self.tSize, journal) + 1;
      unsafeSet(self.tSize, newSize, journal);
      if (capacity * LoadFactor < newSize) {
        resize(journal, buckets);
      } else {
        const newBucket = prepend2(bucket, [key, value6]);
        unsafeSet(buckets.chunk[index], newBucket, journal);
      }
    }
  });
});
var setIfAbsent = dual(3, (self, key, value6) => flatMap4(get28(self, key), match2({
  onNone: () => set16(self, key, value6),
  onSome: () => void_3
})));
var size9 = (self) => get9(self.tSize);
var takeFirst = dual(2, (self, pf) => pipe(effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  const capacity = buckets.chunk.length;
  const size18 = unsafeGet4(self.tSize, journal);
  let result = none();
  let index = 0;
  while (index < capacity && isNone(result)) {
    const bucket = unsafeGet4(buckets.chunk[index], journal);
    const recreate = some2(bucket, (entry) => isSome(pf(entry[0], entry[1])));
    if (recreate) {
      const iterator = bucket[Symbol.iterator]();
      let newBucket = empty2();
      let next3;
      while ((next3 = iterator.next()) && !next3.done && isNone(result)) {
        const option3 = pf(next3.value[0], next3.value[1]);
        if (isSome(option3) && isNone(result)) {
          result = option3;
        } else {
          newBucket = prepend2(newBucket, next3.value);
        }
      }
      unsafeSet(buckets.chunk[index], newBucket, journal);
    }
    index = index + 1;
  }
  if (isSome(result)) {
    unsafeSet(self.tSize, size18 - 1, journal);
  }
  return result;
}), collect((option3) => isSome(option3) ? some(option3.value) : none())));
var takeFirstSTM = dual(2, (self, pf) => pipe(findSTM(self, (key, value6) => map10(pf(key, value6), (a) => [key, a])), collect((option3) => isSome(option3) ? some(option3.value) : none()), flatMap4((entry) => as4(remove8(self, entry[0]), entry[1]))));
var takeSome = dual(2, (self, pf) => pipe(effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  const capacity = buckets.chunk.length;
  const builder = [];
  let newSize = 0;
  let index = 0;
  while (index < capacity) {
    const bucket = unsafeGet4(buckets.chunk[index], journal);
    const recreate = some2(bucket, (entry) => isSome(pf(entry[0], entry[1])));
    if (recreate) {
      const iterator = bucket[Symbol.iterator]();
      let newBucket = empty2();
      let next3;
      while ((next3 = iterator.next()) && !next3.done) {
        const option3 = pf(next3.value[0], next3.value[1]);
        if (isSome(option3)) {
          builder.push(option3.value);
        } else {
          newBucket = prepend2(newBucket, next3.value);
          newSize = newSize + 1;
        }
      }
      unsafeSet(buckets.chunk[index], newBucket, journal);
    } else {
      newSize = newSize + bucket.length;
    }
    index = index + 1;
  }
  unsafeSet(self.tSize, newSize, journal);
  if (builder.length > 0) {
    return some(builder);
  }
  return none();
}), collect((option3) => isSome(option3) ? some(option3.value) : none())));
var takeSomeSTM = dual(2, (self, pf) => pipe(findAllSTM(self, (key, value6) => map10(pf(key, value6), (a) => [key, a])), map10((chunk3) => isNonEmptyArray(chunk3) ? some(chunk3) : none()), collect((option3) => isSome(option3) ? some(option3.value) : none()), flatMap4((entries5) => as4(removeAll(self, entries5.map((entry) => entry[0])), map3(entries5, (entry) => entry[1])))));
var toReadonlyArray = (self) => effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  const capacity = buckets.chunk.length;
  const builder = [];
  let index = 0;
  while (index < capacity) {
    const bucket = buckets.chunk[index];
    for (const entry of unsafeGet4(bucket, journal)) {
      builder.push(entry);
    }
    index = index + 1;
  }
  return builder;
});
var toChunk = (self) => reduce12(self, empty2(), (acc, value6, key) => append(acc, [key, value6]));
var toHashMap = (self) => reduce12(self, empty4(), (acc, value6, key) => pipe(acc, set2(key, value6)));
var toArray3 = (self) => reduce12(self, [], (acc, value6, key) => {
  acc.unshift([key, value6]);
  return acc;
});
var toMap = (self) => reduce12(self, /* @__PURE__ */ new Map(), (acc, value6, key) => acc.set(key, value6));
var transform3 = dual(2, (self, f) => effect2((journal) => {
  const buckets = pipe(self.tBuckets, unsafeGet4(journal));
  const capacity = buckets.chunk.length;
  const newBuckets = Array.from({
    length: capacity
  }, () => empty2());
  let newSize = 0;
  let index = 0;
  while (index < capacity) {
    const bucket = buckets.chunk[index];
    const pairs = unsafeGet4(bucket, journal);
    const iterator = pairs[Symbol.iterator]();
    let next3;
    while ((next3 = iterator.next()) && !next3.done) {
      const newPair = f(next3.value[0], next3.value[1]);
      const index2 = indexOf(newPair[0], capacity);
      const newBucket = newBuckets[index2];
      if (!some2(newBucket, (entry) => equals(entry[0], newPair[0]))) {
        newBuckets[index2] = prepend2(newBucket, newPair);
        newSize = newSize + 1;
      }
    }
    index = index + 1;
  }
  index = 0;
  while (index < capacity) {
    unsafeSet(buckets.chunk[index], newBuckets[index], journal);
    index = index + 1;
  }
  unsafeSet(self.tSize, newSize, journal);
}));
var transformSTM3 = dual(2, (self, f) => pipe(flatMap4(toReadonlyArray(self), forEach3((entry) => f(entry[0], entry[1]))), flatMap4((newData) => effect2((journal) => {
  const buckets = unsafeGet4(self.tBuckets, journal);
  const capacity = buckets.chunk.length;
  const newBuckets = Array.from({
    length: capacity
  }, () => empty2());
  const iterator = newData[Symbol.iterator]();
  let newSize = 0;
  let next3;
  while ((next3 = iterator.next()) && !next3.done) {
    const index2 = indexOf(next3.value[0], capacity);
    const newBucket = newBuckets[index2];
    if (!some2(newBucket, (entry) => equals(entry[0])(next3.value[0]))) {
      newBuckets[index2] = prepend2(newBucket, next3.value);
      newSize = newSize + 1;
    }
  }
  let index = 0;
  while (index < capacity) {
    unsafeSet(buckets.chunk[index], newBuckets[index], journal);
    index = index + 1;
  }
  unsafeSet(self.tSize, newSize, journal);
}))));
var transformValues = dual(2, (self, f) => transform3(self, (key, value6) => [key, f(value6)]));
var transformValuesSTM = dual(2, (self, f) => transformSTM3(self, (key, value6) => map10(f(value6), (value7) => [key, value7])));
var updateWith = dual(3, (self, key, f) => flatMap4(get28(self, key), (option3) => match2(f(option3), {
  onNone: () => as4(remove8(self, key), none()),
  onSome: (value6) => as4(set16(self, key, value6), some(value6))
})));
var values4 = (self) => map10(toReadonlyArray(self), map3((entry) => entry[1]));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TMap.js
var TMapTypeId2 = TMapTypeId;
var empty20 = empty19;
var find2 = find;
var findSTM2 = findSTM;
var findAll2 = findAll;
var findAllSTM2 = findAllSTM;
var forEach9 = forEach8;
var fromIterable15 = fromIterable14;
var get29 = get28;
var getOrElse3 = getOrElse2;
var has13 = has12;
var isEmpty4 = isEmpty3;
var keys7 = keys6;
var make70 = make69;
var merge7 = merge6;
var reduce13 = reduce12;
var reduceSTM4 = reduceSTM3;
var remove9 = remove8;
var removeAll2 = removeAll;
var removeIf2 = removeIf;
var retainIf2 = retainIf;
var set17 = set16;
var setIfAbsent2 = setIfAbsent;
var size10 = size9;
var takeFirst2 = takeFirst;
var takeFirstSTM2 = takeFirstSTM;
var takeSome2 = takeSome;
var takeSomeSTM2 = takeSomeSTM;
var toChunk2 = toChunk;
var toHashMap2 = toHashMap;
var toArray4 = toArray3;
var toMap2 = toMap;
var transform4 = transform3;
var transformSTM4 = transformSTM3;
var transformValues2 = transformValues;
var transformValuesSTM2 = transformValuesSTM;
var updateWith2 = updateWith;
var values5 = values4;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TPriorityQueue.js
var TPriorityQueue_exports = {};
__export(TPriorityQueue_exports, {
  TPriorityQueueTypeId: () => TPriorityQueueTypeId2,
  empty: () => empty22,
  fromIterable: () => fromIterable17,
  isEmpty: () => isEmpty6,
  isNonEmpty: () => isNonEmpty4,
  make: () => make72,
  offer: () => offer4,
  offerAll: () => offerAll2,
  peek: () => peek2,
  peekOption: () => peekOption2,
  removeIf: () => removeIf4,
  retainIf: () => retainIf4,
  size: () => size12,
  take: () => take4,
  takeAll: () => takeAll2,
  takeOption: () => takeOption2,
  takeUpTo: () => takeUpTo2,
  toArray: () => toArray6,
  toChunk: () => toChunk4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tPriorityQueue.js
var TPriorityQueueSymbolKey = "effect/TPriorityQueue";
var TPriorityQueueTypeId = Symbol.for(TPriorityQueueSymbolKey);
var tPriorityQueueVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var TPriorityQueueImpl = class {
  ref;
  [TPriorityQueueTypeId] = tPriorityQueueVariance;
  constructor(ref) {
    this.ref = ref;
  }
};
var empty21 = (order) => pipe(make19(empty16(order)), map10((ref) => new TPriorityQueueImpl(ref)));
var fromIterable16 = (order) => (iterable) => pipe(make19(fromIterable(iterable).reduce((map22, value6) => pipe(map22, set12(value6, pipe(map22, get22(value6), match2({
  onNone: () => of(value6),
  onSome: prepend(value6)
})))), empty16(order))), map10((ref) => new TPriorityQueueImpl(ref)));
var isEmpty5 = (self) => map10(get9(self.ref), isEmpty2);
var isNonEmpty3 = (self) => map10(get9(self.ref), isNonEmpty2);
var make71 = (order) => (...elements) => fromIterable16(order)(elements);
var offer3 = dual(2, (self, value6) => update5(self.ref, (map22) => set12(map22, value6, match2(get22(map22, value6), {
  onNone: () => of(value6),
  onSome: prepend(value6)
}))));
var offerAll = dual(2, (self, values8) => update5(self.ref, (map22) => fromIterable(values8).reduce((map23, value6) => set12(map23, value6, match2(get22(map23, value6), {
  onNone: () => of(value6),
  onSome: prepend(value6)
})), map22)));
var peek = (self) => withSTMRuntime((runtime4) => {
  const map22 = unsafeGet4(self.ref, runtime4.journal);
  return match2(headOption(map22), {
    onNone: () => retry,
    onSome: (elements) => succeed7(elements[0])
  });
});
var peekOption = (self) => modify4(self.ref, (map22) => [map(headOption(map22), (elements) => elements[0]), map22]);
var removeIf3 = dual(2, (self, predicate) => retainIf3(self, (a) => !predicate(a)));
var retainIf3 = dual(2, (self, predicate) => update5(self.ref, (map22) => reduce9(map22, empty16(getOrder2(map22)), (map23, value6, key) => {
  const filtered = filter2(value6, predicate);
  return filtered.length > 0 ? set12(map23, key, filtered) : remove7(map23, key);
})));
var size11 = (self) => modify4(self.ref, (map22) => [reduce9(map22, 0, (n, as7) => n + as7.length), map22]);
var take3 = (self) => withSTMRuntime((runtime4) => {
  const map22 = unsafeGet4(self.ref, runtime4.journal);
  return match2(headOption(map22), {
    onNone: () => retry,
    onSome: (values8) => {
      const head5 = values8[1][0];
      const tail = values8[1].slice(1);
      unsafeSet(self.ref, tail.length > 0 ? set12(map22, head5, tail) : remove7(map22, head5), runtime4.journal);
      return succeed7(head5);
    }
  });
});
var takeAll = (self) => modify4(self.ref, (map22) => {
  const builder = [];
  for (const entry of map22) {
    for (const value6 of entry[1]) {
      builder.push(value6);
    }
  }
  return [builder, empty16(getOrder2(map22))];
});
var takeOption = (self) => effect2((journal) => {
  const map22 = pipe(self.ref, unsafeGet4(journal));
  return match2(headOption(map22), {
    onNone: () => none(),
    onSome: ([key, value6]) => {
      const tail = value6.slice(1);
      unsafeSet(self.ref, tail.length > 0 ? set12(map22, key, tail) : remove7(map22, key), journal);
      return some(value6[0]);
    }
  });
});
var takeUpTo = dual(2, (self, n) => modify4(self.ref, (map22) => {
  const builder = [];
  const iterator = map22[Symbol.iterator]();
  let updated = map22;
  let index = 0;
  let next3;
  while ((next3 = iterator.next()) && !next3.done && index < n) {
    const [key, value6] = next3.value;
    const [left2, right2] = pipe(value6, splitAt(n - index));
    for (const value7 of left2) {
      builder.push(value7);
    }
    if (right2.length > 0) {
      updated = set12(updated, key, right2);
    } else {
      updated = remove7(updated, key);
    }
    index = index + left2.length;
  }
  return [builder, updated];
}));
var toChunk3 = (self) => modify4(self.ref, (map22) => {
  const builder = [];
  for (const entry of map22) {
    for (const value6 of entry[1]) {
      builder.push(value6);
    }
  }
  return [unsafeFromArray(builder), map22];
});
var toArray5 = (self) => modify4(self.ref, (map22) => {
  const builder = [];
  for (const entry of map22) {
    for (const value6 of entry[1]) {
      builder.push(value6);
    }
  }
  return [builder, map22];
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TPriorityQueue.js
var TPriorityQueueTypeId2 = TPriorityQueueTypeId;
var empty22 = empty21;
var fromIterable17 = fromIterable16;
var isEmpty6 = isEmpty5;
var isNonEmpty4 = isNonEmpty3;
var make72 = make71;
var offer4 = offer3;
var offerAll2 = offerAll;
var peek2 = peek;
var peekOption2 = peekOption;
var removeIf4 = removeIf3;
var retainIf4 = retainIf3;
var size12 = size11;
var take4 = take3;
var takeAll2 = takeAll;
var takeOption2 = takeOption;
var takeUpTo2 = takeUpTo;
var toChunk4 = toChunk3;
var toArray6 = toArray5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TRandom.js
var TRandom_exports = {};
__export(TRandom_exports, {
  TRandomTypeId: () => TRandomTypeId2,
  Tag: () => Tag2,
  live: () => live2,
  next: () => next2,
  nextBoolean: () => nextBoolean2,
  nextInt: () => nextInt2,
  nextIntBetween: () => nextIntBetween2,
  nextRange: () => nextRange2,
  shuffle: () => shuffle2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tRandom.js
var TRandomSymbolKey = "effect/TRandom";
var TRandomTypeId = Symbol.for(TRandomSymbolKey);
var randomInteger = (state) => {
  const prng = new PCGRandom();
  prng.setState(state);
  return [prng.integer(0), prng.getState()];
};
var randomIntegerBetween = (low, high) => {
  return (state) => {
    const prng = new PCGRandom();
    prng.setState(state);
    return [prng.integer(high - low) + low, prng.getState()];
  };
};
var randomNumber = (state) => {
  const prng = new PCGRandom();
  prng.setState(state);
  return [prng.number(), prng.getState()];
};
var withState = (state, f) => {
  return pipe(state, modify4(f));
};
var shuffleWith = (iterable, nextIntBounded) => {
  const swap = (buffer, index1, index2) => pipe(buffer, get26(index1), flatMap4((tmp) => pipe(buffer, updateSTM(index1, () => pipe(buffer, get26(index2))), zipRight5(pipe(buffer, update9(index2, () => tmp))))));
  return pipe(fromIterable12(iterable), flatMap4((buffer) => {
    const array = [];
    for (let i = array.length; i >= 2; i = i - 1) {
      array.push(i);
    }
    return pipe(array, forEach3((n) => pipe(nextIntBounded(n), flatMap4((k) => swap(buffer, n - 1, k))), {
      discard: true
    }), zipRight5(toArray(buffer)));
  }));
};
var Tag = GenericTag("effect/TRandom");
var TRandomImpl = class {
  state;
  [TRandomTypeId] = TRandomTypeId;
  constructor(state) {
    this.state = state;
    this.next = withState(this.state, randomNumber);
    this.nextBoolean = flatMap4(this.next, (n) => succeed7(n > 0.5));
    this.nextInt = withState(this.state, randomInteger);
  }
  next;
  nextBoolean;
  nextInt;
  nextRange(min, max) {
    return flatMap4(this.next, (n) => succeed7((max - min) * n + min));
  }
  nextIntBetween(low, high) {
    return withState(this.state, randomIntegerBetween(low, high));
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var live = effect(Tag, pipe(make19(new PCGRandom(Math.random() * 4294967296 >>> 0).getState()), map10((seed) => new TRandomImpl(seed)), commit));
var next = flatMap4(Tag, (random) => random.next);
var nextBoolean = flatMap4(Tag, (random) => random.nextBoolean);
var nextInt = flatMap4(Tag, (random) => random.nextInt);
var nextIntBetween = (low, high) => flatMap4(Tag, (random) => random.nextIntBetween(low, high));
var nextRange = (min, max) => flatMap4(Tag, (random) => random.nextRange(min, max));
var shuffle = (elements) => flatMap4(Tag, (random) => random.shuffle(elements));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TRandom.js
var TRandomTypeId2 = TRandomTypeId;
var Tag2 = Tag;
var live2 = live;
var next2 = next;
var nextBoolean2 = nextBoolean;
var nextInt2 = nextInt;
var nextIntBetween2 = nextIntBetween;
var nextRange2 = nextRange;
var shuffle2 = shuffle;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TReentrantLock.js
var TReentrantLock_exports = {};
__export(TReentrantLock_exports, {
  TReentrantLockTypeId: () => TReentrantLockTypeId2,
  acquireRead: () => acquireRead2,
  acquireWrite: () => acquireWrite2,
  fiberReadLocks: () => fiberReadLocks2,
  fiberWriteLocks: () => fiberWriteLocks2,
  lock: () => lock2,
  locked: () => locked2,
  make: () => make74,
  readLock: () => readLock2,
  readLocked: () => readLocked2,
  readLocks: () => readLocks2,
  releaseRead: () => releaseRead2,
  releaseWrite: () => releaseWrite2,
  withLock: () => withLock2,
  withReadLock: () => withReadLock2,
  withWriteLock: () => withWriteLock2,
  writeLock: () => writeLock2,
  writeLocked: () => writeLocked2,
  writeLocks: () => writeLocks2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tReentrantLock.js
var TReentrantLockSymbolKey = "effect/TReentrantLock";
var TReentrantLockTypeId = Symbol.for(TReentrantLockSymbolKey);
var WriteLockTypeId = /* @__PURE__ */ Symbol.for("effect/TReentrantLock/WriteLock");
var ReadLockTypeId = /* @__PURE__ */ Symbol.for("effect/TReentrantLock/ReadLock");
var TReentranLockImpl = class {
  state;
  [TReentrantLockTypeId] = TReentrantLockTypeId;
  constructor(state) {
    this.state = state;
  }
};
var ReadLock = class {
  readers;
  [ReadLockTypeId] = ReadLockTypeId;
  constructor(readers) {
    this.readers = readers;
  }
  get readLocks() {
    return Array.from(this.readers).reduce((acc, curr) => acc + curr[1], 0);
  }
  get writeLocks() {
    return 0;
  }
  readLocksHeld(fiberId4) {
    return getOrElse(get3(this.readers, fiberId4), () => 0);
  }
  writeLocksHeld(_fiberId) {
    return 0;
  }
};
var WriteLock = class {
  readLocks;
  writeLocks;
  fiberId;
  [WriteLockTypeId] = WriteLockTypeId;
  constructor(readLocks3, writeLocks3, fiberId4) {
    this.readLocks = readLocks3;
    this.writeLocks = writeLocks3;
    this.fiberId = fiberId4;
  }
  readLocksHeld(fiberId4) {
    return equals(fiberId4)(this.fiberId) ? this.readLocks : 0;
  }
  writeLocksHeld(fiberId4) {
    return equals(fiberId4)(this.fiberId) ? this.writeLocks : 0;
  }
};
var isReadLock = (lock3) => {
  return ReadLockTypeId in lock3;
};
var isWriteLock = (lock3) => {
  return WriteLockTypeId in lock3;
};
var emptyReadLock = new ReadLock(empty4());
var makeReadLock = (fiberId4, count7) => {
  if (count7 <= 0) {
    return emptyReadLock;
  }
  return new ReadLock(make5([fiberId4, count7]));
};
var noOtherHolder = (readLock3, fiberId4) => {
  return isEmpty(readLock3.readers) || size(readLock3.readers) === 1 && has3(readLock3.readers, fiberId4);
};
var adjustReadLock = (readLock3, fiberId4, adjustment) => {
  const total = readLock3.readLocksHeld(fiberId4);
  const newTotal = total + adjustment;
  if (newTotal < 0) {
    throw new Error("BUG - TReentrantLock.ReadLock.adjust - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  if (newTotal === 0) {
    return new ReadLock(remove(readLock3.readers, fiberId4));
  }
  return new ReadLock(set2(readLock3.readers, fiberId4, newTotal));
};
var adjustRead = (self, delta) => withSTMRuntime((runtime4) => {
  const lock3 = unsafeGet4(self.state, runtime4.journal);
  if (isReadLock(lock3)) {
    const result = adjustReadLock(lock3, runtime4.fiberId, delta);
    unsafeSet(self.state, result, runtime4.journal);
    return succeed7(result.readLocksHeld(runtime4.fiberId));
  }
  if (isWriteLock(lock3) && equals(runtime4.fiberId)(lock3.fiberId)) {
    const newTotal = lock3.readLocks + delta;
    if (newTotal < 0) {
      throw new Error(`Defect: Fiber ${threadName(runtime4.fiberId)} releasing read locks it does not hold, newTotal: ${newTotal}`);
    }
    unsafeSet(self.state, new WriteLock(newTotal, lock3.writeLocks, runtime4.fiberId), runtime4.journal);
    return succeed7(newTotal);
  }
  return retry;
});
var acquireRead = (self) => adjustRead(self, 1);
var acquireWrite = (self) => withSTMRuntime((runtime4) => {
  const lock3 = unsafeGet4(self.state, runtime4.journal);
  if (isReadLock(lock3) && noOtherHolder(lock3, runtime4.fiberId)) {
    unsafeSet(self.state, new WriteLock(lock3.readLocksHeld(runtime4.fiberId), 1, runtime4.fiberId), runtime4.journal);
    return succeed7(1);
  }
  if (isWriteLock(lock3) && equals(runtime4.fiberId)(lock3.fiberId)) {
    unsafeSet(self.state, new WriteLock(lock3.readLocks, lock3.writeLocks + 1, runtime4.fiberId), runtime4.journal);
    return succeed7(lock3.writeLocks + 1);
  }
  return retry;
});
var fiberReadLocks = (self) => effect2((journal, fiberId4) => unsafeGet4(self.state, journal).readLocksHeld(fiberId4));
var fiberWriteLocks = (self) => effect2((journal, fiberId4) => unsafeGet4(self.state, journal).writeLocksHeld(fiberId4));
var lock = (self) => writeLock(self);
var locked = (self) => zipWith3(readLocked(self), writeLocked(self), (x, y) => x || y);
var make73 = map10(make19(emptyReadLock), (readLock3) => new TReentranLockImpl(readLock3));
var readLock = (self) => acquireRelease2(commit(acquireRead(self)), () => commit(releaseRead(self)));
var readLocks = (self) => map10(get9(self.state), (state) => state.readLocks);
var readLocked = (self) => map10(get9(self.state), (state) => state.readLocks > 0);
var releaseRead = (self) => adjustRead(self, -1);
var releaseWrite = (self) => withSTMRuntime((runtime4) => {
  const lock3 = unsafeGet4(self.state, runtime4.journal);
  if (isWriteLock(lock3) && lock3.writeLocks === 1 && equals(runtime4.fiberId)(lock3.fiberId)) {
    const result = makeReadLock(lock3.fiberId, lock3.readLocks);
    unsafeSet(self.state, result, runtime4.journal);
    return succeed7(result.writeLocksHeld(runtime4.fiberId));
  }
  if (isWriteLock(lock3) && equals(runtime4.fiberId)(lock3.fiberId)) {
    const result = new WriteLock(lock3.readLocks, lock3.writeLocks - 1, runtime4.fiberId);
    unsafeSet(self.state, result, runtime4.journal);
    return succeed7(result.writeLocksHeld(runtime4.fiberId));
  }
  throw new Error(`Defect: Fiber ${threadName(runtime4.fiberId)} releasing write lock it does not hold`);
});
var withLock = dual(2, (effect3, self) => withWriteLock(effect3, self));
var withReadLock = dual(2, (effect3, self) => uninterruptibleMask2((restore) => zipRight3(restore(commit(acquireRead(self))), ensuring2(effect3, commit(releaseRead(self))))));
var withWriteLock = dual(2, (effect3, self) => uninterruptibleMask2((restore) => zipRight3(restore(commit(acquireWrite(self))), ensuring2(effect3, commit(releaseWrite(self))))));
var writeLock = (self) => acquireRelease2(commit(acquireWrite(self)), () => commit(releaseWrite(self)));
var writeLocked = (self) => map10(get9(self.state), (state) => state.writeLocks > 0);
var writeLocks = (self) => map10(get9(self.state), (state) => state.writeLocks);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TReentrantLock.js
var TReentrantLockTypeId2 = TReentrantLockTypeId;
var acquireRead2 = acquireRead;
var acquireWrite2 = acquireWrite;
var fiberReadLocks2 = fiberReadLocks;
var fiberWriteLocks2 = fiberWriteLocks;
var lock2 = lock;
var locked2 = locked;
var make74 = make73;
var readLock2 = readLock;
var readLocks2 = readLocks;
var readLocked2 = readLocked;
var releaseRead2 = releaseRead;
var releaseWrite2 = releaseWrite;
var withLock2 = withLock;
var withReadLock2 = withReadLock;
var withWriteLock2 = withWriteLock;
var writeLock2 = writeLock;
var writeLocked2 = writeLocked;
var writeLocks2 = writeLocks;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TRef.js
var TRef_exports = {};
__export(TRef_exports, {
  TRefTypeId: () => TRefTypeId2,
  get: () => get30,
  getAndSet: () => getAndSet6,
  getAndUpdate: () => getAndUpdate6,
  getAndUpdateSome: () => getAndUpdateSome6,
  make: () => make75,
  modify: () => modify9,
  modifySome: () => modifySome6,
  set: () => set18,
  setAndGet: () => setAndGet6,
  update: () => update11,
  updateAndGet: () => updateAndGet6,
  updateSome: () => updateSome6,
  updateSomeAndGet: () => updateSomeAndGet6
});
var TRefTypeId2 = TRefTypeId;
var get30 = get9;
var getAndSet6 = getAndSet3;
var getAndUpdate6 = getAndUpdate3;
var getAndUpdateSome6 = getAndUpdateSome3;
var make75 = make19;
var modify9 = modify4;
var modifySome6 = modifySome3;
var set18 = set8;
var setAndGet6 = setAndGet3;
var update11 = update5;
var updateAndGet6 = updateAndGet3;
var updateSome6 = updateSome3;
var updateSomeAndGet6 = updateSomeAndGet3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TSemaphore.js
var TSemaphore_exports = {};
__export(TSemaphore_exports, {
  TSemaphoreTypeId: () => TSemaphoreTypeId2,
  acquire: () => acquire3,
  acquireN: () => acquireN2,
  available: () => available2,
  make: () => make77,
  release: () => release3,
  releaseN: () => releaseN2,
  unsafeMake: () => unsafeMake10,
  withPermit: () => withPermit2,
  withPermitScoped: () => withPermitScoped2,
  withPermits: () => withPermits2,
  withPermitsScoped: () => withPermitsScoped2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tSemaphore.js
var TSemaphoreSymbolKey = "effect/TSemaphore";
var TSemaphoreTypeId = Symbol.for(TSemaphoreSymbolKey);
var TSemaphoreImpl = class {
  permits;
  [TSemaphoreTypeId] = TSemaphoreTypeId;
  constructor(permits) {
    this.permits = permits;
  }
};
var make76 = (permits) => map15(make19(permits), (permits2) => new TSemaphoreImpl(permits2));
var acquire2 = (self) => acquireN(self, 1);
var acquireN = dual(2, (self, n) => withSTMRuntime((driver) => {
  if (n < 0) {
    throw new IllegalArgumentException(`Unexpected negative value ${n} passed to Semaphore.acquireN`);
  }
  const value6 = unsafeGet4(self.permits, driver.journal);
  if (value6 < n) {
    return retry4;
  } else {
    return succeed10(unsafeSet(self.permits, value6 - n, driver.journal));
  }
}));
var available = (self) => get9(self.permits);
var release2 = (self) => releaseN(self, 1);
var releaseN = dual(2, (self, n) => withSTMRuntime((driver) => {
  if (n < 0) {
    throw new IllegalArgumentException(`Unexpected negative value ${n} passed to Semaphore.releaseN`);
  }
  const current = unsafeGet4(self.permits, driver.journal);
  return succeed10(unsafeSet(self.permits, current + n, driver.journal));
}));
var withPermit = dual(2, (self, semaphore) => withPermits(self, semaphore, 1));
var withPermits = dual(3, (self, semaphore, permits) => uninterruptibleMask2((restore) => zipRight3(restore(commit(acquireN(permits)(semaphore))), ensuring2(self, commit(releaseN(permits)(semaphore))))));
var withPermitScoped = (self) => withPermitsScoped(self, 1);
var withPermitsScoped = dual(2, (self, permits) => acquireReleaseInterruptible(commit(acquireN(self, permits)), () => commit(releaseN(self, permits))));
var unsafeMakeSemaphore2 = (permits) => {
  return new TSemaphoreImpl(new TRefImpl(permits));
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TSemaphore.js
var TSemaphoreTypeId2 = TSemaphoreTypeId;
var acquire3 = acquire2;
var acquireN2 = acquireN;
var available2 = available;
var make77 = make76;
var release3 = release2;
var releaseN2 = releaseN;
var withPermit2 = withPermit;
var withPermits2 = withPermits;
var withPermitScoped2 = withPermitScoped;
var withPermitsScoped2 = withPermitsScoped;
var unsafeMake10 = unsafeMakeSemaphore2;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TSet.js
var TSet_exports = {};
__export(TSet_exports, {
  TSetTypeId: () => TSetTypeId2,
  add: () => add7,
  difference: () => difference2,
  empty: () => empty24,
  forEach: () => forEach11,
  fromIterable: () => fromIterable19,
  has: () => has15,
  intersection: () => intersection2,
  isEmpty: () => isEmpty8,
  make: () => make79,
  reduce: () => reduce15,
  reduceSTM: () => reduceSTM6,
  remove: () => remove11,
  removeAll: () => removeAll4,
  removeIf: () => removeIf6,
  retainIf: () => retainIf6,
  size: () => size14,
  takeFirst: () => takeFirst4,
  takeFirstSTM: () => takeFirstSTM4,
  takeSome: () => takeSome4,
  takeSomeSTM: () => takeSomeSTM4,
  toArray: () => toArray8,
  toChunk: () => toChunk6,
  toHashSet: () => toHashSet2,
  toReadonlySet: () => toReadonlySet2,
  transform: () => transform6,
  transformSTM: () => transformSTM6,
  union: () => union4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tSet.js
var TSetSymbolKey = "effect/TSet";
var TSetTypeId = Symbol.for(TSetSymbolKey);
var tSetVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var TSetImpl = class {
  tMap;
  [TSetTypeId] = tSetVariance;
  constructor(tMap) {
    this.tMap = tMap;
  }
};
var isTSet = (u) => hasProperty(u, TSetTypeId);
var add6 = dual(2, (self, value6) => set16(self.tMap, value6, void 0));
var difference = dual(2, (self, other) => flatMap4(toHashSet(other), (values8) => removeIf5(self, (value6) => has2(values8, value6), {
  discard: true
})));
var empty23 = () => fromIterable18([]);
var forEach10 = dual(2, (self, f) => reduceSTM5(self, void 0, (_, value6) => f(value6)));
var fromIterable18 = (iterable) => map10(fromIterable14(Array.from(iterable).map((a) => [a, void 0])), (tMap) => new TSetImpl(tMap));
var has14 = dual(2, (self, value6) => has12(self.tMap, value6));
var intersection = dual(2, (self, other) => flatMap4(toHashSet(other), (values8) => pipe(self, retainIf5((value6) => pipe(values8, has2(value6)), {
  discard: true
}))));
var isEmpty7 = (self) => isEmpty3(self.tMap);
var make78 = (...elements) => fromIterable18(elements);
var reduce14 = dual(3, (self, zero, f) => reduce12(self.tMap, zero, (acc, _, key) => f(acc, key)));
var reduceSTM5 = dual(3, (self, zero, f) => reduceSTM3(self.tMap, zero, (acc, _, key) => f(acc, key)));
var remove10 = dual(2, (self, value6) => remove8(self.tMap, value6));
var removeAll3 = dual(2, (self, iterable) => removeAll(self.tMap, iterable));
var removeIf5 = dual((args) => isTSet(args[0]), (self, predicate, options) => options?.discard === true ? removeIf(self.tMap, (key) => predicate(key), {
  discard: true
}) : pipe(removeIf(self.tMap, (key) => predicate(key)), map10(map3((entry) => entry[0]))));
var retainIf5 = dual((args) => isTSet(args[0]), (self, predicate, options) => options?.discard === true ? retainIf(self.tMap, (key) => predicate(key), {
  discard: true
}) : pipe(retainIf(self.tMap, (key) => predicate(key)), map10(map3((entry) => entry[0]))));
var size13 = (self) => map10(toChunk5(self), (chunk3) => chunk3.length);
var takeFirst3 = dual(2, (self, pf) => takeFirst(self.tMap, (key) => pf(key)));
var takeFirstSTM3 = dual(2, (self, pf) => takeFirstSTM(self.tMap, (key) => pf(key)));
var takeSome3 = dual(2, (self, pf) => takeSome(self.tMap, (key) => pf(key)));
var takeSomeSTM3 = dual(2, (self, pf) => takeSomeSTM(self.tMap, (key) => pf(key)));
var toChunk5 = (self) => keys6(self.tMap).pipe(map15(unsafeFromArray));
var toHashSet = (self) => reduce14(self, empty3(), (acc, value6) => pipe(acc, add2(value6)));
var toArray7 = (self) => reduce14(self, [], (acc, value6) => [...acc, value6]);
var toReadonlySet = (self) => map10(toArray7(self), (values8) => new Set(values8));
var transform5 = dual(2, (self, f) => transform3(self.tMap, (key, value6) => [f(key), value6]));
var transformSTM5 = dual(2, (self, f) => transformSTM3(self.tMap, (key, value6) => map10(f(key), (a) => [a, value6])));
var union3 = dual(2, (self, other) => forEach10(other, (value6) => add6(self, value6)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TSet.js
var TSetTypeId2 = TSetTypeId;
var add7 = add6;
var difference2 = difference;
var empty24 = empty23;
var forEach11 = forEach10;
var fromIterable19 = fromIterable18;
var has15 = has14;
var intersection2 = intersection;
var isEmpty8 = isEmpty7;
var make79 = make78;
var reduce15 = reduce14;
var reduceSTM6 = reduceSTM5;
var remove11 = remove10;
var removeAll4 = removeAll3;
var removeIf6 = removeIf5;
var retainIf6 = retainIf5;
var size14 = size13;
var takeFirst4 = takeFirst3;
var takeFirstSTM4 = takeFirstSTM3;
var takeSome4 = takeSome3;
var takeSomeSTM4 = takeSomeSTM3;
var toChunk6 = toChunk5;
var toHashSet2 = toHashSet;
var toArray8 = toArray7;
var toReadonlySet2 = toReadonlySet;
var transform6 = transform5;
var transformSTM6 = transformSTM5;
var union4 = union3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TSubscriptionRef.js
var TSubscriptionRef_exports = {};
__export(TSubscriptionRef_exports, {
  TSubscriptionRefTypeId: () => TSubscriptionRefTypeId2,
  changes: () => changes,
  changesScoped: () => changesScoped2,
  changesStream: () => changesStream2,
  get: () => get32,
  getAndSet: () => getAndSet8,
  getAndUpdate: () => getAndUpdate8,
  getAndUpdateSome: () => getAndUpdateSome8,
  make: () => make81,
  modify: () => modify11,
  modifySome: () => modifySome8,
  set: () => set20,
  setAndGet: () => setAndGet8,
  update: () => update13,
  updateAndGet: () => updateAndGet8,
  updateSome: () => updateSome8,
  updateSomeAndGet: () => updateSomeAndGet8
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tSubscriptionRef.js
var TSubscriptionRefSymbolKey = "effect/TSubscriptionRef";
var TSubscriptionRefTypeId = Symbol.for(TSubscriptionRefSymbolKey);
var TSubscriptionRefVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var TDequeueMerge = class {
  first;
  second;
  [TDequeueTypeId] = tDequeueVariance;
  constructor(first4, second) {
    this.first = first4;
    this.second = second;
  }
  peek = gen4(this, function* () {
    const first4 = yield* this.peekOption;
    if (first4._tag === "Some") {
      return first4.value;
    }
    return yield* retry4;
  });
  peekOption = gen4(this, function* () {
    const first4 = yield* this.first.peekOption;
    if (first4._tag === "Some") {
      return first4;
    }
    const second = yield* this.second.peekOption;
    if (second._tag === "Some") {
      return second;
    }
    return none();
  });
  take = gen4(this, function* () {
    if (!(yield* this.first.isEmpty)) {
      return yield* this.first.take;
    }
    if (!(yield* this.second.isEmpty)) {
      return yield* this.second.take;
    }
    return yield* retry4;
  });
  takeAll = gen4(this, function* () {
    return [...yield* this.first.takeAll, ...yield* this.second.takeAll];
  });
  takeUpTo(max) {
    return gen4(this, function* () {
      const first4 = yield* this.first.takeUpTo(max);
      if (first4.length >= max) {
        return first4;
      }
      return [...first4, ...yield* this.second.takeUpTo(max - first4.length)];
    });
  }
  capacity() {
    return this.first.capacity() + this.second.capacity();
  }
  size = gen4(this, function* () {
    return (yield* this.first.size) + (yield* this.second.size);
  });
  isFull = gen4(this, function* () {
    return (yield* this.first.isFull) && (yield* this.second.isFull);
  });
  isEmpty = gen4(this, function* () {
    return (yield* this.first.isEmpty) && (yield* this.second.isEmpty);
  });
  shutdown = gen4(this, function* () {
    yield* this.first.shutdown;
    yield* this.second.shutdown;
  });
  isShutdown = gen4(this, function* () {
    return (yield* this.first.isShutdown) && (yield* this.second.isShutdown);
  });
  awaitShutdown = gen4(this, function* () {
    yield* this.first.awaitShutdown;
    yield* this.second.awaitShutdown;
  });
};
var TSubscriptionRefImpl = class {
  ref;
  pubsub;
  [TSubscriptionRefTypeId] = TSubscriptionRefVariance;
  [TRefTypeId2] = tRefVariance;
  constructor(ref, pubsub) {
    this.ref = ref;
    this.pubsub = pubsub;
  }
  get todos() {
    return this.ref.todos;
  }
  get versioned() {
    return this.ref.versioned;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  get changes() {
    return gen4(this, function* () {
      const first4 = yield* unbounded4();
      yield* offer2(first4, yield* get30(this.ref));
      return new TDequeueMerge(first4, yield* subscribe(this.pubsub));
    });
  }
  modify(f) {
    return pipe(get30(this.ref), map15(f), flatMap5(([b, a]) => pipe(set18(this.ref, a), as5(b), zipLeft6(publish2(this.pubsub, a)))));
  }
};
var make80 = (value6) => pipe(all5([unbounded3(), make75(value6)]), map15(([pubsub, ref]) => new TSubscriptionRefImpl(ref, pubsub)));
var get31 = (self) => get30(self.ref);
var set19 = dual(2, (self, value6) => self.modify(() => [void 0, value6]));
var getAndSet7 = dual(2, (self, value6) => self.modify((a) => [a, value6]));
var getAndUpdate7 = dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome7 = dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: () => [a, a],
  onSome: (b) => [a, b]
})));
var setAndGet7 = dual(2, (self, value6) => self.modify(() => [value6, value6]));
var modify10 = dual(2, (self, f) => self.modify(f));
var modifySome7 = dual(3, (self, fallback, f) => self.modify((a) => match2(f(a), {
  onNone: () => [fallback, a],
  onSome: (b) => b
})));
var update12 = dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet7 = dual(2, (self, f) => self.modify((a) => {
  const b = f(a);
  return [b, b];
}));
var updateSome7 = dual(2, (self, f) => self.modify((a) => [void 0, match2(f(a), {
  onNone: () => a,
  onSome: (b) => b
})]));
var updateSomeAndGet7 = dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: () => [a, a],
  onSome: (b) => [b, b]
})));
var changesScoped = (self) => acquireRelease2(self.changes, shutdown);
var changesStream = (self) => unwrap2(map8(self.changes, fromTQueue));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TSubscriptionRef.js
var TSubscriptionRefTypeId2 = TSubscriptionRefTypeId;
var get32 = get31;
var getAndSet8 = getAndSet7;
var getAndUpdate8 = getAndUpdate7;
var getAndUpdateSome8 = getAndUpdateSome7;
var make81 = make80;
var modify11 = modify10;
var modifySome8 = modifySome7;
var set20 = set19;
var setAndGet8 = setAndGet7;
var update13 = update12;
var updateAndGet8 = updateAndGet7;
var updateSome8 = updateSome7;
var updateSomeAndGet8 = updateSomeAndGet7;
var changesScoped2 = changesScoped;
var changesStream2 = changesStream;
var changes = (self) => self.changes;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Take.js
var Take_exports = {};
__export(Take_exports, {
  TakeTypeId: () => TakeTypeId2,
  chunk: () => chunk2,
  die: () => die7,
  dieMessage: () => dieMessage6,
  done: () => done5,
  end: () => end2,
  fail: () => fail9,
  failCause: () => failCause6,
  fromEffect: () => fromEffect8,
  fromExit: () => fromExit2,
  fromPull: () => fromPull2,
  isDone: () => isDone3,
  isFailure: () => isFailure5,
  isSuccess: () => isSuccess5,
  make: () => make82,
  map: () => map19,
  match: () => match16,
  matchEffect: () => matchEffect3,
  of: () => of3,
  tap: () => tap5
});
var TakeTypeId2 = TakeTypeId;
var chunk2 = chunk;
var die7 = die4;
var dieMessage6 = dieMessage3;
var done5 = done2;
var end2 = end;
var fail9 = fail4;
var failCause6 = failCause4;
var fromEffect8 = fromEffect3;
var fromExit2 = fromExit;
var fromPull2 = fromPull;
var isDone3 = isDone2;
var isFailure5 = isFailure3;
var isSuccess5 = isSuccess3;
var make82 = make20;
var map19 = map11;
var match16 = match9;
var matchEffect3 = matchEffect2;
var of3 = of2;
var tap5 = tap3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestAnnotation.js
var TestAnnotation_exports = {};
__export(TestAnnotation_exports, {
  TestAnnotationTypeId: () => TestAnnotationTypeId,
  compose: () => compose,
  fibers: () => fibers,
  ignored: () => ignored,
  isTestAnnotation: () => isTestAnnotation,
  make: () => make83,
  repeated: () => repeated,
  retried: () => retried,
  tagged: () => tagged5
});
var TestAnnotationSymbolKey = "effect/TestAnnotation";
var TestAnnotationTypeId = Symbol.for(TestAnnotationSymbolKey);
var TestAnnotationImpl = class {
  identifier;
  initial;
  combine;
  [TestAnnotationTypeId] = {
    _A: (_) => _
  };
  constructor(identifier, initial, combine5) {
    this.identifier = identifier;
    this.initial = initial;
    this.combine = combine5;
  }
  [symbol2]() {
    return pipe(hash(TestAnnotationSymbolKey), combine(hash(this.identifier)), cached(this));
  }
  [symbol3](that) {
    return isTestAnnotation(that) && this.identifier === that.identifier;
  }
};
var isTestAnnotation = (u) => hasProperty(u, TestAnnotationTypeId);
var make83 = (identifier, initial, combine5) => {
  return new TestAnnotationImpl(identifier, initial, combine5);
};
var compose = (left2, right2) => {
  if (isLeft(left2) && isLeft(right2)) {
    return left(left2.left + right2.left);
  }
  if (isRight(left2) && isRight(right2)) {
    return right(pipe(left2.right, appendAll(right2.right)));
  }
  if (isRight(left2) && isLeft(right2)) {
    return right2;
  }
  if (isLeft(left2) && isRight(right2)) {
    return right2;
  }
  throw new Error(getBugErrorMessage("TestAnnotation.compose"));
};
var fibers = make83("fibers", left(0), compose);
var ignored = make83("ignored", 0, (a, b) => a + b);
var repeated = make83("repeated", 0, (a, b) => a + b);
var retried = make83("retried", 0, (a, b) => a + b);
var tagged5 = make83("tagged", empty3(), (a, b) => pipe(a, union(b)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestAnnotationMap.js
var TestAnnotationMap_exports = {};
__export(TestAnnotationMap_exports, {
  TestAnnotationMapTypeId: () => TestAnnotationMapTypeId,
  annotate: () => annotate,
  combine: () => combine4,
  empty: () => empty25,
  get: () => get33,
  isTestAnnotationMap: () => isTestAnnotationMap,
  make: () => make84,
  overwrite: () => overwrite,
  update: () => update14
});
var TestAnnotationMapTypeId = /* @__PURE__ */ Symbol.for("effect/TestAnnotationMap");
var TestAnnotationMapImpl = class {
  map;
  [TestAnnotationMapTypeId] = TestAnnotationMapTypeId;
  constructor(map22) {
    this.map = map22;
  }
};
var isTestAnnotationMap = (u) => hasProperty(u, TestAnnotationMapTypeId);
var empty25 = () => new TestAnnotationMapImpl(empty4());
var make84 = (map22) => {
  return new TestAnnotationMapImpl(map22);
};
var overwrite = dual(3, (self, key, value6) => make84(set2(self.map, key, value6)));
var update14 = dual(3, (self, key, f) => {
  let value6 = key.initial;
  if (has3(self.map, key)) {
    value6 = unsafeGet2(self.map, key);
  }
  return overwrite(self, key, f(value6));
});
var get33 = dual(2, (self, key) => {
  if (has3(self.map, key)) {
    return unsafeGet2(self.map, key);
  }
  return key.initial;
});
var annotate = dual(3, (self, key, value6) => update14(self, key, (_) => key.combine(_, value6)));
var combine4 = dual(2, (self, that) => {
  let result = self.map;
  for (const entry of that.map) {
    if (has3(result, entry[0])) {
      const value6 = get3(result, entry[0]);
      result = set2(result, entry[0], entry[0].combine(value6, entry[1]));
    } else {
      result = set2(result, entry[0], entry[1]);
    }
  }
  return make84(result);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestAnnotations.js
var TestAnnotations_exports = {};
__export(TestAnnotations_exports, {
  TestAnnotations: () => TestAnnotations,
  TestAnnotationsTypeId: () => TestAnnotationsTypeId,
  isTestAnnotations: () => isTestAnnotations,
  make: () => make85
});
var TestAnnotationsTypeId = /* @__PURE__ */ Symbol.for("effect/TestAnnotations");
var AnnotationsImpl = class {
  ref;
  [TestAnnotationsTypeId] = TestAnnotationsTypeId;
  constructor(ref) {
    this.ref = ref;
  }
  get(key) {
    return map5(get5(this.ref), get33(key));
  }
  annotate(key, value6) {
    return update3(this.ref, annotate(key, value6));
  }
  get supervisedFibers() {
    return descriptorWith((descriptor) => flatMap(this.get(fibers), (either3) => {
      switch (either3._tag) {
        case "Left": {
          return succeed(empty11(Order2));
        }
        case "Right": {
          return pipe(either3.right, forEachSequential((ref) => sync(() => get(ref))), map5(reduce(empty11(Order2), (a, b) => union2(a, b))), map5(filter3((fiber) => !equals(fiber.id(), descriptor.id))));
        }
      }
    }));
  }
};
var TestAnnotations = GenericTag("effect/Annotations");
var isTestAnnotations = (u) => hasProperty(u, TestAnnotationsTypeId);
var make85 = (ref) => new AnnotationsImpl(ref);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestClock.js
var TestClock_exports = {};
__export(TestClock_exports, {
  TestClock: () => TestClock,
  TestClockImpl: () => TestClockImpl,
  adjust: () => adjust,
  adjustWith: () => adjustWith,
  currentTimeMillis: () => currentTimeMillis,
  defaultTestClock: () => defaultTestClock,
  live: () => live3,
  makeData: () => makeData,
  save: () => save,
  setTime: () => setTime,
  sleep: () => sleep2,
  sleeps: () => sleeps,
  testClock: () => testClock,
  testClockWith: () => testClockWith
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/testing/suspendedWarningData.js
var OP_SUSPENDED_WARNING_DATA_START = "Start";
var OP_SUSPENDED_WARNING_DATA_PENDING = "Pending";
var OP_SUSPENDED_WARNING_DATA_DONE = "Done";
var start = {
  _tag: OP_SUSPENDED_WARNING_DATA_START
};
var pending2 = (fiber) => {
  return {
    _tag: OP_SUSPENDED_WARNING_DATA_PENDING,
    fiber
  };
};
var done6 = {
  _tag: OP_SUSPENDED_WARNING_DATA_DONE
};
var isStart = (self) => {
  return self._tag === OP_SUSPENDED_WARNING_DATA_START;
};
var isPending2 = (self) => {
  return self._tag === OP_SUSPENDED_WARNING_DATA_PENDING;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/testing/warningData.js
var OP_WARNING_DATA_START = "Start";
var OP_WARNING_DATA_PENDING = "Pending";
var OP_WARNING_DATA_DONE = "Done";
var start2 = {
  _tag: OP_WARNING_DATA_START
};
var pending3 = (fiber) => {
  return {
    _tag: OP_WARNING_DATA_PENDING,
    fiber
  };
};
var done7 = {
  _tag: OP_WARNING_DATA_DONE
};
var isStart2 = (self) => {
  return self._tag === OP_WARNING_DATA_START;
};
var isPending3 = (self) => {
  return self._tag === OP_WARNING_DATA_PENDING;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestLive.js
var TestLive_exports = {};
__export(TestLive_exports, {
  TestLive: () => TestLive,
  TestLiveTypeId: () => TestLiveTypeId,
  make: () => make86
});
var TestLiveTypeId = /* @__PURE__ */ Symbol.for("effect/TestLive");
var TestLive = GenericTag("effect/TestLive");
var LiveImpl = class {
  services;
  [TestLiveTypeId] = TestLiveTypeId;
  constructor(services) {
    this.services = services;
  }
  provide(effect3) {
    return fiberRefLocallyWith(currentServices, merge(this.services))(effect3);
  }
};
var make86 = (services) => new LiveImpl(services);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestClock.js
var makeData = (instant, sleeps2) => ({
  instant,
  sleeps: sleeps2
});
var TestClock = GenericTag("effect/TestClock");
var warning = "Warning: A test is using time, but is not advancing the test clock, which may result in the test hanging. Use TestClock.adjust to manually advance the time.";
var suspendedWarning = "Warning: A test is advancing the test clock, but a fiber is not suspending, which may result in the test hanging. Use TestAspect.diagnose to identity the fiber that is not suspending.";
var TestClockImpl = class {
  clockState;
  live;
  annotations;
  warningState;
  suspendedWarningState;
  [ClockTypeId] = ClockTypeId;
  constructor(clockState, live6, annotations2, warningState, suspendedWarningState) {
    this.clockState = clockState;
    this.live = live6;
    this.annotations = annotations2;
    this.warningState = warningState;
    this.suspendedWarningState = suspendedWarningState;
    this.currentTimeMillis = map5(get4(this.clockState), (data) => data.instant);
    this.currentTimeNanos = map5(get4(this.clockState), (data) => BigInt(data.instant * 1e6));
  }
  /**
   * Unsafely returns the current time in milliseconds.
   */
  unsafeCurrentTimeMillis() {
    return unsafeGet3(this.clockState).instant;
  }
  /**
   * Unsafely returns the current time in nanoseconds.
   */
  unsafeCurrentTimeNanos() {
    return BigInt(unsafeGet3(this.clockState).instant * 1e6);
  }
  /**
   * Returns the current clock time in milliseconds.
   */
  currentTimeMillis;
  /**
   * Returns the current clock time in nanoseconds.
   */
  currentTimeNanos;
  /**
   * Saves the `TestClock`'s current state in an effect which, when run, will
   * restore the `TestClock` state to the saved state.
   */
  get save() {
    return map5(get4(this.clockState), (data) => set3(this.clockState, data));
  }
  /**
   * Sets the current clock time to the specified instant. Any effects that
   * were scheduled to occur on or before the new time will be run in order.
   */
  setTime(instant) {
    return zipRight(this.warningDone(), this.run(() => instant));
  }
  /**
   * Semantically blocks the current fiber until the clock time is equal to or
   * greater than the specified duration. Once the clock time is adjusted to
   * on or after the duration, the fiber will automatically be resumed.
   */
  sleep(durationInput) {
    const duration = decode(durationInput);
    return flatMap(deferredMake(), (deferred) => pipe(modify(this.clockState, (data) => {
      const end3 = data.instant + toMillis(duration);
      if (end3 > data.instant) {
        return [true, makeData(data.instant, pipe(data.sleeps, prepend2([end3, deferred])))];
      }
      return [false, data];
    }), flatMap((shouldAwait) => shouldAwait ? pipe(this.warningStart(), zipRight(deferredAwait(deferred))) : pipe(deferredSucceed(deferred, void 0), asVoid))));
  }
  /**
   * Returns a list of the times at which all queued effects are scheduled to
   * resume.
   */
  get sleeps() {
    return map5(get4(this.clockState), (data) => map4(data.sleeps, (_) => _[0]));
  }
  /**
   * Increments the current clock time by the specified duration. Any effects
   * that were scheduled to occur on or before the new time will be run in
   * order.
   */
  adjust(durationInput) {
    const duration = decode(durationInput);
    return zipRight(this.warningDone(), this.run((n) => n + toMillis(duration)));
  }
  /**
   * Increments the current clock time by the specified duration. Any effects
   * that were scheduled to occur on or before the new time will be run in
   * order.
   */
  adjustWith(durationInput) {
    const duration = decode(durationInput);
    return (effect3) => zipLeftOptions(effect3, this.adjust(duration), {
      concurrent: true
    });
  }
  /**
   * Returns a set of all fibers in this test.
   */
  supervisedFibers() {
    return this.annotations.supervisedFibers;
  }
  /**
   * Captures a "snapshot" of the identifier and status of all fibers in this
   * test other than the current fiber. Fails with the `void` value if any of
   * these fibers are not done or suspended. Note that because we cannot
   * synchronize on the status of multiple fibers at the same time this
   * snapshot may not be fully consistent.
   */
  freeze() {
    return flatMap(this.supervisedFibers(), (fibers2) => pipe(fibers2, reduce4(empty4(), (map22, fiber) => pipe(fiber.status, flatMap((status) => {
      if (isDone(status)) {
        return succeed(set2(map22, fiber.id(), status));
      }
      if (isSuspended(status)) {
        return succeed(set2(map22, fiber.id(), status));
      }
      return fail(void 0);
    })))));
  }
  /**
   * Forks a fiber that will display a warning message if a test is using time
   * but is not advancing the `TestClock`.
   */
  warningStart() {
    return updateSomeEffect(this.warningState, (data) => isStart2(data) ? some(pipe(this.live.provide(pipe(logWarning(warning), delay(seconds(5)))), interruptible2, fork, map5((fiber) => pending3(fiber)))) : none());
  }
  /**
   * Cancels the warning message that is displayed if a test is using time but
   * is not advancing the `TestClock`.
   */
  warningDone() {
    return updateSomeEffect(this.warningState, (warningData) => {
      if (isStart2(warningData)) {
        return some(succeed(done7));
      }
      if (isPending3(warningData)) {
        return some(pipe(interruptFiber(warningData.fiber), as(done7)));
      }
      return none();
    });
  }
  yieldTimer = async_((resume) => {
    const timer3 = setTimeout(() => {
      resume(void_);
    }, 0);
    return sync(() => clearTimeout(timer3));
  });
  /**
   * Returns whether all descendants of this fiber are done or suspended.
   */
  suspended() {
    return pipe(this.freeze(), zip(pipe(this.yieldTimer, zipRight(this.freeze()))), flatMap(([first4, last4]) => equals(first4, last4) ? succeed(first4) : fail(void 0)));
  }
  /**
   * Polls until all descendants of this fiber are done or suspended.
   */
  awaitSuspended() {
    return pipe(this.suspendedWarningStart(), zipRight(pipe(this.suspended(), zipWith(pipe(this.yieldTimer, zipRight(this.suspended())), equals), filterOrFail(identity, constVoid), eventually)), zipRight(this.suspendedWarningDone()));
  }
  /**
   * Forks a fiber that will display a warning message if a test is advancing
   * the `TestClock` but a fiber is not suspending.
   */
  suspendedWarningStart() {
    return updateSomeEffect(this.suspendedWarningState, (suspendedWarningData) => {
      if (isStart(suspendedWarningData)) {
        return some(pipe(this.live.provide(pipe(logWarning(suspendedWarning), zipRight(set3(this.suspendedWarningState, done6)), delay(seconds(5)))), interruptible2, fork, map5((fiber) => pending2(fiber))));
      }
      return none();
    });
  }
  /**
   * Cancels the warning message that is displayed if a test is advancing the
   * `TestClock` but a fiber is not suspending.
   */
  suspendedWarningDone() {
    return updateSomeEffect(this.suspendedWarningState, (suspendedWarningData) => {
      if (isPending2(suspendedWarningData)) {
        return some(pipe(interruptFiber(suspendedWarningData.fiber), as(start)));
      }
      return none();
    });
  }
  /**
   * Runs all effects scheduled to occur on or before the specified instant,
   * which may depend on the current time, in order.
   */
  run(f) {
    return pipe(this.awaitSuspended(), zipRight(pipe(modify(this.clockState, (data) => {
      const end3 = f(data.instant);
      const sorted = pipe(data.sleeps, sort(pipe(Order, mapInput((_) => _[0]))));
      if (isNonEmpty(sorted)) {
        const [instant, deferred] = headNonEmpty(sorted);
        if (instant <= end3) {
          return [some([end3, deferred]), makeData(instant, tailNonEmpty(sorted))];
        }
      }
      return [none(), makeData(end3, data.sleeps)];
    }), flatMap((option3) => {
      switch (option3._tag) {
        case "None": {
          return void_;
        }
        case "Some": {
          const [end3, deferred] = option3.value;
          return pipe(deferredSucceed(deferred, void 0), zipRight(yieldNow()), zipRight(this.run(() => end3)));
        }
      }
    }))));
  }
};
var live3 = (data) => scoped(TestClock, gen(function* () {
  const live6 = yield* TestLive;
  const annotations2 = yield* TestAnnotations;
  const clockState = yield* sync(() => unsafeMake2(data));
  const warningState = yield* makeSynchronized(start2);
  const suspendedWarningState = yield* makeSynchronized(start);
  const testClock2 = new TestClockImpl(clockState, live6, annotations2, warningState, suspendedWarningState);
  yield* withClockScoped(testClock2);
  yield* addFinalizer(() => zipRight(testClock2.warningDone(), testClock2.suspendedWarningDone()));
  return testClock2;
}));
var defaultTestClock = live3(makeData((/* @__PURE__ */ new Date(0)).getTime(), empty2()));
var adjust = (durationInput) => {
  const duration = decode(durationInput);
  return testClockWith((testClock2) => testClock2.adjust(duration));
};
var adjustWith = dual(2, (effect3, durationInput) => {
  const duration = decode(durationInput);
  return testClockWith((testClock2) => testClock2.adjustWith(duration)(effect3));
});
var save = () => testClockWith((testClock2) => testClock2.save);
var setTime = (input) => testClockWith((testClock2) => testClock2.setTime(typeof input === "number" ? input : unsafeMake5(input).epochMillis));
var sleep2 = (durationInput) => {
  const duration = decode(durationInput);
  return testClockWith((testClock2) => testClock2.sleep(duration));
};
var sleeps = () => testClockWith((testClock2) => testClock2.sleeps);
var testClock = () => testClockWith(succeed);
var testClockWith = (f) => fiberRefGetWith(currentServices, (services) => f(pipe(services, get2(clockTag))));
var currentTimeMillis = testClockWith((testClock2) => testClock2.currentTimeMillis);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestConfig.js
var TestConfig_exports = {};
__export(TestConfig_exports, {
  TestConfig: () => TestConfig,
  make: () => make87
});
var TestConfig = GenericTag("effect/TestConfig");
var make87 = (params) => params;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestContext.js
var TestContext_exports = {};
__export(TestContext_exports, {
  LiveContext: () => LiveContext,
  TestContext: () => TestContext,
  live: () => live5
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestServices.js
var TestServices_exports = {};
__export(TestServices_exports, {
  annotate: () => annotate2,
  annotations: () => annotations,
  annotationsLayer: () => annotationsLayer,
  annotationsWith: () => annotationsWith,
  currentServices: () => currentServices3,
  get: () => get34,
  live: () => live4,
  liveLayer: () => liveLayer,
  liveServices: () => liveServices3,
  liveWith: () => liveWith,
  provideLive: () => provideLive,
  provideWithLive: () => provideWithLive,
  repeats: () => repeats,
  retries: () => retries,
  samples: () => samples,
  shrinks: () => shrinks,
  size: () => size15,
  sized: () => sized,
  sizedLayer: () => sizedLayer,
  sizedWith: () => sizedWith,
  supervisedFibers: () => supervisedFibers,
  testConfig: () => testConfig,
  testConfigLayer: () => testConfigLayer,
  testConfigWith: () => testConfigWith,
  withAnnotations: () => withAnnotations,
  withAnnotationsScoped: () => withAnnotationsScoped,
  withLive: () => withLive,
  withLiveScoped: () => withLiveScoped,
  withSize: () => withSize,
  withSized: () => withSized,
  withSizedScoped: () => withSizedScoped,
  withTestConfig: () => withTestConfig,
  withTestConfigScoped: () => withTestConfigScoped
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestSized.js
var TestSized_exports = {};
__export(TestSized_exports, {
  TestSized: () => TestSized,
  TestSizedTypeId: () => TestSizedTypeId,
  fromFiberRef: () => fromFiberRef,
  make: () => make88
});
var TestSizedTypeId = /* @__PURE__ */ Symbol.for("effect/TestSized");
var TestSized = GenericTag("effect/TestSized");
var SizedImpl = class {
  fiberRef;
  [TestSizedTypeId] = TestSizedTypeId;
  constructor(fiberRef) {
    this.fiberRef = fiberRef;
  }
  get size() {
    return fiberRefGet(this.fiberRef);
  }
  withSize(size18) {
    return (effect3) => fiberRefLocally(this.fiberRef, size18)(effect3);
  }
};
var make88 = (size18) => new SizedImpl(fiberRefUnsafeMake(size18));
var fromFiberRef = (fiberRef) => new SizedImpl(fiberRef);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestServices.js
var liveServices3 = pipe(make3(TestAnnotations, make85(unsafeMake2(empty25()))), add(TestLive, make86(liveServices)), add(TestSized, make88(100)), add(TestConfig, make87({
  repeats: 100,
  retries: 100,
  samples: 200,
  shrinks: 1e3
})));
var currentServices3 = fiberRefUnsafeMakeContext(liveServices3);
var annotations = () => annotationsWith(succeed);
var annotationsWith = (f) => fiberRefGetWith(currentServices3, (services) => f(get2(services, TestAnnotations)));
var withAnnotations = dual(2, (effect3, annotations2) => fiberRefLocallyWith(currentServices3, add(TestAnnotations, annotations2))(effect3));
var withAnnotationsScoped = (annotations2) => fiberRefLocallyScopedWith(currentServices3, add(TestAnnotations, annotations2));
var annotationsLayer = () => scoped(TestAnnotations, pipe(sync(() => unsafeMake2(empty25())), map5(make85), tap(withAnnotationsScoped)));
var get34 = (key) => annotationsWith((annotations2) => annotations2.get(key));
var annotate2 = (key, value6) => annotationsWith((annotations2) => annotations2.annotate(key, value6));
var supervisedFibers = () => annotationsWith((annotations2) => annotations2.supervisedFibers);
var liveWith = (f) => fiberRefGetWith(currentServices3, (services) => f(get2(services, TestLive)));
var live4 = liveWith(succeed);
var withLive = dual(2, (effect3, live6) => fiberRefLocallyWith(currentServices3, add(TestLive, live6))(effect3));
var withLiveScoped = (live6) => fiberRefLocallyScopedWith(currentServices3, add(TestLive, live6));
var liveLayer = () => scoped(TestLive, pipe(context(), map5(make86), tap(withLiveScoped)));
var provideLive = (effect3) => liveWith((live6) => live6.provide(effect3));
var provideWithLive = dual(2, (self, f) => fiberRefGetWith(currentServices, (services) => provideLive(f(fiberRefLocally(currentServices, services)(self)))));
var sizedWith = (f) => fiberRefGetWith(currentServices3, (services) => f(get2(services, TestSized)));
var sized = sizedWith(succeed);
var withSized = dual(2, (effect3, sized2) => fiberRefLocallyWith(currentServices3, add(TestSized, sized2))(effect3));
var withSizedScoped = (sized2) => fiberRefLocallyScopedWith(currentServices3, add(TestSized, sized2));
var sizedLayer = (size18) => scoped(TestSized, pipe(fiberRefMake(size18), map5(fromFiberRef), tap(withSizedScoped)));
var size15 = sizedWith((sized2) => sized2.size);
var withSize = dual(2, (effect3, size18) => sizedWith((sized2) => sized2.withSize(size18)(effect3)));
var testConfigWith = (f) => fiberRefGetWith(currentServices3, (services) => f(get2(services, TestConfig)));
var testConfig = testConfigWith(succeed);
var withTestConfig = dual(2, (effect3, config) => fiberRefLocallyWith(currentServices3, add(TestConfig, config))(effect3));
var withTestConfigScoped = (config) => fiberRefLocallyScopedWith(currentServices3, add(TestConfig, config));
var testConfigLayer = (params) => scoped(TestConfig, suspend2(() => {
  const testConfig2 = make87(params);
  return pipe(withTestConfigScoped(testConfig2), as(testConfig2));
}));
var repeats = testConfigWith((config) => succeed(config.repeats));
var retries = testConfigWith((config) => succeed(config.retries));
var samples = testConfigWith((config) => succeed(config.samples));
var shrinks = testConfigWith((config) => succeed(config.shrinks));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TestContext.js
var live5 = pipe(annotationsLayer(), merge2(liveLayer()), merge2(sizedLayer(100)), merge2(pipe(defaultTestClock, provideMerge(merge2(liveLayer(), annotationsLayer())))), merge2(testConfigLayer({
  repeats: 100,
  retries: 100,
  samples: 200,
  shrinks: 1e3
})));
var LiveContext = syncContext(() => liveServices);
var TestContext = provideMerge(live5, LiveContext);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Trie.js
var Trie_exports = {};
__export(Trie_exports, {
  compact: () => compact2,
  empty: () => empty27,
  entries: () => entries4,
  entriesWithPrefix: () => entriesWithPrefix2,
  filter: () => filter9,
  filterMap: () => filterMap3,
  forEach: () => forEach13,
  fromIterable: () => fromIterable21,
  get: () => get36,
  has: () => has17,
  insert: () => insert3,
  insertMany: () => insertMany2,
  isEmpty: () => isEmpty10,
  keys: () => keys9,
  keysWithPrefix: () => keysWithPrefix2,
  longestPrefixOf: () => longestPrefixOf2,
  make: () => make90,
  map: () => map21,
  modify: () => modify13,
  reduce: () => reduce17,
  remove: () => remove13,
  removeMany: () => removeMany2,
  size: () => size17,
  toEntries: () => toEntries,
  toEntriesWithPrefix: () => toEntriesWithPrefix2,
  unsafeGet: () => unsafeGet8,
  values: () => values7,
  valuesWithPrefix: () => valuesWithPrefix2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/trie.js
var TrieSymbolKey = "effect/Trie";
var TrieTypeId = Symbol.for(TrieSymbolKey);
var trieVariance = {
  /* c8 ignore next */
  _Value: (_) => _
};
var TrieProto = {
  [TrieTypeId]: trieVariance,
  [Symbol.iterator]() {
    return new TrieIterator(this, (k, v) => [k, v], () => true);
  },
  [symbol2]() {
    let hash3 = hash(TrieSymbolKey);
    for (const item of this) {
      hash3 ^= pipe(hash(item[0]), combine(hash(item[1])));
    }
    return cached(this, hash3);
  },
  [symbol3](that) {
    if (isTrie(that)) {
      const entries5 = Array.from(that);
      return Array.from(this).every((itemSelf, i) => {
        const itemThat = entries5[i];
        return equals(itemSelf[0], itemThat[0]) && equals(itemSelf[1], itemThat[1]);
      });
    }
    return false;
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Trie",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl3 = (root) => {
  const trie = Object.create(TrieProto);
  trie._root = root;
  trie._count = root?.count ?? 0;
  return trie;
};
var TrieIterator = class _TrieIterator {
  trie;
  f;
  filter;
  stack = [];
  constructor(trie, f, filter10) {
    this.trie = trie;
    this.f = f;
    this.filter = filter10;
    const root = trie._root !== void 0 ? trie._root : void 0;
    if (root !== void 0) {
      this.stack.push([root, "", false]);
    }
  }
  next() {
    while (this.stack.length > 0) {
      const [node, keyString, isAdded] = this.stack.pop();
      if (isAdded) {
        const value6 = node.value;
        if (value6 !== void 0) {
          const key = keyString + node.key;
          if (this.filter(key, value6)) {
            return {
              done: false,
              value: this.f(key, value6)
            };
          }
        }
      } else {
        this.addToStack(node, keyString);
      }
    }
    return {
      done: true,
      value: void 0
    };
  }
  addToStack(node, keyString) {
    if (node.right !== void 0) {
      this.stack.push([node.right, keyString, false]);
    }
    if (node.mid !== void 0) {
      this.stack.push([node.mid, keyString + node.key, false]);
    }
    this.stack.push([node, keyString, true]);
    if (node.left !== void 0) {
      this.stack.push([node.left, keyString, false]);
    }
  }
  [Symbol.iterator]() {
    return new _TrieIterator(this.trie, this.f, this.filter);
  }
};
var isTrie = (u) => hasProperty(u, TrieTypeId);
var empty26 = () => makeImpl3(void 0);
var fromIterable20 = (entries5) => {
  let trie = empty26();
  for (const [key, value6] of entries5) {
    trie = insert2(trie, key, value6);
  }
  return trie;
};
var make89 = (...entries5) => {
  return fromIterable20(entries5);
};
var insert2 = dual(3, (self, key, value6) => {
  if (key.length === 0) return self;
  const dStack = [];
  const nStack = [];
  let n = self._root ?? {
    key: key[0],
    count: 0
  };
  const count7 = n.count + 1;
  let cIndex = 0;
  while (cIndex < key.length) {
    const c = key[cIndex];
    nStack.push(n);
    if (c > n.key) {
      dStack.push(1);
      if (n.right === void 0) {
        n = {
          key: c,
          count: count7
        };
      } else {
        n = n.right;
      }
    } else if (c < n.key) {
      dStack.push(-1);
      if (n.left === void 0) {
        n = {
          key: c,
          count: count7
        };
      } else {
        n = n.left;
      }
    } else {
      if (cIndex === key.length - 1) {
        n.value = value6;
      } else if (n.mid === void 0) {
        dStack.push(0);
        n = {
          key: key[cIndex + 1],
          count: count7
        };
      } else {
        dStack.push(0);
        n = n.mid;
      }
      cIndex += 1;
    }
  }
  for (let s = nStack.length - 2; s >= 0; --s) {
    const n2 = nStack[s];
    const d = dStack[s];
    if (d === -1) {
      nStack[s] = {
        key: n2.key,
        count: count7,
        value: n2.value,
        left: nStack[s + 1],
        mid: n2.mid,
        right: n2.right
      };
    } else if (d === 1) {
      nStack[s] = {
        key: n2.key,
        count: count7,
        value: n2.value,
        left: n2.left,
        mid: n2.mid,
        right: nStack[s + 1]
      };
    } else {
      nStack[s] = {
        key: n2.key,
        count: count7,
        value: n2.value,
        left: n2.left,
        mid: nStack[s + 1],
        right: n2.right
      };
    }
  }
  nStack[0].count = count7;
  return makeImpl3(nStack[0]);
});
var size16 = (self) => self._root?.count ?? 0;
var isEmpty9 = (self) => size16(self) === 0;
var keys8 = (self) => new TrieIterator(self, (key) => key, () => true);
var values6 = (self) => new TrieIterator(self, (_, value6) => value6, () => true);
var entries3 = (self) => new TrieIterator(self, (key, value6) => [key, value6], () => true);
var reduce16 = dual(3, (self, zero, f) => {
  let accumulator = zero;
  for (const entry of self) {
    accumulator = f(accumulator, entry[1], entry[0]);
  }
  return accumulator;
});
var map20 = dual(2, (self, f) => reduce16(self, empty26(), (trie, value6, key) => insert2(trie, key, f(value6, key))));
var filter8 = dual(2, (self, f) => reduce16(self, empty26(), (trie, value6, key) => f(value6, key) ? insert2(trie, key, value6) : trie));
var filterMap2 = dual(2, (self, f) => reduce16(self, empty26(), (trie, value6, key) => {
  const option3 = f(value6, key);
  return isSome(option3) ? insert2(trie, key, option3.value) : trie;
}));
var compact = (self) => filterMap2(self, identity);
var forEach12 = dual(2, (self, f) => reduce16(self, void 0, (_, value6, key) => f(value6, key)));
var keysWithPrefix = dual(2, (self, prefix) => new TrieIterator(self, (key) => key, (key) => key.startsWith(prefix)));
var valuesWithPrefix = dual(2, (self, prefix) => new TrieIterator(self, (_, value6) => value6, (key) => key.startsWith(prefix)));
var entriesWithPrefix = dual(2, (self, prefix) => new TrieIterator(self, (key, value6) => [key, value6], (key) => key.startsWith(prefix)));
var toEntriesWithPrefix = dual(2, (self, prefix) => Array.from(entriesWithPrefix(self, prefix)));
var get35 = dual(2, (self, key) => {
  let n = self._root;
  if (n === void 0 || key.length === 0) return none();
  let cIndex = 0;
  while (cIndex < key.length) {
    const c = key[cIndex];
    if (c > n.key) {
      if (n.right === void 0) {
        return none();
      } else {
        n = n.right;
      }
    } else if (c < n.key) {
      if (n.left === void 0) {
        return none();
      } else {
        n = n.left;
      }
    } else {
      if (cIndex === key.length - 1) {
        return fromNullable(n.value);
      } else {
        if (n.mid === void 0) {
          return none();
        } else {
          n = n.mid;
          cIndex += 1;
        }
      }
    }
  }
  return none();
});
var has16 = dual(2, (self, key) => isSome(get35(self, key)));
var unsafeGet7 = dual(2, (self, key) => {
  const element = get35(self, key);
  if (isNone(element)) {
    throw new Error("Expected trie to contain key");
  }
  return element.value;
});
var remove12 = dual(2, (self, key) => {
  let n = self._root;
  if (n === void 0 || key.length === 0) return self;
  const count7 = n.count - 1;
  const dStack = [];
  const nStack = [];
  let cIndex = 0;
  while (cIndex < key.length) {
    const c = key[cIndex];
    if (c > n.key) {
      if (n.right === void 0) {
        return self;
      } else {
        nStack.push(n);
        dStack.push(1);
        n = n.right;
      }
    } else if (c < n.key) {
      if (n.left === void 0) {
        return self;
      } else {
        nStack.push(n);
        dStack.push(-1);
        n = n.left;
      }
    } else {
      if (cIndex === key.length - 1) {
        if (n.value !== void 0) {
          nStack.push(n);
          dStack.push(0);
          cIndex += 1;
        } else {
          return self;
        }
      } else {
        if (n.mid === void 0) {
          return self;
        } else {
          nStack.push(n);
          dStack.push(0);
          n = n.mid;
          cIndex += 1;
        }
      }
    }
  }
  const removeNode2 = nStack[nStack.length - 1];
  nStack[nStack.length - 1] = {
    key: removeNode2.key,
    count: count7,
    left: removeNode2.left,
    mid: removeNode2.mid,
    right: removeNode2.right
  };
  for (let s = nStack.length - 2; s >= 0; --s) {
    const n2 = nStack[s];
    const d = dStack[s];
    const child = nStack[s + 1];
    const nc = child.left === void 0 && child.mid === void 0 && child.right === void 0 ? void 0 : child;
    if (d === -1) {
      nStack[s] = {
        key: n2.key,
        count: count7,
        value: n2.value,
        left: nc,
        mid: n2.mid,
        right: n2.right
      };
    } else if (d === 1) {
      nStack[s] = {
        key: n2.key,
        count: count7,
        value: n2.value,
        left: n2.left,
        mid: n2.mid,
        right: nc
      };
    } else {
      nStack[s] = {
        key: n2.key,
        count: count7,
        value: n2.value,
        left: n2.left,
        mid: nc,
        right: n2.right
      };
    }
  }
  nStack[0].count = count7;
  return makeImpl3(nStack[0]);
});
var removeMany = dual(2, (self, keys10) => {
  let trie = self;
  for (const key of keys10) {
    trie = remove12(key)(trie);
  }
  return trie;
});
var insertMany = dual(2, (self, iter) => {
  let trie = self;
  for (const [key, value6] of iter) {
    trie = insert2(key, value6)(trie);
  }
  return trie;
});
var modify12 = dual(3, (self, key, f) => {
  let n = self._root;
  if (n === void 0 || key.length === 0) return self;
  const dStack = [];
  const nStack = [];
  let cIndex = 0;
  while (cIndex < key.length) {
    const c = key[cIndex];
    if (c > n.key) {
      if (n.right === void 0) {
        return self;
      } else {
        nStack.push(n);
        dStack.push(1);
        n = n.right;
      }
    } else if (c < n.key) {
      if (n.left === void 0) {
        return self;
      } else {
        nStack.push(n);
        dStack.push(-1);
        n = n.left;
      }
    } else {
      if (cIndex === key.length - 1) {
        if (n.value !== void 0) {
          nStack.push(n);
          dStack.push(0);
          cIndex += 1;
        } else {
          return self;
        }
      } else {
        if (n.mid === void 0) {
          return self;
        } else {
          nStack.push(n);
          dStack.push(0);
          n = n.mid;
          cIndex += 1;
        }
      }
    }
  }
  const updateNode2 = nStack[nStack.length - 1];
  if (updateNode2.value === void 0) {
    return self;
  }
  nStack[nStack.length - 1] = {
    key: updateNode2.key,
    count: updateNode2.count,
    value: f(updateNode2.value),
    // Update
    left: updateNode2.left,
    mid: updateNode2.mid,
    right: updateNode2.right
  };
  for (let s = nStack.length - 2; s >= 0; --s) {
    const n2 = nStack[s];
    const d = dStack[s];
    const child = nStack[s + 1];
    if (d === -1) {
      nStack[s] = {
        key: n2.key,
        count: n2.count,
        value: n2.value,
        left: child,
        mid: n2.mid,
        right: n2.right
      };
    } else if (d === 1) {
      nStack[s] = {
        key: n2.key,
        count: n2.count,
        value: n2.value,
        left: n2.left,
        mid: n2.mid,
        right: child
      };
    } else {
      nStack[s] = {
        key: n2.key,
        count: n2.count,
        value: n2.value,
        left: n2.left,
        mid: child,
        right: n2.right
      };
    }
  }
  return makeImpl3(nStack[0]);
});
var longestPrefixOf = dual(2, (self, key) => {
  let n = self._root;
  if (n === void 0 || key.length === 0) return none();
  let longestPrefixNode = void 0;
  let cIndex = 0;
  while (cIndex < key.length) {
    const c = key[cIndex];
    if (n.value !== void 0) {
      longestPrefixNode = [key.slice(0, cIndex + 1), n.value];
    }
    if (c > n.key) {
      if (n.right === void 0) {
        break;
      } else {
        n = n.right;
      }
    } else if (c < n.key) {
      if (n.left === void 0) {
        break;
      } else {
        n = n.left;
      }
    } else {
      if (n.mid === void 0) {
        break;
      } else {
        n = n.mid;
        cIndex += 1;
      }
    }
  }
  return fromNullable(longestPrefixNode);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Trie.js
var empty27 = empty26;
var fromIterable21 = fromIterable20;
var make90 = make89;
var insert3 = insert2;
var keys9 = keys8;
var values7 = values6;
var entries4 = entries3;
var toEntries = (self) => Array.from(entries4(self));
var keysWithPrefix2 = keysWithPrefix;
var valuesWithPrefix2 = valuesWithPrefix;
var entriesWithPrefix2 = entriesWithPrefix;
var toEntriesWithPrefix2 = toEntriesWithPrefix;
var longestPrefixOf2 = longestPrefixOf;
var size17 = size16;
var get36 = get35;
var has17 = has16;
var isEmpty10 = isEmpty9;
var unsafeGet8 = unsafeGet7;
var remove13 = remove12;
var reduce17 = reduce16;
var map21 = map20;
var filter9 = filter8;
var filterMap3 = filterMap2;
var compact2 = compact;
var forEach13 = forEach12;
var modify13 = modify12;
var removeMany2 = removeMany;
var insertMany2 = insertMany;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Types.js
var Types_exports = {};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Unify.js
var Unify_exports = {};
__export(Unify_exports, {
  unify: () => unify
});
var unify = identity;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/UpstreamPullRequest.js
var UpstreamPullRequest_exports = {};
__export(UpstreamPullRequest_exports, {
  NoUpstream: () => NoUpstream2,
  Pulled: () => Pulled2,
  UpstreamPullRequestTypeId: () => UpstreamPullRequestTypeId2,
  isNoUpstream: () => isNoUpstream2,
  isPulled: () => isPulled2,
  isUpstreamPullRequest: () => isUpstreamPullRequest2,
  match: () => match17
});
var UpstreamPullRequestTypeId2 = UpstreamPullRequestTypeId;
var Pulled2 = Pulled;
var NoUpstream2 = NoUpstream;
var isUpstreamPullRequest2 = isUpstreamPullRequest;
var isPulled2 = isPulled;
var isNoUpstream2 = isNoUpstream;
var match17 = match5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/UpstreamPullStrategy.js
var UpstreamPullStrategy_exports = {};
__export(UpstreamPullStrategy_exports, {
  PullAfterAllEnqueued: () => PullAfterAllEnqueued2,
  PullAfterNext: () => PullAfterNext2,
  UpstreamPullStrategyTypeId: () => UpstreamPullStrategyTypeId2,
  isPullAfterAllEnqueued: () => isPullAfterAllEnqueued2,
  isPullAfterNext: () => isPullAfterNext2,
  isUpstreamPullStrategy: () => isUpstreamPullStrategy2,
  match: () => match18
});
var UpstreamPullStrategyTypeId2 = UpstreamPullStrategyTypeId;
var PullAfterNext2 = PullAfterNext;
var PullAfterAllEnqueued2 = PullAfterAllEnqueued;
var isUpstreamPullStrategy2 = isUpstreamPullStrategy;
var isPullAfterNext2 = isPullAfterNext;
var isPullAfterAllEnqueued2 = isPullAfterAllEnqueued;
var match18 = match4;
export {
  Arbitrary_exports as Arbitrary,
  Array_exports as Array,
  BigDecimal_exports as BigDecimal,
  BigInt_exports as BigInt,
  Boolean_exports as Boolean,
  Brand_exports as Brand,
  Cache_exports as Cache,
  Cause_exports as Cause,
  Channel_exports as Channel,
  ChildExecutorDecision_exports as ChildExecutorDecision,
  Chunk_exports as Chunk,
  Clock_exports as Clock,
  Config_exports as Config,
  ConfigError_exports as ConfigError,
  ConfigProvider_exports as ConfigProvider,
  ConfigProviderPathPatch_exports as ConfigProviderPathPatch,
  Console_exports as Console,
  Context_exports as Context,
  Cron_exports as Cron,
  Data_exports as Data,
  DateTime_exports as DateTime,
  DefaultServices_exports as DefaultServices,
  Deferred_exports as Deferred,
  Differ_exports as Differ,
  Duration_exports as Duration,
  Effect_exports as Effect,
  Effectable_exports as Effectable,
  Either_exports as Either,
  Encoding_exports as Encoding,
  Equal_exports as Equal,
  Equivalence_exports as Equivalence,
  ExecutionPlan_exports as ExecutionPlan,
  ExecutionStrategy_exports as ExecutionStrategy,
  Exit_exports as Exit,
  FastCheck_exports as FastCheck,
  Fiber_exports as Fiber,
  FiberHandle_exports as FiberHandle,
  FiberId_exports as FiberId,
  FiberMap_exports as FiberMap,
  FiberRef_exports as FiberRef,
  FiberRefs_exports as FiberRefs,
  FiberRefsPatch_exports as FiberRefsPatch,
  FiberSet_exports as FiberSet,
  FiberStatus_exports as FiberStatus,
  Function_exports as Function,
  GlobalValue_exports as GlobalValue,
  Graph_exports as Graph,
  GroupBy_exports as GroupBy,
  HKT_exports as HKT,
  Hash_exports as Hash,
  HashMap_exports as HashMap,
  HashRing_exports as HashRing,
  HashSet_exports as HashSet,
  Inspectable_exports as Inspectable,
  Iterable_exports as Iterable,
  JSONSchema_exports as JSONSchema,
  KeyedPool_exports as KeyedPool,
  Layer_exports as Layer,
  LayerMap_exports as LayerMap,
  List_exports as List,
  LogLevel_exports as LogLevel,
  LogSpan_exports as LogSpan,
  Logger_exports as Logger,
  Mailbox_exports as Mailbox,
  ManagedRuntime_exports as ManagedRuntime,
  Match_exports as Match,
  MergeDecision_exports as MergeDecision,
  MergeState_exports as MergeState,
  MergeStrategy_exports as MergeStrategy,
  Metric_exports as Metric,
  MetricBoundaries_exports as MetricBoundaries,
  MetricHook_exports as MetricHook,
  MetricKey_exports as MetricKey,
  MetricKeyType_exports as MetricKeyType,
  MetricLabel_exports as MetricLabel,
  MetricPair_exports as MetricPair,
  MetricPolling_exports as MetricPolling,
  MetricRegistry_exports as MetricRegistry,
  MetricState_exports as MetricState,
  Micro_exports as Micro,
  ModuleVersion_exports as ModuleVersion,
  MutableHashMap_exports as MutableHashMap,
  MutableHashSet_exports as MutableHashSet,
  MutableList_exports as MutableList,
  MutableQueue_exports as MutableQueue,
  MutableRef_exports as MutableRef,
  NonEmptyIterable_exports as NonEmptyIterable,
  Number_exports as Number,
  Option_exports as Option,
  Order_exports as Order,
  Ordering_exports as Ordering,
  ParseResult_exports as ParseResult,
  PartitionedSemaphore_exports as PartitionedSemaphore,
  Pipeable_exports as Pipeable,
  Pool_exports as Pool,
  Predicate_exports as Predicate,
  Pretty_exports as Pretty,
  PrimaryKey_exports as PrimaryKey,
  PubSub_exports as PubSub,
  Queue_exports as Queue,
  Random_exports as Random,
  RateLimiter_exports as RateLimiter,
  RcMap_exports as RcMap,
  RcRef_exports as RcRef,
  Readable_exports as Readable,
  Record_exports as Record,
  RedBlackTree_exports as RedBlackTree,
  Redacted_exports as Redacted,
  Ref_exports as Ref,
  RegExp_exports as RegExp,
  Reloadable_exports as Reloadable,
  Request_exports as Request,
  RequestBlock_exports as RequestBlock,
  RequestResolver_exports as RequestResolver,
  Resource_exports as Resource,
  Runtime_exports as Runtime,
  RuntimeFlags_exports as RuntimeFlags,
  RuntimeFlagsPatch_exports as RuntimeFlagsPatch,
  STM_exports as STM,
  Schedule_exports as Schedule,
  ScheduleDecision_exports as ScheduleDecision,
  ScheduleInterval_exports as ScheduleInterval,
  ScheduleIntervals_exports as ScheduleIntervals,
  Scheduler_exports as Scheduler,
  Schema_exports as Schema,
  SchemaAST_exports as SchemaAST,
  Scope_exports as Scope,
  ScopedCache_exports as ScopedCache,
  ScopedRef_exports as ScopedRef,
  Secret_exports as Secret,
  SingleProducerAsyncInput_exports as SingleProducerAsyncInput,
  Sink_exports as Sink,
  SortedMap_exports as SortedMap,
  SortedSet_exports as SortedSet,
  Stream_exports as Stream,
  StreamEmit_exports as StreamEmit,
  StreamHaltStrategy_exports as StreamHaltStrategy,
  Streamable_exports as Streamable,
  String_exports as String,
  Struct_exports as Struct,
  Subscribable_exports as Subscribable,
  SubscriptionRef_exports as SubscriptionRef,
  Supervisor_exports as Supervisor,
  Symbol_exports as Symbol,
  SynchronizedRef_exports as SynchronizedRef,
  TArray_exports as TArray,
  TDeferred_exports as TDeferred,
  TMap_exports as TMap,
  TPriorityQueue_exports as TPriorityQueue,
  TPubSub_exports as TPubSub,
  TQueue_exports as TQueue,
  TRandom_exports as TRandom,
  TReentrantLock_exports as TReentrantLock,
  TRef_exports as TRef,
  TSemaphore_exports as TSemaphore,
  TSet_exports as TSet,
  TSubscriptionRef_exports as TSubscriptionRef,
  Take_exports as Take,
  TestAnnotation_exports as TestAnnotation,
  TestAnnotationMap_exports as TestAnnotationMap,
  TestAnnotations_exports as TestAnnotations,
  TestClock_exports as TestClock,
  TestConfig_exports as TestConfig,
  TestContext_exports as TestContext,
  TestLive_exports as TestLive,
  TestServices_exports as TestServices,
  TestSized_exports as TestSized,
  Tracer_exports as Tracer,
  Trie_exports as Trie,
  Tuple_exports as Tuple,
  Types_exports as Types,
  Unify_exports as Unify,
  UpstreamPullRequest_exports as UpstreamPullRequest,
  UpstreamPullStrategy_exports as UpstreamPullStrategy,
  Utils_exports as Utils,
  absurd,
  flow,
  hole,
  identity,
  pipe,
  unsafeCoerce
};
//# sourceMappingURL=effect.js.map
