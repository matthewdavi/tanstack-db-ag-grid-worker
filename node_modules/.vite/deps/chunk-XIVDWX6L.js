import {
  CurrentIterationMetadata,
  driver,
  forever as forever2,
  spaced
} from "./chunk-YQ6ETMON.js";
import {
  CurrentMemoMap,
  EffectTypeId,
  FiberFailureCauseId,
  LayerTypeId,
  MemoMapTypeId,
  _await,
  _void,
  acquireRelease as acquireRelease2,
  acquireUseRelease,
  all as all3,
  annotateLogs,
  annotateSpans,
  as as as2,
  asSome,
  asVoid as asVoid2,
  async,
  build,
  buildWithMemoMap,
  buildWithScope,
  catchAll,
  catchAll2,
  catchAllCause,
  catchAllCause2,
  catchSomeCause,
  clock,
  context,
  context2,
  contextWith,
  currentCache,
  currentCacheEnabled,
  defaultRuntime,
  defaultRuntimeFlags,
  deleteFiberRef,
  die as die3,
  die2 as die4,
  dieMessage,
  dieSync,
  dieSync2,
  disableRuntimeFlag,
  discard,
  dropUntil,
  dropWhile as dropWhile2,
  empty as empty4,
  enableRuntimeFlag,
  ensuring as ensuring2,
  exit,
  extendScope,
  fail as fail3,
  fail2 as fail4,
  fail3 as fail5,
  failCause as failCause2,
  failCause2 as failCause3,
  failCause3 as failCause4,
  failCauseSync,
  failCauseSync2,
  failSync,
  failSync2,
  fiberFailure,
  fiberIdWith,
  fiberRefLocally as fiberRefLocally2,
  fiberRefLocallyScoped as fiberRefLocallyScoped2,
  fiberRefLocallyScopedWith as fiberRefLocallyScopedWith2,
  fiberRefLocallyWith,
  filter as filter2,
  findFirst as findFirst2,
  flatMap as flatMap3,
  flatMap2 as flatMap4,
  flatten as flatten2,
  flatten2 as flatten3,
  forEach as forEach2,
  forever,
  forkDaemon,
  forkIn as forkIn2,
  forkScoped,
  fresh,
  fromEffect,
  fromEffectContext,
  fromEffectDiscard,
  fromFunction,
  gen,
  ignore,
  interrupt2 as interrupt5,
  interruptible as interruptible2,
  intoDeferred,
  isAsyncFiberException,
  isDone,
  isEffect as isEffect2,
  isFiberFailure,
  isFresh,
  isLayer,
  launch,
  locallyEffect,
  make as make6,
  make2 as make8,
  makeMemoMap,
  makeSemaphore,
  makeSpan,
  map as map6,
  map2 as map7,
  mapBoth as mapBoth2,
  mapError,
  mapError2,
  match as match4,
  match2 as match5,
  matchCause,
  matchCause2,
  matchCauseEffect,
  matchEffect as matchEffect2,
  memoize,
  merge as merge3,
  mergeAll,
  mock,
  never,
  onError,
  orDie,
  orDie2,
  orElse as orElse2,
  orElse2 as orElse3,
  passthrough,
  poll as poll3,
  project,
  promise,
  provide,
  provide2,
  provideMerge,
  provideService,
  provideServiceEffect,
  race,
  raceFirst,
  raceWith,
  reduce as reduce2,
  repeat,
  retry,
  runFork,
  runPromise,
  runSync,
  runtime,
  scheduleFromStep,
  scope,
  scope2,
  scopeWith,
  scoped,
  scoped2,
  scopedContext,
  scopedDiscard,
  scopedWith,
  service,
  setFiberRef,
  succeed as succeed3,
  succeed2 as succeed4,
  succeed3 as succeed5,
  succeedContext,
  succeedNone,
  suspend as suspend2,
  suspend2 as suspend3,
  sync as sync2,
  sync2 as sync3,
  syncContext,
  takeWhile as takeWhile2,
  tap as tap2,
  tap2 as tap3,
  tapError,
  tapErrorCause,
  tapErrorCause2,
  timeoutFail,
  timeoutFailCause,
  toRuntime,
  toRuntimeWithMemoMap,
  tryPromise,
  uninterruptible as uninterruptible2,
  uninterruptibleMask as uninterruptibleMask2,
  unsafeDone,
  unsafeFork,
  unsafeMakeLatch,
  unsafeRunCallback,
  unsafeRunPromise,
  unsafeRunPromiseExit,
  unsafeRunSync,
  unsafeRunSyncExit,
  unwrapEffect,
  unwrapScoped,
  updateContext,
  updateFiberRefs,
  updateRuntimeFlags,
  when,
  withFiberRuntime as withFiberRuntime2,
  withParentSpan,
  withSpan,
  zip as zip2,
  zipLeft,
  zipRight as zipRight3,
  zipWith as zipWith2,
  zipWith2 as zipWith3
} from "./chunk-PUCBIOTD.js";
import {
  _await as _await2,
  inheritAll,
  interrupt as interrupt4,
  interruptAs,
  join,
  poll as poll4
} from "./chunk-UDX4HWWS.js";
import {
  IllegalArgumentException,
  NoSuchElementException,
  RuntimeException,
  die as die2,
  fail as fail2,
  failureOrCause,
  flipCauseOption,
  interrupt as interrupt3,
  interruptors,
  isDieType,
  isInterrupted,
  isInterruptedOnly,
  isRuntimeException,
  map as map5,
  parallel,
  scheduleDefectRefailCause,
  squash
} from "./chunk-7AVSUGOT.js";
import {
  addFinalizer as addFinalizer2,
  addFinalizerExit,
  close,
  extend,
  fork,
  make as make7
} from "./chunk-N4COQNTR.js";
import {
  DequeueTypeId,
  DequeueTypeId2,
  EnqueueTypeId,
  awaitShutdown,
  bounded,
  capacity,
  dequeueVariance,
  dropping,
  enqueueVariance,
  isEmpty as isEmpty3,
  isFull,
  isShutdown,
  offer as offer2,
  offerAll as offerAll2,
  poll as poll2,
  shutdown,
  size as size2,
  sliding,
  take as take2,
  takeAll,
  takeBetween,
  takeN,
  takeUpTo,
  unbounded2
} from "./chunk-7QORDSG4.js";
import {
  EmptyMutableQueue,
  isEmpty as isEmpty2,
  offer,
  offerAll,
  poll,
  pollUpTo,
  unbounded
} from "./chunk-NKVUKIFR.js";
import {
  forkIn,
  unsafeMakeSemaphore
} from "./chunk-CBQWHB33.js";
import {
  Class,
  TypeId,
  Zip,
  acquireRelease,
  addFinalizer,
  addSpanStackTrace,
  all as all2,
  bind,
  bindTo as bindTo2,
  clockTag,
  currentLoggers,
  currentMinimumLogLevel,
  currentRuntimeFlags,
  currentScheduler,
  currentServices,
  currentSupervisor,
  currentTimeMillis,
  endSpan,
  ensuring,
  fiberRefLocallyScoped,
  fiberRefLocallyScopedWith,
  fiberRefMake,
  fiberRefMakeContext,
  fiberRefMakeRuntimeFlags,
  fiberRefMakeWith,
  fiberRefUnsafeMakeSupervisor,
  forEachConcurrentDiscard,
  forEachParUnbounded,
  get2 as get3,
  getAndSet2 as getAndSet,
  make2 as make5,
  makeSpanScoped,
  modify2 as modify,
  nextPow2,
  randomTag,
  scopeMake,
  scopeTag,
  sequential,
  sequential2,
  set2 as set3,
  sleep,
  sleep2,
  spanTag,
  update2 as update,
  withConfigProviderScoped,
  withRuntimeFlagsScoped,
  withTracerScoped
} from "./chunk-WCCQGKCT.js";
import {
  CooperativeYielding,
  FiberRefTypeId,
  Interruption,
  InvalidPubSubCapacityException,
  OpSupervision,
  RuntimeMetrics,
  SingleShotGen,
  WindDown,
  add,
  add2,
  all,
  append2 as append,
  appendAll2 as appendAll,
  as2 as as,
  asVoid,
  combineAll,
  currentConcurrency,
  currentContext,
  currentInterruptedCause,
  currentLogAnnotations,
  currentLogLevel,
  currentLogSpan,
  currentMaxOpsBeforeYield,
  currentMetricLabels,
  currentRequestBatching,
  currentSchedulingPriority,
  currentTracerEnabled,
  currentTracerSpanAnnotations,
  currentTracerSpanLinks,
  currentTracerTimingEnabled,
  currentUnhandledErrorLogLevel,
  currentVersionMismatchErrorLogLevel,
  deferredAwait,
  deferredInterruptWith,
  deferredMake,
  deferredSucceed,
  deferredUnsafeDone,
  deferredUnsafeMake,
  die3 as die,
  disable,
  drop,
  dropWhile,
  empty3 as empty,
  empty5 as empty2,
  empty6 as empty3,
  enable,
  every,
  exitInterrupt,
  exitVoid,
  fail3 as fail,
  failCause2 as failCause,
  fiberId,
  fiberRefDelete,
  fiberRefGet,
  fiberRefGetAndSet,
  fiberRefGetAndUpdate,
  fiberRefGetAndUpdateSome,
  fiberRefGetWith,
  fiberRefLocally,
  fiberRefModify,
  fiberRefModifySome,
  fiberRefReset,
  fiberRefSet,
  fiberRefUnsafeMake,
  fiberRefUnsafeMakeContext,
  fiberRefUnsafeMakeHashSet,
  fiberRefUnsafeMakePatch,
  fiberRefUpdate,
  fiberRefUpdateAndGet,
  fiberRefUpdateSome,
  fiberRefUpdateSomeAndGet,
  filter3 as filter,
  filterMap2 as filterMap,
  filterMapWhile,
  findFirst2 as findFirst,
  flatMap2 as flatMap,
  flatMap4 as flatMap2,
  flatten2 as flatten,
  forEach,
  forEachEffect,
  fromIterable,
  fromIterable2,
  get,
  get2,
  has2 as has,
  has3 as has2,
  head2 as head,
  head3 as head2,
  interrupt2 as interrupt,
  interrupt3 as interrupt2,
  interruptFiber,
  interruptible2 as interruptible,
  isChunk,
  isContext,
  isEffect,
  isEmpty,
  isExit,
  isFailure2 as isFailure,
  isNonEmpty,
  isNonEmptyArray2 as isNonEmptyArray,
  isSuccess,
  last2 as last,
  make,
  make3 as make2,
  make4 as make3,
  make5 as make4,
  map3 as map2,
  map5 as map3,
  map6 as map4,
  mapAccum,
  mapBoth2 as mapBoth,
  match3,
  matchEffect2 as matchEffect,
  merge as merge2,
  of2 as of,
  onInterrupt,
  partitionMap2 as partitionMap,
  prepend2 as prepend,
  range,
  reduce3 as reduce,
  reduceRight2 as reduceRight,
  remove,
  scopeClose,
  set,
  set2,
  size,
  span,
  splitAt2 as splitAt,
  splitWhere,
  succeed,
  succeed2,
  suspend,
  sync,
  take,
  takeRight,
  takeWhile,
  tap,
  toReadonlyArray,
  uninterruptible,
  uninterruptibleMask,
  unsafeFromArray,
  unsafeGet2 as unsafeGet,
  unsafeGet3 as unsafeGet2,
  unsafeGet4 as unsafeGet3,
  unsafeHead2 as unsafeHead,
  unsafeLast,
  void_,
  void_2,
  whenEffect,
  withFiberRuntime,
  zip4 as zip,
  zipRight,
  zipRight2,
  zipWith
} from "./chunk-ZLEQSUEA.js";
import {
  left,
  match,
  merge,
  right
} from "./chunk-TVBZ6A6Y.js";
import {
  decode,
  greaterThan,
  isFinite,
  millis,
  toMillis,
  zero
} from "./chunk-AQW37YSD.js";
import {
  OP_COMMIT,
  StreamTypeId,
  YieldWrap,
  bindTo,
  cached,
  combine,
  constFalse,
  constTrue,
  constUndefined,
  constVoid,
  dual,
  effectVariance,
  equals,
  fromNullable,
  getOrElse,
  getOrThrow,
  hasProperty,
  hash,
  identity,
  internalCall,
  isIterable,
  isNone2 as isNone,
  isSome,
  let_,
  map,
  match as match2,
  none2 as none,
  orElse,
  pipe,
  pipeArguments,
  random,
  some2 as some,
  symbol2 as symbol,
  symbol3 as symbol2,
  yieldWrapGet
} from "./chunk-H62K6C3Q.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Stream.js
var Stream_exports = {};
__export(Stream_exports, {
  DefaultChunkSize: () => DefaultChunkSize2,
  Do: () => Do2,
  StreamTypeId: () => StreamTypeId3,
  accumulate: () => accumulate2,
  accumulateChunks: () => accumulateChunks2,
  acquireRelease: () => acquireRelease4,
  aggregate: () => aggregate2,
  aggregateWithin: () => aggregateWithin2,
  aggregateWithinEither: () => aggregateWithinEither2,
  as: () => as7,
  async: () => _async2,
  asyncEffect: () => asyncEffect2,
  asyncPush: () => asyncPush2,
  asyncScoped: () => asyncScoped2,
  bind: () => bind4,
  bindEffect: () => bindEffect2,
  bindTo: () => bindTo5,
  branchAfter: () => branchAfter2,
  broadcast: () => broadcast2,
  broadcastDynamic: () => broadcastDynamic2,
  broadcastedQueues: () => broadcastedQueues2,
  broadcastedQueuesDynamic: () => broadcastedQueuesDynamic2,
  buffer: () => buffer3,
  bufferChunks: () => bufferChunks2,
  catchAll: () => catchAll7,
  catchAllCause: () => catchAllCause6,
  catchSome: () => catchSome3,
  catchSomeCause: () => catchSomeCause3,
  catchTag: () => catchTag3,
  catchTags: () => catchTags3,
  changes: () => changes2,
  changesWith: () => changesWith2,
  changesWithEffect: () => changesWithEffect2,
  chunks: () => chunks2,
  chunksWith: () => chunksWith2,
  combine: () => combine3,
  combineChunks: () => combineChunks2,
  concat: () => concat2,
  concatAll: () => concatAll3,
  context: () => context8,
  contextWith: () => contextWith6,
  contextWithEffect: () => contextWithEffect4,
  contextWithStream: () => contextWithStream2,
  cross: () => cross2,
  crossLeft: () => crossLeft2,
  crossRight: () => crossRight2,
  crossWith: () => crossWith2,
  debounce: () => debounce2,
  decodeText: () => decodeText2,
  die: () => die11,
  dieMessage: () => dieMessage6,
  dieSync: () => dieSync7,
  distributedWith: () => distributedWith2,
  distributedWithDynamic: () => distributedWithDynamic2,
  drain: () => drain4,
  drainFork: () => drainFork2,
  drop: () => drop4,
  dropRight: () => dropRight2,
  dropUntil: () => dropUntil4,
  dropUntilEffect: () => dropUntilEffect3,
  dropWhile: () => dropWhile5,
  dropWhileEffect: () => dropWhileEffect3,
  either: () => either3,
  empty: () => empty7,
  encodeText: () => encodeText2,
  ensuring: () => ensuring7,
  ensuringWith: () => ensuringWith4,
  execute: () => execute2,
  fail: () => fail13,
  failCause: () => failCause11,
  failCauseSync: () => failCauseSync7,
  failSync: () => failSync8,
  filter: () => filter6,
  filterEffect: () => filterEffect2,
  filterMap: () => filterMap3,
  filterMapEffect: () => filterMapEffect2,
  filterMapWhile: () => filterMapWhile3,
  filterMapWhileEffect: () => filterMapWhileEffect2,
  finalizer: () => finalizer2,
  find: () => find2,
  findEffect: () => findEffect3,
  flatMap: () => flatMap10,
  flatten: () => flatten8,
  flattenChunks: () => flattenChunks2,
  flattenEffect: () => flattenEffect2,
  flattenExitOption: () => flattenExitOption2,
  flattenIterables: () => flattenIterables2,
  flattenTake: () => flattenTake2,
  forever: () => forever4,
  fromAsyncIterable: () => fromAsyncIterable2,
  fromChannel: () => fromChannel3,
  fromChunk: () => fromChunk2,
  fromChunkPubSub: () => fromChunkPubSub2,
  fromChunkQueue: () => fromChunkQueue2,
  fromChunks: () => fromChunks2,
  fromEffect: () => fromEffect7,
  fromEffectOption: () => fromEffectOption2,
  fromEventListener: () => fromEventListener2,
  fromIterable: () => fromIterable4,
  fromIterableEffect: () => fromIterableEffect2,
  fromIteratorSucceed: () => fromIteratorSucceed2,
  fromPubSub: () => fromPubSub4,
  fromPull: () => fromPull3,
  fromQueue: () => fromQueue4,
  fromReadableStream: () => fromReadableStream2,
  fromReadableStreamByob: () => fromReadableStreamByob2,
  fromSchedule: () => fromSchedule2,
  fromTPubSub: () => fromTPubSub2,
  fromTQueue: () => fromTQueue2,
  groupAdjacentBy: () => groupAdjacentBy2,
  groupBy: () => groupBy2,
  groupByKey: () => groupByKey2,
  grouped: () => grouped2,
  groupedWithin: () => groupedWithin2,
  haltAfter: () => haltAfter2,
  haltWhen: () => haltWhen2,
  haltWhenDeferred: () => haltWhenDeferred2,
  identity: () => identity2,
  interleave: () => interleave2,
  interleaveWith: () => interleaveWith2,
  interruptAfter: () => interruptAfter2,
  interruptWhen: () => interruptWhen3,
  interruptWhenDeferred: () => interruptWhenDeferred3,
  intersperse: () => intersperse2,
  intersperseAffixes: () => intersperseAffixes2,
  iterate: () => iterate3,
  let: () => let_4,
  make: () => make22,
  map: () => map14,
  mapAccum: () => mapAccum3,
  mapAccumEffect: () => mapAccumEffect2,
  mapBoth: () => mapBoth5,
  mapChunks: () => mapChunks2,
  mapChunksEffect: () => mapChunksEffect2,
  mapConcat: () => mapConcat2,
  mapConcatChunk: () => mapConcatChunk2,
  mapConcatChunkEffect: () => mapConcatChunkEffect2,
  mapConcatEffect: () => mapConcatEffect2,
  mapEffect: () => mapEffect3,
  mapError: () => mapError8,
  mapErrorCause: () => mapErrorCause3,
  mapInputContext: () => mapInputContext4,
  merge: () => merge7,
  mergeAll: () => mergeAll6,
  mergeEither: () => mergeEither2,
  mergeLeft: () => mergeLeft2,
  mergeRight: () => mergeRight2,
  mergeWith: () => mergeWith3,
  mergeWithTag: () => mergeWithTag2,
  mkString: () => mkString3,
  never: () => never5,
  onDone: () => onDone2,
  onEnd: () => onEnd2,
  onError: () => onError3,
  onStart: () => onStart2,
  orDie: () => orDie7,
  orDieWith: () => orDieWith4,
  orElse: () => orElse9,
  orElseEither: () => orElseEither3,
  orElseFail: () => orElseFail3,
  orElseIfEmpty: () => orElseIfEmpty2,
  orElseIfEmptyChunk: () => orElseIfEmptyChunk2,
  orElseIfEmptyStream: () => orElseIfEmptyStream2,
  orElseSucceed: () => orElseSucceed3,
  paginate: () => paginate2,
  paginateChunk: () => paginateChunk2,
  paginateChunkEffect: () => paginateChunkEffect2,
  paginateEffect: () => paginateEffect2,
  partition: () => partition3,
  partitionEither: () => partitionEither2,
  peel: () => peel2,
  pipeThrough: () => pipeThrough2,
  pipeThroughChannel: () => pipeThroughChannel2,
  pipeThroughChannelOrFail: () => pipeThroughChannelOrFail2,
  prepend: () => prepend3,
  provideContext: () => provideContext5,
  provideLayer: () => provideLayer3,
  provideService: () => provideService6,
  provideServiceEffect: () => provideServiceEffect3,
  provideServiceStream: () => provideServiceStream2,
  provideSomeContext: () => provideSomeContext3,
  provideSomeLayer: () => provideSomeLayer3,
  race: () => race4,
  raceAll: () => raceAll2,
  range: () => range3,
  rechunk: () => rechunk2,
  refineOrDie: () => refineOrDie4,
  refineOrDieWith: () => refineOrDieWith4,
  repeat: () => repeat3,
  repeatEffect: () => repeatEffect2,
  repeatEffectChunk: () => repeatEffectChunk2,
  repeatEffectChunkOption: () => repeatEffectChunkOption2,
  repeatEffectOption: () => repeatEffectOption2,
  repeatEffectWithSchedule: () => repeatEffectWithSchedule2,
  repeatEither: () => repeatEither2,
  repeatElements: () => repeatElements2,
  repeatElementsWith: () => repeatElementsWith2,
  repeatValue: () => repeatValue2,
  repeatWith: () => repeatWith2,
  retry: () => retry6,
  run: () => run3,
  runCollect: () => runCollect3,
  runCount: () => runCount2,
  runDrain: () => runDrain3,
  runFold: () => runFold2,
  runFoldEffect: () => runFoldEffect2,
  runFoldScoped: () => runFoldScoped2,
  runFoldScopedEffect: () => runFoldScopedEffect2,
  runFoldWhile: () => runFoldWhile2,
  runFoldWhileEffect: () => runFoldWhileEffect2,
  runFoldWhileScoped: () => runFoldWhileScoped2,
  runFoldWhileScopedEffect: () => runFoldWhileScopedEffect2,
  runForEach: () => runForEach2,
  runForEachChunk: () => runForEachChunk2,
  runForEachChunkScoped: () => runForEachChunkScoped2,
  runForEachScoped: () => runForEachScoped2,
  runForEachWhile: () => runForEachWhile2,
  runForEachWhileScoped: () => runForEachWhileScoped2,
  runHead: () => runHead2,
  runIntoPubSub: () => runIntoPubSub2,
  runIntoPubSubScoped: () => runIntoPubSubScoped2,
  runIntoQueue: () => runIntoQueue2,
  runIntoQueueElementsScoped: () => runIntoQueueElementsScoped2,
  runIntoQueueScoped: () => runIntoQueueScoped2,
  runLast: () => runLast2,
  runScoped: () => runScoped3,
  runSum: () => runSum2,
  scan: () => scan2,
  scanEffect: () => scanEffect2,
  scanReduce: () => scanReduce2,
  scanReduceEffect: () => scanReduceEffect2,
  schedule: () => schedule2,
  scheduleWith: () => scheduleWith2,
  scoped: () => scoped6,
  scopedWith: () => scopedWith4,
  share: () => share2,
  sliding: () => sliding9,
  slidingSize: () => slidingSize2,
  some: () => some5,
  someOrElse: () => someOrElse2,
  someOrFail: () => someOrFail2,
  split: () => split2,
  splitLines: () => splitLines3,
  splitOnChunk: () => splitOnChunk2,
  succeed: () => succeed12,
  suspend: () => suspend10,
  sync: () => sync9,
  take: () => take8,
  takeRight: () => takeRight3,
  takeUntil: () => takeUntil2,
  takeUntilEffect: () => takeUntilEffect2,
  takeWhile: () => takeWhile4,
  tap: () => tap8,
  tapBoth: () => tapBoth3,
  tapError: () => tapError5,
  tapErrorCause: () => tapErrorCause5,
  tapSink: () => tapSink2,
  throttle: () => throttle2,
  throttleEffect: () => throttleEffect2,
  tick: () => tick2,
  timeout: () => timeout2,
  timeoutFail: () => timeoutFail3,
  timeoutFailCause: () => timeoutFailCause3,
  timeoutTo: () => timeoutTo2,
  toAsyncIterable: () => toAsyncIterable2,
  toAsyncIterableEffect: () => toAsyncIterableEffect2,
  toAsyncIterableRuntime: () => toAsyncIterableRuntime2,
  toChannel: () => toChannel3,
  toPubSub: () => toPubSub3,
  toPull: () => toPull3,
  toQueue: () => toQueue3,
  toQueueOfElements: () => toQueueOfElements2,
  toReadableStream: () => toReadableStream2,
  toReadableStreamEffect: () => toReadableStreamEffect2,
  toReadableStreamRuntime: () => toReadableStreamRuntime2,
  transduce: () => transduce2,
  unfold: () => unfold2,
  unfoldChunk: () => unfoldChunk2,
  unfoldChunkEffect: () => unfoldChunkEffect2,
  unfoldEffect: () => unfoldEffect2,
  unwrap: () => unwrap4,
  unwrapScoped: () => unwrapScoped6,
  unwrapScopedWith: () => unwrapScopedWith4,
  updateService: () => updateService4,
  void: () => void_7,
  when: () => when4,
  whenCase: () => whenCase2,
  whenCaseEffect: () => whenCaseEffect2,
  whenEffect: () => whenEffect3,
  withExecutionPlan: () => withExecutionPlan2,
  withSpan: () => withSpan5,
  zip: () => zip7,
  zipAll: () => zipAll2,
  zipAllLeft: () => zipAllLeft2,
  zipAllRight: () => zipAllRight2,
  zipAllSortedByKey: () => zipAllSortedByKey2,
  zipAllSortedByKeyLeft: () => zipAllSortedByKeyLeft2,
  zipAllSortedByKeyRight: () => zipAllSortedByKeyRight2,
  zipAllSortedByKeyWith: () => zipAllSortedByKeyWith2,
  zipAllWith: () => zipAllWith2,
  zipFlatten: () => zipFlatten2,
  zipLatest: () => zipLatest2,
  zipLatestAll: () => zipLatestAll2,
  zipLatestWith: () => zipLatestWith2,
  zipLeft: () => zipLeft6,
  zipRight: () => zipRight8,
  zipWith: () => zipWith8,
  zipWithChunks: () => zipWithChunks2,
  zipWithIndex: () => zipWithIndex2,
  zipWithNext: () => zipWithNext2,
  zipWithPrevious: () => zipWithPrevious2,
  zipWithPreviousAndNext: () => zipWithPreviousAndNext2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRef.js
var FiberRef_exports = {};
__export(FiberRef_exports, {
  FiberRefTypeId: () => FiberRefTypeId2,
  currentConcurrency: () => currentConcurrency2,
  currentContext: () => currentContext2,
  currentLogAnnotations: () => currentLogAnnotations2,
  currentLogLevel: () => currentLogLevel2,
  currentLogSpan: () => currentLogSpan2,
  currentLoggers: () => currentLoggers2,
  currentMaxOpsBeforeYield: () => currentMaxOpsBeforeYield2,
  currentMetricLabels: () => currentMetricLabels2,
  currentMinimumLogLevel: () => currentMinimumLogLevel2,
  currentRequestBatchingEnabled: () => currentRequestBatchingEnabled,
  currentRequestCache: () => currentRequestCache,
  currentRequestCacheEnabled: () => currentRequestCacheEnabled,
  currentRuntimeFlags: () => currentRuntimeFlags2,
  currentScheduler: () => currentScheduler2,
  currentSchedulingPriority: () => currentSchedulingPriority2,
  currentSupervisor: () => currentSupervisor2,
  currentTracerEnabled: () => currentTracerEnabled2,
  currentTracerSpanAnnotations: () => currentTracerSpanAnnotations2,
  currentTracerSpanLinks: () => currentTracerSpanLinks2,
  currentTracerTimingEnabled: () => currentTracerTimingEnabled2,
  delete: () => _delete,
  get: () => get4,
  getAndSet: () => getAndSet2,
  getAndUpdate: () => getAndUpdate,
  getAndUpdateSome: () => getAndUpdateSome,
  getWith: () => getWith,
  interruptedCause: () => interruptedCause,
  make: () => make9,
  makeContext: () => makeContext,
  makeRuntimeFlags: () => makeRuntimeFlags,
  makeWith: () => makeWith,
  modify: () => modify2,
  modifySome: () => modifySome,
  reset: () => reset,
  set: () => set4,
  unhandledErrorLogLevel: () => unhandledErrorLogLevel,
  unsafeMake: () => unsafeMake,
  unsafeMakeContext: () => unsafeMakeContext,
  unsafeMakeHashSet: () => unsafeMakeHashSet,
  unsafeMakePatch: () => unsafeMakePatch,
  unsafeMakeSupervisor: () => unsafeMakeSupervisor,
  update: () => update2,
  updateAndGet: () => updateAndGet,
  updateSome: () => updateSome,
  updateSomeAndGet: () => updateSomeAndGet,
  versionMismatchErrorLogLevel: () => versionMismatchErrorLogLevel
});
var FiberRefTypeId2 = FiberRefTypeId;
var make9 = fiberRefMake;
var makeWith = fiberRefMakeWith;
var makeContext = fiberRefMakeContext;
var makeRuntimeFlags = fiberRefMakeRuntimeFlags;
var unsafeMake = fiberRefUnsafeMake;
var unsafeMakeHashSet = fiberRefUnsafeMakeHashSet;
var unsafeMakeContext = fiberRefUnsafeMakeContext;
var unsafeMakeSupervisor = fiberRefUnsafeMakeSupervisor;
var unsafeMakePatch = fiberRefUnsafeMakePatch;
var get4 = fiberRefGet;
var getAndSet2 = fiberRefGetAndSet;
var getAndUpdate = fiberRefGetAndUpdate;
var getAndUpdateSome = fiberRefGetAndUpdateSome;
var getWith = fiberRefGetWith;
var set4 = fiberRefSet;
var _delete = fiberRefDelete;
var reset = fiberRefReset;
var modify2 = fiberRefModify;
var modifySome = fiberRefModifySome;
var update2 = fiberRefUpdate;
var updateSome = fiberRefUpdateSome;
var updateAndGet = fiberRefUpdateAndGet;
var updateSomeAndGet = fiberRefUpdateSomeAndGet;
var currentConcurrency2 = currentConcurrency;
var currentRequestBatchingEnabled = currentRequestBatching;
var currentRequestCache = currentCache;
var currentRequestCacheEnabled = currentCacheEnabled;
var currentContext2 = currentContext;
var currentSchedulingPriority2 = currentSchedulingPriority;
var currentMaxOpsBeforeYield2 = currentMaxOpsBeforeYield;
var unhandledErrorLogLevel = currentUnhandledErrorLogLevel;
var versionMismatchErrorLogLevel = currentVersionMismatchErrorLogLevel;
var currentLogAnnotations2 = currentLogAnnotations;
var currentLoggers2 = currentLoggers;
var currentLogLevel2 = currentLogLevel;
var currentMinimumLogLevel2 = currentMinimumLogLevel;
var currentLogSpan2 = currentLogSpan;
var currentRuntimeFlags2 = currentRuntimeFlags;
var currentScheduler2 = currentScheduler;
var currentSupervisor2 = currentSupervisor;
var currentMetricLabels2 = currentMetricLabels;
var currentTracerEnabled2 = currentTracerEnabled;
var currentTracerTimingEnabled2 = currentTracerTimingEnabled;
var currentTracerSpanAnnotations2 = currentTracerSpanAnnotations;
var currentTracerSpanLinks2 = currentTracerSpanLinks;
var interruptedCause = currentInterruptedCause;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Layer.js
var Layer_exports = {};
__export(Layer_exports, {
  CurrentMemoMap: () => CurrentMemoMap2,
  LayerTypeId: () => LayerTypeId2,
  MemoMapTypeId: () => MemoMapTypeId2,
  annotateLogs: () => annotateLogs2,
  annotateSpans: () => annotateSpans2,
  build: () => build2,
  buildWithMemoMap: () => buildWithMemoMap2,
  buildWithScope: () => buildWithScope2,
  catchAll: () => catchAll3,
  catchAllCause: () => catchAllCause3,
  context: () => context3,
  die: () => die5,
  dieSync: () => dieSync3,
  discard: () => discard2,
  effect: () => effect,
  effectContext: () => effectContext,
  effectDiscard: () => effectDiscard,
  empty: () => empty5,
  extendScope: () => extendScope2,
  fail: () => fail6,
  failCause: () => failCause5,
  failCauseSync: () => failCauseSync3,
  failSync: () => failSync3,
  fiberRefLocallyScopedWith: () => fiberRefLocallyScopedWith3,
  flatMap: () => flatMap5,
  flatten: () => flatten4,
  fresh: () => fresh2,
  function: () => fromFunction2,
  isFresh: () => isFresh2,
  isLayer: () => isLayer2,
  launch: () => launch2,
  locally: () => locally,
  locallyEffect: () => locallyEffect2,
  locallyScoped: () => locallyScoped,
  locallyWith: () => locallyWith,
  makeMemoMap: () => makeMemoMap2,
  map: () => map8,
  mapError: () => mapError3,
  match: () => match6,
  matchCause: () => matchCause3,
  memoize: () => memoize2,
  merge: () => merge4,
  mergeAll: () => mergeAll2,
  mock: () => mock2,
  orDie: () => orDie3,
  orElse: () => orElse4,
  parentSpan: () => parentSpan2,
  passthrough: () => passthrough2,
  project: () => project2,
  provide: () => provide3,
  provideMerge: () => provideMerge2,
  retry: () => retry2,
  scope: () => scope3,
  scoped: () => scoped3,
  scopedContext: () => scopedContext2,
  scopedDiscard: () => scopedDiscard2,
  service: () => service2,
  setClock: () => setClock,
  setConfigProvider: () => setConfigProvider2,
  setRandom: () => setRandom,
  setRequestBatching: () => setRequestBatching,
  setRequestCache: () => setRequestCache,
  setRequestCaching: () => setRequestCaching,
  setScheduler: () => setScheduler,
  setTracer: () => setTracer2,
  setTracerEnabled: () => setTracerEnabled,
  setTracerTiming: () => setTracerTiming,
  setUnhandledErrorLogLevel: () => setUnhandledErrorLogLevel,
  setVersionMismatchErrorLogLevel: () => setVersionMismatchErrorLogLevel,
  span: () => span3,
  succeed: () => succeed6,
  succeedContext: () => succeedContext2,
  suspend: () => suspend4,
  sync: () => sync4,
  syncContext: () => syncContext2,
  tap: () => tap4,
  tapError: () => tapError2,
  tapErrorCause: () => tapErrorCause3,
  toRuntime: () => toRuntime2,
  toRuntimeWithMemoMap: () => toRuntimeWithMemoMap2,
  unwrapEffect: () => unwrapEffect2,
  unwrapScoped: () => unwrapScoped2,
  updateService: () => updateService,
  withParentSpan: () => withParentSpan2,
  withSpan: () => withSpan2,
  zipWith: () => zipWith4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/layer/circular.js
var minimumLogLevel = (level) => scopedDiscard(fiberRefLocallyScoped(currentMinimumLogLevel, level));
var withMinimumLogLevel = dual(2, (self, level) => fiberRefLocally(currentMinimumLogLevel, level)(self));
var addLogger = (logger) => scopedDiscard(fiberRefLocallyScopedWith(currentLoggers, add2(logger)));
var addLoggerEffect = (effect4) => unwrapEffect(map3(effect4, addLogger));
var addLoggerScoped = (effect4) => unwrapScoped(map3(effect4, addLogger));
var removeLogger = (logger) => scopedDiscard(fiberRefLocallyScopedWith(currentLoggers, remove(logger)));
var replaceLogger = dual(2, (self, that) => flatMap3(removeLogger(self), () => addLogger(that)));
var replaceLoggerEffect = dual(2, (self, that) => flatMap3(removeLogger(self), () => addLoggerEffect(that)));
var replaceLoggerScoped = dual(2, (self, that) => flatMap3(removeLogger(self), () => addLoggerScoped(that)));
var addSupervisor = (supervisor) => scopedDiscard(fiberRefLocallyScopedWith(currentSupervisor, (current2) => new Zip(current2, supervisor)));
var enableCooperativeYielding = scopedDiscard(withRuntimeFlagsScoped(enable(CooperativeYielding)));
var enableInterruption = scopedDiscard(withRuntimeFlagsScoped(enable(Interruption)));
var enableOpSupervision = scopedDiscard(withRuntimeFlagsScoped(enable(OpSupervision)));
var enableRuntimeMetrics = scopedDiscard(withRuntimeFlagsScoped(enable(RuntimeMetrics)));
var enableWindDown = scopedDiscard(withRuntimeFlagsScoped(enable(WindDown)));
var disableCooperativeYielding = scopedDiscard(withRuntimeFlagsScoped(disable(CooperativeYielding)));
var disableInterruption = scopedDiscard(withRuntimeFlagsScoped(disable(Interruption)));
var disableOpSupervision = scopedDiscard(withRuntimeFlagsScoped(disable(OpSupervision)));
var disableRuntimeMetrics = scopedDiscard(withRuntimeFlagsScoped(disable(RuntimeMetrics)));
var disableWindDown = scopedDiscard(withRuntimeFlagsScoped(disable(WindDown)));
var setConfigProvider = (configProvider) => scopedDiscard(withConfigProviderScoped(configProvider));
var parentSpan = (span4) => succeedContext(make4(spanTag, span4));
var span2 = (name, options) => {
  options = addSpanStackTrace(options);
  return scoped(spanTag, options?.onEnd ? tap(makeSpanScoped(name, options), (span4) => addFinalizer((exit2) => options.onEnd(span4, exit2))) : makeSpanScoped(name, options));
};
var setTracer = (tracer) => scopedDiscard(withTracerScoped(tracer));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Layer.js
var LayerTypeId2 = LayerTypeId;
var MemoMapTypeId2 = MemoMapTypeId;
var CurrentMemoMap2 = CurrentMemoMap;
var isLayer2 = isLayer;
var isFresh2 = isFresh;
var annotateLogs2 = annotateLogs;
var annotateSpans2 = annotateSpans;
var build2 = build;
var buildWithScope2 = buildWithScope;
var catchAll3 = catchAll;
var catchAllCause3 = catchAllCause;
var context3 = context;
var die5 = die3;
var dieSync3 = dieSync;
var discard2 = discard;
var effect = fromEffect;
var effectDiscard = fromEffectDiscard;
var effectContext = fromEffectContext;
var empty5 = empty4;
var extendScope2 = extendScope;
var fail6 = fail4;
var failSync3 = failSync;
var failCause5 = failCause3;
var failCauseSync3 = failCauseSync;
var flatMap5 = flatMap3;
var flatten4 = flatten2;
var fresh2 = fresh;
var mock2 = mock;
var fromFunction2 = fromFunction;
var launch2 = launch;
var map8 = map6;
var mapError3 = mapError;
var match6 = match4;
var matchCause3 = matchCause;
var memoize2 = memoize;
var merge4 = merge3;
var mergeAll2 = mergeAll;
var orDie3 = orDie;
var orElse4 = orElse2;
var passthrough2 = passthrough;
var project2 = project;
var locallyEffect2 = locallyEffect;
var locally = fiberRefLocally2;
var locallyWith = fiberRefLocallyWith;
var locallyScoped = fiberRefLocallyScoped2;
var fiberRefLocallyScopedWith3 = fiberRefLocallyScopedWith2;
var retry2 = retry;
var scope3 = scope;
var scoped3 = scoped;
var scopedDiscard2 = scopedDiscard;
var scopedContext2 = scopedContext;
var service2 = service;
var succeed6 = succeed4;
var succeedContext2 = succeedContext;
var suspend4 = suspend2;
var sync4 = sync2;
var syncContext2 = syncContext;
var tap4 = tap2;
var tapError2 = tapError;
var tapErrorCause3 = tapErrorCause;
var toRuntime2 = toRuntime;
var toRuntimeWithMemoMap2 = toRuntimeWithMemoMap;
var provide3 = provide;
var provideMerge2 = provideMerge;
var zipWith4 = zipWith2;
var unwrapEffect2 = unwrapEffect;
var unwrapScoped2 = unwrapScoped;
var setClock = (clock2) => scopedDiscard2(fiberRefLocallyScopedWith(currentServices, add(clockTag, clock2)));
var setConfigProvider2 = setConfigProvider;
var parentSpan2 = parentSpan;
var setRandom = (random2) => scopedDiscard2(fiberRefLocallyScopedWith(currentServices, add(randomTag, random2)));
var setRequestBatching = (requestBatching) => scopedDiscard2(fiberRefLocallyScoped(currentRequestBatching, requestBatching));
var setRequestCaching = (requestCaching) => scopedDiscard2(fiberRefLocallyScoped(currentCacheEnabled, requestCaching));
var setRequestCache = (cache) => scopedDiscard2(isEffect(cache) ? flatMap2(cache, (x) => fiberRefLocallyScoped(currentCache, x)) : fiberRefLocallyScoped(currentCache, cache));
var setScheduler = (scheduler) => scopedDiscard2(fiberRefLocallyScoped(currentScheduler, scheduler));
var span3 = span2;
var setTracer2 = setTracer;
var setTracerEnabled = (enabled) => scopedDiscard2(fiberRefLocallyScoped(currentTracerEnabled, enabled));
var setTracerTiming = (enabled) => scopedDiscard2(fiberRefLocallyScoped(currentTracerTimingEnabled, enabled));
var setUnhandledErrorLogLevel = (level) => scopedDiscard2(fiberRefLocallyScoped(currentUnhandledErrorLogLevel, level));
var setVersionMismatchErrorLogLevel = (level) => scopedDiscard2(fiberRefLocallyScoped(currentVersionMismatchErrorLogLevel, level));
var withSpan2 = withSpan;
var withParentSpan2 = withParentSpan;
var makeMemoMap2 = makeMemoMap;
var buildWithMemoMap2 = buildWithMemoMap;
var updateService = dual(3, (layer, tag, f) => provide3(layer, map8(context3(), (c) => add(c, tag, f(unsafeGet2(c, tag))))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/PubSub.js
var PubSub_exports = {};
__export(PubSub_exports, {
  awaitShutdown: () => awaitShutdown3,
  bounded: () => bounded3,
  capacity: () => capacity3,
  dropping: () => dropping3,
  isEmpty: () => isEmpty5,
  isFull: () => isFull3,
  isShutdown: () => isShutdown3,
  publish: () => publish2,
  publishAll: () => publishAll2,
  shutdown: () => shutdown3,
  size: () => size4,
  sliding: () => sliding3,
  subscribe: () => subscribe2,
  unbounded: () => unbounded4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/pubsub.js
var AbsentValue = /* @__PURE__ */ Symbol.for("effect/PubSub/AbsentValue");
var addSubscribers = (subscription, pollers) => (subscribers) => {
  if (!subscribers.has(subscription)) {
    subscribers.set(subscription, /* @__PURE__ */ new Set());
  }
  const set6 = subscribers.get(subscription);
  set6.add(pollers);
};
var removeSubscribers = (subscription, pollers) => (subscribers) => {
  if (!subscribers.has(subscription)) {
    return;
  }
  const set6 = subscribers.get(subscription);
  set6.delete(pollers);
  if (set6.size === 0) {
    subscribers.delete(subscription);
  }
};
var bounded2 = (capacity8) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity8);
  return makePubSub(pubsub, new BackPressureStrategy());
});
var dropping2 = (capacity8) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity8);
  return makePubSub(pubsub, new DroppingStrategy());
});
var sliding2 = (capacity8) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity8);
  return makePubSub(pubsub, new SlidingStrategy());
});
var unbounded3 = (options) => suspend(() => {
  const pubsub = makeUnboundedPubSub(options);
  return makePubSub(pubsub, new DroppingStrategy());
});
var capacity2 = (self) => self.capacity();
var size3 = (self) => self.size;
var isFull2 = (self) => self.isFull;
var isEmpty4 = (self) => self.isEmpty;
var shutdown2 = (self) => self.shutdown;
var isShutdown2 = (self) => self.isShutdown;
var awaitShutdown2 = (self) => self.awaitShutdown;
var publish = dual(2, (self, value) => self.publish(value));
var publishAll = dual(2, (self, elements) => self.publishAll(elements));
var subscribe = (self) => self.subscribe;
var makeBoundedPubSub = (capacity8) => {
  const options = typeof capacity8 === "number" ? {
    capacity: capacity8
  } : capacity8;
  ensureCapacity(options.capacity);
  const replayBuffer = options.replay && options.replay > 0 ? new ReplayBuffer(Math.ceil(options.replay)) : void 0;
  if (options.capacity === 1) {
    return new BoundedPubSubSingle(replayBuffer);
  } else if (nextPow2(options.capacity) === options.capacity) {
    return new BoundedPubSubPow2(options.capacity, replayBuffer);
  } else {
    return new BoundedPubSubArb(options.capacity, replayBuffer);
  }
};
var makeUnboundedPubSub = (options) => new UnboundedPubSub(options?.replay ? new ReplayBuffer(options.replay) : void 0);
var makeSubscription = (pubsub, subscribers, strategy) => map3(deferredMake(), (deferred) => unsafeMakeSubscription(pubsub, subscribers, pubsub.subscribe(), unbounded(), deferred, make3(false), strategy));
var unsafeMakeSubscription = (pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy) => new SubscriptionImpl(pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy, pubsub.replayWindow());
var BoundedPubSubArb = class {
  capacity;
  replayBuffer;
  array;
  publisherIndex = 0;
  subscribers;
  subscriberCount = 0;
  subscribersIndex = 0;
  constructor(capacity8, replayBuffer) {
    this.capacity = capacity8;
    this.replayBuffer = replayBuffer;
    this.array = Array.from({
      length: capacity8
    });
    this.subscribers = Array.from({
      length: capacity8
    });
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherIndex === this.subscribersIndex;
  }
  isFull() {
    return this.publisherIndex === this.subscribersIndex + this.capacity;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      const index = this.publisherIndex % this.capacity;
      this.array[index] = value;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty();
    }
    const chunk2 = fromIterable2(elements);
    const n = chunk2.length;
    const size9 = this.publisherIndex - this.subscribersIndex;
    const available = this.capacity - size9;
    const forPubSub = Math.min(n, available);
    if (forPubSub === 0) {
      return chunk2;
    }
    let iteratorIndex = 0;
    const publishAllIndex = this.publisherIndex + forPubSub;
    while (this.publisherIndex !== publishAllIndex) {
      const a = unsafeGet(chunk2, iteratorIndex++);
      const index = this.publisherIndex % this.capacity;
      this.array[index] = a;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
      if (this.replayBuffer) {
        this.replayBuffer.offer(a);
      }
    }
    return drop(chunk2, iteratorIndex);
  }
  slide() {
    if (this.subscribersIndex !== this.publisherIndex) {
      const index = this.subscribersIndex % this.capacity;
      this.array[index] = AbsentValue;
      this.subscribers[index] = 0;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubArbSubscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubArbSubscription = class {
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.publisherIndex === this.subscriberIndex || this.self.publisherIndex === this.self.subscribersIndex;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    if (this.subscriberIndex !== this.self.publisherIndex) {
      const index = this.subscriberIndex % this.self.capacity;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      this.subscriberIndex += 1;
      return elem;
    }
    return default_;
  }
  pollUpTo(n) {
    if (this.unsubscribed) {
      return empty();
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    const size9 = this.self.publisherIndex - this.subscriberIndex;
    const toPoll = Math.min(n, size9);
    if (toPoll <= 0) {
      return empty();
    }
    const builder = [];
    const pollUpToIndex = this.subscriberIndex + toPoll;
    while (this.subscriberIndex !== pollUpToIndex) {
      const index = this.subscriberIndex % this.self.capacity;
      const a = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      builder.push(a);
      this.subscriberIndex += 1;
    }
    return fromIterable2(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
      while (this.subscriberIndex !== this.self.publisherIndex) {
        const index = this.subscriberIndex % this.self.capacity;
        this.self.subscribers[index] -= 1;
        if (this.self.subscribers[index] === 0) {
          this.self.array[index] = AbsentValue;
          this.self.subscribersIndex += 1;
        }
        this.subscriberIndex += 1;
      }
    }
  }
};
var BoundedPubSubPow2 = class {
  capacity;
  replayBuffer;
  array;
  mask;
  publisherIndex = 0;
  subscribers;
  subscriberCount = 0;
  subscribersIndex = 0;
  constructor(capacity8, replayBuffer) {
    this.capacity = capacity8;
    this.replayBuffer = replayBuffer;
    this.array = Array.from({
      length: capacity8
    });
    this.mask = capacity8 - 1;
    this.subscribers = Array.from({
      length: capacity8
    });
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherIndex === this.subscribersIndex;
  }
  isFull() {
    return this.publisherIndex === this.subscribersIndex + this.capacity;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      const index = this.publisherIndex & this.mask;
      this.array[index] = value;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty();
    }
    const chunk2 = fromIterable2(elements);
    const n = chunk2.length;
    const size9 = this.publisherIndex - this.subscribersIndex;
    const available = this.capacity - size9;
    const forPubSub = Math.min(n, available);
    if (forPubSub === 0) {
      return chunk2;
    }
    let iteratorIndex = 0;
    const publishAllIndex = this.publisherIndex + forPubSub;
    while (this.publisherIndex !== publishAllIndex) {
      const elem = unsafeGet(chunk2, iteratorIndex++);
      const index = this.publisherIndex & this.mask;
      this.array[index] = elem;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
      if (this.replayBuffer) {
        this.replayBuffer.offer(elem);
      }
    }
    return drop(chunk2, iteratorIndex);
  }
  slide() {
    if (this.subscribersIndex !== this.publisherIndex) {
      const index = this.subscribersIndex & this.mask;
      this.array[index] = AbsentValue;
      this.subscribers[index] = 0;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubPow2Subscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubPow2Subscription = class {
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.publisherIndex === this.subscriberIndex || this.self.publisherIndex === this.self.subscribersIndex;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    if (this.subscriberIndex !== this.self.publisherIndex) {
      const index = this.subscriberIndex & this.self.mask;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      this.subscriberIndex += 1;
      return elem;
    }
    return default_;
  }
  pollUpTo(n) {
    if (this.unsubscribed) {
      return empty();
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    const size9 = this.self.publisherIndex - this.subscriberIndex;
    const toPoll = Math.min(n, size9);
    if (toPoll <= 0) {
      return empty();
    }
    const builder = [];
    const pollUpToIndex = this.subscriberIndex + toPoll;
    while (this.subscriberIndex !== pollUpToIndex) {
      const index = this.subscriberIndex & this.self.mask;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      builder.push(elem);
      this.subscriberIndex += 1;
    }
    return fromIterable2(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
      while (this.subscriberIndex !== this.self.publisherIndex) {
        const index = this.subscriberIndex & this.self.mask;
        this.self.subscribers[index] -= 1;
        if (this.self.subscribers[index] === 0) {
          this.self.array[index] = AbsentValue;
          this.self.subscribersIndex += 1;
        }
        this.subscriberIndex += 1;
      }
    }
  }
};
var BoundedPubSubSingle = class {
  replayBuffer;
  publisherIndex = 0;
  subscriberCount = 0;
  subscribers = 0;
  value = AbsentValue;
  capacity = 1;
  constructor(replayBuffer) {
    this.replayBuffer = replayBuffer;
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  isEmpty() {
    return this.subscribers === 0;
  }
  isFull() {
    return !this.isEmpty();
  }
  size() {
    return this.isEmpty() ? 0 : 1;
  }
  publish(value) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      this.value = value;
      this.subscribers = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty();
    }
    const chunk2 = fromIterable2(elements);
    if (isEmpty(chunk2)) {
      return chunk2;
    }
    if (this.publish(unsafeHead(chunk2))) {
      return drop(chunk2, 1);
    } else {
      return chunk2;
    }
  }
  slide() {
    if (this.isFull()) {
      this.subscribers = 0;
      this.value = AbsentValue;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubSingleSubscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubSingleSubscription = class {
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.subscribers === 0 || this.subscriberIndex === this.self.publisherIndex;
  }
  size() {
    return this.isEmpty() ? 0 : 1;
  }
  poll(default_) {
    if (this.isEmpty()) {
      return default_;
    }
    const elem = this.self.value;
    this.self.subscribers -= 1;
    if (this.self.subscribers === 0) {
      this.self.value = AbsentValue;
    }
    this.subscriberIndex += 1;
    return elem;
  }
  pollUpTo(n) {
    if (this.isEmpty() || n < 1) {
      return empty();
    }
    const a = this.self.value;
    this.self.subscribers -= 1;
    if (this.self.subscribers === 0) {
      this.self.value = AbsentValue;
    }
    this.subscriberIndex += 1;
    return of(a);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      if (this.subscriberIndex !== this.self.publisherIndex) {
        this.self.subscribers -= 1;
        if (this.self.subscribers === 0) {
          this.self.value = AbsentValue;
        }
      }
    }
  }
};
var UnboundedPubSub = class {
  replayBuffer;
  publisherHead = {
    value: AbsentValue,
    subscribers: 0,
    next: null
  };
  publisherTail = this.publisherHead;
  publisherIndex = 0;
  subscribersIndex = 0;
  capacity = Number.MAX_SAFE_INTEGER;
  constructor(replayBuffer) {
    this.replayBuffer = replayBuffer;
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherHead === this.publisherTail;
  }
  isFull() {
    return false;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value) {
    const subscribers = this.publisherTail.subscribers;
    if (subscribers !== 0) {
      this.publisherTail.next = {
        value,
        subscribers,
        next: null
      };
      this.publisherTail = this.publisherTail.next;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.publisherTail.subscribers !== 0) {
      for (const a of elements) {
        this.publish(a);
      }
    } else if (this.replayBuffer) {
      this.replayBuffer.offerAll(elements);
    }
    return empty();
  }
  slide() {
    if (this.publisherHead !== this.publisherTail) {
      this.publisherHead = this.publisherHead.next;
      this.publisherHead.value = AbsentValue;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.publisherTail.subscribers += 1;
    return new UnboundedPubSubSubscription(this, this.publisherTail, this.publisherIndex, false);
  }
};
var UnboundedPubSubSubscription = class {
  self;
  subscriberHead;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberHead, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberHead = subscriberHead;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    if (this.unsubscribed) {
      return true;
    }
    let empty8 = true;
    let loop2 = true;
    while (loop2) {
      if (this.subscriberHead === this.self.publisherTail) {
        loop2 = false;
      } else {
        if (this.subscriberHead.next.value !== AbsentValue) {
          empty8 = false;
          loop2 = false;
        } else {
          this.subscriberHead = this.subscriberHead.next;
          this.subscriberIndex += 1;
        }
      }
    }
    return empty8;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    let loop2 = true;
    let polled = default_;
    while (loop2) {
      if (this.subscriberHead === this.self.publisherTail) {
        loop2 = false;
      } else {
        const elem = this.subscriberHead.next.value;
        if (elem !== AbsentValue) {
          polled = elem;
          this.subscriberHead.subscribers -= 1;
          if (this.subscriberHead.subscribers === 0) {
            this.self.publisherHead = this.self.publisherHead.next;
            this.self.publisherHead.value = AbsentValue;
            this.self.subscribersIndex += 1;
          }
          loop2 = false;
        }
        this.subscriberHead = this.subscriberHead.next;
        this.subscriberIndex += 1;
      }
    }
    return polled;
  }
  pollUpTo(n) {
    const builder = [];
    const default_ = AbsentValue;
    let i = 0;
    while (i !== n) {
      const a = this.poll(default_);
      if (a === default_) {
        i = n;
      } else {
        builder.push(a);
        i += 1;
      }
    }
    return fromIterable2(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.publisherTail.subscribers -= 1;
      while (this.subscriberHead !== this.self.publisherTail) {
        if (this.subscriberHead.next.value !== AbsentValue) {
          this.subscriberHead.subscribers -= 1;
          if (this.subscriberHead.subscribers === 0) {
            this.self.publisherHead = this.self.publisherHead.next;
            this.self.publisherHead.value = AbsentValue;
            this.self.subscribersIndex += 1;
          }
        }
        this.subscriberHead = this.subscriberHead.next;
      }
    }
  }
};
var SubscriptionImpl = class extends Class {
  pubsub;
  subscribers;
  subscription;
  pollers;
  shutdownHook;
  shutdownFlag;
  strategy;
  replayWindow;
  [DequeueTypeId] = dequeueVariance;
  constructor(pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy, replayWindow) {
    super();
    this.pubsub = pubsub;
    this.subscribers = subscribers;
    this.subscription = subscription;
    this.pollers = pollers;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
    this.replayWindow = replayWindow;
  }
  commit() {
    return this.take;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  capacity() {
    return this.pubsub.capacity;
  }
  isActive() {
    return !get(this.shutdownFlag);
  }
  get size() {
    return suspend(() => get(this.shutdownFlag) ? interrupt : succeed(this.subscription.size() + this.replayWindow.remaining));
  }
  unsafeSize() {
    if (get(this.shutdownFlag)) {
      return none();
    }
    return some(this.subscription.size() + this.replayWindow.remaining);
  }
  get isFull() {
    return suspend(() => get(this.shutdownFlag) ? interrupt : succeed(this.subscription.size() === this.capacity()));
  }
  get isEmpty() {
    return map3(this.size, (size9) => size9 === 0);
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      set(this.shutdownFlag, true);
      return pipe(forEachParUnbounded(unsafePollAllQueue(this.pollers), (d) => deferredInterruptWith(d, state.id()), false), zipRight(sync(() => {
        this.subscribers.delete(this.subscription);
        this.subscription.unsubscribe();
        this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      })), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid);
    }));
  }
  get isShutdown() {
    return sync(() => get(this.shutdownFlag));
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  get take() {
    return withFiberRuntime((state) => {
      if (get(this.shutdownFlag)) {
        return interrupt;
      }
      if (this.replayWindow.remaining > 0) {
        const message2 = this.replayWindow.take();
        return succeed(message2);
      }
      const message = isEmpty2(this.pollers) ? this.subscription.poll(EmptyMutableQueue) : EmptyMutableQueue;
      if (message === EmptyMutableQueue) {
        const deferred = deferredUnsafeMake(state.id());
        return pipe(suspend(() => {
          pipe(this.pollers, offer(deferred));
          pipe(this.subscribers, addSubscribers(this.subscription, this.pollers));
          this.strategy.unsafeCompletePollers(this.pubsub, this.subscribers, this.subscription, this.pollers);
          return get(this.shutdownFlag) ? interrupt : deferredAwait(deferred);
        }), onInterrupt(() => sync(() => unsafeRemove(this.pollers, deferred))));
      } else {
        this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
        return succeed(message);
      }
    });
  }
  get takeAll() {
    return suspend(() => {
      if (get(this.shutdownFlag)) {
        return interrupt;
      }
      const as8 = isEmpty2(this.pollers) ? unsafePollAllSubscription(this.subscription) : empty();
      this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      if (this.replayWindow.remaining > 0) {
        return succeed(appendAll(this.replayWindow.takeAll(), as8));
      }
      return succeed(as8);
    });
  }
  takeUpTo(max) {
    return suspend(() => {
      if (get(this.shutdownFlag)) {
        return interrupt;
      }
      let replay = void 0;
      if (this.replayWindow.remaining >= max) {
        const as9 = this.replayWindow.takeN(max);
        return succeed(as9);
      } else if (this.replayWindow.remaining > 0) {
        replay = this.replayWindow.takeAll();
        max = max - replay.length;
      }
      const as8 = isEmpty2(this.pollers) ? unsafePollN(this.subscription, max) : empty();
      this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      return replay ? succeed(appendAll(replay, as8)) : succeed(as8);
    });
  }
  takeBetween(min, max) {
    return suspend(() => takeRemainderLoop(this, min, max, empty()));
  }
};
var takeRemainderLoop = (self, min, max, acc) => {
  if (max < min) {
    return succeed(acc);
  }
  return pipe(self.takeUpTo(max), flatMap2((bs) => {
    const remaining = min - bs.length;
    if (remaining === 1) {
      return pipe(self.take, map3((b) => pipe(acc, appendAll(bs), append(b))));
    }
    if (remaining > 1) {
      return pipe(self.take, flatMap2((b) => takeRemainderLoop(self, remaining - 1, max - bs.length - 1, pipe(acc, appendAll(bs), append(b)))));
    }
    return succeed(pipe(acc, appendAll(bs)));
  }));
};
var PubSubImpl = class {
  pubsub;
  subscribers;
  scope;
  shutdownHook;
  shutdownFlag;
  strategy;
  [EnqueueTypeId] = enqueueVariance;
  [DequeueTypeId] = dequeueVariance;
  constructor(pubsub, subscribers, scope4, shutdownHook, shutdownFlag, strategy) {
    this.pubsub = pubsub;
    this.subscribers = subscribers;
    this.scope = scope4;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
  }
  capacity() {
    return this.pubsub.capacity;
  }
  get size() {
    return suspend(() => get(this.shutdownFlag) ? interrupt : sync(() => this.pubsub.size()));
  }
  unsafeSize() {
    if (get(this.shutdownFlag)) {
      return none();
    }
    return some(this.pubsub.size());
  }
  get isFull() {
    return map3(this.size, (size9) => size9 === this.capacity());
  }
  get isEmpty() {
    return map3(this.size, (size9) => size9 === 0);
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  get isShutdown() {
    return sync(() => get(this.shutdownFlag));
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      pipe(this.shutdownFlag, set(true));
      return pipe(this.scope.close(exitInterrupt(state.id())), zipRight(this.strategy.shutdown), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid);
    }));
  }
  publish(value) {
    return suspend(() => {
      if (get(this.shutdownFlag)) {
        return interrupt;
      }
      if (this.pubsub.publish(value)) {
        this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
        return succeed(true);
      }
      return this.strategy.handleSurplus(this.pubsub, this.subscribers, of(value), this.shutdownFlag);
    });
  }
  isActive() {
    return !get(this.shutdownFlag);
  }
  unsafeOffer(value) {
    if (get(this.shutdownFlag)) {
      return false;
    }
    if (this.pubsub.publish(value)) {
      this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
      return true;
    }
    return false;
  }
  publishAll(elements) {
    return suspend(() => {
      if (get(this.shutdownFlag)) {
        return interrupt;
      }
      const surplus = unsafePublishAll(this.pubsub, elements);
      this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
      if (isEmpty(surplus)) {
        return succeed(true);
      }
      return this.strategy.handleSurplus(this.pubsub, this.subscribers, surplus, this.shutdownFlag);
    });
  }
  get subscribe() {
    const acquire = tap(all2([this.scope.fork(sequential), makeSubscription(this.pubsub, this.subscribers, this.strategy)]), (tuple) => tuple[0].addFinalizer(() => tuple[1].shutdown));
    return map3(acquireRelease(acquire, (tuple, exit2) => tuple[0].close(exit2)), (tuple) => tuple[1]);
  }
  offer(value) {
    return this.publish(value);
  }
  offerAll(elements) {
    return this.publishAll(elements);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makePubSub = (pubsub, strategy) => flatMap2(scopeMake(), (scope4) => map3(deferredMake(), (deferred) => unsafeMakePubSub(pubsub, /* @__PURE__ */ new Map(), scope4, deferred, make3(false), strategy)));
var unsafeMakePubSub = (pubsub, subscribers, scope4, shutdownHook, shutdownFlag, strategy) => new PubSubImpl(pubsub, subscribers, scope4, shutdownHook, shutdownFlag, strategy);
var ensureCapacity = (capacity8) => {
  if (capacity8 <= 0) {
    throw new InvalidPubSubCapacityException(`Cannot construct PubSub with capacity of ${capacity8}`);
  }
};
var unsafeCompleteDeferred = (deferred, a) => {
  deferredUnsafeDone(deferred, succeed(a));
};
var unsafeOfferAll = (queue, as8) => {
  return pipe(queue, offerAll(as8));
};
var unsafePollAllQueue = (queue) => {
  return pipe(queue, pollUpTo(Number.POSITIVE_INFINITY));
};
var unsafePollAllSubscription = (subscription) => {
  return subscription.pollUpTo(Number.POSITIVE_INFINITY);
};
var unsafePollN = (subscription, max) => {
  return subscription.pollUpTo(max);
};
var unsafePublishAll = (pubsub, as8) => {
  return pubsub.publishAll(as8);
};
var unsafeRemove = (queue, value) => {
  unsafeOfferAll(queue, pipe(unsafePollAllQueue(queue), filter((elem) => elem !== value)));
};
var BackPressureStrategy = class {
  publishers = unbounded();
  get shutdown() {
    return flatMap2(fiberId, (fiberId3) => flatMap2(sync(() => unsafePollAllQueue(this.publishers)), (publishers) => forEachConcurrentDiscard(publishers, ([_, deferred, last3]) => last3 ? pipe(deferredInterruptWith(deferred, fiberId3), asVoid) : void_, false, false)));
  }
  handleSurplus(pubsub, subscribers, elements, isShutdown8) {
    return withFiberRuntime((state) => {
      const deferred = deferredUnsafeMake(state.id());
      return pipe(suspend(() => {
        this.unsafeOffer(elements, deferred);
        this.unsafeOnPubSubEmptySpace(pubsub, subscribers);
        this.unsafeCompleteSubscribers(pubsub, subscribers);
        return get(isShutdown8) ? interrupt : deferredAwait(deferred);
      }), onInterrupt(() => sync(() => this.unsafeRemove(deferred))));
    });
  }
  unsafeOnPubSubEmptySpace(pubsub, subscribers) {
    let keepPolling = true;
    while (keepPolling && !pubsub.isFull()) {
      const publisher = pipe(this.publishers, poll(EmptyMutableQueue));
      if (publisher === EmptyMutableQueue) {
        keepPolling = false;
      } else {
        const published = pubsub.publish(publisher[0]);
        if (published && publisher[2]) {
          unsafeCompleteDeferred(publisher[1], true);
        } else if (!published) {
          unsafeOfferAll(this.publishers, pipe(unsafePollAllQueue(this.publishers), prepend(publisher)));
        }
        this.unsafeCompleteSubscribers(pubsub, subscribers);
      }
    }
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
  unsafeOffer(elements, deferred) {
    const iterator = elements[Symbol.iterator]();
    let next = iterator.next();
    if (!next.done) {
      while (1) {
        const value = next.value;
        next = iterator.next();
        if (next.done) {
          pipe(this.publishers, offer([value, deferred, true]));
          break;
        }
        pipe(this.publishers, offer([value, deferred, false]));
      }
    }
  }
  unsafeRemove(deferred) {
    unsafeOfferAll(this.publishers, pipe(unsafePollAllQueue(this.publishers), filter(([_, a]) => a !== deferred)));
  }
};
var DroppingStrategy = class {
  get shutdown() {
    return void_;
  }
  handleSurplus(_pubsub, _subscribers, _elements, _isShutdown) {
    return succeed(false);
  }
  unsafeOnPubSubEmptySpace(_pubsub, _subscribers) {
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
};
var SlidingStrategy = class {
  get shutdown() {
    return void_;
  }
  handleSurplus(pubsub, subscribers, elements, _isShutdown) {
    return sync(() => {
      this.unsafeSlidingPublish(pubsub, elements);
      this.unsafeCompleteSubscribers(pubsub, subscribers);
      return true;
    });
  }
  unsafeOnPubSubEmptySpace(_pubsub, _subscribers) {
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
  unsafeSlidingPublish(pubsub, elements) {
    const it = elements[Symbol.iterator]();
    let next = it.next();
    if (!next.done && pubsub.capacity > 0) {
      let a = next.value;
      let loop2 = true;
      while (loop2) {
        pubsub.slide();
        const pub = pubsub.publish(a);
        if (pub && (next = it.next()) && !next.done) {
          a = next.value;
        } else if (pub) {
          loop2 = false;
        }
      }
    }
  }
};
var unsafeStrategyCompletePollers = (strategy, pubsub, subscribers, subscription, pollers) => {
  let keepPolling = true;
  while (keepPolling && !subscription.isEmpty()) {
    const poller = pipe(pollers, poll(EmptyMutableQueue));
    if (poller === EmptyMutableQueue) {
      pipe(subscribers, removeSubscribers(subscription, pollers));
      if (isEmpty2(pollers)) {
        keepPolling = false;
      } else {
        pipe(subscribers, addSubscribers(subscription, pollers));
      }
    } else {
      const pollResult = subscription.poll(EmptyMutableQueue);
      if (pollResult === EmptyMutableQueue) {
        unsafeOfferAll(pollers, pipe(unsafePollAllQueue(pollers), prepend(poller)));
      } else {
        unsafeCompleteDeferred(poller, pollResult);
        strategy.unsafeOnPubSubEmptySpace(pubsub, subscribers);
      }
    }
  }
};
var unsafeStrategyCompleteSubscribers = (strategy, pubsub, subscribers) => {
  for (const [subscription, pollersSet] of subscribers) {
    for (const pollers of pollersSet) {
      strategy.unsafeCompletePollers(pubsub, subscribers, subscription, pollers);
    }
  }
};
var ReplayBuffer = class {
  capacity;
  constructor(capacity8) {
    this.capacity = capacity8;
  }
  head = {
    value: AbsentValue,
    next: null
  };
  tail = this.head;
  size = 0;
  index = 0;
  slide() {
    this.index++;
  }
  offer(a) {
    this.tail.value = a;
    this.tail.next = {
      value: AbsentValue,
      next: null
    };
    this.tail = this.tail.next;
    if (this.size === this.capacity) {
      this.head = this.head.next;
    } else {
      this.size += 1;
    }
  }
  offerAll(as8) {
    for (const a of as8) {
      this.offer(a);
    }
  }
};
var ReplayWindowImpl = class {
  buffer;
  head;
  index;
  remaining;
  constructor(buffer4) {
    this.buffer = buffer4;
    this.index = buffer4.index;
    this.remaining = buffer4.size;
    this.head = buffer4.head;
  }
  fastForward() {
    while (this.index < this.buffer.index) {
      this.head = this.head.next;
      this.index++;
    }
  }
  take() {
    if (this.remaining === 0) {
      return void 0;
    } else if (this.index < this.buffer.index) {
      this.fastForward();
    }
    this.remaining--;
    const value = this.head.value;
    this.head = this.head.next;
    return value;
  }
  takeN(n) {
    if (this.remaining === 0) {
      return empty();
    } else if (this.index < this.buffer.index) {
      this.fastForward();
    }
    const len = Math.min(n, this.remaining);
    const items = new Array(len);
    for (let i = 0; i < len; i++) {
      const value = this.head.value;
      this.head = this.head.next;
      items[i] = value;
    }
    this.remaining -= len;
    return unsafeFromArray(items);
  }
  takeAll() {
    return this.takeN(this.remaining);
  }
};
var emptyReplayWindow = {
  remaining: 0,
  take: () => void 0,
  takeN: () => empty(),
  takeAll: () => empty()
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/PubSub.js
var bounded3 = bounded2;
var dropping3 = dropping2;
var sliding3 = sliding2;
var unbounded4 = unbounded3;
var capacity3 = capacity2;
var size4 = size3;
var isFull3 = isFull2;
var isEmpty5 = isEmpty4;
var shutdown3 = shutdown2;
var isShutdown3 = isShutdown2;
var awaitShutdown3 = awaitShutdown2;
var publish2 = publish;
var publishAll2 = publishAll;
var subscribe2 = subscribe;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelChildExecutorDecision.js
var OP_CONTINUE = "Continue";
var OP_CLOSE = "Close";
var OP_YIELD = "Yield";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/childExecutorDecision.js
var ChildExecutorDecisionSymbolKey = "effect/ChannelChildExecutorDecision";
var ChildExecutorDecisionTypeId = Symbol.for(ChildExecutorDecisionSymbolKey);
var proto = {
  [ChildExecutorDecisionTypeId]: ChildExecutorDecisionTypeId
};
var Continue = (_) => {
  const op = Object.create(proto);
  op._tag = OP_CONTINUE;
  return op;
};
var Close = (value) => {
  const op = Object.create(proto);
  op._tag = OP_CLOSE;
  op.value = value;
  return op;
};
var Yield = (_) => {
  const op = Object.create(proto);
  op._tag = OP_YIELD;
  return op;
};
var isChildExecutorDecision = (u) => hasProperty(u, ChildExecutorDecisionTypeId);
var isContinue = (self) => self._tag === OP_CONTINUE;
var isClose = (self) => self._tag === OP_CLOSE;
var isYield = (self) => self._tag === OP_YIELD;
var match7 = dual(2, (self, {
  onClose,
  onContinue,
  onYield
}) => {
  switch (self._tag) {
    case OP_CONTINUE: {
      return onContinue();
    }
    case OP_CLOSE: {
      return onClose(self.value);
    }
    case OP_YIELD: {
      return onYield();
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/continuation.js
var OP_CONTINUATION_K = "ContinuationK";
var OP_CONTINUATION_FINALIZER = "ContinuationFinalizer";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/continuation.js
var ContinuationTypeId = /* @__PURE__ */ Symbol.for("effect/ChannelContinuation");
var continuationVariance = {
  /* c8 ignore next */
  _Env: (_) => _,
  /* c8 ignore next */
  _InErr: (_) => _,
  /* c8 ignore next */
  _InElem: (_) => _,
  /* c8 ignore next */
  _InDone: (_) => _,
  /* c8 ignore next */
  _OutErr: (_) => _,
  /* c8 ignore next */
  _OutDone: (_) => _,
  /* c8 ignore next */
  _OutErr2: (_) => _,
  /* c8 ignore next */
  _OutElem: (_) => _,
  /* c8 ignore next */
  _OutDone2: (_) => _
};
var ContinuationKImpl = class {
  onSuccess;
  onHalt;
  _tag = OP_CONTINUATION_K;
  [ContinuationTypeId] = continuationVariance;
  constructor(onSuccess, onHalt) {
    this.onSuccess = onSuccess;
    this.onHalt = onHalt;
  }
  onExit(exit2) {
    return isFailure(exit2) ? this.onHalt(exit2.cause) : this.onSuccess(exit2.value);
  }
};
var ContinuationFinalizerImpl = class {
  finalizer;
  _tag = OP_CONTINUATION_FINALIZER;
  [ContinuationTypeId] = continuationVariance;
  constructor(finalizer3) {
    this.finalizer = finalizer3;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelUpstreamPullStrategy.js
var OP_PULL_AFTER_NEXT = "PullAfterNext";
var OP_PULL_AFTER_ALL_ENQUEUED = "PullAfterAllEnqueued";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/upstreamPullStrategy.js
var UpstreamPullStrategySymbolKey = "effect/ChannelUpstreamPullStrategy";
var UpstreamPullStrategyTypeId = Symbol.for(UpstreamPullStrategySymbolKey);
var upstreamPullStrategyVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var proto2 = {
  [UpstreamPullStrategyTypeId]: upstreamPullStrategyVariance
};
var PullAfterNext = (emitSeparator) => {
  const op = Object.create(proto2);
  op._tag = OP_PULL_AFTER_NEXT;
  op.emitSeparator = emitSeparator;
  return op;
};
var PullAfterAllEnqueued = (emitSeparator) => {
  const op = Object.create(proto2);
  op._tag = OP_PULL_AFTER_ALL_ENQUEUED;
  op.emitSeparator = emitSeparator;
  return op;
};
var isUpstreamPullStrategy = (u) => hasProperty(u, UpstreamPullStrategyTypeId);
var isPullAfterNext = (self) => self._tag === OP_PULL_AFTER_NEXT;
var isPullAfterAllEnqueued = (self) => self._tag === OP_PULL_AFTER_ALL_ENQUEUED;
var match8 = dual(2, (self, {
  onAllEnqueued,
  onNext
}) => {
  switch (self._tag) {
    case OP_PULL_AFTER_NEXT: {
      return onNext(self.emitSeparator);
    }
    case OP_PULL_AFTER_ALL_ENQUEUED: {
      return onAllEnqueued(self.emitSeparator);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channel.js
var OP_BRACKET_OUT = "BracketOut";
var OP_BRIDGE = "Bridge";
var OP_CONCAT_ALL = "ConcatAll";
var OP_EMIT = "Emit";
var OP_ENSURING = "Ensuring";
var OP_FAIL = "Fail";
var OP_FOLD = "Fold";
var OP_FROM_EFFECT = "FromEffect";
var OP_PIPE_TO = "PipeTo";
var OP_PROVIDE = "Provide";
var OP_READ = "Read";
var OP_SUCCEED = "Succeed";
var OP_SUCCEED_NOW = "SucceedNow";
var OP_SUSPEND = "Suspend";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/core-stream.js
var ChannelSymbolKey = "effect/Channel";
var ChannelTypeId = Symbol.for(ChannelSymbolKey);
var channelVariance = {
  /* c8 ignore next */
  _Env: (_) => _,
  /* c8 ignore next */
  _InErr: (_) => _,
  /* c8 ignore next */
  _InElem: (_) => _,
  /* c8 ignore next */
  _InDone: (_) => _,
  /* c8 ignore next */
  _OutErr: (_) => _,
  /* c8 ignore next */
  _OutElem: (_) => _,
  /* c8 ignore next */
  _OutDone: (_) => _
};
var proto3 = {
  [ChannelTypeId]: channelVariance,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isChannel = (u) => hasProperty(u, ChannelTypeId) || isEffect2(u);
var acquireReleaseOut = dual(2, (self, release) => {
  const op = Object.create(proto3);
  op._tag = OP_BRACKET_OUT;
  op.acquire = () => self;
  op.finalizer = release;
  return op;
});
var catchAllCause4 = dual(2, (self, f) => {
  const op = Object.create(proto3);
  op._tag = OP_FOLD;
  op.channel = self;
  op.k = new ContinuationKImpl(succeed7, f);
  return op;
});
var collectElements = (self) => {
  return suspend5(() => {
    const builder = [];
    return flatMap6(pipeTo(self, collectElementsReader(builder)), (value) => sync5(() => [fromIterable2(builder), value]));
  });
};
var collectElementsReader = (builder) => readWith({
  onInput: (outElem) => flatMap6(sync5(() => {
    builder.push(outElem);
  }), () => collectElementsReader(builder)),
  onFailure: fail7,
  onDone: succeedNow
});
var concatAll = (channels) => concatAllWith(channels, constVoid, constVoid);
var concatAllWith = (channels, f, g) => {
  const op = Object.create(proto3);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = f;
  op.combineAll = g;
  op.onPull = () => PullAfterNext(none());
  op.onEmit = () => Continue;
  op.value = () => channels;
  op.k = identity;
  return op;
};
var concatMapWith = dual(4, (self, f, g, h) => {
  const op = Object.create(proto3);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = g;
  op.combineAll = h;
  op.onPull = () => PullAfterNext(none());
  op.onEmit = () => Continue;
  op.value = () => self;
  op.k = f;
  return op;
});
var concatMapWithCustom = dual(6, (self, f, g, h, onPull, onEmit) => {
  const op = Object.create(proto3);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = g;
  op.combineAll = h;
  op.onPull = onPull;
  op.onEmit = onEmit;
  op.value = () => self;
  op.k = f;
  return op;
});
var embedInput = dual(2, (self, input) => {
  const op = Object.create(proto3);
  op._tag = OP_BRIDGE;
  op.input = input;
  op.channel = self;
  return op;
});
var ensuringWith = dual(2, (self, finalizer3) => {
  const op = Object.create(proto3);
  op._tag = OP_ENSURING;
  op.channel = self;
  op.finalizer = finalizer3;
  return op;
});
var fail7 = (error) => failCause6(fail2(error));
var failSync4 = (evaluate2) => failCauseSync4(() => fail2(evaluate2()));
var failCause6 = (cause) => failCauseSync4(() => cause);
var failCauseSync4 = (evaluate2) => {
  const op = Object.create(proto3);
  op._tag = OP_FAIL;
  op.error = evaluate2;
  return op;
};
var flatMap6 = dual(2, (self, f) => {
  const op = Object.create(proto3);
  op._tag = OP_FOLD;
  op.channel = self;
  op.k = new ContinuationKImpl(f, failCause6);
  return op;
});
var foldCauseChannel = dual(2, (self, options) => {
  const op = Object.create(proto3);
  op._tag = OP_FOLD;
  op.channel = self;
  op.k = new ContinuationKImpl(options.onSuccess, options.onFailure);
  return op;
});
var fromEffect2 = (effect4) => {
  const op = Object.create(proto3);
  op._tag = OP_FROM_EFFECT;
  op.effect = () => effect4;
  return op;
};
var pipeTo = dual(2, (self, that) => {
  const op = Object.create(proto3);
  op._tag = OP_PIPE_TO;
  op.left = () => self;
  op.right = () => that;
  return op;
});
var provideContext = dual(2, (self, env) => {
  const op = Object.create(proto3);
  op._tag = OP_PROVIDE;
  op.context = () => env;
  op.inner = self;
  return op;
});
var readOrFail = (error) => {
  const op = Object.create(proto3);
  op._tag = OP_READ;
  op.more = succeed7;
  op.done = new ContinuationKImpl(() => fail7(error), () => fail7(error));
  return op;
};
var readWith = (options) => readWithCause({
  onInput: options.onInput,
  onFailure: (cause) => match(failureOrCause(cause), {
    onLeft: options.onFailure,
    onRight: failCause6
  }),
  onDone: options.onDone
});
var readWithCause = (options) => {
  const op = Object.create(proto3);
  op._tag = OP_READ;
  op.more = options.onInput;
  op.done = new ContinuationKImpl(options.onDone, options.onFailure);
  return op;
};
var succeed7 = (value) => sync5(() => value);
var succeedNow = (result) => {
  const op = Object.create(proto3);
  op._tag = OP_SUCCEED_NOW;
  op.terminal = result;
  return op;
};
var suspend5 = (evaluate2) => {
  const op = Object.create(proto3);
  op._tag = OP_SUSPEND;
  op.channel = evaluate2;
  return op;
};
var sync5 = (evaluate2) => {
  const op = Object.create(proto3);
  op._tag = OP_SUCCEED;
  op.evaluate = evaluate2;
  return op;
};
var void_3 = succeedNow(void 0);
var write = (out) => {
  const op = Object.create(proto3);
  op._tag = OP_EMIT;
  op.out = out;
  return op;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelState.js
var OP_DONE = "Done";
var OP_EMIT2 = "Emit";
var OP_FROM_EFFECT2 = "FromEffect";
var OP_READ2 = "Read";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/channelState.js
var ChannelStateTypeId = /* @__PURE__ */ Symbol.for("effect/ChannelState");
var channelStateVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var proto4 = {
  [ChannelStateTypeId]: channelStateVariance
};
var Done = () => {
  const op = Object.create(proto4);
  op._tag = OP_DONE;
  return op;
};
var Emit = () => {
  const op = Object.create(proto4);
  op._tag = OP_EMIT2;
  return op;
};
var fromEffect3 = (effect4) => {
  const op = Object.create(proto4);
  op._tag = OP_FROM_EFFECT2;
  op.effect = effect4;
  return op;
};
var Read = (upstream, onEffect, onEmit, onDone3) => {
  const op = Object.create(proto4);
  op._tag = OP_READ2;
  op.upstream = upstream;
  op.onEffect = onEffect;
  op.onEmit = onEmit;
  op.onDone = onDone3;
  return op;
};
var isFromEffect = (self) => self._tag === OP_FROM_EFFECT2;
var effect2 = (self) => isFromEffect(self) ? self.effect : _void;
var effectOrUndefinedIgnored = (self) => isFromEffect(self) ? ignore(self.effect) : void 0;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/subexecutor.js
var OP_PULL_FROM_CHILD = "PullFromChild";
var OP_PULL_FROM_UPSTREAM = "PullFromUpstream";
var OP_DRAIN_CHILD_EXECUTORS = "DrainChildExecutors";
var OP_EMIT3 = "Emit";
var PullFromChild = class {
  childExecutor;
  parentSubexecutor;
  onEmit;
  _tag = OP_PULL_FROM_CHILD;
  constructor(childExecutor, parentSubexecutor, onEmit) {
    this.childExecutor = childExecutor;
    this.parentSubexecutor = parentSubexecutor;
    this.onEmit = onEmit;
  }
  close(exit2) {
    const fin1 = this.childExecutor.close(exit2);
    const fin2 = this.parentSubexecutor.close(exit2);
    if (fin1 !== void 0 && fin2 !== void 0) {
      return zipWith3(exit(fin1), exit(fin2), (exit1, exit22) => pipe(exit1, zipRight2(exit22)));
    } else if (fin1 !== void 0) {
      return fin1;
    } else if (fin2 !== void 0) {
      return fin2;
    } else {
      return void 0;
    }
  }
  enqueuePullFromChild(_child) {
    return this;
  }
};
var PullFromUpstream = class _PullFromUpstream {
  upstreamExecutor;
  createChild;
  lastDone;
  activeChildExecutors;
  combineChildResults;
  combineWithChildResult;
  onPull;
  onEmit;
  _tag = OP_PULL_FROM_UPSTREAM;
  constructor(upstreamExecutor, createChild, lastDone, activeChildExecutors, combineChildResults, combineWithChildResult, onPull, onEmit) {
    this.upstreamExecutor = upstreamExecutor;
    this.createChild = createChild;
    this.lastDone = lastDone;
    this.activeChildExecutors = activeChildExecutors;
    this.combineChildResults = combineChildResults;
    this.combineWithChildResult = combineWithChildResult;
    this.onPull = onPull;
    this.onEmit = onEmit;
  }
  close(exit2) {
    const fin1 = this.upstreamExecutor.close(exit2);
    const fins = [...this.activeChildExecutors.map((child) => child !== void 0 ? child.childExecutor.close(exit2) : void 0), fin1];
    const result = fins.reduce((acc, next) => {
      if (acc !== void 0 && next !== void 0) {
        return zipWith3(acc, exit(next), (exit1, exit22) => zipRight2(exit1, exit22));
      } else if (acc !== void 0) {
        return acc;
      } else if (next !== void 0) {
        return exit(next);
      } else {
        return void 0;
      }
    }, void 0);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(child) {
    return new _PullFromUpstream(this.upstreamExecutor, this.createChild, this.lastDone, [...this.activeChildExecutors, child], this.combineChildResults, this.combineWithChildResult, this.onPull, this.onEmit);
  }
};
var DrainChildExecutors = class _DrainChildExecutors {
  upstreamExecutor;
  lastDone;
  activeChildExecutors;
  upstreamDone;
  combineChildResults;
  combineWithChildResult;
  onPull;
  _tag = OP_DRAIN_CHILD_EXECUTORS;
  constructor(upstreamExecutor, lastDone, activeChildExecutors, upstreamDone, combineChildResults, combineWithChildResult, onPull) {
    this.upstreamExecutor = upstreamExecutor;
    this.lastDone = lastDone;
    this.activeChildExecutors = activeChildExecutors;
    this.upstreamDone = upstreamDone;
    this.combineChildResults = combineChildResults;
    this.combineWithChildResult = combineWithChildResult;
    this.onPull = onPull;
  }
  close(exit2) {
    const fin1 = this.upstreamExecutor.close(exit2);
    const fins = [...this.activeChildExecutors.map((child) => child !== void 0 ? child.childExecutor.close(exit2) : void 0), fin1];
    const result = fins.reduce((acc, next) => {
      if (acc !== void 0 && next !== void 0) {
        return zipWith3(acc, exit(next), (exit1, exit22) => zipRight2(exit1, exit22));
      } else if (acc !== void 0) {
        return acc;
      } else if (next !== void 0) {
        return exit(next);
      } else {
        return void 0;
      }
    }, void 0);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(child) {
    return new _DrainChildExecutors(this.upstreamExecutor, this.lastDone, [...this.activeChildExecutors, child], this.upstreamDone, this.combineChildResults, this.combineWithChildResult, this.onPull);
  }
};
var Emit2 = class {
  value;
  next;
  _tag = OP_EMIT3;
  constructor(value, next) {
    this.value = value;
    this.next = next;
  }
  close(exit2) {
    const result = this.next.close(exit2);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(_child) {
    return this;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelUpstreamPullRequest.js
var OP_PULLED = "Pulled";
var OP_NO_UPSTREAM = "NoUpstream";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/upstreamPullRequest.js
var UpstreamPullRequestSymbolKey = "effect/ChannelUpstreamPullRequest";
var UpstreamPullRequestTypeId = Symbol.for(UpstreamPullRequestSymbolKey);
var upstreamPullRequestVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var proto5 = {
  [UpstreamPullRequestTypeId]: upstreamPullRequestVariance
};
var Pulled = (value) => {
  const op = Object.create(proto5);
  op._tag = OP_PULLED;
  op.value = value;
  return op;
};
var NoUpstream = (activeDownstreamCount) => {
  const op = Object.create(proto5);
  op._tag = OP_NO_UPSTREAM;
  op.activeDownstreamCount = activeDownstreamCount;
  return op;
};
var isUpstreamPullRequest = (u) => hasProperty(u, UpstreamPullRequestTypeId);
var isPulled = (self) => self._tag === OP_PULLED;
var isNoUpstream = (self) => self._tag === OP_NO_UPSTREAM;
var match9 = dual(2, (self, {
  onNoUpstream,
  onPulled
}) => {
  switch (self._tag) {
    case OP_PULLED: {
      return onPulled(self.value);
    }
    case OP_NO_UPSTREAM: {
      return onNoUpstream(self.activeDownstreamCount);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/channelExecutor.js
var ChannelExecutor = class _ChannelExecutor {
  _activeSubexecutor = void 0;
  _cancelled = void 0;
  _closeLastSubstream = void 0;
  _currentChannel;
  _done = void 0;
  _doneStack = [];
  _emitted = void 0;
  _executeCloseLastSubstream;
  _input = void 0;
  _inProgressFinalizer = void 0;
  _providedEnv;
  constructor(initialChannel, providedEnv, executeCloseLastSubstream) {
    this._currentChannel = initialChannel;
    this._executeCloseLastSubstream = executeCloseLastSubstream;
    this._providedEnv = providedEnv;
  }
  run() {
    let result = void 0;
    while (result === void 0) {
      if (this._cancelled !== void 0) {
        result = this.processCancellation();
      } else if (this._activeSubexecutor !== void 0) {
        result = this.runSubexecutor();
      } else {
        try {
          if (this._currentChannel === void 0) {
            result = Done();
          } else {
            if (isEffect2(this._currentChannel)) {
              this._currentChannel = fromEffect2(this._currentChannel);
            }
            switch (this._currentChannel._tag) {
              case OP_BRACKET_OUT: {
                result = this.runBracketOut(this._currentChannel);
                break;
              }
              case OP_BRIDGE: {
                const bridgeInput = this._currentChannel.input;
                this._currentChannel = this._currentChannel.channel;
                if (this._input !== void 0) {
                  const inputExecutor = this._input;
                  this._input = void 0;
                  const drainer = () => flatMap4(bridgeInput.awaitRead(), () => suspend3(() => {
                    const state = inputExecutor.run();
                    switch (state._tag) {
                      case OP_DONE: {
                        return match3(inputExecutor.getDone(), {
                          onFailure: (cause) => bridgeInput.error(cause),
                          onSuccess: (value) => bridgeInput.done(value)
                        });
                      }
                      case OP_EMIT2: {
                        return flatMap4(bridgeInput.emit(inputExecutor.getEmit()), () => drainer());
                      }
                      case OP_FROM_EFFECT2: {
                        return matchCauseEffect(state.effect, {
                          onFailure: (cause) => bridgeInput.error(cause),
                          onSuccess: () => drainer()
                        });
                      }
                      case OP_READ2: {
                        return readUpstream(state, () => drainer(), (cause) => bridgeInput.error(cause));
                      }
                    }
                  }));
                  result = fromEffect3(flatMap4(forkDaemon(interruptible2(drainer())), (fiber) => sync3(() => this.addFinalizer((exit2) => flatMap4(interrupt4(fiber), () => suspend3(() => {
                    const effect4 = this.restorePipe(exit2, inputExecutor);
                    return effect4 !== void 0 ? effect4 : _void;
                  }))))));
                }
                break;
              }
              case OP_CONCAT_ALL: {
                const executor = new _ChannelExecutor(this._currentChannel.value(), this._providedEnv, (effect4) => sync3(() => {
                  const prevLastClose = this._closeLastSubstream === void 0 ? _void : this._closeLastSubstream;
                  this._closeLastSubstream = pipe(prevLastClose, zipRight3(effect4));
                }));
                executor._input = this._input;
                const channel = this._currentChannel;
                this._activeSubexecutor = new PullFromUpstream(executor, (value) => channel.k(value), void 0, [], (x, y) => channel.combineInners(x, y), (x, y) => channel.combineAll(x, y), (request) => channel.onPull(request), (value) => channel.onEmit(value));
                this._closeLastSubstream = void 0;
                this._currentChannel = void 0;
                break;
              }
              case OP_EMIT: {
                this._emitted = this._currentChannel.out;
                this._currentChannel = this._activeSubexecutor !== void 0 ? void 0 : void_3;
                result = Emit();
                break;
              }
              case OP_ENSURING: {
                this.runEnsuring(this._currentChannel);
                break;
              }
              case OP_FAIL: {
                result = this.doneHalt(this._currentChannel.error());
                break;
              }
              case OP_FOLD: {
                this._doneStack.push(this._currentChannel.k);
                this._currentChannel = this._currentChannel.channel;
                break;
              }
              case OP_FROM_EFFECT: {
                const effect4 = this._providedEnv === void 0 ? this._currentChannel.effect() : pipe(this._currentChannel.effect(), provide2(this._providedEnv));
                result = fromEffect3(matchCauseEffect(effect4, {
                  onFailure: (cause) => {
                    const state = this.doneHalt(cause);
                    return state !== void 0 && isFromEffect(state) ? state.effect : _void;
                  },
                  onSuccess: (value) => {
                    const state = this.doneSucceed(value);
                    return state !== void 0 && isFromEffect(state) ? state.effect : _void;
                  }
                }));
                break;
              }
              case OP_PIPE_TO: {
                const previousInput = this._input;
                const leftExec = new _ChannelExecutor(this._currentChannel.left(), this._providedEnv, (effect4) => this._executeCloseLastSubstream(effect4));
                leftExec._input = previousInput;
                this._input = leftExec;
                this.addFinalizer((exit2) => {
                  const effect4 = this.restorePipe(exit2, previousInput);
                  return effect4 !== void 0 ? effect4 : _void;
                });
                this._currentChannel = this._currentChannel.right();
                break;
              }
              case OP_PROVIDE: {
                const previousEnv = this._providedEnv;
                this._providedEnv = this._currentChannel.context();
                this._currentChannel = this._currentChannel.inner;
                this.addFinalizer(() => sync3(() => {
                  this._providedEnv = previousEnv;
                }));
                break;
              }
              case OP_READ: {
                const read2 = this._currentChannel;
                result = Read(this._input, identity, (emitted) => {
                  try {
                    this._currentChannel = read2.more(emitted);
                  } catch (error) {
                    this._currentChannel = read2.done.onExit(die(error));
                  }
                  return void 0;
                }, (exit2) => {
                  const onExit = (exit3) => {
                    return read2.done.onExit(exit3);
                  };
                  this._currentChannel = onExit(exit2);
                  return void 0;
                });
                break;
              }
              case OP_SUCCEED: {
                result = this.doneSucceed(this._currentChannel.evaluate());
                break;
              }
              case OP_SUCCEED_NOW: {
                result = this.doneSucceed(this._currentChannel.terminal);
                break;
              }
              case OP_SUSPEND: {
                this._currentChannel = this._currentChannel.channel();
                break;
              }
            }
          }
        } catch (error) {
          this._currentChannel = failCause6(die2(error));
        }
      }
    }
    return result;
  }
  getDone() {
    return this._done;
  }
  getEmit() {
    return this._emitted;
  }
  cancelWith(exit2) {
    this._cancelled = exit2;
  }
  clearInProgressFinalizer() {
    this._inProgressFinalizer = void 0;
  }
  storeInProgressFinalizer(finalizer3) {
    this._inProgressFinalizer = finalizer3;
  }
  popAllFinalizers(exit2) {
    const finalizers = [];
    let next = this._doneStack.pop();
    while (next) {
      if (next._tag === "ContinuationFinalizer") {
        finalizers.push(next.finalizer);
      }
      next = this._doneStack.pop();
    }
    const effect4 = finalizers.length === 0 ? _void : runFinalizers(finalizers, exit2);
    this.storeInProgressFinalizer(effect4);
    return effect4;
  }
  popNextFinalizers() {
    const builder = [];
    while (this._doneStack.length !== 0) {
      const cont = this._doneStack[this._doneStack.length - 1];
      if (cont._tag === OP_CONTINUATION_K) {
        return builder;
      }
      builder.push(cont);
      this._doneStack.pop();
    }
    return builder;
  }
  restorePipe(exit2, prev) {
    const currInput = this._input;
    this._input = prev;
    if (currInput !== void 0) {
      const effect4 = currInput.close(exit2);
      return effect4;
    }
    return _void;
  }
  close(exit2) {
    let runInProgressFinalizers = void 0;
    const finalizer3 = this._inProgressFinalizer;
    if (finalizer3 !== void 0) {
      runInProgressFinalizers = pipe(finalizer3, ensuring2(sync3(() => this.clearInProgressFinalizer())));
    }
    let closeSelf = void 0;
    const selfFinalizers = this.popAllFinalizers(exit2);
    if (selfFinalizers !== void 0) {
      closeSelf = pipe(selfFinalizers, ensuring2(sync3(() => this.clearInProgressFinalizer())));
    }
    const closeSubexecutors = this._activeSubexecutor === void 0 ? void 0 : this._activeSubexecutor.close(exit2);
    if (closeSubexecutors === void 0 && runInProgressFinalizers === void 0 && closeSelf === void 0) {
      return void 0;
    }
    return pipe(
      exit(ifNotNull(closeSubexecutors)),
      zip2(exit(ifNotNull(runInProgressFinalizers))),
      zip2(exit(ifNotNull(closeSelf))),
      map7(([[exit1, exit22], exit3]) => pipe(exit1, zipRight2(exit22), zipRight2(exit3))),
      uninterruptible2,
      // TODO: remove
      flatMap4((exit3) => suspend3(() => exit3))
    );
  }
  doneSucceed(value) {
    if (this._doneStack.length === 0) {
      this._done = succeed2(value);
      this._currentChannel = void 0;
      return Done();
    }
    const head5 = this._doneStack[this._doneStack.length - 1];
    if (head5._tag === OP_CONTINUATION_K) {
      this._doneStack.pop();
      this._currentChannel = head5.onSuccess(value);
      return void 0;
    }
    const finalizers = this.popNextFinalizers();
    if (this._doneStack.length === 0) {
      this._doneStack = finalizers.reverse();
      this._done = succeed2(value);
      this._currentChannel = void 0;
      return Done();
    }
    const finalizerEffect = runFinalizers(finalizers.map((f) => f.finalizer), succeed2(value));
    this.storeInProgressFinalizer(finalizerEffect);
    const effect4 = pipe(finalizerEffect, ensuring2(sync3(() => this.clearInProgressFinalizer())), uninterruptible2, flatMap4(() => sync3(() => this.doneSucceed(value))));
    return fromEffect3(effect4);
  }
  doneHalt(cause) {
    if (this._doneStack.length === 0) {
      this._done = failCause(cause);
      this._currentChannel = void 0;
      return Done();
    }
    const head5 = this._doneStack[this._doneStack.length - 1];
    if (head5._tag === OP_CONTINUATION_K) {
      this._doneStack.pop();
      try {
        this._currentChannel = head5.onHalt(cause);
      } catch (error) {
        this._currentChannel = failCause6(die2(error));
      }
      return void 0;
    }
    const finalizers = this.popNextFinalizers();
    if (this._doneStack.length === 0) {
      this._doneStack = finalizers.reverse();
      this._done = failCause(cause);
      this._currentChannel = void 0;
      return Done();
    }
    const finalizerEffect = runFinalizers(finalizers.map((f) => f.finalizer), failCause(cause));
    this.storeInProgressFinalizer(finalizerEffect);
    const effect4 = pipe(finalizerEffect, ensuring2(sync3(() => this.clearInProgressFinalizer())), uninterruptible2, flatMap4(() => sync3(() => this.doneHalt(cause))));
    return fromEffect3(effect4);
  }
  processCancellation() {
    this._currentChannel = void 0;
    this._done = this._cancelled;
    this._cancelled = void 0;
    return Done();
  }
  runBracketOut(bracketOut) {
    const effect4 = uninterruptible2(matchCauseEffect(this.provide(bracketOut.acquire()), {
      onFailure: (cause) => sync3(() => {
        this._currentChannel = failCause6(cause);
      }),
      onSuccess: (out) => sync3(() => {
        this.addFinalizer((exit2) => this.provide(bracketOut.finalizer(out, exit2)));
        this._currentChannel = write(out);
      })
    }));
    return fromEffect3(effect4);
  }
  provide(effect4) {
    if (this._providedEnv === void 0) {
      return effect4;
    }
    return pipe(effect4, provide2(this._providedEnv));
  }
  runEnsuring(ensuring8) {
    this.addFinalizer(ensuring8.finalizer);
    this._currentChannel = ensuring8.channel;
  }
  addFinalizer(f) {
    this._doneStack.push(new ContinuationFinalizerImpl(f));
  }
  runSubexecutor() {
    const subexecutor = this._activeSubexecutor;
    switch (subexecutor._tag) {
      case OP_PULL_FROM_CHILD: {
        return this.pullFromChild(subexecutor.childExecutor, subexecutor.parentSubexecutor, subexecutor.onEmit, subexecutor);
      }
      case OP_PULL_FROM_UPSTREAM: {
        return this.pullFromUpstream(subexecutor);
      }
      case OP_DRAIN_CHILD_EXECUTORS: {
        return this.drainChildExecutors(subexecutor);
      }
      case OP_EMIT3: {
        this._emitted = subexecutor.value;
        this._activeSubexecutor = subexecutor.next;
        return Emit();
      }
    }
  }
  replaceSubexecutor(nextSubExec) {
    this._currentChannel = void 0;
    this._activeSubexecutor = nextSubExec;
  }
  finishWithExit(exit2) {
    const state = match3(exit2, {
      onFailure: (cause) => this.doneHalt(cause),
      onSuccess: (value) => this.doneSucceed(value)
    });
    this._activeSubexecutor = void 0;
    return state === void 0 ? _void : effect2(state);
  }
  finishSubexecutorWithCloseEffect(subexecutorDone, ...closeFuncs) {
    this.addFinalizer(() => pipe(closeFuncs, forEach2((closeFunc) => pipe(sync3(() => closeFunc(subexecutorDone)), flatMap4((closeEffect) => closeEffect !== void 0 ? closeEffect : _void)), {
      discard: true
    })));
    const state = pipe(subexecutorDone, match3({
      onFailure: (cause) => this.doneHalt(cause),
      onSuccess: (value) => this.doneSucceed(value)
    }));
    this._activeSubexecutor = void 0;
    return state;
  }
  applyUpstreamPullStrategy(upstreamFinished, queue, strategy) {
    switch (strategy._tag) {
      case OP_PULL_AFTER_NEXT: {
        const shouldPrepend = !upstreamFinished || queue.some((subexecutor) => subexecutor !== void 0);
        return [strategy.emitSeparator, shouldPrepend ? [void 0, ...queue] : queue];
      }
      case OP_PULL_AFTER_ALL_ENQUEUED: {
        const shouldEnqueue = !upstreamFinished || queue.some((subexecutor) => subexecutor !== void 0);
        return [strategy.emitSeparator, shouldEnqueue ? [...queue, void 0] : queue];
      }
    }
  }
  pullFromChild(childExecutor, parentSubexecutor, onEmitted, subexecutor) {
    return Read(childExecutor, identity, (emitted) => {
      const childExecutorDecision = onEmitted(emitted);
      switch (childExecutorDecision._tag) {
        case OP_CONTINUE: {
          break;
        }
        case OP_CLOSE: {
          this.finishWithDoneValue(childExecutor, parentSubexecutor, childExecutorDecision.value);
          break;
        }
        case OP_YIELD: {
          const modifiedParent = parentSubexecutor.enqueuePullFromChild(subexecutor);
          this.replaceSubexecutor(modifiedParent);
          break;
        }
      }
      this._activeSubexecutor = new Emit2(emitted, this._activeSubexecutor);
      return void 0;
    }, match3({
      onFailure: (cause) => {
        const state = this.handleSubexecutorFailure(childExecutor, parentSubexecutor, cause);
        return state === void 0 ? void 0 : effectOrUndefinedIgnored(state);
      },
      onSuccess: (doneValue) => {
        this.finishWithDoneValue(childExecutor, parentSubexecutor, doneValue);
        return void 0;
      }
    }));
  }
  finishWithDoneValue(childExecutor, parentSubexecutor, doneValue) {
    const subexecutor = parentSubexecutor;
    switch (subexecutor._tag) {
      case OP_PULL_FROM_UPSTREAM: {
        const modifiedParent = new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone !== void 0 ? subexecutor.combineChildResults(subexecutor.lastDone, doneValue) : doneValue, subexecutor.activeChildExecutors, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit);
        this._closeLastSubstream = childExecutor.close(succeed2(doneValue));
        this.replaceSubexecutor(modifiedParent);
        break;
      }
      case OP_DRAIN_CHILD_EXECUTORS: {
        const modifiedParent = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone !== void 0 ? subexecutor.combineChildResults(subexecutor.lastDone, doneValue) : doneValue, subexecutor.activeChildExecutors, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
        this._closeLastSubstream = childExecutor.close(succeed2(doneValue));
        this.replaceSubexecutor(modifiedParent);
        break;
      }
      default: {
        break;
      }
    }
  }
  handleSubexecutorFailure(childExecutor, parentSubexecutor, cause) {
    return this.finishSubexecutorWithCloseEffect(failCause(cause), (exit2) => parentSubexecutor.close(exit2), (exit2) => childExecutor.close(exit2));
  }
  pullFromUpstream(subexecutor) {
    if (subexecutor.activeChildExecutors.length === 0) {
      return this.performPullFromUpstream(subexecutor);
    }
    const activeChild = subexecutor.activeChildExecutors[0];
    const parentSubexecutor = new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, subexecutor.activeChildExecutors.slice(1), subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit);
    if (activeChild === void 0) {
      return this.performPullFromUpstream(parentSubexecutor);
    }
    this.replaceSubexecutor(new PullFromChild(activeChild.childExecutor, parentSubexecutor, activeChild.onEmit));
    return void 0;
  }
  performPullFromUpstream(subexecutor) {
    return Read(subexecutor.upstreamExecutor, (effect4) => {
      const closeLastSubstream = this._closeLastSubstream === void 0 ? _void : this._closeLastSubstream;
      this._closeLastSubstream = void 0;
      return pipe(this._executeCloseLastSubstream(closeLastSubstream), zipRight3(effect4));
    }, (emitted) => {
      if (this._closeLastSubstream !== void 0) {
        const closeLastSubstream = this._closeLastSubstream;
        this._closeLastSubstream = void 0;
        return pipe(this._executeCloseLastSubstream(closeLastSubstream), map7(() => {
          const childExecutor2 = new _ChannelExecutor(subexecutor.createChild(emitted), this._providedEnv, this._executeCloseLastSubstream);
          childExecutor2._input = this._input;
          const [emitSeparator2, updatedChildExecutors2] = this.applyUpstreamPullStrategy(false, subexecutor.activeChildExecutors, subexecutor.onPull(Pulled(emitted)));
          this._activeSubexecutor = new PullFromChild(childExecutor2, new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, updatedChildExecutors2, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit), subexecutor.onEmit);
          if (isSome(emitSeparator2)) {
            this._activeSubexecutor = new Emit2(emitSeparator2.value, this._activeSubexecutor);
          }
          return void 0;
        }));
      }
      const childExecutor = new _ChannelExecutor(subexecutor.createChild(emitted), this._providedEnv, this._executeCloseLastSubstream);
      childExecutor._input = this._input;
      const [emitSeparator, updatedChildExecutors] = this.applyUpstreamPullStrategy(false, subexecutor.activeChildExecutors, subexecutor.onPull(Pulled(emitted)));
      this._activeSubexecutor = new PullFromChild(childExecutor, new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, updatedChildExecutors, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit), subexecutor.onEmit);
      if (isSome(emitSeparator)) {
        this._activeSubexecutor = new Emit2(emitSeparator.value, this._activeSubexecutor);
      }
      return void 0;
    }, (exit2) => {
      if (subexecutor.activeChildExecutors.some((subexecutor2) => subexecutor2 !== void 0)) {
        const drain5 = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, [void 0, ...subexecutor.activeChildExecutors], subexecutor.upstreamExecutor.getDone(), subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
        if (this._closeLastSubstream !== void 0) {
          const closeLastSubstream2 = this._closeLastSubstream;
          this._closeLastSubstream = void 0;
          return pipe(this._executeCloseLastSubstream(closeLastSubstream2), map7(() => this.replaceSubexecutor(drain5)));
        }
        this.replaceSubexecutor(drain5);
        return void 0;
      }
      const closeLastSubstream = this._closeLastSubstream;
      const state = this.finishSubexecutorWithCloseEffect(pipe(exit2, map4((a) => subexecutor.combineWithChildResult(subexecutor.lastDone, a))), () => closeLastSubstream, (exit3) => subexecutor.upstreamExecutor.close(exit3));
      return state === void 0 ? void 0 : (
        // NOTE: assuming finalizers cannot fail
        effectOrUndefinedIgnored(state)
      );
    });
  }
  drainChildExecutors(subexecutor) {
    if (subexecutor.activeChildExecutors.length === 0) {
      const lastClose = this._closeLastSubstream;
      if (lastClose !== void 0) {
        this.addFinalizer(() => succeed5(lastClose));
      }
      return this.finishSubexecutorWithCloseEffect(subexecutor.upstreamDone, () => lastClose, (exit2) => subexecutor.upstreamExecutor.close(exit2));
    }
    const activeChild = subexecutor.activeChildExecutors[0];
    const rest = subexecutor.activeChildExecutors.slice(1);
    if (activeChild === void 0) {
      const [emitSeparator, remainingExecutors] = this.applyUpstreamPullStrategy(true, rest, subexecutor.onPull(NoUpstream(rest.reduce((n, curr) => curr !== void 0 ? n + 1 : n, 0))));
      this.replaceSubexecutor(new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, remainingExecutors, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull));
      if (isSome(emitSeparator)) {
        this._emitted = emitSeparator.value;
        return Emit();
      }
      return void 0;
    }
    const parentSubexecutor = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, rest, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
    this.replaceSubexecutor(new PullFromChild(activeChild.childExecutor, parentSubexecutor, activeChild.onEmit));
    return void 0;
  }
};
var ifNotNull = (effect4) => effect4 !== void 0 ? effect4 : _void;
var runFinalizers = (finalizers, exit2) => {
  return pipe(forEach2(finalizers, (fin) => exit(fin(exit2))), map7((exits) => pipe(all(exits), getOrElse(() => void_2))), flatMap4((exit3) => suspend3(() => exit3)));
};
var readUpstream = (r, onSuccess, onFailure) => {
  const readStack = [r];
  const read2 = () => {
    const current2 = readStack.pop();
    if (current2 === void 0 || current2.upstream === void 0) {
      return dieMessage("Unexpected end of input for channel execution");
    }
    const state = current2.upstream.run();
    switch (state._tag) {
      case OP_EMIT2: {
        const emitEffect = current2.onEmit(current2.upstream.getEmit());
        if (readStack.length === 0) {
          if (emitEffect === void 0) {
            return suspend3(onSuccess);
          }
          return pipe(emitEffect, matchCauseEffect({
            onFailure,
            onSuccess
          }));
        }
        if (emitEffect === void 0) {
          return suspend3(() => read2());
        }
        return pipe(emitEffect, matchCauseEffect({
          onFailure,
          onSuccess: () => read2()
        }));
      }
      case OP_DONE: {
        const doneEffect = current2.onDone(current2.upstream.getDone());
        if (readStack.length === 0) {
          if (doneEffect === void 0) {
            return suspend3(onSuccess);
          }
          return pipe(doneEffect, matchCauseEffect({
            onFailure,
            onSuccess
          }));
        }
        if (doneEffect === void 0) {
          return suspend3(() => read2());
        }
        return pipe(doneEffect, matchCauseEffect({
          onFailure,
          onSuccess: () => read2()
        }));
      }
      case OP_FROM_EFFECT2: {
        readStack.push(current2);
        return pipe(current2.onEffect(state.effect), catchAllCause2((cause) => suspend3(() => {
          const doneEffect = current2.onDone(failCause(cause));
          return doneEffect === void 0 ? _void : doneEffect;
        })), matchCauseEffect({
          onFailure,
          onSuccess: () => read2()
        }));
      }
      case OP_READ2: {
        readStack.push(current2);
        readStack.push(state);
        return suspend3(() => read2());
      }
    }
  };
  return read2();
};
var runIn = dual(2, (self, scope4) => {
  const run4 = (channelDeferred, scopeDeferred, scope5) => acquireUseRelease(sync3(() => new ChannelExecutor(self, void 0, identity)), (exec) => suspend3(() => runScopedInterpret(exec.run(), exec).pipe(intoDeferred(channelDeferred), zipRight3(_await(channelDeferred)), zipLeft(_await(scopeDeferred)))), (exec, exit2) => {
    const finalize = exec.close(exit2);
    if (finalize === void 0) {
      return _void;
    }
    return tapErrorCause2(finalize, (cause) => addFinalizer2(scope5, failCause4(cause)));
  });
  return uninterruptibleMask2((restore) => all3([fork(scope4, sequential2), make6(), make6()]).pipe(flatMap4(([child, channelDeferred, scopeDeferred]) => restore(run4(channelDeferred, scopeDeferred, child)).pipe(forkIn2(scope4), flatMap4((fiber) => scope4.addFinalizer((exit2) => {
    const interruptors2 = isFailure(exit2) ? interruptors(exit2.cause) : void 0;
    return isDone(channelDeferred).pipe(flatMap4((isDone4) => isDone4 ? succeed3(scopeDeferred, void 0).pipe(zipRight3(_await2(fiber)), zipRight3(inheritAll(fiber))) : succeed3(scopeDeferred, void 0).pipe(zipRight3(interruptors2 && size(interruptors2) > 0 ? interruptAs(fiber, combineAll(interruptors2)) : interrupt4(fiber)), zipRight3(inheritAll(fiber)))));
  }).pipe(zipRight3(restore(_await(channelDeferred)))))))));
});
var runScopedInterpret = (channelState, exec) => {
  const op = channelState;
  switch (op._tag) {
    case OP_FROM_EFFECT2: {
      return pipe(op.effect, flatMap4(() => runScopedInterpret(exec.run(), exec)));
    }
    case OP_EMIT2: {
      return runScopedInterpret(exec.run(), exec);
    }
    case OP_DONE: {
      return suspend3(() => exec.getDone());
    }
    case OP_READ2: {
      return readUpstream(op, () => runScopedInterpret(exec.run(), exec), failCause4);
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelMergeDecision.js
var OP_DONE2 = "Done";
var OP_AWAIT = "Await";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/mergeDecision.js
var MergeDecisionSymbolKey = "effect/ChannelMergeDecision";
var MergeDecisionTypeId = Symbol.for(MergeDecisionSymbolKey);
var proto6 = {
  [MergeDecisionTypeId]: {
    _R: (_) => _,
    _E0: (_) => _,
    _Z0: (_) => _,
    _E: (_) => _,
    _Z: (_) => _
  }
};
var Done2 = (effect4) => {
  const op = Object.create(proto6);
  op._tag = OP_DONE2;
  op.effect = effect4;
  return op;
};
var Await = (f) => {
  const op = Object.create(proto6);
  op._tag = OP_AWAIT;
  op.f = f;
  return op;
};
var AwaitConst = (effect4) => Await(() => effect4);
var isMergeDecision = (u) => hasProperty(u, MergeDecisionTypeId);
var match10 = dual(2, (self, {
  onAwait,
  onDone: onDone3
}) => {
  const op = self;
  switch (op._tag) {
    case OP_DONE2:
      return onDone3(op.effect);
    case OP_AWAIT:
      return onAwait(op.f);
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelMergeState.js
var OP_BOTH_RUNNING = "BothRunning";
var OP_LEFT_DONE = "LeftDone";
var OP_RIGHT_DONE = "RightDone";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/mergeState.js
var MergeStateSymbolKey = "effect/ChannelMergeState";
var MergeStateTypeId = Symbol.for(MergeStateSymbolKey);
var proto7 = {
  [MergeStateTypeId]: MergeStateTypeId
};
var BothRunning = (left2, right2) => {
  const op = Object.create(proto7);
  op._tag = OP_BOTH_RUNNING;
  op.left = left2;
  op.right = right2;
  return op;
};
var LeftDone = (f) => {
  const op = Object.create(proto7);
  op._tag = OP_LEFT_DONE;
  op.f = f;
  return op;
};
var RightDone = (f) => {
  const op = Object.create(proto7);
  op._tag = OP_RIGHT_DONE;
  op.f = f;
  return op;
};
var isMergeState = (u) => hasProperty(u, MergeStateTypeId);
var isBothRunning = (self) => {
  return self._tag === OP_BOTH_RUNNING;
};
var isLeftDone = (self) => {
  return self._tag === OP_LEFT_DONE;
};
var isRightDone = (self) => {
  return self._tag === OP_RIGHT_DONE;
};
var match11 = dual(2, (self, {
  onBothRunning,
  onLeftDone,
  onRightDone
}) => {
  switch (self._tag) {
    case OP_BOTH_RUNNING: {
      return onBothRunning(self.left, self.right);
    }
    case OP_LEFT_DONE: {
      return onLeftDone(self.f);
    }
    case OP_RIGHT_DONE: {
      return onRightDone(self.f);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelMergeStrategy.js
var OP_BACK_PRESSURE = "BackPressure";
var OP_BUFFER_SLIDING = "BufferSliding";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/mergeStrategy.js
var MergeStrategySymbolKey = "effect/ChannelMergeStrategy";
var MergeStrategyTypeId = Symbol.for(MergeStrategySymbolKey);
var proto8 = {
  [MergeStrategyTypeId]: MergeStrategyTypeId
};
var BackPressure = (_) => {
  const op = Object.create(proto8);
  op._tag = OP_BACK_PRESSURE;
  return op;
};
var BufferSliding = (_) => {
  const op = Object.create(proto8);
  op._tag = OP_BUFFER_SLIDING;
  return op;
};
var isMergeStrategy = (u) => hasProperty(u, MergeStrategyTypeId);
var isBackPressure = (self) => self._tag === OP_BACK_PRESSURE;
var isBufferSliding = (self) => self._tag === OP_BUFFER_SLIDING;
var match12 = dual(2, (self, {
  onBackPressure,
  onBufferSliding
}) => {
  switch (self._tag) {
    case OP_BACK_PRESSURE: {
      return onBackPressure();
    }
    case OP_BUFFER_SLIDING: {
      return onBufferSliding();
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/singleProducerAsyncInput.js
var OP_STATE_EMPTY = "Empty";
var OP_STATE_EMIT = "Emit";
var OP_STATE_ERROR = "Error";
var OP_STATE_DONE = "Done";
var stateEmpty = (notifyProducer) => ({
  _tag: OP_STATE_EMPTY,
  notifyProducer
});
var stateEmit = (notifyConsumers) => ({
  _tag: OP_STATE_EMIT,
  notifyConsumers
});
var stateError = (cause) => ({
  _tag: OP_STATE_ERROR,
  cause
});
var stateDone = (done4) => ({
  _tag: OP_STATE_DONE,
  done: done4
});
var SingleProducerAsyncInputImpl = class {
  ref;
  constructor(ref) {
    this.ref = ref;
  }
  awaitRead() {
    return flatten3(modify(this.ref, (state) => state._tag === OP_STATE_EMPTY ? [_await(state.notifyProducer), state] : [_void, state]));
  }
  get close() {
    return fiberIdWith((fiberId3) => this.error(interrupt3(fiberId3)));
  }
  done(value) {
    return flatten3(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          return [forEach2(state.notifyConsumers, (deferred) => succeed3(deferred, left(value)), {
            discard: true
          }), stateDone(value)];
        }
        case OP_STATE_ERROR: {
          return [interrupt5, state];
        }
        case OP_STATE_DONE: {
          return [interrupt5, state];
        }
      }
    }));
  }
  emit(element) {
    return flatMap4(make6(), (deferred) => flatten3(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          const notifyConsumer = state.notifyConsumers[0];
          const notifyConsumers = state.notifyConsumers.slice(1);
          if (notifyConsumer !== void 0) {
            return [succeed3(notifyConsumer, right(element)), notifyConsumers.length === 0 ? stateEmpty(deferred) : stateEmit(notifyConsumers)];
          }
          throw new Error("Bug: Channel.SingleProducerAsyncInput.emit - Queue was empty! please report an issue at https://github.com/Effect-TS/effect/issues");
        }
        case OP_STATE_ERROR: {
          return [interrupt5, state];
        }
        case OP_STATE_DONE: {
          return [interrupt5, state];
        }
      }
    })));
  }
  error(cause) {
    return flatten3(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          return [forEach2(state.notifyConsumers, (deferred) => failCause2(deferred, cause), {
            discard: true
          }), stateError(cause)];
        }
        case OP_STATE_ERROR: {
          return [interrupt5, state];
        }
        case OP_STATE_DONE: {
          return [interrupt5, state];
        }
      }
    }));
  }
  get take() {
    return this.takeWith((cause) => failCause(map5(cause, left)), (elem) => succeed2(elem), (done4) => fail(right(done4)));
  }
  takeWith(onError4, onElement, onDone3) {
    return flatMap4(make6(), (deferred) => flatten3(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [zipRight3(succeed3(state.notifyProducer, void 0), matchCause2(_await(deferred), {
            onFailure: onError4,
            onSuccess: match({
              onLeft: onDone3,
              onRight: onElement
            })
          })), stateEmit([deferred])];
        }
        case OP_STATE_EMIT: {
          return [matchCause2(_await(deferred), {
            onFailure: onError4,
            onSuccess: match({
              onLeft: onDone3,
              onRight: onElement
            })
          }), stateEmit([...state.notifyConsumers, deferred])];
        }
        case OP_STATE_ERROR: {
          return [succeed5(onError4(state.cause)), state];
        }
        case OP_STATE_DONE: {
          return [succeed5(onDone3(state.done)), state];
        }
      }
    })));
  }
};
var make10 = () => pipe(make6(), flatMap4((deferred) => make5(stateEmpty(deferred))), map7((ref) => new SingleProducerAsyncInputImpl(ref)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel.js
var acquireUseRelease2 = (acquire, use, release) => flatMap6(fromEffect2(make5(() => _void)), (ref) => pipe(fromEffect2(uninterruptible2(tap3(acquire, (a) => set3(ref, (exit2) => release(a, exit2))))), flatMap6(use), ensuringWith((exit2) => flatMap4(get3(ref), (f) => f(exit2)))));
var as3 = dual(2, (self, value) => map9(self, () => value));
var asVoid3 = (self) => map9(self, constVoid);
var buffer = (options) => suspend5(() => {
  const doBuffer = (empty8, isEmpty10, ref) => unwrap(modify(ref, (inElem) => isEmpty10(inElem) ? [readWith({
    onInput: (input) => flatMap6(write(input), () => doBuffer(empty8, isEmpty10, ref)),
    onFailure: (error) => fail7(error),
    onDone: (done4) => succeedNow(done4)
  }), inElem] : [flatMap6(write(inElem), () => doBuffer(empty8, isEmpty10, ref)), empty8]));
  return doBuffer(options.empty, options.isEmpty, options.ref);
});
var bufferChunk = (ref) => buffer({
  empty: empty(),
  isEmpty,
  ref
});
var catchAll4 = dual(2, (self, f) => catchAllCause4(self, (cause) => match(failureOrCause(cause), {
  onLeft: f,
  onRight: failCause6
})));
var concatMap = dual(2, (self, f) => concatMapWith(self, f, () => void 0, () => void 0));
var collect = dual(2, (self, pf) => {
  const collector = readWith({
    onInput: (out) => match2(pf(out), {
      onNone: () => collector,
      onSome: (out2) => flatMap6(write(out2), () => collector)
    }),
    onFailure: fail7,
    onDone: succeedNow
  });
  return pipeTo(self, collector);
});
var concatOut = (self) => concatAll(self);
var mapInput = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap6(write(inElem), () => reader),
    onFailure: fail7,
    onDone: (done4) => succeedNow(f(done4))
  });
  return pipeTo(reader, self);
});
var mapInputEffect = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap6(write(inElem), () => reader),
    onFailure: fail7,
    onDone: (done4) => fromEffect2(f(done4))
  });
  return pipeTo(reader, self);
});
var mapInputError = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap6(write(inElem), () => reader),
    onFailure: (error) => fail7(f(error)),
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputErrorEffect = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap6(write(inElem), () => reader),
    onFailure: (error) => fromEffect2(f(error)),
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputIn = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap6(write(f(inElem)), () => reader),
    onFailure: fail7,
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputInEffect = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap6(flatMap6(fromEffect2(f(inElem)), write), () => reader),
    onFailure: fail7,
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var doneCollect = (self) => suspend5(() => {
  const builder = [];
  return pipe(pipeTo(self, doneCollectReader(builder)), flatMap6((outDone) => succeed7([unsafeFromArray(builder), outDone])));
});
var doneCollectReader = (builder) => {
  return readWith({
    onInput: (outElem) => flatMap6(sync5(() => {
      builder.push(outElem);
    }), () => doneCollectReader(builder)),
    onFailure: fail7,
    onDone: succeed7
  });
};
var drain = (self) => {
  const drainer = readWithCause({
    onInput: () => drainer,
    onFailure: failCause6,
    onDone: succeed7
  });
  return pipeTo(self, drainer);
};
var emitCollect = (self) => flatMap6(doneCollect(self), write);
var ensuring3 = dual(2, (self, finalizer3) => ensuringWith(self, () => finalizer3));
var context4 = () => fromEffect2(context2());
var contextWith2 = (f) => map9(context4(), f);
var contextWithChannel = (f) => flatMap6(context4(), f);
var contextWithEffect = (f) => mapEffect(context4(), f);
var flatten5 = (self) => flatMap6(self, identity);
var foldChannel = dual(2, (self, options) => foldCauseChannel(self, {
  onFailure: (cause) => {
    const either4 = failureOrCause(cause);
    switch (either4._tag) {
      case "Left": {
        return options.onFailure(either4.left);
      }
      case "Right": {
        return failCause6(either4.right);
      }
    }
  },
  onSuccess: options.onSuccess
}));
var fromEither = (either4) => suspend5(() => match(either4, {
  onLeft: fail7,
  onRight: succeed7
}));
var fromInput = (input) => unwrap(input.takeWith(failCause6, (elem) => flatMap6(write(elem), () => fromInput(input)), succeed7));
var fromPubSub = (pubsub) => unwrapScoped3(map7(subscribe2(pubsub), fromQueue));
var fromPubSubScoped = (pubsub) => map7(subscribe2(pubsub), fromQueue);
var fromOption = (option2) => suspend5(() => match2(option2, {
  onNone: () => fail7(none()),
  onSome: succeed7
}));
var fromQueue = (queue) => suspend5(() => fromQueueInternal(queue));
var fromQueueInternal = (queue) => pipe(fromEffect2(take2(queue)), flatMap6(match({
  onLeft: match3({
    onFailure: failCause6,
    onSuccess: succeedNow
  }),
  onRight: (elem) => flatMap6(write(elem), () => fromQueueInternal(queue))
})));
var identityChannel = () => readWith({
  onInput: (input) => flatMap6(write(input), () => identityChannel()),
  onFailure: fail7,
  onDone: succeedNow
});
var interruptWhen = dual(2, (self, effect4) => mergeWith(self, {
  other: fromEffect2(effect4),
  onSelfDone: (selfDone) => Done2(suspend3(() => selfDone)),
  onOtherDone: (effectDone) => Done2(suspend3(() => effectDone))
}));
var interruptWhenDeferred = dual(2, (self, deferred) => interruptWhen(self, _await(deferred)));
var map9 = dual(2, (self, f) => flatMap6(self, (a) => sync5(() => f(a))));
var mapEffect = dual(2, (self, f) => flatMap6(self, (z) => fromEffect2(f(z))));
var mapError4 = dual(2, (self, f) => mapErrorCause(self, map5(f)));
var mapErrorCause = dual(2, (self, f) => catchAllCause4(self, (cause) => failCause6(f(cause))));
var mapOut = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (outElem) => flatMap6(write(f(outElem)), () => reader),
    onFailure: fail7,
    onDone: succeedNow
  });
  return pipeTo(self, reader);
});
var mapOutEffect = dual(2, (self, f) => {
  const reader = readWithCause({
    onInput: (outElem) => pipe(fromEffect2(f(outElem)), flatMap6(write), flatMap6(() => reader)),
    onFailure: failCause6,
    onDone: succeedNow
  });
  return pipeTo(self, reader);
});
var mapOutEffectPar = dual(3, (self, f, n) => unwrapScopedWith((scope4) => gen(function* () {
  const input = yield* make10();
  const queueReader = fromInput(input);
  const queue = yield* bounded(n);
  yield* addFinalizer2(scope4, shutdown(queue));
  const errorSignal = yield* make6();
  const withPermits = n === Number.POSITIVE_INFINITY ? (_) => identity : (yield* makeSemaphore(n)).withPermits;
  const pull = yield* queueReader.pipe(pipeTo(self), toPullIn(scope4));
  yield* pull.pipe(matchCauseEffect({
    onFailure: (cause) => offer2(queue, failCause4(cause)),
    onSuccess: match({
      onLeft: (outDone) => zipRight3(interruptible2(withPermits(n)(_void)), asVoid2(offer2(queue, succeed5(left(outDone))))),
      onRight: (outElem) => gen(function* () {
        const deferred = yield* make6();
        const latch = yield* make6();
        yield* offer2(queue, map7(_await(deferred), right));
        yield* succeed3(latch, void 0).pipe(zipRight3(uninterruptibleMask2((restore) => exit(restore(_await(errorSignal))).pipe(raceFirst(exit(restore(f(outElem)))), flatMap4(identity))).pipe(tapErrorCause2((cause) => failCause2(errorSignal, cause)), intoDeferred(deferred))), withPermits(1), forkIn2(scope4));
        yield* _await(latch);
      })
    })
  }), forever, interruptible2, forkIn2(scope4));
  const consumer = unwrap(matchCause2(flatten3(take2(queue)), {
    onFailure: failCause6,
    onSuccess: match({
      onLeft: succeedNow,
      onRight: (outElem) => flatMap6(write(outElem), () => consumer)
    })
  }));
  return embedInput(consumer, input);
})));
var mergeAll3 = (options) => {
  return (channels) => mergeAllWith(options)(channels, constVoid);
};
var mergeAllUnbounded = (channels) => mergeAllWith({
  concurrency: "unbounded"
})(channels, constVoid);
var mergeAllUnboundedWith = (channels, f) => mergeAllWith({
  concurrency: "unbounded"
})(channels, f);
var mergeAllWith = ({
  bufferSize = 16,
  concurrency,
  mergeStrategy = BackPressure()
}) => (channels, f) => unwrapScopedWith((scope4) => gen(function* () {
  const concurrencyN = concurrency === "unbounded" ? Number.MAX_SAFE_INTEGER : concurrency;
  const input = yield* make10();
  const queueReader = fromInput(input);
  const queue = yield* bounded(bufferSize);
  yield* addFinalizer2(scope4, shutdown(queue));
  const cancelers = yield* unbounded2();
  yield* addFinalizer2(scope4, shutdown(cancelers));
  const lastDone = yield* make5(none());
  const errorSignal = yield* make6();
  const withPermits = (yield* makeSemaphore(concurrencyN)).withPermits;
  const pull = yield* toPullIn(pipeTo(queueReader, channels), scope4);
  function evaluatePull(pull2) {
    return pull2.pipe(flatMap4(match({
      onLeft: (done4) => succeed5(some(done4)),
      onRight: (outElem) => as2(offer2(queue, succeed5(right(outElem))), none())
    })), repeat({
      until: (_) => isSome(_)
    }), flatMap4((outDone) => update(lastDone, match2({
      onNone: () => some(outDone.value),
      onSome: (lastDone2) => some(f(lastDone2, outDone.value))
    }))), catchAllCause2((cause) => isInterrupted(cause) ? failCause4(cause) : offer2(queue, failCause4(cause)).pipe(zipRight3(succeed3(errorSignal, void 0)), asVoid2)));
  }
  yield* pull.pipe(matchCauseEffect({
    onFailure: (cause) => offer2(queue, failCause4(cause)).pipe(zipRight3(succeed5(false))),
    onSuccess: match({
      onLeft: (outDone) => raceWith(interruptible2(_await(errorSignal)), interruptible2(withPermits(concurrencyN)(_void)), {
        onSelfDone: (_, permitAcquisition) => as2(interrupt4(permitAcquisition), false),
        onOtherDone: (_, failureAwait) => zipRight3(interrupt4(failureAwait), get3(lastDone).pipe(flatMap4(match2({
          onNone: () => offer2(queue, succeed5(left(outDone))),
          onSome: (lastDone2) => offer2(queue, succeed5(left(f(lastDone2, outDone))))
        })), as2(false)))
      }),
      onRight: (channel) => match12(mergeStrategy, {
        onBackPressure: () => gen(function* () {
          const latch = yield* make6();
          const raceEffects = scopedWith((scope5) => toPullIn(pipeTo(queueReader, channel), scope5).pipe(flatMap4((pull2) => race(exit(evaluatePull(pull2)), exit(interruptible2(_await(errorSignal))))), flatMap4(identity)));
          yield* succeed3(latch, void 0).pipe(zipRight3(raceEffects), withPermits(1), forkIn2(scope4));
          yield* _await(latch);
          const errored = yield* isDone(errorSignal);
          return !errored;
        }),
        onBufferSliding: () => gen(function* () {
          const canceler = yield* make6();
          const latch = yield* make6();
          const size9 = yield* size2(cancelers);
          yield* take2(cancelers).pipe(flatMap4((canceler2) => succeed3(canceler2, void 0)), when(() => size9 >= concurrencyN));
          yield* offer2(cancelers, canceler);
          const raceEffects = scopedWith((scope5) => toPullIn(pipeTo(queueReader, channel), scope5).pipe(flatMap4((pull2) => exit(evaluatePull(pull2)).pipe(race(exit(interruptible2(_await(errorSignal)))), race(exit(interruptible2(_await(canceler)))))), flatMap4(identity)));
          yield* succeed3(latch, void 0).pipe(zipRight3(raceEffects), withPermits(1), forkIn2(scope4));
          yield* _await(latch);
          const errored = yield* isDone(errorSignal);
          return !errored;
        })
      })
    })
  }), repeat({
    while: (_) => _
  }), forkIn2(scope4));
  const consumer = pipe(take2(queue), flatten3, matchCause2({
    onFailure: failCause6,
    onSuccess: match({
      onLeft: succeedNow,
      onRight: (outElem) => flatMap6(write(outElem), () => consumer)
    })
  }), unwrap);
  return embedInput(consumer, input);
}));
var mergeMap = dual(3, (self, f, options) => mergeAll3(options)(mapOut(self, f)));
var mergeOut = dual(2, (self, n) => mergeAll3({
  concurrency: n
})(mapOut(self, identity)));
var mergeOutWith = dual(3, (self, n, f) => mergeAllWith({
  concurrency: n
})(mapOut(self, identity), f));
var mergeWith = dual(2, (self, options) => {
  function merge8(scope4) {
    return gen(function* () {
      const input = yield* make10();
      const queueReader = fromInput(input);
      const pullL = yield* toPullIn(pipeTo(queueReader, self), scope4);
      const pullR = yield* toPullIn(pipeTo(queueReader, options.other), scope4);
      function handleSide(exit2, fiber, pull) {
        return (done4, both, single) => {
          function onDecision(decision) {
            const op = decision;
            if (op._tag === OP_DONE2) {
              return succeed5(fromEffect2(zipRight3(interrupt4(fiber), op.effect)));
            }
            return map7(_await2(fiber), match3({
              onFailure: (cause) => fromEffect2(op.f(failCause(cause))),
              onSuccess: match({
                onLeft: (done5) => fromEffect2(op.f(succeed2(done5))),
                onRight: (elem) => zipRight4(write(elem), go(single(op.f)))
              })
            }));
          }
          return match3(exit2, {
            onFailure: (cause) => onDecision(done4(failCause(cause))),
            onSuccess: match({
              onLeft: (z) => onDecision(done4(succeed2(z))),
              onRight: (elem) => succeed5(flatMap6(write(elem), () => flatMap6(fromEffect2(forkIn2(interruptible2(pull), scope4)), (leftFiber) => go(both(leftFiber, fiber)))))
            })
          });
        };
      }
      function go(state) {
        switch (state._tag) {
          case OP_BOTH_RUNNING: {
            const leftJoin = interruptible2(join(state.left));
            const rightJoin = interruptible2(join(state.right));
            return unwrap(raceWith(leftJoin, rightJoin, {
              onSelfDone: (leftExit, rf) => zipRight3(interrupt4(rf), handleSide(leftExit, state.right, pullL)(options.onSelfDone, BothRunning, (f) => LeftDone(f))),
              onOtherDone: (rightExit, lf) => zipRight3(interrupt4(lf), handleSide(rightExit, state.left, pullR)(options.onOtherDone, (left2, right2) => BothRunning(right2, left2), (f) => RightDone(f)))
            }));
          }
          case OP_LEFT_DONE: {
            return unwrap(map7(exit(pullR), match3({
              onFailure: (cause) => fromEffect2(state.f(failCause(cause))),
              onSuccess: match({
                onLeft: (done4) => fromEffect2(state.f(succeed2(done4))),
                onRight: (elem) => flatMap6(write(elem), () => go(LeftDone(state.f)))
              })
            })));
          }
          case OP_RIGHT_DONE: {
            return unwrap(map7(exit(pullL), match3({
              onFailure: (cause) => fromEffect2(state.f(failCause(cause))),
              onSuccess: match({
                onLeft: (done4) => fromEffect2(state.f(succeed2(done4))),
                onRight: (elem) => flatMap6(write(elem), () => go(RightDone(state.f)))
              })
            })));
          }
        }
      }
      return fromEffect2(withFiberRuntime2((parent) => {
        const inherit = withFiberRuntime2((state) => {
          ;
          state.transferChildren(parent.scope());
          return _void;
        });
        const leftFiber = interruptible2(pullL).pipe(ensuring2(inherit), forkIn2(scope4));
        const rightFiber = interruptible2(pullR).pipe(ensuring2(inherit), forkIn2(scope4));
        return zipWith3(leftFiber, rightFiber, (left2, right2) => BothRunning(left2, right2));
      })).pipe(flatMap6(go), embedInput(input));
    });
  }
  return unwrapScopedWith(merge8);
});
var never2 = fromEffect2(never);
var orDie4 = dual(2, (self, error) => orDieWith(self, error));
var orDieWith = dual(2, (self, f) => catchAll4(self, (e) => failCauseSync4(() => die2(f(e)))));
var orElse5 = dual(2, (self, that) => catchAll4(self, that));
var pipeToOrFail = dual(2, (self, that) => suspend5(() => {
  let channelException = void 0;
  const reader = readWith({
    onInput: (outElem) => flatMap6(write(outElem), () => reader),
    onFailure: (outErr) => {
      channelException = ChannelException(outErr);
      return failCause6(die2(channelException));
    },
    onDone: succeedNow
  });
  const writer = readWithCause({
    onInput: (outElem) => pipe(write(outElem), flatMap6(() => writer)),
    onFailure: (cause) => isDieType(cause) && isChannelException(cause.defect) && equals(cause.defect, channelException) ? fail7(cause.defect.error) : failCause6(cause),
    onDone: succeedNow
  });
  return pipeTo(pipeTo(pipeTo(self, reader), that), writer);
}));
var provideService2 = dual(3, (self, tag, service3) => {
  return flatMap6(context4(), (context9) => provideContext(self, add(context9, tag, service3)));
});
var provideLayer = dual(2, (self, layer) => unwrapScopedWith((scope4) => map7(buildWithScope2(layer, scope4), (context9) => provideContext(self, context9))));
var mapInputContext = dual(2, (self, f) => contextWithChannel((context9) => provideContext(self, f(context9))));
var provideSomeLayer = dual(2, (self, layer) => (
  // @ts-expect-error
  provideLayer(self, merge4(context3(), layer))
));
var read = () => readOrFail(none());
var repeated = (self) => flatMap6(self, () => repeated(self));
var run = (self) => scopedWith((scope4) => runIn(self, scope4));
var runCollect = (self) => run(collectElements(self));
var runDrain = (self) => run(drain(self));
var runScoped = (self) => scopeWith((scope4) => runIn(self, scope4));
var scoped4 = (effect4) => unwrap(uninterruptibleMask2((restore) => map7(make7(), (scope4) => acquireReleaseOut(tapErrorCause2(restore(extend(effect4, scope4)), (cause) => close(scope4, failCause(cause))), (_, exit2) => close(scope4, exit2)))));
var scopedWith2 = (f) => unwrapScoped3(map7(scope2, (scope4) => flatMap6(fromEffect2(f(scope4)), write)));
var splitLines = () => suspend5(() => {
  let stringBuilder = "";
  let midCRLF = false;
  const splitLinesChunk = (chunk2) => {
    const chunkBuilder = [];
    map2(chunk2, (str) => {
      if (str.length !== 0) {
        let from = 0;
        let indexOfCR = str.indexOf("\r");
        let indexOfLF = str.indexOf("\n");
        if (midCRLF) {
          if (indexOfLF === 0) {
            chunkBuilder.push(stringBuilder);
            stringBuilder = "";
            from = 1;
            indexOfLF = str.indexOf("\n", from);
          } else {
            stringBuilder = stringBuilder + "\r";
          }
          midCRLF = false;
        }
        while (indexOfCR !== -1 || indexOfLF !== -1) {
          if (indexOfCR === -1 || indexOfLF !== -1 && indexOfLF < indexOfCR) {
            if (stringBuilder.length === 0) {
              chunkBuilder.push(str.substring(from, indexOfLF));
            } else {
              chunkBuilder.push(stringBuilder + str.substring(from, indexOfLF));
              stringBuilder = "";
            }
            from = indexOfLF + 1;
            indexOfLF = str.indexOf("\n", from);
          } else {
            if (str.length === indexOfCR + 1) {
              midCRLF = true;
              indexOfCR = -1;
            } else {
              if (indexOfLF === indexOfCR + 1) {
                if (stringBuilder.length === 0) {
                  chunkBuilder.push(str.substring(from, indexOfCR));
                } else {
                  stringBuilder = stringBuilder + str.substring(from, indexOfCR);
                  chunkBuilder.push(stringBuilder);
                  stringBuilder = "";
                }
                from = indexOfCR + 2;
                indexOfCR = str.indexOf("\r", from);
                indexOfLF = str.indexOf("\n", from);
              } else {
                indexOfCR = str.indexOf("\r", indexOfCR + 1);
              }
            }
          }
        }
        if (midCRLF) {
          stringBuilder = stringBuilder + str.substring(from, str.length - 1);
        } else {
          stringBuilder = stringBuilder + str.substring(from, str.length);
        }
      }
    });
    return unsafeFromArray(chunkBuilder);
  };
  const loop2 = readWithCause({
    onInput: (input) => {
      const out = splitLinesChunk(input);
      return isEmpty(out) ? loop2 : flatMap6(write(out), () => loop2);
    },
    onFailure: (cause) => stringBuilder.length === 0 ? failCause6(cause) : flatMap6(write(of(stringBuilder)), () => failCause6(cause)),
    onDone: (done4) => stringBuilder.length === 0 ? succeed7(done4) : flatMap6(write(of(stringBuilder)), () => succeed7(done4))
  });
  return loop2;
});
var toPubSub = (pubsub) => toQueue(pubsub);
var toPull = (self) => flatMap4(scope2, (scope4) => toPullIn(self, scope4));
var toPullIn = dual(2, (self, scope4) => zip2(sync3(() => new ChannelExecutor(self, void 0, identity)), runtime()).pipe(tap3(([executor, runtime2]) => addFinalizerExit(scope4, (exit2) => {
  const finalizer3 = executor.close(exit2);
  return finalizer3 !== void 0 ? provide2(finalizer3, runtime2) : _void;
})), uninterruptible2, map7(([executor]) => suspend3(() => interpretToPull(executor.run(), executor)))));
var interpretToPull = (channelState, exec) => {
  const state = channelState;
  switch (state._tag) {
    case OP_DONE: {
      return match3(exec.getDone(), {
        onFailure: failCause4,
        onSuccess: (done4) => succeed5(left(done4))
      });
    }
    case OP_EMIT2: {
      return succeed5(right(exec.getEmit()));
    }
    case OP_FROM_EFFECT2: {
      return pipe(state.effect, flatMap4(() => interpretToPull(exec.run(), exec)));
    }
    case OP_READ2: {
      return readUpstream(state, () => interpretToPull(exec.run(), exec), (cause) => failCause4(cause));
    }
  }
};
var toQueue = (queue) => suspend5(() => toQueueInternal(queue));
var toQueueInternal = (queue) => {
  return readWithCause({
    onInput: (elem) => flatMap6(fromEffect2(offer2(queue, right(elem))), () => toQueueInternal(queue)),
    onFailure: (cause) => fromEffect2(offer2(queue, left(failCause(cause)))),
    onDone: (done4) => fromEffect2(offer2(queue, left(succeed2(done4))))
  });
};
var unwrap = (channel) => flatten5(fromEffect2(channel));
var unwrapScoped3 = (self) => concatAllWith(scoped4(self), (d, _) => d, (d, _) => d);
var unwrapScopedWith = (f) => concatAllWith(scopedWith2(f), (d, _) => d, (d, _) => d);
var updateService2 = dual(3, (self, tag, f) => mapInputContext(self, (context9) => merge2(context9, make4(tag, f(unsafeGet2(context9, tag))))));
var withSpan3 = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  const acquire = all3([makeSpan(name, options), context2(), clock, get4(currentTracerTimingEnabled2)]);
  if (dataFirst) {
    const self = arguments[0];
    return acquireUseRelease2(acquire, ([span4, context9]) => provideContext(self, add(context9, spanTag, span4)), ([span4, , clock2, timingEnabled], exit2) => endSpan(span4, exit2, clock2, timingEnabled));
  }
  return (self) => acquireUseRelease2(acquire, ([span4, context9]) => provideContext(self, add(context9, spanTag, span4)), ([span4, , clock2, timingEnabled], exit2) => endSpan(span4, exit2, clock2, timingEnabled));
};
var writeAll = (...outs) => writeChunk(fromIterable2(outs));
var writeChunk = (outs) => writeChunkWriter(0, outs.length, outs);
var writeChunkWriter = (idx, len, chunk2) => {
  return idx === len ? void_3 : pipe(write(pipe(chunk2, unsafeGet(idx))), flatMap6(() => writeChunkWriter(idx + 1, len, chunk2)));
};
var zip3 = dual((args) => isChannel(args[1]), (self, that, options) => options?.concurrent ? mergeWith(self, {
  other: that,
  onSelfDone: (exit1) => Await((exit2) => suspend3(() => zip(exit1, exit2))),
  onOtherDone: (exit2) => Await((exit1) => suspend3(() => zip(exit1, exit2)))
}) : flatMap6(self, (a) => map9(that, (b) => [a, b])));
var zipLeft2 = dual((args) => isChannel(args[1]), (self, that, options) => options?.concurrent ? map9(zip3(self, that, {
  concurrent: true
}), (tuple) => tuple[0]) : flatMap6(self, (z) => as3(that, z)));
var zipRight4 = dual((args) => isChannel(args[1]), (self, that, options) => options?.concurrent ? map9(zip3(self, that, {
  concurrent: true
}), (tuple) => tuple[1]) : flatMap6(self, () => that));
var ChannelExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Channel/ChannelException");
var ChannelException = (error) => ({
  _tag: "ChannelException",
  [ChannelExceptionTypeId]: ChannelExceptionTypeId,
  error
});
var isChannelException = (u) => hasProperty(u, ChannelExceptionTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MergeDecision.js
var MergeDecision_exports = {};
__export(MergeDecision_exports, {
  Await: () => Await2,
  AwaitConst: () => AwaitConst2,
  Done: () => Done3,
  MergeDecisionTypeId: () => MergeDecisionTypeId2,
  isMergeDecision: () => isMergeDecision2,
  match: () => match13
});
var MergeDecisionTypeId2 = MergeDecisionTypeId;
var Done3 = Done2;
var Await2 = Await;
var AwaitConst2 = AwaitConst;
var isMergeDecision2 = isMergeDecision;
var match13 = match10;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RcRef.js
var RcRef_exports = {};
__export(RcRef_exports, {
  TypeId: () => TypeId3,
  get: () => get6,
  invalidate: () => invalidate2,
  make: () => make12
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/rcRef.js
var TypeId2 = /* @__PURE__ */ Symbol.for("effect/RcRef");
var stateEmpty2 = {
  _tag: "Empty"
};
var stateClosed = {
  _tag: "Closed"
};
var variance = {
  _A: identity,
  _E: identity
};
var RcRefImpl = class extends Class {
  acquire;
  context;
  scope;
  idleTimeToLive;
  [TypeId2] = variance;
  [TypeId] = TypeId;
  state = stateEmpty2;
  semaphore = unsafeMakeSemaphore(1);
  constructor(acquire, context9, scope4, idleTimeToLive) {
    super();
    this.acquire = acquire;
    this.context = context9;
    this.scope = scope4;
    this.idleTimeToLive = idleTimeToLive;
    this.get = get5(this);
  }
  get;
  commit() {
    return this.get;
  }
};
var make11 = (options) => withFiberRuntime((fiber) => {
  const context9 = fiber.getFiberRef(currentContext);
  const scope4 = get2(context9, scopeTag);
  const ref = new RcRefImpl(options.acquire, context9, scope4, options.idleTimeToLive ? decode(options.idleTimeToLive) : void 0);
  return as(scope4.addFinalizer(() => ref.semaphore.withPermits(1)(suspend(() => {
    const close2 = ref.state._tag === "Acquired" ? scopeClose(ref.state.scope, exitVoid) : void_;
    ref.state = stateClosed;
    return close2;
  }))), ref);
});
var get5 = (self_) => {
  const self = self_;
  const isInfinite = self.idleTimeToLive && !isFinite(self.idleTimeToLive);
  return uninterruptibleMask((restore) => suspend(() => {
    switch (self.state._tag) {
      case "Closed": {
        return interrupt;
      }
      case "Acquired": {
        self.state.refCount++;
        return self.state.fiber ? as(interruptFiber(self.state.fiber), self.state) : succeed(self.state);
      }
      case "Empty": {
        return scopeMake().pipe(bindTo2("scope"), bind("value", ({
          scope: scope4
        }) => restore(fiberRefLocally(self.acquire, currentContext, add(self.context, scopeTag, scope4)))), map3(({
          scope: scope4,
          value
        }) => {
          const state = {
            _tag: "Acquired",
            value,
            scope: scope4,
            fiber: void 0,
            refCount: 1
          };
          self.state = state;
          return state;
        }));
      }
    }
  })).pipe(self.semaphore.withPermits(1), bindTo2("state"), bind("scope", () => scopeTag), tap(({
    scope: scope4,
    state
  }) => scope4.addFinalizer(() => suspend(() => {
    state.refCount--;
    if (state.refCount > 0 || isInfinite) {
      return void_;
    }
    if (self.idleTimeToLive === void 0) {
      self.state = stateEmpty2;
      return scopeClose(state.scope, exitVoid);
    }
    return sleep2(self.idleTimeToLive).pipe(interruptible, zipRight(suspend(() => {
      if (self.state._tag === "Acquired" && self.state.refCount === 0) {
        self.state = stateEmpty2;
        return scopeClose(state.scope, exitVoid);
      }
      return void_;
    })), ensuring(sync(() => {
      state.fiber = void 0;
    })), forkIn(self.scope), tap((fiber) => {
      state.fiber = fiber;
    }), self.semaphore.withPermits(1));
  }))), map3(({
    state
  }) => state.value));
};
var invalidate = (self_) => {
  const self = self_;
  return uninterruptible(suspend(() => {
    if (self.state._tag !== "Acquired") {
      return void_;
    }
    const state = self.state;
    self.state = stateEmpty2;
    return state.scope.close(exitVoid);
  }));
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RcRef.js
var TypeId3 = TypeId2;
var make12 = make11;
var get6 = get5;
var invalidate2 = invalidate;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Runtime.js
var Runtime_exports = {};
__export(Runtime_exports, {
  FiberFailureCauseId: () => FiberFailureCauseId2,
  FiberFailureId: () => FiberFailureId,
  defaultRuntime: () => defaultRuntime2,
  defaultRuntimeFlags: () => defaultRuntimeFlags2,
  deleteFiberRef: () => deleteFiberRef2,
  disableRuntimeFlag: () => disableRuntimeFlag2,
  enableRuntimeFlag: () => enableRuntimeFlag2,
  isAsyncFiberException: () => isAsyncFiberException2,
  isFiberFailure: () => isFiberFailure2,
  make: () => make13,
  makeFiberFailure: () => makeFiberFailure,
  provideService: () => provideService3,
  runCallback: () => runCallback,
  runFork: () => runFork2,
  runPromise: () => runPromise2,
  runPromiseExit: () => runPromiseExit,
  runSync: () => runSync2,
  runSyncExit: () => runSyncExit,
  setFiberRef: () => setFiberRef2,
  updateContext: () => updateContext2,
  updateFiberRefs: () => updateFiberRefs2,
  updateRuntimeFlags: () => updateRuntimeFlags2
});
var runFork2 = unsafeFork;
var runSyncExit = unsafeRunSyncExit;
var runSync2 = unsafeRunSync;
var runCallback = unsafeRunCallback;
var runPromise2 = unsafeRunPromise;
var runPromiseExit = unsafeRunPromiseExit;
var defaultRuntime2 = defaultRuntime;
var defaultRuntimeFlags2 = defaultRuntimeFlags;
var make13 = make8;
var FiberFailureId = /* @__PURE__ */ Symbol.for("effect/Runtime/FiberFailure");
var FiberFailureCauseId2 = FiberFailureCauseId;
var isAsyncFiberException2 = isAsyncFiberException;
var isFiberFailure2 = isFiberFailure;
var makeFiberFailure = fiberFailure;
var updateRuntimeFlags2 = updateRuntimeFlags;
var enableRuntimeFlag2 = enableRuntimeFlag;
var disableRuntimeFlag2 = disableRuntimeFlag;
var updateContext2 = updateContext;
var provideService3 = provideService;
var updateFiberRefs2 = updateFiberRefs;
var setFiberRef2 = setFiberRef;
var deleteFiberRef2 = deleteFiberRef;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/StreamHaltStrategy.js
var StreamHaltStrategy_exports = {};
__export(StreamHaltStrategy_exports, {
  Both: () => Both2,
  Either: () => Either2,
  Left: () => Left2,
  Right: () => Right2,
  fromInput: () => fromInput3,
  isBoth: () => isBoth2,
  isEither: () => isEither2,
  isLeft: () => isLeft2,
  isRight: () => isRight2,
  match: () => match15
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/streamHaltStrategy.js
var OP_LEFT = "Left";
var OP_RIGHT = "Right";
var OP_BOTH = "Both";
var OP_EITHER = "Either";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/haltStrategy.js
var Left = {
  _tag: OP_LEFT
};
var Right = {
  _tag: OP_RIGHT
};
var Both = {
  _tag: OP_BOTH
};
var Either = {
  _tag: OP_EITHER
};
var fromInput2 = (input) => {
  switch (input) {
    case "left":
      return Left;
    case "right":
      return Right;
    case "both":
      return Both;
    case "either":
      return Either;
    default:
      return input;
  }
};
var isLeft = (self) => self._tag === OP_LEFT;
var isRight = (self) => self._tag === OP_RIGHT;
var isBoth = (self) => self._tag === OP_BOTH;
var isEither = (self) => self._tag === OP_EITHER;
var match14 = dual(2, (self, options) => {
  switch (self._tag) {
    case OP_LEFT: {
      return options.onLeft();
    }
    case OP_RIGHT: {
      return options.onRight();
    }
    case OP_BOTH: {
      return options.onBoth();
    }
    case OP_EITHER: {
      return options.onEither();
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/StreamHaltStrategy.js
var Left2 = Left;
var Right2 = Right;
var Both2 = Both;
var Either2 = Either;
var fromInput3 = fromInput2;
var isLeft2 = isLeft;
var isRight2 = isRight;
var isBoth2 = isBoth;
var isEither2 = isEither;
var match15 = match14;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TPubSub.js
var TPubSub_exports = {};
__export(TPubSub_exports, {
  TPubSubTypeId: () => TPubSubTypeId2,
  awaitShutdown: () => awaitShutdown6,
  bounded: () => bounded6,
  capacity: () => capacity6,
  dropping: () => dropping6,
  isEmpty: () => isEmpty8,
  isFull: () => isFull6,
  isShutdown: () => isShutdown6,
  publish: () => publish4,
  publishAll: () => publishAll4,
  shutdown: () => shutdown6,
  size: () => size7,
  sliding: () => sliding6,
  subscribe: () => subscribe4,
  subscribeScoped: () => subscribeScoped2,
  unbounded: () => unbounded7
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/sink.js
var SinkTypeId = /* @__PURE__ */ Symbol.for("effect/Sink");
var sinkVariance = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _L: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var SinkImpl = class {
  channel;
  [SinkTypeId] = sinkVariance;
  constructor(channel) {
    this.channel = channel;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isSink = (u) => hasProperty(u, SinkTypeId);
var suspend6 = (evaluate2) => new SinkImpl(suspend5(() => toChannel(evaluate2())));
var as4 = dual(2, (self, a) => pipe(self, map10(() => a)));
var collectAll = () => new SinkImpl(collectAllLoop(empty()));
var collectAllLoop = (acc) => readWithCause({
  onInput: (chunk2) => collectAllLoop(pipe(acc, appendAll(chunk2))),
  onFailure: failCause6,
  onDone: () => succeed7(acc)
});
var collectAllN = (n) => suspend6(() => fromChannel(collectAllNLoop(n, empty())));
var collectAllNLoop = (n, acc) => readWithCause({
  onInput: (chunk2) => {
    const [collected, leftovers] = splitAt(chunk2, n);
    if (collected.length < n) {
      return collectAllNLoop(n - collected.length, appendAll(acc, collected));
    }
    if (isEmpty(leftovers)) {
      return succeed7(appendAll(acc, collected));
    }
    return flatMap6(write(leftovers), () => succeed7(appendAll(acc, collected)));
  },
  onFailure: failCause6,
  onDone: () => succeed7(acc)
});
var collectAllFrom = (self) => collectAllWhileWith(self, {
  initial: empty(),
  while: constTrue,
  body: (chunk2, a) => pipe(chunk2, append(a))
});
var collectAllToMap = (key, merge8) => {
  return foldLeftChunks(empty3(), (map15, chunk2) => pipe(chunk2, reduce(map15, (map16, input) => {
    const k = key(input);
    const v = pipe(map16, has2(k)) ? merge8(pipe(map16, unsafeGet3(k)), input) : input;
    return pipe(map16, set2(k, v));
  })));
};
var collectAllToMapN = (n, key, merge8) => {
  return foldWeighted({
    initial: empty3(),
    maxCost: n,
    cost: (acc, input) => pipe(acc, has2(key(input))) ? 0 : 1,
    body: (acc, input) => {
      const k = key(input);
      const v = pipe(acc, has2(k)) ? merge8(pipe(acc, unsafeGet3(k)), input) : input;
      return pipe(acc, set2(k, v));
    }
  });
};
var collectAllToSet = () => foldLeftChunks(empty2(), (acc, chunk2) => pipe(chunk2, reduce(acc, (acc2, input) => pipe(acc2, add2(input)))));
var collectAllToSetN = (n) => foldWeighted({
  initial: empty2(),
  maxCost: n,
  cost: (acc, input) => has(acc, input) ? 0 : 1,
  body: (acc, input) => add2(acc, input)
});
var collectAllUntil = (p) => {
  return pipe(fold([empty(), true], (tuple) => tuple[1], ([chunk2, _], input) => [pipe(chunk2, append(input)), !p(input)]), map10((tuple) => tuple[0]));
};
var collectAllUntilEffect = (p) => {
  return pipe(foldEffect([empty(), true], (tuple) => tuple[1], ([chunk2, _], input) => pipe(p(input), map7((bool) => [pipe(chunk2, append(input)), !bool]))), map10((tuple) => tuple[0]));
};
var collectAllWhile = (predicate) => fromChannel(collectAllWhileReader(predicate, empty()));
var collectAllWhileReader = (predicate, done4) => readWith({
  onInput: (input) => {
    const [collected, leftovers] = pipe(toReadonlyArray(input), span(predicate));
    if (leftovers.length === 0) {
      return collectAllWhileReader(predicate, pipe(done4, appendAll(unsafeFromArray(collected))));
    }
    return pipe(write(unsafeFromArray(leftovers)), zipRight4(succeed7(pipe(done4, appendAll(unsafeFromArray(collected))))));
  },
  onFailure: fail7,
  onDone: () => succeed7(done4)
});
var collectAllWhileEffect = (predicate) => fromChannel(collectAllWhileEffectReader(predicate, empty()));
var collectAllWhileEffectReader = (predicate, done4) => readWith({
  onInput: (input) => pipe(fromEffect2(pipe(input, takeWhile2(predicate), map7(unsafeFromArray))), flatMap6((collected) => {
    const leftovers = pipe(input, drop(collected.length));
    if (isEmpty(leftovers)) {
      return collectAllWhileEffectReader(predicate, pipe(done4, appendAll(collected)));
    }
    return pipe(write(leftovers), zipRight4(succeed7(pipe(done4, appendAll(collected)))));
  })),
  onFailure: fail7,
  onDone: () => succeed7(done4)
});
var collectAllWhileWith = dual(2, (self, options) => {
  const refs = pipe(make5(empty()), zip2(make5(false)));
  const newChannel = pipe(fromEffect2(refs), flatMap6(([leftoversRef, upstreamDoneRef]) => {
    const upstreamMarker = readWith({
      onInput: (input) => pipe(write(input), flatMap6(() => upstreamMarker)),
      onFailure: fail7,
      onDone: (done4) => pipe(fromEffect2(set3(upstreamDoneRef, true)), as3(done4))
    });
    return pipe(upstreamMarker, pipeTo(bufferChunk(leftoversRef)), pipeTo(collectAllWhileWithLoop(self, leftoversRef, upstreamDoneRef, options.initial, options.while, options.body)));
  }));
  return new SinkImpl(newChannel);
});
var collectAllWhileWithLoop = (self, leftoversRef, upstreamDoneRef, currentResult, p, f) => {
  return pipe(toChannel(self), doneCollect, foldChannel({
    onFailure: fail7,
    onSuccess: ([leftovers, doneValue]) => p(doneValue) ? pipe(fromEffect2(set3(leftoversRef, flatten(leftovers))), flatMap6(() => pipe(fromEffect2(get3(upstreamDoneRef)), flatMap6((upstreamDone) => {
      const accumulatedResult = f(currentResult, doneValue);
      return upstreamDone ? pipe(write(flatten(leftovers)), as3(accumulatedResult)) : collectAllWhileWithLoop(self, leftoversRef, upstreamDoneRef, accumulatedResult, p, f);
    })))) : pipe(write(flatten(leftovers)), as3(currentResult))
  }));
};
var collectLeftover = (self) => new SinkImpl(pipe(collectElements(toChannel(self)), map9(([chunks3, z]) => [z, flatten(chunks3)])));
var mapInput2 = dual(2, (self, f) => pipe(self, mapInputChunks(map2(f))));
var mapInputEffect2 = dual(2, (self, f) => mapInputChunksEffect(self, (chunk2) => map7(forEach2(chunk2, (v) => f(v)), unsafeFromArray)));
var mapInputChunks = dual(2, (self, f) => {
  const loop2 = readWith({
    onInput: (chunk2) => pipe(write(f(chunk2)), flatMap6(() => loop2)),
    onFailure: fail7,
    onDone: succeed7
  });
  return new SinkImpl(pipe(loop2, pipeTo(toChannel(self))));
});
var mapInputChunksEffect = dual(2, (self, f) => {
  const loop2 = readWith({
    onInput: (chunk2) => pipe(fromEffect2(f(chunk2)), flatMap6(write), flatMap6(() => loop2)),
    onFailure: fail7,
    onDone: succeed7
  });
  return new SinkImpl(pipe(loop2, pipeToOrFail(toChannel(self))));
});
var die6 = (defect) => failCause7(die2(defect));
var dieMessage2 = (message) => failCause7(die2(new RuntimeException(message)));
var dieSync4 = (evaluate2) => failCauseSync5(() => die2(evaluate2()));
var dimap = dual(2, (self, options) => map10(mapInput2(self, options.onInput), options.onDone));
var dimapEffect = dual(2, (self, options) => mapEffect2(mapInputEffect2(self, options.onInput), options.onDone));
var dimapChunks = dual(2, (self, options) => map10(mapInputChunks(self, options.onInput), options.onDone));
var dimapChunksEffect = dual(2, (self, options) => mapEffect2(mapInputChunksEffect(self, options.onInput), options.onDone));
var drain2 = new SinkImpl(drain(identityChannel()));
var drop2 = (n) => suspend6(() => new SinkImpl(dropLoop(n)));
var dropLoop = (n) => readWith({
  onInput: (input) => {
    const dropped = pipe(input, drop(n));
    const leftover2 = Math.max(n - input.length, 0);
    const more = isEmpty(input) || leftover2 > 0;
    if (more) {
      return dropLoop(leftover2);
    }
    return pipe(write(dropped), zipRight4(identityChannel()));
  },
  onFailure: fail7,
  onDone: () => void_3
});
var dropUntil2 = (predicate) => new SinkImpl(pipe(toChannel(dropWhile3((input) => !predicate(input))), pipeToOrFail(toChannel(drop2(1)))));
var dropUntilEffect = (predicate) => suspend6(() => new SinkImpl(dropUntilEffectReader(predicate)));
var dropUntilEffectReader = (predicate) => readWith({
  onInput: (input) => pipe(input, dropUntil(predicate), map7((leftover2) => {
    const more = leftover2.length === 0;
    return more ? dropUntilEffectReader(predicate) : pipe(write(unsafeFromArray(leftover2)), zipRight4(identityChannel()));
  }), unwrap),
  onFailure: fail7,
  onDone: () => void_3
});
var dropWhile3 = (predicate) => new SinkImpl(dropWhileReader(predicate));
var dropWhileReader = (predicate) => readWith({
  onInput: (input) => {
    const out = pipe(input, dropWhile(predicate));
    if (isEmpty(out)) {
      return dropWhileReader(predicate);
    }
    return pipe(write(out), zipRight4(identityChannel()));
  },
  onFailure: fail7,
  onDone: succeedNow
});
var dropWhileEffect = (predicate) => suspend6(() => new SinkImpl(dropWhileEffectReader(predicate)));
var dropWhileEffectReader = (predicate) => readWith({
  onInput: (input) => pipe(input, dropWhile2(predicate), map7((leftover2) => {
    const more = leftover2.length === 0;
    return more ? dropWhileEffectReader(predicate) : pipe(write(unsafeFromArray(leftover2)), zipRight4(identityChannel()));
  }), unwrap),
  onFailure: fail7,
  onDone: () => void_3
});
var ensuring4 = dual(2, (self, finalizer3) => new SinkImpl(pipe(self, toChannel, ensuring3(finalizer3))));
var ensuringWith2 = dual(2, (self, finalizer3) => new SinkImpl(pipe(self, toChannel, ensuringWith(finalizer3))));
var context5 = () => fromEffect4(context2());
var contextWith3 = (f) => pipe(context5(), map10(f));
var contextWithEffect2 = (f) => pipe(context5(), mapEffect2(f));
var contextWithSink = (f) => new SinkImpl(unwrap(contextWith((context9) => toChannel(f(context9)))));
var every2 = (predicate) => fold(true, identity, (acc, input) => acc && predicate(input));
var fail8 = (e) => new SinkImpl(fail7(e));
var failSync5 = (evaluate2) => new SinkImpl(failSync4(evaluate2));
var failCause7 = (cause) => new SinkImpl(failCause6(cause));
var failCauseSync5 = (evaluate2) => new SinkImpl(failCauseSync4(evaluate2));
var filterInput = (f) => {
  return (self) => pipe(self, mapInputChunks(filter(f)));
};
var filterInputEffect = dual(2, (self, f) => mapInputChunksEffect(self, (chunk2) => map7(filter2(chunk2, f), unsafeFromArray)));
var findEffect = dual(2, (self, f) => {
  const newChannel = pipe(fromEffect2(pipe(make5(empty()), zip2(make5(false)))), flatMap6(([leftoversRef, upstreamDoneRef]) => {
    const upstreamMarker = readWith({
      onInput: (input) => pipe(write(input), flatMap6(() => upstreamMarker)),
      onFailure: fail7,
      onDone: (done4) => pipe(fromEffect2(set3(upstreamDoneRef, true)), as3(done4))
    });
    const loop2 = foldChannel(collectElements(toChannel(self)), {
      onFailure: fail7,
      onSuccess: ([leftovers, doneValue]) => pipe(fromEffect2(f(doneValue)), flatMap6((satisfied) => pipe(fromEffect2(set3(leftoversRef, flatten(leftovers))), zipRight4(pipe(fromEffect2(get3(upstreamDoneRef)), flatMap6((upstreamDone) => {
        if (satisfied) {
          return pipe(write(flatten(leftovers)), as3(some(doneValue)));
        }
        if (upstreamDone) {
          return pipe(write(flatten(leftovers)), as3(none()));
        }
        return loop2;
      }))))))
    });
    return pipe(upstreamMarker, pipeTo(bufferChunk(leftoversRef)), pipeTo(loop2));
  }));
  return new SinkImpl(newChannel);
});
var fold = (s, contFn, f) => suspend6(() => new SinkImpl(foldReader(s, contFn, f)));
var foldReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => {
      const [nextS, leftovers] = foldChunkSplit(s, input, contFn, f, 0, input.length);
      if (isNonEmpty(leftovers)) {
        return pipe(write(leftovers), as3(nextS));
      }
      return foldReader(nextS, contFn, f);
    },
    onFailure: fail7,
    onDone: () => succeedNow(s)
  });
};
var foldChunkSplit = (s, chunk2, contFn, f, index, length) => {
  if (index === length) {
    return [s, empty()];
  }
  const s1 = f(s, pipe(chunk2, unsafeGet(index)));
  if (contFn(s1)) {
    return foldChunkSplit(s1, chunk2, contFn, f, index + 1, length);
  }
  return [s1, pipe(chunk2, drop(index + 1))];
};
var foldSink = dual(2, (self, options) => {
  const newChannel = pipe(toChannel(self), collectElements, foldChannel({
    onFailure: (error) => toChannel(options.onFailure(error)),
    onSuccess: ([leftovers, z]) => suspend5(() => {
      const leftoversRef = {
        ref: pipe(leftovers, filter(isNonEmpty))
      };
      const refReader = pipe(
        sync5(() => {
          const ref = leftoversRef.ref;
          leftoversRef.ref = empty();
          return ref;
        }),
        // This cast is safe because of the L1 >: L <: In1 bound. It follows that
        // L <: In1 and therefore Chunk[L] can be safely cast to Chunk[In1].
        flatMap6((chunk2) => writeChunk(chunk2))
      );
      const passthrough3 = identityChannel();
      const continuationSink = pipe(refReader, zipRight4(passthrough3), pipeTo(toChannel(options.onSuccess(z))));
      return flatMap6(collectElements(continuationSink), ([newLeftovers, z1]) => pipe(succeed7(leftoversRef.ref), flatMap6(writeChunk), zipRight4(writeChunk(newLeftovers)), as3(z1)));
    })
  }));
  return new SinkImpl(newChannel);
});
var foldChunks = (s, contFn, f) => suspend6(() => new SinkImpl(foldChunksReader(s, contFn, f)));
var foldChunksReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => foldChunksReader(f(s, input), contFn, f),
    onFailure: fail7,
    onDone: () => succeedNow(s)
  });
};
var foldChunksEffect = (s, contFn, f) => suspend6(() => new SinkImpl(foldChunksEffectReader(s, contFn, f)));
var foldChunksEffectReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => pipe(fromEffect2(f(s, input)), flatMap6((s2) => foldChunksEffectReader(s2, contFn, f))),
    onFailure: fail7,
    onDone: () => succeedNow(s)
  });
};
var foldEffect = (s, contFn, f) => suspend6(() => new SinkImpl(foldEffectReader(s, contFn, f)));
var foldEffectReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => pipe(fromEffect2(foldChunkSplitEffect(s, input, contFn, f)), flatMap6(([nextS, leftovers]) => pipe(leftovers, match2({
      onNone: () => foldEffectReader(nextS, contFn, f),
      onSome: (leftover2) => pipe(write(leftover2), as3(nextS))
    })))),
    onFailure: fail7,
    onDone: () => succeedNow(s)
  });
};
var foldChunkSplitEffect = (s, chunk2, contFn, f) => foldChunkSplitEffectInternal(s, chunk2, 0, chunk2.length, contFn, f);
var foldChunkSplitEffectInternal = (s, chunk2, index, length, contFn, f) => {
  if (index === length) {
    return succeed5([s, none()]);
  }
  return pipe(f(s, pipe(chunk2, unsafeGet(index))), flatMap4((s1) => contFn(s1) ? foldChunkSplitEffectInternal(s1, chunk2, index + 1, length, contFn, f) : succeed5([s1, some(pipe(chunk2, drop(index + 1)))])));
};
var foldLeft = (s, f) => ignoreLeftover(fold(s, constTrue, f));
var foldLeftChunks = (s, f) => foldChunks(s, constTrue, f);
var foldLeftChunksEffect = (s, f) => ignoreLeftover(foldChunksEffect(s, constTrue, f));
var foldLeftEffect = (s, f) => foldEffect(s, constTrue, f);
var foldUntil = (s, max, f) => pipe(fold([s, 0], (tuple) => tuple[1] < max, ([output, count2], input) => [f(output, input), count2 + 1]), map10((tuple) => tuple[0]));
var foldUntilEffect = (s, max, f) => pipe(foldEffect([s, 0], (tuple) => tuple[1] < max, ([output, count2], input) => pipe(f(output, input), map7((s2) => [s2, count2 + 1]))), map10((tuple) => tuple[0]));
var foldWeighted = (options) => foldWeightedDecompose({
  ...options,
  decompose: of
});
var foldWeightedDecompose = (options) => suspend6(() => new SinkImpl(foldWeightedDecomposeLoop(options.initial, 0, false, options.maxCost, options.cost, options.decompose, options.body)));
var foldWeightedDecomposeLoop = (s, cost, dirty, max, costFn, decompose, f) => readWith({
  onInput: (input) => {
    const [nextS, nextCost, nextDirty, leftovers] = foldWeightedDecomposeFold(input, s, cost, dirty, max, costFn, decompose, f);
    if (isNonEmpty(leftovers)) {
      return pipe(write(leftovers), zipRight4(succeedNow(nextS)));
    }
    if (cost > max) {
      return succeedNow(nextS);
    }
    return foldWeightedDecomposeLoop(nextS, nextCost, nextDirty, max, costFn, decompose, f);
  },
  onFailure: fail7,
  onDone: () => succeedNow(s)
});
var foldWeightedDecomposeFold = (input, s, cost, dirty, max, costFn, decompose, f) => {
  for (let index = 0; index < input.length; index++) {
    const elem = unsafeGet(input, index);
    const prevCost = cost;
    cost = cost + costFn(s, elem);
    if (cost <= max) {
      s = f(s, elem);
      dirty = true;
      continue;
    }
    const decomposed = decompose(elem);
    if (decomposed.length <= 1 && !dirty) {
      return [f(s, elem), cost, true, drop(input, index + 1)];
    }
    if (decomposed.length <= 1 && dirty) {
      return [s, prevCost, dirty, drop(input, index)];
    }
    input = appendAll(decomposed, drop(input, index + 1));
    cost = prevCost;
    index = -1;
  }
  return [s, cost, dirty, empty()];
};
var foldWeightedDecomposeEffect = (options) => suspend6(() => new SinkImpl(foldWeightedDecomposeEffectLoop(options.initial, options.maxCost, options.cost, options.decompose, options.body, 0, false)));
var foldWeightedEffect = (options) => foldWeightedDecomposeEffect({
  ...options,
  decompose: (input) => succeed5(of(input))
});
var foldWeightedDecomposeEffectLoop = (s, max, costFn, decompose, f, cost, dirty) => readWith({
  onInput: (input) => pipe(fromEffect2(foldWeightedDecomposeEffectFold(s, max, costFn, decompose, f, input, dirty, cost, 0)), flatMap6(([nextS, nextCost, nextDirty, leftovers]) => {
    if (isNonEmpty(leftovers)) {
      return pipe(write(leftovers), zipRight4(succeedNow(nextS)));
    }
    if (cost > max) {
      return succeedNow(nextS);
    }
    return foldWeightedDecomposeEffectLoop(nextS, max, costFn, decompose, f, nextCost, nextDirty);
  })),
  onFailure: fail7,
  onDone: () => succeedNow(s)
});
var foldWeightedDecomposeEffectFold = (s, max, costFn, decompose, f, input, dirty, cost, index) => {
  if (index === input.length) {
    return succeed5([s, cost, dirty, empty()]);
  }
  const elem = pipe(input, unsafeGet(index));
  return pipe(costFn(s, elem), map7((newCost) => cost + newCost), flatMap4((total) => {
    if (total <= max) {
      return pipe(f(s, elem), flatMap4((s2) => foldWeightedDecomposeEffectFold(s2, max, costFn, decompose, f, input, true, total, index + 1)));
    }
    return pipe(decompose(elem), flatMap4((decomposed) => {
      if (decomposed.length <= 1 && !dirty) {
        return pipe(f(s, elem), map7((s2) => [s2, total, true, pipe(input, drop(index + 1))]));
      }
      if (decomposed.length <= 1 && dirty) {
        return succeed5([s, cost, dirty, pipe(input, drop(index))]);
      }
      const next = pipe(decomposed, appendAll(pipe(input, drop(index + 1))));
      return foldWeightedDecomposeEffectFold(s, max, costFn, decompose, f, next, dirty, cost, 0);
    }));
  }));
};
var flatMap7 = dual(2, (self, f) => foldSink(self, {
  onFailure: fail8,
  onSuccess: f
}));
var forEach3 = (f) => {
  const process = readWithCause({
    onInput: (input) => pipe(fromEffect2(forEach2(input, (v) => f(v), {
      discard: true
    })), flatMap6(() => process)),
    onFailure: failCause6,
    onDone: () => void_3
  });
  return new SinkImpl(process);
};
var forEachChunk = (f) => {
  const process = readWithCause({
    onInput: (input) => pipe(fromEffect2(f(input)), flatMap6(() => process)),
    onFailure: failCause6,
    onDone: () => void_3
  });
  return new SinkImpl(process);
};
var forEachWhile = (f) => {
  const process = readWithCause({
    onInput: (input) => forEachWhileReader(f, input, 0, input.length, process),
    onFailure: failCause6,
    onDone: () => void_3
  });
  return new SinkImpl(process);
};
var forEachWhileReader = (f, input, index, length, cont) => {
  if (index === length) {
    return cont;
  }
  return pipe(fromEffect2(f(pipe(input, unsafeGet(index)))), flatMap6((bool) => bool ? forEachWhileReader(f, input, index + 1, length, cont) : write(pipe(input, drop(index)))), catchAll4((error) => pipe(write(pipe(input, drop(index))), zipRight4(fail7(error)))));
};
var forEachChunkWhile = (f) => {
  const reader = readWith({
    onInput: (input) => pipe(fromEffect2(f(input)), flatMap6((cont) => cont ? reader : void_3)),
    onFailure: fail7,
    onDone: () => void_3
  });
  return new SinkImpl(reader);
};
var fromChannel = (channel) => new SinkImpl(channel);
var fromEffect4 = (effect4) => new SinkImpl(fromEffect2(effect4));
var fromPubSub2 = (pubsub, options) => fromQueue2(pubsub, options);
var fromPush = (push) => new SinkImpl(unwrapScoped3(pipe(push, map7(fromPushPull))));
var fromPushPull = (push) => readWith({
  onInput: (input) => foldChannel(fromEffect2(push(some(input))), {
    onFailure: ([either4, leftovers]) => match(either4, {
      onLeft: (error) => pipe(write(leftovers), zipRight4(fail7(error))),
      onRight: (z) => pipe(write(leftovers), zipRight4(succeedNow(z)))
    }),
    onSuccess: () => fromPushPull(push)
  }),
  onFailure: fail7,
  onDone: () => foldChannel(fromEffect2(push(none())), {
    onFailure: ([either4, leftovers]) => match(either4, {
      onLeft: (error) => pipe(write(leftovers), zipRight4(fail7(error))),
      onRight: (z) => pipe(write(leftovers), zipRight4(succeedNow(z)))
    }),
    onSuccess: () => fromEffect2(dieMessage("BUG: Sink.fromPush - please report an issue at https://github.com/Effect-TS/effect/issues"))
  })
});
var fromQueue2 = (queue, options) => options?.shutdown ? unwrapScoped4(map7(acquireRelease2(succeed5(queue), shutdown), fromQueue2)) : forEachChunk((input) => offerAll2(queue, input));
var head3 = () => fold(none(), isNone, (option2, input) => match2(option2, {
  onNone: () => some(input),
  onSome: () => option2
}));
var ignoreLeftover = (self) => new SinkImpl(drain(toChannel(self)));
var last2 = () => foldLeftChunks(none(), (s, input) => orElse(last(input), () => s));
var leftover = (chunk2) => new SinkImpl(suspend5(() => write(chunk2)));
var map10 = dual(2, (self, f) => {
  return new SinkImpl(pipe(toChannel(self), map9(f)));
});
var mapEffect2 = dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapEffect(f))));
var mapError5 = dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapError4(f))));
var mapLeftover = dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapOut(map2(f)))));
var never3 = fromEffect4(never);
var orElse6 = dual(2, (self, that) => new SinkImpl(pipe(toChannel(self), orElse5(() => toChannel(that())))));
var provideContext2 = dual(2, (self, context9) => new SinkImpl(pipe(toChannel(self), provideContext(context9))));
var race2 = dual(2, (self, that) => pipe(self, raceBoth(that), map10(merge)));
var raceBoth = dual((args) => isSink(args[1]), (self, that, options) => raceWith2(self, {
  other: that,
  onSelfDone: (selfDone) => Done2(map7(selfDone, left)),
  onOtherDone: (thatDone) => Done2(map7(thatDone, right)),
  capacity: options?.capacity ?? 16
}));
var raceWith2 = dual(2, (self, options) => {
  function race5(scope4) {
    return gen(function* () {
      const pubsub = yield* bounded3(options?.capacity ?? 16);
      const subscription1 = yield* extend(subscribe2(pubsub), scope4);
      const subscription2 = yield* extend(subscribe2(pubsub), scope4);
      const reader = toPubSub(pubsub);
      const writer = fromQueue(subscription1).pipe(pipeTo(toChannel(self)), zipLeft2(fromEffect2(shutdown(subscription1))), mergeWith({
        other: fromQueue(subscription2).pipe(pipeTo(toChannel(options.other)), zipLeft2(fromEffect2(shutdown(subscription2)))),
        onSelfDone: options.onSelfDone,
        onOtherDone: options.onOtherDone
      }));
      const racedChannel = mergeWith(reader, {
        other: writer,
        onSelfDone: () => Await(identity),
        onOtherDone: (exit2) => Done2(exit2)
      });
      return new SinkImpl(racedChannel);
    });
  }
  return unwrapScopedWith2(race5);
});
var refineOrDie = dual(2, (self, pf) => pipe(self, refineOrDieWith(pf, identity)));
var refineOrDieWith = dual(3, (self, pf, f) => {
  const newChannel = pipe(self, toChannel, catchAll4((error) => match2(pf(error), {
    onNone: () => failCauseSync4(() => die2(f(error))),
    onSome: fail7
  })));
  return new SinkImpl(newChannel);
});
var some2 = (predicate) => fold(false, (bool) => !bool, (acc, input) => acc || predicate(input));
var splitWhere2 = dual(2, (self, f) => {
  const newChannel = pipe(fromEffect2(make5(empty())), flatMap6((ref) => pipe(splitWhereSplitter(false, ref, f), pipeToOrFail(toChannel(self)), collectElements, flatMap6(([leftovers, z]) => pipe(fromEffect2(get3(ref)), flatMap6((leftover2) => pipe(write(pipe(leftover2, appendAll(flatten(leftovers)))), zipRight4(succeed7(z)))))))));
  return new SinkImpl(newChannel);
});
var splitWhereSplitter = (written, leftovers, f) => readWithCause({
  onInput: (input) => {
    if (isEmpty(input)) {
      return splitWhereSplitter(written, leftovers, f);
    }
    if (written) {
      const index2 = indexWhere(input, f);
      if (index2 === -1) {
        return zipRight4(write(input), splitWhereSplitter(true, leftovers, f));
      }
      const [left3, right3] = splitAt(input, index2);
      return zipRight4(write(left3), fromEffect2(set3(leftovers, right3)));
    }
    const index = indexWhere(input, f, 1);
    if (index === -1) {
      return zipRight4(write(input), splitWhereSplitter(true, leftovers, f));
    }
    const [left2, right2] = pipe(input, splitAt(Math.max(index, 1)));
    return zipRight4(write(left2), fromEffect2(set3(leftovers, right2)));
  },
  onFailure: failCause6,
  onDone: succeed7
});
var indexWhere = (self, predicate, from = 0) => {
  const iterator = self[Symbol.iterator]();
  let index = 0;
  let result = -1;
  let next;
  while (result < 0 && (next = iterator.next()) && !next.done) {
    const a = next.value;
    if (index >= from && predicate(a)) {
      result = index;
    }
    index = index + 1;
  }
  return result;
};
var succeed8 = (a) => new SinkImpl(succeed7(a));
var sum = foldLeftChunks(0, (acc, chunk2) => acc + reduce(chunk2, 0, (s, a) => s + a));
var summarized = dual(3, (self, summary, f) => {
  const newChannel = pipe(fromEffect2(summary), flatMap6((start) => pipe(self, toChannel, flatMap6((done4) => pipe(fromEffect2(summary), map9((end4) => [done4, f(start, end4)]))))));
  return new SinkImpl(newChannel);
});
var sync6 = (evaluate2) => new SinkImpl(sync5(evaluate2));
var take3 = (n) => pipe(foldChunks(empty(), (chunk2) => chunk2.length < n, (acc, chunk2) => pipe(acc, appendAll(chunk2))), flatMap7((acc) => {
  const [taken, leftover2] = pipe(acc, splitAt(n));
  return new SinkImpl(pipe(write(leftover2), zipRight4(succeedNow(taken))));
}));
var toChannel = (self) => isEffect2(self) ? toChannel(fromEffect4(self)) : self.channel;
var unwrap2 = (effect4) => new SinkImpl(unwrap(pipe(effect4, map7((sink) => toChannel(sink)))));
var unwrapScoped4 = (effect4) => new SinkImpl(unwrapScoped3(effect4.pipe(map7((sink) => toChannel(sink)))));
var unwrapScopedWith2 = (f) => new SinkImpl(unwrapScopedWith((scope4) => f(scope4).pipe(map7((sink) => toChannel(sink)))));
var withDuration = (self) => pipe(self, summarized(currentTimeMillis, (start, end4) => millis(end4 - start)));
var zip4 = dual((args) => isSink(args[1]), (self, that, options) => zipWith5(self, that, (z, z2) => [z, z2], options));
var zipLeft3 = dual((args) => isSink(args[1]), (self, that, options) => zipWith5(self, that, (z, _) => z, options));
var zipRight5 = dual((args) => isSink(args[1]), (self, that, options) => zipWith5(self, that, (_, z2) => z2, options));
var zipWith5 = dual((args) => isSink(args[1]), (self, that, f, options) => options?.concurrent ? raceWith2(self, {
  other: that,
  onSelfDone: match3({
    onFailure: (cause) => Done2(failCause4(cause)),
    onSuccess: (leftZ) => Await(match3({
      onFailure: failCause4,
      onSuccess: (rightZ) => succeed5(f(leftZ, rightZ))
    }))
  }),
  onOtherDone: match3({
    onFailure: (cause) => Done2(failCause4(cause)),
    onSuccess: (rightZ) => Await(match3({
      onFailure: failCause4,
      onSuccess: (leftZ) => succeed5(f(leftZ, rightZ))
    }))
  })
}) : flatMap7(self, (z) => map10(that, (z2) => f(z, z2))));
var channelToSink = (self) => new SinkImpl(self);
var count = foldLeftChunks(0, (acc, chunk2) => acc + chunk2.length);
var mkString = suspend6(() => {
  const strings = [];
  return pipe(foldLeftChunks(void 0, (_, elems) => map2(elems, (elem) => {
    strings.push(String(elem));
  })), map10(() => strings.join("")));
});
var timed = pipe(withDuration(drain2), map10((tuple) => tuple[1]));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/versioned.js
var Versioned = class {
  value;
  constructor(value) {
    this.value = value;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/entry.js
var make14 = (ref, isNew) => ({
  ref,
  isNew,
  isChanged: false,
  expected: ref.versioned,
  newValue: ref.versioned.value
});
var unsafeGet4 = (self) => {
  return self.newValue;
};
var unsafeSet = (self, value) => {
  self.isChanged = true;
  self.newValue = value;
};
var commit = (self) => {
  self.ref.versioned = new Versioned(self.newValue);
};
var copy = (self) => ({
  ref: self.ref,
  isNew: self.isNew,
  isChanged: self.isChanged,
  expected: self.expected,
  newValue: self.newValue
});
var isInvalid = (self) => {
  return self.ref.versioned !== self.expected;
};
var isChanged = (self) => {
  return self.isChanged;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/journal.js
var JournalAnalysisInvalid = "Invalid";
var JournalAnalysisReadWrite = "ReadWrite";
var JournalAnalysisReadOnly = "ReadOnly";
var commitJournal = (journal) => {
  for (const entry of journal) {
    commit(entry[1]);
  }
};
var analyzeJournal = (journal) => {
  let val = JournalAnalysisReadOnly;
  for (const [, entry] of journal) {
    val = isInvalid(entry) ? JournalAnalysisInvalid : isChanged(entry) ? JournalAnalysisReadWrite : val;
    if (val === JournalAnalysisInvalid) {
      return val;
    }
  }
  return val;
};
var prepareResetJournal = (journal) => {
  const saved = /* @__PURE__ */ new Map();
  for (const entry of journal) {
    saved.set(entry[0], copy(entry[1]));
  }
  return () => {
    journal.clear();
    for (const entry of saved) {
      journal.set(entry[0], entry[1]);
    }
  };
};
var collectTodos = (journal) => {
  const allTodos = /* @__PURE__ */ new Map();
  for (const [, entry] of journal) {
    for (const todo of entry.ref.todos) {
      allTodos.set(todo[0], todo[1]);
    }
    entry.ref.todos = /* @__PURE__ */ new Map();
  }
  return allTodos;
};
var execTodos = (todos) => {
  const todosSorted = Array.from(todos.entries()).sort((x, y) => x[0] - y[0]);
  for (const [_, todo] of todosSorted) {
    todo();
  }
};
var addTodo = (txnId, journal, todoEffect) => {
  let added = false;
  for (const [, entry] of journal) {
    if (!entry.ref.todos.has(txnId)) {
      entry.ref.todos.set(txnId, todoEffect);
      added = true;
    }
  }
  return added;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/stm.js
var OP_WITH_STM_RUNTIME = "WithSTMRuntime";
var OP_ON_FAILURE = "OnFailure";
var OP_ON_RETRY = "OnRetry";
var OP_ON_SUCCESS = "OnSuccess";
var OP_PROVIDE2 = "Provide";
var OP_SYNC = "Sync";
var OP_SUCCEED2 = "Succeed";
var OP_RETRY = "Retry";
var OP_FAIL2 = "Fail";
var OP_DIE = "Die";
var OP_INTERRUPT = "Interrupt";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/tExit.js
var OP_FAIL3 = "Fail";
var OP_DIE2 = "Die";
var OP_INTERRUPT2 = "Interrupt";
var OP_SUCCEED3 = "Succeed";
var OP_RETRY2 = "Retry";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/tryCommit.js
var OP_DONE3 = "Done";
var OP_SUSPEND2 = "Suspend";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/stmState.js
var OP_DONE4 = "Done";
var OP_INTERRUPTED = "Interrupted";
var OP_RUNNING = "Running";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/stmState.js
var STMStateSymbolKey = "effect/STM/State";
var STMStateTypeId = Symbol.for(STMStateSymbolKey);
var isSTMState = (u) => hasProperty(u, STMStateTypeId);
var isRunning = (self) => {
  return self._tag === OP_RUNNING;
};
var isDone2 = (self) => {
  return self._tag === OP_DONE4;
};
var done = (exit2) => {
  return {
    [STMStateTypeId]: STMStateTypeId,
    _tag: OP_DONE4,
    exit: exit2,
    [symbol]() {
      return pipe(hash(STMStateSymbolKey), combine(hash(OP_DONE4)), combine(hash(exit2)), cached(this));
    },
    [symbol2](that) {
      return isSTMState(that) && that._tag === OP_DONE4 && equals(exit2, that.exit);
    }
  };
};
var interruptedHash = pipe(hash(STMStateSymbolKey), combine(hash(OP_INTERRUPTED)), combine(hash("interrupted")));
var interrupted = {
  [STMStateTypeId]: STMStateTypeId,
  _tag: OP_INTERRUPTED,
  [symbol]() {
    return interruptedHash;
  },
  [symbol2](that) {
    return isSTMState(that) && that._tag === OP_INTERRUPTED;
  }
};
var runningHash = pipe(hash(STMStateSymbolKey), combine(hash(OP_RUNNING)), combine(hash("running")));
var running = {
  [STMStateTypeId]: STMStateTypeId,
  _tag: OP_RUNNING,
  [symbol]() {
    return runningHash;
  },
  [symbol2](that) {
    return isSTMState(that) && that._tag === OP_RUNNING;
  }
};
var fromTExit = (tExit) => {
  switch (tExit._tag) {
    case OP_FAIL3: {
      return done(fail(tExit.error));
    }
    case OP_DIE2: {
      return done(die(tExit.defect));
    }
    case OP_INTERRUPT2: {
      return done(interrupt2(tExit.fiberId));
    }
    case OP_SUCCEED3: {
      return done(succeed2(tExit.value));
    }
    case OP_RETRY2: {
      throw new Error("BUG: STM.STMState.fromTExit - please report an issue at https://github.com/Effect-TS/effect/issues");
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tExit.js
var TExitSymbolKey = "effect/TExit";
var TExitTypeId = Symbol.for(TExitSymbolKey);
var variance2 = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _E: (_) => _
};
var isExit2 = (u) => hasProperty(u, TExitTypeId);
var isSuccess2 = (self) => {
  return self._tag === OP_SUCCEED3;
};
var isRetry = (self) => {
  return self._tag === OP_RETRY2;
};
var fail9 = (error) => ({
  [TExitTypeId]: variance2,
  _tag: OP_FAIL3,
  error,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_FAIL3)), combine(hash(error)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_FAIL3 && equals(error, that.error);
  }
});
var die7 = (defect) => ({
  [TExitTypeId]: variance2,
  _tag: OP_DIE2,
  defect,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_DIE2)), combine(hash(defect)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_DIE2 && equals(defect, that.defect);
  }
});
var interrupt6 = (fiberId3) => ({
  [TExitTypeId]: variance2,
  _tag: OP_INTERRUPT2,
  fiberId: fiberId3,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_INTERRUPT2)), combine(hash(fiberId3)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_INTERRUPT2 && equals(fiberId3, that.fiberId);
  }
});
var succeed9 = (value) => ({
  [TExitTypeId]: variance2,
  _tag: OP_SUCCEED3,
  value,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_SUCCEED3)), combine(hash(value)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_SUCCEED3 && equals(value, that.value);
  }
});
var retryHash = pipe(hash(TExitSymbolKey), combine(hash(OP_RETRY2)), combine(hash("retry")));
var retry3 = {
  [TExitTypeId]: variance2,
  _tag: OP_RETRY2,
  [symbol]() {
    return retryHash;
  },
  [symbol2](that) {
    return isExit2(that) && isRetry(that);
  }
};
var void_4 = succeed9(void 0);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tryCommit.js
var done2 = (exit2) => {
  return {
    _tag: OP_DONE3,
    exit: exit2
  };
};
var suspend7 = (journal) => {
  return {
    _tag: OP_SUSPEND2,
    journal
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/txnId.js
var txnCounter = {
  ref: 0
};
var make15 = () => {
  const newId = txnCounter.ref + 1;
  txnCounter.ref = newId;
  return newId;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/core.js
var STMSymbolKey = "effect/STM";
var STMTypeId = Symbol.for(STMSymbolKey);
var stmVariance = {
  /* c8 ignore next */
  _R: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var STMPrimitive = class {
  effect_instruction_i0;
  _op = OP_COMMIT;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  [EffectTypeId];
  [StreamTypeId];
  [SinkTypeId];
  [ChannelTypeId];
  get [STMTypeId]() {
    return stmVariance;
  }
  constructor(effect_instruction_i0) {
    this.effect_instruction_i0 = effect_instruction_i0;
    this[EffectTypeId] = effectVariance;
    this[StreamTypeId] = stmVariance;
    this[SinkTypeId] = stmVariance;
    this[ChannelTypeId] = stmVariance;
  }
  [symbol2](that) {
    return this === that;
  }
  [symbol]() {
    return cached(this, random(this));
  }
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  }
  commit() {
    return unsafeAtomically(this, constVoid, constVoid);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isSTM = (u) => hasProperty(u, STMTypeId);
var commit2 = (self) => unsafeAtomically(self, constVoid, constVoid);
var unsafeAtomically = (self, onDone3, onInterrupt2) => withFiberRuntime((state) => {
  const fiberId3 = state.id();
  const env = state.getFiberRef(currentContext2);
  const scheduler = state.getFiberRef(currentScheduler2);
  const priority = state.getFiberRef(currentSchedulingPriority2);
  const commitResult = tryCommitSync(fiberId3, self, env, scheduler, priority);
  switch (commitResult._tag) {
    case OP_DONE3: {
      onDone3(commitResult.exit);
      return commitResult.exit;
    }
    case OP_SUSPEND2: {
      const txnId = make15();
      const state2 = {
        value: running
      };
      const effect4 = async((k) => tryCommitAsync(fiberId3, self, txnId, state2, env, scheduler, priority, k));
      return uninterruptibleMask2((restore) => pipe(restore(effect4), catchAllCause2((cause) => {
        let currentState = state2.value;
        if (isRunning(currentState)) {
          state2.value = interrupted;
        }
        currentState = state2.value;
        if (isDone2(currentState)) {
          onDone3(currentState.exit);
          return currentState.exit;
        }
        onInterrupt2();
        return failCause4(cause);
      })));
    }
  }
});
var tryCommit = (fiberId3, stm, state, env, scheduler, priority) => {
  const journal = /* @__PURE__ */ new Map();
  const tExit = new STMDriver(stm, journal, fiberId3, env).run();
  const analysis = analyzeJournal(journal);
  if (analysis === JournalAnalysisReadWrite) {
    commitJournal(journal);
  } else if (analysis === JournalAnalysisInvalid) {
    throw new Error("BUG: STM.TryCommit.tryCommit - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  switch (tExit._tag) {
    case OP_SUCCEED3: {
      state.value = fromTExit(tExit);
      return completeTodos(succeed2(tExit.value), journal, scheduler, priority);
    }
    case OP_FAIL3: {
      state.value = fromTExit(tExit);
      const cause = fail2(tExit.error);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_DIE2: {
      state.value = fromTExit(tExit);
      const cause = die2(tExit.defect);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_INTERRUPT2: {
      state.value = fromTExit(tExit);
      const cause = interrupt3(fiberId3);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_RETRY2: {
      return suspend7(journal);
    }
  }
};
var tryCommitSync = (fiberId3, stm, env, scheduler, priority) => {
  const journal = /* @__PURE__ */ new Map();
  const tExit = new STMDriver(stm, journal, fiberId3, env).run();
  const analysis = analyzeJournal(journal);
  if (analysis === JournalAnalysisReadWrite && isSuccess2(tExit)) {
    commitJournal(journal);
  } else if (analysis === JournalAnalysisInvalid) {
    throw new Error("BUG: STM.TryCommit.tryCommitSync - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  switch (tExit._tag) {
    case OP_SUCCEED3: {
      return completeTodos(succeed2(tExit.value), journal, scheduler, priority);
    }
    case OP_FAIL3: {
      const cause = fail2(tExit.error);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_DIE2: {
      const cause = die2(tExit.defect);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_INTERRUPT2: {
      const cause = interrupt3(fiberId3);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_RETRY2: {
      return suspend7(journal);
    }
  }
};
var tryCommitAsync = (fiberId3, self, txnId, state, context9, scheduler, priority, k) => {
  if (isRunning(state.value)) {
    const result = tryCommit(fiberId3, self, state, context9, scheduler, priority);
    switch (result._tag) {
      case OP_DONE3: {
        completeTryCommit(result.exit, k);
        break;
      }
      case OP_SUSPEND2: {
        addTodo(txnId, result.journal, () => tryCommitAsync(fiberId3, self, txnId, state, context9, scheduler, priority, k));
        break;
      }
    }
  }
};
var completeTodos = (exit2, journal, scheduler, priority) => {
  const todos = collectTodos(journal);
  if (todos.size > 0) {
    scheduler.scheduleTask(() => execTodos(todos), priority);
  }
  return done2(exit2);
};
var completeTryCommit = (exit2, k) => {
  k(exit2);
};
var context6 = () => effect3((_, __, env) => env);
var contextWith4 = (f) => map11(context6(), f);
var contextWithSTM = (f) => flatMap8(context6(), f);
var STMDriver = class {
  self;
  journal;
  fiberId;
  contStack = [];
  env;
  constructor(self, journal, fiberId3, r0) {
    this.self = self;
    this.journal = journal;
    this.fiberId = fiberId3;
    this.env = r0;
  }
  getEnv() {
    return this.env;
  }
  pushStack(cont) {
    this.contStack.push(cont);
  }
  popStack() {
    return this.contStack.pop();
  }
  nextSuccess() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_SUCCESS) {
      current2 = this.popStack();
    }
    return current2;
  }
  nextFailure() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_FAILURE) {
      current2 = this.popStack();
    }
    return current2;
  }
  nextRetry() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_RETRY) {
      current2 = this.popStack();
    }
    return current2;
  }
  run() {
    let curr = this.self;
    let exit2 = void 0;
    while (exit2 === void 0 && curr !== void 0) {
      try {
        const current2 = curr;
        if (current2) {
          switch (current2._op) {
            case "Tag": {
              curr = effect3((_, __, env) => unsafeGet2(env, current2));
              break;
            }
            case "Left": {
              curr = fail10(current2.left);
              break;
            }
            case "None": {
              curr = fail10(new NoSuchElementException());
              break;
            }
            case "Right": {
              curr = succeed10(current2.right);
              break;
            }
            case "Some": {
              curr = succeed10(current2.value);
              break;
            }
            case "Commit": {
              switch (current2.effect_instruction_i0) {
                case OP_DIE: {
                  exit2 = die7(internalCall(() => current2.effect_instruction_i1()));
                  break;
                }
                case OP_FAIL2: {
                  const cont = this.nextFailure();
                  if (cont === void 0) {
                    exit2 = fail9(internalCall(() => current2.effect_instruction_i1()));
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(internalCall(() => current2.effect_instruction_i1())));
                  }
                  break;
                }
                case OP_RETRY: {
                  const cont = this.nextRetry();
                  if (cont === void 0) {
                    exit2 = retry3;
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2());
                  }
                  break;
                }
                case OP_INTERRUPT: {
                  exit2 = interrupt6(this.fiberId);
                  break;
                }
                case OP_WITH_STM_RUNTIME: {
                  curr = internalCall(() => current2.effect_instruction_i1(this));
                  break;
                }
                case OP_ON_SUCCESS:
                case OP_ON_FAILURE:
                case OP_ON_RETRY: {
                  this.pushStack(current2);
                  curr = current2.effect_instruction_i1;
                  break;
                }
                case OP_PROVIDE2: {
                  const env = this.env;
                  this.env = internalCall(() => current2.effect_instruction_i2(env));
                  curr = pipe(current2.effect_instruction_i1, ensuring5(sync7(() => this.env = env)));
                  break;
                }
                case OP_SUCCEED2: {
                  const value = current2.effect_instruction_i1;
                  const cont = this.nextSuccess();
                  if (cont === void 0) {
                    exit2 = succeed9(value);
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(value));
                  }
                  break;
                }
                case OP_SYNC: {
                  const value = internalCall(() => current2.effect_instruction_i1());
                  const cont = this.nextSuccess();
                  if (cont === void 0) {
                    exit2 = succeed9(value);
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(value));
                  }
                  break;
                }
              }
              break;
            }
          }
        }
      } catch (e) {
        curr = die8(e);
      }
    }
    return exit2;
  }
};
var catchAll5 = dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_ON_FAILURE);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var mapInputContext2 = dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_PROVIDE2);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var die8 = (defect) => dieSync5(() => defect);
var dieMessage3 = (message) => dieSync5(() => new RuntimeException(message));
var dieSync5 = (evaluate2) => {
  const stm = new STMPrimitive(OP_DIE);
  stm.effect_instruction_i1 = evaluate2;
  return stm;
};
var effect3 = (f) => withSTMRuntime((_) => succeed10(f(_.journal, _.fiberId, _.getEnv())));
var ensuring5 = dual(2, (self, finalizer3) => matchSTM(self, {
  onFailure: (e) => zipRight6(finalizer3, fail10(e)),
  onSuccess: (a) => zipRight6(finalizer3, succeed10(a))
}));
var fail10 = (error) => failSync6(() => error);
var failSync6 = (evaluate2) => {
  const stm = new STMPrimitive(OP_FAIL2);
  stm.effect_instruction_i1 = evaluate2;
  return stm;
};
var flatMap8 = dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_ON_SUCCESS);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var matchSTM = dual(2, (self, {
  onFailure,
  onSuccess
}) => pipe(self, map11(right), catchAll5((e) => pipe(onFailure(e), map11(left))), flatMap8((either4) => {
  switch (either4._tag) {
    case "Left": {
      return succeed10(either4.left);
    }
    case "Right": {
      return onSuccess(either4.right);
    }
  }
})));
var withSTMRuntime = (f) => {
  const stm = new STMPrimitive(OP_WITH_STM_RUNTIME);
  stm.effect_instruction_i1 = f;
  return stm;
};
var interrupt7 = withSTMRuntime((_) => {
  const stm = new STMPrimitive(OP_INTERRUPT);
  stm.effect_instruction_i1 = _.fiberId;
  return stm;
});
var interruptAs2 = (fiberId3) => {
  const stm = new STMPrimitive(OP_INTERRUPT);
  stm.effect_instruction_i1 = fiberId3;
  return stm;
};
var map11 = dual(2, (self, f) => pipe(self, flatMap8((a) => sync7(() => f(a)))));
var orTry = dual(2, (self, that) => {
  const stm = new STMPrimitive(OP_ON_RETRY);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = that;
  return stm;
});
var retry4 = new STMPrimitive(OP_RETRY);
var succeed10 = (value) => {
  const stm = new STMPrimitive(OP_SUCCEED2);
  stm.effect_instruction_i1 = value;
  return stm;
};
var sync7 = (evaluate2) => {
  const stm = new STMPrimitive(OP_SYNC);
  stm.effect_instruction_i1 = evaluate2;
  return stm;
};
var zip5 = dual(2, (self, that) => pipe(self, zipWith6(that, (a, a1) => [a, a1])));
var zipLeft4 = dual(2, (self, that) => pipe(self, flatMap8((a) => pipe(that, map11(() => a)))));
var zipRight6 = dual(2, (self, that) => pipe(self, flatMap8(() => that)));
var zipWith6 = dual(3, (self, that, f) => pipe(self, flatMap8((a) => pipe(that, map11((b) => f(a, b))))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/strategy.js
var OP_BACKPRESSURE_STRATEGY = "BackPressure";
var OP_DROPPING_STRATEGY = "Dropping";
var OP_SLIDING_STRATEGY = "Sliding";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/stm.js
var acquireUseRelease3 = dual(3, (acquire, use, release) => uninterruptibleMask2((restore) => {
  let state = running;
  return pipe(restore(unsafeAtomically(acquire, (exit2) => {
    state = done(exit2);
  }, () => {
    state = interrupted;
  })), matchCauseEffect({
    onFailure: (cause) => {
      if (isDone2(state) && isSuccess(state.exit)) {
        return pipe(release(state.exit.value), matchCauseEffect({
          onFailure: (cause2) => failCause4(parallel(cause, cause2)),
          onSuccess: () => failCause4(cause)
        }));
      }
      return failCause4(cause);
    },
    onSuccess: (a) => pipe(restore(use(a)), matchCauseEffect({
      onFailure: (cause) => pipe(release(a), matchCauseEffect({
        onFailure: (cause2) => failCause4(parallel(cause, cause2)),
        onSuccess: () => failCause4(cause)
      })),
      onSuccess: (a2) => pipe(release(a), as2(a2))
    }))
  }));
}));
var as5 = dual(2, (self, value) => pipe(self, map11(() => value)));
var asSome2 = (self) => pipe(self, map11(some));
var asSomeError = (self) => pipe(self, mapError6(some));
var asVoid4 = (self) => pipe(self, map11(constVoid));
var attempt = (evaluate2) => suspend8(() => {
  try {
    return succeed10(evaluate2());
  } catch (defect) {
    return fail10(defect);
  }
});
var bind2 = dual(3, (self, tag, f) => flatMap8(self, (k) => map11(f(k), (a) => ({
  ...k,
  [tag]: a
}))));
var bindTo3 = dual(2, (self, tag) => map11(self, (a) => ({
  [tag]: a
})));
var let_2 = dual(3, (self, tag, f) => map11(self, (k) => ({
  ...k,
  [tag]: f(k)
})));
var catchSome = dual(2, (self, pf) => catchAll5(self, (e) => getOrElse(pf(e), () => fail10(e))));
var catchTag = dual(3, (self, k, f) => catchAll5(self, (e) => {
  if ("_tag" in e && e["_tag"] === k) {
    return f(e);
  }
  return fail10(e);
}));
var catchTags = dual(2, (self, cases) => catchAll5(self, (e) => {
  const keys = Object.keys(cases);
  if ("_tag" in e && keys.includes(e["_tag"])) {
    return cases[e["_tag"]](e);
  }
  return fail10(e);
}));
var check = (predicate) => suspend8(() => predicate() ? void_5 : retry4);
var collect2 = dual(2, (self, pf) => collectSTM(self, (a) => map(pf(a), succeed10)));
var collectSTM = dual(2, (self, pf) => matchSTM(self, {
  onFailure: fail10,
  onSuccess: (a) => {
    const option2 = pf(a);
    return isSome(option2) ? option2.value : retry4;
  }
}));
var commitEither = (self) => flatten3(commit2(either(self)));
var cond = (predicate, error, result) => {
  return suspend8(() => predicate() ? sync7(result) : failSync6(error));
};
var either = (self) => match16(self, {
  onFailure: left,
  onSuccess: right
});
var eventually = (self) => matchSTM(self, {
  onFailure: () => eventually(self),
  onSuccess: succeed10
});
var every3 = dual(2, (iterable, predicate) => flatMap8(sync7(() => iterable[Symbol.iterator]()), (iterator) => {
  const loop2 = suspend8(() => {
    const next = iterator.next();
    if (next.done) {
      return succeed10(true);
    }
    return pipe(predicate(next.value), flatMap8((bool) => bool ? loop2 : succeed10(bool)));
  });
  return loop2;
}));
var exists = dual(2, (iterable, predicate) => flatMap8(sync7(() => iterable[Symbol.iterator]()), (iterator) => {
  const loop2 = suspend8(() => {
    const next = iterator.next();
    if (next.done) {
      return succeed10(false);
    }
    return flatMap8(predicate(next.value), (bool) => bool ? succeed10(bool) : loop2);
  });
  return loop2;
}));
var fiberId2 = effect3((_, fiberId3) => fiberId3);
var filter3 = dual(2, (iterable, predicate) => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith6(predicate(curr), (as8, p) => {
  if (p) {
    as8.push(curr);
    return as8;
  }
  return as8;
})), succeed10([])));
var filterNot = dual(2, (iterable, predicate) => filter3(iterable, (a) => negate(predicate(a))));
var filterOrDie = dual(3, (self, predicate, defect) => filterOrElse(self, predicate, () => dieSync5(defect)));
var filterOrDieMessage = dual(3, (self, predicate, message) => filterOrElse(self, predicate, () => dieMessage3(message)));
var filterOrElse = dual(3, (self, predicate, orElse10) => flatMap8(self, (a) => predicate(a) ? succeed10(a) : orElse10(a)));
var filterOrFail = dual(3, (self, predicate, orFailWith) => filterOrElse(self, predicate, (a) => failSync6(() => orFailWith(a))));
var flatten6 = (self) => flatMap8(self, identity);
var flip = (self) => matchSTM(self, {
  onFailure: succeed10,
  onSuccess: fail10
});
var flipWith = dual(2, (self, f) => flip(f(flip(self))));
var match16 = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: (e) => succeed10(onFailure(e)),
  onSuccess: (a) => succeed10(onSuccess(a))
}));
var forEach4 = dual((args) => isIterable(args[0]), (iterable, f, options) => {
  if (options?.discard) {
    return pipe(sync7(() => iterable[Symbol.iterator]()), flatMap8((iterator) => {
      const loop2 = suspend8(() => {
        const next = iterator.next();
        if (next.done) {
          return void_5;
        }
        return pipe(f(next.value), flatMap8(() => loop2));
      });
      return loop2;
    }));
  }
  return suspend8(() => fromIterable(iterable).reduce((acc, curr) => zipWith6(acc, f(curr), (array, elem) => {
    array.push(elem);
    return array;
  }), succeed10([])));
});
var fromEither2 = (either4) => {
  switch (either4._tag) {
    case "Left": {
      return fail10(either4.left);
    }
    case "Right": {
      return succeed10(either4.right);
    }
  }
};
var fromOption2 = (option2) => match2(option2, {
  onNone: () => fail10(none()),
  onSome: succeed10
});
var gen2 = (...args) => suspend8(() => {
  const f = args.length === 1 ? args[0] : args[1].bind(args[0]);
  const iterator = f(pipe);
  const state = iterator.next();
  const run4 = (state2) => state2.done ? succeed10(state2.value) : flatMap8(yieldWrapGet(state2.value), (val) => run4(iterator.next(val)));
  return run4(state);
});
var head4 = (self) => pipe(self, matchSTM({
  onFailure: (e) => fail10(some(e)),
  onSuccess: (a) => {
    const i = a[Symbol.iterator]();
    const res = i.next();
    if (res.done) {
      return fail10(none());
    } else {
      return succeed10(res.value);
    }
  }
}));
var if_ = dual((args) => typeof args[0] === "boolean" || isSTM(args[0]), (self, {
  onFalse,
  onTrue
}) => {
  if (typeof self === "boolean") {
    return self ? onTrue : onFalse;
  }
  return flatMap8(self, (bool) => bool ? onTrue : onFalse);
});
var ignore2 = (self) => match16(self, {
  onFailure: () => void_5,
  onSuccess: () => void_5
});
var isFailure2 = (self) => match16(self, {
  onFailure: constTrue,
  onSuccess: constFalse
});
var isSuccess3 = (self) => match16(self, {
  onFailure: constFalse,
  onSuccess: constTrue
});
var iterate = (initial, options) => iterateLoop(initial, options.while, options.body);
var iterateLoop = (initial, cont, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap8((z) => iterateLoop(z, cont, body)));
  }
  return succeed10(initial);
};
var loop = (initial, options) => options.discard ? loopDiscardLoop(initial, options.while, options.step, options.body) : map11(loopLoop(initial, options.while, options.step, options.body), (a) => Array.from(a));
var loopLoop = (initial, cont, inc, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap8((a) => pipe(loopLoop(inc(initial), cont, inc, body), map11(append(a)))));
  }
  return succeed10(empty());
};
var loopDiscardLoop = (initial, cont, inc, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap8(() => loopDiscardLoop(inc(initial), cont, inc, body)));
  }
  return void_5;
};
var mapAttempt = dual(2, (self, f) => matchSTM(self, {
  onFailure: (e) => fail10(e),
  onSuccess: (a) => attempt(() => f(a))
}));
var mapBoth3 = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: (e) => fail10(onFailure(e)),
  onSuccess: (a) => succeed10(onSuccess(a))
}));
var mapError6 = dual(2, (self, f) => matchSTM(self, {
  onFailure: (e) => fail10(f(e)),
  onSuccess: succeed10
}));
var merge5 = (self) => matchSTM(self, {
  onFailure: (e) => succeed10(e),
  onSuccess: succeed10
});
var mergeAll4 = dual(3, (iterable, zero2, f) => suspend8(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith6(curr, f)), succeed10(zero2))));
var negate = (self) => pipe(self, map11((b) => !b));
var none2 = (self) => matchSTM(self, {
  onFailure: (e) => fail10(some(e)),
  onSuccess: match2({
    onNone: () => void_5,
    onSome: () => fail10(none())
  })
});
var option = (self) => match16(self, {
  onFailure: () => none(),
  onSuccess: some
});
var orDie5 = (self) => pipe(self, orDieWith2(identity));
var orDieWith2 = dual(2, (self, f) => pipe(self, mapError6(f), catchAll5(die8)));
var orElse7 = dual(2, (self, that) => flatMap8(effect3((journal) => prepareResetJournal(journal)), (reset2) => pipe(orTry(self, () => flatMap8(sync7(reset2), that)), catchAll5(() => flatMap8(sync7(reset2), that)))));
var orElseEither = dual(2, (self, that) => orElse7(map11(self, left), () => map11(that(), right)));
var orElseFail = dual(2, (self, error) => orElse7(self, () => failSync6(error)));
var orElseOptional = dual(2, (self, that) => catchAll5(self, match2({
  onNone: that,
  onSome: (e) => fail10(some(e))
})));
var orElseSucceed = dual(2, (self, value) => orElse7(self, () => sync7(value)));
var provideContext3 = dual(2, (self, env) => mapInputContext2(self, (_) => env));
var provideSomeContext = dual(2, (self, context9) => mapInputContext2(self, (parent) => merge2(parent, context9)));
var provideService4 = dual(3, (self, tag, resource) => provideServiceSTM(self, tag, succeed10(resource)));
var provideServiceSTM = dual(3, (self, tag, stm) => contextWithSTM((env) => flatMap8(stm, (service3) => provideContext3(self, add(env, tag, service3)))));
var reduce3 = dual(3, (iterable, zero2, f) => suspend8(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, flatMap8((s) => f(s, curr))), succeed10(zero2))));
var reduceAll = dual(3, (iterable, initial, f) => suspend8(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith6(curr, f)), initial)));
var reduceRight2 = dual(3, (iterable, zero2, f) => suspend8(() => Array.from(iterable).reduceRight((acc, curr) => pipe(acc, flatMap8((s) => f(s, curr))), succeed10(zero2))));
var refineOrDie2 = dual(2, (self, pf) => refineOrDieWith2(self, pf, identity));
var refineOrDieWith2 = dual(3, (self, pf, f) => catchAll5(self, (e) => match2(pf(e), {
  onNone: () => die8(f(e)),
  onSome: fail10
})));
var reject = dual(2, (self, pf) => rejectSTM(self, (a) => map(pf(a), fail10)));
var rejectSTM = dual(2, (self, pf) => flatMap8(self, (a) => match2(pf(a), {
  onNone: () => succeed10(a),
  onSome: flatMap8(fail10)
})));
var repeatUntil = dual(2, (self, predicate) => repeatUntilLoop(self, predicate));
var repeatUntilLoop = (self, predicate) => flatMap8(self, (a) => predicate(a) ? succeed10(a) : repeatUntilLoop(self, predicate));
var repeatWhile = dual(2, (self, predicate) => repeatWhileLoop(self, predicate));
var repeatWhileLoop = (self, predicate) => flatMap8(self, (a) => predicate(a) ? repeatWhileLoop(self, predicate) : succeed10(a));
var replicate = dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateSTM = dual(2, (self, n) => all4(replicate(self, n)));
var replicateSTMDiscard = dual(2, (self, n) => all4(replicate(self, n), {
  discard: true
}));
var retryUntil = dual(2, (self, predicate) => matchSTM(self, {
  onFailure: fail10,
  onSuccess: (a) => predicate(a) ? succeed10(a) : retry4
}));
var retryWhile = dual(2, (self, predicate) => matchSTM(self, {
  onFailure: fail10,
  onSuccess: (a) => !predicate(a) ? succeed10(a) : retry4
}));
var partition = dual(2, (elements, f) => pipe(forEach4(elements, (a) => either(f(a))), map11((as8) => partitionMap(as8, identity))));
var some3 = (self) => matchSTM(self, {
  onFailure: (e) => fail10(some(e)),
  onSuccess: match2({
    onNone: () => fail10(none()),
    onSome: succeed10
  })
});
var all4 = (input, options) => {
  if (Symbol.iterator in input) {
    return forEach4(input, identity, options);
  } else if (options?.discard) {
    return forEach4(Object.values(input), identity, options);
  }
  return map11(forEach4(Object.entries(input), ([_, e]) => map11(e, (a) => [_, a])), (values) => {
    const res = {};
    for (const [k, v] of values) {
      ;
      res[k] = v;
    }
    return res;
  });
};
var succeedNone2 = succeed10(none());
var succeedSome = (value) => succeed10(some(value));
var summarized2 = dual(3, (self, summary, f) => flatMap8(summary, (start) => flatMap8(self, (value) => map11(summary, (end4) => [f(start, end4), value]))));
var suspend8 = (evaluate2) => flatten6(sync7(evaluate2));
var tap5 = dual(2, (self, f) => flatMap8(self, (a) => as5(f(a), a)));
var tapBoth = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: (e) => pipe(onFailure(e), zipRight6(fail10(e))),
  onSuccess: (a) => pipe(onSuccess(a), as5(a))
}));
var tapError3 = dual(2, (self, f) => matchSTM(self, {
  onFailure: (e) => zipRight6(f(e), fail10(e)),
  onSuccess: succeed10
}));
var try_ = (arg) => {
  const evaluate2 = typeof arg === "function" ? arg : arg.try;
  return suspend8(() => {
    try {
      return succeed10(evaluate2());
    } catch (error) {
      return fail10("catch" in arg ? arg.catch(error) : error);
    }
  });
};
var void_5 = succeed10(void 0);
var unless = dual(2, (self, predicate) => suspend8(() => predicate() ? succeedNone2 : asSome2(self)));
var unlessSTM = dual(2, (self, predicate) => flatMap8(predicate, (bool) => bool ? succeedNone2 : asSome2(self)));
var unsome = (self) => matchSTM(self, {
  onFailure: match2({
    onNone: () => succeed10(none()),
    onSome: fail10
  }),
  onSuccess: (a) => succeed10(some(a))
});
var validateAll = dual(2, (elements, f) => flatMap8(partition(elements, f), ([errors, values]) => isNonEmptyArray(errors) ? fail10(errors) : succeed10(values)));
var validateFirst = dual(2, (elements, f) => flip(forEach4(elements, (a) => flip(f(a)))));
var when2 = dual(2, (self, predicate) => suspend8(() => predicate() ? asSome2(self) : succeedNone2));
var whenSTM = dual(2, (self, predicate) => flatMap8(predicate, (bool) => bool ? asSome2(self) : succeedNone2));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tRef.js
var TRefSymbolKey = "effect/TRef";
var TRefTypeId = Symbol.for(TRefSymbolKey);
var tRefVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var TRefImpl = class {
  [TRefTypeId] = tRefVariance;
  /** @internal */
  todos;
  /** @internal */
  versioned;
  constructor(value) {
    this.versioned = new Versioned(value);
    this.todos = /* @__PURE__ */ new Map();
  }
  modify(f) {
    return effect3((journal) => {
      const entry = getOrMakeEntry(this, journal);
      const [retValue, newValue] = f(unsafeGet4(entry));
      unsafeSet(entry, newValue);
      return retValue;
    });
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make16 = (value) => effect3((journal) => {
  const ref = new TRefImpl(value);
  journal.set(ref, make14(ref, true));
  return ref;
});
var get7 = (self) => self.modify((a) => [a, a]);
var set5 = dual(2, (self, value) => self.modify(() => [void 0, value]));
var getAndSet3 = dual(2, (self, value) => self.modify((a) => [a, value]));
var getAndUpdate2 = dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome2 = dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: () => [a, a],
  onSome: (b) => [a, b]
})));
var setAndGet = dual(2, (self, value) => self.modify(() => [value, value]));
var modify3 = dual(2, (self, f) => self.modify(f));
var modifySome2 = dual(3, (self, fallback, f) => self.modify((a) => match2(f(a), {
  onNone: () => [fallback, a],
  onSome: (b) => b
})));
var update3 = dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet2 = dual(2, (self, f) => self.modify((a) => {
  const b = f(a);
  return [b, b];
}));
var updateSome2 = dual(2, (self, f) => self.modify((a) => [void 0, match2(f(a), {
  onNone: () => a,
  onSome: (b) => b
})]));
var updateSomeAndGet2 = dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: () => [a, a],
  onSome: (b) => [b, b]
})));
var getOrMakeEntry = (self, journal) => {
  if (journal.has(self)) {
    return journal.get(self);
  }
  const entry = make14(self, false);
  journal.set(self, entry);
  return entry;
};
var unsafeGet5 = dual(2, (self, journal) => unsafeGet4(getOrMakeEntry(self, journal)));
var unsafeSet2 = dual(3, (self, value, journal) => {
  const entry = getOrMakeEntry(self, journal);
  unsafeSet(entry, value);
  return void 0;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tQueue.js
var TEnqueueSymbolKey = "effect/TQueue/TEnqueue";
var TEnqueueTypeId = Symbol.for(TEnqueueSymbolKey);
var TDequeueSymbolKey = "effect/TQueue/TDequeue";
var TDequeueTypeId = Symbol.for(TDequeueSymbolKey);
var BackPressure2 = {
  _tag: OP_BACKPRESSURE_STRATEGY
};
var Dropping = {
  _tag: OP_DROPPING_STRATEGY
};
var Sliding = {
  _tag: OP_SLIDING_STRATEGY
};
var tDequeueVariance = {
  /* c8 ignore next */
  _Out: (_) => _
};
var tEnqueueVariance = {
  /* c8 ignore next */
  _In: (_) => _
};
var TQueueImpl = class {
  ref;
  requestedCapacity;
  strategy;
  [TDequeueTypeId] = tDequeueVariance;
  [TEnqueueTypeId] = tEnqueueVariance;
  constructor(ref, requestedCapacity, strategy) {
    this.ref = ref;
    this.requestedCapacity = requestedCapacity;
    this.strategy = strategy;
  }
  capacity() {
    return this.requestedCapacity;
  }
  size = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    return succeed10(queue.length);
  });
  isFull = map11(this.size, (size9) => size9 === this.requestedCapacity);
  isEmpty = map11(this.size, (size9) => size9 === 0);
  shutdown = withSTMRuntime((runtime2) => {
    unsafeSet2(this.ref, void 0, runtime2.journal);
    return void_5;
  });
  isShutdown = effect3((journal) => {
    const queue = unsafeGet5(this.ref, journal);
    return queue === void 0;
  });
  awaitShutdown = flatMap8(this.isShutdown, (isShutdown8) => isShutdown8 ? void_5 : retry4);
  offer(value) {
    return withSTMRuntime((runtime2) => {
      const queue = pipe(this.ref, unsafeGet5(runtime2.journal));
      if (queue === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      if (queue.length < this.requestedCapacity) {
        queue.push(value);
        unsafeSet2(this.ref, queue, runtime2.journal);
        return succeed10(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry4;
        }
        case OP_DROPPING_STRATEGY: {
          return succeed10(false);
        }
        case OP_SLIDING_STRATEGY: {
          if (queue.length === 0) {
            return succeed10(true);
          }
          queue.shift();
          queue.push(value);
          unsafeSet2(this.ref, queue, runtime2.journal);
          return succeed10(true);
        }
      }
    });
  }
  offerAll(iterable) {
    return withSTMRuntime((runtime2) => {
      const as8 = Array.from(iterable);
      const queue = unsafeGet5(this.ref, runtime2.journal);
      if (queue === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      if (queue.length + as8.length <= this.requestedCapacity) {
        unsafeSet2(this.ref, [...queue, ...as8], runtime2.journal);
        return succeed10(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry4;
        }
        case OP_DROPPING_STRATEGY: {
          const forQueue = as8.slice(0, this.requestedCapacity - queue.length);
          unsafeSet2(this.ref, [...queue, ...forQueue], runtime2.journal);
          return succeed10(false);
        }
        case OP_SLIDING_STRATEGY: {
          const forQueue = as8.slice(0, this.requestedCapacity - queue.length);
          const toDrop = queue.length + forQueue.length - this.requestedCapacity;
          const newQueue = queue.slice(toDrop);
          unsafeSet2(this.ref, [...newQueue, ...forQueue], runtime2.journal);
          return succeed10(true);
        }
      }
    });
  }
  peek = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    if (queue.length === 0) {
      return retry4;
    }
    return succeed10(queue[0]);
  });
  peekOption = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    return succeed10(fromNullable(queue[0]));
  });
  take = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    if (queue.length === 0) {
      return retry4;
    }
    const dequeued = queue.shift();
    unsafeSet2(this.ref, queue, runtime2.journal);
    return succeed10(dequeued);
  });
  takeAll = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    unsafeSet2(this.ref, [], runtime2.journal);
    return succeed10(queue);
  });
  takeUpTo(max) {
    return withSTMRuntime((runtime2) => {
      const queue = unsafeGet5(this.ref, runtime2.journal);
      if (queue === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      const [toTake, remaining] = splitAt(unsafeFromArray(queue), max);
      unsafeSet2(this.ref, Array.from(remaining), runtime2.journal);
      return succeed10(Array.from(toTake));
    });
  }
};
var isTQueue = (u) => {
  return isTEnqueue(u) && isTDequeue(u);
};
var isTEnqueue = (u) => hasProperty(u, TEnqueueTypeId);
var isTDequeue = (u) => hasProperty(u, TDequeueTypeId);
var awaitShutdown4 = (self) => self.awaitShutdown;
var bounded4 = (requestedCapacity) => makeQueue(requestedCapacity, BackPressure2);
var capacity4 = (self) => {
  return self.capacity();
};
var dropping4 = (requestedCapacity) => makeQueue(requestedCapacity, Dropping);
var isEmpty6 = (self) => self.isEmpty;
var isFull4 = (self) => self.isFull;
var isShutdown4 = (self) => self.isShutdown;
var offer3 = dual(2, (self, value) => self.offer(value));
var offerAll3 = dual(2, (self, iterable) => self.offerAll(iterable));
var peek = (self) => self.peek;
var peekOption = (self) => self.peekOption;
var poll5 = (self) => pipe(self.takeUpTo(1), map11(head));
var seek = dual(2, (self, predicate) => seekLoop(self, predicate));
var seekLoop = (self, predicate) => flatMap8(self.take, (a) => predicate(a) ? succeed10(a) : seekLoop(self, predicate));
var shutdown4 = (self) => self.shutdown;
var size5 = (self) => self.size;
var sliding4 = (requestedCapacity) => makeQueue(requestedCapacity, Sliding);
var take4 = (self) => self.take;
var takeAll2 = (self) => self.takeAll;
var takeBetween2 = dual(3, (self, min, max) => suspend8(() => {
  const takeRemainder = (min2, max2, acc) => {
    if (max2 < min2) {
      return succeed10(acc);
    }
    return pipe(self.takeUpTo(max2), flatMap8((taken) => {
      const remaining = min2 - taken.length;
      if (remaining === 1) {
        return pipe(self.take, map11((a) => pipe(acc, appendAll(unsafeFromArray(taken)), append(a))));
      }
      if (remaining > 1) {
        return pipe(self.take, flatMap8((a) => takeRemainder(remaining - 1, max2 - taken.length - 1, pipe(acc, appendAll(unsafeFromArray(taken)), append(a)))));
      }
      return succeed10(pipe(acc, appendAll(unsafeFromArray(taken))));
    }));
  };
  return map11(takeRemainder(min, max, empty()), (c) => Array.from(c));
}));
var takeN2 = dual(2, (self, n) => pipe(self, takeBetween2(n, n)));
var takeUpTo2 = dual(2, (self, max) => self.takeUpTo(max));
var unbounded5 = () => makeQueue(Number.MAX_SAFE_INTEGER, Dropping);
var makeQueue = (requestedCapacity, strategy) => map11(make16([]), (ref) => new TQueueImpl(ref, requestedCapacity, strategy));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tPubSub.js
var TPubSubSymbolKey = "effect/TPubSub";
var TPubSubTypeId = Symbol.for(TPubSubSymbolKey);
var AbsentValue2 = /* @__PURE__ */ Symbol.for("effect/TPubSub/AbsentValue");
var makeNode = (head5, subscribers, tail) => ({
  head: head5,
  subscribers,
  tail
});
var TPubSubImpl = class {
  pubsubSize;
  publisherHead;
  publisherTail;
  requestedCapacity;
  strategy;
  subscriberCount;
  subscribers;
  [TPubSubTypeId] = {
    _A: (_) => _
  };
  [TEnqueueTypeId] = tEnqueueVariance;
  constructor(pubsubSize, publisherHead, publisherTail, requestedCapacity, strategy, subscriberCount, subscribers) {
    this.pubsubSize = pubsubSize;
    this.publisherHead = publisherHead;
    this.publisherTail = publisherTail;
    this.requestedCapacity = requestedCapacity;
    this.strategy = strategy;
    this.subscriberCount = subscriberCount;
    this.subscribers = subscribers;
  }
  isShutdown = effect3((journal) => {
    const currentPublisherTail = unsafeGet5(this.publisherTail, journal);
    return currentPublisherTail === void 0;
  });
  awaitShutdown = flatMap8(this.isShutdown, (isShutdown8) => isShutdown8 ? void_5 : retry4);
  capacity() {
    return this.requestedCapacity;
  }
  size = withSTMRuntime((runtime2) => {
    const currentPublisherTail = unsafeGet5(this.publisherTail, runtime2.journal);
    if (currentPublisherTail === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    return succeed10(unsafeGet5(this.pubsubSize, runtime2.journal));
  });
  isEmpty = map11(this.size, (size9) => size9 === 0);
  isFull = map11(this.size, (size9) => size9 === this.capacity());
  offer(value) {
    return withSTMRuntime((runtime2) => {
      const currentPublisherTail = unsafeGet5(this.publisherTail, runtime2.journal);
      if (currentPublisherTail === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      const currentSubscriberCount = unsafeGet5(this.subscriberCount, runtime2.journal);
      if (currentSubscriberCount === 0) {
        return succeed10(true);
      }
      const currentPubSubSize = unsafeGet5(this.pubsubSize, runtime2.journal);
      if (currentPubSubSize < this.requestedCapacity) {
        const updatedPublisherTail = new TRefImpl(void 0);
        const updatedNode = makeNode(value, currentSubscriberCount, updatedPublisherTail);
        unsafeSet2(currentPublisherTail, updatedNode, runtime2.journal);
        unsafeSet2(this.publisherTail, updatedPublisherTail, runtime2.journal);
        unsafeSet2(this.pubsubSize, currentPubSubSize + 1, runtime2.journal);
        return succeed10(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry4;
        }
        case OP_DROPPING_STRATEGY: {
          return succeed10(false);
        }
        case OP_SLIDING_STRATEGY: {
          if (this.requestedCapacity > 0) {
            let currentPublisherHead = unsafeGet5(this.publisherHead, runtime2.journal);
            let loop2 = true;
            while (loop2) {
              const node = unsafeGet5(currentPublisherHead, runtime2.journal);
              if (node === void 0) {
                return retry4;
              }
              const head5 = node.head;
              const tail = node.tail;
              if (head5 !== AbsentValue2) {
                const updatedNode2 = makeNode(AbsentValue2, node.subscribers, node.tail);
                unsafeSet2(currentPublisherHead, updatedNode2, runtime2.journal);
                unsafeSet2(this.publisherHead, tail, runtime2.journal);
                loop2 = false;
              } else {
                currentPublisherHead = tail;
              }
            }
          }
          const updatedPublisherTail = new TRefImpl(void 0);
          const updatedNode = makeNode(value, currentSubscriberCount, updatedPublisherTail);
          unsafeSet2(currentPublisherTail, updatedNode, runtime2.journal);
          unsafeSet2(this.publisherTail, updatedPublisherTail, runtime2.journal);
          return succeed10(true);
        }
      }
    });
  }
  offerAll(iterable) {
    return map11(forEach4(iterable, (a) => this.offer(a)), every(identity));
  }
  shutdown = effect3((journal) => {
    const currentPublisherTail = unsafeGet5(this.publisherTail, journal);
    if (currentPublisherTail !== void 0) {
      unsafeSet2(this.publisherTail, void 0, journal);
      const currentSubscribers = unsafeGet5(this.subscribers, journal);
      forEach(currentSubscribers, (subscriber) => {
        unsafeSet2(subscriber, void 0, journal);
      });
      unsafeSet2(this.subscribers, empty2(), journal);
    }
  });
};
var TPubSubSubscriptionImpl = class {
  pubsubSize;
  publisherHead;
  requestedCapacity;
  subscriberHead;
  subscriberCount;
  subscribers;
  [TPubSubTypeId] = TPubSubTypeId;
  [TDequeueTypeId] = tDequeueVariance;
  constructor(pubsubSize, publisherHead, requestedCapacity, subscriberHead, subscriberCount, subscribers) {
    this.pubsubSize = pubsubSize;
    this.publisherHead = publisherHead;
    this.requestedCapacity = requestedCapacity;
    this.subscriberHead = subscriberHead;
    this.subscriberCount = subscriberCount;
    this.subscribers = subscribers;
  }
  isShutdown = effect3((journal) => {
    const currentSubscriberHead = unsafeGet5(this.subscriberHead, journal);
    return currentSubscriberHead === void 0;
  });
  awaitShutdown = flatMap8(this.isShutdown, (isShutdown8) => isShutdown8 ? void_5 : retry4);
  capacity() {
    return this.requestedCapacity;
  }
  size = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let loop2 = true;
    let size9 = 0;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        loop2 = false;
      } else {
        const head5 = node.head;
        const tail = node.tail;
        if (head5 !== AbsentValue2) {
          size9 = size9 + 1;
          if (size9 >= Number.MAX_SAFE_INTEGER) {
            loop2 = false;
          }
        }
        currentSubscriberHead = tail;
      }
    }
    return succeed10(size9);
  });
  isEmpty = map11(this.size, (size9) => size9 === 0);
  isFull = map11(this.size, (size9) => size9 === this.capacity());
  peek = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let value = AbsentValue2;
    let loop2 = true;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        return retry4;
      }
      const head5 = node.head;
      const tail = node.tail;
      if (head5 !== AbsentValue2) {
        value = head5;
        loop2 = false;
      } else {
        currentSubscriberHead = tail;
      }
    }
    return succeed10(value);
  });
  peekOption = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let value = none();
    let loop2 = true;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        value = none();
        loop2 = false;
      } else {
        const head5 = node.head;
        const tail = node.tail;
        if (head5 !== AbsentValue2) {
          value = some(head5);
          loop2 = false;
        } else {
          currentSubscriberHead = tail;
        }
      }
    }
    return succeed10(value);
  });
  shutdown = effect3((journal) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, journal);
    if (currentSubscriberHead !== void 0) {
      unsafeSet2(this.subscriberHead, void 0, journal);
      let loop2 = true;
      while (loop2) {
        const node = unsafeGet5(currentSubscriberHead, journal);
        if (node === void 0) {
          loop2 = false;
        } else {
          const head5 = node.head;
          const tail = node.tail;
          if (head5 !== AbsentValue2) {
            const subscribers = node.subscribers;
            if (subscribers === 1) {
              const size9 = unsafeGet5(this.pubsubSize, journal);
              const updatedNode = makeNode(AbsentValue2, 0, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, journal);
              unsafeSet2(this.publisherHead, tail, journal);
              unsafeSet2(this.pubsubSize, size9 - 1, journal);
            } else {
              const updatedNode = makeNode(head5, subscribers - 1, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, journal);
            }
          }
          currentSubscriberHead = tail;
        }
      }
      const currentSubscriberCount = unsafeGet5(this.subscriberCount, journal);
      unsafeSet2(this.subscriberCount, currentSubscriberCount - 1, journal);
      unsafeSet2(this.subscribers, remove(unsafeGet5(this.subscribers, journal), this.subscriberHead), journal);
    }
  });
  take = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let value = AbsentValue2;
    let loop2 = true;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        return retry4;
      }
      const head5 = node.head;
      const tail = node.tail;
      if (head5 !== AbsentValue2) {
        const subscribers = node.subscribers;
        if (subscribers === 1) {
          const size9 = unsafeGet5(this.pubsubSize, runtime2.journal);
          const updatedNode = makeNode(AbsentValue2, 0, tail);
          unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
          unsafeSet2(this.publisherHead, tail, runtime2.journal);
          unsafeSet2(this.pubsubSize, size9 - 1, runtime2.journal);
        } else {
          const updatedNode = makeNode(head5, subscribers - 1, tail);
          unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
        }
        unsafeSet2(this.subscriberHead, tail, runtime2.journal);
        value = head5;
        loop2 = false;
      } else {
        currentSubscriberHead = tail;
      }
    }
    return succeed10(value);
  });
  takeAll = this.takeUpTo(Number.POSITIVE_INFINITY);
  takeUpTo(max) {
    return withSTMRuntime((runtime2) => {
      let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
      if (currentSubscriberHead === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      const builder = [];
      let n = 0;
      while (n !== max) {
        const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
        if (node === void 0) {
          n = max;
        } else {
          const head5 = node.head;
          const tail = node.tail;
          if (head5 !== AbsentValue2) {
            const subscribers = node.subscribers;
            if (subscribers === 1) {
              const size9 = unsafeGet5(this.pubsubSize, runtime2.journal);
              const updatedNode = makeNode(AbsentValue2, 0, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
              unsafeSet2(this.publisherHead, tail, runtime2.journal);
              unsafeSet2(this.pubsubSize, size9 - 1, runtime2.journal);
            } else {
              const updatedNode = makeNode(head5, subscribers - 1, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
            }
            builder.push(head5);
            n = n + 1;
          }
          currentSubscriberHead = tail;
        }
      }
      unsafeSet2(this.subscriberHead, currentSubscriberHead, runtime2.journal);
      return succeed10(builder);
    });
  }
};
var makeTPubSub = (requestedCapacity, strategy) => pipe(all4([make16(void 0), make16(0)]), flatMap8(([empty8, pubsubSize]) => pipe(all4([make16(empty8), make16(empty8), make16(0), make16(empty2())]), map11(([publisherHead, publisherTail, subscriberCount, subscribers]) => new TPubSubImpl(pubsubSize, publisherHead, publisherTail, requestedCapacity, strategy, subscriberCount, subscribers)))));
var makeSubscription2 = (pubsubSize, publisherHead, publisherTail, requestedCapacity, subscriberCount, subscribers) => pipe(get7(publisherTail), flatMap8((currentPublisherTail) => pipe(all4([make16(currentPublisherTail), get7(subscriberCount), get7(subscribers)]), tap5(([_, currentSubscriberCount]) => pipe(subscriberCount, set5(currentSubscriberCount + 1))), tap5(([subscriberHead, _, currentSubscribers]) => pipe(subscribers, set5(pipe(currentSubscribers, add2(subscriberHead))))), map11(([subscriberHead]) => new TPubSubSubscriptionImpl(pubsubSize, publisherHead, requestedCapacity, subscriberHead, subscriberCount, subscribers)))));
var awaitShutdown5 = (self) => self.awaitShutdown;
var bounded5 = (requestedCapacity) => makeTPubSub(requestedCapacity, BackPressure2);
var capacity5 = (self) => self.capacity();
var dropping5 = (requestedCapacity) => makeTPubSub(requestedCapacity, Dropping);
var isEmpty7 = (self) => self.isEmpty;
var isFull5 = (self) => self.isFull;
var isShutdown5 = (self) => self.isShutdown;
var publish3 = dual(2, (self, value) => self.offer(value));
var publishAll3 = dual(2, (self, iterable) => self.offerAll(iterable));
var size6 = (self) => self.size;
var shutdown5 = (self) => self.shutdown;
var sliding5 = (requestedCapacity) => makeTPubSub(requestedCapacity, Sliding);
var subscribe3 = (self) => makeSubscription2(self.pubsubSize, self.publisherHead, self.publisherTail, self.requestedCapacity, self.subscriberCount, self.subscribers);
var subscribeScoped = (self) => acquireRelease2(subscribe3(self), (dequeue) => shutdown4(dequeue));
var unbounded6 = () => makeTPubSub(Number.MAX_SAFE_INTEGER, Dropping);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TPubSub.js
var TPubSubTypeId2 = TPubSubTypeId;
var awaitShutdown6 = awaitShutdown5;
var bounded6 = bounded5;
var capacity6 = capacity5;
var dropping6 = dropping5;
var isEmpty8 = isEmpty7;
var isFull6 = isFull5;
var shutdown6 = shutdown5;
var isShutdown6 = isShutdown5;
var publish4 = publish3;
var publishAll4 = publishAll3;
var size7 = size6;
var sliding6 = sliding5;
var subscribe4 = subscribe3;
var subscribeScoped2 = subscribeScoped;
var unbounded7 = unbounded6;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TQueue.js
var TQueue_exports = {};
__export(TQueue_exports, {
  TDequeueTypeId: () => TDequeueTypeId2,
  TEnqueueTypeId: () => TEnqueueTypeId2,
  awaitShutdown: () => awaitShutdown7,
  bounded: () => bounded7,
  capacity: () => capacity7,
  dropping: () => dropping7,
  isEmpty: () => isEmpty9,
  isFull: () => isFull7,
  isShutdown: () => isShutdown7,
  isTDequeue: () => isTDequeue2,
  isTEnqueue: () => isTEnqueue2,
  isTQueue: () => isTQueue2,
  offer: () => offer4,
  offerAll: () => offerAll4,
  peek: () => peek2,
  peekOption: () => peekOption2,
  poll: () => poll6,
  seek: () => seek2,
  shutdown: () => shutdown7,
  size: () => size8,
  sliding: () => sliding7,
  take: () => take5,
  takeAll: () => takeAll3,
  takeBetween: () => takeBetween3,
  takeN: () => takeN3,
  takeUpTo: () => takeUpTo3,
  unbounded: () => unbounded8
});
var TDequeueTypeId2 = TDequeueTypeId;
var TEnqueueTypeId2 = TEnqueueTypeId;
var isTQueue2 = isTQueue;
var isTDequeue2 = isTDequeue;
var isTEnqueue2 = isTEnqueue;
var awaitShutdown7 = awaitShutdown4;
var bounded7 = bounded4;
var capacity7 = capacity4;
var dropping7 = dropping4;
var isEmpty9 = isEmpty6;
var isFull7 = isFull4;
var isShutdown7 = isShutdown4;
var offer4 = offer3;
var offerAll4 = offerAll3;
var peek2 = peek;
var peekOption2 = peekOption;
var poll6 = poll5;
var seek2 = seek;
var shutdown7 = shutdown4;
var size8 = size5;
var sliding7 = sliding4;
var take5 = take4;
var takeAll3 = takeAll2;
var takeBetween3 = takeBetween2;
var takeN3 = takeN2;
var takeUpTo3 = takeUpTo2;
var unbounded8 = unbounded5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/ringBuffer.js
var RingBuffer = class {
  capacity;
  array;
  size = 0;
  current = 0;
  constructor(capacity8) {
    this.capacity = capacity8;
    this.array = Array.from({
      length: capacity8
    }, constUndefined);
  }
  head() {
    return fromNullable(this.array[this.current]);
  }
  lastOrNull() {
    if (this.size === 0) {
      return void 0;
    }
    const index = this.current === 0 ? this.array.length - 1 : this.current - 1;
    return this.array[index] ?? void 0;
  }
  put(value) {
    this.array[this.current] = value;
    this.increment();
  }
  dropLast() {
    if (this.size > 0) {
      this.decrement();
      this.array[this.current] = void 0;
    }
  }
  toChunk() {
    const begin = this.current - this.size;
    const newArray = begin < 0 ? [...this.array.slice(this.capacity + begin, this.capacity), ...this.array.slice(0, this.current)] : this.array.slice(begin, this.current);
    return fromIterable2(newArray);
  }
  increment() {
    if (this.size < this.capacity) {
      this.size += 1;
    }
    this.current = (this.current + 1) % this.capacity;
  }
  decrement() {
    this.size -= 1;
    if (this.current > 0) {
      this.current -= 1;
    } else {
      this.current = this.capacity - 1;
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/debounceState.js
var OP_NOT_STARTED = "NotStarted";
var OP_PREVIOUS = "Previous";
var OP_CURRENT = "Current";
var notStarted = {
  _tag: OP_NOT_STARTED
};
var previous = (fiber) => ({
  _tag: OP_PREVIOUS,
  fiber
});
var current = (fiber) => ({
  _tag: OP_CURRENT,
  fiber
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/emit.js
var make17 = (emit2) => {
  const ops = {
    chunk(as8) {
      return this(succeed5(as8));
    },
    die(defect) {
      return this(die4(defect));
    },
    dieMessage(message) {
      return this(dieMessage(message));
    },
    done(exit2) {
      return this(suspend3(() => mapBoth(exit2, {
        onFailure: some,
        onSuccess: of
      })));
    },
    end() {
      return this(fail5(none()));
    },
    fail(e) {
      return this(fail5(some(e)));
    },
    fromEffect(effect4) {
      return this(mapBoth2(effect4, {
        onFailure: some,
        onSuccess: of
      }));
    },
    fromEffectChunk(effect4) {
      return this(pipe(effect4, mapError2(some)));
    },
    halt(cause) {
      return this(failCause4(pipe(cause, map5(some))));
    },
    single(value) {
      return this(succeed5(of(value)));
    }
  };
  return Object.assign(emit2, ops);
};
var makePush = (queue, scheduler) => {
  let finished = false;
  let buffer4 = [];
  let running2 = false;
  function array(items) {
    if (finished) return false;
    if (items.length <= 5e4) {
      buffer4.push.apply(buffer4, items);
    } else {
      for (let i = 0; i < items.length; i++) {
        buffer4.push(items[0]);
      }
    }
    if (!running2) {
      running2 = true;
      scheduler.scheduleTask(flush, 0);
    }
    return true;
  }
  function flush() {
    running2 = false;
    if (buffer4.length > 0) {
      queue.unsafeOffer(buffer4);
      buffer4 = [];
    }
  }
  function done4(exit2) {
    if (finished) return;
    finished = true;
    if (exit2._tag === "Success") {
      buffer4.push(exit2.value);
    }
    flush();
    queue.unsafeOffer(exit2._tag === "Success" ? void_2 : exit2);
  }
  return {
    single(value) {
      if (finished) return false;
      buffer4.push(value);
      if (!running2) {
        running2 = true;
        scheduler.scheduleTask(flush, 0);
      }
      return true;
    },
    array,
    chunk(chunk2) {
      return array(toReadonlyArray(chunk2));
    },
    done: done4,
    end() {
      if (finished) return;
      finished = true;
      flush();
      queue.unsafeOffer(void_2);
    },
    halt(cause) {
      return done4(failCause(cause));
    },
    fail(error) {
      return done4(fail(error));
    },
    die(defect) {
      return done4(die(defect));
    },
    dieMessage(message) {
      return done4(die(new Error(message)));
    }
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/handoff.js
var HandoffTypeId = /* @__PURE__ */ Symbol.for("effect/Stream/Handoff");
var OP_HANDOFF_STATE_EMPTY = "Empty";
var OP_HANDOFF_STATE_FULL = "Full";
var handoffStateEmpty = (notifyConsumer) => ({
  _tag: OP_HANDOFF_STATE_EMPTY,
  notifyConsumer
});
var handoffStateFull = (value, notifyProducer) => ({
  _tag: OP_HANDOFF_STATE_FULL,
  value,
  notifyProducer
});
var handoffStateMatch = (onEmpty, onFull) => {
  return (self) => {
    switch (self._tag) {
      case OP_HANDOFF_STATE_EMPTY: {
        return onEmpty(self.notifyConsumer);
      }
      case OP_HANDOFF_STATE_FULL: {
        return onFull(self.value, self.notifyProducer);
      }
    }
  };
};
var handoffVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var make18 = () => pipe(make6(), flatMap4((deferred) => make5(handoffStateEmpty(deferred))), map7((ref) => ({
  [HandoffTypeId]: handoffVariance,
  ref
})));
var offer5 = dual(2, (self, value) => {
  return flatMap4(make6(), (deferred) => flatten3(modify(self.ref, (state) => pipe(state, handoffStateMatch((notifyConsumer) => [zipRight3(succeed3(notifyConsumer, void 0), _await(deferred)), handoffStateFull(value, deferred)], (_, notifyProducer) => [flatMap4(_await(notifyProducer), () => pipe(self, offer5(value))), state])))));
});
var take6 = (self) => flatMap4(make6(), (deferred) => flatten3(modify(self.ref, (state) => pipe(state, handoffStateMatch((notifyConsumer) => [flatMap4(_await(notifyConsumer), () => take6(self)), state], (value, notifyProducer) => [as2(succeed3(notifyProducer, void 0), value), handoffStateEmpty(deferred)])))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/handoffSignal.js
var OP_EMIT4 = "Emit";
var OP_HALT = "Halt";
var OP_END = "End";
var emit = (elements) => ({
  _tag: OP_EMIT4,
  elements
});
var halt = (cause) => ({
  _tag: OP_HALT,
  cause
});
var end = (reason) => ({
  _tag: OP_END,
  reason
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/take.js
var TakeSymbolKey = "effect/Take";
var TakeTypeId = Symbol.for(TakeSymbolKey);
var takeVariance = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _E: (_) => _
};
var TakeImpl = class {
  exit;
  [TakeTypeId] = takeVariance;
  constructor(exit2) {
    this.exit = exit2;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var chunk = (chunk2) => new TakeImpl(succeed2(chunk2));
var die9 = (defect) => new TakeImpl(die(defect));
var dieMessage4 = (message) => new TakeImpl(die(new RuntimeException(message)));
var done3 = (self) => suspend3(() => self.exit);
var end2 = new TakeImpl(fail(none()));
var fail11 = (error) => new TakeImpl(fail(some(error)));
var failCause8 = (cause) => new TakeImpl(failCause(pipe(cause, map5(some))));
var fromEffect5 = (effect4) => matchCause2(effect4, {
  onFailure: failCause8,
  onSuccess: of2
});
var fromExit = (exit2) => new TakeImpl(pipe(exit2, mapBoth({
  onFailure: some,
  onSuccess: of
})));
var fromPull = (pull) => matchCause2(pull, {
  onFailure: (cause) => match2(flipCauseOption(cause), {
    onNone: () => end2,
    onSome: failCause8
  }),
  onSuccess: chunk
});
var isDone3 = (self) => match3(self.exit, {
  onFailure: (cause) => isNone(flipCauseOption(cause)),
  onSuccess: constFalse
});
var isFailure3 = (self) => match3(self.exit, {
  onFailure: (cause) => isSome(flipCauseOption(cause)),
  onSuccess: constFalse
});
var isSuccess4 = (self) => match3(self.exit, {
  onFailure: constFalse,
  onSuccess: constTrue
});
var make19 = (exit2) => new TakeImpl(exit2);
var match17 = dual(2, (self, {
  onEnd: onEnd3,
  onFailure,
  onSuccess
}) => match3(self.exit, {
  onFailure: (cause) => match2(flipCauseOption(cause), {
    onNone: onEnd3,
    onSome: onFailure
  }),
  onSuccess
}));
var matchEffect3 = dual(2, (self, {
  onEnd: onEnd3,
  onFailure,
  onSuccess
}) => matchEffect(self.exit, {
  onFailure: (cause) => match2(flipCauseOption(cause), {
    onNone: () => onEnd3,
    onSome: onFailure
  }),
  onSuccess
}));
var map12 = dual(2, (self, f) => new TakeImpl(pipe(self.exit, map4(map2(f)))));
var of2 = (value) => new TakeImpl(succeed2(of(value)));
var tap6 = dual(2, (self, f) => pipe(self.exit, forEachEffect(f), asVoid2));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/pull.js
var end3 = () => fail5(none());
var failCause9 = (cause) => mapError2(failCause4(cause), some);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/sinkEndReason.js
var OP_SCHEDULE_END = "ScheduleEnd";
var OP_UPSTREAM_END = "UpstreamEnd";
var ScheduleEnd = {
  _tag: OP_SCHEDULE_END
};
var UpstreamEnd = {
  _tag: OP_UPSTREAM_END
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/zipAllState.js
var OP_DRAIN_LEFT = "DrainLeft";
var OP_DRAIN_RIGHT = "DrainRight";
var OP_PULL_BOTH = "PullBoth";
var OP_PULL_LEFT = "PullLeft";
var OP_PULL_RIGHT = "PullRight";
var DrainLeft = {
  _tag: OP_DRAIN_LEFT
};
var DrainRight = {
  _tag: OP_DRAIN_RIGHT
};
var PullBoth = {
  _tag: OP_PULL_BOTH
};
var PullLeft = (rightChunk) => ({
  _tag: OP_PULL_LEFT,
  rightChunk
});
var PullRight = (leftChunk) => ({
  _tag: OP_PULL_RIGHT,
  leftChunk
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/zipChunksState.js
var OP_PULL_BOTH2 = "PullBoth";
var OP_PULL_LEFT2 = "PullLet";
var OP_PULL_RIGHT2 = "PullRight";
var PullBoth2 = {
  _tag: OP_PULL_BOTH2
};
var PullLeft2 = (rightChunk) => ({
  _tag: OP_PULL_LEFT2,
  rightChunk
});
var PullRight2 = (leftChunk) => ({
  _tag: OP_PULL_RIGHT2,
  leftChunk
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream.js
var StreamSymbolKey = "effect/Stream";
var StreamTypeId2 = Symbol.for(StreamSymbolKey);
var streamVariance = {
  _R: (_) => _,
  _E: (_) => _,
  _A: (_) => _
};
var StreamImpl = class {
  channel;
  [StreamTypeId2] = streamVariance;
  constructor(channel) {
    this.channel = channel;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isStream = (u) => hasProperty(u, StreamTypeId2) || isEffect2(u);
var DefaultChunkSize = 4096;
var accumulate = (self) => chunks(accumulateChunks(self));
var accumulateChunks = (self) => {
  const accumulator = (s) => readWith({
    onInput: (input) => {
      const next = appendAll(s, input);
      return flatMap6(write(next), () => accumulator(next));
    },
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipeTo(toChannel2(self), accumulator(empty())));
};
var acquireRelease3 = (acquire, release) => scoped5(acquireRelease2(acquire, release));
var aggregate = dual(2, (self, sink) => aggregateWithin(self, sink, forever2));
var aggregateWithin = dual(3, (self, sink, schedule3) => filterMap2(aggregateWithinEither(self, sink, schedule3), (_) => match(_, {
  onLeft: none,
  onRight: some
})));
var aggregateWithinEither = dual(3, (self, sink, schedule3) => {
  const layer = all3([make18(), make5(ScheduleEnd), make5(empty()), driver(schedule3), make5(false), make5(false)]);
  return fromEffect6(layer).pipe(flatMap9(([handoff, sinkEndReason, sinkLeftovers, scheduleDriver, consumed, endAfterEmit]) => {
    const handoffProducer = readWithCause({
      onInput: (input) => flatMap6(fromEffect2(pipe(handoff, offer5(emit(input)), when(() => isNonEmpty(input)))), () => handoffProducer),
      onFailure: (cause) => fromEffect2(offer5(handoff, halt(cause))),
      onDone: () => fromEffect2(offer5(handoff, end(UpstreamEnd)))
    });
    const handoffConsumer = pipe(getAndSet(sinkLeftovers, empty()), flatMap4((leftovers) => {
      if (isNonEmpty(leftovers)) {
        return pipe(set3(consumed, true), zipRight3(succeed5(pipe(write(leftovers), flatMap6(() => handoffConsumer)))));
      }
      return pipe(take6(handoff), map7((signal) => {
        switch (signal._tag) {
          case OP_EMIT4: {
            return pipe(fromEffect2(set3(consumed, true)), zipRight4(write(signal.elements)), zipRight4(fromEffect2(get3(endAfterEmit))), flatMap6((bool) => bool ? void_3 : handoffConsumer));
          }
          case OP_HALT: {
            return failCause6(signal.cause);
          }
          case OP_END: {
            if (signal.reason._tag === OP_SCHEDULE_END) {
              return pipe(get3(consumed), map7((bool) => bool ? fromEffect2(pipe(set3(sinkEndReason, ScheduleEnd), zipRight3(set3(endAfterEmit, true)))) : pipe(fromEffect2(pipe(set3(sinkEndReason, ScheduleEnd), zipRight3(set3(endAfterEmit, true)))), flatMap6(() => handoffConsumer))), unwrap);
            }
            return pipe(set3(sinkEndReason, signal.reason), zipRight3(set3(endAfterEmit, true)), fromEffect2);
          }
        }
      }));
    }), unwrap);
    const timeout3 = (lastB) => scheduleDriver.next(lastB);
    const scheduledAggregator = (sinkFiber, scheduleFiber, scope4) => {
      const forkSink = pipe(set3(consumed, false), zipRight3(set3(endAfterEmit, false)), zipRight3(pipe(handoffConsumer, pipeToOrFail(toChannel(sink)), collectElements, run, forkIn2(scope4))));
      const handleSide = (leftovers, b, c) => pipe(set3(sinkLeftovers, flatten(leftovers)), zipRight3(map7(get3(sinkEndReason), (reason) => {
        switch (reason._tag) {
          case OP_SCHEDULE_END: {
            return pipe(all3([get3(consumed), forkSink, pipe(timeout3(some(b)), forkIn2(scope4))]), map7(([wasConsumed, sinkFiber2, scheduleFiber2]) => {
              const toWrite = pipe(c, match2({
                onNone: () => of(right(b)),
                onSome: (c2) => make2(right(b), left(c2))
              }));
              if (wasConsumed) {
                return pipe(write(toWrite), flatMap6(() => scheduledAggregator(sinkFiber2, scheduleFiber2, scope4)));
              }
              return scheduledAggregator(sinkFiber2, scheduleFiber2, scope4);
            }), unwrap);
          }
          case OP_UPSTREAM_END: {
            return pipe(get3(consumed), map7((wasConsumed) => wasConsumed ? write(of(right(b))) : void_3), unwrap);
          }
        }
      })), unwrap);
      return unwrap(raceWith(join(sinkFiber), join(scheduleFiber), {
        onSelfDone: (sinkExit, _) => pipe(interrupt4(scheduleFiber), zipRight3(pipe(suspend3(() => sinkExit), map7(([leftovers, b]) => handleSide(leftovers, b, none()))))),
        onOtherDone: (scheduleExit, _) => matchCauseEffect(suspend3(() => scheduleExit), {
          onFailure: (cause) => match(failureOrCause(cause), {
            onLeft: () => pipe(handoff, offer5(end(ScheduleEnd)), forkDaemon, zipRight3(pipe(join(sinkFiber), map7(([leftovers, b]) => handleSide(leftovers, b, none()))))),
            onRight: (cause2) => pipe(handoff, offer5(halt(cause2)), forkDaemon, zipRight3(pipe(join(sinkFiber), map7(([leftovers, b]) => handleSide(leftovers, b, none())))))
          }),
          onSuccess: (c) => pipe(handoff, offer5(end(ScheduleEnd)), forkDaemon, zipRight3(pipe(join(sinkFiber), map7(([leftovers, b]) => handleSide(leftovers, b, some(c))))))
        })
      }));
    };
    return unwrapScopedWith3((scope4) => pipeTo(toChannel2(self), handoffProducer).pipe(run, forkIn2(scope4), zipRight3(pipeToOrFail(handoffConsumer, toChannel(sink)).pipe(collectElements, run, forkIn2(scope4), flatMap4((sinkFiber) => timeout3(none()).pipe(forkIn2(scope4), map7((scheduleFiber) => new StreamImpl(scheduledAggregator(sinkFiber, scheduleFiber, scope4)))))))));
  }));
});
var as6 = dual(2, (self, value) => map13(self, () => value));
var queueFromBufferOptions = (bufferSize) => {
  if (bufferSize === "unbounded") {
    return unbounded2();
  } else if (typeof bufferSize === "number" || bufferSize === void 0) {
    return bounded(bufferSize ?? 16);
  }
  switch (bufferSize.strategy) {
    case "dropping":
      return dropping(bufferSize.bufferSize ?? 16);
    case "sliding":
      return sliding(bufferSize.bufferSize ?? 16);
    default:
      return bounded(bufferSize.bufferSize ?? 16);
  }
};
var _async = (register, bufferSize) => acquireRelease2(queueFromBufferOptions(bufferSize), (queue) => shutdown(queue)).pipe(flatMap4((output) => runtime().pipe(flatMap4((runtime2) => sync3(() => {
  const runPromiseExit2 = runPromiseExit(runtime2);
  const canceler = register(make17((resume) => fromPull(resume).pipe(flatMap4((take9) => offer2(output, take9)), asVoid2, runPromiseExit2).then((exit2) => {
    if (isFailure(exit2)) {
      if (!isInterrupted(exit2.cause)) {
        throw squash(exit2.cause);
      }
    }
  })));
  return canceler;
})), map7((value) => {
  const loop2 = take2(output).pipe(flatMap4((take9) => done3(take9)), match5({
    onFailure: (maybeError) => fromEffect2(shutdown(output)).pipe(zipRight4(match2(maybeError, {
      onNone: () => void_3,
      onSome: (error) => fail7(error)
    }))),
    onSuccess: (chunk2) => write(chunk2).pipe(flatMap6(() => loop2))
  }), unwrap);
  return fromChannel2(loop2).pipe(ensuring6(value ?? _void));
}))), unwrapScoped5);
var asyncEffect = (register, bufferSize) => pipe(acquireRelease2(queueFromBufferOptions(bufferSize), (queue) => shutdown(queue)), flatMap4((output) => pipe(runtime(), flatMap4((runtime2) => pipe(register(make17((k) => pipe(fromPull(k), flatMap4((take9) => offer2(output, take9)), asVoid2, runPromiseExit(runtime2)).then((exit2) => {
  if (isFailure(exit2)) {
    if (!isInterrupted(exit2.cause)) {
      throw squash(exit2.cause);
    }
  }
}))), map7(() => {
  const loop2 = pipe(take2(output), flatMap4(done3), match5({
    onFailure: (maybeError) => pipe(fromEffect2(shutdown(output)), zipRight4(match2(maybeError, {
      onNone: () => void_3,
      onSome: fail7
    }))),
    onSuccess: (chunk2) => pipe(write(chunk2), flatMap6(() => loop2))
  }), unwrap);
  return loop2;
}))))), unwrapScoped3, fromChannel2);
var queueFromBufferOptionsPush = (options) => {
  if (options?.bufferSize === "unbounded" || options?.bufferSize === void 0 && options?.strategy === void 0) {
    return unbounded2();
  }
  switch (options?.strategy) {
    case "sliding":
      return sliding(options.bufferSize ?? 16);
    default:
      return dropping(options?.bufferSize ?? 16);
  }
};
var asyncPush = (register, options) => acquireRelease2(queueFromBufferOptionsPush(options), shutdown).pipe(tap3((queue) => getWith(currentScheduler2, (scheduler) => register(makePush(queue, scheduler)))), map7((queue) => {
  const loop2 = flatMap6(take2(queue), (item) => isExit(item) ? isSuccess(item) ? void_3 : failCause6(item.cause) : zipRight4(write(unsafeFromArray(item)), loop2));
  return loop2;
}), unwrapScoped3, fromChannel2);
var asyncScoped = (register, bufferSize) => pipe(acquireRelease2(queueFromBufferOptions(bufferSize), (queue) => shutdown(queue)), flatMap4((output) => pipe(runtime(), flatMap4((runtime2) => pipe(register(make17((k) => pipe(fromPull(k), flatMap4((take9) => offer2(output, take9)), asVoid2, runPromiseExit(runtime2)).then((exit2) => {
  if (isFailure(exit2)) {
    if (!isInterrupted(exit2.cause)) {
      throw squash(exit2.cause);
    }
  }
}))), zipRight3(make5(false)), flatMap4((ref) => pipe(get3(ref), map7((isDone4) => isDone4 ? end3() : pipe(take2(output), flatMap4(done3), onError(() => pipe(set3(ref, true), zipRight3(shutdown(output)))))))))))), scoped5, flatMap9(repeatEffectChunkOption));
var branchAfter = dual(3, (self, n, f) => suspend9(() => {
  const buffering = (acc) => readWith({
    onInput: (input) => {
      const nextSize = acc.length + input.length;
      if (nextSize >= n) {
        const [b1, b2] = pipe(input, splitAt(n - acc.length));
        return running2(pipe(acc, appendAll(b1)), b2);
      }
      return buffering(pipe(acc, appendAll(input)));
    },
    onFailure: fail7,
    onDone: () => running2(acc, empty())
  });
  const running2 = (prefix, leftover2) => pipeTo(zipRight4(write(leftover2), identityChannel()), toChannel2(f(prefix)));
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(buffering(empty()))));
}));
var broadcast = dual(3, (self, n, maximumLag) => pipe(self, broadcastedQueues(n, maximumLag), map7((tuple) => tuple.map((queue) => flattenTake(fromQueue3(queue, {
  shutdown: true
}))))));
var broadcastDynamic = dual(2, (self, maximumLag) => map7(toPubSub2(self, maximumLag), (pubsub) => flattenTake(fromPubSub3(pubsub))));
var share = dual(2, (self, options) => map7(make12({
  acquire: broadcastDynamic(self, options),
  idleTimeToLive: options.idleTimeToLive
}), (rcRef) => unwrapScoped5(get6(rcRef))));
var broadcastedQueues = dual(3, (self, n, maximumLag) => flatMap4(pubsubFromOptions(maximumLag), (pubsub) => pipe(all3(Array.from({
  length: n
}, () => subscribe2(pubsub))), tap3(() => forkScoped(runIntoPubSubScoped(self, pubsub))))));
var broadcastedQueuesDynamic = dual(2, (self, maximumLag) => map7(toPubSub2(self, maximumLag), subscribe2));
var buffer2 = dual(2, (self, options) => {
  if (options.capacity === "unbounded") {
    return bufferUnbounded(self);
  } else if (options.strategy === "dropping") {
    return bufferDropping(self, options.capacity);
  } else if (options.strategy === "sliding") {
    return bufferSliding(self, options.capacity);
  }
  const queue = toQueueOfElements(self, options);
  return new StreamImpl(unwrapScoped3(map7(queue, (queue2) => {
    const process = pipe(fromEffect2(take2(queue2)), flatMap6(match3({
      onFailure: (cause) => pipe(flipCauseOption(cause), match2({
        onNone: () => void_3,
        onSome: failCause6
      })),
      onSuccess: (value) => flatMap6(write(of(value)), () => process)
    })));
    return process;
  })));
});
var bufferChunks = dual(2, (self, options) => {
  if (options.strategy === "dropping") {
    return bufferChunksDropping(self, options.capacity);
  } else if (options.strategy === "sliding") {
    return bufferChunksSliding(self, options.capacity);
  }
  const queue = toQueue2(self, options);
  return new StreamImpl(unwrapScoped3(map7(queue, (queue2) => {
    const process = pipe(fromEffect2(take2(queue2)), flatMap6(match17({
      onEnd: () => void_3,
      onFailure: failCause6,
      onSuccess: (value) => pipe(write(value), flatMap6(() => process))
    })));
    return process;
  })));
});
var bufferChunksDropping = dual(2, (self, capacity8) => {
  const queue = acquireRelease2(dropping(capacity8), (queue2) => shutdown(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(self)));
});
var bufferChunksSliding = dual(2, (self, capacity8) => {
  const queue = acquireRelease2(sliding(capacity8), (queue2) => shutdown(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(self)));
});
var bufferDropping = dual(2, (self, capacity8) => {
  const queue = acquireRelease2(dropping(capacity8), (queue2) => shutdown(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(rechunk(1)(self))));
});
var bufferSliding = dual(2, (self, capacity8) => {
  const queue = acquireRelease2(sliding(capacity8), (queue2) => shutdown(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(pipe(self, rechunk(1)))));
});
var bufferUnbounded = (self) => {
  const queue = toQueue2(self, {
    strategy: "unbounded"
  });
  return new StreamImpl(unwrapScoped3(map7(queue, (queue2) => {
    const process = pipe(fromEffect2(take2(queue2)), flatMap6(match17({
      onEnd: () => void_3,
      onFailure: failCause6,
      onSuccess: (value) => flatMap6(write(value), () => process)
    })));
    return process;
  })));
};
var bufferSignal = (scoped7, bufferChannel) => {
  const producer = (queue, ref) => {
    const terminate = (take9) => pipe(get3(ref), tap3(_await), zipRight3(make6()), flatMap4((deferred) => pipe(offer2(queue, [take9, deferred]), zipRight3(set3(ref, deferred)), zipRight3(_await(deferred)))), asVoid2, fromEffect2);
    return readWithCause({
      onInput: (input) => pipe(make6(), flatMap4((deferred) => pipe(offer2(queue, [chunk(input), deferred]), flatMap4((added) => pipe(set3(ref, deferred), when(() => added))))), asVoid2, fromEffect2, flatMap6(() => producer(queue, ref))),
      onFailure: (error) => terminate(failCause8(error)),
      onDone: () => terminate(end2)
    });
  };
  const consumer = (queue) => {
    const process = pipe(fromEffect2(take2(queue)), flatMap6(([take9, deferred]) => zipRight4(fromEffect2(succeed3(deferred, void 0)), match17(take9, {
      onEnd: () => void_3,
      onFailure: failCause6,
      onSuccess: (value) => pipe(write(value), flatMap6(() => process))
    }))));
    return process;
  };
  return unwrapScoped3(pipe(scoped7, flatMap4((queue) => pipe(make6(), tap3((start) => succeed3(start, void 0)), flatMap4((start) => pipe(make5(start), flatMap4((ref) => pipe(bufferChannel, pipeTo(producer(queue, ref)), runScoped, forkScoped)), as2(consumer(queue))))))));
};
var catchAll6 = dual(2, (self, f) => catchAllCause5(self, (cause) => match(failureOrCause(cause), {
  onLeft: f,
  onRight: failCause10
})));
var catchAllCause5 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), catchAllCause4((cause) => toChannel2(f(cause))))));
var catchSome2 = dual(2, (self, pf) => pipe(self, catchAll6((error) => pipe(pf(error), getOrElse(() => fail12(error))))));
var catchSomeCause2 = dual(2, (self, pf) => pipe(self, catchAllCause5((cause) => pipe(pf(cause), getOrElse(() => failCause10(cause))))));
var catchTag2 = dual(3, (self, k, f) => catchAll6(self, (e) => {
  if ("_tag" in e && e["_tag"] === k) {
    return f(e);
  }
  return fail12(e);
}));
var catchTags2 = dual(2, (self, cases) => catchAll6(self, (e) => {
  const keys = Object.keys(cases);
  if ("_tag" in e && keys.includes(e["_tag"])) {
    return cases[e["_tag"]](e);
  }
  return fail12(e);
}));
var changes = (self) => pipe(self, changesWith((x, y) => equals(y)(x)));
var changesWith = dual(2, (self, f) => {
  const writer = (last3) => readWithCause({
    onInput: (input) => {
      const [newLast, newChunk] = reduce(input, [last3, empty()], ([option2, outputs], output) => {
        if (isSome(option2) && f(option2.value, output)) {
          return [some(output), outputs];
        }
        return [some(output), pipe(outputs, append(output))];
      });
      if (isEmpty(newChunk)) return writer(newLast);
      return flatMap6(write(newChunk), () => writer(newLast));
    },
    onFailure: failCause6,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer(none()))));
});
var changesWithEffect = dual(2, (self, f) => {
  const writer = (last3) => readWithCause({
    onInput: (input) => pipe(input, reduce2([last3, empty()], ([option2, outputs], output) => {
      if (isSome(option2)) {
        return pipe(f(option2.value, output), map7((bool) => bool ? [some(output), outputs] : [some(output), pipe(outputs, append(output))]));
      }
      return succeed5([some(output), pipe(outputs, append(output))]);
    }), fromEffect2, flatMap6(([newLast, newChunk]) => pipe(write(newChunk), flatMap6(() => writer(newLast))))),
    onFailure: failCause6,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer(none()))));
});
var chunks = (self) => pipe(self, mapChunks(of));
var chunksWith = dual(2, (self, f) => flattenChunks(f(chunks(self))));
var unsome2 = (effect4) => catchAll2(asSome(effect4), (o) => o._tag === "None" ? succeedNone : fail5(o.value));
var combine2 = dual(4, (self, that, s, f) => {
  function producer(handoff, latch) {
    return fromEffect2(take6(latch)).pipe(zipRight4(readWithCause({
      onInput: (input) => flatMap6(fromEffect2(offer5(handoff, succeed2(input))), () => producer(handoff, latch)),
      onFailure: (cause) => fromEffect2(offer5(handoff, failCause(pipe(cause, map5(some))))),
      onDone: () => flatMap6(fromEffect2(offer5(handoff, fail(none()))), () => producer(handoff, latch))
    })));
  }
  return new StreamImpl(unwrapScopedWith((scope4) => all3([make18(), make18(), make18(), make18()]).pipe(tap3(([left2, _, latchL]) => toChannel2(self).pipe(concatMap(writeChunk), pipeTo(producer(left2, latchL)), runIn(scope4), forkIn2(scope4))), tap3(([, right2, _, rightL]) => toChannel2(that).pipe(concatMap(writeChunk), pipeTo(producer(right2, rightL)), runIn(scope4), forkIn2(scope4))), map7(([left2, right2, latchL, latchR]) => {
    const pullLeft = offer5(latchL, void 0).pipe(zipRight3(take6(left2).pipe(flatMap4(identity))));
    const pullRight = offer5(latchR, void 0).pipe(zipRight3(take6(right2).pipe(flatMap4(identity))));
    return toChannel2(unfoldEffect(s, (s2) => flatMap4(f(s2, pullLeft, pullRight), unsome2)));
  }))));
});
var combineChunks = dual(4, (self, that, s, f) => {
  const producer = (handoff, latch) => zipRight4(fromEffect2(take6(latch)), readWithCause({
    onInput: (input) => flatMap6(fromEffect2(pipe(handoff, offer5(chunk(input)))), () => producer(handoff, latch)),
    onFailure: (cause) => fromEffect2(offer5(handoff, failCause8(cause))),
    onDone: () => fromEffect2(offer5(handoff, end2))
  }));
  return new StreamImpl(unwrapScopedWith((scope4) => all3([make18(), make18(), make18(), make18()]).pipe(tap3(([left2, _, latchL]) => pipeTo(toChannel2(self), producer(left2, latchL)).pipe(runIn(scope4), forkIn2(scope4))), tap3(([_, right2, __, latchR]) => pipeTo(toChannel2(that), producer(right2, latchR)).pipe(runIn(scope4), forkIn2(scope4))), map7(([left2, right2, latchL, latchR]) => {
    const pullLeft = offer5(latchL, void 0).pipe(zipRight3(take6(left2).pipe(flatMap4(done3))));
    const pullRight = offer5(latchR, void 0).pipe(zipRight3(take6(right2).pipe(flatMap4(done3))));
    return toChannel2(unfoldChunkEffect(s, (s2) => flatMap4(f(s2, pullLeft, pullRight), unsome2)));
  }))));
});
var concat = dual(2, (self, that) => new StreamImpl(pipe(toChannel2(self), zipRight4(toChannel2(that)))));
var concatAll2 = (streams) => suspend9(() => pipe(streams, reduce(empty6, (x, y) => concat(y)(x))));
var cross = dual(2, (left2, right2) => pipe(left2, crossWith(right2, (a, a2) => [a, a2])));
var crossLeft = dual(2, (left2, right2) => pipe(left2, crossWith(right2, (a, _) => a)));
var crossRight = dual(2, (left2, right2) => flatMap9(left2, () => right2));
var crossWith = dual(3, (left2, right2, f) => pipe(left2, flatMap9((a) => pipe(right2, map13((b) => f(a, b))))));
var debounce = dual(2, (self, duration) => unwrapScopedWith3((scope4) => gen(function* () {
  const handoff = yield* make18();
  function enqueue(last3) {
    return sleep(duration).pipe(as2(last3), forkIn2(scope4), map7((fiber) => consumer(previous(fiber))));
  }
  const producer = readWithCause({
    onInput: (input) => match2(last(input), {
      onNone: () => producer,
      onSome: (elem) => fromEffect2(offer5(handoff, emit(of(elem)))).pipe(flatMap6(() => producer))
    }),
    onFailure: (cause) => fromEffect2(offer5(handoff, halt(cause))),
    onDone: () => fromEffect2(offer5(handoff, end(UpstreamEnd)))
  });
  function consumer(state) {
    switch (state._tag) {
      case OP_NOT_STARTED: {
        return unwrap(take6(handoff).pipe(map7((signal) => {
          switch (signal._tag) {
            case OP_EMIT4: {
              return unwrap(enqueue(signal.elements));
            }
            case OP_HALT: {
              return failCause6(signal.cause);
            }
            case OP_END: {
              return void_3;
            }
          }
        })));
      }
      case OP_PREVIOUS: {
        return unwrap(take6(handoff).pipe(forkIn2(scope4), flatMap4((handoffFiber) => raceWith(join(state.fiber), join(handoffFiber), {
          onSelfDone: (leftExit, current2) => match3(leftExit, {
            onFailure: (cause) => interrupt4(current2).pipe(as2(failCause6(cause))),
            onSuccess: (chunk2) => interrupt4(current2).pipe(zipRight3(succeed5(write(chunk2).pipe(flatMap6(() => consumer(current(handoffFiber)))))))
          }),
          onOtherDone: (rightExit, previous2) => match3(rightExit, {
            onFailure: (cause) => interrupt4(previous2).pipe(as2(failCause6(cause))),
            onSuccess: (signal) => {
              switch (signal._tag) {
                case OP_EMIT4: {
                  return interrupt4(previous2).pipe(zipRight3(enqueue(signal.elements)));
                }
                case OP_HALT: {
                  return interrupt4(previous2).pipe(as2(failCause6(signal.cause)));
                }
                case OP_END: {
                  return join(previous2).pipe(map7((chunk2) => write(chunk2).pipe(zipRight4(void_3))));
                }
              }
            }
          })
        }))));
      }
      case OP_CURRENT: {
        return unwrap(join(state.fiber).pipe(map7((signal) => {
          switch (signal._tag) {
            case OP_EMIT4: {
              return unwrap(enqueue(signal.elements));
            }
            case OP_HALT: {
              return failCause6(signal.cause);
            }
            case OP_END: {
              return void_3;
            }
          }
        })));
      }
    }
  }
  return scopedWith3((scope5) => pipeTo(toChannel2(self), producer).pipe(runIn(scope5), forkIn2(scope5))).pipe(crossRight(new StreamImpl(consumer(notStarted))));
})));
var die10 = (defect) => fromEffect6(die4(defect));
var dieSync6 = (evaluate2) => fromEffect6(dieSync2(evaluate2));
var dieMessage5 = (message) => fromEffect6(dieMessage(message));
var distributedWith = dual(2, (self, options) => pipe(make6(), flatMap4((deferred) => pipe(self, distributedWithDynamic({
  maximumLag: options.maximumLag,
  decide: (a) => flatMap4(_await(deferred), (f) => f(a))
}), flatMap4((next) => pipe(all3(map2(range(0, options.size - 1), (id) => map7(next, ([key, queue]) => [[key, id], queue]))), map7(unsafeFromArray), flatMap4((entries) => {
  const [mappings, queues] = reduceRight(entries, [/* @__PURE__ */ new Map(), empty()], ([mappings2, queues2], [mapping, queue]) => [mappings2.set(mapping[0], mapping[1]), pipe(queues2, prepend(queue))]);
  return pipe(succeed3(deferred, (a) => map7(options.decide(a), (f) => (key) => f(mappings.get(key)))), as2(Array.from(queues)));
})))))));
var distributedWithDynamicId = {
  ref: 0
};
var newDistributedWithDynamicId = () => {
  const current2 = distributedWithDynamicId.ref;
  distributedWithDynamicId.ref = current2 + 1;
  return current2;
};
var distributedWithDynamic = dual(2, (self, options) => distributedWithDynamicCallback(self, options.maximumLag, options.decide, () => _void));
var distributedWithDynamicCallback = dual(4, (self, maximumLag, decide, done4) => pipe(acquireRelease2(make5(/* @__PURE__ */ new Map()), (ref, _) => pipe(get3(ref), flatMap4((queues) => pipe(queues.values(), forEach2(shutdown))))), flatMap4((queuesRef) => gen(function* () {
  const offer6 = (a) => pipe(decide(a), flatMap4((shouldProcess) => pipe(get3(queuesRef), flatMap4((queues) => pipe(queues.entries(), reduce2(empty(), (acc, [id, queue]) => {
    if (shouldProcess(id)) {
      return pipe(offer2(queue, succeed2(a)), matchCauseEffect({
        onFailure: (cause) => (
          // Ignore all downstream queues that were shut
          // down and remove them later
          isInterrupted(cause) ? succeed5(pipe(acc, prepend(id))) : failCause4(cause)
        ),
        onSuccess: () => succeed5(acc)
      }));
    }
    return succeed5(acc);
  }), flatMap4((ids) => {
    if (isNonEmpty(ids)) {
      return update(queuesRef, (map15) => {
        for (const id of ids) {
          map15.delete(id);
        }
        return map15;
      });
    }
    return _void;
  }))))), asVoid2);
  const queuesLock = yield* makeSemaphore(1);
  const newQueue = yield* make5(pipe(bounded(maximumLag), flatMap4((queue) => {
    const id = newDistributedWithDynamicId();
    return pipe(update(queuesRef, (map15) => map15.set(id, queue)), as2([id, queue]));
  })));
  const finalize = (endTake) => (
    // Make sure that no queues are currently being added
    queuesLock.withPermits(1)(pipe(set3(newQueue, pipe(
      // All newly created queues should end immediately
      bounded(1),
      tap3((queue) => offer2(queue, endTake)),
      flatMap4((queue) => {
        const id = newDistributedWithDynamicId();
        return pipe(update(queuesRef, (map15) => map15.set(id, queue)), as2(make(id, queue)));
      })
    )), zipRight3(pipe(get3(queuesRef), flatMap4((map15) => pipe(fromIterable2(map15.values()), forEach2((queue) => pipe(offer2(queue, endTake), catchSomeCause((cause) => isInterrupted(cause) ? some(_void) : none()))))))), zipRight3(done4(endTake)), asVoid2))
  );
  yield* pipe(self, runForEachScoped(offer6), matchCauseEffect({
    onFailure: (cause) => finalize(failCause(pipe(cause, map5(some)))),
    onSuccess: () => finalize(fail(none()))
  }), forkScoped);
  return queuesLock.withPermits(1)(flatten3(get3(newQueue)));
}))));
var drain3 = (self) => new StreamImpl(drain(toChannel2(self)));
var drainFork = dual(2, (self, that) => fromEffect6(make6()).pipe(flatMap9((backgroundDied) => scopedWith3((scope4) => toChannel2(that).pipe(drain, runIn(scope4), catchAllCause2((cause) => failCause2(backgroundDied, cause)), forkIn2(scope4))).pipe(crossRight(interruptWhenDeferred2(self, backgroundDied))))));
var drop3 = dual(2, (self, n) => {
  const loop2 = (r) => readWith({
    onInput: (input) => {
      const dropped = pipe(input, drop(r));
      const leftover2 = Math.max(0, r - input.length);
      const more = isEmpty(input) || leftover2 > 0;
      if (more) {
        return loop2(leftover2);
      }
      return pipe(write(dropped), zipRight4(identityChannel()));
    },
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2(n))));
});
var dropRight = dual(2, (self, n) => {
  if (n <= 0) {
    return identityStream();
  }
  return suspend9(() => {
    const queue = new RingBuffer(n);
    const reader = readWith({
      onInput: (input) => {
        const outputs = pipe(input, filterMap((elem) => {
          const head5 = queue.head();
          queue.put(elem);
          return head5;
        }));
        return pipe(write(outputs), flatMap6(() => reader));
      },
      onFailure: fail7,
      onDone: () => void_3
    });
    return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(reader)));
  });
});
var dropUntil3 = dual(2, (self, predicate) => drop3(dropWhile4(self, (a) => !predicate(a)), 1));
var dropUntilEffect2 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => pipe(dropUntil(input, predicate), map7(unsafeFromArray), map7((leftover2) => {
      const more = isEmpty(leftover2);
      if (more) {
        return suspend5(() => loop2);
      }
      return pipe(write(leftover2), zipRight4(identityChannel()));
    }), unwrap),
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var dropWhile4 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => {
      const output = dropWhile(input, predicate);
      if (isEmpty(output)) {
        return suspend5(() => loop2);
      }
      return zipRight4(write(output), identityChannel());
    },
    onFailure: fail7,
    onDone: succeedNow
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), loop2));
});
var dropWhileEffect2 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => pipe(dropWhile2(input, predicate), map7(unsafeFromArray), map7((leftover2) => {
      const more = isEmpty(leftover2);
      if (more) {
        return suspend5(() => loop2);
      }
      return zipRight4(write(leftover2), identityChannel());
    }), unwrap),
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), loop2));
});
var either2 = (self) => pipe(self, map13(right), catchAll6((error) => make20(left(error))));
var empty6 = new StreamImpl(void_3);
var ensuring6 = dual(2, (self, finalizer3) => new StreamImpl(pipe(toChannel2(self), ensuring3(finalizer3))));
var ensuringWith3 = dual(2, (self, finalizer3) => new StreamImpl(ensuringWith(toChannel2(self), finalizer3)));
var context7 = () => fromEffect6(context2());
var contextWith5 = (f) => pipe(context7(), map13(f));
var contextWithEffect3 = (f) => pipe(context7(), mapEffectSequential(f));
var contextWithStream = (f) => pipe(context7(), flatMap9(f));
var execute = (effect4) => drain3(fromEffect6(effect4));
var fail12 = (error) => fromEffectOption(fail5(some(error)));
var failSync7 = (evaluate2) => fromEffectOption(failSync2(() => some(evaluate2())));
var failCause10 = (cause) => fromEffect6(failCause4(cause));
var failCauseSync6 = (evaluate2) => fromEffect6(failCauseSync2(evaluate2));
var filter4 = dual(2, (self, predicate) => mapChunks(self, filter(predicate)));
var filterEffect = dual(2, (self, f) => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (input) => loop2(input[Symbol.iterator]()),
        onFailure: failCause6,
        onDone: succeed7
      });
    } else {
      return pipe(f(next.value), map7((bool) => bool ? pipe(write(of(next.value)), flatMap6(() => loop2(iterator))) : loop2(iterator)), unwrap);
    }
  };
  return new StreamImpl(suspend5(() => pipe(toChannel2(self), pipeTo(loop2(empty()[Symbol.iterator]())))));
});
var filterMap2 = dual(2, (self, pf) => mapChunks(self, filterMap(pf)));
var filterMapEffect = dual(2, (self, pf) => suspend9(() => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (input) => loop2(input[Symbol.iterator]()),
        onFailure: failCause6,
        onDone: succeed7
      });
    } else {
      return pipe(pf(next.value), match2({
        onNone: () => sync3(() => loop2(iterator)),
        onSome: map7((a2) => flatMap6(write(of(a2)), () => loop2(iterator)))
      }), unwrap);
    }
  };
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2(empty()[Symbol.iterator]()))));
}));
var filterMapWhile2 = dual(2, (self, pf) => {
  const loop2 = readWith({
    onInput: (input) => {
      const mapped = filterMapWhile(input, pf);
      if (mapped.length === input.length) {
        return pipe(write(mapped), flatMap6(() => loop2));
      }
      return write(mapped);
    },
    onFailure: fail7,
    onDone: succeed7
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var filterMapWhileEffect = dual(2, (self, pf) => suspend9(() => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (input) => loop2(input[Symbol.iterator]()),
        onFailure: failCause6,
        onDone: succeed7
      });
    } else {
      return unwrap(match2(pf(next.value), {
        onNone: () => succeed5(void_3),
        onSome: map7((a2) => flatMap6(write(of(a2)), () => loop2(iterator)))
      }));
    }
  };
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2(empty()[Symbol.iterator]()))));
}));
var finalizer = (finalizer3) => acquireRelease3(_void, () => finalizer3);
var find = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => match2(findFirst(input, predicate), {
      onNone: () => loop2,
      onSome: (n) => write(of(n))
    }),
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2)));
});
var findEffect2 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => pipe(findFirst2(input, predicate), map7(match2({
      onNone: () => loop2,
      onSome: (n) => write(of(n))
    })), unwrap),
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2)));
});
var flatMap9 = dual((args) => isStream(args[0]), (self, f, options) => {
  const bufferSize = options?.bufferSize ?? 16;
  if (options?.switch) {
    return matchConcurrency(options?.concurrency, () => flatMapParSwitchBuffer(self, 1, bufferSize, f), (n) => flatMapParSwitchBuffer(self, n, bufferSize, f));
  }
  return matchConcurrency(options?.concurrency, () => new StreamImpl(concatMap(toChannel2(self), (as8) => pipe(as8, map2((a) => toChannel2(f(a))), reduce(void_3, (left2, right2) => pipe(left2, zipRight4(right2)))))), (_) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mergeMap((out) => toChannel2(f(out)), options))));
});
var matchConcurrency = (concurrency, sequential3, bounded8) => {
  switch (concurrency) {
    case void 0:
      return sequential3();
    case "unbounded":
      return bounded8(Number.MAX_SAFE_INTEGER);
    default:
      return concurrency > 1 ? bounded8(concurrency) : sequential3();
  }
};
var flatMapParSwitchBuffer = dual(4, (self, n, bufferSize, f) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mergeMap((out) => toChannel2(f(out)), {
  concurrency: n,
  mergeStrategy: BufferSliding(),
  bufferSize
}))));
var flatten7 = dual((args) => isStream(args[0]), (self, options) => flatMap9(self, identity, options));
var flattenChunks = (self) => {
  const flatten9 = readWithCause({
    onInput: (chunks3) => flatMap6(writeChunk(chunks3), () => flatten9),
    onFailure: failCause6,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(flatten9)));
};
var flattenEffect = dual((args) => isStream(args[0]), (self, options) => options?.unordered ? flatMap9(self, (a) => fromEffect6(a), {
  concurrency: options.concurrency
}) : matchConcurrency(options?.concurrency, () => mapEffectSequential(self, identity), (n) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mapOutEffectPar(identity, n), mapOut(of)))));
var flattenExitOption = (self) => {
  const processChunk = (chunk2, cont) => {
    const [toEmit, rest] = pipe(chunk2, splitWhere((exit2) => !isSuccess(exit2)));
    const next = pipe(head2(rest), match2({
      onNone: () => cont,
      onSome: match3({
        onFailure: (cause) => match2(flipCauseOption(cause), {
          onNone: () => void_3,
          onSome: failCause6
        }),
        onSuccess: () => void_3
      })
    }));
    return pipe(write(pipe(toEmit, filterMap((exit2) => isSuccess(exit2) ? some(exit2.value) : none()))), flatMap6(() => next));
  };
  const process = readWithCause({
    onInput: (chunk2) => processChunk(chunk2, process),
    onFailure: (cause) => failCause6(cause),
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(process)));
};
var flattenIterables = (self) => pipe(self, map13(fromIterable2), flattenChunks);
var flattenTake = (self) => flattenChunks(flattenExitOption(pipe(self, map13((take9) => take9.exit))));
var forever3 = (self) => new StreamImpl(repeated(toChannel2(self)));
var fromAsyncIterable = (iterable, onError4) => pipe(acquireRelease2(sync3(() => iterable[Symbol.asyncIterator]()), (iterator) => iterator.return ? promise(async () => iterator.return()) : _void), map7((iterator) => repeatEffectOption(pipe(tryPromise({
  try: async () => iterator.next(),
  catch: (reason) => some(onError4(reason))
}), flatMap4((result) => result.done ? fail5(none()) : succeed5(result.value))))), unwrapScoped5);
var fromChannel2 = (channel) => new StreamImpl(channel);
var toChannel2 = (stream) => {
  if ("channel" in stream) {
    return stream.channel;
  } else if (isEffect2(stream)) {
    return toChannel2(fromEffect6(stream));
  } else {
    throw new TypeError(`Expected a Stream.`);
  }
};
var fromChunk = (chunk2) => new StreamImpl(isEmpty(chunk2) ? void_3 : write(chunk2));
var fromChunkPubSub = (pubsub, options) => {
  if (options?.scoped) {
    const effect4 = map7(subscribe2(pubsub), fromChunkQueue);
    return options.shutdown ? map7(effect4, ensuring6(shutdown3(pubsub))) : effect4;
  }
  const stream = flatMap9(scoped5(subscribe2(pubsub)), fromChunkQueue);
  return options?.shutdown ? ensuring6(stream, shutdown3(pubsub)) : stream;
};
var fromChunkQueue = (queue, options) => pipe(take2(queue), catchAllCause2((cause) => pipe(isShutdown(queue), flatMap4((isShutdown8) => isShutdown8 && isInterrupted(cause) ? end3() : failCause9(cause)))), repeatEffectChunkOption, options?.shutdown ? ensuring6(shutdown(queue)) : identity);
var fromChunks = (...chunks3) => pipe(fromIterable3(chunks3), flatMap9(fromChunk));
var fromEffect6 = (effect4) => pipe(effect4, mapError2(some), fromEffectOption);
var fromEffectOption = (effect4) => new StreamImpl(unwrap(match5(effect4, {
  onFailure: match2({
    onNone: () => void_3,
    onSome: fail7
  }),
  onSuccess: (a) => write(of(a))
})));
var fromPubSub3 = (pubsub, options) => {
  const maxChunkSize = options?.maxChunkSize ?? DefaultChunkSize;
  if (options?.scoped) {
    const effect4 = map7(subscribe2(pubsub), (queue) => fromQueue3(queue, {
      maxChunkSize,
      shutdown: true
    }));
    return options.shutdown ? map7(effect4, ensuring6(shutdown3(pubsub))) : effect4;
  }
  const stream = flatMap9(scoped5(subscribe2(pubsub)), (queue) => fromQueue3(queue, {
    maxChunkSize
  }));
  return options?.shutdown ? ensuring6(stream, shutdown3(pubsub)) : stream;
};
var fromTPubSub = (pubsub) => {
  return unwrapScoped5(map7(subscribeScoped2(pubsub), (queue) => fromTQueue(queue)));
};
var fromIterable3 = (iterable) => suspend9(() => isChunk(iterable) ? fromChunk(iterable) : fromIteratorSucceed(iterable[Symbol.iterator]()));
var fromIterableEffect = (effect4) => pipe(effect4, map7(fromIterable3), unwrap3);
var fromIteratorSucceed = (iterator, maxChunkSize = DefaultChunkSize) => {
  return pipe(sync3(() => {
    let builder = [];
    const loop2 = (iterator2) => pipe(sync3(() => {
      let next = iterator2.next();
      if (maxChunkSize === 1) {
        if (next.done) {
          return void_3;
        }
        return pipe(write(of(next.value)), flatMap6(() => loop2(iterator2)));
      }
      builder = [];
      let count2 = 0;
      while (next.done === false) {
        builder.push(next.value);
        count2 = count2 + 1;
        if (count2 >= maxChunkSize) {
          break;
        }
        next = iterator2.next();
      }
      if (count2 > 0) {
        return pipe(write(unsafeFromArray(builder)), flatMap6(() => loop2(iterator2)));
      }
      return void_3;
    }), unwrap);
    return new StreamImpl(loop2(iterator));
  }), unwrap3);
};
var fromPull2 = (effect4) => pipe(effect4, map7(repeatEffectChunkOption), unwrapScoped5);
var fromQueue3 = (queue, options) => pipe(takeBetween(queue, 1, options?.maxChunkSize ?? DefaultChunkSize), catchAllCause2((cause) => pipe(isShutdown(queue), flatMap4((isShutdown8) => isShutdown8 && isInterrupted(cause) ? end3() : failCause9(cause)))), repeatEffectChunkOption, options?.shutdown ? ensuring6(shutdown(queue)) : identity);
var fromTQueue = (queue) => pipe(take5(queue), map7(of), catchAllCause2((cause) => pipe(isShutdown7(queue), flatMap4((isShutdown8) => isShutdown8 && isInterrupted(cause) ? end3() : failCause9(cause)))), repeatEffectChunkOption);
var fromSchedule = (schedule3) => pipe(driver(schedule3), map7((driver2) => repeatEffectOption(driver2.next(void 0))), unwrap3);
var fromReadableStream = (...args) => {
  const evaluate2 = args.length === 1 ? args[0].evaluate : args[0];
  const onError4 = args.length === 1 ? args[0].onError : args[1];
  const releaseLockOnEnd = args.length === 1 ? args[0].releaseLockOnEnd === true : false;
  return unwrapScoped5(map7(acquireRelease2(sync3(() => evaluate2().getReader()), (reader) => releaseLockOnEnd ? sync3(() => reader.releaseLock()) : promise(() => reader.cancel())), (reader) => repeatEffectOption(flatMap4(tryPromise({
    try: () => reader.read(),
    catch: (reason) => some(onError4(reason))
  }), ({
    done: done4,
    value
  }) => done4 ? fail5(none()) : succeed5(value)))));
};
var fromReadableStreamByob = (...args) => {
  const evaluate2 = args.length === 1 ? args[0].evaluate : args[0];
  const onError4 = args.length === 1 ? args[0].onError : args[1];
  const allocSize = (args.length === 1 ? args[0].bufferSize : args[2]) ?? 4096;
  const releaseLockOnEnd = args.length === 1 ? args[0].releaseLockOnEnd === true : false;
  return unwrapScoped5(map7(acquireRelease2(sync3(() => evaluate2().getReader({
    mode: "byob"
  })), (reader) => releaseLockOnEnd ? sync3(() => reader.releaseLock()) : promise(() => reader.cancel())), (reader) => catchAll6(forever3(readChunkStreamByobReader(reader, onError4, allocSize)), (error) => error === EOF ? empty6 : fail12(error))));
};
var EOF = /* @__PURE__ */ Symbol.for("effect/Stream/EOF");
var readChunkStreamByobReader = (reader, onError4, size9) => {
  const buffer4 = new ArrayBuffer(size9);
  return paginateEffect(0, (offset) => flatMap4(tryPromise({
    try: () => reader.read(new Uint8Array(buffer4, offset, buffer4.byteLength - offset)),
    catch: (reason) => onError4(reason)
  }), ({
    done: done4,
    value
  }) => {
    if (done4) {
      return fail5(EOF);
    }
    const newOffset = offset + value.byteLength;
    return succeed5([value, newOffset >= buffer4.byteLength ? none() : some(newOffset)]);
  }));
};
var groupAdjacentBy = dual(2, (self, f) => {
  const groupAdjacentByChunk = (state, chunk2) => {
    if (isEmpty(chunk2)) {
      return [state, empty()];
    }
    const builder = [];
    let from = 0;
    let until = 0;
    let key = void 0;
    let previousChunk = empty();
    switch (state._tag) {
      case "Some": {
        const tuple = state.value;
        key = tuple[0];
        let loop2 = true;
        while (loop2 && until < chunk2.length) {
          const input = unsafeGet(chunk2, until);
          const updatedKey = f(input);
          if (!equals(key, updatedKey)) {
            const previousChunk2 = tuple[1];
            const additionalChunk = unsafeFromArray(Array.from(chunk2).slice(from, until));
            const group = appendAll(previousChunk2, additionalChunk);
            builder.push([key, group]);
            key = updatedKey;
            from = until;
            loop2 = false;
          }
          until = until + 1;
        }
        if (loop2) {
          previousChunk = tuple[1];
        }
        break;
      }
      case "None": {
        key = f(unsafeGet(chunk2, until));
        until = until + 1;
        break;
      }
    }
    while (until < chunk2.length) {
      const input = unsafeGet(chunk2, until);
      const updatedKey = f(input);
      if (!equals(key, updatedKey)) {
        builder.push([key, unsafeFromArray(Array.from(chunk2).slice(from, until))]);
        key = updatedKey;
        from = until;
      }
      until = until + 1;
    }
    const nonEmptyChunk = appendAll(previousChunk, unsafeFromArray(Array.from(chunk2).slice(from, until)));
    const output = unsafeFromArray(builder);
    return [some([key, nonEmptyChunk]), output];
  };
  const groupAdjacent = (state) => readWithCause({
    onInput: (input) => {
      const [updatedState, output] = groupAdjacentByChunk(state, input);
      return isEmpty(output) ? groupAdjacent(updatedState) : flatMap6(write(output), () => groupAdjacent(updatedState));
    },
    onFailure: (cause) => match2(state, {
      onNone: () => failCause6(cause),
      onSome: (output) => flatMap6(write(of(output)), () => failCause6(cause))
    }),
    onDone: (done4) => match2(state, {
      onNone: () => succeedNow(done4),
      onSome: (output) => flatMap6(write(of(output)), () => succeedNow(done4))
    })
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), groupAdjacent(none())));
});
var grouped = dual(2, (self, chunkSize) => pipe(self, rechunk(chunkSize), chunks));
var groupedWithin = dual(3, (self, chunkSize, duration) => aggregateWithin(self, collectAllN(chunkSize), spaced(duration)));
var haltWhen = dual(2, (self, effect4) => {
  const writer = (fiber) => pipe(poll4(fiber), map7(match2({
    onNone: () => readWith({
      onInput: (input) => flatMap6(write(input), () => writer(fiber)),
      onFailure: fail7,
      onDone: () => void_3
    }),
    onSome: match3({
      onFailure: failCause6,
      onSuccess: () => void_3
    })
  })), unwrap);
  return new StreamImpl(unwrapScopedWith((scope4) => effect4.pipe(forkIn2(scope4), map7((fiber) => toChannel2(self).pipe(pipeTo(writer(fiber)))))));
});
var haltAfter = dual(2, (self, duration) => pipe(self, haltWhen(sleep(duration))));
var haltWhenDeferred = dual(2, (self, deferred) => {
  const writer = pipe(poll3(deferred), map7(match2({
    onNone: () => readWith({
      onInput: (input) => pipe(write(input), flatMap6(() => writer)),
      onFailure: fail7,
      onDone: () => void_3
    }),
    onSome: (effect4) => unwrap(match5(effect4, {
      onFailure: fail7,
      onSuccess: () => void_3
    }))
  })), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer)));
});
var identityStream = () => new StreamImpl(identityChannel());
var interleave = dual(2, (self, that) => pipe(self, interleaveWith(that, forever3(make20(true, false)))));
var interleaveWith = dual(3, (self, that, decider) => {
  const producer = (handoff) => readWithCause({
    onInput: (value) => flatMap6(fromEffect2(offer5(handoff, of2(value))), () => producer(handoff)),
    onFailure: (cause) => fromEffect2(offer5(handoff, failCause8(cause))),
    onDone: () => fromEffect2(offer5(handoff, end2))
  });
  return new StreamImpl(unwrapScopedWith((scope4) => pipe(make18(), zip2(make18()), tap3(([left2]) => toChannel2(self).pipe(concatMap(writeChunk), pipeTo(producer(left2)), runIn(scope4), forkIn2(scope4))), tap3(([_, right2]) => toChannel2(that).pipe(concatMap(writeChunk), pipeTo(producer(right2)), runIn(scope4), forkIn2(scope4))), map7(([left2, right2]) => {
    const process = (leftDone, rightDone) => readWithCause({
      onInput: (bool) => {
        if (bool && !leftDone) {
          return pipe(fromEffect2(take6(left2)), flatMap6(match17({
            onEnd: () => rightDone ? void_3 : process(true, rightDone),
            onFailure: failCause6,
            onSuccess: (chunk2) => pipe(write(chunk2), flatMap6(() => process(leftDone, rightDone)))
          })));
        }
        if (!bool && !rightDone) {
          return pipe(fromEffect2(take6(right2)), flatMap6(match17({
            onEnd: () => leftDone ? void_3 : process(leftDone, true),
            onFailure: failCause6,
            onSuccess: (chunk2) => pipe(write(chunk2), flatMap6(() => process(leftDone, rightDone)))
          })));
        }
        return process(leftDone, rightDone);
      },
      onFailure: failCause6,
      onDone: () => void_3
    });
    return pipe(toChannel2(decider), concatMap(writeChunk), pipeTo(process(false, false)));
  }))));
});
var intersperse = dual(2, (self, element) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(suspend5(() => {
  const writer = (isFirst) => readWithCause({
    onInput: (chunk2) => {
      const builder = [];
      let flagResult = isFirst;
      for (const output of chunk2) {
        if (flagResult) {
          flagResult = false;
          builder.push(output);
        } else {
          builder.push(element);
          builder.push(output);
        }
      }
      return pipe(write(unsafeFromArray(builder)), flatMap6(() => writer(flagResult)));
    },
    onFailure: failCause6,
    onDone: () => void_3
  });
  return writer(true);
})))));
var intersperseAffixes = dual(2, (self, {
  end: end4,
  middle,
  start
}) => pipe(make20(start), concat(pipe(self, intersperse(middle))), concat(make20(end4))));
var interruptAfter = dual(2, (self, duration) => pipe(self, interruptWhen2(sleep(duration))));
var interruptWhen2 = dual(2, (self, effect4) => new StreamImpl(pipe(toChannel2(self), interruptWhen(effect4))));
var interruptWhenDeferred2 = dual(2, (self, deferred) => new StreamImpl(pipe(toChannel2(self), interruptWhenDeferred(deferred))));
var iterate2 = (value, next) => unfold(value, (a) => some([a, next(a)]));
var make20 = (...as8) => fromIterable3(as8);
var map13 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOut(map2(f)))));
var mapAccum2 = dual(3, (self, s, f) => {
  const accumulator = (s2) => readWith({
    onInput: (input) => {
      const [nextS, chunk2] = mapAccum(input, s2, f);
      return flatMap6(write(chunk2), () => accumulator(nextS));
    },
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(accumulator(s))));
});
var mapAccumEffect = dual(3, (self, s, f) => suspend9(() => {
  const accumulator = (s2) => readWith({
    onInput: (input) => pipe(suspend3(() => {
      const outputs = [];
      const emit2 = (output) => sync3(() => {
        outputs.push(output);
      });
      return pipe(input, reduce2(s2, (s3, a) => pipe(f(s3, a), flatMap4(([s4, a2]) => pipe(emit2(a2), as2(s4))))), match5({
        onFailure: (error) => {
          if (outputs.length !== 0) {
            return zipRight4(write(unsafeFromArray(outputs)), fail7(error));
          }
          return fail7(error);
        },
        onSuccess: (s3) => flatMap6(write(unsafeFromArray(outputs)), () => accumulator(s3))
      }));
    }), unwrap),
    onFailure: fail7,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(accumulator(s))));
}));
var mapBoth4 = dual(2, (self, options) => pipe(self, mapError7(options.onFailure), map13(options.onSuccess)));
var mapChunks = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOut(f))));
var mapChunksEffect = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOutEffect(f))));
var mapConcat = dual(2, (self, f) => pipe(self, mapConcatChunk((a) => fromIterable2(f(a)))));
var mapConcatChunk = dual(2, (self, f) => pipe(self, mapChunks(flatMap(f))));
var mapConcatChunkEffect = dual(2, (self, f) => pipe(self, mapEffectSequential(f), mapConcatChunk(identity)));
var mapConcatEffect = dual(2, (self, f) => pipe(self, mapEffectSequential((a) => pipe(f(a), map7(fromIterable2))), mapConcatChunk(identity)));
var mapEffectSequential = dual(2, (self, f) => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (elem) => loop2(elem[Symbol.iterator]()),
        onFailure: failCause6,
        onDone: succeed7
      });
    } else {
      const value = next.value;
      return unwrap(map7(f(value), (a2) => flatMap6(write(of(a2)), () => loop2(iterator))));
    }
  };
  return new StreamImpl(pipe(toChannel2(self), pipeTo(suspend5(() => loop2(empty()[Symbol.iterator]())))));
});
var mapEffectPar = dual(3, (self, n, f) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mapOutEffectPar(f, n), mapOut(of))));
var mapError7 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapError4(f))));
var mapErrorCause2 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapErrorCause(f))));
var merge6 = dual((args) => isStream(args[1]), (self, that, options) => mergeWith2(self, that, {
  onSelf: identity,
  onOther: identity,
  haltStrategy: options?.haltStrategy
}));
var mergeAll5 = dual((args) => Symbol.iterator in args[0], (streams, options) => flatten7(fromIterable3(streams), options));
var mergeWithTag = dual(2, (streams, options) => {
  const keys = Object.keys(streams);
  const values = keys.map((key) => streams[key].pipe(map13((value) => ({
    _tag: key,
    value
  }))));
  return mergeAll5(values, options);
});
var mergeEither = dual(2, (self, that) => mergeWith2(self, that, {
  onSelf: left,
  onOther: right
}));
var mergeLeft = dual(2, (left2, right2) => pipe(left2, merge6(drain3(right2))));
var mergeRight = dual(2, (left2, right2) => pipe(drain3(left2), merge6(right2)));
var mergeWith2 = dual(3, (self, other, options) => {
  const strategy = options.haltStrategy ? fromInput2(options.haltStrategy) : Both2;
  const handler = (terminate) => (exit2) => terminate || !isSuccess(exit2) ? (
    // TODO: remove
    Done3(suspend3(() => exit2))
  ) : Await2((exit3) => suspend3(() => exit3));
  return new StreamImpl(mergeWith(toChannel2(map13(self, options.onSelf)), {
    other: toChannel2(map13(other, options.onOther)),
    onSelfDone: handler(strategy._tag === "Either" || strategy._tag === "Left"),
    onOtherDone: handler(strategy._tag === "Either" || strategy._tag === "Right")
  }));
});
var mkString2 = (self) => run2(self, mkString);
var never4 = fromEffect6(never);
var onEnd = dual(2, (self, effect4) => concat(self, drain3(fromEffect6(effect4))));
var onError2 = dual(2, (self, cleanup) => pipe(self, catchAllCause5((cause) => fromEffect6(pipe(cleanup(cause), zipRight3(failCause4(cause)))))));
var onDone = dual(2, (self, cleanup) => new StreamImpl(pipe(toChannel2(self), ensuringWith((exit2) => isSuccess(exit2) ? cleanup() : _void))));
var onStart = dual(2, (self, effect4) => unwrap3(as2(effect4, self)));
var orDie6 = (self) => pipe(self, orDieWith3(identity));
var orDieWith3 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), orDieWith(f))));
var orElse8 = dual(2, (self, that) => new StreamImpl(pipe(toChannel2(self), orElse5(() => toChannel2(that())))));
var orElseEither2 = dual(2, (self, that) => pipe(self, map13(left), orElse8(() => pipe(that(), map13(right)))));
var orElseFail2 = dual(2, (self, error) => pipe(self, orElse8(() => failSync7(error))));
var orElseIfEmpty = dual(2, (self, element) => pipe(self, orElseIfEmptyChunk(() => of(element()))));
var orElseIfEmptyChunk = dual(2, (self, chunk2) => pipe(self, orElseIfEmptyStream(() => new StreamImpl(write(chunk2())))));
var orElseIfEmptyStream = dual(2, (self, stream) => {
  const writer = readWith({
    onInput: (input) => {
      if (isEmpty(input)) {
        return suspend5(() => writer);
      }
      return pipe(write(input), zipRight4(identityChannel()));
    },
    onFailure: fail7,
    onDone: () => suspend5(() => toChannel2(stream()))
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer)));
});
var orElseSucceed2 = dual(2, (self, value) => pipe(self, orElse8(() => sync8(value))));
var paginate = (s, f) => paginateChunk(s, (s2) => {
  const page = f(s2);
  return [of(page[0]), page[1]];
});
var paginateChunk = (s, f) => {
  const loop2 = (s2) => {
    const page = f(s2);
    return match2(page[1], {
      onNone: () => zipRight4(write(page[0]), void_3),
      onSome: (s3) => flatMap6(write(page[0]), () => loop2(s3))
    });
  };
  return new StreamImpl(suspend5(() => loop2(s)));
};
var paginateChunkEffect = (s, f) => {
  const loop2 = (s2) => unwrap(map7(f(s2), ([chunk2, option2]) => match2(option2, {
    onNone: () => zipRight4(write(chunk2), void_3),
    onSome: (s3) => flatMap6(write(chunk2), () => loop2(s3))
  })));
  return new StreamImpl(suspend5(() => loop2(s)));
};
var paginateEffect = (s, f) => paginateChunkEffect(s, (s2) => pipe(f(s2), map7(([a, s3]) => [of(a), s3])));
var peel = dual(2, (self, sink) => {
  const OP_EMIT5 = "Emit";
  const OP_HALT2 = "Halt";
  const OP_END2 = "End";
  return pipe(make6(), flatMap4((deferred) => pipe(make18(), map7((handoff) => {
    const consumer = foldSink(collectLeftover(sink), {
      onFailure: (error) => zipRight5(fromEffect4(fail3(deferred, error)), fail8(error)),
      onSuccess: ([z, leftovers]) => {
        const loop2 = readWithCause({
          onInput: (elements) => flatMap6(fromEffect2(offer5(handoff, {
            _tag: OP_EMIT5,
            elements
          })), () => loop2),
          onFailure: (cause) => zipRight4(fromEffect2(offer5(handoff, {
            _tag: OP_HALT2,
            cause
          })), failCause6(cause)),
          onDone: (_) => zipRight4(fromEffect2(offer5(handoff, {
            _tag: OP_END2
          })), void_3)
        });
        return fromChannel(pipe(fromEffect2(succeed3(deferred, z)), zipRight4(fromEffect2(pipe(handoff, offer5({
          _tag: OP_EMIT5,
          elements: leftovers
        })))), zipRight4(loop2)));
      }
    });
    const producer = pipe(take6(handoff), map7((signal) => {
      switch (signal._tag) {
        case OP_EMIT5: {
          return pipe(write(signal.elements), flatMap6(() => producer));
        }
        case OP_HALT2: {
          return failCause6(signal.cause);
        }
        case OP_END2: {
          return void_3;
        }
      }
    }), unwrap);
    return pipe(self, tapErrorCause4((cause) => failCause2(deferred, cause)), run2(consumer), forkScoped, zipRight3(_await(deferred)), map7((z) => [z, new StreamImpl(producer)]));
  }))), flatten3);
});
var partition2 = dual((args) => typeof args[1] === "function", (self, predicate, options) => partitionEither(self, (a) => succeed5(predicate(a) ? right(a) : left(a)), options));
var partitionEither = dual((args) => typeof args[1] === "function", (self, predicate, options) => pipe(mapEffectSequential(self, predicate), distributedWith({
  size: 2,
  maximumLag: options?.bufferSize ?? 16,
  decide: match({
    onLeft: () => succeed5((n) => n === 0),
    onRight: () => succeed5((n) => n === 1)
  })
}), flatMap4(([queue1, queue2]) => succeed5([filterMap2(flattenExitOption(fromQueue3(queue1, {
  shutdown: true
})), (_) => match(_, {
  onLeft: some,
  onRight: none
})), filterMap2(flattenExitOption(fromQueue3(queue2, {
  shutdown: true
})), (_) => match(_, {
  onLeft: none,
  onRight: some
}))]))));
var pipeThrough = dual(2, (self, sink) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(toChannel(sink)))));
var pipeThroughChannel = dual(2, (self, channel) => new StreamImpl(pipeTo(toChannel2(self), channel)));
var pipeThroughChannelOrFail = dual(2, (self, chan) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(chan))));
var prepend2 = dual(2, (self, values) => new StreamImpl(zipRight4(write(values), toChannel2(self))));
var provideContext4 = dual(2, (self, context9) => new StreamImpl(pipe(toChannel2(self), provideContext(context9))));
var provideSomeContext2 = dual(2, (self, context9) => mapInputContext3(self, merge2(context9)));
var provideLayer2 = dual(2, (self, layer) => new StreamImpl(unwrapScopedWith((scope4) => buildWithScope2(layer, scope4).pipe(map7((env) => pipe(toChannel2(self), provideContext(env)))))));
var provideService5 = dual(3, (self, tag, resource) => provideServiceEffect2(self, tag, succeed5(resource)));
var provideServiceEffect2 = dual(3, (self, tag, effect4) => provideServiceStream(self, tag, fromEffect6(effect4)));
var provideServiceStream = dual(3, (self, tag, stream) => contextWithStream((env) => flatMap9(stream, (service3) => pipe(self, provideContext4(add(env, tag, service3))))));
var mapInputContext3 = dual(2, (self, f) => contextWithStream((env) => pipe(self, provideContext4(f(env)))));
var provideSomeLayer2 = dual(2, (self, layer) => (
  // @ts-expect-error
  // @effect-diagnostics-next-line missingEffectContext:off
  pipe(self, provideLayer2(pipe(context3(), merge4(layer))))
));
var range2 = (min, max, chunkSize = DefaultChunkSize) => suspend9(() => {
  if (min > max) {
    return empty6;
  }
  const go = (min2, max2, chunkSize2) => {
    const remaining = max2 - min2 + 1;
    if (remaining > chunkSize2) {
      return pipe(write(range(min2, min2 + chunkSize2 - 1)), flatMap6(() => go(min2 + chunkSize2, max2, chunkSize2)));
    }
    return write(range(min2, min2 + remaining - 1));
  };
  return new StreamImpl(go(min, max, chunkSize));
});
var race3 = dual(2, (left2, right2) => raceAll(left2, right2));
var raceAll = (...streams) => make6().pipe(map7((halt2) => {
  let winner = null;
  return mergeAll5(streams.map((stream, index) => stream.pipe(takeWhile3(() => {
    if (winner === null) {
      winner = index;
      unsafeDone(halt2, void_2);
      return true;
    }
    return winner === index;
  }), interruptWhen2(_await(halt2).pipe(flatMap4(() => winner === index ? never : _void))))), {
    concurrency: streams.length
  });
}), unwrap3);
var rechunk = dual(2, (self, n) => suspend9(() => {
  const target = Math.max(n, 1);
  const process = rechunkProcess(new StreamRechunker(target), target);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(process)));
}));
var rechunkProcess = (rechunker, target) => readWithCause({
  onInput: (chunk2) => {
    if (chunk2.length === target && rechunker.isEmpty()) {
      return flatMap6(write(chunk2), () => rechunkProcess(rechunker, target));
    }
    if (chunk2.length > 0) {
      const chunks3 = [];
      let result = void 0;
      let index = 0;
      while (index < chunk2.length) {
        while (index < chunk2.length && result === void 0) {
          result = rechunker.write(pipe(chunk2, unsafeGet(index)));
          index = index + 1;
        }
        if (result !== void 0) {
          chunks3.push(result);
          result = void 0;
        }
      }
      return flatMap6(writeAll(...chunks3), () => rechunkProcess(rechunker, target));
    }
    return suspend5(() => rechunkProcess(rechunker, target));
  },
  onFailure: (cause) => zipRight4(rechunker.emitIfNotEmpty(), failCause6(cause)),
  onDone: () => rechunker.emitIfNotEmpty()
});
var StreamRechunker = class {
  n;
  builder = [];
  pos = 0;
  constructor(n) {
    this.n = n;
  }
  isEmpty() {
    return this.pos === 0;
  }
  write(elem) {
    this.builder.push(elem);
    this.pos += 1;
    if (this.pos === this.n) {
      const result = unsafeFromArray(this.builder);
      this.builder = [];
      this.pos = 0;
      return result;
    }
    return void 0;
  }
  emitIfNotEmpty() {
    if (this.pos !== 0) {
      return write(unsafeFromArray(this.builder));
    }
    return void_3;
  }
};
var refineOrDie3 = dual(2, (self, pf) => pipe(self, refineOrDieWith3(pf, identity)));
var refineOrDieWith3 = dual(3, (self, pf, f) => new StreamImpl(catchAll4(toChannel2(self), (error) => match2(pf(error), {
  onNone: () => failCause6(die2(f(error))),
  onSome: fail7
}))));
var repeat2 = dual(2, (self, schedule3) => filterMap2(repeatEither(self, schedule3), (_) => match(_, {
  onLeft: none,
  onRight: some
})));
var repeatEffect = (effect4) => repeatEffectOption(pipe(effect4, mapError2(some)));
var repeatEffectChunk = (effect4) => repeatEffectChunkOption(pipe(effect4, mapError2(some)));
var repeatEffectChunkOption = (effect4) => unfoldChunkEffect(effect4, (effect5) => pipe(map7(effect5, (chunk2) => some([chunk2, effect5])), catchAll2(match2({
  onNone: () => succeed5(none()),
  onSome: fail5
}))));
var repeatEffectOption = (effect4) => repeatEffectChunkOption(pipe(effect4, map7(of)));
var repeatEither = dual(2, (self, schedule3) => repeatWith(self, schedule3, {
  onElement: (a) => right(a),
  onSchedule: left
}));
var repeatElements = dual(2, (self, schedule3) => filterMap2(repeatElementsWith(self, schedule3, {
  onElement: (a) => some(a),
  onSchedule: none
}), identity));
var repeatElementsWith = dual(3, (self, schedule3, options) => {
  const driver2 = pipe(driver(schedule3), map7((driver3) => {
    const feed = (input) => match2(head2(input), {
      onNone: () => loop2,
      onSome: (a) => zipRight4(write(of(options.onElement(a))), step(pipe(input, drop(1)), a))
    });
    const step = (input, a) => {
      const advance = pipe(driver3.next(a), as2(pipe(write(of(options.onElement(a))), flatMap6(() => step(input, a)))));
      const reset2 = pipe(driver3.last, orDie2, flatMap4((b) => pipe(driver3.reset, map7(() => pipe(write(of(options.onSchedule(b))), zipRight4(feed(input)))))));
      return pipe(advance, orElse3(() => reset2), unwrap);
    };
    const loop2 = readWith({
      onInput: feed,
      onFailure: fail7,
      onDone: () => void_3
    });
    return loop2;
  }), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(driver2)));
});
var repeatValue = (value) => new StreamImpl(repeated(write(of(value))));
var repeatWith = dual(3, (self, schedule3, options) => {
  return pipe(driver(schedule3), map7((driver2) => {
    const provideLastIterationInfo = provideServiceEffect2(CurrentIterationMetadata, get3(driver2.iterationMeta));
    const process = pipe(self, provideLastIterationInfo, map13(options.onElement), toChannel2);
    const loop2 = unwrap(match5(driver2.next(void 0), {
      onFailure: () => void_3,
      onSuccess: (output) => flatMap6(process, () => zipRight4(write(of(options.onSchedule(output))), loop2))
    }));
    return new StreamImpl(zipRight4(process, loop2));
  }), unwrap3);
});
var repeatWithSchedule = (value, schedule3) => repeatEffectWithSchedule(succeed5(value), schedule3);
var repeatEffectWithSchedule = (effect4, schedule3) => flatMap9(fromEffect6(zip2(effect4, driver(schedule3))), ([a, driver2]) => {
  const provideLastIterationInfo = provideServiceEffect(CurrentIterationMetadata, get3(driver2.iterationMeta));
  return concat(succeed11(a), unfoldEffect(a, (s) => matchEffect2(driver2.next(s), {
    onFailure: succeed5,
    onSuccess: () => map7(provideLastIterationInfo(effect4), (nextA) => some([nextA, nextA]))
  })));
});
var retry5 = dual(2, (self, policy) => driver(policy).pipe(map7((driver2) => {
  const provideLastIterationInfo = provideServiceEffect2(CurrentIterationMetadata, get3(driver2.iterationMeta));
  const loop2 = toChannel2(provideLastIterationInfo(self)).pipe(mapOutEffect((out) => as2(driver2.reset, out)), catchAll4((error) => driver2.next(error).pipe(match5({
    onFailure: () => fail7(error),
    onSuccess: () => loop2
  }), unwrap)));
  return loop2;
}), unwrap, fromChannel2));
var withExecutionPlan = dual((args) => isStream(args[0]), (self, policy, options) => suspend9(() => {
  const preventFallbackOnPartialStream = options?.preventFallbackOnPartialStream ?? false;
  let i = 0;
  let lastError = none();
  const loop2 = suspend9(() => {
    const step = policy.steps[i++];
    if (!step) {
      return fail12(getOrThrow(lastError));
    }
    let nextStream = isContext(step.provide) ? provideSomeContext2(self, step.provide) : provideSomeLayer2(self, step.provide);
    let receivedElements = false;
    if (isSome(lastError)) {
      const error = lastError.value;
      let attempted = false;
      const wrapped = nextStream;
      nextStream = suspend9(() => {
        if (attempted) return wrapped;
        attempted = true;
        return fail12(error);
      });
      nextStream = scheduleDefectRefail(retry5(nextStream, scheduleFromStep(step, false)));
    } else {
      const schedule3 = scheduleFromStep(step, true);
      nextStream = schedule3 ? scheduleDefectRefail(retry5(nextStream, schedule3)) : nextStream;
    }
    return catchAll6(preventFallbackOnPartialStream ? mapChunks(nextStream, (chunk2) => {
      receivedElements = true;
      return chunk2;
    }) : nextStream, (error) => {
      if (preventFallbackOnPartialStream && receivedElements) {
        return fail12(error);
      }
      lastError = some(error);
      return loop2;
    });
  });
  return loop2;
}));
var scheduleDefectRefail = (self) => catchAllCause5(self, (cause) => failCause10(scheduleDefectRefailCause(cause)));
var run2 = dual(2, (self, sink) => toChannel2(self).pipe(pipeToOrFail(toChannel(sink)), runDrain));
var runCollect2 = (self) => run2(self, collectAll());
var runCount = (self) => run2(self, count);
var runDrain2 = (self) => run2(self, drain2);
var runFold = dual(3, (self, s, f) => runFoldWhile(self, s, constTrue, f));
var runFoldEffect = dual(3, (self, s, f) => runFoldWhileEffect(self, s, constTrue, f));
var runFoldScoped = dual(3, (self, s, f) => pipe(self, runFoldWhileScoped(s, constTrue, f)));
var runFoldScopedEffect = dual(3, (self, s, f) => pipe(self, runFoldWhileScopedEffect(s, constTrue, f)));
var runFoldWhile = dual(4, (self, s, cont, f) => run2(self, fold(s, cont, f)));
var runFoldWhileEffect = dual(4, (self, s, cont, f) => run2(self, foldEffect(s, cont, f)));
var runFoldWhileScoped = dual(4, (self, s, cont, f) => pipe(self, runScoped2(fold(s, cont, f))));
var runFoldWhileScopedEffect = dual(4, (self, s, cont, f) => pipe(self, runScoped2(foldEffect(s, cont, f))));
var runForEach = dual(2, (self, f) => run2(self, forEach3(f)));
var runForEachChunk = dual(2, (self, f) => run2(self, forEachChunk(f)));
var runForEachChunkScoped = dual(2, (self, f) => pipe(self, runScoped2(forEachChunk(f))));
var runForEachScoped = dual(2, (self, f) => pipe(self, runScoped2(forEach3(f))));
var runForEachWhile = dual(2, (self, f) => run2(self, forEachWhile(f)));
var runForEachWhileScoped = dual(2, (self, f) => pipe(self, runScoped2(forEachWhile(f))));
var runHead = (self) => run2(self, head3());
var runIntoPubSub = dual(2, (self, pubsub) => pipe(self, runIntoQueue(pubsub)));
var runIntoPubSubScoped = dual(2, (self, pubsub) => pipe(self, runIntoQueueScoped(pubsub)));
var runIntoQueue = dual(2, (self, queue) => pipe(self, runIntoQueueScoped(queue), scoped2));
var runIntoQueueElementsScoped = dual(2, (self, queue) => {
  const writer = readWithCause({
    onInput: (input) => flatMap6(fromEffect2(offerAll2(queue, map2(input, succeed2))), () => writer),
    onFailure: (cause) => fromEffect2(offer2(queue, failCause(map5(cause, some)))),
    onDone: () => fromEffect2(offer2(queue, fail(none())))
  });
  return pipe(pipeTo(toChannel2(self), writer), drain, runScoped, asVoid2);
});
var runIntoQueueScoped = dual(2, (self, queue) => {
  const writer = readWithCause({
    onInput: (input) => flatMap6(write(chunk(input)), () => writer),
    onFailure: (cause) => write(failCause8(cause)),
    onDone: () => write(end2)
  });
  return pipe(pipeTo(toChannel2(self), writer), mapOutEffect((take9) => offer2(queue, take9)), drain, runScoped, asVoid2);
});
var runLast = (self) => run2(self, last2());
var runScoped2 = dual(2, (self, sink) => pipe(toChannel2(self), pipeToOrFail(toChannel(sink)), drain, runScoped));
var runSum = (self) => run2(self, sum);
var scan = dual(3, (self, s, f) => pipe(self, scanEffect(s, (s2, a) => succeed5(f(s2, a)))));
var scanReduce = dual(2, (self, f) => pipe(self, scanReduceEffect((a2, a) => succeed5(f(a2, a)))));
var scanReduceEffect = dual(2, (self, f) => pipe(self, mapAccumEffect(none(), (option2, a) => {
  switch (option2._tag) {
    case "None": {
      return succeed5([some(a), a]);
    }
    case "Some": {
      return pipe(f(option2.value, a), map7((b) => [some(b), b]));
    }
  }
})));
var schedule = dual(2, (self, schedule3) => filterMap2(scheduleWith(self, schedule3, {
  onElement: some,
  onSchedule: none
}), identity));
var scheduleWith = dual(3, (self, schedule3, options) => {
  const loop2 = (driver2, iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (chunk2) => loop2(driver2, chunk2[Symbol.iterator]()),
        onFailure: failCause6,
        onDone: succeedNow
      });
    }
    return unwrap(matchEffect2(driver2.next(next.value), {
      onFailure: () => pipe(driver2.last, orDie2, map7((b) => pipe(write(make2(options.onElement(next.value), options.onSchedule(b))), flatMap6(() => loop2(driver2, iterator)))), zipLeft(driver2.reset)),
      onSuccess: () => succeed5(pipe(write(of(options.onElement(next.value))), flatMap6(() => loop2(driver2, iterator))))
    }));
  };
  return new StreamImpl(pipe(fromEffect2(driver(schedule3)), flatMap6((driver2) => pipe(toChannel2(self), pipeTo(loop2(driver2, empty()[Symbol.iterator]()))))));
});
var scanEffect = dual(3, (self, s, f) => new StreamImpl(pipe(write(of(s)), flatMap6(() => toChannel2(pipe(self, mapAccumEffect(s, (s2, a) => pipe(f(s2, a), map7((s3) => [s3, s3])))))))));
var scoped5 = (effect4) => new StreamImpl(ensuring3(scoped4(pipe(effect4, map7(of))), _void));
var scopedWith3 = (f) => new StreamImpl(scopedWith2((scope4) => f(scope4).pipe(map7(of))));
var some4 = (self) => pipe(self, mapError7(some), someOrFail(() => none()));
var someOrElse = dual(2, (self, fallback) => pipe(self, map13(getOrElse(fallback))));
var someOrFail = dual(2, (self, error) => mapEffectSequential(self, match2({
  onNone: () => failSync2(error),
  onSome: succeed5
})));
var sliding8 = dual(2, (self, chunkSize) => slidingSize(self, chunkSize, 1));
var slidingSize = dual(3, (self, chunkSize, stepSize) => {
  if (chunkSize <= 0 || stepSize <= 0) {
    return die10(new IllegalArgumentException("Invalid bounds - `chunkSize` and `stepSize` must be greater than zero"));
  }
  return new StreamImpl(suspend5(() => {
    const queue = new RingBuffer(chunkSize);
    const emitOnStreamEnd = (queueSize, channelEnd) => {
      if (queueSize < chunkSize) {
        const items = queue.toChunk();
        const result2 = isEmpty(items) ? empty() : of(items);
        return pipe(write(result2), flatMap6(() => channelEnd));
      }
      const lastEmitIndex = queueSize - (queueSize - chunkSize) % stepSize;
      if (lastEmitIndex === queueSize) {
        return channelEnd;
      }
      const leftovers = queueSize - (lastEmitIndex - chunkSize + stepSize);
      const lastItems = pipe(queue.toChunk(), takeRight(leftovers));
      const result = isEmpty(lastItems) ? empty() : of(lastItems);
      return pipe(write(result), flatMap6(() => channelEnd));
    };
    const reader = (queueSize) => readWithCause({
      onInput: (input) => flatMap6(write(filterMap(input, (element, index) => {
        queue.put(element);
        const currentIndex = queueSize + index + 1;
        if (currentIndex < chunkSize || (currentIndex - chunkSize) % stepSize > 0) {
          return none();
        }
        return some(queue.toChunk());
      })), () => reader(queueSize + input.length)),
      onFailure: (cause) => emitOnStreamEnd(queueSize, failCause6(cause)),
      onDone: () => emitOnStreamEnd(queueSize, void_3)
    });
    return pipe(toChannel2(self), pipeTo(reader(0)));
  }));
});
var split = dual(2, (self, predicate) => {
  const split3 = (leftovers, input) => {
    const [chunk2, remaining] = pipe(leftovers, appendAll(input), splitWhere(predicate));
    if (isEmpty(chunk2) || isEmpty(remaining)) {
      return loop2(pipe(chunk2, appendAll(pipe(remaining, drop(1)))));
    }
    return pipe(write(of(chunk2)), flatMap6(() => split3(empty(), pipe(remaining, drop(1)))));
  };
  const loop2 = (leftovers) => readWith({
    onInput: (input) => split3(leftovers, input),
    onFailure: fail7,
    onDone: () => {
      if (isEmpty(leftovers)) {
        return void_3;
      }
      if (isNone(pipe(leftovers, findFirst(predicate)))) {
        return zipRight4(write(of(leftovers)), void_3);
      }
      return zipRight4(split3(empty(), leftovers), void_3);
    }
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2(empty()))));
});
var splitOnChunk = dual(2, (self, delimiter) => {
  const next = (leftover2, delimiterIndex) => readWithCause({
    onInput: (inputChunk) => {
      let buffer4;
      const [carry, delimiterCursor] = pipe(inputChunk, reduce([pipe(leftover2, getOrElse(() => empty())), delimiterIndex], ([carry2, delimiterCursor2], a) => {
        const concatenated = pipe(carry2, append(a));
        if (delimiterCursor2 < delimiter.length && equals(a, pipe(delimiter, unsafeGet(delimiterCursor2)))) {
          if (delimiterCursor2 + 1 === delimiter.length) {
            if (buffer4 === void 0) {
              buffer4 = [];
            }
            buffer4.push(pipe(concatenated, take(concatenated.length - delimiter.length)));
            return [empty(), 0];
          }
          return [concatenated, delimiterCursor2 + 1];
        }
        return [concatenated, equals(a, pipe(delimiter, unsafeGet(0))) ? 1 : 0];
      }));
      const output = buffer4 === void 0 ? empty() : unsafeFromArray(buffer4);
      return flatMap6(write(output), () => next(isNonEmpty(carry) ? some(carry) : none(), delimiterCursor));
    },
    onFailure: (cause) => match2(leftover2, {
      onNone: () => failCause6(cause),
      onSome: (chunk2) => zipRight4(write(of(chunk2)), failCause6(cause))
    }),
    onDone: (done4) => match2(leftover2, {
      onNone: () => succeed7(done4),
      onSome: (chunk2) => zipRight4(write(of(chunk2)), succeed7(done4))
    })
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(next(none(), 0))));
});
var splitLines2 = (self) => pipeThroughChannel(self, splitLines());
var succeed11 = (value) => fromChunk(of(value));
var sync8 = (evaluate2) => suspend9(() => fromChunk(of(evaluate2())));
var suspend9 = (stream) => new StreamImpl(suspend5(() => toChannel2(stream())));
var take7 = dual(2, (self, n) => {
  if (!Number.isInteger(n)) {
    return die10(new IllegalArgumentException(`${n} must be an integer`));
  }
  const loop2 = (n2) => readWith({
    onInput: (input) => {
      const taken = pipe(input, take(Math.min(n2, Number.POSITIVE_INFINITY)));
      const leftover2 = Math.max(0, n2 - taken.length);
      const more = leftover2 > 0;
      if (more) {
        return pipe(write(taken), flatMap6(() => loop2(leftover2)));
      }
      return write(taken);
    },
    onFailure: fail7,
    onDone: succeed7
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(0 < n ? loop2(n) : void_3)));
});
var takeRight2 = dual(2, (self, n) => {
  if (n <= 0) {
    return empty6;
  }
  return new StreamImpl(pipe(succeed5(new RingBuffer(n)), map7((queue) => {
    const reader = readWith({
      onInput: (input) => {
        for (const element of input) {
          queue.put(element);
        }
        return reader;
      },
      onFailure: fail7,
      onDone: () => pipe(write(queue.toChunk()), zipRight4(void_3))
    });
    return pipe(toChannel2(self), pipeTo(reader));
  }), unwrap));
});
var takeUntil = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => {
      const taken = pipe(input, takeWhile((a) => !predicate(a)));
      const last3 = pipe(input, drop(taken.length), take(1));
      if (isEmpty(last3)) {
        return pipe(write(taken), flatMap6(() => loop2));
      }
      return write(pipe(taken, appendAll(last3)));
    },
    onFailure: fail7,
    onDone: succeed7
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var takeUntilEffect = dual(2, (self, predicate) => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (elem) => loop2(elem[Symbol.iterator]()),
        onFailure: failCause6,
        onDone: succeed7
      });
    }
    return pipe(predicate(next.value), map7((bool) => bool ? write(of(next.value)) : pipe(write(of(next.value)), flatMap6(() => loop2(iterator)))), unwrap);
  };
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2(empty()[Symbol.iterator]()))));
});
var takeWhile3 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => {
      const taken = pipe(input, takeWhile(predicate));
      const more = taken.length === input.length;
      if (more) {
        return pipe(write(taken), flatMap6(() => loop2));
      }
      return write(taken);
    },
    onFailure: fail7,
    onDone: succeed7
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var tap7 = dual(2, (self, f) => mapEffectSequential(self, (a) => as2(f(a), a)));
var tapBoth2 = dual(2, (self, options) => pipe(self, tapError4(options.onFailure), tap7(options.onSuccess)));
var tapError4 = dual(2, (self, f) => catchAll6(self, (error) => fromEffect6(zipRight3(f(error), fail5(error)))));
var tapErrorCause4 = dual(2, (self, f) => {
  const loop2 = readWithCause({
    onInput: (chunk2) => flatMap6(write(chunk2), () => loop2),
    onFailure: (cause) => fromEffect2(zipRight3(f(cause), failCause4(cause))),
    onDone: succeedNow
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2)));
});
var tapSink = dual(2, (self, sink) => pipe(fromEffect6(all3([bounded(1), make6()])), flatMap9(([queue, deferred]) => {
  const right2 = flattenTake(fromQueue3(queue, {
    maxChunkSize: 1
  }));
  const loop2 = readWithCause({
    onInput: (chunk2) => pipe(fromEffect2(offer2(queue, chunk(chunk2))), foldCauseChannel({
      onFailure: () => flatMap6(write(chunk2), () => identityChannel()),
      onSuccess: () => flatMap6(write(chunk2), () => loop2)
    })),
    onFailure: (cause) => pipe(fromEffect2(offer2(queue, failCause8(cause))), foldCauseChannel({
      onFailure: () => failCause6(cause),
      onSuccess: () => failCause6(cause)
    })),
    onDone: () => pipe(fromEffect2(offer2(queue, end2)), foldCauseChannel({
      onFailure: () => void_3,
      onSuccess: () => void_3
    }))
  });
  return pipe(new StreamImpl(pipe(pipeTo(toChannel2(self), loop2), ensuring3(zipRight3(forkDaemon(offer2(queue, end2)), _await(deferred))))), merge6(execute(pipe(run2(right2, sink), ensuring2(zipRight3(shutdown(queue), succeed3(deferred, void 0)))))));
})));
var throttle = dual(2, (self, options) => throttleEffect(self, {
  ...options,
  cost: (chunk2) => succeed5(options.cost(chunk2))
}));
var throttleEffect = dual(2, (self, options) => {
  if (options.strategy === "enforce") {
    return throttleEnforceEffect(self, options.cost, options.units, options.duration, options.burst ?? 0);
  }
  return throttleShapeEffect(self, options.cost, options.units, options.duration, options.burst ?? 0);
});
var throttleEnforceEffect = (self, cost, units, duration, burst) => {
  const loop2 = (tokens, timestampMillis) => readWithCause({
    onInput: (input) => pipe(cost(input), zip2(currentTimeMillis), map7(([weight, currentTimeMillis2]) => {
      const elapsed = currentTimeMillis2 - timestampMillis;
      const cycles = elapsed / toMillis(duration);
      const sum2 = tokens + cycles * units;
      const max = units + burst < 0 ? Number.POSITIVE_INFINITY : units + burst;
      const available = sum2 < 0 ? max : Math.min(sum2, max);
      if (weight <= available) {
        return pipe(write(input), flatMap6(() => loop2(available - weight, currentTimeMillis2)));
      }
      return loop2(tokens, timestampMillis);
    }), unwrap),
    onFailure: failCause6,
    onDone: () => void_3
  });
  const throttled = pipe(currentTimeMillis, map7((currentTimeMillis2) => loop2(units, currentTimeMillis2)), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(throttled)));
};
var throttleShapeEffect = (self, costFn, units, duration, burst) => {
  const loop2 = (tokens, timestampMillis) => readWithCause({
    onInput: (input) => pipe(costFn(input), zip2(currentTimeMillis), map7(([weight, currentTimeMillis2]) => {
      const elapsed = currentTimeMillis2 - timestampMillis;
      const cycles = elapsed / toMillis(duration);
      const sum2 = tokens + cycles * units;
      const max = units + burst < 0 ? Number.POSITIVE_INFINITY : units + burst;
      const available = sum2 < 0 ? max : Math.min(sum2, max);
      const remaining = available - weight;
      const waitCycles = remaining >= 0 ? 0 : -remaining / units;
      const delay = millis(Math.max(0, waitCycles * toMillis(duration)));
      if (greaterThan(delay, zero)) {
        return pipe(fromEffect2(sleep(delay)), zipRight4(write(input)), flatMap6(() => loop2(remaining, currentTimeMillis2)));
      }
      return flatMap6(write(input), () => loop2(remaining, currentTimeMillis2));
    }), unwrap),
    onFailure: failCause6,
    onDone: () => void_3
  });
  const throttled = pipe(currentTimeMillis, map7((currentTimeMillis2) => loop2(units, currentTimeMillis2)), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(throttled)));
};
var tick = (interval) => repeatWithSchedule(void 0, spaced(interval));
var timeout = dual(2, (self, duration) => pipe(toPull2(self), map7(timeoutFail({
  onTimeout: () => none(),
  duration
})), fromPull2));
var timeoutFail2 = dual(3, (self, error, duration) => pipe(self, timeoutTo(duration, failSync7(error))));
var timeoutFailCause2 = dual(3, (self, cause, duration) => pipe(toPull2(self), map7(timeoutFailCause({
  onTimeout: () => map5(cause(), some),
  duration
})), fromPull2));
var timeoutTo = dual(3, (self, duration, that) => {
  const StreamTimeout = new RuntimeException("Stream Timeout");
  return pipe(self, timeoutFailCause2(() => die2(StreamTimeout), duration), catchSomeCause2((cause) => isDieType(cause) && isRuntimeException(cause.defect) && cause.defect.message !== void 0 && cause.defect.message === "Stream Timeout" ? some(that) : none()));
});
var pubsubFromOptions = (options) => {
  if (typeof options === "number") {
    return bounded3(options);
  } else if (options.capacity === "unbounded") {
    return unbounded4({
      replay: options.replay
    });
  }
  switch (options.strategy) {
    case "dropping":
      return dropping3(options);
    case "sliding":
      return sliding3(options);
    default:
      return bounded3(options);
  }
};
var toPubSub2 = dual(2, (self, capacity8) => pipe(acquireRelease2(pubsubFromOptions(capacity8), (pubsub) => shutdown3(pubsub)), tap3((pubsub) => pipe(self, runIntoPubSubScoped(pubsub), forkScoped))));
var toPull2 = (self) => map7(toPull(toChannel2(self)), (pull) => pipe(pull, mapError2(some), flatMap4(match({
  onLeft: () => fail5(none()),
  onRight: succeed5
}))));
var toQueue2 = dual((args) => isStream(args[0]), (self, options) => tap3(acquireRelease2(options?.strategy === "unbounded" ? unbounded2() : options?.strategy === "dropping" ? dropping(options.capacity ?? 2) : options?.strategy === "sliding" ? sliding(options.capacity ?? 2) : bounded(options?.capacity ?? 2), (queue) => shutdown(queue)), (queue) => forkScoped(runIntoQueueScoped(self, queue))));
var toQueueOfElements = dual((args) => isStream(args[0]), (self, options) => tap3(acquireRelease2(bounded(options?.capacity ?? 2), (queue) => shutdown(queue)), (queue) => forkScoped(runIntoQueueElementsScoped(self, queue))));
var toReadableStream = dual((args) => isStream(args[0]), (self, options) => toReadableStreamRuntime(self, defaultRuntime2, options));
var toReadableStreamEffect = dual((args) => isStream(args[0]), (self, options) => map7(runtime(), (runtime2) => toReadableStreamRuntime(self, runtime2, options)));
var toReadableStreamRuntime = dual((args) => isStream(args[0]), (self, runtime2, options) => {
  const runFork3 = runFork2(runtime2);
  let currentResolve = void 0;
  let fiber = void 0;
  const latch = unsafeMakeLatch(false);
  return new ReadableStream({
    start(controller) {
      fiber = runFork3(runForEachChunk(self, (chunk2) => {
        if (chunk2.length === 0) return _void;
        return latch.whenOpen(sync3(() => {
          latch.unsafeClose();
          for (const item of chunk2) {
            controller.enqueue(item);
          }
          currentResolve();
          currentResolve = void 0;
        }));
      }));
      fiber.addObserver((exit2) => {
        try {
          if (exit2._tag === "Failure") {
            controller.error(squash(exit2.cause));
          } else {
            controller.close();
          }
        } catch {
        }
      });
    },
    pull() {
      return new Promise((resolve) => {
        currentResolve = resolve;
        runSync(latch.open);
      });
    },
    cancel() {
      if (!fiber) return;
      return runPromise(asVoid2(interrupt4(fiber)));
    }
  }, options?.strategy);
});
var transduce = dual(2, (self, sink) => {
  const newChannel = suspend5(() => {
    const leftovers = {
      ref: empty()
    };
    const upstreamDone = {
      ref: false
    };
    const buffer4 = suspend5(() => {
      const leftover2 = leftovers.ref;
      if (isEmpty(leftover2)) {
        return readWith({
          onInput: (input) => pipe(write(input), flatMap6(() => buffer4)),
          onFailure: fail7,
          onDone: succeedNow
        });
      }
      leftovers.ref = empty();
      return pipe(writeChunk(leftover2), flatMap6(() => buffer4));
    });
    const concatAndGet = (chunk2) => {
      const leftover2 = leftovers.ref;
      const concatenated = appendAll(leftover2, filter(chunk2, (chunk3) => chunk3.length !== 0));
      leftovers.ref = concatenated;
      return concatenated;
    };
    const upstreamMarker = readWith({
      onInput: (input) => flatMap6(write(input), () => upstreamMarker),
      onFailure: fail7,
      onDone: (done4) => zipRight4(sync5(() => {
        upstreamDone.ref = true;
      }), succeedNow(done4))
    });
    const transducer = pipe(sink, toChannel, collectElements, flatMap6(([leftover2, z]) => pipe(succeed7([upstreamDone.ref, concatAndGet(leftover2)]), flatMap6(([done4, newLeftovers]) => {
      const nextChannel = done4 && isEmpty(newLeftovers) ? void_3 : transducer;
      return pipe(write(of(z)), flatMap6(() => nextChannel));
    }))));
    return pipe(toChannel2(self), pipeTo(upstreamMarker), pipeTo(buffer4), pipeToOrFail(transducer));
  });
  return new StreamImpl(newChannel);
});
var toAsyncIterableRuntime = dual((args) => isStream(args[0]), (self, runtime2) => {
  const runFork3 = runFork2(runtime2);
  return {
    [Symbol.asyncIterator]() {
      let currentResolve = void 0;
      let currentReject = void 0;
      let fiber = void 0;
      const latch = unsafeMakeLatch(false);
      let returned = false;
      return {
        next() {
          if (!fiber) {
            fiber = runFork3(runForEach(self, (value) => latch.whenOpen(sync3(() => {
              latch.unsafeClose();
              currentResolve({
                done: false,
                value
              });
              currentResolve = currentReject = void 0;
            }))));
            fiber.addObserver((exit2) => {
              if (returned) return;
              fiber = runFork(latch.whenOpen(sync3(() => {
                if (exit2._tag === "Failure") {
                  currentReject(squash(exit2.cause));
                } else {
                  currentResolve({
                    done: true,
                    value: void 0
                  });
                }
                currentResolve = currentReject = void 0;
              })));
            });
          }
          return new Promise((resolve, reject2) => {
            currentResolve = resolve;
            currentReject = reject2;
            latch.unsafeOpen();
          });
        },
        return() {
          returned = true;
          if (!fiber) return Promise.resolve({
            done: true,
            value: void 0
          });
          return runPromise(as2(interrupt4(fiber), {
            done: true,
            value: void 0
          }));
        }
      };
    }
  };
});
var toAsyncIterable = (self) => toAsyncIterableRuntime(self, defaultRuntime2);
var toAsyncIterableEffect = (self) => map7(runtime(), (runtime2) => toAsyncIterableRuntime(self, runtime2));
var unfold = (s, f) => unfoldChunk(s, (s2) => pipe(f(s2), map(([a, s3]) => [of(a), s3])));
var unfoldChunk = (s, f) => {
  const loop2 = (s2) => match2(f(s2), {
    onNone: () => void_3,
    onSome: ([chunk2, s3]) => flatMap6(write(chunk2), () => loop2(s3))
  });
  return new StreamImpl(suspend5(() => loop2(s)));
};
var unfoldChunkEffect = (s, f) => suspend9(() => {
  const loop2 = (s2) => unwrap(map7(f(s2), match2({
    onNone: () => void_3,
    onSome: ([chunk2, s3]) => flatMap6(write(chunk2), () => loop2(s3))
  })));
  return new StreamImpl(loop2(s));
});
var unfoldEffect = (s, f) => unfoldChunkEffect(s, (s2) => pipe(f(s2), map7(map(([a, s3]) => [of(a), s3]))));
var void_6 = succeed11(void 0);
var unwrap3 = (effect4) => flatten7(fromEffect6(effect4));
var unwrapScoped5 = (effect4) => flatten7(scoped5(effect4));
var unwrapScopedWith3 = (f) => flatten7(scopedWith3((scope4) => f(scope4)));
var updateService3 = dual(3, (self, tag, f) => pipe(self, mapInputContext3((context9) => pipe(context9, add(tag, f(pipe(context9, unsafeGet2(tag))))))));
var when3 = dual(2, (self, test) => pipe(self, whenEffect2(sync3(test))));
var whenCase = (evaluate2, pf) => whenCaseEffect(pf)(sync3(evaluate2));
var whenCaseEffect = dual(2, (self, pf) => pipe(fromEffect6(self), flatMap9((a) => pipe(pf(a), getOrElse(() => empty6)))));
var whenEffect2 = dual(2, (self, effect4) => pipe(fromEffect6(effect4), flatMap9((bool) => bool ? self : empty6)));
var withSpan4 = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return new StreamImpl(withSpan3(toChannel2(self), name, options));
  }
  return (self) => new StreamImpl(withSpan3(toChannel2(self), name, options));
};
var zip6 = dual(2, (self, that) => pipe(self, zipWith7(that, (a, a2) => [a, a2])));
var zipFlatten = dual(2, (self, that) => pipe(self, zipWith7(that, (a, a2) => [...a, a2])));
var zipAll = dual(2, (self, options) => zipAllWith(self, {
  other: options.other,
  onSelf: (a) => [a, options.defaultOther],
  onOther: (a2) => [options.defaultSelf, a2],
  onBoth: (a, a2) => [a, a2]
}));
var zipAllLeft = dual(3, (self, other, defaultSelf) => zipAllWith(self, {
  other,
  onSelf: identity,
  onOther: () => defaultSelf,
  onBoth: (a) => a
}));
var zipAllRight = dual(3, (self, other, defaultRight) => zipAllWith(self, {
  other,
  onSelf: () => defaultRight,
  onOther: identity,
  onBoth: (_, a2) => a2
}));
var zipAllSortedByKey = dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: (a) => [a, options.defaultOther],
  onOther: (a2) => [options.defaultSelf, a2],
  onBoth: (a, a2) => [a, a2],
  order: options.order
}));
var zipAllSortedByKeyLeft = dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: identity,
  onOther: () => options.defaultSelf,
  onBoth: (a) => a,
  order: options.order
}));
var zipAllSortedByKeyRight = dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: () => options.defaultOther,
  onOther: identity,
  onBoth: (_, a2) => a2,
  order: options.order
}));
var zipAllSortedByKeyWith = dual(2, (self, options) => {
  const pull = (state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_DRAIN_LEFT: {
        return pipe(pullLeft, match5({
          onFailure: fail,
          onSuccess: (leftChunk) => succeed2([map2(leftChunk, ([k, a]) => [k, options.onSelf(a)]), DrainLeft])
        }));
      }
      case OP_DRAIN_RIGHT: {
        return pipe(pullRight, match5({
          onFailure: fail,
          onSuccess: (rightChunk) => succeed2([map2(rightChunk, ([k, a2]) => [k, options.onOther(a2)]), DrainRight])
        }));
      }
      case OP_PULL_BOTH: {
        return pipe(unsome2(pullLeft), zip2(unsome2(pullRight), {
          concurrent: true
        }), matchEffect2({
          onFailure: (error) => succeed5(fail(some(error))),
          onSuccess: ([leftOption, rightOption]) => {
            if (isSome(leftOption) && isSome(rightOption)) {
              if (isEmpty(leftOption.value) && isEmpty(rightOption.value)) {
                return pull(PullBoth, pullLeft, pullRight);
              }
              if (isEmpty(leftOption.value)) {
                return pull(PullLeft(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty(rightOption.value)) {
                return pull(PullRight(leftOption.value), pullLeft, pullRight);
              }
              return succeed5(succeed2(merge8(leftOption.value, rightOption.value)));
            }
            if (isSome(leftOption) && isNone(rightOption)) {
              if (isEmpty(leftOption.value)) {
                return pull(DrainLeft, pullLeft, pullRight);
              }
              return succeed5(succeed2([pipe(leftOption.value, map2(([k, a]) => [k, options.onSelf(a)])), DrainLeft]));
            }
            if (isNone(leftOption) && isSome(rightOption)) {
              if (isEmpty(rightOption.value)) {
                return pull(DrainRight, pullLeft, pullRight);
              }
              return succeed5(succeed2([pipe(rightOption.value, map2(([k, a2]) => [k, options.onOther(a2)])), DrainRight]));
            }
            return succeed5(fail(none()));
          }
        }));
      }
      case OP_PULL_LEFT: {
        return matchEffect2(pullLeft, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([pipe(state.rightChunk, map2(([k, a2]) => [k, options.onOther(a2)])), DrainRight])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (leftChunk) => isEmpty(leftChunk) ? pull(PullLeft(state.rightChunk), pullLeft, pullRight) : succeed5(succeed2(merge8(leftChunk, state.rightChunk)))
        });
      }
      case OP_PULL_RIGHT: {
        return matchEffect2(pullRight, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([map2(state.leftChunk, ([k, a]) => [k, options.onSelf(a)]), DrainLeft])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (rightChunk) => isEmpty(rightChunk) ? pull(PullRight(state.leftChunk), pullLeft, pullRight) : succeed5(succeed2(merge8(state.leftChunk, rightChunk)))
        });
      }
    }
  };
  const merge8 = (leftChunk, rightChunk) => {
    const hasNext = (chunk2, index) => index < chunk2.length - 1;
    const builder = [];
    let state = void 0;
    let leftIndex = 0;
    let rightIndex = 0;
    let leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
    let rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
    let k1 = leftTuple[0];
    let a = leftTuple[1];
    let k2 = rightTuple[0];
    let a2 = rightTuple[1];
    let loop2 = true;
    while (loop2) {
      const compare = options.order(k1, k2);
      if (compare === 0) {
        builder.push([k1, options.onBoth(a, a2)]);
        if (hasNext(leftChunk, leftIndex) && hasNext(rightChunk, rightIndex)) {
          leftIndex = leftIndex + 1;
          rightIndex = rightIndex + 1;
          leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
          rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
          k1 = leftTuple[0];
          a = leftTuple[1];
          k2 = rightTuple[0];
          a2 = rightTuple[1];
        } else if (hasNext(leftChunk, leftIndex)) {
          state = PullRight(pipe(leftChunk, drop(leftIndex + 1)));
          loop2 = false;
        } else if (hasNext(rightChunk, rightIndex)) {
          state = PullLeft(pipe(rightChunk, drop(rightIndex + 1)));
          loop2 = false;
        } else {
          state = PullBoth;
          loop2 = false;
        }
      } else if (compare < 0) {
        builder.push([k1, options.onSelf(a)]);
        if (hasNext(leftChunk, leftIndex)) {
          leftIndex = leftIndex + 1;
          leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
          k1 = leftTuple[0];
          a = leftTuple[1];
        } else {
          const rightBuilder = [];
          rightBuilder.push(rightTuple);
          while (hasNext(rightChunk, rightIndex)) {
            rightIndex = rightIndex + 1;
            rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
            rightBuilder.push(rightTuple);
          }
          state = PullLeft(unsafeFromArray(rightBuilder));
          loop2 = false;
        }
      } else {
        builder.push([k2, options.onOther(a2)]);
        if (hasNext(rightChunk, rightIndex)) {
          rightIndex = rightIndex + 1;
          rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
          k2 = rightTuple[0];
          a2 = rightTuple[1];
        } else {
          const leftBuilder = [];
          leftBuilder.push(leftTuple);
          while (hasNext(leftChunk, leftIndex)) {
            leftIndex = leftIndex + 1;
            leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
            leftBuilder.push(leftTuple);
          }
          state = PullRight(unsafeFromArray(leftBuilder));
          loop2 = false;
        }
      }
    }
    return [unsafeFromArray(builder), state];
  };
  return combineChunks(self, options.other, PullBoth, pull);
});
var zipAllWith = dual(2, (self, options) => {
  const pull = (state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_DRAIN_LEFT: {
        return matchEffect2(pullLeft, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (leftChunk) => succeed5(succeed2([map2(leftChunk, options.onSelf), DrainLeft]))
        });
      }
      case OP_DRAIN_RIGHT: {
        return matchEffect2(pullRight, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (rightChunk) => succeed5(succeed2([map2(rightChunk, options.onOther), DrainRight]))
        });
      }
      case OP_PULL_BOTH: {
        return pipe(unsome2(pullLeft), zip2(unsome2(pullRight), {
          concurrent: true
        }), matchEffect2({
          onFailure: (error) => succeed5(fail(some(error))),
          onSuccess: ([leftOption, rightOption]) => {
            if (isSome(leftOption) && isSome(rightOption)) {
              if (isEmpty(leftOption.value) && isEmpty(rightOption.value)) {
                return pull(PullBoth, pullLeft, pullRight);
              }
              if (isEmpty(leftOption.value)) {
                return pull(PullLeft(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty(rightOption.value)) {
                return pull(PullRight(leftOption.value), pullLeft, pullRight);
              }
              return succeed5(succeed2(zip8(leftOption.value, rightOption.value, options.onBoth)));
            }
            if (isSome(leftOption) && isNone(rightOption)) {
              return succeed5(succeed2([map2(leftOption.value, options.onSelf), DrainLeft]));
            }
            if (isNone(leftOption) && isSome(rightOption)) {
              return succeed5(succeed2([map2(rightOption.value, options.onOther), DrainRight]));
            }
            return succeed5(fail(none()));
          }
        }));
      }
      case OP_PULL_LEFT: {
        return matchEffect2(pullLeft, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([map2(state.rightChunk, options.onOther), DrainRight])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (leftChunk) => {
            if (isEmpty(leftChunk)) {
              return pull(PullLeft(state.rightChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.rightChunk)) {
              return pull(PullRight(leftChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(leftChunk, state.rightChunk, options.onBoth)));
          }
        });
      }
      case OP_PULL_RIGHT: {
        return matchEffect2(pullRight, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([map2(state.leftChunk, options.onSelf), DrainLeft])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (rightChunk) => {
            if (isEmpty(rightChunk)) {
              return pull(PullRight(state.leftChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.leftChunk)) {
              return pull(PullLeft(rightChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(state.leftChunk, rightChunk, options.onBoth)));
          }
        });
      }
    }
  };
  const zip8 = (leftChunk, rightChunk, f) => {
    const [output, either4] = zipChunks(leftChunk, rightChunk, f);
    switch (either4._tag) {
      case "Left": {
        if (isEmpty(either4.left)) {
          return [output, PullBoth];
        }
        return [output, PullRight(either4.left)];
      }
      case "Right": {
        if (isEmpty(either4.right)) {
          return [output, PullBoth];
        }
        return [output, PullLeft(either4.right)];
      }
    }
  };
  return combineChunks(self, options.other, PullBoth, pull);
});
var zipLatest = dual(2, (left2, right2) => pipe(left2, zipLatestWith(right2, (a, a2) => [a, a2])));
var zipLatestAll = (...streams) => {
  if (streams.length === 0) {
    return empty6;
  } else if (streams.length === 1) {
    return map13(streams[0], (x) => [x]);
  }
  const [head5, ...tail] = streams;
  return zipLatestWith(head5, zipLatestAll(...tail), (first2, second) => [first2, ...second]);
};
var zipLatestWith = dual(3, (left2, right2, f) => {
  const pullNonEmpty = (pull) => pipe(pull, flatMap4((chunk2) => isEmpty(chunk2) ? pullNonEmpty(pull) : succeed5(chunk2)));
  return pipe(toPull2(left2), map7(pullNonEmpty), zip2(pipe(toPull2(right2), map7(pullNonEmpty))), flatMap4(([left3, right3]) => pipe(fromEffectOption(raceWith(left3, right3, {
    onSelfDone: (leftDone, rightFiber) => pipe(suspend3(() => leftDone), zipWith3(join(rightFiber), (l, r) => [l, r, true])),
    onOtherDone: (rightDone, leftFiber) => pipe(suspend3(() => rightDone), zipWith3(join(leftFiber), (l, r) => [r, l, false]))
  })), flatMap9(([l, r, leftFirst]) => pipe(fromEffect6(make5([unsafeLast(l), unsafeLast(r)])), flatMap9((latest) => pipe(fromChunk(leftFirst ? pipe(r, map2((a2) => f(unsafeLast(l), a2))) : pipe(l, map2((a) => f(a, unsafeLast(r))))), concat(pipe(repeatEffectOption(left3), mergeEither(repeatEffectOption(right3)), mapEffectSequential(match({
    onLeft: (leftChunk) => modify(latest, ([_, rightLatest]) => [pipe(leftChunk, map2((a) => f(a, rightLatest))), [unsafeLast(leftChunk), rightLatest]]),
    onRight: (rightChunk) => modify(latest, ([leftLatest, _]) => [pipe(rightChunk, map2((a2) => f(leftLatest, a2))), [leftLatest, unsafeLast(rightChunk)]])
  })), flatMap9(fromChunk))))))), toPull2)), fromPull2);
});
var zipLeft5 = dual(2, (left2, right2) => pipe(left2, zipWithChunks(right2, (left3, right3) => {
  if (left3.length > right3.length) {
    return [pipe(left3, take(right3.length)), left(pipe(left3, take(right3.length)))];
  }
  return [left3, right(pipe(right3, drop(left3.length)))];
})));
var zipRight7 = dual(2, (left2, right2) => pipe(left2, zipWithChunks(right2, (left3, right3) => {
  if (left3.length > right3.length) {
    return [right3, left(pipe(left3, take(right3.length)))];
  }
  return [pipe(right3, take(left3.length)), right(pipe(right3, drop(left3.length)))];
})));
var zipWith7 = dual(3, (left2, right2, f) => pipe(left2, zipWithChunks(right2, (leftChunk, rightChunk) => zipChunks(leftChunk, rightChunk, f))));
var zipWithChunks = dual(3, (self, that, f) => {
  const pull = (state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_PULL_BOTH2: {
        return pipe(unsome2(pullLeft), zip2(unsome2(pullRight), {
          concurrent: true
        }), matchEffect2({
          onFailure: (error) => succeed5(fail(some(error))),
          onSuccess: ([leftOption, rightOption]) => {
            if (isSome(leftOption) && isSome(rightOption)) {
              if (isEmpty(leftOption.value) && isEmpty(rightOption.value)) {
                return pull(PullBoth2, pullLeft, pullRight);
              }
              if (isEmpty(leftOption.value)) {
                return pull(PullLeft2(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty(rightOption.value)) {
                return pull(PullRight2(leftOption.value), pullLeft, pullRight);
              }
              return succeed5(succeed2(zip8(leftOption.value, rightOption.value)));
            }
            return succeed5(fail(none()));
          }
        }));
      }
      case OP_PULL_LEFT2: {
        return matchEffect2(pullLeft, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (leftChunk) => {
            if (isEmpty(leftChunk)) {
              return pull(PullLeft2(state.rightChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.rightChunk)) {
              return pull(PullRight2(leftChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(leftChunk, state.rightChunk)));
          }
        });
      }
      case OP_PULL_RIGHT2: {
        return matchEffect2(pullRight, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (rightChunk) => {
            if (isEmpty(rightChunk)) {
              return pull(PullRight2(state.leftChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.leftChunk)) {
              return pull(PullLeft2(rightChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(state.leftChunk, rightChunk)));
          }
        });
      }
    }
  };
  const zip8 = (leftChunk, rightChunk) => {
    const [output, either4] = f(leftChunk, rightChunk);
    switch (either4._tag) {
      case "Left": {
        if (isEmpty(either4.left)) {
          return [output, PullBoth2];
        }
        return [output, PullRight2(either4.left)];
      }
      case "Right": {
        if (isEmpty(either4.right)) {
          return [output, PullBoth2];
        }
        return [output, PullLeft2(either4.right)];
      }
    }
  };
  return pipe(self, combineChunks(that, PullBoth2, pull));
});
var zipWithIndex = (self) => pipe(self, mapAccum2(0, (index, a) => [index + 1, [a, index]]));
var zipWithNext = (self) => {
  const process = (last3) => readWithCause({
    onInput: (input) => {
      const [newLast, chunk2] = mapAccum(input, last3, (prev, curr) => [some(curr), pipe(prev, map((a) => [a, curr]))]);
      const output = filterMap(chunk2, (option2) => isSome(option2) ? some([option2.value[0], some(option2.value[1])]) : none());
      return flatMap6(write(output), () => process(newLast));
    },
    onFailure: failCause6,
    onDone: () => match2(last3, {
      onNone: () => void_3,
      onSome: (value) => zipRight4(write(of([value, none()])), void_3)
    })
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(process(none()))));
};
var zipWithPrevious = (self) => pipe(self, mapAccum2(none(), (prev, curr) => [some(curr), [prev, curr]]));
var zipWithPreviousAndNext = (self) => pipe(zipWithNext(zipWithPrevious(self)), map13(([[prev, curr], next]) => [prev, curr, pipe(next, map((tuple) => tuple[1]))]));
var zipChunks = (left2, right2, f) => {
  if (left2.length > right2.length) {
    return [pipe(left2, take(right2.length), zipWith(right2, f)), left(pipe(left2, drop(right2.length)))];
  }
  return [pipe(left2, zipWith(pipe(right2, take(left2.length)), f)), right(pipe(right2, drop(left2.length)))];
};
var Do = succeed11({});
var bind3 = dual((args) => typeof args[0] !== "string", (self, tag, f, options) => flatMap9(self, (k) => map13(f(k), (a) => ({
  ...k,
  [tag]: a
})), options));
var bindTo4 = bindTo(map13);
var let_3 = let_(map13);
var channelToStream = (self) => {
  return new StreamImpl(self);
};
var decodeText = dual((args) => isStream(args[0]), (self, encoding = "utf-8") => suspend9(() => {
  const decoder = new TextDecoder(encoding);
  return map13(self, (s) => decoder.decode(s, {
    stream: true
  }));
}));
var encodeText = (self) => suspend9(() => {
  const encoder = new TextEncoder();
  return map13(self, (s) => encoder.encode(s));
});
var fromEventListener = (target, type, options) => asyncPush((emit2) => acquireRelease2(sync3(() => target.addEventListener(type, emit2.single, options)), () => sync3(() => target.removeEventListener(type, emit2.single, options))), {
  bufferSize: typeof options === "object" ? options.bufferSize : void 0
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/groupBy.js
var GroupBySymbolKey = "effect/GroupBy";
var GroupByTypeId = Symbol.for(GroupBySymbolKey);
var groupByVariance = {
  /* c8 ignore next */
  _R: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _K: (_) => _,
  /* c8 ignore next */
  _V: (_) => _
};
var isGroupBy = (u) => hasProperty(u, GroupByTypeId);
var evaluate = dual((args) => isGroupBy(args[0]), (self, f, options) => flatMap9(self.grouped, ([key, queue]) => f(key, flattenTake(fromQueue3(queue, {
  shutdown: true
}))), {
  concurrency: "unbounded",
  bufferSize: options?.bufferSize ?? 16
}));
var filter5 = dual(2, (self, predicate) => make21(pipe(self.grouped, filterEffect((tuple) => {
  if (predicate(tuple[0])) {
    return pipe(succeed5(tuple), as2(true));
  }
  return pipe(shutdown(tuple[1]), as2(false));
}))));
var first = dual(2, (self, n) => make21(pipe(zipWithIndex(self.grouped), filterEffect((tuple) => {
  const index = tuple[1];
  const queue = tuple[0][1];
  if (index < n) {
    return pipe(succeed5(tuple), as2(true));
  }
  return pipe(shutdown(queue), as2(false));
}), map13((tuple) => tuple[0]))));
var make21 = (grouped3) => ({
  [GroupByTypeId]: groupByVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  grouped: grouped3
});
var groupBy = dual((args) => isStream(args[0]), (self, f, options) => make21(unwrapScoped5(gen(function* () {
  const decider = yield* make6();
  const output = yield* acquireRelease2(bounded(options?.bufferSize ?? 16), (queue) => shutdown(queue));
  const ref = yield* make5(/* @__PURE__ */ new Map());
  const add3 = yield* pipe(mapEffectSequential(self, f), distributedWithDynamicCallback(options?.bufferSize ?? 16, ([key, value]) => flatMap4(_await(decider), (f2) => f2(key, value)), (exit2) => offer2(output, exit2)));
  yield* succeed3(decider, (key, _) => pipe(get3(ref), map7((map15) => fromNullable(map15.get(key))), flatMap4(match2({
    onNone: () => flatMap4(add3, ([index, queue]) => zipRight3(update(ref, (map15) => map15.set(key, index)), pipe(offer2(output, succeed2([key, mapDequeue(queue, (exit2) => new TakeImpl(pipe(exit2, map4((tuple) => of(tuple[1])))))])), as2((n) => n === index)))),
    onSome: (index) => succeed5((n) => n === index)
  }))));
  return flattenExitOption(fromQueue3(output, {
    shutdown: true
  }));
}))));
var mapEffectOptions = dual((args) => typeof args[0] !== "function", (self, f, options) => {
  if (options?.key) {
    return evaluate(groupByKey(self, options.key, {
      bufferSize: options.bufferSize
    }), (_, s) => mapEffectSequential(s, f));
  }
  return matchConcurrency(options?.concurrency, () => mapEffectSequential(self, f), (n) => options?.unordered ? flatMap9(self, (a) => fromEffect6(f(a)), {
    concurrency: n
  }) : mapEffectPar(self, n, f));
});
var bindEffect = dual((args) => typeof args[0] !== "string", (self, tag, f, options) => mapEffectOptions(self, (k) => map7(f(k), (a) => ({
  ...k,
  [tag]: a
})), options));
var mapDequeue = (dequeue, f) => new MapDequeue(dequeue, f);
var MapDequeue = class extends Class {
  dequeue;
  f;
  [DequeueTypeId2] = {
    _Out: (_) => _
  };
  constructor(dequeue, f) {
    super();
    this.dequeue = dequeue;
    this.f = f;
  }
  capacity() {
    return capacity(this.dequeue);
  }
  get size() {
    return size2(this.dequeue);
  }
  unsafeSize() {
    return this.dequeue.unsafeSize();
  }
  get awaitShutdown() {
    return awaitShutdown(this.dequeue);
  }
  isActive() {
    return this.dequeue.isActive();
  }
  get isShutdown() {
    return isShutdown(this.dequeue);
  }
  get shutdown() {
    return shutdown(this.dequeue);
  }
  get isFull() {
    return isFull(this.dequeue);
  }
  get isEmpty() {
    return isEmpty3(this.dequeue);
  }
  get take() {
    return pipe(take2(this.dequeue), map7((a) => this.f(a)));
  }
  get takeAll() {
    return pipe(takeAll(this.dequeue), map7(map2((a) => this.f(a))));
  }
  takeUpTo(max) {
    return pipe(takeUpTo(this.dequeue, max), map7(map2((a) => this.f(a))));
  }
  takeBetween(min, max) {
    return pipe(takeBetween(this.dequeue, min, max), map7(map2((a) => this.f(a))));
  }
  takeN(n) {
    return pipe(takeN(this.dequeue, n), map7(map2((a) => this.f(a))));
  }
  poll() {
    return pipe(poll2(this.dequeue), map7(map((a) => this.f(a))));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  commit() {
    return this.take;
  }
};
var groupByKey = dual((args) => typeof args[0] !== "function", (self, f, options) => {
  const loop2 = (map15, outerQueue) => readWithCause({
    onInput: (input) => flatMap6(fromEffect2(forEach2(groupByIterable(input, f), ([key, values]) => {
      const innerQueue = map15.get(key);
      if (innerQueue === void 0) {
        return pipe(bounded(options?.bufferSize ?? 16), flatMap4((innerQueue2) => pipe(sync3(() => {
          map15.set(key, innerQueue2);
        }), zipRight3(offer2(outerQueue, of2([key, innerQueue2]))), zipRight3(pipe(offer2(innerQueue2, chunk(values)), catchSomeCause((cause) => isInterruptedOnly(cause) ? some(_void) : none()))))));
      }
      return catchSomeCause(offer2(innerQueue, chunk(values)), (cause) => isInterruptedOnly(cause) ? some(_void) : none());
    }, {
      discard: true
    })), () => loop2(map15, outerQueue)),
    onFailure: (cause) => fromEffect2(offer2(outerQueue, failCause8(cause))),
    onDone: () => fromEffect2(pipe(forEach2(map15.entries(), ([_, innerQueue]) => pipe(offer2(innerQueue, end2), catchSomeCause((cause) => isInterruptedOnly(cause) ? some(_void) : none())), {
      discard: true
    }), zipRight3(offer2(outerQueue, end2))))
  });
  return make21(unwrapScopedWith3((scope4) => gen(function* () {
    const map15 = /* @__PURE__ */ new Map();
    const queue = yield* unbounded2();
    yield* addFinalizer2(scope4, shutdown(queue));
    return yield* toChannel2(self).pipe(pipeTo(loop2(map15, queue)), drain, runIn(scope4), forkIn2(scope4), as2(flattenTake(fromQueue3(queue, {
      shutdown: true
    }))));
  })));
});
var groupByIterable = dual(2, (iterable, f) => {
  const builder = [];
  const iterator = iterable[Symbol.iterator]();
  const map15 = /* @__PURE__ */ new Map();
  let next;
  while ((next = iterator.next()) && !next.done) {
    const value = next.value;
    const key = f(value);
    if (map15.has(key)) {
      const innerBuilder = map15.get(key);
      innerBuilder.push(value);
    } else {
      const innerBuilder = [value];
      builder.push([key, innerBuilder]);
      map15.set(key, innerBuilder);
    }
  }
  return unsafeFromArray(builder.map((tuple) => [tuple[0], unsafeFromArray(tuple[1])]));
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Stream.js
var StreamTypeId3 = StreamTypeId2;
var DefaultChunkSize2 = DefaultChunkSize;
var accumulate2 = accumulate;
var accumulateChunks2 = accumulateChunks;
var acquireRelease4 = acquireRelease3;
var aggregate2 = aggregate;
var aggregateWithin2 = aggregateWithin;
var aggregateWithinEither2 = aggregateWithinEither;
var as7 = as6;
var _async2 = _async;
var asyncEffect2 = asyncEffect;
var asyncPush2 = asyncPush;
var asyncScoped2 = asyncScoped;
var branchAfter2 = branchAfter;
var broadcast2 = broadcast;
var share2 = share;
var broadcastDynamic2 = broadcastDynamic;
var broadcastedQueues2 = broadcastedQueues;
var broadcastedQueuesDynamic2 = broadcastedQueuesDynamic;
var buffer3 = buffer2;
var bufferChunks2 = bufferChunks;
var catchAll7 = catchAll6;
var catchAllCause6 = catchAllCause5;
var catchSome3 = catchSome2;
var catchTag3 = catchTag2;
var catchTags3 = catchTags2;
var catchSomeCause3 = catchSomeCause2;
var changes2 = changes;
var changesWith2 = changesWith;
var changesWithEffect2 = changesWithEffect;
var chunks2 = chunks;
var chunksWith2 = chunksWith;
var combine3 = combine2;
var combineChunks2 = combineChunks;
var concat2 = concat;
var concatAll3 = concatAll2;
var cross2 = cross;
var crossLeft2 = crossLeft;
var crossRight2 = crossRight;
var crossWith2 = crossWith;
var debounce2 = debounce;
var die11 = die10;
var dieSync7 = dieSync6;
var dieMessage6 = dieMessage5;
var distributedWith2 = distributedWith;
var distributedWithDynamic2 = distributedWithDynamic;
var drain4 = drain3;
var drainFork2 = drainFork;
var drop4 = drop3;
var dropRight2 = dropRight;
var dropUntil4 = dropUntil3;
var dropUntilEffect3 = dropUntilEffect2;
var dropWhile5 = dropWhile4;
var dropWhileEffect3 = dropWhileEffect2;
var either3 = either2;
var empty7 = empty6;
var ensuring7 = ensuring6;
var ensuringWith4 = ensuringWith3;
var context8 = context7;
var contextWith6 = contextWith5;
var contextWithEffect4 = contextWithEffect3;
var contextWithStream2 = contextWithStream;
var execute2 = execute;
var fail13 = fail12;
var failSync8 = failSync7;
var failCause11 = failCause10;
var failCauseSync7 = failCauseSync6;
var filter6 = filter4;
var filterEffect2 = filterEffect;
var filterMap3 = filterMap2;
var filterMapEffect2 = filterMapEffect;
var filterMapWhile3 = filterMapWhile2;
var filterMapWhileEffect2 = filterMapWhileEffect;
var finalizer2 = finalizer;
var find2 = find;
var findEffect3 = findEffect2;
var flatMap10 = flatMap9;
var flatten8 = flatten7;
var flattenChunks2 = flattenChunks;
var flattenEffect2 = flattenEffect;
var flattenExitOption2 = flattenExitOption;
var flattenIterables2 = flattenIterables;
var flattenTake2 = flattenTake;
var forever4 = forever3;
var fromAsyncIterable2 = fromAsyncIterable;
var fromChannel3 = fromChannel2;
var toChannel3 = toChannel2;
var fromChunk2 = fromChunk;
var fromChunkPubSub2 = fromChunkPubSub;
var fromChunkQueue2 = fromChunkQueue;
var fromChunks2 = fromChunks;
var fromEffect7 = fromEffect6;
var fromEffectOption2 = fromEffectOption;
var fromPubSub4 = fromPubSub3;
var fromTPubSub2 = fromTPubSub;
var fromIterable4 = fromIterable3;
var fromIterableEffect2 = fromIterableEffect;
var fromIteratorSucceed2 = fromIteratorSucceed;
var fromPull3 = fromPull2;
var fromQueue4 = fromQueue3;
var fromTQueue2 = fromTQueue;
var fromReadableStream2 = fromReadableStream;
var fromReadableStreamByob2 = fromReadableStreamByob;
var fromSchedule2 = fromSchedule;
var groupAdjacentBy2 = groupAdjacentBy;
var groupBy2 = groupBy;
var groupByKey2 = groupByKey;
var grouped2 = grouped;
var groupedWithin2 = groupedWithin;
var haltAfter2 = haltAfter;
var haltWhen2 = haltWhen;
var haltWhenDeferred2 = haltWhenDeferred;
var identity2 = identityStream;
var interleave2 = interleave;
var interleaveWith2 = interleaveWith;
var intersperse2 = intersperse;
var intersperseAffixes2 = intersperseAffixes;
var interruptAfter2 = interruptAfter;
var interruptWhen3 = interruptWhen2;
var interruptWhenDeferred3 = interruptWhenDeferred2;
var iterate3 = iterate2;
var make22 = make20;
var map14 = map13;
var mapAccum3 = mapAccum2;
var mapAccumEffect2 = mapAccumEffect;
var mapBoth5 = mapBoth4;
var mapChunks2 = mapChunks;
var mapChunksEffect2 = mapChunksEffect;
var mapConcat2 = mapConcat;
var mapConcatChunk2 = mapConcatChunk;
var mapConcatChunkEffect2 = mapConcatChunkEffect;
var mapConcatEffect2 = mapConcatEffect;
var mapEffect3 = mapEffectOptions;
var mapError8 = mapError7;
var mapErrorCause3 = mapErrorCause2;
var merge7 = merge6;
var mergeAll6 = mergeAll5;
var mergeWithTag2 = mergeWithTag;
var mergeWith3 = mergeWith2;
var mergeEither2 = mergeEither;
var mergeLeft2 = mergeLeft;
var mergeRight2 = mergeRight;
var mkString3 = mkString2;
var never5 = never4;
var onEnd2 = onEnd;
var onError3 = onError2;
var onDone2 = onDone;
var onStart2 = onStart;
var orDie7 = orDie6;
var orDieWith4 = orDieWith3;
var orElse9 = orElse8;
var orElseEither3 = orElseEither2;
var orElseFail3 = orElseFail2;
var orElseIfEmpty2 = orElseIfEmpty;
var orElseIfEmptyChunk2 = orElseIfEmptyChunk;
var orElseIfEmptyStream2 = orElseIfEmptyStream;
var orElseSucceed3 = orElseSucceed2;
var paginate2 = paginate;
var paginateChunk2 = paginateChunk;
var paginateChunkEffect2 = paginateChunkEffect;
var paginateEffect2 = paginateEffect;
var partition3 = partition2;
var partitionEither2 = partitionEither;
var peel2 = peel;
var pipeThrough2 = pipeThrough;
var pipeThroughChannel2 = pipeThroughChannel;
var pipeThroughChannelOrFail2 = pipeThroughChannelOrFail;
var prepend3 = prepend2;
var provideContext5 = provideContext4;
var provideSomeContext3 = provideSomeContext2;
var provideLayer3 = provideLayer2;
var provideService6 = provideService5;
var provideServiceEffect3 = provideServiceEffect2;
var provideServiceStream2 = provideServiceStream;
var mapInputContext4 = mapInputContext3;
var provideSomeLayer3 = provideSomeLayer2;
var race4 = race3;
var raceAll2 = raceAll;
var range3 = range2;
var rechunk2 = rechunk;
var refineOrDie4 = refineOrDie3;
var refineOrDieWith4 = refineOrDieWith3;
var repeat3 = repeat2;
var repeatEffect2 = repeatEffect;
var repeatEffectChunk2 = repeatEffectChunk;
var repeatEffectChunkOption2 = repeatEffectChunkOption;
var repeatEffectOption2 = repeatEffectOption;
var repeatEffectWithSchedule2 = repeatEffectWithSchedule;
var repeatEither2 = repeatEither;
var repeatElements2 = repeatElements;
var repeatElementsWith2 = repeatElementsWith;
var repeatValue2 = repeatValue;
var repeatWith2 = repeatWith;
var retry6 = retry5;
var withExecutionPlan2 = withExecutionPlan;
var run3 = run2;
var runCollect3 = runCollect2;
var runCount2 = runCount;
var runDrain3 = runDrain2;
var runFold2 = runFold;
var runFoldEffect2 = runFoldEffect;
var runFoldScoped2 = runFoldScoped;
var runFoldScopedEffect2 = runFoldScopedEffect;
var runFoldWhile2 = runFoldWhile;
var runFoldWhileEffect2 = runFoldWhileEffect;
var runFoldWhileScoped2 = runFoldWhileScoped;
var runFoldWhileScopedEffect2 = runFoldWhileScopedEffect;
var runForEach2 = runForEach;
var runForEachChunk2 = runForEachChunk;
var runForEachChunkScoped2 = runForEachChunkScoped;
var runForEachScoped2 = runForEachScoped;
var runForEachWhile2 = runForEachWhile;
var runForEachWhileScoped2 = runForEachWhileScoped;
var runHead2 = runHead;
var runIntoPubSub2 = runIntoPubSub;
var runIntoPubSubScoped2 = runIntoPubSubScoped;
var runIntoQueue2 = runIntoQueue;
var runIntoQueueElementsScoped2 = runIntoQueueElementsScoped;
var runIntoQueueScoped2 = runIntoQueueScoped;
var runLast2 = runLast;
var runScoped3 = runScoped2;
var runSum2 = runSum;
var scan2 = scan;
var scanEffect2 = scanEffect;
var scanReduce2 = scanReduce;
var scanReduceEffect2 = scanReduceEffect;
var schedule2 = schedule;
var scheduleWith2 = scheduleWith;
var scoped6 = scoped5;
var scopedWith4 = scopedWith3;
var sliding9 = sliding8;
var slidingSize2 = slidingSize;
var some5 = some4;
var someOrElse2 = someOrElse;
var someOrFail2 = someOrFail;
var split2 = split;
var splitOnChunk2 = splitOnChunk;
var splitLines3 = splitLines2;
var succeed12 = succeed11;
var sync9 = sync8;
var suspend10 = suspend9;
var take8 = take7;
var takeRight3 = takeRight2;
var takeUntil2 = takeUntil;
var takeUntilEffect2 = takeUntilEffect;
var takeWhile4 = takeWhile3;
var tap8 = tap7;
var tapBoth3 = tapBoth2;
var tapError5 = tapError4;
var tapErrorCause5 = tapErrorCause4;
var tapSink2 = tapSink;
var throttle2 = throttle;
var throttleEffect2 = throttleEffect;
var tick2 = tick;
var timeout2 = timeout;
var timeoutFail3 = timeoutFail2;
var timeoutFailCause3 = timeoutFailCause2;
var timeoutTo2 = timeoutTo;
var toPubSub3 = toPubSub2;
var toPull3 = toPull2;
var toQueue3 = toQueue2;
var toQueueOfElements2 = toQueueOfElements;
var toReadableStream2 = toReadableStream;
var toReadableStreamEffect2 = toReadableStreamEffect;
var toReadableStreamRuntime2 = toReadableStreamRuntime;
var toAsyncIterableRuntime2 = toAsyncIterableRuntime;
var toAsyncIterableEffect2 = toAsyncIterableEffect;
var toAsyncIterable2 = toAsyncIterable;
var transduce2 = transduce;
var unfold2 = unfold;
var unfoldChunk2 = unfoldChunk;
var unfoldChunkEffect2 = unfoldChunkEffect;
var unfoldEffect2 = unfoldEffect;
var void_7 = void_6;
var unwrap4 = unwrap3;
var unwrapScoped6 = unwrapScoped5;
var unwrapScopedWith4 = unwrapScopedWith3;
var updateService4 = updateService3;
var when4 = when3;
var whenCase2 = whenCase;
var whenCaseEffect2 = whenCaseEffect;
var whenEffect3 = whenEffect2;
var withSpan5 = withSpan4;
var zip7 = zip6;
var zipFlatten2 = zipFlatten;
var zipAll2 = zipAll;
var zipAllLeft2 = zipAllLeft;
var zipAllRight2 = zipAllRight;
var zipAllSortedByKey2 = zipAllSortedByKey;
var zipAllSortedByKeyLeft2 = zipAllSortedByKeyLeft;
var zipAllSortedByKeyRight2 = zipAllSortedByKeyRight;
var zipAllSortedByKeyWith2 = zipAllSortedByKeyWith;
var zipAllWith2 = zipAllWith;
var zipLatest2 = zipLatest;
var zipLatestAll2 = zipLatestAll;
var zipLatestWith2 = zipLatestWith;
var zipLeft6 = zipLeft5;
var zipRight8 = zipRight7;
var zipWith8 = zipWith7;
var zipWithChunks2 = zipWithChunks;
var zipWithNext2 = zipWithNext;
var zipWithPrevious2 = zipWithPrevious;
var zipWithPreviousAndNext2 = zipWithPreviousAndNext;
var zipWithIndex2 = zipWithIndex;
var Do2 = Do;
var bind4 = bind3;
var bindEffect2 = bindEffect;
var bindTo5 = bindTo4;
var let_4 = let_3;
var decodeText2 = decodeText;
var encodeText2 = encodeText;
var fromEventListener2 = fromEventListener;

export {
  unsafeMake,
  get4 as get,
  currentContext2 as currentContext,
  FiberRef_exports,
  minimumLogLevel,
  withMinimumLogLevel,
  addLogger,
  addLoggerEffect,
  addLoggerScoped,
  removeLogger,
  replaceLogger,
  replaceLoggerEffect,
  replaceLoggerScoped,
  addSupervisor,
  enableCooperativeYielding,
  enableInterruption,
  enableOpSupervision,
  enableRuntimeMetrics,
  enableWindDown,
  disableCooperativeYielding,
  disableInterruption,
  disableOpSupervision,
  disableRuntimeMetrics,
  disableWindDown,
  CurrentMemoMap2 as CurrentMemoMap,
  isLayer2 as isLayer,
  buildWithScope2 as buildWithScope,
  effect,
  merge4 as merge,
  scoped3 as scoped,
  scopedDiscard2 as scopedDiscard,
  scopedContext2 as scopedContext,
  succeed6 as succeed,
  succeedContext2 as succeedContext,
  sync4 as sync,
  provide3 as provide,
  provideMerge2 as provideMerge,
  unwrapScoped2 as unwrapScoped,
  makeMemoMap2 as makeMemoMap,
  buildWithMemoMap2 as buildWithMemoMap,
  Layer_exports,
  unbounded4 as unbounded,
  publish2 as publish,
  PubSub_exports,
  ChildExecutorDecisionTypeId,
  Continue,
  Close,
  Yield,
  isChildExecutorDecision,
  isContinue,
  isClose,
  isYield,
  match7 as match,
  UpstreamPullStrategyTypeId,
  PullAfterNext,
  PullAfterAllEnqueued,
  isUpstreamPullStrategy,
  isPullAfterNext,
  isPullAfterAllEnqueued,
  match8 as match2,
  ChannelTypeId,
  isChannel,
  acquireReleaseOut,
  catchAllCause4 as catchAllCause,
  concatAll,
  concatAllWith,
  concatMapWith,
  concatMapWithCustom,
  embedInput,
  ensuringWith,
  fail7 as fail,
  failSync4 as failSync,
  failCause6 as failCause,
  failCauseSync4 as failCauseSync,
  flatMap6 as flatMap,
  foldCauseChannel,
  fromEffect2 as fromEffect,
  pipeTo,
  provideContext,
  readOrFail,
  readWith,
  readWithCause,
  succeed7 as succeed2,
  suspend5 as suspend,
  sync5 as sync2,
  void_3 as void_,
  write,
  UpstreamPullRequestTypeId,
  Pulled,
  NoUpstream,
  isUpstreamPullRequest,
  isPulled,
  isNoUpstream,
  match9 as match3,
  runIn,
  MergeStateTypeId,
  BothRunning,
  LeftDone,
  RightDone,
  isMergeState,
  isBothRunning,
  isLeftDone,
  isRightDone,
  match11 as match4,
  MergeStrategyTypeId,
  BackPressure,
  BufferSliding,
  isMergeStrategy,
  isBackPressure,
  isBufferSliding,
  match12 as match5,
  make10 as make,
  acquireUseRelease2 as acquireUseRelease,
  as3 as as,
  asVoid3 as asVoid,
  buffer,
  bufferChunk,
  catchAll4 as catchAll,
  concatMap,
  collect,
  concatOut,
  mapInput,
  mapInputEffect,
  mapInputError,
  mapInputErrorEffect,
  mapInputIn,
  mapInputInEffect,
  doneCollect,
  drain,
  emitCollect,
  ensuring3 as ensuring,
  context4 as context,
  contextWith2 as contextWith,
  contextWithChannel,
  contextWithEffect,
  flatten5 as flatten,
  foldChannel,
  fromEither,
  fromInput,
  fromPubSub,
  fromPubSubScoped,
  fromOption,
  fromQueue,
  identityChannel,
  interruptWhen,
  interruptWhenDeferred,
  map9 as map,
  mapEffect,
  mapError4 as mapError,
  mapErrorCause,
  mapOut,
  mapOutEffect,
  mapOutEffectPar,
  mergeAll3 as mergeAll,
  mergeAllUnbounded,
  mergeAllUnboundedWith,
  mergeAllWith,
  mergeMap,
  mergeOut,
  mergeOutWith,
  mergeWith,
  never2 as never,
  orDie4 as orDie,
  orDieWith,
  orElse5 as orElse,
  pipeToOrFail,
  provideService2 as provideService,
  provideLayer,
  mapInputContext,
  provideSomeLayer,
  read,
  repeated,
  run,
  runCollect,
  runDrain,
  runScoped,
  scoped4 as scoped2,
  scopedWith2 as scopedWith,
  splitLines,
  toPubSub,
  toPull,
  toPullIn,
  toQueue,
  unwrap,
  unwrapScoped3 as unwrapScoped2,
  unwrapScopedWith,
  updateService2 as updateService,
  withSpan3 as withSpan,
  writeAll,
  writeChunk,
  zip3 as zip,
  zipLeft2 as zipLeft,
  zipRight4 as zipRight,
  ChannelExceptionTypeId,
  ChannelException,
  isChannelException,
  SinkTypeId,
  suspend6 as suspend2,
  as4 as as2,
  collectAll,
  collectAllN,
  collectAllFrom,
  collectAllToMap,
  collectAllToMapN,
  collectAllToSet,
  collectAllToSetN,
  collectAllUntil,
  collectAllUntilEffect,
  collectAllWhile,
  collectAllWhileEffect,
  collectAllWhileWith,
  collectLeftover,
  mapInput2,
  mapInputEffect2,
  mapInputChunks,
  mapInputChunksEffect,
  die6 as die,
  dieMessage2 as dieMessage,
  dieSync4 as dieSync,
  dimap,
  dimapEffect,
  dimapChunks,
  dimapChunksEffect,
  drain2,
  drop2 as drop,
  dropUntil2 as dropUntil,
  dropUntilEffect,
  dropWhile3 as dropWhile,
  dropWhileEffect,
  ensuring4 as ensuring2,
  ensuringWith2,
  context5 as context2,
  contextWith3 as contextWith2,
  contextWithEffect2,
  contextWithSink,
  every2 as every,
  fail8 as fail2,
  failSync5 as failSync2,
  failCause7 as failCause2,
  failCauseSync5 as failCauseSync2,
  filterInput,
  filterInputEffect,
  findEffect,
  fold,
  foldSink,
  foldChunks,
  foldChunksEffect,
  foldEffect,
  foldLeft,
  foldLeftChunks,
  foldLeftChunksEffect,
  foldLeftEffect,
  foldUntil,
  foldUntilEffect,
  foldWeighted,
  foldWeightedDecompose,
  foldWeightedDecomposeEffect,
  foldWeightedEffect,
  flatMap7 as flatMap2,
  forEach3 as forEach,
  forEachChunk,
  forEachWhile,
  forEachChunkWhile,
  fromChannel,
  fromEffect4 as fromEffect2,
  fromPubSub2,
  fromPush,
  fromQueue2,
  head3 as head,
  ignoreLeftover,
  last2 as last,
  leftover,
  map10 as map2,
  mapEffect2,
  mapError5 as mapError2,
  mapLeftover,
  never3 as never2,
  orElse6 as orElse2,
  provideContext2,
  race2 as race,
  raceBoth,
  raceWith2 as raceWith,
  refineOrDie,
  refineOrDieWith,
  some2 as some,
  splitWhere2 as splitWhere,
  succeed8 as succeed3,
  sum,
  summarized,
  sync6 as sync3,
  take3 as take,
  toChannel,
  unwrap2,
  unwrapScoped4 as unwrapScoped3,
  unwrapScopedWith2,
  withDuration,
  zip4 as zip2,
  zipLeft3 as zipLeft2,
  zipRight5 as zipRight2,
  zipWith5 as zipWith,
  channelToSink,
  count,
  mkString,
  timed,
  MergeDecision_exports,
  RcRef_exports,
  runFork2 as runFork,
  defaultRuntime2 as defaultRuntime,
  make13 as make2,
  updateContext2 as updateContext,
  Runtime_exports,
  StreamHaltStrategy_exports,
  STMTypeId,
  isSTM,
  commit2 as commit,
  context6 as context3,
  contextWith4 as contextWith3,
  contextWithSTM,
  catchAll5 as catchAll2,
  mapInputContext2,
  die8 as die2,
  dieMessage3 as dieMessage2,
  dieSync5 as dieSync2,
  effect3 as effect2,
  ensuring5 as ensuring3,
  fail10 as fail3,
  failSync6 as failSync3,
  flatMap8 as flatMap3,
  matchSTM,
  withSTMRuntime,
  interrupt7 as interrupt,
  interruptAs2 as interruptAs,
  map11 as map3,
  orTry,
  retry4 as retry,
  succeed10 as succeed4,
  sync7 as sync4,
  zip5 as zip3,
  zipLeft4 as zipLeft3,
  zipRight6 as zipRight3,
  zipWith6 as zipWith2,
  acquireUseRelease3 as acquireUseRelease2,
  as5 as as3,
  asSome2 as asSome,
  asSomeError,
  asVoid4 as asVoid2,
  attempt,
  bind2 as bind,
  bindTo3 as bindTo,
  let_2 as let_,
  catchSome,
  catchTag,
  catchTags,
  check,
  collect2,
  collectSTM,
  commitEither,
  cond,
  either,
  eventually,
  every3 as every2,
  exists,
  fiberId2 as fiberId,
  filter3 as filter,
  filterNot,
  filterOrDie,
  filterOrDieMessage,
  filterOrElse,
  filterOrFail,
  flatten6 as flatten2,
  flip,
  flipWith,
  match16 as match6,
  forEach4 as forEach2,
  fromEither2,
  fromOption2,
  gen2 as gen,
  head4 as head2,
  if_,
  ignore2 as ignore,
  isFailure2 as isFailure,
  isSuccess3 as isSuccess,
  iterate,
  loop,
  mapAttempt,
  mapBoth3 as mapBoth,
  mapError6 as mapError3,
  merge5 as merge2,
  mergeAll4 as mergeAll2,
  negate,
  none2 as none,
  option,
  orDie5 as orDie2,
  orDieWith2,
  orElse7 as orElse3,
  orElseEither,
  orElseFail,
  orElseOptional,
  orElseSucceed,
  provideContext3,
  provideSomeContext,
  provideService4 as provideService2,
  provideServiceSTM,
  reduce3 as reduce,
  reduceAll,
  reduceRight2 as reduceRight,
  refineOrDie2,
  refineOrDieWith2,
  reject,
  rejectSTM,
  repeatUntil,
  repeatWhile,
  replicate,
  replicateSTM,
  replicateSTMDiscard,
  retryUntil,
  retryWhile,
  partition,
  some3 as some2,
  all4 as all,
  succeedNone2 as succeedNone,
  succeedSome,
  summarized2,
  suspend8 as suspend3,
  tap5 as tap,
  tapBoth,
  tapError3 as tapError,
  try_,
  void_5 as void_2,
  unless,
  unlessSTM,
  unsome,
  validateAll,
  validateFirst,
  when2 as when,
  whenSTM,
  TRefTypeId,
  tRefVariance,
  TRefImpl,
  make16 as make3,
  get7 as get2,
  set5 as set,
  getAndSet3 as getAndSet,
  getAndUpdate2 as getAndUpdate,
  getAndUpdateSome2 as getAndUpdateSome,
  setAndGet,
  modify3 as modify,
  modifySome2 as modifySome,
  update3 as update,
  updateAndGet2 as updateAndGet,
  updateSome2 as updateSome,
  updateSomeAndGet2 as updateSomeAndGet,
  unsafeGet5 as unsafeGet,
  unsafeSet2 as unsafeSet,
  tDequeueVariance,
  publish4 as publish2,
  subscribe4 as subscribe,
  unbounded7 as unbounded2,
  TPubSub_exports,
  TDequeueTypeId2 as TDequeueTypeId,
  offer4 as offer,
  shutdown7 as shutdown,
  unbounded8 as unbounded3,
  TQueue_exports,
  TakeTypeId,
  chunk,
  die9 as die3,
  dieMessage4 as dieMessage3,
  done3 as done,
  end2 as end,
  fail11 as fail4,
  failCause8 as failCause3,
  fromEffect5 as fromEffect3,
  fromExit,
  fromPull,
  isDone3 as isDone,
  isFailure3 as isFailure2,
  isSuccess4 as isSuccess2,
  make19 as make4,
  match17 as match7,
  matchEffect3 as matchEffect,
  map12 as map4,
  of2 as of,
  tap6 as tap2,
  isStream,
  concat,
  fromChannel2,
  toChannel2,
  fromPubSub3,
  fromTQueue,
  make20 as make5,
  unwrap3,
  unwrapScoped5 as unwrapScoped4,
  channelToStream,
  GroupByTypeId,
  evaluate,
  filter5 as filter2,
  first,
  make21 as make6,
  StreamTypeId3 as StreamTypeId,
  DefaultChunkSize2 as DefaultChunkSize,
  accumulate2 as accumulate,
  accumulateChunks2 as accumulateChunks,
  acquireRelease4 as acquireRelease,
  aggregate2 as aggregate,
  aggregateWithin2 as aggregateWithin,
  aggregateWithinEither2 as aggregateWithinEither,
  as7 as as4,
  _async2 as _async,
  asyncEffect2 as asyncEffect,
  asyncPush2 as asyncPush,
  asyncScoped2 as asyncScoped,
  branchAfter2 as branchAfter,
  broadcast2 as broadcast,
  share2 as share,
  broadcastDynamic2 as broadcastDynamic,
  broadcastedQueues2 as broadcastedQueues,
  broadcastedQueuesDynamic2 as broadcastedQueuesDynamic,
  buffer3 as buffer2,
  bufferChunks2 as bufferChunks,
  catchAll7 as catchAll3,
  catchAllCause6 as catchAllCause2,
  catchSome3 as catchSome2,
  catchTag3 as catchTag2,
  catchTags3 as catchTags2,
  catchSomeCause3 as catchSomeCause,
  changes2 as changes,
  changesWith2 as changesWith,
  changesWithEffect2 as changesWithEffect,
  chunks2 as chunks,
  chunksWith2 as chunksWith,
  combine3 as combine,
  combineChunks2 as combineChunks,
  concat2,
  concatAll3 as concatAll2,
  cross2 as cross,
  crossLeft2 as crossLeft,
  crossRight2 as crossRight,
  crossWith2 as crossWith,
  debounce2 as debounce,
  die11 as die4,
  dieSync7 as dieSync3,
  dieMessage6 as dieMessage4,
  distributedWith2 as distributedWith,
  distributedWithDynamic2 as distributedWithDynamic,
  drain4 as drain3,
  drainFork2 as drainFork,
  drop4 as drop2,
  dropRight2 as dropRight,
  dropUntil4 as dropUntil2,
  dropUntilEffect3 as dropUntilEffect2,
  dropWhile5 as dropWhile2,
  dropWhileEffect3 as dropWhileEffect2,
  either3 as either2,
  empty7 as empty,
  ensuring7 as ensuring4,
  ensuringWith4 as ensuringWith3,
  context8 as context4,
  contextWith6 as contextWith4,
  contextWithEffect4 as contextWithEffect3,
  contextWithStream2 as contextWithStream,
  execute2 as execute,
  fail13 as fail5,
  failSync8 as failSync4,
  failCause11 as failCause4,
  failCauseSync7 as failCauseSync3,
  filter6 as filter3,
  filterEffect2 as filterEffect,
  filterMap3 as filterMap,
  filterMapEffect2 as filterMapEffect,
  filterMapWhile3 as filterMapWhile,
  filterMapWhileEffect2 as filterMapWhileEffect,
  finalizer2 as finalizer,
  find2 as find,
  findEffect3 as findEffect2,
  flatMap10 as flatMap4,
  flatten8 as flatten3,
  flattenChunks2 as flattenChunks,
  flattenEffect2 as flattenEffect,
  flattenExitOption2 as flattenExitOption,
  flattenIterables2 as flattenIterables,
  flattenTake2 as flattenTake,
  forever4 as forever,
  fromAsyncIterable2 as fromAsyncIterable,
  fromChannel3,
  toChannel3,
  fromChunk2 as fromChunk,
  fromChunkPubSub2 as fromChunkPubSub,
  fromChunkQueue2 as fromChunkQueue,
  fromChunks2 as fromChunks,
  fromEffect7 as fromEffect4,
  fromEffectOption2 as fromEffectOption,
  fromPubSub4,
  fromTPubSub2 as fromTPubSub,
  fromIterable4 as fromIterable,
  fromIterableEffect2 as fromIterableEffect,
  fromIteratorSucceed2 as fromIteratorSucceed,
  fromPull3 as fromPull2,
  fromQueue4 as fromQueue3,
  fromTQueue2,
  fromReadableStream2 as fromReadableStream,
  fromReadableStreamByob2 as fromReadableStreamByob,
  fromSchedule2 as fromSchedule,
  groupAdjacentBy2 as groupAdjacentBy,
  groupBy2 as groupBy,
  groupByKey2 as groupByKey,
  grouped2 as grouped,
  groupedWithin2 as groupedWithin,
  haltAfter2 as haltAfter,
  haltWhen2 as haltWhen,
  haltWhenDeferred2 as haltWhenDeferred,
  identity2 as identity,
  interleave2 as interleave,
  interleaveWith2 as interleaveWith,
  intersperse2 as intersperse,
  intersperseAffixes2 as intersperseAffixes,
  interruptAfter2 as interruptAfter,
  interruptWhen3 as interruptWhen2,
  interruptWhenDeferred3 as interruptWhenDeferred2,
  iterate3 as iterate2,
  make22 as make7,
  map14 as map5,
  mapAccum3 as mapAccum,
  mapAccumEffect2 as mapAccumEffect,
  mapBoth5 as mapBoth2,
  mapChunks2 as mapChunks,
  mapChunksEffect2 as mapChunksEffect,
  mapConcat2 as mapConcat,
  mapConcatChunk2 as mapConcatChunk,
  mapConcatChunkEffect2 as mapConcatChunkEffect,
  mapConcatEffect2 as mapConcatEffect,
  mapEffect3,
  mapError8 as mapError4,
  mapErrorCause3 as mapErrorCause2,
  merge7 as merge3,
  mergeAll6 as mergeAll3,
  mergeWithTag2 as mergeWithTag,
  mergeWith3 as mergeWith2,
  mergeEither2 as mergeEither,
  mergeLeft2 as mergeLeft,
  mergeRight2 as mergeRight,
  mkString3 as mkString2,
  never5 as never3,
  onEnd2 as onEnd,
  onError3 as onError,
  onDone2 as onDone,
  onStart2 as onStart,
  orDie7 as orDie3,
  orDieWith4 as orDieWith3,
  orElse9 as orElse4,
  orElseEither3 as orElseEither2,
  orElseFail3 as orElseFail2,
  orElseIfEmpty2 as orElseIfEmpty,
  orElseIfEmptyChunk2 as orElseIfEmptyChunk,
  orElseIfEmptyStream2 as orElseIfEmptyStream,
  orElseSucceed3 as orElseSucceed2,
  paginate2 as paginate,
  paginateChunk2 as paginateChunk,
  paginateChunkEffect2 as paginateChunkEffect,
  paginateEffect2 as paginateEffect,
  partition3 as partition2,
  partitionEither2 as partitionEither,
  peel2 as peel,
  pipeThrough2 as pipeThrough,
  pipeThroughChannel2 as pipeThroughChannel,
  pipeThroughChannelOrFail2 as pipeThroughChannelOrFail,
  prepend3 as prepend,
  provideContext5 as provideContext4,
  provideSomeContext3 as provideSomeContext2,
  provideLayer3 as provideLayer2,
  provideService6 as provideService3,
  provideServiceEffect3 as provideServiceEffect,
  provideServiceStream2 as provideServiceStream,
  mapInputContext4 as mapInputContext3,
  provideSomeLayer3 as provideSomeLayer2,
  race4 as race2,
  raceAll2 as raceAll,
  range3 as range,
  rechunk2 as rechunk,
  refineOrDie4 as refineOrDie3,
  refineOrDieWith4 as refineOrDieWith3,
  repeat3 as repeat,
  repeatEffect2 as repeatEffect,
  repeatEffectChunk2 as repeatEffectChunk,
  repeatEffectChunkOption2 as repeatEffectChunkOption,
  repeatEffectOption2 as repeatEffectOption,
  repeatEffectWithSchedule2 as repeatEffectWithSchedule,
  repeatEither2 as repeatEither,
  repeatElements2 as repeatElements,
  repeatElementsWith2 as repeatElementsWith,
  repeatValue2 as repeatValue,
  repeatWith2 as repeatWith,
  retry6 as retry2,
  withExecutionPlan2 as withExecutionPlan,
  run3 as run2,
  runCollect3 as runCollect2,
  runCount2 as runCount,
  runDrain3 as runDrain2,
  runFold2 as runFold,
  runFoldEffect2 as runFoldEffect,
  runFoldScoped2 as runFoldScoped,
  runFoldScopedEffect2 as runFoldScopedEffect,
  runFoldWhile2 as runFoldWhile,
  runFoldWhileEffect2 as runFoldWhileEffect,
  runFoldWhileScoped2 as runFoldWhileScoped,
  runFoldWhileScopedEffect2 as runFoldWhileScopedEffect,
  runForEach2 as runForEach,
  runForEachChunk2 as runForEachChunk,
  runForEachChunkScoped2 as runForEachChunkScoped,
  runForEachScoped2 as runForEachScoped,
  runForEachWhile2 as runForEachWhile,
  runForEachWhileScoped2 as runForEachWhileScoped,
  runHead2 as runHead,
  runIntoPubSub2 as runIntoPubSub,
  runIntoPubSubScoped2 as runIntoPubSubScoped,
  runIntoQueue2 as runIntoQueue,
  runIntoQueueElementsScoped2 as runIntoQueueElementsScoped,
  runIntoQueueScoped2 as runIntoQueueScoped,
  runLast2 as runLast,
  runScoped3 as runScoped2,
  runSum2 as runSum,
  scan2 as scan,
  scanEffect2 as scanEffect,
  scanReduce2 as scanReduce,
  scanReduceEffect2 as scanReduceEffect,
  schedule2 as schedule,
  scheduleWith2 as scheduleWith,
  scoped6 as scoped3,
  scopedWith4 as scopedWith2,
  sliding9 as sliding,
  slidingSize2 as slidingSize,
  some5 as some3,
  someOrElse2 as someOrElse,
  someOrFail2 as someOrFail,
  split2 as split,
  splitOnChunk2 as splitOnChunk,
  splitLines3 as splitLines2,
  succeed12 as succeed5,
  sync9 as sync5,
  suspend10 as suspend4,
  take8 as take2,
  takeRight3 as takeRight,
  takeUntil2 as takeUntil,
  takeUntilEffect2 as takeUntilEffect,
  takeWhile4 as takeWhile,
  tap8 as tap3,
  tapBoth3 as tapBoth2,
  tapError5 as tapError2,
  tapErrorCause5 as tapErrorCause,
  tapSink2 as tapSink,
  throttle2 as throttle,
  throttleEffect2 as throttleEffect,
  tick2 as tick,
  timeout2 as timeout,
  timeoutFail3 as timeoutFail,
  timeoutFailCause3 as timeoutFailCause,
  timeoutTo2 as timeoutTo,
  toPubSub3 as toPubSub2,
  toPull3 as toPull2,
  toQueue3 as toQueue2,
  toQueueOfElements2 as toQueueOfElements,
  toReadableStream2 as toReadableStream,
  toReadableStreamEffect2 as toReadableStreamEffect,
  toReadableStreamRuntime2 as toReadableStreamRuntime,
  toAsyncIterableRuntime2 as toAsyncIterableRuntime,
  toAsyncIterableEffect2 as toAsyncIterableEffect,
  toAsyncIterable2 as toAsyncIterable,
  transduce2 as transduce,
  unfold2 as unfold,
  unfoldChunk2 as unfoldChunk,
  unfoldChunkEffect2 as unfoldChunkEffect,
  unfoldEffect2 as unfoldEffect,
  void_7 as void_3,
  unwrap4,
  unwrapScoped6 as unwrapScoped5,
  unwrapScopedWith4 as unwrapScopedWith3,
  updateService4 as updateService2,
  when4 as when2,
  whenCase2 as whenCase,
  whenCaseEffect2 as whenCaseEffect,
  whenEffect3 as whenEffect,
  withSpan5 as withSpan2,
  zip7 as zip4,
  zipFlatten2 as zipFlatten,
  zipAll2 as zipAll,
  zipAllLeft2 as zipAllLeft,
  zipAllRight2 as zipAllRight,
  zipAllSortedByKey2 as zipAllSortedByKey,
  zipAllSortedByKeyLeft2 as zipAllSortedByKeyLeft,
  zipAllSortedByKeyRight2 as zipAllSortedByKeyRight,
  zipAllSortedByKeyWith2 as zipAllSortedByKeyWith,
  zipAllWith2 as zipAllWith,
  zipLatest2 as zipLatest,
  zipLatestAll2 as zipLatestAll,
  zipLatestWith2 as zipLatestWith,
  zipLeft6 as zipLeft4,
  zipRight8 as zipRight4,
  zipWith8 as zipWith3,
  zipWithChunks2 as zipWithChunks,
  zipWithNext2 as zipWithNext,
  zipWithPrevious2 as zipWithPrevious,
  zipWithPreviousAndNext2 as zipWithPreviousAndNext,
  zipWithIndex2 as zipWithIndex,
  Do2 as Do,
  bind4 as bind2,
  bindEffect2 as bindEffect,
  bindTo5 as bindTo2,
  let_4 as let_2,
  decodeText2 as decodeText,
  encodeText2 as encodeText,
  fromEventListener2 as fromEventListener,
  Stream_exports
};
//# sourceMappingURL=chunk-XIVDWX6L.js.map
