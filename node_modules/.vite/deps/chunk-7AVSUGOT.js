import {
  forkScoped
} from "./chunk-CBQWHB33.js";
import {
  Order,
  RandomTypeId,
  choice,
  currentTimeMillis,
  fixed,
  get,
  greaterThanOrEqualTo as greaterThanOrEqualTo3,
  make as make3,
  make4,
  negate,
  next,
  nextBoolean,
  nextInt,
  nextIntBetween,
  nextRange,
  provideServiceEffect,
  randomTag,
  randomWith,
  set,
  shuffle,
  sign,
  sleep2 as sleep,
  try_,
  unsafeMake,
  update
} from "./chunk-WCCQGKCT.js";
import {
  ArrayProto,
  CauseTypeId,
  ExceededCapacityException,
  ExceededCapacityExceptionTypeId,
  IllegalArgumentException,
  IllegalArgumentExceptionTypeId,
  InterruptedException,
  InterruptedExceptionTypeId,
  InvalidPubSubCapacityExceptionTypeId,
  NoSuchElementException,
  NoSuchElementExceptionTypeId,
  Reference,
  RuntimeException,
  RuntimeExceptionTypeId,
  Structural,
  TimeoutException,
  TimeoutExceptionTypeId,
  UnknownException,
  UnknownExceptionTypeId,
  YieldableError,
  add,
  andThen,
  append2 as append,
  as,
  as2,
  catchAll,
  catchAllCause,
  causeSquash,
  causeSquashWith,
  contains,
  contextWithEffect,
  defects,
  die,
  die2,
  dieOption,
  dieSync,
  empty10 as empty2,
  empty3 as empty,
  fail,
  fail2,
  failCause,
  failSync,
  failureOption,
  failureOrCause,
  failures,
  filter4 as filter2,
  find,
  flatMap3 as flatMap,
  flatMap4 as flatMap2,
  flatten4 as flatten,
  flipCauseOption,
  fromIterable,
  fromIterable2,
  head3 as head,
  headNonEmpty2 as headNonEmpty,
  interrupt,
  interruptOption,
  interruptors,
  isCause,
  isDie,
  isDieType,
  isEmpty,
  isEmpty3 as isEmpty2,
  isEmptyType,
  isExceededCapacityException,
  isFailType,
  isFailure,
  isIllegalArgumentException,
  isInterruptType,
  isInterrupted,
  isInterruptedException,
  isInterruptedOnly,
  isNoSuchElementException,
  isNonEmpty,
  isNonEmptyArray,
  isParallelType,
  isRuntimeException,
  isSequentialType,
  isTimeoutException,
  isUnknownException,
  keepDefects,
  linearize,
  map4 as map3,
  map5 as map4,
  mapInputContext,
  match2,
  matchEffect,
  of2 as of,
  orDie,
  originalInstance,
  parallel,
  prepend2 as prepend,
  pretty,
  prettyErrors,
  provideContext,
  reduce6 as reduce,
  reduceWithContext,
  reverse,
  sequential,
  size3 as size,
  sort,
  stripFailures,
  stripSomeDefects,
  struct,
  succeed,
  suspend,
  sync,
  tailNonEmpty2 as tailNonEmpty,
  tap,
  zipLeft,
  zipRight,
  zipWith2 as zipWith
} from "./chunk-ZLEQSUEA.js";
import {
  all,
  fromOption,
  getOrThrowWith,
  isLeft,
  left,
  map,
  merge,
  right
} from "./chunk-TVBZ6A6Y.js";
import {
  decode,
  greaterThanOrEqualTo as greaterThanOrEqualTo2,
  lessThan as lessThan2,
  millis,
  sum,
  times,
  toMillis,
  zero
} from "./chunk-AQW37YSD.js";
import {
  NodeInspectSymbol,
  StructuralPrototype,
  array,
  array2,
  between,
  cached,
  clamp,
  combine,
  constVoid,
  dual,
  equals,
  filter,
  format,
  fromNullable,
  getBugErrorMessage,
  getOrElse,
  getOrUndefined,
  globalValue,
  greaterThan,
  greaterThanOrEqualTo,
  hasProperty,
  hash,
  identity,
  isNone2 as isNone,
  isSome,
  isString,
  isTagged,
  lessThan,
  lessThanOrEqualTo,
  liftThrowable,
  make,
  make2,
  map as map2,
  match,
  max,
  min,
  none2 as none,
  number,
  number2,
  pipe,
  pipeArguments,
  some2 as some,
  string,
  string2,
  string3,
  symbol2 as symbol,
  symbol3 as symbol2
} from "./chunk-H62K6C3Q.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cause.js
var Cause_exports = {};
__export(Cause_exports, {
  CauseTypeId: () => CauseTypeId2,
  ExceededCapacityException: () => ExceededCapacityException2,
  ExceededCapacityExceptionTypeId: () => ExceededCapacityExceptionTypeId2,
  IllegalArgumentException: () => IllegalArgumentException2,
  IllegalArgumentExceptionTypeId: () => IllegalArgumentExceptionTypeId2,
  InterruptedException: () => InterruptedException2,
  InterruptedExceptionTypeId: () => InterruptedExceptionTypeId2,
  InvalidPubSubCapacityExceptionTypeId: () => InvalidPubSubCapacityExceptionTypeId2,
  NoSuchElementException: () => NoSuchElementException2,
  NoSuchElementExceptionTypeId: () => NoSuchElementExceptionTypeId2,
  RuntimeException: () => RuntimeException2,
  RuntimeExceptionTypeId: () => RuntimeExceptionTypeId2,
  TimeoutException: () => TimeoutException2,
  TimeoutExceptionTypeId: () => TimeoutExceptionTypeId2,
  UnknownException: () => UnknownException2,
  UnknownExceptionTypeId: () => UnknownExceptionTypeId2,
  YieldableError: () => YieldableError2,
  andThen: () => andThen2,
  as: () => as3,
  contains: () => contains2,
  defects: () => defects2,
  die: () => die3,
  dieOption: () => dieOption2,
  empty: () => empty3,
  fail: () => fail3,
  failureOption: () => failureOption2,
  failureOrCause: () => failureOrCause2,
  failures: () => failures2,
  filter: () => filter3,
  find: () => find2,
  flatMap: () => flatMap3,
  flatten: () => flatten2,
  flipCauseOption: () => flipCauseOption2,
  interrupt: () => interrupt2,
  interruptOption: () => interruptOption2,
  interruptors: () => interruptors2,
  isCause: () => isCause2,
  isDie: () => isDie2,
  isDieType: () => isDieType2,
  isEmpty: () => isEmpty3,
  isEmptyType: () => isEmptyType2,
  isExceededCapacityException: () => isExceededCapacityException2,
  isFailType: () => isFailType2,
  isFailure: () => isFailure2,
  isIllegalArgumentException: () => isIllegalArgumentException2,
  isInterruptType: () => isInterruptType2,
  isInterrupted: () => isInterrupted2,
  isInterruptedException: () => isInterruptedException2,
  isInterruptedOnly: () => isInterruptedOnly2,
  isNoSuchElementException: () => isNoSuchElementException2,
  isParallelType: () => isParallelType2,
  isRuntimeException: () => isRuntimeException2,
  isSequentialType: () => isSequentialType2,
  isTimeoutException: () => isTimeoutException2,
  isUnknownException: () => isUnknownException2,
  keepDefects: () => keepDefects2,
  linearize: () => linearize2,
  map: () => map5,
  match: () => match3,
  originalError: () => originalError,
  parallel: () => parallel2,
  pretty: () => pretty2,
  prettyErrors: () => prettyErrors2,
  reduce: () => reduce2,
  reduceWithContext: () => reduceWithContext2,
  sequential: () => sequential2,
  size: () => size2,
  squash: () => squash,
  squashWith: () => squashWith,
  stripFailures: () => stripFailures2,
  stripSomeDefects: () => stripSomeDefects2
});
var CauseTypeId2 = CauseTypeId;
var RuntimeExceptionTypeId2 = RuntimeExceptionTypeId;
var InterruptedExceptionTypeId2 = InterruptedExceptionTypeId;
var IllegalArgumentExceptionTypeId2 = IllegalArgumentExceptionTypeId;
var NoSuchElementExceptionTypeId2 = NoSuchElementExceptionTypeId;
var InvalidPubSubCapacityExceptionTypeId2 = InvalidPubSubCapacityExceptionTypeId;
var ExceededCapacityExceptionTypeId2 = ExceededCapacityExceptionTypeId;
var TimeoutExceptionTypeId2 = TimeoutExceptionTypeId;
var UnknownExceptionTypeId2 = UnknownExceptionTypeId;
var YieldableError2 = YieldableError;
var empty3 = empty2;
var fail3 = fail;
var die3 = die;
var interrupt2 = interrupt;
var parallel2 = parallel;
var sequential2 = sequential;
var isCause2 = isCause;
var isEmptyType2 = isEmptyType;
var isFailType2 = isFailType;
var isDieType2 = isDieType;
var isInterruptType2 = isInterruptType;
var isSequentialType2 = isSequentialType;
var isParallelType2 = isParallelType;
var size2 = size;
var isEmpty3 = isEmpty2;
var isFailure2 = isFailure;
var isDie2 = isDie;
var isInterrupted2 = isInterrupted;
var isInterruptedOnly2 = isInterruptedOnly;
var failures2 = failures;
var defects2 = defects;
var interruptors2 = interruptors;
var failureOption2 = failureOption;
var failureOrCause2 = failureOrCause;
var flipCauseOption2 = flipCauseOption;
var dieOption2 = dieOption;
var interruptOption2 = interruptOption;
var keepDefects2 = keepDefects;
var linearize2 = linearize;
var stripFailures2 = stripFailures;
var stripSomeDefects2 = stripSomeDefects;
var as3 = as;
var map5 = map3;
var flatMap3 = flatMap;
var andThen2 = andThen;
var flatten2 = flatten;
var contains2 = contains;
var squash = causeSquash;
var squashWith = causeSquashWith;
var find2 = find;
var filter3 = filter2;
var match3 = match2;
var reduce2 = reduce;
var reduceWithContext2 = reduceWithContext;
var InterruptedException2 = InterruptedException;
var isInterruptedException2 = isInterruptedException;
var IllegalArgumentException2 = IllegalArgumentException;
var isIllegalArgumentException2 = isIllegalArgumentException;
var NoSuchElementException2 = NoSuchElementException;
var isNoSuchElementException2 = isNoSuchElementException;
var RuntimeException2 = RuntimeException;
var isRuntimeException2 = isRuntimeException;
var TimeoutException2 = TimeoutException;
var isTimeoutException2 = isTimeoutException;
var UnknownException2 = UnknownException;
var isUnknownException2 = isUnknownException;
var ExceededCapacityException2 = ExceededCapacityException;
var isExceededCapacityException2 = isExceededCapacityException;
var pretty2 = pretty;
var prettyErrors2 = prettyErrors;
var originalError = originalInstance;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleInterval.js
var ScheduleInterval_exports = {};
__export(ScheduleInterval_exports, {
  IntervalTypeId: () => IntervalTypeId2,
  after: () => after2,
  before: () => before2,
  empty: () => empty5,
  intersect: () => intersect2,
  isEmpty: () => isEmpty5,
  isNonEmpty: () => isNonEmpty3,
  lessThan: () => lessThan4,
  make: () => make6,
  max: () => max3,
  min: () => min3,
  size: () => size4,
  union: () => union2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule/interval.js
var IntervalSymbolKey = "effect/ScheduleInterval";
var IntervalTypeId = Symbol.for(IntervalSymbolKey);
var empty4 = {
  [IntervalTypeId]: IntervalTypeId,
  startMillis: 0,
  endMillis: 0
};
var make5 = (startMillis, endMillis) => {
  if (startMillis > endMillis) {
    return empty4;
  }
  return {
    [IntervalTypeId]: IntervalTypeId,
    startMillis,
    endMillis
  };
};
var lessThan3 = dual(2, (self, that) => min2(self, that) === self);
var min2 = dual(2, (self, that) => {
  if (self.endMillis <= that.startMillis) return self;
  if (that.endMillis <= self.startMillis) return that;
  if (self.startMillis < that.startMillis) return self;
  if (that.startMillis < self.startMillis) return that;
  if (self.endMillis <= that.endMillis) return self;
  return that;
});
var max2 = dual(2, (self, that) => min2(self, that) === self ? that : self);
var isEmpty4 = (self) => {
  return self.startMillis >= self.endMillis;
};
var isNonEmpty2 = (self) => {
  return !isEmpty4(self);
};
var intersect = dual(2, (self, that) => {
  const start3 = Math.max(self.startMillis, that.startMillis);
  const end3 = Math.min(self.endMillis, that.endMillis);
  return make5(start3, end3);
});
var size3 = (self) => {
  return millis(self.endMillis - self.startMillis);
};
var union = dual(2, (self, that) => {
  const start3 = Math.max(self.startMillis, that.startMillis);
  const end3 = Math.min(self.endMillis, that.endMillis);
  return start3 < end3 ? none() : some(make5(start3, end3));
});
var after = (startMilliseconds) => {
  return make5(startMilliseconds, Number.POSITIVE_INFINITY);
};
var before = (endMilliseconds) => {
  return make5(Number.NEGATIVE_INFINITY, endMilliseconds);
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleInterval.js
var IntervalTypeId2 = IntervalTypeId;
var make6 = make5;
var empty5 = empty4;
var lessThan4 = lessThan3;
var min3 = min2;
var max3 = max2;
var isEmpty5 = isEmpty4;
var isNonEmpty3 = isNonEmpty2;
var intersect2 = intersect;
var size4 = size3;
var union2 = union;
var after2 = after;
var before2 = before;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleIntervals.js
var ScheduleIntervals_exports = {};
__export(ScheduleIntervals_exports, {
  IntervalsTypeId: () => IntervalsTypeId2,
  empty: () => empty7,
  end: () => end2,
  fromIterable: () => fromIterable4,
  intersect: () => intersect4,
  isNonEmpty: () => isNonEmpty5,
  lessThan: () => lessThan6,
  make: () => make8,
  max: () => max5,
  start: () => start2,
  union: () => union4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule/intervals.js
var IntervalsSymbolKey = "effect/ScheduleIntervals";
var IntervalsTypeId = Symbol.for(IntervalsSymbolKey);
var make7 = (intervals) => {
  return {
    [IntervalsTypeId]: IntervalsTypeId,
    intervals
  };
};
var empty6 = make7(empty());
var fromIterable3 = (intervals) => Array.from(intervals).reduce((intervals2, interval) => pipe(intervals2, union3(make7(of(interval)))), empty6);
var union3 = dual(2, (self, that) => {
  if (!isNonEmpty(that.intervals)) {
    return self;
  }
  if (!isNonEmpty(self.intervals)) {
    return that;
  }
  if (headNonEmpty(self.intervals).startMillis < headNonEmpty(that.intervals).startMillis) {
    return unionLoop(tailNonEmpty(self.intervals), that.intervals, headNonEmpty(self.intervals), empty());
  }
  return unionLoop(self.intervals, tailNonEmpty(that.intervals), headNonEmpty(that.intervals), empty());
});
var unionLoop = (_self, _that, _interval, _acc) => {
  let self = _self;
  let that = _that;
  let interval = _interval;
  let acc = _acc;
  while (isNonEmpty(self) || isNonEmpty(that)) {
    if (!isNonEmpty(self) && isNonEmpty(that)) {
      if (interval.endMillis < headNonEmpty(that).startMillis) {
        acc = pipe(acc, prepend(interval));
        interval = headNonEmpty(that);
        that = tailNonEmpty(that);
        self = empty();
      } else {
        interval = make6(interval.startMillis, headNonEmpty(that).endMillis);
        that = tailNonEmpty(that);
        self = empty();
      }
    } else if (isNonEmpty(self) && isEmpty(that)) {
      if (interval.endMillis < headNonEmpty(self).startMillis) {
        acc = pipe(acc, prepend(interval));
        interval = headNonEmpty(self);
        that = empty();
        self = tailNonEmpty(self);
      } else {
        interval = make6(interval.startMillis, headNonEmpty(self).endMillis);
        that = empty();
        self = tailNonEmpty(self);
      }
    } else if (isNonEmpty(self) && isNonEmpty(that)) {
      if (headNonEmpty(self).startMillis < headNonEmpty(that).startMillis) {
        if (interval.endMillis < headNonEmpty(self).startMillis) {
          acc = pipe(acc, prepend(interval));
          interval = headNonEmpty(self);
          self = tailNonEmpty(self);
        } else {
          interval = make6(interval.startMillis, headNonEmpty(self).endMillis);
          self = tailNonEmpty(self);
        }
      } else if (interval.endMillis < headNonEmpty(that).startMillis) {
        acc = pipe(acc, prepend(interval));
        interval = headNonEmpty(that);
        that = tailNonEmpty(that);
      } else {
        interval = make6(interval.startMillis, headNonEmpty(that).endMillis);
        that = tailNonEmpty(that);
      }
    } else {
      throw new Error(getBugErrorMessage("Intervals.unionLoop"));
    }
  }
  return make7(pipe(acc, prepend(interval), reverse));
};
var intersect3 = dual(2, (self, that) => intersectLoop(self.intervals, that.intervals, empty()));
var intersectLoop = (_left, _right, _acc) => {
  let left2 = _left;
  let right2 = _right;
  let acc = _acc;
  while (isNonEmpty(left2) && isNonEmpty(right2)) {
    const interval = pipe(headNonEmpty(left2), intersect2(headNonEmpty(right2)));
    const intervals = isEmpty5(interval) ? acc : pipe(acc, prepend(interval));
    if (pipe(headNonEmpty(left2), lessThan4(headNonEmpty(right2)))) {
      left2 = tailNonEmpty(left2);
    } else {
      right2 = tailNonEmpty(right2);
    }
    acc = intervals;
  }
  return make7(reverse(acc));
};
var start = (self) => {
  return pipe(self.intervals, head, getOrElse(() => empty5)).startMillis;
};
var end = (self) => {
  return pipe(self.intervals, head, getOrElse(() => empty5)).endMillis;
};
var lessThan5 = dual(2, (self, that) => start(self) < start(that));
var isNonEmpty4 = (self) => {
  return isNonEmpty(self.intervals);
};
var max4 = dual(2, (self, that) => lessThan5(self, that) ? that : self);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleIntervals.js
var IntervalsTypeId2 = IntervalsTypeId;
var make8 = make7;
var empty7 = empty6;
var fromIterable4 = fromIterable3;
var union4 = union3;
var intersect4 = intersect3;
var start2 = start;
var end2 = end;
var lessThan6 = lessThan5;
var isNonEmpty5 = isNonEmpty4;
var max5 = max4;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleDecision.js
var ScheduleDecision_exports = {};
__export(ScheduleDecision_exports, {
  continue: () => _continue2,
  continueWith: () => continueWith2,
  done: () => done2,
  isContinue: () => isContinue2,
  isDone: () => isDone2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule/decision.js
var OP_CONTINUE = "Continue";
var OP_DONE = "Done";
var _continue = (intervals) => {
  return {
    _tag: OP_CONTINUE,
    intervals
  };
};
var continueWith = (interval) => {
  return {
    _tag: OP_CONTINUE,
    intervals: make8(of(interval))
  };
};
var done = {
  _tag: OP_DONE
};
var isContinue = (self) => {
  return self._tag === OP_CONTINUE;
};
var isDone = (self) => {
  return self._tag === OP_DONE;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleDecision.js
var _continue2 = _continue;
var continueWith2 = continueWith;
var done2 = done;
var isContinue2 = isContinue;
var isDone2 = isDone;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Data.js
var Data_exports = {};
__export(Data_exports, {
  Class: () => Class,
  Error: () => Error2,
  Structural: () => Structural2,
  TaggedClass: () => TaggedClass,
  TaggedError: () => TaggedError,
  array: () => array3,
  case: () => _case,
  struct: () => struct2,
  tagged: () => tagged,
  taggedEnum: () => taggedEnum,
  tuple: () => tuple,
  unsafeArray: () => unsafeArray,
  unsafeStruct: () => unsafeStruct
});
var struct2 = struct;
var unsafeStruct = (as5) => Object.setPrototypeOf(as5, StructuralPrototype);
var tuple = (...as5) => unsafeArray(as5);
var array3 = (as5) => unsafeArray(as5.slice(0));
var unsafeArray = (as5) => Object.setPrototypeOf(as5, ArrayProto);
var _case = () => (args) => args === void 0 ? Object.create(StructuralPrototype) : struct2(args);
var tagged = (tag) => (args) => {
  const value = args === void 0 ? Object.create(StructuralPrototype) : struct2(args);
  value._tag = tag;
  return value;
};
var Class = Structural;
var TaggedClass = (tag) => {
  class Base extends Class {
    _tag = tag;
  }
  return Base;
};
var Structural2 = Structural;
var taggedEnum = () => new Proxy({}, {
  get(_target, tag, _receiver) {
    if (tag === "$is") {
      return isTagged;
    } else if (tag === "$match") {
      return taggedMatch;
    }
    return tagged(tag);
  }
});
function taggedMatch() {
  if (arguments.length === 1) {
    const cases2 = arguments[0];
    return function(value2) {
      return cases2[value2._tag](value2);
    };
  }
  const value = arguments[0];
  const cases = arguments[1];
  return cases[value._tag](value);
}
var Error2 = (function() {
  const plainArgsSymbol = /* @__PURE__ */ Symbol.for("effect/Data/Error/plainArgs");
  const O = {
    BaseEffectError: class extends YieldableError {
      constructor(args) {
        super(args?.message, args?.cause ? {
          cause: args.cause
        } : void 0);
        if (args) {
          Object.assign(this, args);
          Object.defineProperty(this, plainArgsSymbol, {
            value: args,
            enumerable: false
          });
        }
      }
      toJSON() {
        return {
          ...this[plainArgsSymbol],
          ...this
        };
      }
    }
  };
  return O.BaseEffectError;
})();
var TaggedError = (tag) => {
  const O = {
    BaseEffectError: class extends Error2 {
      _tag = tag;
    }
  };
  O.BaseEffectError.prototype.name = tag;
  return O.BaseEffectError;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/String.js
var String_exports = {};
__export(String_exports, {
  Equivalence: () => Equivalence,
  Order: () => Order2,
  at: () => at,
  camelToSnake: () => camelToSnake,
  capitalize: () => capitalize,
  charAt: () => charAt,
  charCodeAt: () => charCodeAt,
  codePointAt: () => codePointAt,
  concat: () => concat,
  empty: () => empty8,
  endsWith: () => endsWith,
  includes: () => includes,
  indexOf: () => indexOf,
  isEmpty: () => isEmpty6,
  isNonEmpty: () => isNonEmpty6,
  isString: () => isString2,
  kebabToSnake: () => kebabToSnake,
  lastIndexOf: () => lastIndexOf,
  length: () => length,
  linesIterator: () => linesIterator,
  linesWithSeparators: () => linesWithSeparators,
  localeCompare: () => localeCompare,
  match: () => match4,
  matchAll: () => matchAll,
  normalize: () => normalize,
  padEnd: () => padEnd,
  padStart: () => padStart,
  pascalToSnake: () => pascalToSnake,
  repeat: () => repeat,
  replace: () => replace,
  replaceAll: () => replaceAll,
  search: () => search,
  slice: () => slice,
  snakeToCamel: () => snakeToCamel,
  snakeToKebab: () => snakeToKebab,
  snakeToPascal: () => snakeToPascal,
  split: () => split,
  startsWith: () => startsWith,
  stripMargin: () => stripMargin,
  stripMarginWith: () => stripMarginWith,
  substring: () => substring,
  takeLeft: () => takeLeft,
  takeRight: () => takeRight,
  toLocaleLowerCase: () => toLocaleLowerCase,
  toLocaleUpperCase: () => toLocaleUpperCase,
  toLowerCase: () => toLowerCase,
  toUpperCase: () => toUpperCase,
  trim: () => trim,
  trimEnd: () => trimEnd,
  trimStart: () => trimStart,
  uncapitalize: () => uncapitalize
});
var isString2 = isString;
var Equivalence = string;
var Order2 = string3;
var empty8 = "";
var concat = dual(2, (self, that) => self + that);
var toUpperCase = (self) => self.toUpperCase();
var toLowerCase = (self) => self.toLowerCase();
var capitalize = (self) => {
  if (self.length === 0) return self;
  return toUpperCase(self[0]) + self.slice(1);
};
var uncapitalize = (self) => {
  if (self.length === 0) return self;
  return toLowerCase(self[0]) + self.slice(1);
};
var replace = (searchValue, replaceValue) => (self) => self.replace(searchValue, replaceValue);
var trim = (self) => self.trim();
var trimStart = (self) => self.trimStart();
var trimEnd = (self) => self.trimEnd();
var slice = (start3, end3) => (self) => self.slice(start3, end3);
var isEmpty6 = (self) => self.length === 0;
var isNonEmpty6 = (self) => self.length > 0;
var length = (self) => self.length;
var split = dual(2, (self, separator) => {
  const out = self.split(separator);
  return isNonEmptyArray(out) ? out : [self];
});
var includes = (searchString, position) => (self) => self.includes(searchString, position);
var startsWith = (searchString, position) => (self) => self.startsWith(searchString, position);
var endsWith = (searchString, position) => (self) => self.endsWith(searchString, position);
var charCodeAt = dual(2, (self, index) => filter(some(self.charCodeAt(index)), (charCode) => !isNaN(charCode)));
var substring = (start3, end3) => (self) => self.substring(start3, end3);
var at = dual(2, (self, index) => fromNullable(self.at(index)));
var charAt = dual(2, (self, index) => filter(some(self.charAt(index)), isNonEmpty6));
var codePointAt = dual(2, (self, index) => fromNullable(self.codePointAt(index)));
var indexOf = (searchString) => (self) => filter(some(self.indexOf(searchString)), greaterThanOrEqualTo3(0));
var lastIndexOf = (searchString) => (self) => filter(some(self.lastIndexOf(searchString)), greaterThanOrEqualTo3(0));
var localeCompare = (that, locales, options) => (self) => sign(self.localeCompare(that, locales, options));
var match4 = (regexp) => (self) => fromNullable(self.match(regexp));
var matchAll = (regexp) => (self) => self.matchAll(regexp);
var normalize = (form) => (self) => self.normalize(form);
var padEnd = (maxLength, fillString) => (self) => self.padEnd(maxLength, fillString);
var padStart = (maxLength, fillString) => (self) => self.padStart(maxLength, fillString);
var repeat = (count2) => (self) => self.repeat(count2);
var replaceAll = (searchValue, replaceValue) => (self) => self.replaceAll(searchValue, replaceValue);
var search = dual(2, (self, regexp) => filter(some(self.search(regexp)), greaterThanOrEqualTo3(0)));
var toLocaleLowerCase = (locale) => (self) => self.toLocaleLowerCase(locale);
var toLocaleUpperCase = (locale) => (self) => self.toLocaleUpperCase(locale);
var takeLeft = dual(2, (self, n) => self.slice(0, Math.max(n, 0)));
var takeRight = dual(2, (self, n) => self.slice(Math.max(0, self.length - Math.floor(n)), Infinity));
var CR = 13;
var LF = 10;
var linesIterator = (self) => linesSeparated(self, true);
var linesWithSeparators = (s) => linesSeparated(s, false);
var stripMarginWith = dual(2, (self, marginChar) => {
  let out = "";
  for (const line of linesWithSeparators(self)) {
    let index = 0;
    while (index < line.length && line.charAt(index) <= " ") {
      index = index + 1;
    }
    const stripped = index < line.length && line.charAt(index) === marginChar ? line.substring(index + 1) : line;
    out = out + stripped;
  }
  return out;
});
var stripMargin = (self) => stripMarginWith(self, "|");
var snakeToCamel = (self) => {
  let str = self[0];
  for (let i = 1; i < self.length; i++) {
    str += self[i] === "_" ? self[++i].toUpperCase() : self[i];
  }
  return str;
};
var snakeToPascal = (self) => {
  let str = self[0].toUpperCase();
  for (let i = 1; i < self.length; i++) {
    str += self[i] === "_" ? self[++i].toUpperCase() : self[i];
  }
  return str;
};
var snakeToKebab = (self) => self.replace(/_/g, "-");
var camelToSnake = (self) => self.replace(/([A-Z])/g, "_$1").toLowerCase();
var pascalToSnake = (self) => (self.slice(0, 1) + self.slice(1).replace(/([A-Z])/g, "_$1")).toLowerCase();
var kebabToSnake = (self) => self.replace(/-/g, "_");
var LinesIterator = class _LinesIterator {
  s;
  stripped;
  index;
  length;
  constructor(s, stripped = false) {
    this.s = s;
    this.stripped = stripped;
    this.index = 0;
    this.length = s.length;
  }
  next() {
    if (this.done) {
      return {
        done: true,
        value: void 0
      };
    }
    const start3 = this.index;
    while (!this.done && !isLineBreak(this.s[this.index])) {
      this.index = this.index + 1;
    }
    let end3 = this.index;
    if (!this.done) {
      const char = this.s[this.index];
      this.index = this.index + 1;
      if (!this.done && isLineBreak2(char, this.s[this.index])) {
        this.index = this.index + 1;
      }
      if (!this.stripped) {
        end3 = this.index;
      }
    }
    return {
      done: false,
      value: this.s.substring(start3, end3)
    };
  }
  [Symbol.iterator]() {
    return new _LinesIterator(this.s, this.stripped);
  }
  get done() {
    return this.index >= this.length;
  }
};
var isLineBreak = (char) => {
  const code = char.charCodeAt(0);
  return code === CR || code === LF;
};
var isLineBreak2 = (char0, char1) => char0.charCodeAt(0) === CR && char1.charCodeAt(0) === LF;
var linesSeparated = (self, stripped) => new LinesIterator(self, stripped);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cron.js
var Cron_exports = {};
__export(Cron_exports, {
  Equivalence: () => Equivalence3,
  ParseError: () => ParseError,
  ParseErrorTypeId: () => ParseErrorTypeId,
  TypeId: () => TypeId2,
  equals: () => equals2,
  isCron: () => isCron,
  isParseError: () => isParseError,
  make: () => make10,
  match: () => match6,
  next: () => next2,
  parse: () => parse,
  sequence: () => sequence,
  unsafeParse: () => unsafeParse
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/dateTime.js
var TypeId = /* @__PURE__ */ Symbol.for("effect/DateTime");
var TimeZoneTypeId = /* @__PURE__ */ Symbol.for("effect/DateTime/TimeZone");
var Proto = {
  [TypeId]: TypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  [NodeInspectSymbol]() {
    return this.toString();
  },
  toJSON() {
    return toDateUtc(this).toJSON();
  }
};
var ProtoUtc = {
  ...Proto,
  _tag: "Utc",
  [symbol]() {
    return cached(this, number2(this.epochMillis));
  },
  [symbol2](that) {
    return isDateTime(that) && that._tag === "Utc" && this.epochMillis === that.epochMillis;
  },
  toString() {
    return `DateTime.Utc(${toDateUtc(this).toJSON()})`;
  }
};
var ProtoZoned = {
  ...Proto,
  _tag: "Zoned",
  [symbol]() {
    return pipe(number2(this.epochMillis), combine(hash(this.zone)), cached(this));
  },
  [symbol2](that) {
    return isDateTime(that) && that._tag === "Zoned" && this.epochMillis === that.epochMillis && equals(this.zone, that.zone);
  },
  toString() {
    return `DateTime.Zoned(${formatIsoZoned(this)})`;
  }
};
var ProtoTimeZone = {
  [TimeZoneTypeId]: TimeZoneTypeId,
  [NodeInspectSymbol]() {
    return this.toString();
  }
};
var ProtoTimeZoneNamed = {
  ...ProtoTimeZone,
  _tag: "Named",
  [symbol]() {
    return cached(this, string2(`Named:${this.id}`));
  },
  [symbol2](that) {
    return isTimeZone(that) && that._tag === "Named" && this.id === that.id;
  },
  toString() {
    return `TimeZone.Named(${this.id})`;
  },
  toJSON() {
    return {
      _id: "TimeZone",
      _tag: "Named",
      id: this.id
    };
  }
};
var ProtoTimeZoneOffset = {
  ...ProtoTimeZone,
  _tag: "Offset",
  [symbol]() {
    return cached(this, string2(`Offset:${this.offset}`));
  },
  [symbol2](that) {
    return isTimeZone(that) && that._tag === "Offset" && this.offset === that.offset;
  },
  toString() {
    return `TimeZone.Offset(${offsetToString(this.offset)})`;
  },
  toJSON() {
    return {
      _id: "TimeZone",
      _tag: "Offset",
      offset: this.offset
    };
  }
};
var makeZonedProto = (epochMillis, zone, partsUtc) => {
  const self = Object.create(ProtoZoned);
  self.epochMillis = epochMillis;
  self.zone = zone;
  Object.defineProperty(self, "partsUtc", {
    value: partsUtc,
    enumerable: false,
    writable: true
  });
  Object.defineProperty(self, "adjustedEpochMillis", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  Object.defineProperty(self, "partsAdjusted", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  return self;
};
var isDateTime = (u) => hasProperty(u, TypeId);
var isDateTimeArgs = (args) => isDateTime(args[0]);
var isTimeZone = (u) => hasProperty(u, TimeZoneTypeId);
var isTimeZoneOffset = (u) => isTimeZone(u) && u._tag === "Offset";
var isTimeZoneNamed = (u) => isTimeZone(u) && u._tag === "Named";
var isUtc = (self) => self._tag === "Utc";
var isZoned = (self) => self._tag === "Zoned";
var Equivalence2 = make((a, b) => a.epochMillis === b.epochMillis);
var Order3 = make2((self, that) => self.epochMillis < that.epochMillis ? -1 : self.epochMillis > that.epochMillis ? 1 : 0);
var clamp2 = clamp(Order3);
var makeUtc = (epochMillis) => {
  const self = Object.create(ProtoUtc);
  self.epochMillis = epochMillis;
  Object.defineProperty(self, "partsUtc", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  return self;
};
var unsafeFromDate = (date) => {
  const epochMillis = date.getTime();
  if (Number.isNaN(epochMillis)) {
    throw new IllegalArgumentException2("Invalid date");
  }
  return makeUtc(epochMillis);
};
var unsafeMake2 = (input) => {
  if (isDateTime(input)) {
    return input;
  } else if (input instanceof Date) {
    return unsafeFromDate(input);
  } else if (typeof input === "object") {
    const date = /* @__PURE__ */ new Date(0);
    setPartsDate(date, input);
    return unsafeFromDate(date);
  } else if (typeof input === "string" && !hasZone(input)) {
    return unsafeFromDate(/* @__PURE__ */ new Date(input + "Z"));
  }
  return unsafeFromDate(new Date(input));
};
var hasZone = (input) => /Z|[+-]\d{2}$|[+-]\d{2}:?\d{2}$|\]$/.test(input);
var minEpochMillis = -864e13 + 12 * 60 * 60 * 1e3;
var maxEpochMillis = 864e13 - 14 * 60 * 60 * 1e3;
var unsafeMakeZoned = (input, options) => {
  if (options?.timeZone === void 0 && isDateTime(input) && isZoned(input)) {
    return input;
  }
  const self = unsafeMake2(input);
  if (self.epochMillis < minEpochMillis || self.epochMillis > maxEpochMillis) {
    throw new RangeError(`Epoch millis out of range: ${self.epochMillis}`);
  }
  let zone;
  if (options?.timeZone === void 0) {
    const offset = new Date(self.epochMillis).getTimezoneOffset() * -60 * 1e3;
    zone = zoneMakeOffset(offset);
  } else if (isTimeZone(options?.timeZone)) {
    zone = options.timeZone;
  } else if (typeof options?.timeZone === "number") {
    zone = zoneMakeOffset(options.timeZone);
  } else {
    const parsedZone = zoneFromString(options.timeZone);
    if (isNone(parsedZone)) {
      throw new IllegalArgumentException2(`Invalid time zone: ${options.timeZone}`);
    }
    zone = parsedZone.value;
  }
  if (options?.adjustForTimeZone !== true) {
    return makeZonedProto(self.epochMillis, zone, self.partsUtc);
  }
  return makeZonedFromAdjusted(self.epochMillis, zone, options?.disambiguation ?? "compatible");
};
var makeZoned = liftThrowable(unsafeMakeZoned);
var make9 = liftThrowable(unsafeMake2);
var zonedStringRegex = /^(.{17,35})\[(.+)\]$/;
var makeZonedFromString = (input) => {
  const match7 = zonedStringRegex.exec(input);
  if (match7 === null) {
    const offset = parseOffset(input);
    return offset !== null ? makeZoned(input, {
      timeZone: offset
    }) : none();
  }
  const [, isoString, timeZone] = match7;
  return makeZoned(isoString, {
    timeZone
  });
};
var now = map4(currentTimeMillis, makeUtc);
var nowAsDate = map4(currentTimeMillis, (millis2) => new Date(millis2));
var unsafeNow = () => makeUtc(Date.now());
var toUtc = (self) => makeUtc(self.epochMillis);
var setZone = dual(isDateTimeArgs, (self, zone, options) => options?.adjustForTimeZone === true ? makeZonedFromAdjusted(self.epochMillis, zone, options?.disambiguation ?? "compatible") : makeZonedProto(self.epochMillis, zone, self.partsUtc));
var setZoneOffset = dual(isDateTimeArgs, (self, offset, options) => setZone(self, zoneMakeOffset(offset), options));
var validZoneCache = globalValue("effect/DateTime/validZoneCache", () => /* @__PURE__ */ new Map());
var formatOptions = {
  day: "numeric",
  month: "numeric",
  year: "numeric",
  hour: "numeric",
  minute: "numeric",
  second: "numeric",
  timeZoneName: "longOffset",
  fractionalSecondDigits: 3,
  hourCycle: "h23"
};
var zoneMakeIntl = (format3) => {
  const zoneId = format3.resolvedOptions().timeZone;
  if (validZoneCache.has(zoneId)) {
    return validZoneCache.get(zoneId);
  }
  const zone = Object.create(ProtoTimeZoneNamed);
  zone.id = zoneId;
  zone.format = format3;
  validZoneCache.set(zoneId, zone);
  return zone;
};
var zoneUnsafeMakeNamed = (zoneId) => {
  if (validZoneCache.has(zoneId)) {
    return validZoneCache.get(zoneId);
  }
  try {
    return zoneMakeIntl(new Intl.DateTimeFormat("en-US", {
      ...formatOptions,
      timeZone: zoneId
    }));
  } catch {
    throw new IllegalArgumentException2(`Invalid time zone: ${zoneId}`);
  }
};
var zoneMakeOffset = (offset) => {
  const zone = Object.create(ProtoTimeZoneOffset);
  zone.offset = offset;
  return zone;
};
var zoneMakeNamed = liftThrowable(zoneUnsafeMakeNamed);
var zoneMakeNamedEffect = (zoneId) => try_({
  try: () => zoneUnsafeMakeNamed(zoneId),
  catch: (e) => e
});
var zoneMakeLocal = () => zoneMakeIntl(new Intl.DateTimeFormat("en-US", formatOptions));
var offsetZoneRegex = /^(?:GMT|[+-])/;
var zoneFromString = (zone) => {
  if (offsetZoneRegex.test(zone)) {
    const offset = parseOffset(zone);
    return offset === null ? none() : some(zoneMakeOffset(offset));
  }
  return zoneMakeNamed(zone);
};
var zoneToString = (self) => {
  if (self._tag === "Offset") {
    return offsetToString(self.offset);
  }
  return self.id;
};
var setZoneNamed = dual(isDateTimeArgs, (self, zoneId, options) => map2(zoneMakeNamed(zoneId), (zone) => setZone(self, zone, options)));
var unsafeSetZoneNamed = dual(isDateTimeArgs, (self, zoneId, options) => setZone(self, zoneUnsafeMakeNamed(zoneId), options));
var distance = dual(2, (self, other) => toEpochMillis(other) - toEpochMillis(self));
var distanceDurationEither = dual(2, (self, other) => {
  const diffMillis = distance(self, other);
  return diffMillis > 0 ? right(millis(diffMillis)) : left(millis(-diffMillis));
});
var distanceDuration = dual(2, (self, other) => millis(Math.abs(distance(self, other))));
var min4 = min(Order3);
var max6 = max(Order3);
var greaterThan2 = greaterThan(Order3);
var greaterThanOrEqualTo4 = greaterThanOrEqualTo(Order3);
var lessThan7 = lessThan(Order3);
var lessThanOrEqualTo2 = lessThanOrEqualTo(Order3);
var between2 = between(Order3);
var isFuture = (self) => map4(now, lessThan7(self));
var unsafeIsFuture = (self) => lessThan7(unsafeNow(), self);
var isPast = (self) => map4(now, greaterThan2(self));
var unsafeIsPast = (self) => greaterThan2(unsafeNow(), self);
var toDateUtc = (self) => new Date(self.epochMillis);
var toDate = (self) => {
  if (self._tag === "Utc") {
    return new Date(self.epochMillis);
  } else if (self.zone._tag === "Offset") {
    return new Date(self.epochMillis + self.zone.offset);
  } else if (self.adjustedEpochMillis !== void 0) {
    return new Date(self.adjustedEpochMillis);
  }
  const parts = self.zone.format.formatToParts(self.epochMillis).filter((_) => _.type !== "literal");
  const date = /* @__PURE__ */ new Date(0);
  date.setUTCFullYear(Number(parts[2].value), Number(parts[0].value) - 1, Number(parts[1].value));
  date.setUTCHours(Number(parts[3].value), Number(parts[4].value), Number(parts[5].value), Number(parts[6].value));
  self.adjustedEpochMillis = date.getTime();
  return date;
};
var zonedOffset = (self) => {
  const date = toDate(self);
  return date.getTime() - toEpochMillis(self);
};
var offsetToString = (offset) => {
  const abs = Math.abs(offset);
  let hours = Math.floor(abs / (60 * 60 * 1e3));
  let minutes = Math.round(abs % (60 * 60 * 1e3) / (60 * 1e3));
  if (minutes === 60) {
    hours += 1;
    minutes = 0;
  }
  return `${offset < 0 ? "-" : "+"}${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}`;
};
var zonedOffsetIso = (self) => offsetToString(zonedOffset(self));
var toEpochMillis = (self) => self.epochMillis;
var removeTime = (self) => withDate(self, (date) => {
  date.setUTCHours(0, 0, 0, 0);
  return makeUtc(date.getTime());
});
var dateToParts = (date) => ({
  millis: date.getUTCMilliseconds(),
  seconds: date.getUTCSeconds(),
  minutes: date.getUTCMinutes(),
  hours: date.getUTCHours(),
  day: date.getUTCDate(),
  weekDay: date.getUTCDay(),
  month: date.getUTCMonth() + 1,
  year: date.getUTCFullYear()
});
var toParts = (self) => {
  if (self._tag === "Utc") {
    return toPartsUtc(self);
  } else if (self.partsAdjusted !== void 0) {
    return self.partsAdjusted;
  }
  self.partsAdjusted = withDate(self, dateToParts);
  return self.partsAdjusted;
};
var toPartsUtc = (self) => {
  if (self.partsUtc !== void 0) {
    return self.partsUtc;
  }
  self.partsUtc = withDateUtc(self, dateToParts);
  return self.partsUtc;
};
var getPartUtc = dual(2, (self, part) => toPartsUtc(self)[part]);
var getPart = dual(2, (self, part) => toParts(self)[part]);
var setPartsDate = (date, parts) => {
  if (parts.year !== void 0) {
    date.setUTCFullYear(parts.year);
  }
  if (parts.month !== void 0) {
    date.setUTCMonth(parts.month - 1);
  }
  if (parts.day !== void 0) {
    date.setUTCDate(parts.day);
  }
  if (parts.weekDay !== void 0) {
    const diff = parts.weekDay - date.getUTCDay();
    date.setUTCDate(date.getUTCDate() + diff);
  }
  if (parts.hours !== void 0) {
    date.setUTCHours(parts.hours);
  }
  if (parts.minutes !== void 0) {
    date.setUTCMinutes(parts.minutes);
  }
  if (parts.seconds !== void 0) {
    date.setUTCSeconds(parts.seconds);
  }
  if (parts.millis !== void 0) {
    date.setUTCMilliseconds(parts.millis);
  }
};
var setParts = dual(2, (self, parts) => mutate(self, (date) => setPartsDate(date, parts)));
var setPartsUtc = dual(2, (self, parts) => mutateUtc(self, (date) => setPartsDate(date, parts)));
var constDayMillis = 24 * 60 * 60 * 1e3;
var makeZonedFromAdjusted = (adjustedMillis, zone, disambiguation) => {
  if (zone._tag === "Offset") {
    return makeZonedProto(adjustedMillis - zone.offset, zone);
  }
  const beforeOffset = calculateNamedOffset(adjustedMillis - constDayMillis, adjustedMillis, zone);
  const afterOffset = calculateNamedOffset(adjustedMillis + constDayMillis, adjustedMillis, zone);
  if (beforeOffset === afterOffset) {
    return makeZonedProto(adjustedMillis - beforeOffset, zone);
  }
  const isForwards = beforeOffset < afterOffset;
  const transitionMillis = beforeOffset - afterOffset;
  if (isForwards) {
    const currentAfterOffset = calculateNamedOffset(adjustedMillis - afterOffset, adjustedMillis, zone);
    if (currentAfterOffset === afterOffset) {
      return makeZonedProto(adjustedMillis - afterOffset, zone);
    }
    const before3 = makeZonedProto(adjustedMillis - beforeOffset, zone);
    const beforeAdjustedMillis = toDate(before3).getTime();
    if (adjustedMillis !== beforeAdjustedMillis) {
      switch (disambiguation) {
        case "reject": {
          const formatted = new Date(adjustedMillis).toISOString();
          throw new RangeError(`Gap time: ${formatted} does not exist in time zone ${zone.id}`);
        }
        case "earlier":
          return makeZonedProto(adjustedMillis - afterOffset, zone);
        case "compatible":
        case "later":
          return before3;
      }
    }
    return before3;
  }
  const currentBeforeOffset = calculateNamedOffset(adjustedMillis - beforeOffset, adjustedMillis, zone);
  if (currentBeforeOffset === beforeOffset) {
    if (disambiguation === "earlier" || disambiguation === "compatible") {
      return makeZonedProto(adjustedMillis - beforeOffset, zone);
    }
    const laterOffset = calculateNamedOffset(adjustedMillis - beforeOffset + transitionMillis, adjustedMillis + transitionMillis, zone);
    if (laterOffset === beforeOffset) {
      return makeZonedProto(adjustedMillis - beforeOffset, zone);
    }
    if (disambiguation === "reject") {
      const formatted = new Date(adjustedMillis).toISOString();
      throw new RangeError(`Ambiguous time: ${formatted} occurs twice in time zone ${zone.id}`);
    }
  }
  return makeZonedProto(adjustedMillis - afterOffset, zone);
};
var offsetRegex = /([+-])(\d{2}):(\d{2})$/;
var parseOffset = (offset) => {
  const match7 = offsetRegex.exec(offset);
  if (match7 === null) {
    return null;
  }
  const [, sign2, hours, minutes] = match7;
  return (sign2 === "+" ? 1 : -1) * (Number(hours) * 60 + Number(minutes)) * 60 * 1e3;
};
var calculateNamedOffset = (utcMillis, adjustedMillis, zone) => {
  const offset = zone.format.formatToParts(utcMillis).find((_) => _.type === "timeZoneName")?.value ?? "";
  if (offset === "GMT") {
    return 0;
  }
  const result = parseOffset(offset);
  if (result === null) {
    return zonedOffset(makeZonedProto(adjustedMillis, zone));
  }
  return result;
};
var mutate = dual(isDateTimeArgs, (self, f, options) => {
  if (self._tag === "Utc") {
    const date = toDateUtc(self);
    f(date);
    return makeUtc(date.getTime());
  }
  const adjustedDate = toDate(self);
  const newAdjustedDate = new Date(adjustedDate.getTime());
  f(newAdjustedDate);
  return makeZonedFromAdjusted(newAdjustedDate.getTime(), self.zone, options?.disambiguation ?? "compatible");
});
var mutateUtc = dual(2, (self, f) => mapEpochMillis(self, (millis2) => {
  const date = new Date(millis2);
  f(date);
  return date.getTime();
}));
var mapEpochMillis = dual(2, (self, f) => {
  const millis2 = f(toEpochMillis(self));
  return self._tag === "Utc" ? makeUtc(millis2) : makeZonedProto(millis2, self.zone);
});
var withDate = dual(2, (self, f) => f(toDate(self)));
var withDateUtc = dual(2, (self, f) => f(toDateUtc(self)));
var match5 = dual(2, (self, options) => self._tag === "Utc" ? options.onUtc(self) : options.onZoned(self));
var addDuration = dual(2, (self, duration2) => mapEpochMillis(self, (millis2) => millis2 + toMillis(duration2)));
var subtractDuration = dual(2, (self, duration2) => mapEpochMillis(self, (millis2) => millis2 - toMillis(duration2)));
var addMillis = (date, amount) => {
  date.setTime(date.getTime() + amount);
};
var add2 = dual(2, (self, parts) => mutate(self, (date) => {
  if (parts.millis) {
    addMillis(date, parts.millis);
  }
  if (parts.seconds) {
    addMillis(date, parts.seconds * 1e3);
  }
  if (parts.minutes) {
    addMillis(date, parts.minutes * 60 * 1e3);
  }
  if (parts.hours) {
    addMillis(date, parts.hours * 60 * 60 * 1e3);
  }
  if (parts.days) {
    date.setUTCDate(date.getUTCDate() + parts.days);
  }
  if (parts.weeks) {
    date.setUTCDate(date.getUTCDate() + parts.weeks * 7);
  }
  if (parts.months) {
    const day = date.getUTCDate();
    date.setUTCMonth(date.getUTCMonth() + parts.months + 1, 0);
    if (day < date.getUTCDate()) {
      date.setUTCDate(day);
    }
  }
  if (parts.years) {
    const day = date.getUTCDate();
    const month = date.getUTCMonth();
    date.setUTCFullYear(date.getUTCFullYear() + parts.years, month + 1, 0);
    if (day < date.getUTCDate()) {
      date.setUTCDate(day);
    }
  }
}));
var subtract = dual(2, (self, parts) => {
  const newParts = {};
  for (const key in parts) {
    newParts[key] = -1 * parts[key];
  }
  return add2(self, newParts);
});
var startOfDate = (date, part, options) => {
  switch (part) {
    case "second": {
      date.setUTCMilliseconds(0);
      break;
    }
    case "minute": {
      date.setUTCSeconds(0, 0);
      break;
    }
    case "hour": {
      date.setUTCMinutes(0, 0, 0);
      break;
    }
    case "day": {
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "week": {
      const weekStartsOn = options?.weekStartsOn ?? 0;
      const day = date.getUTCDay();
      const diff = (day - weekStartsOn + 7) % 7;
      date.setUTCDate(date.getUTCDate() - diff);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "month": {
      date.setUTCDate(1);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "year": {
      date.setUTCMonth(0, 1);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
  }
};
var startOf = dual(isDateTimeArgs, (self, part, options) => mutate(self, (date) => startOfDate(date, part, options)));
var endOfDate = (date, part, options) => {
  switch (part) {
    case "second": {
      date.setUTCMilliseconds(999);
      break;
    }
    case "minute": {
      date.setUTCSeconds(59, 999);
      break;
    }
    case "hour": {
      date.setUTCMinutes(59, 59, 999);
      break;
    }
    case "day": {
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "week": {
      const weekStartsOn = options?.weekStartsOn ?? 0;
      const day = date.getUTCDay();
      const diff = (day - weekStartsOn + 7) % 7;
      date.setUTCDate(date.getUTCDate() - diff + 6);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "month": {
      date.setUTCMonth(date.getUTCMonth() + 1, 0);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "year": {
      date.setUTCMonth(11, 31);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
  }
};
var endOf = dual(isDateTimeArgs, (self, part, options) => mutate(self, (date) => endOfDate(date, part, options)));
var nearest = dual(isDateTimeArgs, (self, part, options) => mutate(self, (date) => {
  if (part === "milli") return;
  const millis2 = date.getTime();
  const start3 = new Date(millis2);
  startOfDate(start3, part, options);
  const startMillis = start3.getTime();
  const end3 = new Date(millis2);
  endOfDate(end3, part, options);
  const endMillis = end3.getTime() + 1;
  const diffStart = millis2 - startMillis;
  const diffEnd = endMillis - millis2;
  if (diffStart < diffEnd) {
    date.setTime(startMillis);
  } else {
    date.setTime(endMillis);
  }
}));
var intlTimeZone = (self) => {
  if (self._tag === "Named") {
    return self.id;
  }
  return offsetToString(self.offset);
};
var format2 = dual(isDateTimeArgs, (self, options) => {
  try {
    return new Intl.DateTimeFormat(options?.locale, {
      timeZone: self._tag === "Utc" ? "UTC" : intlTimeZone(self.zone),
      ...options
    }).format(self.epochMillis);
  } catch {
    return new Intl.DateTimeFormat(options?.locale, {
      timeZone: "UTC",
      ...options
    }).format(toDate(self));
  }
});
var formatLocal = dual(isDateTimeArgs, (self, options) => new Intl.DateTimeFormat(options?.locale, options).format(self.epochMillis));
var formatUtc = dual(isDateTimeArgs, (self, options) => new Intl.DateTimeFormat(options?.locale, {
  ...options,
  timeZone: "UTC"
}).format(self.epochMillis));
var formatIntl = dual(2, (self, format3) => format3.format(self.epochMillis));
var formatIso = (self) => toDateUtc(self).toISOString();
var formatIsoDate = (self) => toDate(self).toISOString().slice(0, 10);
var formatIsoDateUtc = (self) => toDateUtc(self).toISOString().slice(0, 10);
var formatIsoOffset = (self) => {
  const date = toDate(self);
  return self._tag === "Utc" ? date.toISOString() : `${date.toISOString().slice(0, -1)}${zonedOffsetIso(self)}`;
};
var formatIsoZoned = (self) => self.zone._tag === "Offset" ? formatIsoOffset(self) : `${formatIsoOffset(self)}[${self.zone.id}]`;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cron.js
var TypeId2 = /* @__PURE__ */ Symbol.for("effect/Cron");
var CronProto = {
  [TypeId2]: TypeId2,
  [symbol2](that) {
    return isCron(that) && equals2(this, that);
  },
  [symbol]() {
    return pipe(hash(this.tz), combine(array2(fromIterable(this.seconds))), combine(array2(fromIterable(this.minutes))), combine(array2(fromIterable(this.hours))), combine(array2(fromIterable(this.days))), combine(array2(fromIterable(this.months))), combine(array2(fromIterable(this.weekdays))), cached(this));
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Cron",
      tz: this.tz,
      seconds: fromIterable(this.seconds),
      minutes: fromIterable(this.minutes),
      hours: fromIterable(this.hours),
      days: fromIterable(this.days),
      months: fromIterable(this.months),
      weekdays: fromIterable(this.weekdays)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isCron = (u) => hasProperty(u, TypeId2);
var make10 = (values) => {
  const o = Object.create(CronProto);
  o.seconds = new Set(sort(values.seconds ?? [0], Order));
  o.minutes = new Set(sort(values.minutes, Order));
  o.hours = new Set(sort(values.hours, Order));
  o.days = new Set(sort(values.days, Order));
  o.months = new Set(sort(values.months, Order));
  o.weekdays = new Set(sort(values.weekdays, Order));
  o.tz = fromNullable(values.tz);
  const seconds = Array.from(o.seconds);
  const minutes = Array.from(o.minutes);
  const hours = Array.from(o.hours);
  const days = Array.from(o.days);
  const months = Array.from(o.months);
  const weekdays = Array.from(o.weekdays);
  o.first = {
    second: seconds[0] ?? 0,
    minute: minutes[0] ?? 0,
    hour: hours[0] ?? 0,
    day: days[0] ?? 1,
    month: (months[0] ?? 1) - 1,
    weekday: weekdays[0] ?? 0
  };
  o.next = {
    second: nextLookupTable(seconds, 60),
    minute: nextLookupTable(minutes, 60),
    hour: nextLookupTable(hours, 24),
    day: nextLookupTable(days, 32),
    month: nextLookupTable(months, 13),
    weekday: nextLookupTable(weekdays, 7)
  };
  return o;
};
var nextLookupTable = (values, size5) => {
  const result = new Array(size5).fill(void 0);
  if (values.length === 0) {
    return result;
  }
  let current = void 0;
  let index = values.length - 1;
  for (let i = size5 - 1; i >= 0; i--) {
    while (index >= 0 && values[index] >= i) {
      current = values[index--];
    }
    result[i] = current;
  }
  return result;
};
var ParseErrorTypeId = /* @__PURE__ */ Symbol.for("effect/Cron/errors/ParseError");
var ParseError = class extends TaggedError("CronParseError") {
  /**
   * @since 2.0.0
   */
  [ParseErrorTypeId] = ParseErrorTypeId;
};
var isParseError = (u) => hasProperty(u, ParseErrorTypeId);
var parse = (cron2, tz) => {
  const segments = cron2.split(" ").filter(isNonEmpty6);
  if (segments.length !== 5 && segments.length !== 6) {
    return left(new ParseError({
      message: `Invalid number of segments in cron expression`,
      input: cron2
    }));
  }
  if (segments.length === 5) {
    segments.unshift("0");
  }
  const [seconds, minutes, hours, days, months, weekdays] = segments;
  const zone = tz === void 0 || isTimeZone(tz) ? right(tz) : fromOption(zoneFromString(tz), () => new ParseError({
    message: `Invalid time zone in cron expression`,
    input: tz
  }));
  return all({
    tz: zone,
    seconds: parseSegment(seconds, secondOptions),
    minutes: parseSegment(minutes, minuteOptions),
    hours: parseSegment(hours, hourOptions),
    days: parseSegment(days, dayOptions),
    months: parseSegment(months, monthOptions),
    weekdays: parseSegment(weekdays, weekdayOptions)
  }).pipe(map(make10));
};
var unsafeParse = (cron2, tz) => getOrThrowWith(parse(cron2, tz), identity);
var match6 = (cron2, date) => {
  const parts = unsafeMakeZoned(date, {
    timeZone: getOrUndefined(cron2.tz)
  }).pipe(toParts);
  if (cron2.seconds.size !== 0 && !cron2.seconds.has(parts.seconds)) {
    return false;
  }
  if (cron2.minutes.size !== 0 && !cron2.minutes.has(parts.minutes)) {
    return false;
  }
  if (cron2.hours.size !== 0 && !cron2.hours.has(parts.hours)) {
    return false;
  }
  if (cron2.months.size !== 0 && !cron2.months.has(parts.month)) {
    return false;
  }
  if (cron2.days.size === 0 && cron2.weekdays.size === 0) {
    return true;
  }
  if (cron2.weekdays.size === 0) {
    return cron2.days.has(parts.day);
  }
  if (cron2.days.size === 0) {
    return cron2.weekdays.has(parts.weekDay);
  }
  return cron2.days.has(parts.day) || cron2.weekdays.has(parts.weekDay);
};
var daysInMonth = (date) => new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth() + 1, 0)).getUTCDate();
var next2 = (cron2, startFrom) => {
  const tz = getOrUndefined(cron2.tz);
  const zoned = unsafeMakeZoned(startFrom ?? /* @__PURE__ */ new Date(), {
    timeZone: tz
  });
  const utc = tz !== void 0 && isTimeZoneNamed(tz) && tz.id === "UTC";
  const adjustDst = utc ? constVoid : (current) => {
    const adjusted = unsafeMakeZoned(current, {
      timeZone: zoned.zone,
      adjustForTimeZone: true
    }).pipe(toDate);
    const drift = current.getTime() - adjusted.getTime();
    if (drift > 0) {
      current.setTime(current.getTime() + drift);
    }
  };
  const result = mutate(zoned, (current) => {
    current.setUTCSeconds(current.getUTCSeconds() + 1, 0);
    for (let i = 0; i < 1e4; i++) {
      if (cron2.seconds.size !== 0) {
        const currentSecond = current.getUTCSeconds();
        const nextSecond2 = cron2.next.second[currentSecond];
        if (nextSecond2 === void 0) {
          current.setUTCMinutes(current.getUTCMinutes() + 1, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextSecond2 > currentSecond) {
          current.setUTCSeconds(nextSecond2);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.minutes.size !== 0) {
        const currentMinute = current.getUTCMinutes();
        const nextMinute2 = cron2.next.minute[currentMinute];
        if (nextMinute2 === void 0) {
          current.setUTCHours(current.getUTCHours() + 1, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextMinute2 > currentMinute) {
          current.setUTCMinutes(nextMinute2, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.hours.size !== 0) {
        const currentHour = current.getUTCHours();
        const nextHour2 = cron2.next.hour[currentHour];
        if (nextHour2 === void 0) {
          current.setUTCDate(current.getUTCDate() + 1);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextHour2 > currentHour) {
          current.setUTCHours(nextHour2, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.weekdays.size !== 0 || cron2.days.size !== 0) {
        let a = Infinity;
        let b = Infinity;
        if (cron2.weekdays.size !== 0) {
          const currentWeekday = current.getUTCDay();
          const nextWeekday = cron2.next.weekday[currentWeekday];
          a = nextWeekday === void 0 ? 7 - currentWeekday + cron2.first.weekday : nextWeekday - currentWeekday;
        }
        if (cron2.days.size !== 0 && a !== 0) {
          const currentDay = current.getUTCDate();
          const nextDay2 = cron2.next.day[currentDay];
          b = nextDay2 === void 0 ? daysInMonth(current) - currentDay + cron2.first.day : nextDay2 - currentDay;
        }
        const addDays = Math.min(a, b);
        if (addDays !== 0) {
          current.setUTCDate(current.getUTCDate() + addDays);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.months.size !== 0) {
        const currentMonth = current.getUTCMonth() + 1;
        const nextMonth = cron2.next.month[currentMonth];
        if (nextMonth === void 0) {
          current.setUTCFullYear(current.getUTCFullYear() + 1);
          current.setUTCMonth(cron2.first.month, cron2.first.day);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextMonth > currentMonth) {
          current.setUTCMonth(nextMonth - 1, cron2.first.day);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      return;
    }
    throw new Error("Unable to find next cron date");
  });
  return toDateUtc(result);
};
var sequence = function* (cron2, startFrom) {
  while (true) {
    yield startFrom = next2(cron2, startFrom);
  }
};
var Equivalence3 = make((self, that) => restrictionsEquals(self.seconds, that.seconds) && restrictionsEquals(self.minutes, that.minutes) && restrictionsEquals(self.hours, that.hours) && restrictionsEquals(self.days, that.days) && restrictionsEquals(self.months, that.months) && restrictionsEquals(self.weekdays, that.weekdays));
var restrictionsArrayEquals = array(number);
var restrictionsEquals = (self, that) => restrictionsArrayEquals(fromIterable(self), fromIterable(that));
var equals2 = dual(2, (self, that) => Equivalence3(self, that));
var secondOptions = {
  min: 0,
  max: 59
};
var minuteOptions = {
  min: 0,
  max: 59
};
var hourOptions = {
  min: 0,
  max: 23
};
var dayOptions = {
  min: 1,
  max: 31
};
var monthOptions = {
  min: 1,
  max: 12,
  aliases: {
    jan: 1,
    feb: 2,
    mar: 3,
    apr: 4,
    may: 5,
    jun: 6,
    jul: 7,
    aug: 8,
    sep: 9,
    oct: 10,
    nov: 11,
    dec: 12
  }
};
var weekdayOptions = {
  min: 0,
  max: 6,
  aliases: {
    sun: 0,
    mon: 1,
    tue: 2,
    wed: 3,
    thu: 4,
    fri: 5,
    sat: 6
  }
};
var parseSegment = (input, options) => {
  const capacity = options.max - options.min + 1;
  const values = /* @__PURE__ */ new Set();
  const fields = input.split(",");
  for (const field of fields) {
    const [raw, step] = splitStep(field);
    if (raw === "*" && step === void 0) {
      return right(/* @__PURE__ */ new Set());
    }
    if (step !== void 0) {
      if (!Number.isInteger(step)) {
        return left(new ParseError({
          message: `Expected step value to be a positive integer`,
          input
        }));
      }
      if (step < 1) {
        return left(new ParseError({
          message: `Expected step value to be greater than 0`,
          input
        }));
      }
      if (step > options.max) {
        return left(new ParseError({
          message: `Expected step value to be less than ${options.max}`,
          input
        }));
      }
    }
    if (raw === "*") {
      for (let i = options.min; i <= options.max; i += step ?? 1) {
        values.add(i);
      }
    } else {
      const [left2, right2] = splitRange(raw, options.aliases);
      if (!Number.isInteger(left2)) {
        return left(new ParseError({
          message: `Expected a positive integer`,
          input
        }));
      }
      if (left2 < options.min || left2 > options.max) {
        return left(new ParseError({
          message: `Expected a value between ${options.min} and ${options.max}`,
          input
        }));
      }
      if (right2 === void 0) {
        values.add(left2);
      } else {
        if (!Number.isInteger(right2)) {
          return left(new ParseError({
            message: `Expected a positive integer`,
            input
          }));
        }
        if (right2 < options.min || right2 > options.max) {
          return left(new ParseError({
            message: `Expected a value between ${options.min} and ${options.max}`,
            input
          }));
        }
        if (left2 > right2) {
          return left(new ParseError({
            message: `Invalid value range`,
            input
          }));
        }
        for (let i = left2; i <= right2; i += step ?? 1) {
          values.add(i);
        }
      }
    }
    if (values.size >= capacity) {
      return right(/* @__PURE__ */ new Set());
    }
  }
  return right(values);
};
var splitStep = (input) => {
  const seperator = input.indexOf("/");
  if (seperator !== -1) {
    return [input.slice(0, seperator), Number(input.slice(seperator + 1))];
  }
  return [input, void 0];
};
var splitRange = (input, aliases) => {
  const seperator = input.indexOf("-");
  if (seperator !== -1) {
    return [aliasOrValue(input.slice(0, seperator), aliases), aliasOrValue(input.slice(seperator + 1), aliases)];
  }
  return [aliasOrValue(input, aliases), void 0];
};
function aliasOrValue(field, aliases) {
  return aliases?.[field.toLocaleLowerCase()] ?? Number(field);
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Random.js
var Random_exports = {};
__export(Random_exports, {
  Random: () => Random,
  RandomTypeId: () => RandomTypeId2,
  choice: () => choice2,
  fixed: () => fixed2,
  make: () => make11,
  next: () => next3,
  nextBoolean: () => nextBoolean2,
  nextInt: () => nextInt2,
  nextIntBetween: () => nextIntBetween2,
  nextRange: () => nextRange2,
  randomWith: () => randomWith2,
  shuffle: () => shuffle2
});
var RandomTypeId2 = RandomTypeId;
var next3 = next;
var nextInt2 = nextInt;
var nextBoolean2 = nextBoolean;
var nextRange2 = nextRange;
var nextIntBetween2 = nextIntBetween;
var shuffle2 = shuffle;
var choice2 = choice;
var randomWith2 = randomWith;
var Random = randomTag;
var make11 = make4;
var fixed2 = fixed;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule.js
var ScheduleSymbolKey = "effect/Schedule";
var ScheduleTypeId = Symbol.for(ScheduleSymbolKey);
var isSchedule = (u) => hasProperty(u, ScheduleTypeId);
var ScheduleDriverSymbolKey = "effect/ScheduleDriver";
var ScheduleDriverTypeId = Symbol.for(ScheduleDriverSymbolKey);
var defaultIterationMetadata = {
  start: 0,
  now: 0,
  input: void 0,
  output: void 0,
  elapsed: zero,
  elapsedSincePrevious: zero,
  recurrence: 0
};
var CurrentIterationMetadata = Reference()("effect/Schedule/CurrentIterationMetadata", {
  defaultValue: () => defaultIterationMetadata
});
var scheduleVariance = {
  /* c8 ignore next */
  _Out: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var scheduleDriverVariance = {
  /* c8 ignore next */
  _Out: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var ScheduleImpl = class {
  initial;
  step;
  [ScheduleTypeId] = scheduleVariance;
  constructor(initial, step) {
    this.initial = initial;
    this.step = step;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var updateInfo = (iterationMetaRef, now2, input, output) => update(iterationMetaRef, (prev) => prev.recurrence === 0 ? {
  now: now2,
  input,
  output,
  recurrence: prev.recurrence + 1,
  elapsed: zero,
  elapsedSincePrevious: zero,
  start: now2
} : {
  now: now2,
  input,
  output,
  recurrence: prev.recurrence + 1,
  elapsed: millis(now2 - prev.start),
  elapsedSincePrevious: millis(now2 - prev.now),
  start: prev.start
});
var ScheduleDriverImpl = class {
  schedule;
  ref;
  [ScheduleDriverTypeId] = scheduleDriverVariance;
  constructor(schedule, ref) {
    this.schedule = schedule;
    this.ref = ref;
  }
  get state() {
    return map4(get(this.ref), (tuple2) => tuple2[1]);
  }
  get last() {
    return flatMap2(get(this.ref), ([element, _]) => {
      switch (element._tag) {
        case "None": {
          return failSync(() => new NoSuchElementException());
        }
        case "Some": {
          return succeed(element.value);
        }
      }
    });
  }
  iterationMeta = unsafeMake(defaultIterationMetadata);
  get reset() {
    return set(this.ref, [none(), this.schedule.initial]).pipe(zipLeft(set(this.iterationMeta, defaultIterationMetadata)));
  }
  next(input) {
    return pipe(map4(get(this.ref), (tuple2) => tuple2[1]), flatMap2((state) => pipe(currentTimeMillis, flatMap2((now2) => pipe(suspend(() => this.schedule.step(now2, input, state)), flatMap2(([state2, out, decision]) => {
      const setState = set(this.ref, [some(out), state2]);
      if (isDone2(decision)) {
        return setState.pipe(zipRight(fail2(none())));
      }
      const millis2 = start2(decision.intervals) - now2;
      if (millis2 <= 0) {
        return setState.pipe(zipRight(updateInfo(this.iterationMeta, now2, input, out)), as2(out));
      }
      const duration2 = millis(millis2);
      return pipe(setState, zipRight(updateInfo(this.iterationMeta, now2, input, out)), zipRight(sleep(duration2)), as2(out));
    }))))));
  }
};
var makeWithState = (initial, step) => new ScheduleImpl(initial, step);
var addDelay = dual(2, (self, f) => addDelayEffect(self, (out) => sync(() => f(out))));
var addDelayEffect = dual(2, (self, f) => modifyDelayEffect(self, (out, duration2) => map4(f(out), (delay) => sum(duration2, decode(delay)))));
var andThen3 = dual(2, (self, that) => map6(andThenEither(self, that), merge));
var andThenEither = dual(2, (self, that) => makeWithState([self.initial, that.initial, true], (now2, input, state) => state[2] ? flatMap2(self.step(now2, input, state[0]), ([lState, out, decision]) => {
  if (isDone2(decision)) {
    return map4(that.step(now2, input, state[1]), ([rState, out2, decision2]) => [[lState, rState, false], right(out2), decision2]);
  }
  return succeed([[lState, state[1], true], left(out), decision]);
}) : map4(that.step(now2, input, state[1]), ([rState, out, decision]) => [[state[0], rState, false], right(out), decision])));
var as4 = dual(2, (self, out) => map6(self, () => out));
var asVoid = (self) => map6(self, constVoid);
var bothInOut = dual(2, (self, that) => makeWithState([self.initial, that.initial], (now2, [in1, in2], state) => zipWith(self.step(now2, in1, state[0]), that.step(now2, in2, state[1]), ([lState, out, lDecision], [rState, out2, rDecision]) => {
  if (isContinue2(lDecision) && isContinue2(rDecision)) {
    const interval = pipe(lDecision.intervals, union4(rDecision.intervals));
    return [[lState, rState], [out, out2], _continue2(interval)];
  }
  return [[lState, rState], [out, out2], done2];
})));
var check = dual(2, (self, test) => checkEffect(self, (input, out) => sync(() => test(input, out))));
var checkEffect = dual(2, (self, test) => makeWithState(self.initial, (now2, input, state) => flatMap2(self.step(now2, input, state), ([state2, out, decision]) => {
  if (isDone2(decision)) {
    return succeed([state2, out, done2]);
  }
  return map4(test(input, out), (cont) => cont ? [state2, out, decision] : [state2, out, done2]);
})));
var collectAllInputs = () => collectAllOutputs(identity2());
var collectAllOutputs = (self) => reduce3(self, empty(), (outs, out) => pipe(outs, append(out)));
var collectUntil = (f) => collectAllOutputs(recurUntil(f));
var collectUntilEffect = (f) => collectAllOutputs(recurUntilEffect(f));
var collectWhile = (f) => collectAllOutputs(recurWhile(f));
var collectWhileEffect = (f) => collectAllOutputs(recurWhileEffect(f));
var compose = dual(2, (self, that) => makeWithState([self.initial, that.initial], (now2, input, state) => flatMap2(self.step(now2, input, state[0]), ([lState, out, lDecision]) => map4(that.step(now2, out, state[1]), ([rState, out2, rDecision]) => isDone2(lDecision) ? [[lState, rState], out2, done2] : isDone2(rDecision) ? [[lState, rState], out2, done2] : [[lState, rState], out2, _continue2(pipe(lDecision.intervals, max5(rDecision.intervals)))]))));
var mapInput = dual(2, (self, f) => mapInputEffect(self, (input2) => sync(() => f(input2))));
var mapInputContext2 = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => mapInputContext(self.step(now2, input, state), f)));
var mapInputEffect = dual(2, (self, f) => makeWithState(self.initial, (now2, input2, state) => flatMap2(f(input2), (input) => self.step(now2, input, state))));
var cron = (expression, tz) => {
  const parsed = isCron(expression) ? right(expression) : parse(expression, tz);
  return makeWithState([true, [Number.MIN_SAFE_INTEGER, 0, 0]], (now2, _, [initial, previous]) => {
    if (now2 < previous[0]) {
      return succeed([[false, previous], [previous[1], previous[2]], continueWith2(make6(previous[1], previous[2]))]);
    }
    if (isLeft(parsed)) {
      return die2(parsed.left);
    }
    const cron2 = parsed.right;
    const date = new Date(now2);
    let next4;
    if (initial && match6(cron2, date)) {
      next4 = now2;
    }
    next4 = next2(cron2, date).getTime();
    const start3 = beginningOfSecond(next4);
    const end3 = endOfSecond(next4);
    return succeed([[false, [next4, start3, end3]], [start3, end3], continueWith2(make6(start3, end3))]);
  });
};
var dayOfMonth = (day) => {
  return makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
    if (!Number.isInteger(day) || day < 1 || 31 < day) {
      return dieSync(() => new IllegalArgumentException(`Invalid argument in: dayOfMonth(${day}). Must be in range 1...31`));
    }
    const n = state[1];
    const initial = n === 0;
    const day0 = nextDayOfMonth(now2, day, initial);
    const start3 = beginningOfDay(day0);
    const end3 = endOfDay(day0);
    const interval = make6(start3, end3);
    return succeed([[end3, n + 1], n, continueWith2(interval)]);
  });
};
var dayOfWeek = (day) => {
  return makeWithState([Number.MIN_SAFE_INTEGER, 0], (now2, _, state) => {
    if (!Number.isInteger(day) || day < 1 || 7 < day) {
      return dieSync(() => new IllegalArgumentException(`Invalid argument in: dayOfWeek(${day}). Must be in range 1 (Monday)...7 (Sunday)`));
    }
    const n = state[1];
    const initial = n === 0;
    const day0 = nextDay(now2, day, initial);
    const start3 = beginningOfDay(day0);
    const end3 = endOfDay(day0);
    const interval = make6(start3, end3);
    return succeed([[end3, n + 1], n, continueWith2(interval)]);
  });
};
var delayed = dual(2, (self, f) => delayedEffect(self, (duration2) => sync(() => f(duration2))));
var delayedEffect = dual(2, (self, f) => modifyDelayEffect(self, (_, delay) => f(delay)));
var delayedSchedule = (schedule) => addDelay(schedule, (x) => x);
var delays = (self) => makeWithState(self.initial, (now2, input, state) => pipe(self.step(now2, input, state), flatMap2(([state2, _, decision]) => {
  if (isDone2(decision)) {
    return succeed([state2, zero, decision]);
  }
  return succeed([state2, millis(start2(decision.intervals) - now2), decision]);
})));
var mapBoth = dual(2, (self, {
  onInput,
  onOutput
}) => map6(mapInput(self, onInput), onOutput));
var mapBothEffect = dual(2, (self, {
  onInput,
  onOutput
}) => mapEffect(mapInputEffect(self, onInput), onOutput));
var driver = (self) => pipe(make3([none(), self.initial]), map4((ref) => new ScheduleDriverImpl(self, ref)));
var duration = (durationInput) => {
  const duration2 = decode(durationInput);
  const durationMillis = toMillis(duration2);
  return makeWithState(true, (now2, _, state) => succeed(state ? [false, duration2, continueWith2(after2(now2 + durationMillis))] : [false, zero, done2]));
};
var either = dual(2, (self, that) => union5(self, that));
var eitherWith = dual(3, (self, that, f) => unionWith(self, that, f));
var ensuring = dual(2, (self, finalizer) => makeWithState(self.initial, (now2, input, state) => flatMap2(self.step(now2, input, state), ([state2, out, decision]) => isDone2(decision) ? as2(finalizer, [state2, out, decision]) : succeed([state2, out, decision]))));
var exponential = (baseInput, factor = 2) => {
  const base = decode(baseInput);
  return delayedSchedule(map6(forever, (i) => times(base, Math.pow(factor, i))));
};
var fibonacci = (oneInput) => {
  const one = decode(oneInput);
  return delayedSchedule(pipe(unfold([one, one], ([a, b]) => [b, sum(a, b)]), map6((out) => out[0])));
};
var fixed3 = (intervalInput) => {
  const interval = decode(intervalInput);
  const intervalMillis = toMillis(interval);
  return makeWithState([none(), 0], (now2, _, [option, n]) => sync(() => {
    switch (option._tag) {
      case "None": {
        return [[some([now2, now2 + intervalMillis]), n + 1], n, continueWith2(after2(now2 + intervalMillis))];
      }
      case "Some": {
        const [startMillis, lastRun] = option.value;
        const runningBehind = now2 > lastRun + intervalMillis;
        const boundary = equals(interval, zero) ? interval : millis(intervalMillis - (now2 - startMillis) % intervalMillis);
        const sleepTime = equals(boundary, zero) ? interval : boundary;
        const nextRun = runningBehind ? now2 : now2 + toMillis(sleepTime);
        return [[some([startMillis, nextRun]), n + 1], n, continueWith2(after2(nextRun))];
      }
    }
  }));
};
var fromDelay = (delay) => duration(delay);
var fromDelays = (delay, ...delays2) => makeWithState([[delay, ...delays2].map((_) => decode(_)), true], (now2, _, [durations, cont]) => sync(() => {
  if (cont) {
    const x = durations[0];
    const interval = after2(now2 + toMillis(x));
    if (durations.length >= 2) {
      return [[durations.slice(1), true], x, continueWith2(interval)];
    }
    const y = durations.slice(1);
    return [[[x, ...y], false], x, continueWith2(interval)];
  }
  return [[durations, false], zero, done2];
}));
var fromFunction = (f) => map6(identity2(), f);
var hourOfDay = (hour) => makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
  if (!Number.isInteger(hour) || hour < 0 || 23 < hour) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: hourOfDay(${hour}). Must be in range 0...23`));
  }
  const n = state[1];
  const initial = n === 0;
  const hour0 = nextHour(now2, hour, initial);
  const start3 = beginningOfHour(hour0);
  const end3 = endOfHour(hour0);
  const interval = make6(start3, end3);
  return succeed([[end3, n + 1], n, continueWith2(interval)]);
});
var identity2 = () => makeWithState(void 0, (now2, input, state) => succeed([state, input, continueWith2(after2(now2))]));
var intersect5 = dual(2, (self, that) => intersectWith(self, that, intersect4));
var intersectWith = dual(3, (self, that, f) => makeWithState([self.initial, that.initial], (now2, input, state) => pipe(zipWith(self.step(now2, input, state[0]), that.step(now2, input, state[1]), (a, b) => [a, b]), flatMap2(([[lState, out, lDecision], [rState, out2, rDecision]]) => {
  if (isContinue2(lDecision) && isContinue2(rDecision)) {
    return intersectWithLoop(self, that, input, lState, out, lDecision.intervals, rState, out2, rDecision.intervals, f);
  }
  return succeed([[lState, rState], [out, out2], done2]);
}))));
var intersectWithLoop = (self, that, input, lState, out, lInterval, rState, out2, rInterval, f) => {
  const combined = f(lInterval, rInterval);
  if (isNonEmpty5(combined)) {
    return succeed([[lState, rState], [out, out2], _continue2(combined)]);
  }
  if (pipe(lInterval, lessThan6(rInterval))) {
    return flatMap2(self.step(end2(lInterval), input, lState), ([lState2, out3, decision]) => {
      if (isDone2(decision)) {
        return succeed([[lState2, rState], [out3, out2], done2]);
      }
      return intersectWithLoop(self, that, input, lState2, out3, decision.intervals, rState, out2, rInterval, f);
    });
  }
  return flatMap2(that.step(end2(rInterval), input, rState), ([rState2, out22, decision]) => {
    if (isDone2(decision)) {
      return succeed([[lState, rState2], [out, out22], done2]);
    }
    return intersectWithLoop(self, that, input, lState, out, lInterval, rState2, out22, decision.intervals, f);
  });
};
var jittered = (self) => jitteredWith(self, {
  min: 0.8,
  max: 1.2
});
var jitteredWith = dual(2, (self, options) => {
  const {
    max: max7,
    min: min5
  } = Object.assign({
    min: 0.8,
    max: 1.2
  }, options);
  return delayedEffect(self, (duration2) => map4(next3, (random) => {
    const d = toMillis(duration2);
    const jittered2 = d * min5 * (1 - random) + d * max7 * random;
    return millis(jittered2);
  }));
});
var linear = (baseInput) => {
  const base = decode(baseInput);
  return delayedSchedule(map6(forever, (i) => times(base, i + 1)));
};
var map6 = dual(2, (self, f) => mapEffect(self, (out) => sync(() => f(out))));
var mapEffect = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap2(self.step(now2, input, state), ([state2, out, decision]) => map4(f(out), (out2) => [state2, out2, decision]))));
var minuteOfHour = (minute) => makeWithState([Number.MIN_SAFE_INTEGER, 0], (now2, _, state) => {
  if (!Number.isInteger(minute) || minute < 0 || 59 < minute) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: minuteOfHour(${minute}). Must be in range 0...59`));
  }
  const n = state[1];
  const initial = n === 0;
  const minute0 = nextMinute(now2, minute, initial);
  const start3 = beginningOfMinute(minute0);
  const end3 = endOfMinute(minute0);
  const interval = make6(start3, end3);
  return succeed([[end3, n + 1], n, continueWith2(interval)]);
});
var modifyDelay = dual(2, (self, f) => modifyDelayEffect(self, (out, duration2) => sync(() => f(out, duration2))));
var modifyDelayEffect = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap2(self.step(now2, input, state), ([state2, out, decision]) => {
  if (isDone2(decision)) {
    return succeed([state2, out, decision]);
  }
  const intervals = decision.intervals;
  const delay = size4(make6(now2, start2(intervals)));
  return map4(f(out, delay), (durationInput) => {
    const duration2 = decode(durationInput);
    const oldStart = start2(intervals);
    const newStart = now2 + toMillis(duration2);
    const delta = newStart - oldStart;
    const newEnd = Math.max(0, end2(intervals) + delta);
    const newInterval = make6(newStart, newEnd);
    return [state2, out, continueWith2(newInterval)];
  });
})));
var onDecision = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap2(self.step(now2, input, state), ([state2, out, decision]) => as2(f(out, decision), [state2, out, decision]))));
var passthrough = (self) => makeWithState(self.initial, (now2, input, state) => pipe(self.step(now2, input, state), map4(([state2, _, decision]) => [state2, input, decision])));
var provideContext2 = dual(2, (self, context) => makeWithState(self.initial, (now2, input, state) => provideContext(self.step(now2, input, state), context)));
var provideService = dual(3, (self, tag, service) => makeWithState(self.initial, (now2, input, state) => contextWithEffect((env) => provideContext(
  // @ts-expect-error
  self.step(now2, input, state),
  add(env, tag, service)
))));
var recurUntil = (f) => untilInput(identity2(), f);
var recurUntilEffect = (f) => untilInputEffect(identity2(), f);
var recurUntilOption = (pf) => untilOutput(map6(identity2(), pf), isSome);
var recurUpTo = (durationInput) => {
  const duration2 = decode(durationInput);
  return whileOutput(elapsed, (elapsed2) => lessThan2(elapsed2, duration2));
};
var recurWhile = (f) => whileInput(identity2(), f);
var recurWhileEffect = (f) => whileInputEffect(identity2(), f);
var recurs = (n) => whileOutput(forever, (out) => out < n);
var reduce3 = dual(3, (self, zero2, f) => reduceEffect(self, zero2, (z, out) => sync(() => f(z, out))));
var reduceEffect = dual(3, (self, zero2, f) => makeWithState([self.initial, zero2], (now2, input, [s, z]) => flatMap2(self.step(now2, input, s), ([s2, out, decision]) => isDone2(decision) ? succeed([[s2, z], z, decision]) : map4(f(z, out), (z2) => [[s2, z2], z, decision]))));
var repetitions = (self) => reduce3(self, 0, (n, _) => n + 1);
var resetAfter = dual(2, (self, durationInput) => {
  const duration2 = decode(durationInput);
  return pipe(self, intersect5(elapsed), resetWhen(([, time]) => greaterThanOrEqualTo2(time, duration2)), map6((out) => out[0]));
});
var resetWhen = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap2(self.step(now2, input, state), ([state2, out, decision]) => f(out) ? self.step(now2, input, self.initial) : succeed([state2, out, decision]))));
var run = dual(3, (self, now2, input) => pipe(runLoop(self, now2, fromIterable2(input), self.initial, empty()), map4((list) => reverse(list))));
var runLoop = (self, now2, inputs, state, acc) => {
  if (!isNonEmpty(inputs)) {
    return succeed(acc);
  }
  const input = headNonEmpty(inputs);
  const nextInputs = tailNonEmpty(inputs);
  return flatMap2(self.step(now2, input, state), ([state2, out, decision]) => {
    if (isDone2(decision)) {
      return sync(() => pipe(acc, prepend(out)));
    }
    return runLoop(self, start2(decision.intervals), nextInputs, state2, prepend(acc, out));
  });
};
var secondOfMinute = (second) => makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
  if (!Number.isInteger(second) || second < 0 || 59 < second) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: secondOfMinute(${second}). Must be in range 0...59`));
  }
  const n = state[1];
  const initial = n === 0;
  const second0 = nextSecond(now2, second, initial);
  const start3 = beginningOfSecond(second0);
  const end3 = endOfSecond(second0);
  const interval = make6(start3, end3);
  return succeed([[end3, n + 1], n, continueWith2(interval)]);
});
var spaced = (duration2) => addDelay(forever, () => duration2);
var succeed2 = (value) => map6(forever, () => value);
var sync2 = (evaluate) => map6(forever, evaluate);
var tapInput = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => zipRight(f(input), self.step(now2, input, state))));
var tapOutput = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => tap(self.step(now2, input, state), ([, out]) => f(out))));
var unfold = (initial, f) => makeWithState(initial, (now2, _, state) => sync(() => [f(state), state, continueWith2(after2(now2))]));
var union5 = dual(2, (self, that) => unionWith(self, that, union4));
var unionWith = dual(3, (self, that, f) => makeWithState([self.initial, that.initial], (now2, input, state) => zipWith(self.step(now2, input, state[0]), that.step(now2, input, state[1]), ([lState, l, lDecision], [rState, r, rDecision]) => {
  if (isDone2(lDecision) && isDone2(rDecision)) {
    return [[lState, rState], [l, r], done2];
  }
  if (isDone2(lDecision) && isContinue2(rDecision)) {
    return [[lState, rState], [l, r], _continue2(rDecision.intervals)];
  }
  if (isContinue2(lDecision) && isDone2(rDecision)) {
    return [[lState, rState], [l, r], _continue2(lDecision.intervals)];
  }
  if (isContinue2(lDecision) && isContinue2(rDecision)) {
    const combined = f(lDecision.intervals, rDecision.intervals);
    return [[lState, rState], [l, r], _continue2(combined)];
  }
  throw new Error("BUG: Schedule.unionWith - please report an issue at https://github.com/Effect-TS/effect/issues");
})));
var untilInput = dual(2, (self, f) => check(self, (input, _) => !f(input)));
var untilInputEffect = dual(2, (self, f) => checkEffect(self, (input, _) => negate(f(input))));
var untilOutput = dual(2, (self, f) => check(self, (_, out) => !f(out)));
var untilOutputEffect = dual(2, (self, f) => checkEffect(self, (_, out) => negate(f(out))));
var upTo = dual(2, (self, duration2) => zipLeft2(self, recurUpTo(duration2)));
var whileInput = dual(2, (self, f) => check(self, (input, _) => f(input)));
var whileInputEffect = dual(2, (self, f) => checkEffect(self, (input, _) => f(input)));
var whileOutput = dual(2, (self, f) => check(self, (_, out) => f(out)));
var whileOutputEffect = dual(2, (self, f) => checkEffect(self, (_, out) => f(out)));
var windowed = (intervalInput) => {
  const interval = decode(intervalInput);
  const millis2 = toMillis(interval);
  return makeWithState([none(), 0], (now2, _, [option, n]) => {
    switch (option._tag) {
      case "None": {
        return succeed([[some(now2), n + 1], n, continueWith2(after2(now2 + millis2))]);
      }
      case "Some": {
        return succeed([[some(option.value), n + 1], n, continueWith2(after2(now2 + (millis2 - (now2 - option.value) % millis2)))]);
      }
    }
  });
};
var zipLeft2 = dual(2, (self, that) => map6(intersect5(self, that), (out) => out[0]));
var zipRight2 = dual(2, (self, that) => map6(intersect5(self, that), (out) => out[1]));
var zipWith2 = dual(3, (self, that, f) => map6(intersect5(self, that), ([out, out2]) => f(out, out2)));
var beginningOfSecond = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), date.getSeconds(), 0).getTime();
};
var endOfSecond = (now2) => {
  const date = new Date(beginningOfSecond(now2));
  return date.setSeconds(date.getSeconds() + 1);
};
var nextSecond = (now2, second, initial) => {
  const date = new Date(now2);
  if (date.getSeconds() === second && initial) {
    return now2;
  }
  if (date.getSeconds() < second) {
    return date.setSeconds(second);
  }
  const newDate = new Date(date.setSeconds(second));
  return newDate.setTime(newDate.getTime() + 1e3 * 60);
};
var beginningOfMinute = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), 0, 0).getTime();
};
var endOfMinute = (now2) => {
  const date = new Date(beginningOfMinute(now2));
  return date.setMinutes(date.getMinutes() + 1);
};
var nextMinute = (now2, minute, initial) => {
  const date = new Date(now2);
  if (date.getMinutes() === minute && initial) {
    return now2;
  }
  if (date.getMinutes() < minute) {
    return date.setMinutes(minute);
  }
  const newDate = new Date(date.setMinutes(minute));
  return newDate.setTime(newDate.getTime() + 1e3 * 60 * 60);
};
var beginningOfHour = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), 0, 0, 0).getTime();
};
var endOfHour = (now2) => {
  const date = new Date(beginningOfHour(now2));
  return date.setHours(date.getHours() + 1);
};
var nextHour = (now2, hour, initial) => {
  const date = new Date(now2);
  if (date.getHours() === hour && initial) {
    return now2;
  }
  if (date.getHours() < hour) {
    return date.setHours(hour);
  }
  const newDate = new Date(date.setHours(hour));
  return newDate.setTime(newDate.getTime() + 1e3 * 60 * 60 * 24);
};
var beginningOfDay = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), 0, 0, 0, 0).getTime();
};
var endOfDay = (now2) => {
  const date = new Date(beginningOfDay(now2));
  return date.setDate(date.getDate() + 1);
};
var nextDay = (now2, dayOfWeek2, initial) => {
  const date = new Date(now2);
  if (date.getDay() === dayOfWeek2 && initial) {
    return now2;
  }
  const nextDayOfWeek = (7 + dayOfWeek2 - date.getDay()) % 7;
  return date.setDate(date.getDate() + (nextDayOfWeek === 0 ? 7 : nextDayOfWeek));
};
var nextDayOfMonth = (now2, day, initial) => {
  const date = new Date(now2);
  if (date.getDate() === day && initial) {
    return now2;
  }
  if (date.getDate() < day) {
    return date.setDate(day);
  }
  return findNextMonth(now2, day, 1);
};
var findNextMonth = (now2, day, months) => {
  const d = new Date(now2);
  const tmp1 = new Date(d.setDate(day));
  const tmp2 = new Date(tmp1.setMonth(tmp1.getMonth() + months));
  if (tmp2.getDate() === day) {
    const d2 = new Date(now2);
    const tmp3 = new Date(d2.setDate(day));
    return tmp3.setMonth(tmp3.getMonth() + months);
  }
  return findNextMonth(now2, day, months + 1);
};
var ScheduleDefectTypeId = /* @__PURE__ */ Symbol.for("effect/Schedule/ScheduleDefect");
var ScheduleDefect = class {
  error;
  [ScheduleDefectTypeId];
  constructor(error) {
    this.error = error;
    this[ScheduleDefectTypeId] = ScheduleDefectTypeId;
  }
};
var isScheduleDefect = (u) => hasProperty(u, ScheduleDefectTypeId);
var scheduleDefectWrap = (self) => catchAll(self, (e) => die2(new ScheduleDefect(e)));
var scheduleDefectRefailCause = (cause) => match(find(cause, (_) => isDieType(_) && isScheduleDefect(_.defect) ? some(_.defect) : none()), {
  onNone: () => cause,
  onSome: (error) => fail(error.error)
});
var scheduleDefectRefail = (effect) => catchAllCause(effect, (cause) => failCause(scheduleDefectRefailCause(cause)));
var repeat_Effect = dual(2, (self, schedule) => repeatOrElse_Effect(self, schedule, (e, _) => fail2(e)));
var repeat_combined = dual(2, (self, options) => {
  if (isSchedule(options)) {
    return repeat_Effect(self, options);
  }
  const base = options.schedule ?? passthrough(forever);
  const withWhile = options.while ? whileInputEffect(base, (a) => {
    const applied = options.while(a);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : base;
  const withUntil = options.until ? untilInputEffect(withWhile, (a) => {
    const applied = options.until(a);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : withWhile;
  const withTimes = options.times ? intersect5(withUntil, recurs(options.times)).pipe(map6((intersectionPair) => intersectionPair[0])) : withUntil;
  return scheduleDefectRefail(repeat_Effect(self, withTimes));
});
var repeatOrElse_Effect = dual(3, (self, schedule, orElse) => flatMap2(driver(schedule), (driver2) => matchEffect(self, {
  onFailure: (error) => orElse(error, none()),
  onSuccess: (value) => repeatOrElseEffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get(driver2.iterationMeta)), driver2, (error, option) => provideServiceEffect(orElse(error, option), CurrentIterationMetadata, get(driver2.iterationMeta)), value)
})));
var repeatOrElseEffectLoop = (self, driver2, orElse, value) => matchEffect(driver2.next(value), {
  onFailure: () => orDie(driver2.last),
  onSuccess: (b) => matchEffect(self, {
    onFailure: (error) => orElse(error, some(b)),
    onSuccess: (value2) => repeatOrElseEffectLoop(self, driver2, orElse, value2)
  })
});
var retry_Effect = dual(2, (self, policy) => retryOrElse_Effect(self, policy, (e, _) => fail2(e)));
var retry_combined = dual(2, (self, options) => {
  if (isSchedule(options)) {
    return retry_Effect(self, options);
  }
  return scheduleDefectRefail(retry_Effect(self, fromRetryOptions(options)));
});
var fromRetryOptions = (options) => {
  const base = options.schedule ?? forever;
  const withWhile = options.while ? whileInputEffect(base, (e) => {
    const applied = options.while(e);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : base;
  const withUntil = options.until ? untilInputEffect(withWhile, (e) => {
    const applied = options.until(e);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : withWhile;
  return options.times !== void 0 ? intersect5(withUntil, recurs(options.times)) : withUntil;
};
var retryOrElse_Effect = dual(3, (self, policy, orElse) => flatMap2(driver(policy), (driver2) => retryOrElse_EffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get(driver2.iterationMeta)), driver2, (e, out) => provideServiceEffect(orElse(e, out), CurrentIterationMetadata, get(driver2.iterationMeta)))));
var retryOrElse_EffectLoop = (self, driver2, orElse) => {
  return catchAll(self, (e) => matchEffect(driver2.next(e), {
    onFailure: () => pipe(driver2.last, orDie, flatMap2((out) => orElse(e, out))),
    onSuccess: () => retryOrElse_EffectLoop(self, driver2, orElse)
  }));
};
var schedule_Effect = dual(2, (self, schedule) => scheduleFrom_Effect(self, void 0, schedule));
var scheduleFrom_Effect = dual(3, (self, initial, schedule) => flatMap2(driver(schedule), (driver2) => scheduleFrom_EffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get(driver2.iterationMeta)), initial, driver2)));
var scheduleFrom_EffectLoop = (self, initial, driver2) => matchEffect(driver2.next(initial), {
  onFailure: () => orDie(driver2.last),
  onSuccess: () => flatMap2(self, (a) => scheduleFrom_EffectLoop(self, a, driver2))
});
var count = unfold(0, (n) => n + 1);
var elapsed = makeWithState(none(), (now2, _, state) => {
  switch (state._tag) {
    case "None": {
      return succeed([some(now2), zero, continueWith2(after2(now2))]);
    }
    case "Some": {
      return succeed([some(state.value), millis(now2 - state.value), continueWith2(after2(now2))]);
    }
  }
});
var forever = unfold(0, (n) => n + 1);
var once = asVoid(recurs(1));
var stop = asVoid(recurs(0));
var scheduleForked = dual(2, (self, schedule) => forkScoped(schedule_Effect(self, schedule)));

export {
  empty3 as empty,
  fail3 as fail,
  die3 as die,
  interrupt2 as interrupt,
  parallel2 as parallel,
  sequential2 as sequential,
  isCause2 as isCause,
  isFailType2 as isFailType,
  isDieType2 as isDieType,
  isInterrupted2 as isInterrupted,
  isInterruptedOnly2 as isInterruptedOnly,
  interruptors2 as interruptors,
  failureOrCause2 as failureOrCause,
  flipCauseOption2 as flipCauseOption,
  map5 as map,
  squash,
  reduceWithContext2 as reduceWithContext,
  IllegalArgumentException2 as IllegalArgumentException,
  NoSuchElementException2 as NoSuchElementException,
  RuntimeException2 as RuntimeException,
  isRuntimeException2 as isRuntimeException,
  pretty2 as pretty,
  Cause_exports,
  ScheduleInterval_exports,
  start2 as start,
  ScheduleIntervals_exports,
  isDone2 as isDone,
  ScheduleDecision_exports,
  struct2 as struct,
  array3 as array,
  Class,
  Error2 as Error,
  TaggedError,
  Data_exports,
  TypeId,
  TimeZoneTypeId,
  isDateTime,
  isTimeZone,
  isTimeZoneOffset,
  isTimeZoneNamed,
  isUtc,
  isZoned,
  Equivalence2 as Equivalence,
  Order3 as Order,
  clamp2 as clamp,
  unsafeFromDate,
  unsafeMake2 as unsafeMake,
  unsafeMakeZoned,
  makeZoned,
  make9 as make,
  makeZonedFromString,
  now,
  nowAsDate,
  unsafeNow,
  toUtc,
  setZone,
  setZoneOffset,
  zoneUnsafeMakeNamed,
  zoneMakeOffset,
  zoneMakeNamed,
  zoneMakeNamedEffect,
  zoneMakeLocal,
  zoneFromString,
  zoneToString,
  setZoneNamed,
  unsafeSetZoneNamed,
  distance,
  distanceDurationEither,
  distanceDuration,
  min4 as min,
  max6 as max,
  greaterThan2 as greaterThan,
  greaterThanOrEqualTo4 as greaterThanOrEqualTo,
  lessThan7 as lessThan,
  lessThanOrEqualTo2 as lessThanOrEqualTo,
  between2 as between,
  isFuture,
  unsafeIsFuture,
  isPast,
  unsafeIsPast,
  toDateUtc,
  toDate,
  zonedOffset,
  zonedOffsetIso,
  toEpochMillis,
  removeTime,
  toParts,
  toPartsUtc,
  getPartUtc,
  getPart,
  setParts,
  setPartsUtc,
  mutate,
  mutateUtc,
  mapEpochMillis,
  withDate,
  withDateUtc,
  match5 as match,
  addDuration,
  subtractDuration,
  add2 as add,
  subtract,
  startOf,
  endOf,
  nearest,
  format2 as format,
  formatLocal,
  formatUtc,
  formatIntl,
  formatIso,
  formatIsoDate,
  formatIsoDateUtc,
  formatIsoOffset,
  formatIsoZoned,
  capitalize,
  uncapitalize,
  isNonEmpty6 as isNonEmpty,
  String_exports,
  Cron_exports,
  fixed2 as fixed,
  Random_exports,
  ScheduleTypeId,
  isSchedule,
  ScheduleDriverTypeId,
  CurrentIterationMetadata,
  makeWithState,
  addDelay,
  addDelayEffect,
  andThen3 as andThen,
  andThenEither,
  as4 as as,
  asVoid,
  bothInOut,
  check,
  checkEffect,
  collectAllInputs,
  collectAllOutputs,
  collectUntil,
  collectUntilEffect,
  collectWhile,
  collectWhileEffect,
  compose,
  mapInput,
  mapInputContext2 as mapInputContext,
  mapInputEffect,
  cron,
  dayOfMonth,
  dayOfWeek,
  delayed,
  delayedEffect,
  delayedSchedule,
  delays,
  mapBoth,
  mapBothEffect,
  driver,
  duration,
  either,
  eitherWith,
  ensuring,
  exponential,
  fibonacci,
  fixed3 as fixed2,
  fromDelay,
  fromDelays,
  fromFunction,
  hourOfDay,
  identity2 as identity,
  intersect5 as intersect,
  intersectWith,
  jittered,
  jitteredWith,
  linear,
  map6 as map2,
  mapEffect,
  minuteOfHour,
  modifyDelay,
  modifyDelayEffect,
  onDecision,
  passthrough,
  provideContext2 as provideContext,
  provideService,
  recurUntil,
  recurUntilEffect,
  recurUntilOption,
  recurUpTo,
  recurWhile,
  recurWhileEffect,
  recurs,
  reduce3 as reduce,
  reduceEffect,
  repetitions,
  resetAfter,
  resetWhen,
  run,
  secondOfMinute,
  spaced,
  succeed2 as succeed,
  sync2 as sync,
  tapInput,
  tapOutput,
  unfold,
  union5 as union,
  unionWith,
  untilInput,
  untilInputEffect,
  untilOutput,
  untilOutputEffect,
  upTo,
  whileInput,
  whileInputEffect,
  whileOutput,
  whileOutputEffect,
  windowed,
  zipLeft2 as zipLeft,
  zipRight2 as zipRight,
  zipWith2 as zipWith,
  scheduleDefectRefailCause,
  scheduleDefectRefail,
  repeat_combined,
  repeatOrElse_Effect,
  retry_Effect,
  retry_combined,
  fromRetryOptions,
  retryOrElse_Effect,
  schedule_Effect,
  scheduleFrom_Effect,
  count,
  elapsed,
  forever,
  once,
  stop,
  scheduleForked
};
//# sourceMappingURL=chunk-7AVSUGOT.js.map
